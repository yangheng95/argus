# v0.0.9beta Branch And Version Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump to the `0.0.9beta` branch and preserve every existing modification. |
| Acceptance criteria | Create local and git-cc delivery branch `v0.0.9beta` from the fully synchronized `v0.0.8beta` head; synchronize every declared release-family target to canonical Semantic Versioning (SemVer) `0.0.9-beta` and Windows Microsoft Installer (MSI) projection `0.0.9.0`; pass version, focused synchronization, documentation, and normal push-hook checks; preserve all pre-existing tracked and untracked worktree changes outside the bump commit. |
| Hard constraints | Use `script/sync-version.ts` as the single version writer. Do not reset, stash away permanently, create a worktree, touch running OpenCorvus/Overlay processes, hand-edit generated version targets, add compatibility/fallback behavior, or stage unrelated user changes. Commit subjects start with `dsw-33987`; push only to the git-cc remote `myhexin`. |
| Sources read | Repository `AGENTS.md`; `RELEASE.md`; `script/sync-version.ts`; `specs/records/2026-07/2026-07-15-v0.0.4beta-workspace-version-bump.md`; `specs/records/2026-07/2026-07-17-v0.0.8beta-version-correction-and-macos-repackage.md`; current branch/remote/status/version grep evidence. |
| Whole-repository grep | The canonical `0.0.8-beta` release family occurs in root/package manifests, Overlay Tauri configuration and Cargo metadata, process-supervisor Cargo metadata, Bun/Cargo locks, and release documentation. `version:bump` owns those explicit targets and `version:check` verifies convergence. Historical `v0.0.8beta` records remain historical and must not be rewritten. Overlay injects the package version through `vite.config.ts`; every shared browser fixture calls `ensureOverlayDist`, whose freshness inputs previously omitted the package manifest, Tauri configuration, and imported build scripts. The isolated left-Dock browser fixture contains a literal example label rather than a version source. |
| Preservation boundary | Before branch creation, the worktree contained six tracked `packages/opencorvus`/July-record modifications plus untracked `packages/opencorvus/test/tool/ai-sdk-adapter.test.ts`. `git switch -c v0.0.9beta` preserved all seven paths. None belongs to the version bump and none may be staged. |
| Git baseline | `v0.0.8beta` and `myhexin/v0.0.8beta` were identical at `01e9a8eed5`; `myhexin/v0.0.9beta` did not exist. Local `v0.0.9beta` was created directly from that exact head. |

## Plan

1. Run `bun run version:bump 0.0.9beta` so the existing synchronizer writes every declared target.
2. Verify `bun run version:check`, the focused version-synchronizer tests, and documentation health.
3. Review the exact staged path set and confirm the seven pre-existing worktree paths remain unstaged/untracked.
4. Commit the synchronized release family and this record, fetch to prove the remote target is still absent, then push `v0.0.9beta` through normal git-cc hooks.
5. Fetch again and prove local/remote branch equality without altering the preserved worktree modifications.

## Verification Ledger

- `bun run version:bump 0.0.9beta` synchronized all ten release package manifests and Bun workspace entries, two native Cargo manifests/locks, the Tauri application version, and MSI projection to `0.0.9-beta` / `0.0.9.0`. `bun run version:check` reports no drift, and the five focused synchronizer tests pass.
- Documentation health passes 82/82 after staging the new record and both indexes. The current `RELEASE.md` example now names `0.0.9beta`; historical `v0.0.8beta` records remain unchanged.
- The first real Node/Playwright version-label run correctly exposed stale `v0.0.8beta` build output. Root cause was `ensureOverlayDist` omitting three direct Vite inputs from its freshness set. The shared tool now tracks `package.json`, `src-tauri/tauri.conf.json`, and the imported `script/` tree; the browser-runner contract test prevents regression.
- Without deleting generated output, the repaired tool triggered a real 2,495-module Vite build. Five browser-runner/version tests and the real sidebar browser scenario pass. Visual review of `.scratch/sidebar-version-tooltip-hover.png` confirms an unclipped `v0.0.9beta` label and visible author tooltip at 1280x720.
- Exact path review shows only declared release targets, `RELEASE.md`, the two spec indexes, this record, and the two browser-build-tool test files staged. The six pre-existing tracked modifications and untracked `packages/opencorvus/test/tool/ai-sdk-adapter.test.ts` remain outside the index.
- Commit `589f3eb0eb` created the synchronized release line and was pushed as new git-cc branch `myhexin/v0.0.9beta`. Normal hooks passed 10/10 package typechecks, the 31-file API route inventory, 274-operation generated documentation check, Overlay/panel internationalization, and zero-hit secret scanning. A post-push fetch proved local and remote heads identical. Concurrent spec-index and benchmark-record edits that appeared after the commit remain unstaged alongside the original preserved work.
