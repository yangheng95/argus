# Mirror Watch selector

Select `mirror-watch` only for the author's complete AInvest feature-decision delivery: current evidence for AInvest and named competitors, one stable F01-F10 recommendation set, the package's Denis/Oleg source-backed perspectives, an explicit authority-backed AInvest persona cohort, deterministic 3-2-1 aggregation, and the AInvest HyperText Markup Language report.

Do not select it for generic competitor research, source observation, web search, an isolated User Interface mockup, a single survey response, or research outside this complete protocol. Do not select it merely because the Task mentions finance, an expert, persona, dashboard, chart, TradingView, or another competitor. Prism owns its own internal source-observation capability; Mirror Watch is not a Prism stage or dependency.

## Expert Contract

Create the Task with `promptProfile` set to `mirror-watch` and a concrete reason before dispatching any Mirror Watch identity. The Task keeps that package revision for its full lifetime. Bind the sole `research-survey-report` workflow. Its graph is binding on node and dependency order but does not create persisted execution state.

The result must preserve one explicit AInvest product question and one stable ten-feature recommendation set across every survey. It must distinguish current primary research evidence, named expert perspective responses, authoritative persona responses, deterministic aggregation, and the rendered report. Every claimed cohort member must have a separate traceable response Artifact; missing responses, changed feature sets, inferred persona fields, or an aggregate that cannot be reproduced from declared inputs invalidate the result.

Do not accept delivery unless the report and its quantitative claims resolve to the exact declared cohort and input Artifacts, the 3-2-1 ranking is produced by the package aggregation tool, and every canonical output path is present and readable.

After selection, use the platform Task Artifact catalog for all cross-Agent evidence; package prose never defines a second Artifact transport.
