Keep frontend replica work tied to the target surface, the source-backed structure, the interaction states that must work, and rendered evidence.
Track progress by meaningful user-visible source component or region so one goal proves one accountable slice instead of a single page-level claim.

For a single requested source page, one Mission-owned engine task is valid: let that task's Architect decompose the page into source-backed component or region goals.
For a multi-page, page-family, or subpage replica mission, do not compress the page family into one workflow task. First dispatch one serial template/source-baseline task that creates or repairs the shared project scaffold, source evidence package, visual token system, reusable primitives, and implementation contract. Record its terminal task ID and artifact paths before dispatching dependent page work.
After the template/source-baseline task is terminal, dispatch each source page or subpage surface as its own task. Run those tasks in parallel only when each request cites the shared template artifact paths, owns disjoint files or output surfaces, and has no dependency on a sibling subpage task's decision, artifact, or implementation.
Keep dependent subpage tasks queued in frontier.md / handoff.md until the template prerequisite is terminal; do not start them with queue=false before the prerequisite evidence exists.
Each subpage task still uses the normal frontend-replica workflow and Architect component-per-goal decomposition; Mission task fan-out is page/subpage ownership, not a replacement for per-component goals inside a page task.

When a task artifact lists `subpage_research_tasks`, anchors, or uncovered source-page packets, treat them as Mission frontier items for future page/subpage tasks after the current task reaches a lifecycle decision. Do not ask the same workflow task to re-run `frontend_research` for those packets after it already has a valid task-scope research artifact.
Do not accept completion until source structure, target project reuse decisions, template/subpage task ownership, visual evidence, and unresolved scope defects are visible in the task record.
