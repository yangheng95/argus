# v0.0.11beta Branch And Version Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Merge `work-v0.0.10beta-yr-0719` and bump the result to `v0.0.11beta`. |
| Acceptance criteria | Deliver local and legacy remote branch `v0.0.11beta` from the latest `legacy-remote/v0.0.10beta`; prove the requested work-line tip is included; synchronize the release family to canonical Semantic Versioning (SemVer) `0.0.11-beta` and Windows Microsoft Installer (MSI) projection `0.0.11.0`; pass version, documentation, and normal push-hook checks. |
| Hard constraints | Use `script/sync-version.ts` as the single version writer. Do not create an empty merge when the requested tip is already an ancestor, reset, create a worktree, touch running OpenCorvus/Overlay processes, hand-edit generated version targets, add fallback behavior, or stage the pre-existing untracked `C:/` directory. Commit subjects start with `dsw-33987`; push only to legacy remote. |
| Sources read | Repository `AGENTS.md`; `script/sync-version.ts`; `specs/records/2026-07/2026-07-19-v0.0.10beta-branch-and-version-bump.md`; current branch, status, remote divergence, work-line log, ancestry, and whole-repository version grep evidence. |
| Whole-repository grep | Current `0.0.10-beta` release values occur in nine release-family package manifests, Bun workspace lock entries, two Cargo manifests and package lock entries, Tauri application/MSI configuration, the current release example, the live MCP App bridge handshake, and historical/current specs. The synchronizer owns release metadata; the MCP App handshake must consume the existing injected Overlay version rather than remain a second hard-coded source. Historical records and test literals are not release targets. |
| Merge evidence | After fast-forwarding, `legacy-remote/v0.0.10beta` is `61774ce0d`. Requested work-line tip `b44b18a7f` has zero commits outside that delivery line and is already its ancestor; therefore its complete content is present and an artificial empty merge would add no source change. |
| Preservation boundary | No tracked worktree changes exist. The untracked `C:/` directory predates this task and remains outside staging. |
| Independent agent feedback | None requested; this branch/version operation is serial and bounded. |

## Plan

1. Commit and push this Recall/plan on the newly created `v0.0.11beta` branch before implementation.
2. Re-fetch both remote lines, verify the requested work tip remains an ancestor, and record an already-up-to-date merge result rather than inventing an empty merge commit.
3. Run `bun run version:bump 0.0.11beta`, update the current release example, replace the MCP App handshake's hard-coded release with the existing Overlay version source, and leave historical records and test literals unchanged.
4. Run version checks, focused synchronizer tests, documentation health, and the real browser version-label scenario; inspect the screenshot and complete diff.
5. Commit with the required prefix, fetch to detect remote movement, push through normal hooks, fetch again, and prove local/remote equality.

## Verification Ledger

- `v0.0.10beta` was fast-forwarded to legacy remote commit `61774ce0d`, and `v0.0.11beta` was cut from that exact head. Plan commit `de84bed3d` was pushed before implementation.
- A fresh fetch followed by `git merge --ff-only legacy-remote/work-v0.0.10beta-yr-0719` returned `Already up to date`; ancestry verification proves work tip `b44b18a7f` is contained in the target. No empty merge commit was manufactured.
- `bun run version:bump 0.0.11beta` synchronized all declared release targets to `0.0.11-beta` and the MSI projection to `0.0.11.0`; `RELEASE.md` now shows the current compact bump command.
- Whole-repository review found the recently added MCP App bridge had hard-coded the prior release in production. It now consumes the Vite-injected `OVERLAY_VERSION` already used by the rest of the Overlay, and a focused source test rejects future literal bridge versions.
- Version, synchronizer, historical-link, document-health, and product-document verification passed with 92 tests and zero failures. The Node-launched sidebar version scenario passed after a real 2,647-module Vite build.
- Visual review of `.scratch/sidebar-version-tooltip-hover.png` confirms the rendered `v0.0.11beta` label, author Tooltip, and intact desktop layout.
