# work-lcx-v0.0.30beta Branch Convergence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Merge the complete `work-lcx-v0.0.30beta` source history into the remote `v0.0.30beta` branch through the GitLab Merge Request (MR) workflow without discarding either branch's valid changes.

**Architecture:** Treat `myhexin/v0.0.30beta` as the target authority and converge it into the current source branch before opening the MR. Resolve every observed three-way conflict from branch evidence, regenerate derived translation metadata through the repository toolchain, verify the integrated tree, then let GitLab create the target merge commit.

**Tech Stack:** Git, GitLab REST API (Application Programming Interface), Bun, TypeScript, JSON (JavaScript Object Notation), Markdown.

## Global Constraints

- Use `myhexin` (`git-cc.myhexin.com`) as the only delivery remote; an `origin` push is not delivery.
- Do not directly push `HEAD` over `v0.0.30beta`; use a GitLab MR and retain the source branch.
- Every new commit subject starts with `dsw-33987`.
- Do not bypass commit or push hooks.
- Do not create a worktree and do not discard existing branch history.
- Preserve and restore the two exact Git stashes containing concurrent public-Web and public-documentation work; neither stash belongs in the branch merge commit.
- Do not add, update, or run User Interface (UI) automated tests.
- Preserve both branches' valid product changes; resolve conflicts from diffs and generation contracts, not by choosing one entire side.

---

## Recall

### Original user request

Merge the current branch into the remote `v0.0.30beta` branch before continuing the client-package work.

### Acceptance criteria

- The remote source branch contains the complete local source history.
- A GitLab MR targets `v0.0.30beta` from `work-lcx-v0.0.30beta`.
- The five current conflicts are resolved without losing valid source or target changes.
- Repository checks and remote push hooks pass without bypass.
- After GitLab merges the MR, `myhexin/v0.0.30beta` contains committed source tip `6fb82f1bc9` as an ancestor.

### Hard constraints

- The repository's root `AGENTS.md` rules supplied by the user govern this task.
- Remote merges use the GitLab MR workflow from the `git-workflow` and `gitlab` skills.
- No new worktree is authorized.
- The running OpenCorvus client must not be stopped without explicit authorization.
- All specification records live under root `specs/` and must contain this `Recall` section.

### Sources read

- Root `AGENTS.md` instructions supplied in the current request.
- `git-workflow/SKILL.md` and `git-workflow/references/branch-management.md`.
- `gitlab/SKILL.md`, `gitlab/references/gitlab-mr.md`, and the GitLab MR client implementation.
- `specs/README.md` and `specs/records/2026-08/README.md`.
- Both branches' diffs for `packages/overlay/src/i18n/en-US.json` and `packages/overlay/src/i18n/zh-CN.json`.

### Whole-repository search and Git evidence

- `git status --short --branch`: the current source worktree was clean before this plan.
- `git rev-list --left-right --count HEAD...myhexin/work-lcx-v0.0.30beta`: `0 0`; source was already pushed exactly.
- `git rev-list --left-right --count HEAD...myhexin/v0.0.30beta`: `38 36`; the branches have genuine two-way divergence.
- `git merge-tree --write-tree --name-only HEAD myhexin/v0.0.30beta`: conflicts occur only in both translation catalogs and both specification indexes.
- Translation diff evidence shows the source changed derived `panel_revision`, while the target changed browser-preview strings and removed the retired `right_dock.opened` entry; the integrated catalog must preserve target entries plus all source catalog content and then regenerate the revision.
- Integrated backend validation reproduced two closing-instance lease errors in `session-mirror.test.ts`. Root tracing showed the target's sequential Bus dispatch moved `currentProjectDirectory()` evaluation past asynchronous subscriber completion; the repair must capture the directory at publish invocation while preserving exact, wildcard, then global publication order.
- The inspected Bus test path contained pre-existing negative tests centered on absent calls, empty collections, and error non-occurrence; rule 28.1 requires their deletion while retaining explicit positive output and typed-error contracts.
- After the first convergence, `myhexin/v0.0.30beta` advanced to `c60a45070b` with 36 additional target-side commits. A fresh remote merge-tree check found five conflicts: both translation catalogs, `packages/overlay/src/index.html`, and both specification indexes.
- Two concurrent uncommitted work sets were preserved separately as `codex-preserve-public-web-before-v0.0.30beta-merge` and `codex-preserve-public-docs-before-v0.0.30beta-merge`; both must be restored after the source sync and GitLab merge.
- The target-side Kobalte patch used a remove-and-replace hunk for `dist/chunk/2CTBMVJ4.jsx`; Bun 1.3.13 on Windows applied that specific hunk by retaining the removed line and adding both replacement lines, producing a duplicate `elementResize` property and a Vite syntax error. Re-expressing the same contract as a single `animationFrame` property insertion before the unchanged `elementResize` property produced the intended runtime file after a frozen install, and the production Vite build then transformed all 7,073 modules successfully.

### Independent agent feedback

No independent agent was requested or dispatched. The main agent owns conflict analysis, integration, verification, and remote MR completion.

## File map

- `packages/overlay/src/i18n/en-US.json`: preserve the complete integrated English catalog and generated revision.
- `packages/overlay/src/i18n/zh-CN.json`: preserve the matching integrated Simplified Chinese catalog and generated revision.
- `packages/overlay/src/index.html`: preserve the target's current native shell and source-side valid document content through evidence-led conflict resolution.
- `packages/opencorvus/src/bus/index.ts`: preserve sequential subscriber provenance while capturing Project identity inside the active instance lease.
- `packages/opencorvus/test/bus/bus.test.ts`: retain positive Bus results and typed validation errors while deleting prohibited negative tests discovered during root-cause analysis.
- `specs/README.md`: retain both branches' current links and index this convergence plan.
- `specs/records/2026-08/README.md`: retain both branches' August records and index this convergence plan.
- `specs/records/2026-08/2026-08-04-work-lcx-v0.0.30beta-branch-convergence-plan.md`: durable task, evidence, and verification record.

### Task 1: Record and publish the convergence contract

**Files:**
- Create: `specs/records/2026-08/2026-08-04-work-lcx-v0.0.30beta-branch-convergence-plan.md`
- Modify: `specs/README.md`
- Modify: `specs/records/2026-08/README.md`

**Interfaces:**
- Consumes: the user request, branch divergence counts, and `git merge-tree` conflict evidence.
- Produces: the durable conflict-resolution and acceptance contract used by the remaining tasks.

- [ ] **Step 1:** Add this plan and both required index links.
- [ ] **Step 2:** Run `bun test packages/opencorvus/test/script/historical-docs-links.test.ts`.
- [ ] **Step 3:** Run `bun test packages/opencorvus/test/script/document-health.test.ts`.
- [ ] **Step 4:** Run `bun test packages/opencorvus/test/script/product-docs-single-source.test.ts`.
- [ ] **Step 5:** Commit with `git commit -m "dsw-33987 plan v0.0.30beta branch convergence"` and push the current branch to `myhexin` without bypassing hooks.

### Task 2: Converge the target branch into the source branch

**Files:**
- Modify: `packages/overlay/src/i18n/en-US.json`
- Modify: `packages/overlay/src/i18n/zh-CN.json`
- Modify: `packages/opencorvus/src/bus/index.ts`
- Modify: `packages/opencorvus/test/bus/bus.test.ts`
- Modify: `specs/README.md`
- Modify: `specs/records/2026-08/README.md`

**Interfaces:**
- Consumes: the published convergence contract and `myhexin/v0.0.30beta`.
- Produces: one source-branch merge commit whose tree contains the valid histories and content of both branches.

- [ ] **Step 1:** Fetch `myhexin` and merge `myhexin/v0.0.30beta` into the current source branch with an explicit merge commit.
- [ ] **Step 2:** Resolve both specification indexes by retaining the union of valid existing links, with one entry per record.
- [ ] **Step 3:** Resolve both translation catalogs by preserving the integrated key/value catalog, then run the repository's canonical translation revision generator or checker to establish the sole valid `panel_revision`.
- [ ] **Step 4:** Run the changed backend and protocol tests; reproduce any failure in isolation and repair only an evidence-proven integration regression.
- [ ] **Step 5:** Delete prohibited negative tests found in any inspected test path while retaining explicit positive outputs and typed-error contracts.
- [ ] **Step 6:** Inspect `git diff --check`, conflict-marker search results, the staged merge diff, and both parent comparisons before committing.
- [ ] **Step 7:** Commit the completed merge with a `dsw-33987` subject.

### Task 3: Verify, push, and merge through GitLab

**Files:**
- Verify only: the complete merged repository tree.

**Interfaces:**
- Consumes: the conflict-free source merge commit.
- Produces: a merged GitLab MR and a verified remote target ancestry relationship.

- [ ] **Step 1:** Run targeted non-UI translation, documentation, type, route, and build checks discovered from the repository scripts; do not run UI automated tests.
- [ ] **Step 2:** Push `work-lcx-v0.0.30beta` to `myhexin` normally so the configured pre-push hooks run.
- [ ] **Step 3:** Create a GitLab MR from `work-lcx-v0.0.30beta` to `v0.0.30beta`, retain the source branch, and request merge without bypassing approvals or pipelines.
- [ ] **Step 4:** Fetch `myhexin` after the server merge and run `git merge-base --is-ancestor c318b467e7 myhexin/v0.0.30beta`.
- [ ] **Step 5:** Record the final MR URL, target tip, and verification results in the delivery response.
