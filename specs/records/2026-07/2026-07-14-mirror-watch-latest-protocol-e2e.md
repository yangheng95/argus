# Mirror Watch latest-protocol integration and real E2E

Status: Static package/protocol corrections retained; the remaining real E2E was cancelled on 2026-07-17.

## Recall

### User requirements

- Set and pursue a durable goal: integrate `C:\Users\chuan\myhexin-local\mirror-watch` into OpenCorvus with the latest expert-squad protocol and complete end-to-end acceptance.
- Work autonomously; do not wait for another agent to implement the migration.
- Own the focused Mirror Watch package test. Do not rewrite another agent's tests or shared benchmark harness.
- Do not build or restore a complex benchmark controller. Send the real request directly to the existing main OpenCorvus backend and main database with an explicit Task model, let the execution reach a real terminal failure or completion, inspect the persisted message/tool evidence, fix the root cause, and repeat.
- Delete the old-protocol Mirror Watch expert-squad configuration before implementing the latest package.
- Respect the latest package-runtime dependency manifest design, but remove the `v2` suffix: keep the single canonical `expert-squad.jsonc` / `schema_version: 1` contract and do not create a v2 identity, filename, parser, or compatibility path.
- Mirror Watch search must use the source `C:\\Users\\chuan\\myhexin-local\\mirror-watch\\skills\\ifind-search` capability. Generic WebSearch is not the Mirror Watch search implementation.
- Parallel agents are present. Preserve unrelated changes; if an actual overlapping edit or contradictory ownership is found, stop and report it.
- Publish the real work as one direct OpenCorvus Task, then leave its autonomous planner and dynamic agents undisturbed for a full five-minute observation window. Diagnose only after the Task reaches terminal failure; do not steer its internal scheduling.
- Use an independent read-only agent to review implementation completeness, protocol drift, test evidence, and source-algorithm parity before delivery.
- The final acceptance target is one-to-one restoration of the source algorithm's declared outputs through the external expert-squad implementation, not merely successful package loading.
- Restore every dynamic-agent template to its complete normal tool/capability surface. Do not remove or block WebSearch in the manifest, resolver projection, role template, package README, or package skill. The prohibition on using WebSearch belongs only to the concrete Mirror Watch Task instruction; iFind remains the required search implementation for that Task.
- Publish every subsequent real E2E Task to the existing main OpenCorvus database/backend. Do not create another isolated database for acceptance and do not restart, stop, or reload the user's running OpenCorvus process without separate authorization.

### Acceptance criteria

1. The only deliverable package is `.opencorvus/expert-squads/legacy-remote/mirror-watch`, and `ExpertSquadRegistry.loadSourcePackage` accepts it without compatibility or fallback behavior.
2. The package uses the current manifest, selector, scheduler projection, package skill/tool, and dynamic-agent prompt surfaces. Active and inactive resolver/catalog/tool/skill projections remain isolated.
3. The four domain identities are `mirror-watch-research-lead`, `mirror-watch-expert-surveyor`, `mirror-watch-persona-surveyor`, and `mirror-watch-report-analyst`; each identity is distinct from its shared `delegated-worker` runtime template.
4. One deterministic aggregation tool validates the exact declared persona cohort, exact F01-F10 feature contract, unique identities, three ranked picks, and 3-2-1 scores. Invalid or incomplete input fails explicitly; no record is normalized, truncated, skipped, inferred, or silently defaulted.
5. A dedicated focused package test covers loading, projection, contract rejection, exact aggregation, and forbidden legacy surfaces.
6. A directly invoked main backend whose health response names `C:\Users\chuan\.local\share\opencorvus\opencorvus.db` loads the real project package through `prompt_profile.active` and receives natural user requests through ordinary `POST /task`. No Mirror Watch controller, Mission wrapper, polling protocol, dispatch-order tracker, or process supervisor is part of the package. The operator diagnoses a real terminal failure from ordinary backend/task/session evidence and repeats manually only after fixing its root cause.
7. The four-person E2E cohort is described only as a real reduced acceptance sample. It must not be presented as the unavailable 105-person production survey or as proof of 105-session scale.
8. After automated acceptance, a separate read-only agent reviews the final package, tests, evidence, and diff.

### Hard constraints

- No fallback, compatibility alias, dual source, hidden/synthetic message, workflow state machine, gate, seed/demo substitution, mocked E2E claim, or headless visual acceptance.
- Playwright runs under Node, and timeout is measured from the last observable activity.
- Do not restart, refresh, or stop the user's running OpenCorvus/overlay. Real chain E2E uses the main backend and main database; any static visual-preview helper and browser state must remain isolated and test-owned.
- Do not create a worktree, use Git reset, overwrite unrelated dirty files, or bypass hooks.
- Source input remains read-only. Python bytecode and legacy shell/Python runners are not package assets.
- Commits use the `dsw-33987` prefix and are pushed to the `legacy-remote` remote only after acceptance.

### Disk records and source inspected before implementation

- `AGENTS.md`
- `specs/README.md`
- `specs/current/architecture/04-extensions.md`
- `specs/records/2026-07/README.md`
- `specs/records/2026-07/2026-07-13-expert-squad-integration-guide.md`
- `C:\Users\chuan\myhexin-local\mirror-watch\README.md`, `WORKFLOW.md`, `config.yaml`, all agent prompts, skill manifest, Python/shell runners, libraries, templates, and demo inventory
- Current `ExpertSquadRegistry`, resolver, package-tool bundler, package-manager tests, dynamic-agent package tests, MirrorTest package, and Goal Development package
- The current package protocol in `packages/opencorvus/src/expert-squad/protocol-schema.ts`: the unpublished project destructively updates manifest v1 in place, no longer accepts the retired root `runtime_dependencies` field, and has no v2 parser or suffix.
- The staged rollback package under `C:\Users\chuan\myhexin-local\opecorvus-v0.0.2beta-rollback\.opencorvus\expert-squads\mirror\mirror-watch`

### Full-repository and cross-worktree search

The pre-implementation inventory used `rg` over the current repository, the source project, and the rollback worktree for `mirror-watch`, `Mirror Watch`, `virtual_agents`, `virtual-agents`, `protocol-engine`, dynamic agent IDs, manifest fields, package refs, payload entries, tests, and spec indexes.

| Surface                             | Finding                                                                                                                                                                                                                                                                                                                | Decision                                                                                                                                                                                                        |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Current repository package          | No `legacy-remote/mirror-watch` or `mirror/mirror-watch` package exists.                                                                                                                                                                                                                                                     | Add one current package only.                                                                                                                                                                                   |
| Latest manifest root contract       | The single manifest v1 is strict and does not accept the retired root `runtime_dependencies` field. Package tools use package-local modules, the plugin ABI, reviewed Node core modules, and explicit projected refs.                                                                                                  | Remove `runtime_dependencies` destructively; do not add a v2 surface, compatibility parser, or alternate dependency declaration.                                                                                |
| Retired dedicated benchmark         | The earlier `packages/opencorvus/script/benchmark/mirror-watch-real-e2e.mjs` attempted to control and infer the whole run, then terminated a live execution after its own UI-ledger timeout.                                                                                                                           | Do not restore it. Publish through the existing main backend/main database and diagnose only real backend terminal evidence.                                                                                    |
| Current core/runtime/tests          | Registry, resolver, payload, package-manager, and many shared tests are dirty from parallel work.                                                                                                                                                                                                                      | Do not edit them. Add a dedicated package test instead.                                                                                                                                                         |
| Rollback package                    | Fifteen staged package files use `mirror/mirror-watch`, `virtual_agents`, `virtual-agents/`, `protocol-engine/`, unprefixed identities, and old workflow surfaces.                                                                                                                                                     | Delete all fifteen staged additions.                                                                                                                                                                            |
| Rollback dedicated records          | A staged Mirror Watch test and spec describe the rejected protocol.                                                                                                                                                                                                                                                    | Delete both staged additions.                                                                                                                                                                                   |
| Rollback shared files               | Payload, package-manager inventory, and July index contain narrow Mirror Watch entries mixed with unrelated changes.                                                                                                                                                                                                   | Remove only the Mirror Watch imports/block/entries/line; preserve every other hunk.                                                                                                                             |
| Rollback shared harness             | Three staged inactivity-timeout runner files are not Mirror Watch-owned.                                                                                                                                                                                                                                               | Preserve untouched.                                                                                                                                                                                             |
| Source workflow                     | Research, two expert perspectives, per-persona surveys, and persona-only 3-2-1 aggregation are the durable semantics.                                                                                                                                                                                                  | Express as prompts, a package skill, and one strict package tool.                                                                                                                                               |
| Source legacy runtime               | Seed copies, demo reuse, OpenCode/Claude alternatives, `agent_invoke`, fixed shell/Python dispatch, PyYAML mini-parser fallback, frequency inference, malformed-ID normalization, truncation, and invalid-record skipping are present.                                                                                 | Do not migrate any of them.                                                                                                                                                                                     |
| Production inputs and source skills | The source directory does not contain the claimed 105-person corpus, but `C:\Users\chuan\myhexin-local\mirror-watch\skills` does contain both complete seven-file expert skills and the research/design/browser utility skill stack. The earlier claim that the expert skills were absent was a bad search conclusion. | Keep the four-person E2E claim explicit, but replace the reconstructed perspective content with source-backed package skills and adapt every remaining source skill to the current OpenCorvus capability model. |

### Independent agent feedback

- Collision audit confirmed the current repository has no competing Mirror Watch package and identified the exact rollback-only deletion boundary. It also confirmed the source repository must remain untouched and the shared runner changes are unrelated.
- Protocol audit confirmed current allowed roots, strict manifest fields, selector requirement, resolver-owned projection, static `lib/**` reachability, and removal of all legacy package fields/roots. After comparison with `repository-dynamic-agent-packages.test.ts`, it corrected the worker identities to the required `mirror-watch-*` prefix.
- Source/E2E audit confirmed that a strict generic cohort contract can accept the four-person real smoke cohort without fallback: completeness is exact relative to the explicit cohort, while the test truthfully does not claim unavailable 105-person production coverage.

### 2026-07-15 authoritative correction after independent package rejection

This section supersedes every conflicting topology, search-call ordering, credential, and E2E claim later in this record. The latest user requirement is intentionally thin: submit one natural request to the existing main backend and main database through one `POST /task`, let the selected Mirror Watch scheduler and ordinary dispatch tools run, and diagnose the first real failure. No Mirror Watch benchmark controller, Mission-to-Task wrapper, polling protocol claim, worker-order controller, or internal workflow tracker is part of acceptance.

The independent package review rejected the current snapshot on these concrete call points:

| Call point                                          | Rejected behavior                                                                                                                                                         | Authoritative replacement                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agents/orchestrator/system.md`                     | Creates another Task with `panel.create_task` inside the already selected Task and addresses a second downstream Orchestrator.                                            | The selected Task's scheduler directly dispatches the current Task's dynamic agents. There is no nested Mission/Task layer.                                                                                                                                                                                                                                                                                                                     |
| Scheduler/research prompts and `ifind-search` skill | Prescribe five Skill calls, a sixth tool call, six fixed queries, exact order, and no inserted call.                                                                      | Preserve the evidence/result contract, but let the research agent choose a finite evidence plan with ordinary tool use. Fixed call position and query sequence are retired hidden workflow state.                                                                                                                                                                                                                                               |
| Manifest workflow/concurrency                       | Expert and persona nodes are task-scoped and all workers are `single`, preventing the same projected expert/persona identity from serving independent goals concurrently. | Research and report remain task-scoped with `single`; expert and persona nodes are goal-scoped and their dynamic identities declare `goal_concurrency: disjoint_goals`. Dependencies are immutable guidance only.                                                                                                                                                                                                                               |
| Segment aggregation                                 | Declared classifications with zero samples are removed from `order`, so the rendered report contradicts its own zero-value-matrix claim.                                  | Every declared classification remains in deterministic order with count zero, zero score cells, and no winner.                                                                                                                                                                                                                                                                                                                                  |
| `lib/mirror-watch/ifind-search-config.json`         | Stores a fixed endpoint, header, `user_id`, and `app_id` in the package. Prompts and source-backed skills also retain local absolute paths.                               | Delete the package credential/config file. Every iFind call requires one project-relative `config_path`; the tool reads one explicit project-owned JSON object containing endpoint, headers, and request policy, rejects missing/invalid/out-of-project config, and never exposes config contents to the model prompt. No default path, endpoint, identity, or credential exists. Remove every machine-specific absolute path from the package. |
| Selector                                            | Claims a broad generic research-to-survey-to-report product capability.                                                                                                   | Select only for the concrete AInvest financial-product feature research, source-backed expert/persona survey, 3-2-1 aggregation, and report delivery contract implemented by this package.                                                                                                                                                                                                                                                      |
| Focused package test                                | Pins the retired call order and nested Task wording and does not exercise real resolver/catalog/mount/tool isolation.                                                     | Delete those positive assertions; add negative residue checks plus active/inactive `PromptProfileResolver`, catalog, `SkillMount`, and package-tool projection tests.                                                                                                                                                                                                                                                                           |

The configuration contract is deliberately one explicit project input rather than environment lookup or prompt-visible secret fields. The research dispatch names the project-relative configuration path supplied by the user/task. The package tool receives only that path, resolves it inside `context.directory`, rejects symbolic-link/junction traversal, parses the exact JSON schema, and performs one request. This is data integrity, not a fallback or workflow gate.

The complete pre-edit inventory was rerun with `rg` across `.opencorvus/expert-squads/legacy-remote/mirror-watch/**`, the owned package test, `ExpertSquadRegistry`, `PromptProfileResolver`, `SkillMount`, catalog tests, package-tool bundling, and this record for `panel.create_task`, Mission/Task creation, fixed Skill/tool ordering, fixed queries, `virtual_workflows`, `goal_concurrency`, zero-sample segment ordering, endpoint/header/user/app credentials, machine paths, selector breadth, package tools, catalog, mounts, and active/inactive projection. Shared registry/resolver/payload files are parallel-owned and remain untouched.

### Source skill recall correction and complete call-point inventory

The user identified that the prior package had not adapted `C:\Users\chuan\myhexin-local\mirror-watch\skills`. A fresh disk inspection proved the previous protocol audit's “source skills absent” conclusion false. The exact source inventory and source call points are:

| Source skill                                                  | Source evidence and call points                                                                                                                                                                                                    | Latest-protocol decision                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `denis-globa-perspective`                                     | Seven files, 48,783 bytes. `agents/expert-surveyor.md` requires the main skill plus `references/research/01..06.md` for every Denis run.                                                                                           | Replace the fabricated reconstruction with a source-backed, immutable single-file package skill that embeds all seven files with logical file boundaries and SHA-256 provenance. Only the Denis session loads it.                                                                                           |
| `oleg-mukhanov-perspective`                                   | Seven files, 105,604 bytes. The same expert call point requires all seven files for every Oleg run.                                                                                                                                | Replace the fabricated reconstruction with the corresponding full source-backed single-file package skill. Only the Oleg session loads it.                                                                                                                                                                  |
| `mirror-design`                                               | `agents/expert-surveyor.md` and `agents/report-analyst.md` require it for expert survey HTML and the final report. The source has one main skill, six Markdown references, two HTML assets, two Python scripts, and `.DS_Store`.   | Adapt the source design semantics into one immutable package skill. Preserve the relevant main/reference rules and map browser verification to the current built-in Browser capability. Do not package Python scripts, demo HTML modules, or `.DS_Store`.                                                   |
| `ainvest-design-system`                                       | The same expert/report call points require it together with `mirror-design`. The source has one main skill, token/component/rule references, and binary/image assets.                                                              | Adapt the exact report/survey surface subset—closed-system rule, colors, typography, spacing, radius, shadow, cards, hierarchy, table/list, tabs, hover, accessibility—into one immutable package skill. Do not claim the unrelated 77-file component library is projected. Do not package unused binaries. |
| `consulting-analysis`                                         | `agents/research-lead.md` requires it first in live mode to build the comparison blueprint. It is a single 11,929-byte skill.                                                                                                      | Package the source framework semantics as a single skill and project it only to the research lead.                                                                                                                                                                                                          |
| `ifind-search`                                                | `agents/research-lead.md` calls this first for live search. The source contains `SKILL.md`, `references/advanced_usage.md`, and `scripts/ifind_search`. The user explicitly requires it as the Mirror Watch search implementation. | Adapt its channel/query contract into one immutable package skill plus one research-lead-only package tool. The package tool performs exactly one iFind request through one configured endpoint, validates the complete response, and exposes failure. It does not copy or execute the Python script.       |
| `search-anything`, `agent-reach`, `web-read`, `web-scrapling` | The source research lead describes these as alternate search/read paths; their implementations depend on private CLIs, shell/Python, proxy fallback, MCP, or undeclared runtimes.                                                  | Do not copy or project them. iFind is the only search source; current WebFetch/Browser may inspect an iFind-returned URL but may not replace search.                                                                                                                                                        |
| `playwright-cli`, `agent-browser`, `get-ui`, `get-ux`         | `agents/research-lead.md` uses them for live competitor inspection; `mirror-design` references Playwright verification. Their source implementations are external CLIs/scripts.                                                    | Adapt the interaction/screenshot/evidence contract into the same native research/browser skill and use OpenCorvus's projected Browser tools. No bundled browser CLI or second browser runtime.                                                                                                              |
| `s3-share`                                                    | Listed as optional live-mode upload only; no report/expert call point requires it and the current user requested project-local E2E delivery. It shells to an external uploader.                                                    | Do not project or package it. Local project artifacts and screenshot evidence are the sole delivery surface for this integration.                                                                                                                                                                           |
| `dense-financial-ui`                                          | Present on disk but absent from `skills/SKILLS.manifest.md` and absent from every source agent call point.                                                                                                                         | Do not import an undeclared skill. The declared AInvest design system remains the design authority.                                                                                                                                                                                                         |

### 2026-07-15 main-database E2E failure and source-parity correction

The single main-database Task `tsk_f61968425001Lft73S7hwPEHUE` first exposed a stale requested model (`hexin/gpt-5.6-sol`). The root session was updated through the official session-config route to the live-probed `hexin/gpt-5.5`, and the same Task ID was retried. A parallel edit to `packages/opencorvus/src/session/loop.ts` landed after that retry and caused the already-running Bun process to execute a torn intermediate `lifecycle.runOutside(...)()` form. The current disk version and its parallel-owned `session-standby-instance-lifecycle.test.ts` subsequently passed, so this integration does not edit or claim ownership of those files.

After restarting the task-owned hidden backend on the exact main database and retrying the same Task, the real research worker made twenty visible iFind calls. The Task then failed honestly because the package required every F01-F10 feature to carry an exact same-row HTTPS URL, at least 80 normalized characters of verbatim provider summary, and a conclusion that repeated the entire excerpt before adding another 40 characters. The persisted tool evidence showed that several iFind channel combinations returned empty rows and the `bing` rows were frequently irrelevant to the named product. No downstream artifacts were fabricated.

Fresh comparison against the source `agents/research-lead.md`, `WORKFLOW.md`, `templates/feature-recommendations.template.md`, and `templates/survey-agent.template.md` proves that the 80-character excerpt rule, 40-character conclusion rule, and terminal whole-run failure are migration inventions rather than source-algorithm invariants. The source live algorithm requires a real URL for a verified competitor fact, but explicitly requires `数据缺失 (N/A)` when a fact cannot be verified. Its canonical downstream survey carries only `时间`, `做什么`, and `差异化`; it does not replicate a bespoke evidence-chain schema into every F item.

The authoritative repair is therefore destructive and single-source: remove the invented evidence-length gate from the package prompts and skills; preserve exact iFind query/channel/URL evidence when a relevant row exists; require explicit `数据缺失 (N/A)` when iFind does not verify the fact; forbid generated URLs, generated excerpts, another provider, cached/demo inputs, and silent omission. This N/A value is a visible research result, not a fallback. The iFind package tool remains strict and unchanged because its exact provider `data[]` projection correctly exposed the observed empty and irrelevant rows.

The next same-Task run completed research, both expert surveys, and all four persona votes, then reached the deterministic aggregation tool. Aggregation correctly rejected the canonical survey because its persona JSON example used placeholder segmentation values such as `tier: "example-tier"` instead of the declared `S | A | B | C | D` enum. The scheduler then rejected an attempted repair dispatch because the active Task had frozen the pre-repair projection hash while the package files had changed during that run. This is correct projection-drift protection, not a reason to bypass the package or manually aggregate.

The package contract previously required a full eleven-field persona example but never stated where its identity and segment values come from. The repair is to make the exact persona authority file the research worker's one readable project input and require the example to copy the first authority object verbatim for `user`, `name`, `uid`, `tier`, `status`, `interaction_mode`, `top_dimension`, and `usage_freq`. The example may choose three valid F codes and a reason, but it cannot invent placeholder identity or segmentation values. Output-directory preflight remains forbidden; the exact authority file is not an output artifact.

The subsequent retry repaired the persona example and reached expert validation. The Denis answer semantically disclosed model inference in English, but `aggregate-report` validates only the text after the exact `### 诚实边界总声明` heading and requires `LLM` and the Chinese term `推断` within forty characters. The generated phrase `LLM inference ... 产品哲学推断` exceeded that distance, so repeated blind rewrites did not satisfy the validator. The package prompt had required an honesty section but did not expose this exact lexical contract. The repair is prompt-side: require the literal phrase `LLM 通用推断` inside that exact section. The validator remains single-source and unchanged; no alternate marker or compatibility branch is added.

`ExpertSquadRegistry.collectSkillRefs` rejects every ancillary entry next to `SKILL.md`, so copying the old multi-file directories would be an invalid package. The source-backed perspectives therefore flatten their exact seven-file snapshots into one package skill each; the other skills are current-runtime adaptations with explicit scope. This is a protocol transformation, not a fallback or compatibility route.

### iFind correction and runtime evidence

The earlier decision to replace iFind with WebSearch was incorrect after the user's explicit clarification. A complete grep found every contradictory current-owned surface: this Recall, the package README, the research-lead prompt, `mirror-watch-package.test.ts`, and `mirror-watch-real-e2e.mjs`. They must be changed together so no second search source remains.

The source Python implementation cannot be packaged unchanged:

- `scripts/ifind_search:21` calls `os.uname()`, which does not exist in the Windows Python runtime. A real invocation failed before any HTTP request with `AttributeError: module 'os' has no attribute 'uname'`.
- Lines 22-25 select either localhost or the remote service from the hostname, which is a forbidden two-endpoint fallback/dual-source design.
- Lines 67-71 and 185-187 convert HTTP/connection failures into ordinary `{error}` output instead of a failed tool call.
- Line 89 truncates content to 2,000 characters, while the advanced contract says `dl: 0` means no summary limit.
- The CLI `--size` value is parsed and capped but never added to the request payload.

A direct, single remote request to the declared iFind endpoint returned HTTP 200 with `status_code: 0`, `status_msg: "OK"`, a JSON `data` array, `total`, and `took`. Therefore the current protocol adaptation is a package tool, not Python or MCP: a package-owned JSON configuration is the single endpoint/request-policy source; a TypeScript library performs one streaming request with an inactivity timer reset by response-body activity; and the research-lead-only entry tool exposes strict query, channel, and size arguments. The strict manifest has no `runtime_dependencies` root because this implementation uses the platform `fetch` API and package-local code only.

The exact role boundary is:

| Role                          | iFind skill           | iFind tool            | WebSearch                           | WebFetch / Browser                          |
| ----------------------------- | --------------------- | --------------------- | ----------------------------------- | ------------------------------------------- |
| Scheduler                     | hidden                | hidden                | not used for Mirror Watch research  | inspection only before dispatch             |
| Research lead                 | visible and mandatory | visible and mandatory | forbidden as a second search source | may open/verify only URLs returned by iFind |
| Expert/persona/report workers | hidden                | hidden                | no new search assignment            | only their existing non-search duties       |

The main-backend/main-database run must observe at least one real `mirror-watch/shared/ifind-search` tool result from the production research session, prove no WebSearch tool call occurred in that session, and preserve the returned URL/evidence chain into the canonical artifacts. A mocked response or a package-load assertion is not iFind end-to-end acceptance.

## Old-protocol deletion boundary

The rollback worktree contains staged but uncommitted old integration work. Before deletion, its status, name-status, and focused staged diffs were inspected. User authorization to delete the old protocol is explicit; no active agent owns or edits these paths. Deletion is exact: the fifteen old package files, dedicated old test, dedicated old spec, and only their three shared-file references. The shared isolated-runner files and all unrelated staged hunks remain unchanged.

## Latest package design

The package scheduler is the ordinary OpenCorvus orchestrator with a Mirror Watch coordination prompt and shared workflow-contract skill. It dispatches four package-prefixed dynamic worker identities, each backed by the common `delegated-worker` runtime and an identity-specific prompt. This is capability projection, not a second workflow engine.

The report analyst alone receives `mirror-watch/shared/aggregate-report`. The tool statically imports a strict engine under `lib/mirror-watch/`; the engine validates one explicit cohort and produces the only machine-readable aggregation result. Expert votes remain qualitative corroboration and never alter persona-derived scores.

## Verification commands

Focused checks will be recorded after implementation. At minimum:

- Dedicated Mirror Watch package test.
- Registry/manager/resolver/package-tool focused tests needed to prove active/inactive isolation.
- Typecheck and relevant documentation-health tests.
- Direct main-backend/main-database real-model run initiated by exactly one `POST /task` carrying `model: "hexin/gpt-5.5"`; inspect ordinary task/session/tool/artifact evidence after the backend reaches a real failure or completion. Do not add a controller or claim a polling protocol.
- Final read-only independent audit, followed by diff review, commit, and `legacy-remote` push.

## Current acceptance status

- `bun test packages/opencorvus/test/expert-squad/mirror-watch-package.test.ts`: 10 passed, 0 failed, 268 assertions.
- Registry, resolver, package-tool bundle, and SkillMount focused set: 78 passed, 0 failed, 268 assertions.
- `bun run --cwd packages/opencorvus typecheck` through the repository inactivity-timeout wrapper: passed.
- The package now directly schedules the current Task, exposes immutable task/goal workflow guidance, declares disjoint expert/persona goal concurrency, keeps zero-sample classifications visible, requires one explicit project iFind configuration, contains no provider endpoint/identity/credential or machine path, narrows selection to the implemented AInvest delivery, and proves active/inactive runtime projection through real resolver/catalog/mount/package-tool paths.
- Real main-backend/main-database `hexin/gpt-5.5` execution and headed GUI acceptance remain unverified. No earlier run is accepted as current evidence.

## Superseded historical evidence

Every section below records prior rejected snapshots and debugging history only. Their Mission wrapper, nested Task topology, benchmark controller, polling/activity tracker, fixed Skill/iFind call order, hard-coded provider configuration, screenshots, identifiers, counts, and completion statements are not current acceptance evidence and must not be replayed as the current protocol.

### Superseded implementation evidence

- The canonical package is `.opencorvus/expert-squads/legacy-remote/mirror-watch`; its only manifest is `expert-squad.jsonc` with `schema_version: 1` and no retired `runtime_dependencies` field. No `v2` suffix, parser, identity, alias, or compatibility surface exists.
- At that superseded snapshot, the focused Mirror Watch package test passed 9 tests, 0 failures, and 234 assertions against the strict registry, and an isolated real-model backend run was planned as the next step. That planned environment is replaced by the main-database publication constraint.
- The owned real E2E reached a production server, headed Node Playwright, the real Overlay UI, UI selection of Mirror Watch, active/inactive projection checks, one natural user request, Mission `e66b83133af0a556`, task `tsk_f5ef1b612001x43YOJCetTRu0v`, and a real `mirror-watch-research-lead` dynamic session. Evidence is under `.scratch/mirror-watch-real-e2e/2026-07-14T04-42-41-526Z`.

### Unresolved parallel semantic conflict

The E2E did not pass. A real `webfetch` result contained a Markdown image whose URL was an inline `data:image/png;base64,...` payload. The parallel attachment-persistence work in dirty `session/index.ts`, `session/loop.ts`, `session/processor.ts`, and `tool/multimodal-result.ts` now requires explicit `AttachmentStore` materializations and rejected the result with `InlineBase64InPartError`. Both the dynamic research session and its orchestrator became terminal-error sessions, while the task remained `active/running`; the harness then correctly failed only after 480000 ms of real inactivity and captured `99-failure.png`.

The direct producer is clean `tool/webfetch.ts`, but clean existing `test/tool/webfetch.test.ts` still requires direct image responses to use the old inline data-URL representation. Fixing the producer to the new single-source attachment contract necessarily invalidates that existing test. The user explicitly required an independently owned test script and forbade editing other tests, while the parallel owner is actively changing the receiving attachment contract. This is an actual cross-agent semantic contradiction, not an implementation ambiguity, so no overlapping Session or existing-test edit was made.

The contradiction is also present wholly inside the parallel owner's current dirty files. `test/session/extra-tools.test.ts` requires `SessionLoop.materializeToolResultAttachments` to convert a data-URL tool attachment into an AttachmentStore reference, while the current dirty implementation in `session/loop.ts` throws immediately for that same input. The focused command below reproduced the conflict with 0 passing and 1 failing test:

```powershell
bun test packages/opencorvus/test/session/extra-tools.test.ts --test-name-pattern "materializes data URL tool-result attachments into AttachmentStore refs"
```

The observed failure is `Tool result attachments must be explicit AttachmentStore materializations, not data URLs` at `session/loop.ts:1380`, called by the asserted conversion case at `extra-tools.test.ts:935`. Therefore the unresolved choice belongs to the parallel attachment-contract change itself: either the consumer owns conversion, or every producer owns explicit materialization. Editing Mirror Watch or WebFetch cannot legitimately choose between those contradictory contracts.

At that historical checkpoint, acceptance remained incomplete until the attachment-contract owner reconciled the WebFetch producer contract or explicitly transferred ownership of the conflicting existing test. Its planned fresh isolated-project rerun is superseded; current real E2E publication uses the existing main backend/main database, while screenshot inspection and independent audit remain required.

### Resumption correction

The user rejected treating the parallel Session-test contradiction as a blocker for the entire Mirror Watch goal. The ownership boundary is narrower: this task still must not edit the dirty Session/AttachmentStore files or another agent's tests, but it can fix the clean producer that the real E2E proved faulty.

The relevant call-point inventory is:

| Surface                                                 | Current evidence                                                                                                         | Owned decision                                                                                                                                                               |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tool/webfetch.ts` HTML-to-Markdown and raw text output | A fetched HTML `<img src="data:...;base64,...">` survives Turndown and enters `result.output`.                           | Reuse the canonical `util/inline-base64.ts` matcher, remove the inline binary payload from every textual WebFetch format, and disclose the omission in output plus metadata. |
| `util/inline-base64.ts`                                 | Defines the same protocol prefix matcher used by the Session persistence integrity check.                                | Reuse without modifying it; do not create a second recognizer.                                                                                                               |
| Dirty Session/AttachmentStore implementation            | Rejects inline base64 during persistence and has unrelated parallel ownership.                                           | Preserve untouched.                                                                                                                                                          |
| Existing `webfetch.test.ts`                             | Covers the separate direct-image attachment response and is not owned by this migration.                                 | Preserve untouched.                                                                                                                                                          |
| New Mirror Watch regression                             | Must prove HTML, Markdown, and HTML-format text results cannot carry inline base64 while making the omission observable. | Add a new owned test file only.                                                                                                                                              |

This correction does not claim that the parallel attachment contract is resolved. It removes the actual producer defect reached by this benchmark without selecting or overwriting the parallel implementation's unresolved consumer semantics.

### Independent audit correction

The resumed implementation was independently reviewed against the current files. The reviewer found no manifest-structure defect and no P0 issue, but identified production gaps that remain inside this task's owned surfaces. They are acceptance work, not external blockers:

| Surface                    | Root evidence                                                                                                                                                                     | Single-source correction                                                                                                                                                                                                                                                                                  |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WebFetch request lifecycle | The timeout was cleared after response headers, before `response.arrayBuffer()`, so a stalled body was no longer bounded.                                                         | Keep one abort signal active through complete body acquisition and clear it in `finally`.                                                                                                                                                                                                                 |
| WebFetch Cloudflare branch | A 403 challenge triggered a second fetch with another User-Agent.                                                                                                                 | Delete the retry entirely; one request succeeds or exposes its actual status. Add an owned regression proving one call only.                                                                                                                                                                              |
| WebFetch evidence metadata | `originalBytes` described formatted output bytes, while the E2E treated it as raw response-byte evidence.                                                                         | Pass the acquired response byte length into textual formatting; report raw, formatted, shown, and omitted byte counts with unambiguous meanings.                                                                                                                                                          |
| Aggregation project paths  | Lexical `path.relative` checks did not reject a project-local symlink or junction that resolves outside the project; path arguments were silently trimmed.                        | Reject leading/trailing whitespace, reject linked path components, verify real paths stay under the canonical project root, and use the exact validated report path in the returned evidence.                                                                                                             |
| Report rendering           | Untrusted survey/persona/expert strings were interpolated into `innerHTML`.                                                                                                       | Escape every dynamic value at the sole DOM-rendering boundary and add an owned malicious-input regression.                                                                                                                                                                                                |
| Expert validation          | The package contract required a full framework/screen/picks/rejections/confidence/honesty analysis, while the aggregation parser accepted any nonempty `reason` and `usage_freq`. | Make the existing six-field expert vote contract machine-checkable in the production parser: the structured detailed analysis, ten-feature coverage, three ordered picks, seven rejected codes, positioned perspective references, confidence, and honesty boundary must all be present before rendering. |
| Canonical expert schema    | Research/E2E described a five-field agent survey example while the package and aggregator required `persona_source` as a sixth field.                                             | The canonical expert answer is the one six-field package contract everywhere; update the owned E2E and prompts, with no compatibility schema.                                                                                                                                                             |
| Expert page ownership      | The prompt called the expert HTML self-contained while also requiring a separate canonical JavaScript data module.                                                                | Describe the HTML as a standalone accessible page shell with exactly one project-local canonical data module; that module remains the only page data source.                                                                                                                                              |
| E2E readiness timeout      | Response-body activity updated only a local timestamp; the outer readiness loop observed only child stdout/stderr.                                                                | Return request activity from the body reader and fold it into the readiness loop's last-observed-activity timestamp.                                                                                                                                                                                      |

The new call-point grep covered `executeWebFetch`, `formatFetchedText`, all `originalBytes` consumers, `fetchTextWithInactivity` and `waitForHttpService`, every aggregation path resolver/read/write caller, the report template's dynamic `innerHTML` assignments, expert schema declarations in prompts/skills/tool/E2E, and the owned package tests. Existing `webfetch.test.ts`, dirty Session/AttachmentStore files, and unrelated shared benchmarks remain untouched.

### Follow-up independent audit and current validation

The same read-only reviewer rechecked every earlier finding against the updated files and found no P0. Three remaining single-source defects were then corrected inside the owned package and E2E surfaces:

- The E2E now compares the expert data module's `usageFreq` directly with the canonical vote enum and verifies its substantive `usageReason` against the vote's detailed reason; it no longer asks a four-value enum to contain a long explanation.
- The production aggregator, not only the prompt or E2E, now requires the canonical survey to end with exactly one `## 专家答卷契约` JSON example and one `## 画像答卷契约` JSON example with their exact disjoint key sets and identity/category semantics.
- Expert Pick 1-3 analysis must collectively cite at least three distinct positioned package items, and accepted item positions are the real 1-10 range; a repeated reference or invented `#999` cannot pass.

Current focused evidence after these corrections:

- `bun test --timeout 0 packages/opencorvus/test/expert-squad/mirror-watch-package.test.ts`: 7 passed, 0 failed, 60 assertions.
- `bun test --timeout 0 packages/opencorvus/test/tool/mirror-watch-webfetch-inline-binary.test.ts`: 4 passed, 0 failed, 37 assertions.
- `node packages/opencorvus/script/benchmark/mirror-watch-real-e2e.mjs --mirror-watch-contract-self-test`: passed; evidence at `.scratch/mirror-watch-real-e2e/contract-self-test-35520-1784009876353/contract-self-test.json`.

The earlier real E2E run remains useful negative evidence: its old imported orchestrator dispatched repeated replacement research sessions after the first worker wrote staging paths. The current package has removed that behavior at the instruction source: every owner receives the exact final paths in its only fresh session, and a wrong path or failed submission is terminal rather than a replacement/evidence-only retry. That earlier run cannot satisfy acceptance because it imported the package before this correction; its historical instruction for a fresh isolated run is superseded by the existing main-backend/main-database publication requirement.

A subsequent current-package run at `.scratch/mirror-watch-real-e2e/2026-07-14T06-18-56-517Z` exposed a narrower dispatcher defect before final convergence: the Orchestrator used the first `mirror-watch-research-lead` session for a read-only evidence summary and explicitly told it not to write files, then opened a second research session. Screenshot `02-dynamic-agent-dispatch.png` and the real invocation DAG prove this was an actual model dispatch decision, not an inferred naming issue. The package Orchestrator prompt now assigns all inspection/path/persona preflight to the scheduler's own skills and read tools and states that the first and only research-lead session is the production owner for all four exact final artifacts. No host gate or dispatch bypass was added.

The visible “输出格式” question from that run is the same dispatcher defect expressed as a serialization ambiguity, not a search or ranking-algorithm failure. The isolated run database records that the Orchestrator delegated “Provide the four persona tuples exactly as machine-readable JSON objects” to a research-lead session and required “No extra text”, but did not define a single JSON envelope. Four adjacent top-level objects are not one valid JSON document, while an array is; the worker therefore asked whether to emit one array or four independent objects. The single-source correction is upstream ownership, not a worker fallback: the scheduler reads `dashboard/data/personas.json` itself and dispatches the four persona workers directly. If this data ever must cross a tool boundary, the contract must say “one JSON array of exactly four objects in source order”; the production research worker must not be used as a persona-preflight reader.

The first attempt after that prompt correction stopped before any server or Mission was created because the owned harness detected `packages/overlay/dist-vite` changing during its isolated Overlay build. The before/after tree digests showed different JavaScript, CSS, and index hashes while parallel repository work was active. The harness correctly rejected that concurrent build interference rather than treating shared output as isolated evidence; the source/package protocol was not exercised in that attempt.

The next isolated run at `.scratch/mirror-watch-real-e2e/2026-07-14T06-27-53-954Z` proved the dispatcher correction: its first and only research-lead instruction named all four exact final paths, required direct writes, and produced those paths without staging. Early artifact review then found a separate contract defect before downstream completion: `ainvest-h2-feature-survey-agent.md` was only 1,091 bytes and contained the two JSON examples but no F01-F10 survey body. This is invalid because downstream experts and personas need one canonical ten-feature input; the production aggregator's exact-ten-feature parser would reject it. The checker was not relaxed. The package delivery skill and research prompt now explicitly require ten ordered `## F01. <title>` through `## F10. <title>` sections with exact inherited feature fields before the two terminal schema sections, and the Orchestrator treats a schema-only file as a missing artifact rather than dispatch evidence.

Current repository validation also exposed one genuine parallel-owner overlap outside this task's owned paths. `packages/opencorvus/src/expert-squad/payload.ts` already contains another agent's embedded Mirror Watch payload snapshot, including a hard-coded older aggregation engine, so `package-manager.test.ts` reports a 258-line source mismatch against the current project package. That shared file was not overwritten. In the same combined run, 67 tests passed, one was skipped, and two unrelated general-profile tests failed because the parallel `general` projection declares `default/skill/multica-import` without supplying that default skill. The Mirror Watch active/inactive selector and package-tool isolation cases themselves passed.

### iFind E2E resumption findings

The first iFind-enabled run initially stopped in the harness before service start because the legacy-demo similarity checker compared a report template that must use the AInvest Design System against an old page using the same system. Diagnostic shared-span evidence showed that the overlap consisted of the canonical color token names/values, system font stack, standard document metadata, and common token-based border declarations. The checker now removes shared `<style>` blocks only for short-shingle structural/content comparison, while still rejecting full normalized copies and insertion-only near-copies; a contract self-test proves that shared design-system styles pass and an exact styled clone fails. Package source-backed expert skills are no longer compared to demos generated from the same quoted expert sources: source-skill provenance is validated by its SHA-256 inventory and loaded content, while the package-owned report template and every generated user-facing artifact remain subject to legacy separation checks.

The next real run selected Mirror Watch through the headed Overlay, submitted one natural request, created Mission `49d9ac8f1099bf17`, and launched the first production research owner. That worker loaded its projected package skills, but before its first iFind call invoked the Question tool to ask which feature categories the user preferred. The explicit request already names the competitors, AInvest target, ten-feature count, and evidence contract, so this was not missing authority. The isolated server log later records a real `POST /interaction/.../reply` selecting all five choices; it was not the Question auto-reject timer, and this task did not issue that request. Because the browser was intentionally headed and desktop-visible, the run cannot attribute the reply to an autonomous package decision or use it as acceptance evidence. The research-lead prompt now requires it to derive a balanced F01-F10 scope from live iFind evidence and forbids preference/theme/category/format questions; the owned E2E now rejects any Question tool call immediately instead of waiting through a production decision that belongs to the package. The run is retained as negative evidence under `.scratch/mirror-watch-real-e2e/2026-07-14T07-35-44-443Z`.

The same run exposed a harness timeout defect: Mission status repeats changing DAG `emittedAt` liveness timestamps even when no message, tool, or artifact changes. The earlier activity signature retained that volatile DAG copy and therefore treated a heartbeat as productive activity. The current harness removes the duplicate invocation DAG from the Mission-status portion and signs the separately normalized graph, visible messages, queue rows, artifacts, and semantic task status instead. Its contract self-test proves different heartbeat timestamps produce the same activity signature while a real Mission status transition changes it.

The following fresh run at `.scratch/mirror-watch-real-e2e/2026-07-14T08-10-04-742Z` used the current iFind-only package. It reached the real headed Overlay, selected Mirror Watch through settings, submitted the single natural request, and created Mission `3cff696ca8ebfb25`. The Mission's first `panel.create_task` call was rejected by the real tool schema because it used `checks.named.scope: "mirror-watch-h2-research"`; `checks.named` values are executable-check objects whose required `commands` field is an array, not labels. This is a scheduler prompt omission, not a search-algorithm defect and not grounds for a host gate. The package Orchestrator prompt now says to omit `checks` when there is no real executable checker, never use `checks.named` for scope/role metadata, and otherwise supply the exact object shape with a nonempty `commands` array. The owned package test pins that correction. The already-imported isolated run retains its old snapshot and remains negative evidence even if its model later recovers.

The first run with failed-tool-call visibility enabled, `.scratch/mirror-watch-real-e2e/2026-07-14T08-21-27-238Z`, proved that conditional wording was still ambiguous to the Mission model: it emitted `checks.named.artifact_count` as a number and `checks.named.need_live_iFind` as a boolean. The script immediately rejected the visible `AI_InvalidToolInputError`, captured `99-failure.png`, and did not wait for a model repair to erase the failure. Mirror Watch has no task-level executable command checker, so the prompt now removes the condition completely: every Mirror Watch `panel.create_task` call omits `checks`; artifact counts, iFind requirements, scope, role, acceptance criteria, and output-path assertions stay in the request text. This is the single valid task-creation shape for this package, not fallback behavior.

A third fresh run at `.scratch/mirror-watch-real-e2e/2026-07-14T08-26-16-605Z` still emitted `checks.named.artifact_paths` as a string array despite the exact package instruction. Three distinct invalid values across three independent Mission calls (`scope` string, `artifact_count` number plus `need_live_iFind` boolean, and `artifact_paths` array) prove that this is a systemic tool-prompt/schema-description ambiguity rather than a Mirror Watch-only wording gap. The actual `CheckConfig` schema only accepts each `named` value as an executable-command object with a nonempty `commands` array, while the exposed panel field description previously said only “Evaluation check configuration for the new task” and the global Mission dispatch contract did not explain the shape or when to omit it. The root correction is therefore prompt-over-host at both authoritative upstream surfaces: `mission-core.txt` now distinguishes executable checks from request acceptance/artifact metadata, and the `panel.create_task` schema description renders the same object/omission rule. No host preflight, route bypass, or gate was added. The pre-existing unrelated `PanelSurface` type export in the already-dirty `panel/capability.ts` is preserved unchanged.

The next run at `.scratch/mirror-watch-real-e2e/2026-07-14T08-36-41-744Z` proved that upstream task creation was fixed: `panel.create_task` succeeded and created the first OpenCorvus task without `checks`. Its task Orchestrator then tried to call four manifest package refs through the Skill tool and received “not found or not allowed”; the scheduler surface correctly exposed only `mirror-watch-delivery-contract`. Registry evidence shows the manifest ref is the projection identity while `packageSkillFromRef()` mounts the `SKILL.md` definition's `name` as the runtime Skill input. The package prompts had conflated those two fields. All scheduler/worker prompts now use exact visible Skill names (`mirror-watch-delivery-contract`, `consulting-analysis`, `ifind-search`, `mirror-design`, `ainvest-design-system`, and the two perspective names), while package tool calls retain their `mirror-watch/shared/...` IDs. The manifest refs remain unchanged as the sole projection source; no aliases or compatibility lookups were added.

The following fresh run at `.scratch/mirror-watch-real-e2e/2026-07-14T08-46-09-904Z` successfully selected Mirror Watch through the headed Overlay, created Mission `1fdc406b60f6ade6`, dispatched `mirror-watch-research-lead`, loaded all five projected research skills, executed the three mandatory iFind queries plus three iFind-only refinements, and WebFetched only exact URLs already returned by iFind. The harness then rejected a pending Question call. The preserved database records the exact question as “当前可用 iFind 证据偏向哪类页面更稳妥？” with official/help, research/media, and app-store/product-page choices.

This failure exposed a contract-level evidence-source error rather than an iFind search failure. The iFind rows already carried 300–460 character evidence bodies in `response.data[].content`, but the returned 10jqka research-report URLs are client-rendered shells whose successful WebFetch output is only `研报`. The prior prompt and checker incorrectly required the excerpt body to come from WebFetch as well as requiring WebFetch to verify the URL. That dual responsibility made page-class selection appear to need a user preference, even though the product question and live evidence already determine the work.

The single-source correction is:

- `results[].evidence` of the exact visible iFind row is the canonical excerpt body; its same-row exact HTTPS `url` is the provenance locator. A product root URL is valid when that exact iFind row contains the substantive evidence; URL path depth is not evidence quality.
- WebFetch must still successfully verify that exact URL after the iFind result, but a client shell is not required to duplicate the iFind evidence body.
- Evidence rows are selected for feature relevance, a precise URL, and at least 80 normalized content characters, not by asking the user to choose a page class.
- An empty exact iFind query is retained as observable evidence and may be refined only through additional iFind calls. It never switches provider. Acceptance requires the three exact calls, at least one live positive iFind result, iFind provenance for every cited URL/excerpt, and a matching later WebFetch for every cited URL.

The package prompt, delivery/iFind skills, natural request, and owned E2E oracle must change together. The owned package test must pin the evidence-body and no-page-preference rules before the next fresh run. No runtime gate, Question auto-answer, fallback source, or relaxed provenance check is permitted.

The same preserved research instruction proves a second concrete dispatch defect: although the package Orchestrator text names four research artifacts and later separate expert/persona owners, the actual `panel.create_task` request told `mirror-watch-research-lead` to create both expert triplets and all persona votes. That worker then searched for `denis-globa-perspective` and `oleg-mukhanov-perspective`, which were correctly absent from its manifest projection. The root correction is to make the write boundary explicit at both ends: the Orchestrator never assigns `process_answer/**`, `process_answer_aime/**`, or `reports/**` to research; the research role owns only the four named research paths and never searches or loads expert perspective skills. Separate manifest identities remain the only owners. This is a prompt ownership correction, not a new workflow or runtime gate.

The next isolated attempt at `.scratch/mirror-watch-real-e2e/2026-07-14T09-04-35-311Z` stopped before task creation on a failed scheduler `read`. Database evidence shows the scheduler first globbed the correct exact path under `.../opecorvus/.../dashboard/data/personas.json`, then manually reconstructed the next `read.filePath` as `.../opecorus/...`, dropping `v`. The user supplied a stable project-relative authority path, so absolute-path reconstruction was unnecessary. The package scheduler prompt must require using the exact user-supplied project-relative path, or byte-for-byte reuse of a path returned by a successful tool, and forbid retyping/reconstructing an absolute path. This remains prompt-over-host; no path-correction fallback, typo matcher, or failed-tool retry is added.

The fresh run at `.scratch/mirror-watch-real-e2e/2026-07-14T09-06-51-933Z` proved both prompt corrections: the scheduler used the correct persona path, created a task whose research request named only the four canonical research outputs, and the task Orchestrator dispatched exactly one `mirror-watch-research-lead`. The first three exact iFind calls completed. Two subsequent iFind-only refinements then failed during tool-result persistence because their raw provider responses were 108,874 and 125,711 bytes, above the runtime's 51,200-byte persisted tool-result limit. The runtime refused silent truncation.

The source API documents provider-side bounded summaries through `dl`, while `need_content=false` avoids asking for full text. Returning the entire raw provider object despite that search contract is unnecessary and conflicts with the host message boundary. The new single-source tool contract is therefore an explicit compact evidence projection, not post-response truncation:

| Surface          | Decision                                                                                                                                                                                                                                      |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Provider request | Configure `need_content: false` and `dl: 2000`; keep one endpoint and one request.                                                                                                                                                            |
| Result count     | Configure and expose a maximum of ten requested results. The provider request owns the finite result window.                                                                                                                                  |
| Evidence body    | Use the exact provider `summary` field as `results[].evidence`; never fall back to `content` or `source_original`.                                                                                                                            |
| Returned fields  | Return only schema/request metadata, provider status/total/took, and exact projected rows (`channel`, `title`, `url`, optional publish date, `evidence`). Large redundant raw content/extra objects are not part of the declared tool result. |
| Integrity        | Reject a missing/empty/oversized summary, too many rows, invalid provider status, or an over-limit serialized projection. Do not truncate or switch fields/providers.                                                                         |
| E2E              | Prove excerpts come verbatim from `results[].evidence`, every cited URL is the same-row URL and is later WebFetched, empty exact queries remain visible, and all three exact queries execute.                                                 |

The complete call-point grep found only the iFind library, entry tool, research/delivery/consulting skills, README, owned package test, and owned real-E2E script. They must change atomically. No shared runtime truncator, Session code, or other agent's tests are modified.

The next fresh run at `.scratch/mirror-watch-real-e2e/2026-07-14T09-22-12-824Z` reached a new task but failed before dynamic dispatch. Its Mission-created request began “You are the Mirror Watch research lead” and required the five research skills. Because `executor: "opencorvus"` starts a Task Orchestrator, not the projected worker itself, that Orchestrator obeyed the direct-role wording and tried to load four worker-only skills. The failure correctly listed only its scheduler-visible `mirror-watch-delivery-contract` skill. This proves projection isolation is correct and the upstream task audience is wrong.

The full call-point grep found the ambiguity at both authoritative upstream surfaces: `mission-core.txt` says the executor sees the request but does not say that `executor=opencorvus` starts a Task Orchestrator; `panel.create_task.request` is described only as the “Full user request to execute”. The single-source prompt correction is:

- a Mission `panel.create_task` request with `executor=opencorvus` addresses the downstream Task Orchestrator;
- when a projected worker is required, the request starts by instructing that Orchestrator to call `dispatch_agent` with the exact dynamic identity and to pass the enclosed worker brief;
- the request must not impersonate the worker with “You are <worker>”, and the Task Orchestrator must not execute the deliverable or load worker-only skills;
- the Mirror Watch scheduler pins this exact research-task wording, while the global Mission prompt and panel field description state the same audience rule for all packages.

No host routing bypass, direct dynamic-session creation, or tool gate is added. The ordinary Task Orchestrator and `dispatch_agent` remain the only path.

The next fresh run at `.scratch/mirror-watch-real-e2e/2026-07-14T09-33-24-924Z` proved the task-audience correction: the Mission request addressed the Task Orchestrator, the Task Orchestrator called `dispatch_agent`, and the real `mirror-watch-research-lead` loaded all five correct skills and completed all three exact iFind queries. An iFind-only refinement then failed strict validation because the provider returned a 2,001-character summary for configured `dl: 2000`.

A direct replay of `AInvest Aime stock app` reproduced `[473, 2001, 557]` summary lengths. The 2,001-character value ended in the provider-added `...` terminator. Controlled requests proved the service returns `dl + 1` characters on this result: `dl=1997 -> 1998`, `1998 -> 1999`, and `1999 -> 2000`. The provider-request budget is therefore configured as `dl: 1999` while the persisted evidence invariant remains 2,000 characters. This is an observed provider-boundary correction, not post-response truncation; a result above 2,000 still fails explicitly.

### Root-URL evidence-contract correction after the `09-42-19-442Z` run

The fresh isolated run at `.scratch/mirror-watch-real-e2e/2026-07-14T09-42-19-442Z` proved the current Task-Orchestrator audience and dynamic-agent projection end to end: Mission `7d9116f067daf470` created one task, the Task Orchestrator dispatched one real `mirror-watch-research-lead`, and that worker loaded the five declared research skills. It completed all three mandatory iFind calls and more than twenty iFind-only refinements; no WebSearch call or failed iFind tool result occurred. The run still failed correctly when the worker began a pending Question tool call. Because the harness rejects Question immediately, the persisted part contains the call start with `input: {}` rather than a completed question payload; the preceding reasoning says it was still trying to confirm exact WebFetch URLs.

The direct trigger was not an unclear iFind ranking algorithm. The package contract incorrectly equated URL path depth with evidence quality by requiring every source to be a non-root URL. The same run's real iFind results contained substantive, product-relevant evidence for credible exact product URLs including `https://cn.tradingview.com/` (1,999 evidence characters), `https://www.ainvest.com/` (1,269 characters), and `https://finance.yahoo.com/markets/` (163 characters). The first two were rejected only because their path is `/`. At the same time, many non-root results were false positives or unrelated pages. This artificial constraint made the worker repeat searches despite already having usable same-row evidence and eventually seek a page-class decision that the user should never have been asked to make.

The full-repository grep found the bad constraint only in the Mirror Watch package, its owned E2E, and this record. Unrelated uses of “non-root” describe agent-session hierarchy and are preserved. The atomic correction is:

| Call point         | Correction                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Research role      | Accept an exact HTTPS URL from the same iFind row whether its path is `/` or deeper. Select by named-product relevance and substantive `results[].evidence`, never by path depth or a page-class preference.                                                                                                                                                                                                             |
| iFind skill        | Replace the uninstantiated “finite list” with exactly the three mandatory calls plus at most one declared refinement per named product. The known refinements are `site:cn.tradingview.com TradingView stock screener alerts watchlist charting features`, `finance.yahoo.com stock chart watchlist alerts portfolio Yahoo Finance`, and `site:ainvest.com "Start Backtesting" AInvest`. Search never switches provider. |
| Delivery contract  | Permit distinct feature-relevant contiguous excerpts from one rich iFind row to support multiple features; the same paragraph cannot be reused, and every excerpt must remain a verbatim substring of that row's evidence. One WebFetch per unique cited exact URL is sufficient verification.                                                                                                                           |
| Owned E2E          | Validate exact same-row HTTPS provenance without rejecting `/`; keep the 80-character excerpt, later-WebFetch, no-WebSearch, no-Question, and all-three-mandatory-query assertions. Rename the helper and diagnostics so the obsolete rule cannot survive as a hidden oracle.                                                                                                                                            |
| Owned package test | Pin the finite six-call maximum, the three exact optional refinements, root-URL validity, distinct feature-local excerpts, and continued rejection of Question/fallback providers.                                                                                                                                                                                                                                       |

If those six iFind calls do not provide a substantive named-product row, the worker submits an explicit terminal evidence failure through its delegated-worker result. It does not ask the user, broaden indefinitely, invent evidence, reuse another provider, or relax the evidence-body check. This is a prompt/data-contract root correction, not a host gate or Question fallback.

The next run at `.scratch/mirror-watch-real-e2e/2026-07-14T10-05-52-886Z` reached the corrected production research worker and observed the three mandatory iFind calls exactly once. Before those calls, the worker used the generic Bash tool only for the read-only command `git status --short`. The owned E2E's `assertNoLegacyExecutionProvenance` currently rejects every Bash tool call before inspecting its command, so the already-running checker will eventually reject this run even though no legacy Mirror Watch runner, alternate search provider, source tree, or Git mutation was invoked.

That oracle is broader than the actual acceptance boundary. The single correction is to inspect Bash command provenance: allow read-only repository inspection such as `git status`, but reject `agent_invoke`, the source `ifind_search` runner, Python/shell runner execution, `curl`/`wget` alternate retrieval, and Git write/destructive commands. Existing exact legacy-root and `source/*-demo` path rejection remains. The contract self-test must prove a read-only `git status` passes while a relative legacy `scripts/ifind_search` command fails. This changes only the owned benchmark oracle and does not mutate the package snapshot already imported by the live run.

The same `10-05-52-886Z` run then exposed an incomplete finite-plan contract. The role and iFind skill declared six possible query strings but did not bind their required `channels` and `size`. The worker followed the generic channel table and sent all six calls with `channels: ["app_function"]` and `size: 5`. TradingView and Yahoo exact calls returned evidence, but AInvest exact and all three web-oriented refinements returned zero projected rows. Earlier real evidence showed the same AInvest refinement returns substantive rows when sent with `web`, `web_en`, and `app_function` at size ten. The worker's reasoning then considered producing artifacts with an evidence-gap note, which would violate the terminal-failure boundary and cannot pass AInvest provenance acceptance.

The query string alone is therefore not a complete algorithm. The finite iFind plan becomes six exact request triples:

| Topic                    | Query                                                                                   | Channels                        | Size |
| ------------------------ | --------------------------------------------------------------------------------------- | ------------------------------- | ---- |
| TradingView mandatory    | `TradingView product features`                                                          | `app_function`, `web`           | 10   |
| Yahoo Finance mandatory  | `Yahoo Finance product features`                                                        | `app_function`, `web`           | 10   |
| AInvest mandatory        | `AInvest Aime stock app features`                                                       | `app_function`, `web`           | 10   |
| TradingView refinement   | `site:cn.tradingview.com TradingView stock screener alerts watchlist charting features` | `web`, `web_en`                 | 10   |
| Yahoo Finance refinement | `finance.yahoo.com stock chart watchlist alerts portfolio Yahoo Finance`                | `bing`, `web`, `web_en`         | 10   |
| AInvest refinement       | `site:ainvest.com "Start Backtesting" AInvest`                                          | `web`, `web_en`, `app_function` | 10   |

The package role, iFind skill, owned package test, and owned E2E must pin these exact request triples together. The E2E rejects an allowed query with different channels or size, so the generic channel table cannot silently reinterpret this run. This is explicit provider configuration, not a second endpoint, fallback, or host route gate.

The `10-05-52-886Z` run ended naturally after the production research worker wrote and reread all four canonical files, called `submit_delegated_worker_result`, and the Task Orchestrator called `manage_task(complete_task)`. Mission status simultaneously reported success for its only task, every durable Agent DAG node reported `completed`, and the headed failure screenshot visibly showed the “任务已完成” toast. The owned E2E nevertheless rejected `graph.rootStatus === "missing"` because it expected `completed` from `/session/status` for the DAG's structural `rootSessionID`.

That assertion conflated the task root container with an executed Agent node. The durable DAG explicitly excludes task goals/runs/outcomes from executed-agent nodes; the sole top-level Orchestrator child and its dynamic descendants carry execution statuses. `/session/status` contains no live/terminal execution entry for the non-executed structural root, while Mission task success is the authoritative task terminal state. The E2E must therefore assert that the structural root has no execution status (`missing`) and separately keep its existing requirements that Mission task counts are terminal-success and every actual DAG node is `completed`. Accepting `completed` or `missing` as interchangeable would be fallback; the current protocol has one exact structural-root contract.

Visual review of `01-mirror-watch-active.png`, `02-dynamic-agent-dispatch.png`, and `99-failure.png` found no layout breakage: the settings page visibly identifies legacy-remote/Mirror Watch as project-active, the Work Ledger binds the running research identity and session, and the terminal screen shows the four-file worker submission plus Task completion. The final task still remains negative evidence because it used the pre-request-triple package snapshot and its research artifacts could not carry AInvest-query provenance.

The first run with full request triples showed that the formerly broad AInvest refinement was still service-unstable: a direct probe returned two rows, but the production worker received an empty result minutes later with the identical request. A targeted iFind investigation compared four AInvest phrases. `site:ainvest.com "Launch Dashboard" AInvest` and a broad dashboard query returned zero; `site:ainvest.com "Try Screener Beta" AInvest` returned the same two rows in three consecutive runs; and `site:ainvest.com "Start Backtesting" AInvest` returned one substantive HTTPS AInvest root row in three consecutive runs, with a 1,269-character evidence summary. The stable `Start Backtesting` query is now the single AInvest refinement. This replaces the unstable query in place; the package does not keep both as an ordered fallback chain.

That run next failed visibly when WebFetch opened an iFind-returned `https://tradingview.com/` row and Node reported `unknown certificate verification error`. This was not a WebFetch formatter defect: Windows curl failed the TLS handshake for the apex host and Node fetch rejected it, while both curl and Node successfully returned HTTP 200 for `https://cn.tradingview.com/`. A targeted replacement query, `site:cn.tradingview.com TradingView stock screener alerts watchlist charting features`, returned the same eight rows in three consecutive probes, with `https://cn.tradingview.com/` first and 1,999 evidence characters. It replaces the broader TradingView refinement in place. Exact URL verification remains mandatory and TLS errors remain failed tools; no insecure certificate bypass, URL rewriting, retry, or alternate provider is added.

### iFind-only evidence correction after the `10-40-19-362Z` run

The fresh isolated headed run at `.scratch/mirror-watch-real-e2e/2026-07-14T10-40-19-362Z` imported and selected the current package, created Mission `c1869f20ccfc7478`, dispatched the real `mirror-watch-research-lead`, loaded all five projected research skills, and completed all six declared iFind request triples. The observed iFind result counts were `2, 2, 0, 1, 8, 10`; the targeted AInvest request returned a substantive 1,269-character same-row summary with `https://www.ainvest.com/`, and the targeted TradingView request returned substantive `https://cn.tradingview.com/` evidence. No WebSearch or failed iFind call occurred.

The run still failed correctly because the research contract required WebFetch verification after iFind. `https://www.ainvest.com/` returned HTTP `text/html` with a zero-byte body and was persisted as a completed empty WebFetch result. The worker then continued beyond the already sufficient iFind evidence and WebFetched the iFind-returned `https://tradingview-a.com.cn/`, which failed with `unknown certificate verification error`; the E2E immediately rejected the failed production tool call. This proves that mandatory URL re-fetching is not an integrity check on the iFind evidence. It is a second, unrelated availability condition controlled by each destination's TLS and response behavior.

The authoritative evidence source is now one surface: the successful iFind package-tool result. Its exact same-row `results[].evidence` provider summary is the excerpt body and its exact same-row HTTPS `url` is the provenance locator. The report does not claim that the destination page was independently fetched. For this canonical run WebFetch and Browser are not used for research evidence, so they cannot silently become a second source or impose destination reachability on an already successful iFind query.

The previous “three mandatory plus up to three conditional refinements” wording also left an avoidable decision boundary. The canonical run now executes all six declared request triples exactly once, in the declared order. Empty rows remain visible provider evidence; they do not trigger a retry, alternate query, alternate provider, WebFetch, Browser, or user question. The three targeted requests are ordinary members of the fixed plan rather than fallback branches.

The complete call-point grep covered the research-lead prompt, `ifind-search`, `delivery-contract`, and `consulting-analysis` package skills, the package README, the owned package test, the owned real-E2E natural request/oracle/self-test, and this record. Historical source-provenance text embedded in the Denis/Oleg immutable skills is not an active runtime instruction and remains untouched. The current correction updates every active call point together and does not add a host gate or compatibility path.

### Mission task-model projection correction after the `10-58-07-926Z` run

The next isolated headed run at `.scratch/mirror-watch-real-e2e/2026-07-14T10-58-07-926Z` successfully built the actual Overlay, imported the package, selected Mirror Watch through settings, created Mission `ca0bd303d41d2173`, and created Task `tsk_f604a4470001ADls6rGD3k3nJz`. The Mission's `panel.create_task` input explicitly set `model: "openrouter/auto"`. The durable Task root simultaneously recorded `taskConfigSnapshot.model: "hexin/gpt-5.4-mini"` and `configOverlay.model: "openrouter/auto"`. Provider discovery contained `hexin` and did not contain `openrouter`, so the Task Orchestrator failed before creating an Agent node with `ProviderModelNotFoundError`.

This is not an iFind or expert-squad projection failure. The direct trigger is the Mission model inventing an unrequested task-level override. `panel.create_task.model` is optional, and omitting it already makes Task creation inherit the effective configured model snapshot. The root prompt correction is therefore explicit: Mission must omit `create_task.model` unless the user explicitly requested a distinct provider/model for that child task; it must never guess a popular/default model reference. The panel field description states the same rule. The Mirror Watch natural request repeats the inherited-model requirement so the production E2E proves the behavior rather than relying only on a prompt-string test.

The full call-point grep covered the Mission core create-task instructions, the panel `create_task.model` schema description, task API snapshot/overlay construction, config effective snapshot, model resolver, all `configOverlay.model` tests, the owned package test, and the owned E2E. Task API already has the correct single-source behavior when the optional model is absent, so no host route bypass, model fallback, or provider compatibility alias is added.

The same run exposed a checker-observability defect: `/mission/<id>/status` returned the Task as `running` with `taskCounts.failed: 0` even though that Task's visible `error` field already contained the terminal Orchestrator failure. The owned E2E must reject any non-empty Mission task error immediately during both dispatch wait and convergence. This avoids an eight-minute false wait without modifying runtime lifecycle semantics in this integration task.

### Worker capability minimization after the `11-10-27-026Z` run

The next fresh headed run at `.scratch/mirror-watch-real-e2e/2026-07-14T11-10-27-026Z` proved the model correction: the Mission omitted the unrequested override, the Task Orchestrator streamed normally, and it dispatched a real `mirror-watch-research-lead` whose identity/session/running status was verified in the Overlay. The worker loaded the five required skills in order and then attempted a Bash no-op described as “No-op to satisfy immediate tool call requirement”. The persisted call represented `command` as `"true"`, but tool argument decoding produced a boolean and failed schema validation before the first iFind request. The E2E immediately rejected the failed production tool call.

This worker never needs Bash, WebSearch, WebFetch, Browser, Question, or another discovery route. Keeping `inherit_base_tools: true` exposed all of them despite the package's narrower declared protocol. The latest expert-squad architecture already makes the manifest capability projection authoritative, so the root correction is package-level least privilege rather than another prompt-only warning:

| Worker           | Explicit built-in tools                                                  | Package tool                           |
| ---------------- | ------------------------------------------------------------------------ | -------------------------------------- |
| research lead    | `read`, `write`, `edit`, `apply_patch`, `todowrite`, `todoread`, `skill` | `mirror-watch/shared/ifind-search`     |
| expert surveyor  | `read`, `write`, `edit`, `apply_patch`, `todowrite`, `todoread`, `skill` | none                                   |
| persona surveyor | `read`, `write`, `edit`, `apply_patch`, `todowrite`, `todoread`, `skill` | none                                   |
| report analyst   | `read`, `todowrite`, `todoread`, `skill`                                 | `mirror-watch/shared/aggregate-report` |

All four set `inherit_base_tools: false`. The delegated-worker finalizer remains stage-owned and is not a manifest built-in. This is the package's declared visible capability surface, not a host gate. The owned package test must assert the exact tool sets and absence of forbidden discovery/shell tools; the real E2E must continue proving that the iFind package tool is the only live research provider.

The first run with that manifest, `.scratch/mirror-watch-real-e2e/2026-07-14T11-20-00-004Z`, loaded and activated the package but stopped before Mission creation on a new owned-oracle error. `active_agent_projection.agents[]` exposes the resolved `built_in_tool_ids` but intentionally does not expose the source `inherit_base_tools` boolean. The source-package test already validates `inherit_base_tools: false`; the real E2E must validate each resolved exact built-in tool list from catalog. Treating an absent internal field as `false` would be a fallback, so the invalid boolean assertion is removed rather than relaxed.

### Parallel task ownership conflict in the `11-21-45-021Z` run

The next headed run at `.scratch/mirror-watch-real-e2e/2026-07-14T11-21-45-021Z` passed package import, UI activation, resolved minimal-tool projection, model inheritance, Task-Orchestrator startup, and visible dynamic-agent dispatch. Mission `e8bf438fb422ed9d` then created three concurrent unqueued Tasks:

- `tsk_f6060423300158O55Ko9D4Pl1Y` — `Phase 01: AInvest H2 research package`
- `tsk_f6060a66d001zltC0NGCHNyCDK` — `Phase 02: AInvest H2 expert surveys`
- `tsk_f60611e40001nRY1PNWuMGGF6S` — `Phase 03: AInvest H2 persona and report`

The Phase 02 request explicitly said its experts may start only after the canonical survey exists. Nevertheless all three Tasks started their own Mirror Watch scheduler concurrently. Phase 01 dispatched `mirror-watch-research-lead`, and Phase 02 also dispatched `mirror-watch-research-lead`; both sessions targeted the same four canonical research paths in the same isolated project. Phase 02's second research lead completed the first exact iFind request before shutdown. This is an actual overlapping writer conflict, not a naming inference.

The user's original constraint says to stop and report when a real parallel conflict exists. The isolated E2E process was therefore explicitly interrupted; the user's running OpenCorvus process was untouched. During shutdown the Phase 03 Task Orchestrator also exposed an independent `manage_task` schema failure by sending undeclared `summary` and `reason` keys to `query_failed_goals`.

The causal chain is now proven: one selected expert squad package owns one scheduler workflow, but Mission phase-split that coherent workflow into three OpenCorvus Tasks; every Task instantiated the same package scheduler, and that scheduler correctly began its scenario at research. The next correction must be made before another run: Mission creates exactly one OpenCorvus Task for one coherent active-expert-squad workflow and lets that Task's scheduler own the research → expert/persona → report dispatch sequence. The owned E2E must reject a second Mission Task immediately. No code correction is applied after this conflict record because the user explicitly required work to stop and report at that point.

### Single-Task expert-squad ownership correction after resumption

The persistent goal was explicitly resumed after the conflict report. No parallel agent is currently editing the owned Mirror Watch package, prompt, test, benchmark, or record paths, so the earlier stop condition no longer prevents a root correction. The conflict itself remains immutable negative evidence.

The post-resumption call-point inventory used focused repository searches for `create_task`, Mission task granularity, active expert-squad selection, phase splitting, Mirror Watch scheduler ownership, Mission task arrays, and terminal task counts. The authoritative surfaces and atomic decisions are:

| Call point                                                                       | Current evidence                                                                                                                                                                                                                                                       | Single-source correction                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/opencorvus/src/prompt/core/mission-core.txt`                           | The generic rule says to batch related work, but still allows a model to reinterpret sequential stages as separate tasks. It does not state the consequence of an active expert-squad scheduler: every OpenCorvus Task instantiates that scheduler from its beginning. | State the generic invariant directly: one coherent workflow owned by the selected expert-squad scheduler is exactly one OpenCorvus Task. Research, expert/persona, aggregation, report, and acceptance stages that share artifacts are one request; Mission must not phase-split or duplicate them. |
| `.opencorvus/expert-squads/legacy-remote/mirror-watch/agents/orchestrator/system.md`   | Its wording describes a “research task request”, which leaves room for Mission to create later expert/report tasks even though each Task starts the same scheduler.                                                                                                    | Define the sole Mission request as the complete research → two experts → explicit personas → deterministic report workflow. The downstream Task Orchestrator dispatches the research lead once, then owns all dependent dispatches in the same Task.                                                |
| `packages/opencorvus/script/benchmark/mirror-watch-real-e2e.mjs` natural request | The request asks the package to complete all stages but does not explicitly prohibit multiple Mission Tasks.                                                                                                                                                           | Require exactly one OpenCorvus Task, no phase-split child tasks, and no task-level model override.                                                                                                                                                                                                  |
| `observeMission()` in the owned E2E                                              | It rejects duplicate Task identifiers but permits multiple distinct Tasks until they create an overlapping writer.                                                                                                                                                     | Reject as soon as the Mission exposes more than one Task. Zero is valid only before the sole Task is created.                                                                                                                                                                                       |
| Terminal E2E assertions                                                          | They currently accept any positive number of successful Tasks and one DAG per Task.                                                                                                                                                                                    | Require exactly one terminal Task and exactly one Task DAG before artifact and visual acceptance.                                                                                                                                                                                                   |
| `mirror-watch-package.test.ts`                                                   | It pins model/checks/task-addressing prompt rules but not selected-squad Task ownership.                                                                                                                                                                               | Pin the global one-Task invariant and the package's complete-workflow request contract.                                                                                                                                                                                                             |

This is not a host gate or workflow state machine. It corrects the LLM instruction at the two prompt authorities and makes the owned benchmark reject the already-proven invalid topology. The package scheduler still makes evidence-based dispatch decisions through ordinary tools, and `prompt_profile.active` remains the only selected expert-squad source.

### Read-only verification correction after the `11-42-25-231Z` run

The first uninterrupted single-Task run proved the corrected topology through one Mission, one OpenCorvus Task, one Task Orchestrator, one production research lead, five required Skill calls, six successful iFind package-tool calls, and all four canonical research files. It then failed before research submission because the worker used `edit` while its reasoning said `Verifying JSON contract field alignment`; the persisted tool input targeted the human survey and supplied byte-identical `oldString` and `newString`. The edit tool correctly returned `No changes to apply: oldString and newString are identical`, and the owned E2E correctly rejected the failed production tool call.

The full owned-surface search found verification language in the research role and shared delivery contract, but neither distinguished read-only verification from defect repair. The atomic correction is:

| Call point                       | Correction                                                                                                                                                                                                                                                               |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Shared `delivery-contract` skill | Verification and proof use `read` only. `write`, `edit`, and `apply_patch` are production/repair mutations, never verification calls. A mutation is allowed only after identifying a concrete contract mismatch, and its replacement must differ from the current bytes. |
| Research role prompt             | Reread all four outputs with `read`; if they already satisfy the contract, submit without another mutation. Never call a mutation tool merely to confirm alignment or produce a no-op.                                                                                   |
| Owned package test               | Pin both shared and research-role language so the correction cannot regress.                                                                                                                                                                                             |

The benchmark oracle remains strict: any failed tool call is still a failed E2E. No host-side argument gate, no ignored error class, and no retry/continuation worker are added.

### Exact single-artifact repair correction after the `12-05-52-425Z` run

The next single-Task run again completed the five required Skill calls, exactly six successful iFind calls, four canonical writes, and four read-only verification calls with zero errors. Verification then found real contract mismatches: required AInvest Design System annotations were absent and recommendation matrix cells were weaker than the required verbatim field linkage. The worker attempted one `apply_patch` spanning four artifacts, multiple independent changes, and unchanged Markdown hunks. One expected HTML context did exist in the file, but the oversized multi-file patch failed its atomic context verification and became a failed production tool call.

The design annotations remain mandatory; deleting that acceptance condition would hide a real defect. The owned prompt correction instead defines a reliable repair boundary:

- repair exactly one owned artifact and one concrete mismatch per mutation call;
- immediately before that mutation, `read` the exact current target region and copy its current bytes into the edit/patch context;
- never include unchanged hunks, speculative context, or another artifact in the same repair;
- after each successful mutation, reread that artifact before deciding whether another distinct repair is necessary.

This remains LLM instruction and ordinary tool use. It adds no host gate, retry worker, fallback mutator, or state machine, and the E2E continues to reject every failed tool call.

### Embedded perspective boundary correction after the `12-28-25-481Z` run

The fourth single-Task run completed research naturally with zero failed tools, then dispatched exactly two expert surveyors in the same Task. One expert loaded the assigned compiled perspective but attempted three invalid reads before writing: the not-yet-created `process_answer` output directory and two logical source labels, `references/research/05-decisions.md` and `06-timeline.md`. The persisted reasoning explicitly called this “Inspecting directory structure”.

The direct cause is an ordering ambiguity in the adapted perspective skills. Their immutable source text contains historical “see this directory” wording before the later OpenCorvus adaptation section explains that all six files are embedded. Although the content snapshot is correct, the runtime boundary arrives too late to govern the model's first interpretation. The single-source correction is:

- place an authoritative OpenCorvus runtime boundary immediately after each perspective skill frontmatter, before the immutable source body;
- state that every `references/research/**` string is a logical citation label whose content is already present in the same Skill result, never a project/source filesystem path;
- prohibit directory/file existence probes for those labels and prohibit reading the source skill directory;
- make the expert role read only the assigned canonical survey as input, then write the three exact new output files directly; the Write tool creates missing parent directories, so the worker must not read an output directory as preflight.

The original seven-file source bytes and SHA-256 provenance remain embedded and unchanged. This is a precedence correction for the current package runtime, not a compatibility lookup or alternate source.

### Expert input-path dispatch correction after the `12-55-16-683Z` run

The next isolated headed run proved the complete research stage again: one Mission, one OpenCorvus Task, one Task Orchestrator, exactly six successful iFind calls, four successful canonical writes, four read-only verification calls, and a visible completed research worker. The same Task then dispatched exactly two expert surveyors. The Denis session failed its first project-input read by requesting the nonexistent `.opencorvus/authority.json`; it subsequently found the unrelated root marker `.opencorvus.authority.json`. The E2E rejected the first failed call immediately and captured `99-failure.png`.

The persisted delegated instruction omitted the canonical survey path entirely. It said only to work from the research lead's four outputs and the same F01-F10 recommendation, even though the expert role contract requires the Orchestrator to assign one exact canonical survey path and defines that survey as the worker's only project input. The direct trigger was therefore the worker's guessed authority-marker path, while the upstream design cause was incomplete `dispatch_agent` input construction. This is a dispatcher missing-input signal, so the worker prompt must not be made tolerant of discovery or alternate paths.

The full call-point search covered the package Orchestrator prompt, expert role prompt, shared delivery contract, manifest projection, owned package test, and real-E2E path oracle. The single-source correction is limited to the upstream Orchestrator contract:

- every expert `dispatch_agent` instruction must name `dashboard/data/ainvest-h2-feature-survey-agent.md` as the exact and only project input;
- it must name the assigned perspective Skill and the three exact owned output paths in the same instruction;
- it must state that the worker does not discover inputs, inspect `.opencorvus*`, or inspect output directories;
- the owned package test pins that complete dispatch contract.

No alternate authority file, path search, retry worker, host preflight gate, or compatibility alias is added.

### Research dispatch-scope correction after the `13-14-48-507Z` run

After the external reset conflict was reported, the package, owned tests, and record were restored to the worktree. A byte comparison showed the restored package exactly matched the latest isolated package snapshot, so work could resume without overwriting a contradictory package edit.

The next isolated headed run again proved one Mission, one Task, one research owner, five required Skill loads, and exactly six successful iFind calls with no alternate search provider. It then failed when the research lead called `read` on the nonexistent `dashboard/reports` directory. The persisted reasoning said it was inspecting the dashboard for existing artifacts or templates. The E2E immediately captured failure evidence and rejected the run.

The full delegated instruction proves the upstream scope leak: it first assigns only the four exact research outputs, then appends the complete end-to-end Original user input, including both expert output directories, the persona vote directory, and `dashboard/reports/ainvest-h2-report.html`. The research role already forbids creating or modifying those downstream artifacts, but it does not explicitly prohibit a directory/template preflight. The causal chain is therefore: downstream paths leaked into a research-only instruction → the worker treated them as candidate existing inputs/templates → it probed a not-yet-created downstream directory → `read` failed.

The focused call-point search covered the package Orchestrator dispatch contract, research role prompt, shared delivery contract, natural E2E request, and owned package test. The correction is prompt-only and preserves ordinary tool autonomy:

- the research `dispatch_agent` instruction contains the product question, iFind evidence contract, and four exact research output paths only; it must not append the full end-to-end request or name expert, persona, or report paths;
- the four research outputs are new owned files, never preflight inputs or templates;
- the research worker writes those exact paths directly and does not inspect `dashboard`, `dashboard/data`, `dashboard/reports`, or another output parent before writing; the Write tool creates missing parent directories;
- the owned package test pins both the upstream scope boundary and the research-role direct-write boundary.

No directory-creation preflight, path-existence fallback, retry worker, hidden path filter, or ignored read error is added.

### Mission session-error observability correction after the `13-24-58-560Z` run

The next isolated headed run selected Mirror Watch through the real Overlay and created Mission `d46b1a0558d77fe1`, but the Mission's second streamed model turn terminated with a persisted assistant `APIError`: the Hexin gateway returned HTTP 401 before any Task was created. The dedicated E2E did not inspect assistant message-level errors. It observed an unchanged running Mission with zero Tasks until the correct 480,000 ms no-activity timeout elapsed, then captured failure evidence. A direct saved-credential `/models` request and a separate real streaming `/chat/completions` request both returned HTTP 200 after the run, so the persisted 401 remains a real production failure rather than evidence that the current credential is permanently invalid.

The observable message-flow requirement applies before Task creation as well as inside dynamic workers. The harness already retrieves the Mission root messages during every `observeMission()` poll but its `visibleMessageSummary()` dropped `message.info.error`; consequently the activity signature and failure oracle could not see the terminal error that was already visible through the real session API. The benchmark correction is therefore:

- retain each visible assistant message's `info.error` in the summarized observable message stream;
- reject any production owner message with a nonempty error immediately, including the Mission root before a Task exists;
- exercise that exact rejection path in the dedicated contract self-test;
- retain the inactivity timer for genuinely silent, nonterminal stalls.

This does not retry authentication, normalize provider errors, synthesize Task state, or add a host workflow gate. It fixes the test's real message-flow observation surface so subsequent provider or model failures are reported at their actual trigger point.

### Research skill-load ordering correction in the `13-38-25-861Z` run

The next isolated headed run proved that the saved Hexin credential was inherited byte-for-byte by the child Node process, selected Mirror Watch through the real Overlay, created one Mission and one Task, and dispatched one visible research lead. The production research session then completed these calls in persisted order: load `mirror-watch-delivery-contract`, load `consulting-analysis`, load `ifind-search`, execute the first iFind request, load `mirror-design`, load `ainvest-design-system`. The delegated instruction said to load the contract and then the research/design skills, but did not enumerate the complete first-call sequence. The role prompt named all five and said “after those five”, yet the model interpreted the two design skills as deferrable until after evidence retrieval.

All five skills are required before the first iFind request by the user-visible contract. The correction is an explicit prompt sequence, not a host gate:

1. load `mirror-watch-delivery-contract`;
2. load `consulting-analysis`;
3. load `ifind-search`;
4. load `mirror-design`;
5. load `ainvest-design-system`;
6. execute the first exact iFind request.

The package Orchestrator must reproduce this sequence in the research `dispatch_agent` instruction, and the research role states that the two design skills are part of the five-call prerequisite rather than post-search preparation. The owned package test pins both prompt authorities. No Skill call is synthesized, reordered by the host, or treated as optional.

### SSE instance-lifetime failure exposed by the `13-38-25-861Z` run

The same run continued far enough to expose an independent OpenCorvus runtime defect. Both expert surveyors were dispatched inside the sole Task, but the headed Overlay showed `Overlay log upload failed` while the harness eventually received Undici `UND_ERR_HEADERS_TIMEOUT`. The server log proves the timeout was only the observable consequence: the task event stream repeatedly threw `Cannot access instance context through a closed instance cache lease` at `Bus.subscribe(Message.Event.Created, ...)` in `server/routes/orchestrator.ts`. The persisted database then retained both expert dispatches as `running` and Denis's `apply_patch` as `pending` when the isolated process exited.

The complete call-point inventory for this defect is:

| Call point                                        | Evidence and decision                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `server/routes/orchestrator.ts` task event stream | The Hono SSE callback installs five project-scoped `Bus.subscribe` handlers after the request callback's inherited Instance lease has closed. This is the exact throwing line and must remain project-scoped.                                                                                                |
| `server/sse.ts` shared SSE wrapper                | This is the sole server-side SSE primitive and all seven route streams pass through it. It currently forwards an asynchronously invoked callback without giving that callback an independently owned Instance lifetime. Fixing only the task route would leave the same lifecycle defect on sibling streams. |
| `project/instance.ts`                             | The current worktree already provides `runAsIndependentInstanceActivity({ directory, fn })`, which obtains its own read lease and deliberately runs outside the request's inherited context. Reuse this authority; do not add a second lease implementation.                                                 |
| `bus/index.ts`                                    | `Bus.subscribe` obtains its subscription map through `state()`, whose project key is `currentProjectDirectory()`. Removing this project binding or replacing it with global state would create cross-project leakage and is rejected.                                                                        |
| owned Mirror Watch real E2E                       | The real headed flow and server-log audit are the regression proof. The run must not contain closed-lease errors, overlay log upload failures, pending expert calls, or request header timeouts.                                                                                                             |

The single-source correction is to make the shared `streamSSE` wrapper capture the current project directory while the request Instance is valid, then execute the asynchronous Hono stream callback inside `runAsIndependentInstanceActivity`. The stream owns that lease until abort/cleanup, so callbacks and timers retain the same project authority without extending or reusing the closed request lease. This is a lifecycle fix, not a retry, fallback, timeout increase, or hidden error filter.

### Latest Agent Rail and source-template corrections after the `14-16-05-172Z` run

The next headed run verified the repaired SSE path: the Overlay selected Mirror Watch, created one Mission and one Task, displayed the research session's live reasoning and tool calls, and the isolated server emitted no closed-lease error. The production research lead also proved the corrected first-call sequence exactly: five required Skill calls in declared order, followed by six successful exact iFind calls with no alternate provider.

The run nevertheless stopped at the benchmark's first dynamic-Agent visual checkpoint. The current `ConversationAgentRail` renders only after at least three Agent message cards (`MIN_VISIBLE_AGENT_RAIL_MESSAGE_CARDS = 3`) and intentionally removed its tooltip in favor of the locate button's accessible `aria-label`. The owned E2E still waited for a rail stack immediately after the first research dispatch and then expected the retired tooltip. The failure screenshot proves the selected Task and research message/tool stream were already visible, so the direct trigger is an obsolete visual oracle rather than missing production dispatch. The correction is confined to the owned script: verify the first dispatch through the visible Agent card's identity/status, then verify exact session binding in the terminal rail after the full dynamic-agent set exists, using its `aria-label`. Do not reintroduce a tooltip or lower the production rail visibility threshold for the test.

The same run wrote `ainvest-h2-feature-recommendations.md` before benchmark cleanup. It contains ordered F01-F10 fields and live iFind evidence, but omits the source protocol's product-line table, H2 event table, exact `10 个推荐功能` container, competitive panorama matrix, and three-category summary. Repository search proves those requirements exist in `mirror-watch/agents/research-lead.md` and `mirror-watch/templates/feature-recommendations.template.md` but were not compiled into the new package delivery Skill or research role. This is an adaptation omission: the strict artifact oracle currently knows requirements that the production worker cannot see. Preserve the oracle and compile the source template structure into the single delivery contract and role prompt; do not copy the old runner, seed mode, fallback search stack, or demo bytes.

### Full worker capability and execution-lease correction after direct Task `tsk_f618323db001u5sg4n0R9JJ61s`

The user superseded the package-wide capability restriction: every Mirror Watch dynamic agent must inherit the complete normal `delegated-worker` built-in tool surface. WebSearch remains available in the agent template; only the concrete Task instruction may prohibit using it. The manifest therefore uses `inherit_base_tools: true` with no explicit built-in subset for all four workers. The package README, scheduler/worker overlays, and operational Skill boundaries preserve the Task's evidence-channel instruction instead of imposing their own WebSearch/provider ban. The package-specific iFind tool remains projected only to the research lead and remains the canonical implementation whenever the Task assigns iFind. The focused resolver test proves every available delegated-worker built-in—including WebSearch, Bash, Browser Preview, and Question—is projected while active package tools remain role-isolated.

The direct isolated Task was deliberately left undisturbed for five minutes and then inspected. It loaded the delivery contract and read the exact four-person authority, but both `dispatch_agent(target=mirror-watch-research-lead, work_scope={kind:task})` calls failed before worker-session creation with `TypeError: undefined is not an object (evaluating 'input.executionLease.taskID')`. The persisted tool parts prove this was deterministic infrastructure failure, not an iFind/model network error.

The failure process started at 00:42:39 while a parallel execution-lease cutover was being written across the shared tree. `agent/runner.ts` had made `executionLease` mandatory at 00:42:16, while the upstream delegated-worker and dispatch forwarders were written only at 00:43:02, 00:45:47, 00:46:17, and 00:46:50. The non-watch Bun server therefore cached a torn module graph: the consumer required the lease while the loaded producer path did not supply it. The direct cause is the runner dereference; the deeper cause is launching the real run before the parallel destructive interface cutover had landed atomically. No optional lease, fallback acquisition, compatibility adapter, or Mirror Watch-only bypass is acceptable.

The parallel core scope is not owned by this integration and must not be overwritten. Before another Task is published, the whole lease owner/forwarding change and its focused tests must be green. At the time of this record update, package acceptance is `11 pass / 0 fail`, repository typecheck has recovered to green, but the three shared lease/context/dispatch suites still report `3 pass / 12 fail` because their fixtures and handle assertions have not yet been updated. A main-database Task is therefore not yet publishable.

Read-only backend discovery found no running main OpenCorvus server. The only listeners are isolated databases on ports 4114 and 61272. The main database is `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`; its normal sidecar endpoint is `127.0.0.1:7878`, currently closed. Every subsequent real Task must use that endpoint only after `/global/health.paths.database` exactly matches the main database path. It must never be posted to either scratch backend.

### Mirror Watch round-three protocol correction

The independent round-three package review rejected the narrow green package test because the projected iFind schema still normalized `config_path`, result URLs admitted HTTP and embedded credentials, configuration limits had two public meanings, inactive SkillMount and real projected execution were untested, and payload/package-manager evidence remained red. The correction keeps one manifest-v1 package and changes no core dispatch, dynamic lease, frontend-replica, backend, or Overlay surface.

`lib/mirror-watch/project-path.ts` now owns the package's single portable path predicate and assertion. Both iFind and aggregate tool schemas call that predicate without `.trim()` or another parser, and both implementations call the same assertion before filesystem resolution. A valid package path is nonempty, has no leading/trailing whitespace or backslash, is neither POSIX nor Windows absolute/drive-rooted, contains no empty, `.` or `..` segment, equals its POSIX normalization, and has no Windows alternate-data-stream colon, reserved device segment (`CON`, `PRN`, `AUX`, `NUL`, `COM1`-`COM9`, `LPT1`-`LPT9`, including extensions), portable-forbidden/control character, or segment-trailing dot/space. After that lexical identity check, execution still proves project containment and rejects every symbolic-link or junction component. Inputs are rejected rather than aliased. The aggregate implementation's former second path parser was removed, so the package has one lexical path authority rather than parallel iFind/report semantics.

The explicit project configuration retains its exact nine top-level fields but the provider-facing public limits are no longer variable: `max_result_count` must equal `10`, `max_evidence_characters` must equal `2000`, and `request.dl` must equal `1999`. README, iFind Skill and validator state the same values. Result URLs must be valid HTTPS URLs without username or password. Invalid endpoint/result URL parsing emits a generic structural diagnostic rather than reflecting the submitted URL. Successful results and failed HTTP requests are both tested not to expose the configured authorization value.

The owned package test now resolves real research/report capabilities through `PromptProfileResolver`, obtains the actual projected `inputSchema` and tool `execute`, rejects whitespace/backslash/normalization/outside/POSIX absolute/Windows absolute plus ADS/device/illegal-character/control-character/trailing-dot-or-space inputs, and bypasses each schema once to prove both implementations independently reject invalid paths. It also executes a canonical iFind path through a junction to prove failure before any request. The projected aggregate tool executes the complete local cohort and creates the report without an external API. General resolves an actual inactive SkillMount and proves no Mirror Watch production skill is mounted. Direct injected-fetch unit cases prove the fixed configuration literals, HTTPS-only/credential-free result contract, exact evidence projection, and success/failure secret non-disclosure. No external API was called.

The official payload generator regenerated `packages/opencorvus/src/expert-squad/payload.ts`; generated equality and Git-index source-input checks pass. The canonical package-manager payload identity lists now include `legacy-remote/mirror-watch`. Three payload-release tests that previously inherited Bun's fixed five-second wall-clock timeout now disable that timer and use the existing phase-reset inactivity helper, so release, selector projection and stale-root handling measure silence after the last declared activity.

Current evidence is:

- Mirror Watch package: `12 pass / 0 fail / 355 assertions`.
- Mirror Watch plus payload generation account for `18 pass / 0 fail / 382 assertions` in the fresh combined run.
- Inactivity-supervised Mirror Watch, payload generation, prompt-profile resolver, skill-mount projection and package manager: `69 pass / 1 intentional skip / 0 fail / 926 assertions`.
- OpenCorvus TypeScript typecheck passes, and the payload tracked-input check confirms all twenty-one Mirror Watch package files are delivery inputs.

These results close the round-three implementation failures but are not called final package acceptance until a different read-only agent reviews the exact current tree. The real main-database Mirror Watch E2E also remains separate and unaccepted; this correction neither invokes an external provider nor substitutes unit evidence for that run.

### 2026-07-15 iFind secret-boundary rejection and correction Recall

The next independent exact-tree review rejected round-three acceptance despite 147 combined tests passing. `searchIfind` forwarded the raw transport exception message and serialized provider `status_code` / `status_msg` into model-visible errors. A local injected-fetch reproduction made a configured authorization value reappear through both surfaces. The prior HTTP 503 test was false-green because its fixed status text did not contain the configured secret; it did not prove the complete error boundary.

The correction owns one structural diagnostic boundary rather than scanning or replacing secret strings. Model-visible failures retain only the local iFind stage and, for an HTTP failure, the numeric HTTP status. Transport exception messages, HTTP status text and body, stream exceptions, JSON parser details, provider status values, invalid result values, URLs, titles, evidence and configured output limits are never interpolated into errors. Package-owned validation errors may retain static field/index names and fixed public literals. Successful output continues to project only the declared request identity and provider result schema, never configuration headers or request-policy fields.

Acceptance requires local no-network regressions with multiple distinct values in configuration headers and request policy. Each value must remain absent when a transport exception, provider status, HTTP status text/body, invalid JSON, invalid result field or stream exception attempts to reflect it; every failure must still identify its structural stage. At least one case must execute through the real projected package tool. Payload generation/freshness, tracked delivery inputs, active/inactive resolver and SkillMount coverage, package manager, typecheck and diff checks must be rerun. This correction does not touch dynamic execution leases, Multica, the running backend or the main database, and cannot be called accepted until a different agent reviews the exact tree.

Fresh correction evidence is entirely local and does not call the external provider. The focused Mirror Watch suite passes `13 / 13` tests with `482` assertions. The inactivity-supervised Mirror Watch, payload-generation, dynamic-agent registry, registry, prompt-profile resolver, SkillMount projection and package-manager run passes `148` tests with one intentional skip, zero failures and `1389` assertions. It includes the generated payload equality and Git-index tracked-input checks. OpenCorvus TypeScript typecheck passes. The injected failure matrix covers transport exceptions, both provider status fields, HTTP status text/body, content type, stream reads, response/output byte limits, UTF-8, JSON parsing, result channel/title/URL/evidence/metadata, and a real projected package-tool execution; every configured header and request-policy secret is absent from each model-visible error and from the ordinary successful projection. These are implementation and regression results only, not independent acceptance of the current tree.

### 2026-07-15 iFind confidentiality sink and non-cooperative inactivity Recall

The next independent exact-tree review rejected the preceding correction. A local no-network reproduction configured distinct authorization and request-policy values, then returned those values through otherwise valid provider `title`, HTTPS URL path, `summary` evidence and `publish_date` fields. `searchIfind` returned both values in its successful model-visible projection. The existing successful-output assertion was false-green because its provider fixture did not reflect configuration material. The same review found no focused regression for a header fetch that never settles, a body read that never settles, or caller abort; controller abort alone does not make a non-cooperative Promise settle.

The confidentiality correction must model explicit sources and sinks. Confidential sources are the endpoint, every configured header name and value, and every extra request key and primitive value except the three package-public fixed pairs `output: "block"`, `need_content: false` and `dl: 1999`. Model-visible provider sinks are `title`, URL, `publish_date` and evidence; URL inspection includes its percent-decoded visible expression. If any sink contains any confidentiality atom, the entire call fails with one fixed local-stage diagnostic. It must not replace text, remove a field, return partial success, scan for generic secret keywords, or interpolate an atom into the failure.

Header fetch, every body read and caller abort must settle through explicit Promise races even when the fetch or `ReadableStream` ignores the abort signal. Header inactivity begins with the request and body inactivity resets only after a received chunk. Every owned listener and timer must be removed, a pending reader must be cancelled and released, and late rejection must remain observed rather than becoming unhandled. Local regressions must cover each confidentiality source class across every declared success sink, URL percent encoding, an unchanged safe success, non-cooperative header/body operations, caller abort, and real projected package-tool confidentiality plus inactivity failures. The implementation scope remains Mirror Watch iFind library/tests, package README/Skill, generated payload and this record; dynamic execution leases, Multica, the running backend and the main database are excluded.

Implementation follows the full call-point inventory already recorded above and the independent review's exact runtime trace: `tools/ifind-search.ts` calls `searchIfind`; `PromptProfileResolver.packageToolFromDefinition` returns only package output/constant metadata on success; `session.processor` converts failures through `toolFailureCauseFromUnknown`, whose persisted error surface contains `name` and `message` but not raw `stack` or `cause`. Payload generation imports the same package files and the generated-equality/tracked-input tests remain the delivery freshness authority. Final evidence must rerun the focused Mirror Watch suite, the seven-file package/registry/resolver/mount/manager/payload combination, OpenCorvus typecheck and `git diff --check`, then stop for a new independent exact-tree review.

The implementation now derives typed confidentiality atoms directly from parsed configuration rather than from generic keywords. Endpoint raw/canonical/percent-decoded expressions and header names/values are included; header names use ASCII-case-insensitive comparison, while endpoint, header values and extra request keys/primitive values use exact comparison. Only the three exact public request pairs are excluded. Each provider result is validated first and then checked at `title`, raw and percent-decoded URL, evidence and present `publish_date`. A present invalid or blank `publish_date` rejects the complete call instead of being silently omitted. A matching atom rejects the complete call with the fixed field-local diagnostic and no replacement, deletion or partial result.

The request and stream lifetimes now race non-cooperative operations against their owned abort signal. Header inactivity aborts from the last header activity boundary, body inactivity resets only after a received chunk, and caller abort maps to a fixed local diagnostic rather than forwarding its reason. Every race removes its listener; all timers are cleared; an unfinished body reader is cancelled, its pending read and cancellation rejection remain observed, and its lock is released. The focused regression deliberately leaves header fetch and body pull promises unresolved, asserts prompt settlement and exactly one stream cancellation, then rejects both operations late with configuration material to prove that no late rejection becomes unhandled.

Final local no-network evidence on the post-generation tree is:

- focused Mirror Watch package: `15 pass / 0 fail / 2671 assertions`;
- focused package plus generated payload: `21 pass / 0 fail / 2698 assertions`;
- inactivity-supervised Mirror Watch, payload generation, dynamic-agent registry, registry, prompt-profile resolver, SkillMount projection and package manager: `150 pass / 1 intentional skip / 0 fail / 3579 assertions`;
- OpenCorvus `tsc --noEmit`: pass;
- `git diff --check`: pass.

The generated payload equality, tracked delivery inputs, real projected package-tool confidentiality rejection, projected non-cooperative header inactivity, inactive package isolation and safe complete success are all exercised in those runs. This remains deterministic implementation evidence, not a real chain E2E or independent acceptance. No backend was started and no Task was published. By the latest runtime constraint, every future real publication or E2E must use the main backend and main database; an isolated backend may only run deterministic code tests and cannot count as chain acceptance. A different agent must now review the exact current tree before this correction can be accepted.

### 2026-07-15 iFind canonical-wire and projected-output correction Recall

The next independent exact-tree review rejected the preceding implementation. Configuration validation retained headers as a plain object, while execution constructed a different header object with a separately injected content type; request confidentiality atoms were derived with `String(value)`, while fetch serialized a separately assembled JSON object. This allowed case-duplicate headers, normalized header values and JSON number semantics to diverge between validation, confidentiality inspection and the actual wire request. Empty request strings and non-finite parsed numbers were also accepted. The package therefore did not have one canonical transport representation.

The correction has one request construction path. Validation must build one `Headers` instance, reject invalid HTTP field names/values and case-insensitive duplicates with fixed structural diagnostics, reject a configured content type, and append the package-owned JSON content type. Execution passes that exact `Headers` instance to fetch. Raw configured header name/value pairs and the canonical `Headers` name/value pairs are both confidentiality sources. Request policy accepts only nonempty strings, finite numbers and booleans. At execution the complete request object is serialized once and parsed once; those exact bytes are the fetch body, while the parsed object plus the raw configured pairs supply request confidentiality atoms. Float, negative, zero and boolean values retain their JSON meaning. The three public pairs remain exempt only by exact key-and-value pair, so the same value under another key is confidential.

Caller abort must fail before a fetch operation is created when the signal is already aborted, including through the real projected tool. A non-cooperative fetch that resolves or rejects after inactivity/caller abort remains observed; a late resolved response has its body cancellation started and that cancellation's rejection observed. A response body not consumed because HTTP or content-type validation fails is handled under the same ownership rule. No timer, listener, reader or late promise may become an unhandled operation.

The package owns a fixed maximum projected output below the host `PromptProfileResolver` path's `Truncate.output` ceiling of 50 KiB, without importing that host implementation. Configuration must equal the package constant. The complete successful result is serialized as compact one-line JSON, measured in UTF-8 bytes and rejected before return when it exceeds the package maximum. The package tool returns those exact bytes; it must never rely on host truncation and must never return a partial result. Projected-tool tests must prove `metadata.truncated === false`, embedded newlines remain JSON escapes, and both sides of the package byte boundary.

The complete call-point inventory for this correction is:

| Call point                                              | Required decision                                                                                                                                                                                                                                                              |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `lib/mirror-watch/ifind-search.ts` configuration parser | Own the canonical `Headers`, raw/canonical confidentiality sources, finite primitive request policy and fixed package output ceiling.                                                                                                                                          |
| `lib/mirror-watch/ifind-search.ts` request execution    | Serialize and parse the complete request exactly once, send those exact bytes and the same `Headers`, fail before fetch on pre-abort, and observe/cancel every late operation.                                                                                                 |
| `lib/mirror-watch/ifind-search.ts` result projection    | Validate every sink including raw and percent-decoded URLs, compact-serialize the complete result, enforce the UTF-8 package ceiling, and reject whole-call disclosure or overflow.                                                                                            |
| `tools/ifind-search.ts`                                 | Return the library's canonical compact serializer output, not an independently pretty-printed representation.                                                                                                                                                                  |
| package README and iFind Skill                          | Publish the same header/request/output contract without introducing a second policy source.                                                                                                                                                                                    |
| `PromptProfileResolver.packageToolFromDefinition`       | Remain unchanged; the real projected execution test proves the package output reaches its existing 50 KiB truncation boundary intact and reports `truncated: false`.                                                                                                           |
| owned Mirror Watch package test                         | Cover SP/HTAB normalization, header case/duplicates/token/control rejection, empty/non-finite request values, JSON primitive wire fidelity, exact public-pair semantics, raw/decoded URL sinks, pre-abort, late resolve/reject ownership, compact output and UTF-8 boundaries. |
| generated payload and package freshness tests           | Regenerate from the same package sources and prove the tracked delivery bytes match.                                                                                                                                                                                           |

Acceptance remains local and network-free for this correction: focused Mirror Watch tests first, then the seven-file package/registry/resolver/mount/manager/payload run, OpenCorvus typecheck, payload freshness and `git diff --check`, followed by exact-tree self-review and a new independent review. It does not touch dynamic leases, MCP fixtures, Multica, the running backend, Overlay, database or Task records. No backend is started and no Task is published. Any later real Task must use the main backend and main database only.

The implemented correction now constructs one `Headers` instance during configuration validation, rejects case-insensitive duplicate names and invalid field names/values with fixed diagnostics, appends the package-owned JSON content type, and passes that same instance directly to fetch. Both raw configured header pairs and the canonical configured pairs are confidentiality sources. The complete request is serialized once and parsed once; the exact serialized bytes are the fetch body, while raw configured primitives and their parsed canonical wire values supply request atoms. Empty strings and non-finite numbers fail before fetch, while finite floats, negative numbers, zero and booleans retain their JSON values. Each fixed public pair is exempt only under its declared key; `"block"`, `false` and `1999` each become confidential when independently configured under another key.

An already-aborted caller fails at function entry before configuration or fetch, and a caller that aborts during configuration is rechecked before request construction. Projected execution proves global fetch remains uncalled for the pre-aborted case. A fetch that rejects late remains observed; a fetch that resolves after inactivity has its response body cancellation started, and a rejecting cancellation is observed. Pending body reads and cancellation failures remain observed and owned. HTTP and content-type failures likewise cancel their unconsumed response body.

`IFIND_MAX_TOOL_OUTPUT_BYTES` is the sole package output limit at 49,152 UTF-8 bytes, below the unchanged host `Truncate.MAX_BYTES` value of 50 KiB. Configuration must equal that constant. `serializeIfindSearchResult` is the sole package result serializer and returns compact one-line JSON; both the library validator and projected package tool use it. The real projected wrapper reports `metadata.truncated: false` with no `outputPath` below the boundary, preserves embedded newlines as JSON escapes, and receives no output above the boundary because the complete call fails first.

Final local no-network evidence on the regenerated payload tree is:

- focused Mirror Watch: `16 pass / 0 fail / 4481 assertions`;
- focused Mirror Watch plus generated payload: `22 pass / 0 fail / 4514 assertions`;
- inactivity-supervised Mirror Watch, payload generation, dynamic-agent registry, registry, prompt-profile resolver, SkillMount projection and package manager: `151 pass / 1 intentional skip / 0 fail / 5394 assertions`;
- OpenCorvus `tsc --noEmit`: pass;
- owned-tree `git diff --check`: pass;
- historical docs links: `19 pass / 1 fail`; the sole failure reports thirty pre-existing paths under `.scratch/o4/**` and `.scratch/opentest-message-20260714-*/**`. Those non-owned snapshots were neither created nor deleted by this correction and remain an explicit repository cleanup debt rather than a green result.

The official generator updated `packages/opencorvus/src/expert-squad/payload.ts`, and generated equality, TypeScript text embedding, tracked delivery inputs and Bun build resolution all pass. Exact-tree self-review found no remaining known issue inside the seven owned files. This is deterministic correction evidence only. It does not claim real main-database E2E acceptance, and a different agent must review the exact current tree before the iFind correction is accepted.

### 2026-07-15 current-tree main-database E2E and canonical report acceptance

The first final independent reviewer rejected the earlier `tsk_f61968425001Lft73S7hwPEHUE` evidence because the Task completed at `2026-07-14T18:45:24.533Z`, while the canonical-wire iFind correction, focused test and generated payload were written between `18:46:58Z` and `18:47:31Z`. That rejection was correct: a real run that predates the delivered bytes cannot accept the delivered bytes.

The operator removed every generated output from the earlier run while preserving `dashboard/data/personas.json`, rechecked `GET /global/health`, and proved that port 7878 named the main database `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. It then published current-tree Task `tsk_f6201931d001GeVH5FfZrQJAEr` with request ID `mirror-watch-main-db-current-tree-e2e-20260715-01`, project `7aeebab7cd7c8ab17ba49a62fd3f4595c94e7830`, root session `ses_09dfe6bf8ffe5BRrAoIQpGxvHc`, profile `mirror-watch` and live-tested model `hexin/gpt-5.5`. The Task-level request made iFind the only external search channel and prohibited WebSearch without narrowing any worker template.

The Task regenerated the four research outputs from an empty output surface, four exact persona votes, and both expert Markdown/HTML/data triplets. The owned main-backend process ended externally during the first Denis worker session without a shutdown record or exception stack. The operator started a new hidden main-backend process against the same database and project; scheduler recovery retained the same Task and Orchestrator session, created a replacement Denis worker session, and completed the remaining chain. No Task, session, artifact or output was deleted to hide the interruption. Task `tsk_f6201931d001GeVH5FfZrQJAEr` reached `completed` with no error at `1784058155708`.

The Task exposed one operator-request error during second review. Its concrete request incorrectly asked for a self-contained final report even though the source Mirror Watch contract is a single HTML file with the one declared Chart.js 4.4.0 CDN. The package tool correctly created the source-compatible report and returned SHA-256 `f2b5e9b5ac0b5223266d6de00910cac86dfcdfcb130f4d86c0da828a85c487d2`; a later Task-directed manual self-contained rewrite changed the file SHA to `6dc9bcf5a150e1b46b2570055db17bc93fd4baffd3f9833f15ebdb872284adc1` and violated the report analyst's no-edit contract. The operator rejected that post-processing, removed only the completed report, and published main-database canonical-report Task `tsk_f6229b07d001Q3mv73kBI9fZtL` with request ID `mirror-watch-main-db-canonical-report-20260715-01`.

The canonical-report Task consumed the current full-chain inputs, dispatched only `mirror-watch-report-analyst`, called `mirror-watch/shared/aggregate-report` exactly once in session `ses_09dd5f599ffeHNGzpwQjkOIsBq`, and completed at `1784058429213`. The persisted package-tool result and the final disk file both have SHA-256 `f2b5e9b5ac0b5223266d6de00910cac86dfcdfcb130f4d86c0da828a85c487d2`. Its canonical result records `schema_version: 1`, four persona votes, two expert votes, total persona score 24, ten co-selection bundles, four representative quotes, the complete zero-preserving tier/status/mode/frequency matrices, and the report path `dashboard/reports/ainvest-h2-report.html`. No message or file edit follows the successful package-tool result in that Task.

Final deterministic verification on the same tree is:

- Mirror Watch package plus generated payload: `22 pass / 0 fail / 4514 assertions`;
- OpenCorvus TypeScript typecheck: pass;
- `git diff --check`: pass;
- historical-doc links, document health and product-doc single source: pass;
- main database full-chain Task: completed with no terminal error;
- main database canonical-report Task: completed with no terminal error and exact tool/file digest equality.

The four-person cohort remains a deliberately reduced real E2E sample. It is not the absent 105-person production survey and does not prove production-scale concurrency. The final report intentionally retains the source algorithm's single declared Chart.js 4.4.0 CDN and does not introduce an alternate CDN, local substitute or fallback.

### 2026-07-15 iFind trusted-provider and arbitrary request-key correction Recall

The next independent exact-tree review rejected the canonical-wire correction. Its success-sink confidentiality scanner treated configuration names and primitive values as forbidden substrings in provider content. Ordinary results containing `true`, `73`, `application` or header-like language therefore failed despite satisfying the provider schema. Token boundaries, minimum lengths, allowlists, keyword classifiers and redaction would only replace one false-positive policy with another and are explicitly forbidden.

The enforceable confidentiality contract is structural. The client does not copy its endpoint, headers or configured request policy into the result object, and every client-generated failure uses a fixed structural diagnostic that does not interpolate transport exceptions, provider error values or configured values. The provider is the trusted content-protocol participant: valid provider title, URL, publish date and evidence are returned as provider content. If a deployment does not trust that provider, this client cannot infer whether ordinary provider prose was independently authored or maliciously reflected from the request; the package therefore makes no claim to detect malicious provider reflection. This information-flow limit must remain explicit instead of being hidden behind content inspection.

The same review found a separate transport defect. Configuration request entries were copied into a normal object by property assignment, so the valid JSON key `__proto__` changed the object's prototype instead of becoming an own wire property. Request policy must be retained as entries and materialized with `Object.fromEntries`, with dynamic tool fields applied by the same entry composition. There is no dangerous-name blacklist: `__proto__`, `constructor` and `prototype` are valid JSON keys and must be own properties in the single serialized request body. The existing structural ownership rule remains: configuration cannot redefine the call-owned `query`, `channels`, `size`, or `qid` fields, because silent last-write replacement would create two sources. The complete request is serialized once, parsed once for structural verification, and the same serialized bytes are sent to fetch.

This correction is limited to the Mirror Watch iFind library/tool documentation, package README and Skill, owned package test, generated payload, this Recall and the July record index. It removes all confidentiality-atom types, collection, substring comparison and reflection-rejection tests. Replacement regressions prove ordinary same-text content succeeds, arbitrary prototype-like keys survive the exact wire, fixed errors do not interpolate distinct high-entropy configuration values, and the serializer accepts exactly 49,152 UTF-8 bytes while rejecting 49,153. It does not touch dynamic execution leases, Model Context Protocol code, Multica, Overlay, the running backend, the main database or Task records, and it does not call an external API.

Acceptance requires the focused Mirror Watch test, the existing package/payload/registry/resolver/mount/manager combination, OpenCorvus typecheck, payload freshness, documentation health and scoped `git diff --check`. The main-database E2E remains pending and its existing failed-run evidence must not be described as acceptance. A post-test self-review and another exact-tree non-author review remain required.

The implemented correction stores validated request policy as ordered entries and creates the complete request with one `Object.fromEntries` call. It serializes once, parses those same bytes once to verify every configured and call-owned name remains an own property, then sends the original serialization as the only fetch body. The four call-owned names remain structurally reserved and each rejects before fetch. `__proto__`, `constructor` and `prototype` are not special-cased; the regression proves all three exact names and values appear in the serialized body, remain own properties after parsing and do not change the parsed object's prototype.

All success-sink confidentiality machinery is deleted from the source package and generated payload: there is no atom type, atom collector, percent-decoded content scan, substring comparator or reflection rejection. Direct and real projected-tool regressions return valid provider title, URL and evidence containing `true`, `73`, `application` and header-like language. Failure tests inspect only distinct high-entropy configuration values and prove fixed transport, provider, parser, stream and structural errors do not interpolate them. The output serializer accepts an exact 49,152-byte UTF-8 result and rejects the corresponding 49,153-byte result.

Final local no-network evidence on the regenerated payload tree is:

- focused Mirror Watch: `17 pass / 0 fail / 623 assertions`;
- payload generation: `6 pass / 0 fail / 27 assertions`;
- inactivity-supervised Mirror Watch, payload generation, dynamic-agent registry, registry, prompt-profile resolver, SkillMount projection and package manager: `152 pass / 1 intentional skip / 0 fail / 1530 assertions`;
- OpenCorvus `tsc --noEmit`: pass;
- owned-tree `git diff --check`: pass;
- document health plus historical links: `72 pass / 1 fail`; the sole failure is the already recorded scratch snapshot scan over `.scratch/o4/**` and `.scratch/opentest-message-20260714-*/**`. The current record link, monthly tracked-file index and all other document-health assertions pass. This correction did not create, delete or reinterpret those non-owned snapshots and does not report the documentation batch as fully green.

Exact-tree self-review found no remaining scanner residue, key-name special rule, dynamic-field last-write behavior, stale payload text or main-database acceptance claim in the owned surfaces. This is still deterministic implementation evidence rather than real E2E acceptance. It publishes no Task, calls no external API and does not touch the backend, database, dynamic lease or Model Context Protocol implementation. A different non-author must review the final tree before this correction can be accepted.

### 2026-07-15 parallel-overlap invalidation and exact-current-tree rerun Recall

The independent reviewer and operator found a real parallel-edit overlap after the earlier full-chain evidence. The research worker in Task `tsk_f6201931d001GeVH5FfZrQJAEr` ran from `2026-07-14T19:03:42Z` through `2026-07-14T19:17:45Z`, but the current iFind source was written at `2026-07-14T19:31:15.9554547Z`, the generated payload at `2026-07-14T19:31:46.5625526Z`, and the focused test at `2026-07-14T19:35:11.6025909Z`. Therefore Tasks `tsk_f6201931d001GeVH5FfZrQJAEr` and `tsk_f6229b07d001Q3mv73kBI9fZtL` remain useful diagnostic and canonical-report evidence, but they do not accept the exact current bytes. The operator will not edit the overlapping iFind, payload or focused-test files and will instead rerun from an empty generated-output surface after those bytes remain stable.

The earlier full-chain session tree also retained one superseded `dispatch_agent` part in `running` and one failed Denis-session `write` part in `pending`, even though the engine Task reached a terminal completed row and a replacement Denis session succeeded. The exact-current-tree rerun therefore adds a direct terminal-evidence requirement: every persisted tool part in the final Task session tree must be terminal, with no `running` or `pending` residue. This is an observable-message-flow requirement, not a compatibility path or gate.

The same review proved that all three earlier iFind package-tool calls failed before transport because the isolated Task configuration still declared the retired `max_tool_output_bytes: 48000` value while the exact current protocol requires `49152`. The prior recommendations correctly exposed external facts as `数据缺失 (N/A)`, but that honesty does not prove the search chain. The operator corrected only the isolated E2E configuration to the canonical fixed value before publishing the rerun. Acceptance now requires at least one persisted completed iFind package-tool result from the exact current tree; an all-N/A report or configuration-stage failure cannot pass.

The rerun remains constrained to the main backend at port 7878 and the main database `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. It must preserve the exact four-person `dashboard/data/personas.json` cohort, use iFind as the only external search channel, retain full delegated-worker templates, forbid WebSearch only in the concrete Task, generate the source-compatible single HTML report with exactly the declared Chart.js 4.4.0 CDN, call the canonical aggregate tool exactly once, and leave the successful package-tool report unedited. Final acceptance still requires current-tree digest equality, deterministic tests, fresh visual screenshots, independent review, scoped commit and legacy remote push.

### 2026-07-15 durable-goal and exact expert-source correction Recall

After the parallel commit `d1e1ced9170ce94b43195796f8f02829ca411be5` settled, the operator rechecked the current worktree, file digests, main backend and main database before further edits. The Mirror Watch package, payload, focused test and record were unchanged after `2026-07-14T19:58:36Z`; the active backend health response still named `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. No overlapping Mirror Watch writer remained visible, so the earlier conflict stop condition no longer prevented a new package correction.

Main-database Task `tsk_f6231e9b0001zwrStR3aBPHafb` supplied two new production failures. First, its Orchestrator called `manage_task(action=add_goal)` before any requirements or architect snapshot existed. The tool returned the exact host contract: `add_goal: no active requirements/architect spec snapshot`; the active Mirror Watch package exposed only four delegated-worker domain identities and could not dispatch the required core adapters. The model then changed every expert and persona dispatch to `work_scope:{kind:"task"}`. Persisted sessions prove that the two experts and four personas ran serially, so the manifest's `disjoint_goals` policy and source workflow's independent parallel survey semantics never executed. Task-scope dispatch is a fallback and cannot be accepted.

The full repository inventory covered `goal-lifecycle-tools.ts`, `projected-agent-dispatch.ts`, `dispatch-agent-tool.ts`, requirements/architect stage adapters and prompts, execution leases, registry topology validation, every project expert-squad manifest with goal-scoped nodes, MirrorTest and Goal Development package prompts, Mirror Watch manifest/README/Orchestrator/worker prompts, the dedicated package test, payload generation, and resolver/catalog/mount projection tests. Every current package that successfully owns a durable goal workflow explicitly projects requirements and architect runtime templates before goal-scoped implementers. Relaxing the host snapshot requirement, allowing the scheduler to synthesize goals, adding a package workflow tool, or silently using task scope would create a special case, second workflow source, or fallback and is rejected.

The single-source correction is package projection. Add `mirror-watch-requirements-analyst` with `base_role: requirements` and `mirror-watch-workflow-architect` with `base_role: architect`, both full inherited runtime templates and the shared delivery-contract Skill. Their only responsibility is to persist the acceptance contract and initial executable graph. The architect creates one goal for the research four-pack, one per assigned expert perspective, one per exact authority persona, and one final report goal with source-workflow dependencies and disjoint owned paths. All four domain identities remain the only artifact producers, but their virtual-workflow dispatch scope becomes `goal`; research/report retain `goal_concurrency: single`, while expert/persona retain `disjoint_goals`. The scheduler dispatches planning roles task-scoped, then uses exact durable goal IDs. It must not call `add_goal` to manufacture the initial graph or fall back to task-scoped domain dispatch.

The same Task completed three real iFind calls and produced all research/persona/expert inputs, but the sole authorized aggregate call failed with `Mirror Watch aggregation rejected: expert perspective sources must be exactly denis-globa-perspective, oleg-mukhanov-perspective`. The Denis vote used `persona_source: "package perspective skill"`; Oleg used `"oleg-mukhanov-perspective package skill"`. The source `WORKFLOW.md`, source expert template and production aggregator all define `persona_source` as the exact assigned `PERSPECTIVE` identity. The package expert prompt and shared delivery contract required the field but failed to state that its value must be copied byte-for-value from the assigned visible Skill name. The correction makes `persona_source` exactly `denis-globa-perspective` or `oleg-mukhanov-perspective`, makes `name` exactly `Denis Globa` or `Oleg Mukhanov` for that assignment, and pins both values in the focused regression. It does not relax the aggregator or edit generated votes after failure.

The failed report worker also returned a terminal worker result after the aggregate tool error, after which the scheduler dispatched a second report worker. The package already prohibits replacement after terminal failure; the scheduler correction now states the observable consequence explicitly: a failed domain goal or failed sole aggregate call is reported through `manage_task(action=fail_task)` with the exact persisted evidence and is never replaced or retried. This remains prompt-level domain policy over real task/goal evidence, not a host gate.

Acceptance for this correction requires source-package loading with the two planning projections, exact adapter identities, real resolver/catalog/SkillMount isolation, all domain workflow nodes goal-scoped, full delegated-worker capabilities unchanged, exact expert identity-value regressions, fresh generated payload equality, typecheck and documentation checks. The decisive E2E is a new main-database Task from an empty generated-output surface whose session tree proves requirements and architect terminal artifacts, durable goals, goal-scoped expert/persona concurrency, completed iFind evidence, one successful aggregate call, exact tool/file digest equality, no pending/running residue, and no post-tool report edit. Visual screenshots and independent review remain mandatory.

### 2026-07-15 architect graph activation failure Recall

Main-database Task `tsk_f62504d5c001l6mUnAUeUZcF22` proves that adding the two standard planning projections fixed only the first half of the lifecycle. `mirror-watch-requirements-analyst` completed, `mirror-watch-workflow-architect` completed, and the architect adapter persisted eight goals plus seven contracts. The first required goal-scoped dispatch then failed with `Task tsk_f62504d5c001l6mUnAUeUZcF22 has no active plan for goal-scoped execution`. The Task correctly failed instead of falling back to task scope.

The full call-point inventory covers `orchestrator/architect-stage.ts`, `engine/persist.ts:createActivePlanGraph`, `orchestrator/build-tool.ts:createExecutionRunRecord`, `orchestrator/dispatch-agent-tool.ts:acquireDispatchAgentExecutionLease`, `engine/projected-agent-execution-lease.ts:activePlanGoals`, `engine/store.ts:findActivePlanForTask`, the architect dispatch contract and structural-reentry validation, active-plan graph tests, projected execution-lease tests, scheduler capability tests, and the current data write-boundary record. The architect stage currently writes the new spec, goals, contracts and fidelity in one transaction but never promotes those executable goals to an active plan. Build's private lazy-run path is the only production caller that creates an active plan; projected goal-scope admission occurs before any adapter executor, so the Build path cannot repair the missing plan for itself or for non-Build package roles.

The single-source correction is to promote the architect's exact persisted goal set to the active plan inside the existing architect transaction through `engine/persist.ts:createActivePlanGraph`. That writer remains the only plan-table owner. Build run creation must bind its run to that already active plan instead of creating a duplicate graph; its retired lazy plan-creation branch is unreachable because projected goal admission runs first, so it is deleted rather than retained as a seeded-goal fallback. Build must reject a missing plan, an active plan whose spec snapshot differs from the current active spec, or a plan whose goal set differs from the current graph. The execution lease remains strict and receives no fallback, bypass or special Mirror Watch rule.

Acceptance requires a dedicated new test owned by this migration that proves an architect result leaves a real active plan containing every persisted goal and that the next goal-scoped projected lease is admitted. Existing unrelated tests are not edited to manufacture coverage. The focused package test, active-plan and projected-lease suites, payload equality, typecheck, main-database rerun, visual review and independent read-only audit remain required.

Main-database rerun `tsk_f6261a77f001pMOmcXISrVnuj1` proved the plan activation correction in the real chain: requirements and architect completed, eight goals were attached to an active plan, research ran goal-scoped, two experts overlapped in time, and all four persona workers completed. The sole aggregate call then rejected `expert_vote_paths[0].reason screening table must contain exactly one ordered row for every F01-F10 feature`. The Denis Markdown's visible outer analysis had the correct ten-row table, but the fenced JSON `reason` replaced that table with one prose paragraph mentioning F01-F10. The aggregator intentionally validates the JSON reason because it is the canonical expert vote payload.

The exact correction is package instruction clarity, not aggregator relaxation or output repair. Both the expert role prompt and delivery Skill now state that the JSON `reason` value itself must contain the Markdown screening table with exactly one F01-F10 row in order; outer-page tables and prose enumerations do not satisfy it. The dedicated package regression pins that instruction, and the existing deterministic aggregator regression continues to prove the strict table parser.

### 2026-07-15 positioned perspective reference failure Recall

Main-database Task `tsk_f627d7a3a001Da5UMPd8ZIsvAV`, published through the current-source backend bound to `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`, proved the architect plan, goal-scoped dispatch and ordered JSON screening corrections. Requirements, architect, research, both concurrent experts and all four persona workers reached terminal completion; the research session retained completed iFind package-tool results. The sole report worker then called `mirror-watch/shared/aggregate-report` exactly once. The tool correctly rejected `expert_vote_paths[1].reason Pick 2 must cite a positioned package perspective item`, and the worker exposed that terminal error without retry, input repair, fallback aggregation or report creation.

The full call-point inventory covers the expert worker prompt, delivery-contract Skill, both embedded perspective Skills' package-adaptation citation rule, `POSITIONED_PERSPECTIVE_REFERENCE` and `assertExpertAnalysis` in the aggregate library, the focused rejection test, and both generated expert Markdown votes. The validator requires an exact logical file plus integer item reference such as `05-decisions.md#4`. The Oleg JSON reason used labels such as `01-writings.md# regulated fintech`, `05-decisions.md#关键发现`, `05-decisions.md#决策4`, `03-expression-dna.md#6.4` and `01-writings.md#五-FULL`; those labels identify headings or decimals but not the package adaptation's numbered item contract. The earlier prompt said only “a positioned reference such as `05-decisions.md#4`”, so the model could satisfy its prose interpretation while violating the deterministic parser.

The correction remains instruction clarity rather than validator relaxation or generated-output editing. The expert prompt and shared delivery Skill now define the accepted logical files, require `#N` with integer `N` from 1 through 10 inside every Pick section of the canonical JSON `reason`, explicitly reject heading labels, decimals and mixed labels, and require at least three distinct valid references across Pick 1-3. The focused package test pins the same wording while the existing aggregate regressions continue to own parser behavior. After regenerating the payload and passing deterministic tests, all generated outputs except the authority personas file must be removed and a new ordinary Task must be published to the same main database; no failed Task or vote is reused as acceptance.

### 2026-07-15 eight-goal requirements contract failure Recall

Main-database Task `tsk_f629e8742001EO4putLdT5ZQYX` failed before any domain artifact worker ran. Requirements completed with twelve requirements and twenty foundational decisions. The first architect session finalized five goals: research, Denis, Oleg, one combined `Four authoritative persona votes` goal, and report. The Orchestrator correctly identified that the combined persona goal violated the package scheduler contract and requested structural re-entry with persisted spec and wrong-boundary evidence. The second architect repaired ordinary contract/traceability blockers but finalized the same five ownership goals. The Orchestrator then failed the Task explicitly rather than dispatching the combined persona goal, falling back to task scope, inventing goals through `manage_task add_goal`, or claiming persona concurrency.

The full call-point inventory covers the package requirements and architect overlays, the core Requirements decision protocol, `architect/agent.ts:goalCountContractFromRequirementDecisions`, the `architect_goal_min_count` decision key, architect prompt rendering, output-tool minimum-count enforcement, structural re-entry fields and tests, and the focused Mirror Watch prompt regression. The core Architect deliberately has no default goal-count target and accepts one complete goal unless Requirements supplies an explicit count contract. The package architect overlay prohibited a batch persona goal, but the package requirements overlay did not register the core-supported count decision. Consequently both architect passes could rationalize the five-goal graph as structurally valid; the second pass had no new executable minimum-count fact to force the split.

The single-source correction is prompt-level structured input, not a host gate or Mirror Watch-specific validator. Requirements must register exactly one foundational decision `architect_goal_min_count=8`, with the source ownership reason: one research goal, two expert goals, four separate persona goals for the four authority objects, and one report goal. The architect overlay now states the same eight real ownership boundaries unambiguously, forbids any persona goal from owning more than one authority object or vote path, and forbids satisfying the minimum with generic or duplicate goals. Existing core code remains the only interpreter and enforcer of the decision. Focused tests pin both overlays; payload regeneration, deterministic tests, typecheck and another empty-output main-database Task remain required.

### 2026-07-15 Task07 successful chain and terminal tool-error Recall

Main-database Task `tsk_f62ab740b001c8cn06yiGCqPHv`, published through the current-source backend whose `/global/health.paths.database` exactly matched `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`, completed the eight-goal graph successfully. Requirements registered `architect_goal_min_count=8`; Architect created and activated exactly one research goal, two separate expert goals, four separate persona goals, and one report goal. Real iFind package-tool calls completed, the independent expert/persona sessions reached terminal success, and `mirror-watch/shared/aggregate-report` ran exactly once. Its report SHA-256 was `c99cb0fe48906048d3563515bf6c492cc393b4786f58a0f7a6c0df5f90f91c78`, persona score total was 24, all 180 tool parts were terminal, and no write occurred after aggregation.

The run is not final acceptance because three terminal tool errors remain visible. The initial requirements `dispatch_agent` input contained delegated-worker fields `instruction` and `evidence_refs`, architect fields, and explicit null optional fields; the target's strict schema accepts only `target`, `work_scope`, and `reason`. The Orchestrator retried with the correct schema, but the invalid first call is still a message-flow defect. Persona sessions `ses_09d46ec8affdeshG7gANT678cI` and `ses_09d4649ecffewP15783KSAIPM7` each called `read` on their own assigned output path before it existed (`user-1001.json` and `user-1002.json`). Main-database goal/session mapping proves these were not sibling-vote reads: each was a mistaken preflight read of the worker's own new output.

The package correction is prompt-level and preserves full worker capability. The Orchestrator overlay states the exact strict fresh-call shapes for Requirements and initial Architect and forbids cross-adapter/null fields. The persona overlay classifies its assigned vote path as a new owned output, forbids preflight/sibling/parent-directory reads, requires initial creation through `write`, and permits rereading only after creation. No host schema relaxation, retry mechanism, compatibility input, or fallback is added.

The full lifecycle call-point inventory also confirms a same-root integrity debt from the earlier independent audit: `createGoalLifecycleTools.add_goal` found an optional active plan and passed `null` to the sole writer `appendGoalToActiveGraph`, which could insert an `engine_goal` without a plan node. The correction makes the active plan mandatory at both the public action and writer boundary, validates task/spec/active-plan identity before inserting, and always creates the plan node in the same transaction. The Mirror Watch-owned regression must prove a ready spec without an active plan returns `database unchanged` and creates no goal. After prompt, lifecycle, payload, focused test and typecheck validation, all generated outputs except the authority personas file must be removed and a fresh ordinary Task must be published only to the same main database. Final acceptance additionally requires zero tool errors, visual Browser evidence, and a new independent read-only exact-tree audit.

### 2026-07-15 Task08 successful artifacts but invalid durable dependency graph Recall

Main-database Task `tsk_f62c8660c001pRKtn9KfiNZHh7` completed with the strict planning-call and persona-output corrections active. It produced exactly eight goals on one active plan, seven completed iFind package-tool calls, six concurrent expert/persona sessions, exactly one successful aggregate call, and 161 completed tool parts with zero pending/running/error parts. The aggregate result and disk both report SHA-256 `6f4f65536fadc26b2c624515272a6f822711e517e115bc5386499a6f83f4ae6c`; persona score total is 24; the output surface has four research files, two expert triplets, four persona votes, one report HTML, one Chart.js 4.4.0 reference, and no post-aggregate mutation.

The Task is still rejected by the mandatory second review. All eight persisted `engine_goal.depends_on` arrays are empty and the architect contract graph artifact contains zero contracts and zero dependency contracts. The Architect's real `manage_goal` calls explicitly registered every goal with `depends_on: []`; it never called `register_contract`, `register_dependency_contract`, or `register_assembly_owner`. Its final prose called the dependencies "conceptual" and assumed they would be enforced, while `submit_architect` visibly returned `0 graph contracts, 0 dependency reasons` plus `missing_contract_graph_contract`, `verification_owns_feature_paths`, and missing assembly ownership concerns. The Orchestrator then scheduled domain work from that invalid graph. Runtime sequencing succeeded only because the Orchestrator independently waited, which creates a forbidden second workflow source and cannot be accepted as a valid durable graph.

### 2026-07-17 current-tree Task graph-contract blocker Recall

At that historical resumption, the goal required publishing an ordinary Mirror Watch Task and only fixing scheduler-exposed issues. The worktree already contained broad parallel Multica/runtime changes, so that slice owned only Mirror Watch package/test/record surfaces plus untracked runtime inputs. Before publishing, the operator restored the exact four-person authority `dashboard/data/personas.json` from commit `d4224baaeb2610eb95621e7e29c115c9da1ef7a0` and copied ignored project-level `.opencorvus/ifind.json` from `.scratch/mirror-watch-direct-e2e/2026-07-14-clean-3/project/.opencorvus/ifind.json`. Structural verification showed four personas, HTTPS endpoint, `max_result_count=10`, `max_evidence_characters=2000`, `max_tool_output_bytes=49152`, and no credential content was printed.

Task `tsk_f6c2f2fbf0010ViwrfynRRYgvz` was published to the healthy main backend at `127.0.0.1:52122`, whose `/global/health.paths.database` named `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. It used prompt profile `mirror-watch`, one ordinary Task, explicit persona path `dashboard/data/personas.json`, explicit iFind config path `.opencorvus/ifind.json`, and iFind-only external search instructions. Requirements completed with 13 requirements and 20 decisions. The first Architect pass registered exactly eight goals, but zero graph contracts, zero dependency reasons, and zero assembly owners; structural re-entry repeated the same invalid contract graph. The Orchestrator correctly failed the Task before any domain worker, iFind call, aggregate call, visual evidence, or report artifact.

The visible Architect session proves the model knew the intended action but could not complete it through current readiness semantics: after registering the report goal, it wrote that it was taking the assembly-owner checkpoint, then immediately called `submit_architect`. No `register_assembly_owner`, `register_contract`, or `register_dependency_contract` tool part exists. Core `createArchitectOutputTools` currently treats missing graph contracts and assembly owners as concerns, so `isReadyToFinalize()` can expose only `submit_architect` once the eight-goal minimum and REQ coverage are satisfied. This is not an iFind/network issue, and no host fallback or Mirror Watch-specific route gate is added.

The package-level correction uses the existing Architect `contract_audit` acceptance mechanism as the single durable contract source. Every consumer goal must carry goal-local `contract_audit` scorers referencing the exact contract IDs it consumes: each expert/persona goal references `contract_canonical_survey`, and the report goal references `contract_expert_vote_denis`, `contract_expert_vote_oleg`, `contract_persona_vote_user_1001`, `contract_persona_vote_user_1002`, `contract_persona_vote_user_1003`, and `contract_persona_vote_user_1004`. Existing Architect validation already blocks unresolved `contract_audit` IDs before finalization and keeps graph tools available; therefore the package can force its required graph without changing core readiness behavior or adding a second workflow source. Acceptance for this correction requires focused prompt tests, payload freshness if generated payload changes, and a fresh ordinary main-database Task that reaches registered contracts/dependency reasons before domain dispatch.

The package-level root correction makes the Architect procedure explicit rather than adding a host gate. The eight-goal graph must register seven `static_data` contracts: one research survey handoff to all six independent expert/persona goals and one vote handoff from each of those six goals to report. It must then register twelve contract-backed edges: research to each of six, then each of six to report. The report goal is `kind: feature` because it produces the HTML, and it is the sole `mirror-watch-final-report` assembly owner. Architect may submit only after its tool results prove 8 goals, 7 contracts, 12 dependency reasons, and 1 assembly owner. The Orchestrator must treat any zero-count/empty-edge/related-concern result as an invalid persisted architect artifact and use normal structural re-entry before domain dispatch. A new empty-output main-database Task is required; Task08 artifacts cannot be promoted to final acceptance.

### 2026-07-15 Task09 structural re-entry success and expert authorship failure Recall

Main-database Task `tsk_f62e0ef11001Lx0cSD02Ra9Y92` proved that the empty initial graph is no longer consumed. The Orchestrator detected the initial `0 contracts / 0 dependency reasons` result and withheld every domain dispatch. Its first structural-reentry call incorrectly included delegated-worker field `instruction`, creating one terminal schema error; it immediately issued the strict architect shape and opened a second Architect session. That session registered and finalized exactly 8 goals, 7 graph contracts, 12 dependency reasons, and 1 assembly owner. Persisted goals then had the exact executable edge shape: research -> both experts and four personas -> report. Research and all six independent vote goals completed on that graph.

The sole authorized aggregate call failed with `expert_vote_paths[0].reason honesty boundary must disclaim direct expert authorship`. Denis's JSON `reason` contained the required heading and `LLM 通用推断`, and prose outside the honesty section said it was not Denis Globa's direct opinion. Inside the canonical `### 诚实边界总声明`, however, it said `不是 Denis Globa 的直接意见` and omitted `本人`. The validator intentionally requires the authorship boundary to state `不是/并非 ... 本人`; relaxing it would make the boundary less explicit. The worker/package prompt said only "not the named expert's direct opinion", so this is another instruction/parser mismatch rather than an invalid validator.

The correction requires the exact standalone sentence `这不是 <exact assigned name> 本人直接陈述。` inside the JSON reason's honesty section, with the exact assigned name substituted. The delivery Skill and expert role both state that wording without `本人` is invalid. Separately, the Orchestrator overlay now gives the exact structural-reentry target schema and forbids `instruction`, continuation, and null fields. Focused prompt tests and generated payload must pass, then a new empty-output main-database Task must prove both a zero-error message tree and successful aggregation. Task09 remains failed evidence and none of its output is reused.

### 2026-07-15 Task10 completed artifacts but missing assembly-owner Recall

Main-database Task `tsk_f63024ca0001mLBWdSPltvc3O2`, rooted at session `ses_09cfdb297ffeGwp8kGuFJ5TRtM`, completed 181 tool parts with no pending, running, or error part. It retained seven completed iFind package-tool calls, exactly eight goals on one active plan, seven graph contracts, twelve dependency contracts, twelve persisted `depends_on` edges, four persona votes, two expert triplets, and one successful aggregate call. The aggregate and disk report SHA-256 are both `9fe72bf6a473faa2b2646ad64df61b924d7f32d6b7236d5105dc186e8ae6d428`; persona score total is 24; no write followed aggregation. Browser review rendered the final report, all four Chart.js canvases, the ten-row SVG Gantt, both expert tab surfaces, and the human survey. The survey rejected a fourth selection with `只能选择三个功能。`, saved exactly F01/F02/F03, exposed a visible focus ring, and the browser console remained clean.

Independent read-only review still rejected Task10. Its literal `submit_architect` result reported `0 assembly owners` and the concern `Missing assembly ownership`; the Architect reasoning said it would add the owner but called `submit_architect` immediately after dependency/traceability work, and the Orchestrator summarized only the 8/7/12 counts before dispatching research. The report goal, unique aggregate call, and correct dependency graph do not create an assembly record. Therefore Task10 is diagnostic evidence, not final acceptance.

The full correction inventory covers the package Architect and Orchestrator overlays, the `register_assembly_owner` and `submit_architect` tool result vocabulary, the focused Mirror Watch prompt test, the persisted Architect session messages, the contract-graph artifact, and this Recall. No host gate, output repair, retry shim, fallback, or assembly inference is added. The Architect must call `register_assembly_owner` immediately after registering the report goal and retain that completed tool result as a pre-submit checkpoint, before contracts and later traceability can consume attention. The Orchestrator must inspect the literal terminal count: `0 assembly owners` or `Missing assembly ownership` is invalid even when the result begins with `PASS`, and structural re-entry must complete before any domain dispatch. A fresh empty-output Task published only to `C:\Users\chuan\.local\share\opencorvus\opencorvus.db` must prove a literal one-owner Architect result before Task10's artifact evidence can be superseded.

### 2026-07-15 Task11 caller-visible Architect evidence loss Recall

Main-database Task `tsk_f637423de001nVx5hcRam40K11` proved the early assembly checkpoint twice. Both Architect sessions called `register_assembly_owner`; both literal `submit_architect` results reported `1 assembly owners`, eight goals, seven contracts and twelve dependency reasons. The first result still carried seven `contract_without_audit_coverage` concerns, so the Orchestrator correctly requested structural re-entry. The second Architect repaired acceptance blockers and again persisted one assembly owner. No domain worker ran and the Task failed without fallback.

The Task failure reason nevertheless claimed that neither Architect terminal result reported one owner. The causal chain is now proven: `architect-stage.ts` persisted the mapped assembly owner into the active spec and task metadata, but its standard `SubAgentProtocol.yieldResult` exposed only `goals`, `contract_graph`, and `spec_snapshot_id`. The caller-visible `dispatch_agent` result therefore said only `Architect decomposition complete: 8 goals, 7 contracts` and `7 contracts / 12 dependency reasons`; the Orchestrator could not see the nested `submit_architect` output, and `read_context(scope=agent_outcomes)` returned no substitute. Requiring the Orchestrator prompt to inspect a nested terminal transcript was an impossible downstream demand and violated the dispatcher-input boundary.

The single-source repair is generic Architect result projection: add `assembly_ownership: <count> assembly owners` beside the existing contract-graph count in `architect-stage.ts`. This projects already persisted Architect fidelity; it does not infer ownership, add a gate, change lifecycle control, or introduce a Mirror Watch special case. The package Orchestrator consumes that caller-visible field and must not demand hidden nested tool text. The owned regression exercises the real Architect dispatcher and asserts its yielded result contains `assembly_ownership` and `1 assembly owners`; the package regression pins the visible evidence contract. After payload regeneration and deterministic tests, a new empty-output main-database Task is required because Task11 has no domain artifacts.

### 2026-07-15 Task12 projected-dispatch ownership split Recall

Main-database Task `tsk_f6385df1c001FWq7wjlR4jz9il`, rooted at session `ses_09c7a1ecbffeZLnqsbM30T1wuh`, rejects the Task11 evidence fix for a separate scheduler-lifecycle failure. Its first Architect `dispatch_agent` call remained genuinely active while creating session `ses_09c774523ffevWmXZR0Ak7Yu6b`. A passive wake nevertheless began a second Orchestrator turn, which declared the first call orphaned and dispatched Architect again. The duplicate call failed with `TypeError: undefined is not an object (evaluating 'input.executionLease.taskID')`; the Orchestrator marked the Task failed before the original Architect finished. The original call then completed after Task terminal with eight goals, one assembly owner, seven graph contracts and twelve dependency reasons. No domain worker ran. Task12 is failed evidence and its late Architect result cannot be promoted to acceptance.

The observable causal chain is: a streamed `dispatch_agent` tool call outlives its current Orchestrator pass; `dispatchTaskLoop` receives a passive wake; it suppresses re-entry only when `listLiveOrchestratorToolOwnership(task.id)` returns a row; `tool-ownership.ts` accepts only `build | integrity`; Architect therefore has no live ownership row even though `ProjectedAgentExecutionLease` still holds one active task-scoped lease; the task loop starts a second decision and task terminal diverges from the real child runner. The lease correctly serializes matching projected identities, but the queue does not consume that in-memory pool. Reading the lease pool in addition to ownership would create two scheduler ownership sources and is rejected. Prompt changes, Mirror Watch special cases, retry logic, a task-status gate or an optional execution lease would likewise conceal rather than repair the lifecycle boundary.

### 2026-07-15 Task14 source-algorithm parity failure Recall

User acceptance requires the migrated expert squad to preserve the current `mirror-watch` source algorithm output one-to-one. Main-database Task `tsk_f63fa5fa4001PG1ejXbDDOoQQB` completed its requirements, architect, seven iFind calls, two concurrent expert votes, four independent persona votes and sole aggregate call with no non-terminal tool parts. That green execution chain does not accept the delivered aggregator: independent review reran `C:\Users\chuan\myhexin-local\mirror-watch\scripts\lib\aggregate.py` against Task14's four real votes and proved deterministic output drift.

The source call-point inventory covers `scripts/lib/aggregate.py`, `scripts/lib/personas.py`, `templates/report.template.html`, the package `lib/mirror-watch/aggregate-report.ts`, `lib/mirror-watch/report-template.ts`, `tools/aggregate-report.ts`, generated `packages/opencorvus/src/expert-squad/payload.ts`, and the focused package test. The source algorithm filters zero-count values only from each segment `order` while retaining their zero matrices, declares mode order as `standard, deep, click, concise`, preserves first-insertion order for equal-count bundles through Python's stable count-only sort, and emits the exact `build_narrative` KPI, TLDR, insight and three-recommendation structures. The package instead retained zero-count `C` in visible order, declared a different mode order, added a bundle key tie-breaker, and authored a different four-recommendation narrative.

This is not a missing abstraction. It is an unauthorized second implementation of product semantics. The correction deletes those package-only choices and makes the TypeScript aggregation output match the source Python behavior exactly while retaining strict input/data-integrity validation and the two expert votes as qualitative, non-scoring evidence. No host gate, compatibility alias, fallback, retry or output repair is permitted. A new focused parity regression must assert the exact source ordering and narrative values, the official payload must be regenerated, and a new empty-output main-database Task must supersede Task14 before visual and independent acceptance.

The full call-point inventory covers `orchestrator/dispatch-agent-tool.ts` and every production/test `createDispatchAgentTool` caller; all thirteen dispatch-adapter bindings in `orchestrator/tools.ts`; `orchestrator/tool-execution-context.ts`; every adapter `onSessionCreated` callback; `engine/projected-agent-execution-lease.ts`; `engine/tool-ownership.ts`; ownership creation in Build and Integrity; ownership readers in `engine/queue.ts`, `engine/writer.ts`, `engine/task-agent-lifecycle.ts`, `engine/agent-coordination.ts`, server task/session routes and their tests; and the dispatch-agent, queue, tool-ownership and Mirror Watch focused tests. The single-source correction must make the public `dispatch_agent` invocation itself own its persisted tool call from lease admission through executor settlement, regardless of adapter identity. Child-session identity may enrich that same ownership record when the runner creates it, but it cannot be required before ownership begins. Existing Build/Integrity adapter-local ownership must be removed or proven to represent a distinct nested terminal lifetime; retaining two rows for the same public dispatch lifetime is forbidden.

Acceptance requires a focused regression in an owned test file proving that an unresolved Architect `dispatch_agent` creates one live ownership, queues a passive task wake without starting a second Orchestrator decision, attaches the child session to that same ownership, and permits the existing queue policy to drain or discard that wake only after the executor settles. Failure must close the same ownership with a failed outcome; no duplicate Architect session or missing-lease dereference may occur; task terminal must not precede child terminal. After typecheck and relevant lifecycle tests pass, only the dedicated port-7879 helper may be reloaded. A fresh empty-output Task in `C:\Users\chuan\.local\share\opencorvus\opencorvus.db` must then reproduce the full Mirror Watch chain, followed by visual review and a new independent read-only audit.

### 2026-07-15 Task13 projected package-tool running-state race Recall

Main-database Task `tsk_f63a6b195001VsvBNtNXtjRKVa`, rooted at session `ses_09c594a6effeNHbJ4Ccbze2227`, proved the generic `dispatch_agent` ownership repair: requirements, Architect, the eight-goal active plan, seven graph contracts, twelve dependency reasons, one assembly owner, research, two experts and four persona votes all completed without the Task12 duplicate-dispatch failure. The final report worker session `ses_09c4252c8ffefl4WVgxFY7kDaF` then called the sole authorized projected aggregate tool once with the exact five canonical argument groups. Tool part `prt_f63bdd8c40016MsBEMD5YXCkNg` / call `call_4Tk4Qma0SAA3z3qPtQ837Clh` failed before the aggregate implementation ran with `persisted tool part is not the current running invocation`; no report was created and the worker exposed the visible terminal failure without retry, alternate aggregation or output repair.

The full call-point inventory covers `session/processor.ts` tool-input/tool-call/tool-result transitions plus `partFromToolCall` and `ensureToolPart`; `session/loop.ts:wrapExtraTool`; `tool/task-tool-invocation.ts`; `tool/task-tool-execution-scope.ts`; projected runtime binding and package-tool execution tests; the external projected callback test; and the real persisted Task13 transcript. The processor creates a `pending` part while tool arguments stream and owns the locked, idempotent transition to `running` in `ensureToolPart`. `wrapExtraTool` currently uses the in-memory existence of that pending part to skip `ensureToolPart`, then starts the invocation and asks the execution scope to require a persisted `running` part. AI SDK tool execution and the consumer of its `tool-call` stream event are concurrent, so a large argument payload can execute during this window. The real invocation is then rejected by the correct database-integrity assertion because the wrapper itself left its persisted part pending.

The single-source correction is not to accept `pending`, remove invocation authority, add a retry, relax the database assertion or special-case Mirror Watch. Every extra-tool execution must call the processor's existing locked `ensureToolPart` unconditionally and use the returned canonical part identity. That one operation owns creation/reuse and the persisted transition to `running`; the wrapper must not treat `partFromToolCall` cache presence as execution readiness. Acceptance requires a new Mirror Watch-owned regression that starts from a real persisted pending part, invokes the real resolved projected package tool before a processor `tool-call` event can promote it, proves the exact same part becomes running during execution and completes without a duplicate call identity, then reruns focused processor/package-tool tests and typecheck. Only the dedicated port-7879 helper may be reloaded afterward, and Task14 must start from an empty generated-output surface in the same main database.

### 2026-07-15 Task18 rendered-script parse failure Recall

Main-database Task `tsk_f64bb40f3001I97QQNikaPLjgW`, rooted at session `ses_09b44bd8cffeB0lLCvrey6Mhrn`, completed the current eight-goal graph on the isolated port-7879 backend without duplicate dispatch. Its session tree contains one terminal Requirements session, one terminal Architect session, one terminal research session, two terminal expert sessions, four terminal persona sessions and one terminal report session. All 191 persisted tool parts are terminal; the four iFind calls completed; no WebSearch, browser search or other search tool ran; and `mirror-watch/shared/aggregate-report` completed exactly once. The aggregate result and report file share SHA-256 `0ed467f9373ff8cbd4db237332d7fa9db3497d8b5634bdf213680557f5ad973f`. A direct rerun of the source Python aggregator against Task18's four persona votes proved exact equality for feature names, statistics, every segment matrix, bundles, quotes, metadata, footnotes, KPI cards, TLDR, insights and recommendations. The only additive package fields are the declared qualitative expert evidence and schema version.

Task18 is still not final acceptance because the official task-scoped Browser Preview target `art_f64d95e5c001yh6btCs98Iwta0` failed its capture with evidence `art_f64db089c001H2yy8TfK8EUzUs`. Node Playwright against the same target proved HTTP 200 and the expected title, then reported `pageerror: Invalid regular expression flags`. The generated HTML contains `match=/^<strong>([sS]*?)</strong>([sS]*)$/`: the TypeScript template literal consumed the JavaScript regex backslashes and slash escape, so the literal closes at `</strong>` and the remaining characters are parsed as invalid flags. The page body is intentionally populated by that script, which explains the user-visible blank report. Direct `file://` viewing is additionally not an accepted preview path; visual acceptance must use the task-scoped HTTP target.

The complete rendering call-point inventory covers the source `C:\Users\chuan\myhexin-local\mirror-watch\templates\report.template.html`, package `lib/mirror-watch/report-template.ts`, `lib/mirror-watch/aggregate-report.ts` and its renderer call, projected `tools/aggregate-report.ts`, generated `packages/opencorvus/src/expert-squad/payload.ts`, `packages/opencorvus/test/expert-squad/mirror-watch-package.test.ts`, the rendered Task18 report and the official Browser Preview target/evidence. The package source template is the only editable renderer authority; the payload is generated from it, and the report is written only by the aggregate tool. The correction must emit the intended runtime regex literally as `/^<strong>([\s\S]*?)<\/strong>([\s\S]*)$/`, add a regression that parses the emitted inline script rather than merely matching template source text, regenerate the official payload, and publish a new empty-output main-database Task. Task18's report must not be hand-edited or reused as acceptance. No fallback, compatibility branch, host gate, retry or alternate renderer is added.

The remaining acceptance is a new ordinary main-database Task on the current isolated backend, algorithm parity against its own four votes, exactly one completed aggregate call, zero non-terminal/error tool parts, official Browser Preview capture, personal screenshot inspection and an independent read-only exact-tree audit. Only after those checks may the scoped implementation, regression, generated payload, durable Recall and final generated evidence be committed and pushed to legacy remote.

### 2026-07-15 full source-skill preservation failure Recall

The user rejected the package after inspecting the source `C:\Users\chuan\myhexin-local\mirror-watch\skills` tree and the installed expert-squad skills. That rejection is correct. Source inventory contains 16 distinct Skill identities and 159 files: 142 text files and 17 binary files, of which 16 are PNG assets and one is `.DS_Store`. The current package contains only seven immutable `SKILL.md` files: six source identities plus the package-native delivery contract. It omits ten source identities entirely. The AInvest design-system source alone contains 77 files, while the package adaptation retains a rewritten desktop subset and only a short hash list. Mirror Design similarly discards source references, React setup, map modules, assets and scripts. Only the Denis and Oleg perspective skills consistently preserve all six source references. This is an inconsistent adaptation algorithm, not a manifest requirement.

The causal chain is now explicit. The source workflow's `SKILLS.manifest.md` registers complete runtime dependencies, but the package README, package test and earlier record deliberately redefined the expert-squad boundary as the minimum capability surface used by one AInvest report. They then treated concrete Task policy—iFind-only and no WebSearch—as a reason to omit `search-anything`, `agent-reach`, `web-read`, `web-scrapling`, `playwright-cli`, `agent-browser`, `get-ui`, `get-ux` and `s3-share` from the package. The package also classified `dense-financial-ui` as undeclared and omitted it. This contradicts the user's earlier requirement that delegated agents retain full base templates and that WebSearch be prohibited only in the concrete Task instruction. Manifest evidence already proves `inherit_base_tools: true`; omitting source Skills nevertheless removed their domain instructions, references and helper semantics.

The current protocol constraint remains authoritative: `ExpertSquadRegistry.collectSkillRefs` rejects every ancillary entry beside a package `SKILL.md` because package Skills are immutable single-file resources. It does not authorize content loss. The single-source adaptation must retain exactly one package Skill for each of the 16 source identities, preserve the source `SKILL.md` rather than replacing it with a task-specific summary, and preserve every supporting text file byte-for-byte under one canonical package-owned source-resource tree with original logical paths, sizes and SHA-256 digests. Each package Skill must expose that one canonical mapping so agents can progressively read the copied resources with their inherited `read` capability. Source helper scripts remain exact provenance/resources; executable behavior must resolve to an explicit current base tool, package tool or scoped MCP capability and must fail clearly when unavailable. It must not silently substitute a different provider or implementation.

Binary assets cannot be written as ancillary Skill files or passed through the current text-only payload generator without corruption. The 16 source PNG files therefore require one package-owned exact-byte projection backed by generated base64 data and verified SHA-256 values. The projected asset tool may return the selected exact data URI and metadata; it must not guess names, fetch a replacement, compress, transcode or fall back. The source `.DS_Store` is operating-system metadata rather than Skill capability and is explicitly excluded. This is the only excluded source file.

The independent read-only auditor confirmed FAIL and the same counts: 16 source identities / 159 files versus six source identities / seven package Skills, with ten identities missing. It also confirmed that all agent manifests inherit full base tools and that delegated-worker capabilities already contain WebSearch, WebFetch and Browser Preview; the restriction belongs only in the Task. The auditor requires research/browser source identities on the research projection, the two exact perspective Skills on their respective experts, shared delivery/design capabilities on their consumers, and no second visual authority: `dense-financial-ui` is comparative evidence, while AInvest remains the delivery style authority. It requires tests for exact identity set, complete source inventory and hashes, active/inactive resolver isolation, agent grant matrices, binary asset SHA-256, payload freshness and concrete-Task no-WebSearch evidence.

Task19 `tsk_f64e80f2000196LL7vFkE29OTX` completed the renderer correction and its fresh HTTP page now has the expected title, seven populated sections, 11,422 body-text characters and zero Playwright page errors. It is retained as renderer evidence only. Because it ran before full source-skill preservation, it cannot satisfy final expert-squad integration acceptance. After the package Skills, resource archive, manifest projections, tests and payload are corrected, generated outputs must again be cleared except for authority personas and a new main-database Task must prove the complete package projection while still obeying an iFind-only concrete Task.

### 2026-07-15 directory Skill protocol correction Recall

The preceding subsection incorrectly treated the implementation's single-file rejection as an authoritative protocol boundary and proposed a parallel resource archive. The user did not require single-file Skills. Current `specs/current/architecture/04-extensions.md` already defines a normal Skill as `SKILL.md` plus supporting files that materialize on exact load. The expert-squad implementation is the stale side: `ExpertSquadRegistry.collectSkillRefs` rejects supporting entries, `PromptProfileResolver.packageSkillFromRef` exposes only a non-filesystem URI and body text, `SkillTool` materializes only built-in Skills, the package snapshot hashes only `SKILL.md`, `EmbeddedPackageSource.files` is text-only, the payload generator imports every non-TypeScript file as text, and Multica import blocks supporting files even though its source contract supplies their content. The correction is destructive in manifest schema version 1: package Skills become directory snapshots using the same supporting-file semantics as ordinary Skills. No archive tool, asset lookup tool, compatibility parser, fallback loader or second resource source is added.

The complete production call-point inventory is `packages/opencorvus/src/expert-squad/registry.ts` (`PackageSkillSnapshot`, `PreparedPackageSkill`, `collectSkillRefs`, embedded package decoding), `prompt-profile-resolver.ts` (package Skill projection and projection hashes), `manager.ts` (binary-safe payload materialization and existing archive path validation), `multica-import.ts` (supporting-file preservation), `packages/opencorvus/src/skill/skill.ts` (content-addressed exact-load materialization), `packages/opencorvus/src/tool/skill.ts` (generic materialization), and `packages/opencorvus/script/generate-expert-squad-payload.ts` (UTF-8/Base64 descriptors). Tests with old single-file assumptions are `dynamic-agent-registry.test.ts`, `dynamic-agent-resolver.test.ts`, `multica-import.test.ts`, `payload-generation.test.ts`, `tool/skill.test.ts` and `mirror-watch-package.test.ts`. `packages/opencorvus/test/expert-squad/prompt-profile-resolver.test.ts`, builtin Skill payload files and `specs/current/architecture/04-extensions.md` are currently owned by parallel work and must not be edited in this slice.

The directory snapshot must reject symbolic links, absolute paths, traversal, backslashes, colon paths, case-insensitive duplicate paths, file/directory collisions and nested `SKILL.md` identities. Its canonical SHA-256 covers the ordered relative path, byte length and exact bytes of every file. The prepared projection retains exact bytes independently of later package-directory mutation; `SkillTool` materializes that frozen closure into a content-addressed cache only when the exact active Skill is loaded. Inactive packages remain catalog metadata only and never register or materialize resources. Import, export, payload release and Multica conversion preserve the same closure. Binary assets use explicit Base64 descriptors; UTF-8 text remains text. This is one directory model across filesystem packages and released payload packages.

Independent read-only review found a separate source-safety conflict before bulk copy. `skills/s3-share/scripts/s3_upload` contains hard-coded default access credentials, and `skills/get-ux/references/api_response.md` contains a signed URL example. Those values must not be committed or embedded in the application payload and should be rotated at their source. `skills/web-read/SKILL.md` instructs an automatic proxy retry and `skills/agent-reach/SKILL.md` instructs a manual fallback, both conflicting with the repository-wide no-fallback rule. Therefore all 16 Skill identities and their directory structures remain required, but these unsafe values/behaviors must be removed in the package copy with explicit tests; byte-for-byte source preservation is not an acceptable security or architecture criterion for those files. Source-algorithm output parity remains the final behavioral criterion. The source `.DS_Store` remains excluded as operating-system metadata.

### 2026-07-15 directory Skill implementation and pre-E2E audit Recall

The directory Skill correction now uses one model across filesystem packages and generated payload packages. Registry validation accepts supporting files as part of one frozen Skill closure and rejects unsafe paths, case-insensitive duplicates, file/directory collisions and nested Skill identities. Exact Skill loading materializes that closure into a content-addressed directory; inactive packages remain unprepared. Multica preserves supporting paths without backslash normalization, and payload descriptors preserve UTF-8 or Base64 bytes. The generated payload reads the manifest only from its canonical `expert-squad.jsonc` file entry. Package release tests compare every released payload file byte-for-byte, including the sixteen PNG assets.

Mirror Watch contains the complete sixteen-source-Skill identity set and 157 Skill-directory files, plus the package-native delivery contract; only `.DS_Store` and the source root inventory document are outside those Skill closures. The focused regression pins each Skill's complete ordered path digest and byte-closure digest, exact agent grant matrix, active/inactive isolation, binary count, canonical iFind implementation, source-safety adaptations and generated payload freshness. Expert workers now read the six perspective references only beneath the frozen materialized directory returned by exact Skill loading. The obsolete single-file/ancillary prohibition has been deleted from the worker and delivery prompts.

Two independent read-only audits identified and drove removal of the remaining executable drift. The iFind diagnostic now delegates to the canonical package library; its supporting reference exposes only the exact `config_path`, `query`, `channels`, and `size` contract, the eleven declared channels and the one-through-ten size bound. `search-anything` no longer embeds an endpoint, authorization header or provider identities: its diagnostic requires one environment-owned provider configuration and four explicit request arguments, returns the provider response unchanged and is not authorized by the concrete Mirror Watch Task. S3 no longer embeds credentials, endpoint, bucket, region or expiration defaults; upload failure propagates before URL generation, an unknown MIME type fails explicitly, and `meta.json` accepts only the current array-of-objects shape. Signed sample URLs and provider-switch/retry instructions remain absent.

Current static evidence after those corrections is: Mirror Watch plus payload generation `23 pass / 0 fail / 770 assertions`; the broader directory-Skill suites previously produced `154 pass / 1 skip` with one unrelated Package Manager selector failure caused by concurrent General default-Skill changes; TypeScript typecheck reports `TYPECHECK_OK`; scoped and full `git diff --check` report no error. The unrelated selector failure is not accepted as Mirror Watch evidence and its concurrently owned General/Skill files were not edited here.

Source-algorithm one-to-one parity remains a runtime acceptance item, not a claim derived from hand-written TypeScript expectations. The next main-database Task must create a fresh four-person vote cohort, after which the operator will run the current source `C:\Users\chuan\myhexin-local\mirror-watch\scripts\lib\aggregate.py` and the projected TypeScript aggregate implementation against those same votes and require exact equality for names, statistics, every matrix/order, bundles, quotes, metadata, footnotes and narrative. Task18 and Task19 cannot substitute for that proof.

At the pre-E2E snapshot both `127.0.0.1:7878` and the dedicated `127.0.0.1:7879` helper refuse connections. No backend was restarted because process intervention requires separate authorization. The next publication must occur only after health identifies `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`, must clear only generated Mirror Watch outputs while retaining the authority personas, and must prohibit WebSearch only in the concrete Task instruction while keeping the full base agent templates.

### 2026-07-15 Task20 source-order parity failure Recall

Main-database Task `tsk_f656b0b60001UjLT3uLkl2ZZlh` completed its eight-goal graph and wrote a non-empty HTML report through exactly one successful `mirror-watch/shared/aggregate-report` call. That is pipeline evidence, not final source-algorithm acceptance. A read-only operator script at `.scratch/mirror-watch-task20-parity/compare_source_report.py` imported the current source `C:\Users\chuan\myhexin-local\mirror-watch\scripts\lib\aggregate.py`, ran it against Task20's exact four persona votes and canonical survey, removed only the package's declared additive `schema_version` and `qualitativeExperts` fields, and recursively compared values plus dictionary insertion order against the report embedded in the tool-written HTML. All values match, but three ordering differences remain: the source top-level order is `featNames, stats, byTier, byStatus, byMode, byFreq, bundles, bundlesNote, quotes, meta, footnote, kpis, tldr, insights, recommendations`; source `byTier.counts` is `S, A, B, D, C`; and source `byMode.counts` is `deep, standard, concise, click`. The package currently emits different orderings for all three.

The complete owning-path inventory is `.opencorvus/expert-squads/legacy-remote/mirror-watch/lib/mirror-watch/aggregate-report.ts` (`computeSegment`, `buildReport`, `aggregateMirrorWatchArtifacts`), `report-template.ts` (type declaration and `JSON.stringify` serialization), the projected `tools/aggregate-report.ts` wrapper, generated `packages/opencorvus/src/expert-squad/payload.ts`, and `packages/opencorvus/test/expert-squad/mirror-watch-package.test.ts`. The source `_segment` starts an empty `defaultdict(int)`, inserts encountered persona categories in vote order, then appends absent declared categories while evaluating its declared-order filter. The package instead preinitializes `counts` in declared order. The source also constructs the report in the exact top-level order above, while package `buildReport` places `meta` first and `footnote` last. These are deterministic implementation differences, not network failures or accepted additive metadata.

The single-source correction is confined to the package aggregate owner: preserve encountered count-key insertion order, append missing declared categories with zero after processing votes, and construct all source-owned report fields in source insertion order. Additive package fields remain additive without changing the relative order of source fields. The focused package regression must assert the serialized embedded report key order and the source-equivalent segment count order. Task20's report remains immutable evidence and cannot be edited into conformance.

At this snapshot the aggregate owner and focused Mirror Watch test have no uncommitted changes, so there is no semantic parallel overlap in the editable implementation boundary. The generated payload does contain unrelated MirrorTest changes from parallel work. Payload regeneration may proceed only if a before/after comparison proves those unrelated bytes are preserved; otherwise the operator must stop rather than overwrite them. After deterministic tests and payload equality pass, acceptance requires a fresh ordinary Task against the main database on a new isolated random-port backend, a new source-vs-package comparison against that Task's own votes, terminal message/database evidence, official HTTP preview screenshot inspection, an independent read-only audit, scoped commit and legacy remote push.

### 2026-07-15 Task21 intermediate-idle scheduler failure Recall

After the source-order correction passed focused tests, main-database Task `tsk_f659105b5001ialeDjVlUCgtSI` was published directly to the current-source backend at `http://127.0.0.1:53684`; `/global/health` identified `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. Its task-root session is `ses_09a6ef92fffeRPXDB6VWPFqh2v` and its Orchestrator session is `ses_09a6ee9aefferDcMk4AoVKO4vn`. Requirements and Architect reached terminal completion, the Architect persisted exactly eight goals with seven contracts, twelve dependency reasons and one assembly owner, and the research worker `ses_09a663e1dffeq9mbw8bRNRMcaO` completed the four research outputs. Its iFind evidence contained three completed calls and one deterministic HTTPS-contract rejection. Two visible provider-idle retries resumed the same worker session and are not a package fallback or replacement dispatch.

The first invalid transition occurred after the research `dispatch_agent` returned terminal success to the current Orchestrator turn. Instead of immediately dispatching the two expert goals and four persona goals, the Orchestrator emitted a progress-only response ending with “The next scheduler step, on continuation, should dispatch...” and then persisted `session.status=idle` plus `session.idle`. No later protocol event, dispatch, goal-run artifact or terminal-refill fact appeared, while the Task remained active with seven unexecuted goals. Task20 is the authoritative comparison: after the same research child result, its Orchestrator continued in the same turn and issued six concurrent `dispatch_agent` calls. The host runtime's terminal-goal refill mechanism cannot supply the imagined continuation here because this delegated-worker dispatch path returns its terminal result directly to the owning Orchestrator turn and did not produce an independent `goal_run_attempt` artifact. Adding a host idle gate, synthetic wake, retry loop or state machine would violate the repository architecture and mask the prompt decision error.

The owning correction is the active Mirror Watch Orchestrator overlay at `.opencorvus/expert-squads/legacy-remote/mirror-watch/agents/orchestrator/system.md`. It already states the correct dependency and next dispatch, but it does not say that the terminal child result must be consumed in the same Orchestrator turn or that there is no automatic continuation after a progress-only final response. The correction must explicitly require: after each successful domain `dispatch_agent` result, continue the same turn to the next ready dispatch or final lifecycle action; never emit an intermediate progress-only final answer or enter idle while the current graph has ready/unexecuted goals; and never defer work to a hypothetical later continuation. This remains prompt-over-host guidance. The focused Mirror Watch package test must pin the exact behavior. The prompt owner and focused test have no parallel uncommitted changes outside the current Mirror Watch slice.

Task21 is retained as failure evidence and cannot become acceptance for the corrected prompt snapshot. After the prompt regression and generated payload pass, Task21 must first reach an explicit terminal state without deleting its records, then a fresh ordinary main-database Task on a new isolated backend must prove the corrected same-turn research-to-six-goal transition. The source-order parity, final HTTP preview, independent read-only audit, scoped commit and legacy remote push remain required.

### 2026-07-17 v0.0.7beta dispatch contract Recall

The resumed goal's slice-start snapshot was branch `v0.0.7beta` at `bab473fbc2`. At that snapshot, the source backend was `http://127.0.0.1:52122`; its health response named the main database `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`, and the project board was empty. Previously recorded Mirror Watch package plus generated-payload validation passed `24 tests / 795 assertions`. The three newer Mirror Watch scheduler regressions exposed one deterministic protocol drift: `mirror-watch-architect-plan.test.ts` invoked the projected research worker without the now-required public `dispatch_agent.use_worktree` boolean, so schema validation failed before the goal-scoped worker ran.

The full repository search for `use_worktree`, `dispatch_agent`, Architect promotion and Mirror Watch dispatch callers shows that `packages/opencorvus/src/orchestrator/dispatch-agent-tool.ts` owns the required field. Current architecture and prompt records define its semantics: `true` is only for concrete concurrent overlapping-write or ownership-contention risk; read-only work and safe parallel/disjoint mutation use `false`. At the slice-start snapshot, every other positive orchestrator and expert-squad caller declared the field, including `mirror-watch-dispatch-ownership.test.ts`, `opentest-typed-dispatch.test.ts`, `general-package.test.ts` and scheduler-capability tests; the older Mirror Watch architect-plan regression was the sole missing caller. In this staged candidate, every positive caller declares it. The research dispatch is a single isolated goal execution with no concurrent overlapping mutation or ownership contention, so the exact current contract is `use_worktree: false`. The negative `target: "build"` assertion remains a projected-identity rejection and does not need to become a second valid dispatch case.

The code boundary is only `packages/opencorvus/test/expert-squad/mirror-watch-architect-plan.test.ts`; this record is the separate evidence boundary. No production behavior, manifest, payload, fallback, compatibility path, gate or workflow state changes are authorized. After the focused test passes, the next real E2E still requires an ordinary Task on the main database, exact source-algorithm comparison against that Task's votes, terminal message/database evidence, task-scoped HTTP Browser Preview, personal visual review and independent read-only final audit. Task publication remains a final-phase activity after the platform cleanup slices and their exact-tree reviews are complete.

### 2026-07-17 Goal Update: E2E Cancelled

The user cancelled the remaining Mirror Watch E2E. No fresh ordinary main-database Task, model run, Browser Preview
capture or task-scoped visual acceptance will be produced for this Goal. This decision supersedes every pending
fresh-task or final-E2E instruction in this historical record without rewriting the retained failure evidence.
Mirror Watch package protocol, source-algorithm regressions, dispatch contract tests and active/inactive isolation
remain part of the platform integration matrix; they must not be described as a completed real E2E.

### 2026-07-17 cancelled-scope parallel verifier failure evidence

A parallel process published an ordinary main-database Task after the user had cancelled the remaining Mirror Watch E2E. It does not supersede the cancellation and is retained only as immutable historical failure evidence. The process used the live backend at `http://127.0.0.1:52122`, whose `/global/health.paths.database` named `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. Task `tsk_f6c4423ce001QPOIj8nmVM3lbk` completed requirements, persisted exactly eight goals, seven graph contracts, twelve dependency reasons and one assembly owner, completed the research four-pack with real iFind package-tool calls and no WebSearch, completed both expert votes and all four persona votes, then called `mirror-watch/shared/aggregate-report` exactly once. The package tool wrote `dashboard/reports/ainvest-h2-report.html` and returned report SHA-256 `477bfbdd17f9c0ac6df769631a6500143035ec57399ddfc102865aa7349ca061`.

The terminal failure was not a domain, aggregation, or report-rendering failure. The report analyst attempted browser visual/self-review with a shell-quoted inline `node -e` Playwright command. Windows/Node quoting corrupted `report.replace(/\\/g,'/')` into invalid JavaScript and Node failed with `SyntaxError: missing ) after argument list` before launching or loading the page. The worker then treated missing screenshot evidence as terminal failure instead of repairing the verifier command. This is a verifier-tooling prompt defect: fixing the verifier command is not a fallback output, alternate renderer, report edit, recomputation, host gate, or compatibility path.

The owning boundary for that repair was `.opencorvus/expert-squads/legacy-remote/mirror-watch/agents/mirror-watch-report-analyst/system.md`, generated `packages/opencorvus/src/expert-squad/payload.ts`, and the focused package test. The prompt correction required a temporary `.cjs` Playwright verification script under `.scratch/`, run by Node, and self-correction when a verification script failed before page load because of command quoting, shell escaping, or local script/tooling error. Terminal failure remained valid only after the aggregate output existed and a real browser render attempt against that output still failed to produce required visual evidence or exposed a page defect. Task `tsk_f6c4423ce001QPOIj8nmVM3lbk` remains immutable failure evidence; it cannot be hand-edited into acceptance.

### 2026-07-17 cancelled-scope parallel run evidence

A parallel process later published main-database Task `tsk_f6c60e990001uHE4wZIgkMo5ia` after the cancellation. The following facts are retained as historical run evidence only; this run is neither required by nor accepted as the current Goal E2E. It was published through the live backend at `http://127.0.0.1:52122`; `/global/health.paths.database` identified `C:\Users\chuan\.local\share\opencorvus\opencorvus.db`. The persisted task has `error = null` and `time_completed = 1784231236450`. Its visible session tree has twelve sessions: root, orchestrator, requirements, architect, research, two expert workers, four persona workers, and report analyst. The tool evidence contains ten completed `dispatch_agent` calls, one completed `manage_task`, one completed `mirror-watch/shared/aggregate-report` call, ten `mirror-watch/shared/ifind-search` calls, no generic WebSearch/WebFetch/browser search tool, one `register_assembly_owner`, fourteen `register_contract`, twelve `register_dependency_contract`, eleven `register_requirement`, and twenty-two `register_decision` calls. Some iFind rows were provider HTTPS-contract rejections, but the research goal still had completed iFind evidence and no alternate provider.

The report goal called the aggregate package tool exactly once with canonical paths: `dashboard/data/ainvest-h2-feature-survey-agent.md`, `dashboard/data/personas.json`, four `dashboard/data/process_answer_aime/user-*.json` votes, the two expert Markdown votes, and `dashboard/reports/ainvest-h2-report.html`. The tool returned `report_sha256 = 1e6a65cb0eb87fc6145d2af6c43f9b3d895e86ff8f68b3100160b4f72ae0a3ee`. At the time of that parallel run, the report file had the same SHA-256. A source parity check imported `C:\Users\chuan\myhexin-local\mirror-watch\scripts\lib\aggregate.py`, ran it against those four persona votes and survey, removed only package-declared additive `schema_version` and `qualitativeExperts`, and recursively compared values plus dictionary insertion order; it returned `exact = true` and `difference_count = 0`.

Independent read-only audit of that parallel run initially rejected its claimed acceptance because the local `dashboard/reports/ainvest-h2-report.html` had been removed after parity and because the task-written `.scratch/mirror-watch-report-verify.cjs` contained a module-resolution fallback from `playwright` to `@playwright/test`. The missing artifact was not accepted as stale screenshot evidence. The parallel process used the real task's persisted completed `write` tool parts to restore fourteen `dashboard/**` input artifacts, then preserved the generated aggregate report whose SHA matched the aggregate tool result; no report was hand-written. The report analyst prompt and focused package test were tightened so verification scripts must load only the `playwright` package and must not attempt `@playwright/test`, alternate package names, or conditional module resolution. The `.scratch` verifier was updated the same way and rerun.

The parallel run recorded visual evidence at `.scratch/ainvest-h2-report-playwright.png` with `.scratch/ainvest-h2-report-playwright-result.json`. Its result JSON reported `passed = true`, successful file URL navigation, seven report sections, ten feature rows, three recommendation blocks, four persona quotes, two expert blocks, four chart canvases with text equivalents, no page errors, no console errors, and a nonempty screenshot. Manual review at that time recorded the rendered desktop report's core conclusion, product recommendations, statistics chart/table, segment charts/tables, bundle chart and representative quotes, two expert evidence blocks, and method/boundary section. These cancelled-run artifacts are intermediate files rather than current deliverables and may be removed after their factual summary is retained here.

### 2026-07-17 superseded parallel resumption Recall

This section records the parallel process's plan before the later cancellation was applied to it. The Goal Update above supersedes its new-task acceptance item; the local deterministic corrections remain valid package evidence. At that point, the process described its requirement as finishing the existing `C:\Users\chuan\myhexin-local\mirror-watch` migration through the project package protocol, preserving one source of truth with no fallback or compatibility route, using independent reviewers, avoiding contradictory parallel edits, and accepting only a real observable end-to-end run whose package outputs restored the source algorithm one-to-one. The branch was `v0.0.7beta`; the audited baseline commit `871162eb754aa6bc875ae659bf88367fe7c07bea` exactly matched `legacy-remote/v0.0.7beta` before that slice. The canonical package remains `.opencorvus/expert-squads/legacy-remote/mirror-watch`; this record remains the historical implementation and benchmark record.

The 2026-07-17 acceptance delta is:

1. Re-run registry, resolver, catalog, SkillMount, package-tool, payload and focused Mirror Watch tests against the current SDK and Multica package-location implementation rather than relying on the July 15 results.
2. Prove the selector invokes the real strict `select_expert_squad` schema with `profile_id`, not an obsolete or invented field.
3. Prove every direct projected dispatch test declares the current mandatory `use_worktree` decision explicitly.
4. Preserve the exact committed directory-Skill closures and generated payload without replacing source-owned bytes or loosening the closure oracle.
5. Superseded by the Goal Update: do not publish a new ordinary Task for Mirror Watch acceptance.

The full-repository call-point search covered `select_expert_squad` in `packages/opencorvus/src/orchestrator/tools.ts`, every selector package, `dispatch-agent-tool.ts`, all Mirror Watch dispatch regressions, `ExpertSquadRegistry`, `PromptProfileResolver`, `SkillMount`, the two projected package tools, payload generation, the complete Mirror Watch package test and the source-algorithm record. It found these concrete current-tree defects before any implementation edit:

- `.opencorvus/expert-squads/legacy-remote/mirror-watch/selector.md` supplies `expert_squad_id`, while the only production tool schema strictly accepts `profile_id` and `reason`. This would make a real selection call fail.
- `mirror-watch-architect-plan.test.ts` omits the newly mandatory `use_worktree` field from its valid research dispatch fixture, so it fails before testing Architect-to-goal admission.
- The focused closure oracle contains five stale SHA-256 values for already committed Skill bytes: `agent-browser`, both positioned perspective Skills, `playwright-cli`, and `s3-share`. The paths and file counts are unchanged, the generated payload freshness suite passes, and Git shows no later uncommitted Mirror Watch byte edits; the stale constants do not describe the package they claim to pin.
- Both projected package-tool tests fail on macOS because temporary roots are lexically under `/var` while `realpath` resolves `/private/var`. A parallel MirrorTest owner is already changing `packages/opencorvus/src/expert-squad/package-tool-bundle.ts` and its dedicated test to preserve one lexical entry-root identity while retaining realpath confinement. That file is a shared package-loading call point and is an explicit no-edit boundary for this slice. If the parallel change contradicts Mirror Watch semantics or fails to settle, implementation stops rather than creating a second fix.
- One broader built-in Skill test expects `PROVENANCE.md` in a sampled file list and currently fails on a different parallel-owned Skill surface. It is not counted as Mirror Watch acceptance and will not be patched here.

Three independent read-only agents reviewed protocol, source parity and Git collision boundaries. They agreed that Mirror Watch package, focused tests and this record had no uncommitted overlap; changing the shared package-tool bundler here would conflict with the parallel owner; and the Overlay Popover work is unrelated and must not be staged. The protocol reviewer found the selector schema mismatch. The source reviewer confirmed the current macOS environment has no Windows, WSL or shared-volume access to the source `aggregate.py`, source report template, prior comparator workspace or the Windows main database. Historical Task18 proves the earlier source comparison, but Task21 and checkpoint `9d9eba43f7996b6ebf19bfd7522c2562db6179a3` do not prove a current-byte successful chain. Therefore local tests may repair deterministic drift, but this turn must not claim fresh source parity or real E2E completion without new Windows-side evidence.

The owned correction replaces the invalid selector field with the exact `profile_id` plus concrete `reason` contract and adds a focused regression, declares `use_worktree: false` for the disjoint goal-scoped research fixture, and updates only the five stale committed closure digests without changing package Skill bytes. The official payload was regenerated and its diff contains only the selector text. The parallel package-tool owner independently committed the lexical-root correction as `a7305cebe6`; no shared bundler hunk was authored, staged or rewritten by this slice.

Current deterministic evidence after those corrections is: focused Mirror Watch `21 pass / 0 fail / 807 assertions`; payload generation plus package-tool bundling `23 pass / 0 fail / 71 assertions`; registry, manager, resolver, SkillMount and HTTP route coverage `111 pass / 1 intentional skip / 0 fail / 1104 assertions`; document health, historical links and product-doc single-source `81 pass / 0 fail / 1282 assertions`; OpenCorvus TypeScript typecheck passed; scoped `git diff --check` passed. These results accept the local package/protocol correction only. The later cancellation removes the new Windows main-database Task, current-task source-Python comparison and task-scoped HTTP visual run from the current Goal.

### 2026-07-17 current Goal authority: three real E2Es cancelled

The user's latest instruction cancels the Frontend Replica, Mirror Watch and IWC/MirrorTest real E2E runs. It is
the authoritative scope for the continuing platform Goal and supersedes every `current`, `resumed`, `fresh Task`,
`final E2E` or equivalent pending-run instruction in this record. Tasks already published by parallel processes
remain immutable historical facts, but neither their success nor their failure is current Goal acceptance.

After the parallel writer stopped, the restored `dashboard/**` run artifacts, the tracked
`dashboard/data/personas.json`, the ignored project-root `.opencorvus/ifind.json`, and the cancelled-run `.scratch`
workspaces and verifier outputs were classified as intermediate run state and removed. The package-owned Mirror
Watch manifest, prompts, tools, Skills, generated payload and deterministic regressions remain the maintained
platform integration surface. No further Mirror Watch task may be published for this Goal.

### 2026-07-17 package-input integration Recall

The user's newer instruction reopens Mirror Watch package integration after the earlier E2E cancellation and
specifically requires the source persona JSON to be synchronized into the expert squad and the observed missing
`config_path` failure to be removed at its owner. Acceptance requires both mandatory inputs to exist after normal
package installation, one source per input, no path fallback or environment inference, package/payload freshness,
focused regression coverage, and a direct isolated-backend Task only after deterministic validation succeeds.

Disk recall covered `AGENTS.md`, `specs/README.md`, `specs/current/architecture/04-extensions.md`, the July record
index, this record, the expert-squad creator checklist, and the scheduler/database evidence guide. Repository and
source searches covered every `personas.json`, `persona.json`, `config_path`, `ifind.json`, `ifind-search`, package
manifest/projection, prompt, tool, test, generated-payload and historical-record call point. The source project
declares `dashboard/data/personas.json` in `config.yaml`, but that file is absent from the source tree; the only
authoritative four-object cohort used by the migrated eight-goal workflow is preserved in Git history. The source
iFind implementation owns one HTTPS endpoint, one header, `user_id`, `app_id`, and its request policy as constants.
The migrated package instead required an arbitrary project-owned JSON path, while neither the source project,
installed package nor current OpenCorvus project contained that file. Historical `.scratch` and ignored root
copies were explicitly deleted as run residue. Thus the missing-path symptom is proven package-boundary drift,
not an iFind algorithm or caller retry defect.

The correction makes `.opencorvus/expert-squads/legacy-remote/mirror-watch/skills/delivery-contract/references/personas.json`
the visible cohort authority and `skills/ifind-search/references/ifind.json` the provider authority. Prompts use the two exact installed-package
paths; Tasks no longer supply or guess either path. The existing strict project-relative path validator remains the
single path implementation, and the iFind tool still fails visibly if the installed resource is absent or invalid.
No second config source, secret environment lookup, compatibility alias, fallback provider, host gate or workflow
state is introduced. Parallel uncommitted work is confined to Overlay and transport-protocol files; this slice
must not edit or stage those paths.

The user then clarified that self-contained also includes the report template. Full call-point inspection proves
the source renderer authority is `mirror-watch/templates/report.template.html`, injected by `scripts/build_report.py`
at `/*__REPORT_DATA__*/ null`, while the installed squad contains no HTML template asset and instead renders from
a separately authored `lib/mirror-watch/report-template.ts`. That is a translated second source, not a complete
package. The correction copies the source HTML bytes into the delivery Skill closure, makes the aggregate tool read
that exact installed-package file and inject its one data placeholder, and deletes the translated TypeScript
renderer. The report-data interfaces and script-safe JSON serialization remain execution code, not a second visual
template. Acceptance requires source SHA-256 `eab8d1c4def8be883190417cf43b8678a33123a26d7be175525322aa51b44406`,
normal package installation with no source-repository access, Registry reachability, payload freshness, aggregate
regressions, and rendered-browser review in the subsequent real Task. No missing-template fallback or alternate
renderer is allowed.

The user's final boundary expands this correction into the reusable expert-squad authoring/import SDK. Full grep
found the root validator in `ExpertSquadRegistry.TOP_LEVEL_DIRECTORIES`, tool dependency confinement and extension
closure in `PackageToolBundle.prepareWithPluginRuntime`, recursive folder/ZIP/payload copying in
`ExpertSquadPackageManager`, payload discovery/generation, the portable package generator and authoring Skill, and
the installed Codex `opencorvus-expert-squad-creator` Skill/checklist. Manager and payload code already preserve
every Registry-valid file recursively; the missing protocol is that Registry rejects a package `assets/` root and
the package-tool bundler permits only `lib/**` TypeScript, JavaScript and JSON dependencies. The plugin SDK exposes
tool execution but no separate asset path, so content-addressed static imports are the relocation-stable mechanism:
package tools import assets into their compiled closure and never resolve their installed project/global location.

The reusable correction adds `assets/` as a validated package root, permits projected package tools to import
explicit text/JSON assets from that root into their immutable bundle, records imported asset hashes in the existing
tool snapshot, and tests folder/ZIP/payload retention plus owner-boundary and unsupported-extension rejection. It
does not add a second runtime scanner, environment path, alias, fallback loader or mutable asset registry. Model-
visible reference material remains in projected Skill supporting files; tool-only templates/configuration/data use
the package asset closure. The creator Skill and portable authoring guidance must require an input inventory that
proves every runtime input is either a package Skill file, imported package asset, explicit Task value, or declared
external service—not an undeclared local repository path.

### 2026-07-17 self-contained package implementation correction

This section supersedes the intermediate installed-path design immediately above. Fixed project-relative paths
inside `.opencorvus/expert-squads/**` are not relocation-stable for user-global installation and are therefore not
self-contained runtime inputs. The final package owns `assets/personas.json`, `assets/ifind.json`, and
`assets/report.template.html`. The projected persona tool statically imports the cohort, the iFind tool statically
imports its provider configuration, and the aggregate tool statically imports both cohort and report template.
Their public contracts contain no `config_path`, `personas_path`, package installation path, source-repository path,
environment lookup, or alternate input source. The small TypeScript report module now contains only report-data
types, script-safe serialization, title encoding, and the one-placeholder adapter; it contains no visual template.

The copied source HTML exposed a real injection defect because its renderer writes multiple data fields through
`innerHTML`. The sole package HTML asset is therefore source-derived rather than byte-identical: it preserves the
source structure, Chart.js rendering and `/*__REPORT_DATA__*/ null` injection contract, while recursively HTML-
encoding all runtime strings before DOM rendering. The previously recorded source SHA-256 is provenance evidence,
not the accepted installed-asset digest. No second renderer or post-generation patch exists.

The reusable protocol now accepts top-level `assets/**`, but Registry rejects every asset that is not reachable
from a projected package tool's static dependency closure. `PackageToolBundle` permits explicit JSON/HTML/Markdown/
text/CSS/SVG imports from that root, hashes them in the existing relocation-stable snapshot, and rejects unsupported
extensions or owner-boundary escape. `ExpertSquadPackageManager` folder/ZIP import/export and generated payloads
preserve the same validated file closure byte-for-byte. The JavaScript SDK exports `expertSquadAssetPath()`; the
generated portable template includes an asset plus a projected tool that imports it; architecture guidance, the
portable authoring Skill, and the installed `opencorvus-expert-squad-creator` Skill/checklist define the same
self-contained inventory and Skill-supporting-file/tool-asset boundary.

Deterministic evidence after this correction is: SDK/Registry/package-tool/portable-template `95 pass / 0 fail /`
`463 assertions`; Mirror Watch package/input `14 pass / 0 fail / 505 assertions`; payload generation `8 pass /`
`0 fail`; canonical ZIP export/import with the statically imported asset passed; OpenCorvus and JavaScript SDK
TypeScript checks passed; and the creator Skill quick validator passed. These are protocol and package regression
results, not the still-pending main-database E2E or rendered-browser acceptance.

The user's final scope cancels that pending E2E. Acceptance for this resumed correction ends at the self-contained
package/SDK/import/generator convergence, deterministic production-path regressions, typecheck, independent review,
and repository delivery. No backend process is started or changed and no new main-database Task is published.

### 2026-07-20 complete persona cohort correction Recall

The user found that only four personas were packaged although the source workflow owns a production cohort. Disk
recall covered this record, the current expert-squad architecture and creator checklist, the active
`expert-squads/legacy-remote/mirror-watch` package, all package persona call points and tests, and the source project's
`config.yaml`, `scripts/lib/personas.py`, `scripts/run_step3_personas.py`, `scripts/lib/aggregate.py`, and
`agents/persona-surveyor.md`. Repository and source searches covered every `personas.json`, cohort-size statement,
persona parser, vote-authority comparison, prompt contract, payload input, and focused Mirror Watch regression.

The four packaged rows are proven to be the old `--limit 4` smoke cohort, not production authority. The source
workflow declares `persona_limit: 0` as all 105 personas. The complete surviving authority is
`C:\Users\chuan\Downloads\case\inputs\personas.json`: 105 rows, 163932 bytes, SHA-256
`5A3970A833C6745E8021ED8BD07F7FD9484C5D4BD0D3A78A40F8E91908B35713`; the same case contains one completed vote for
each persona. Unlike the synthetic four-row fixture, source persona objects do not contain `usage_freq`. The source
persona worker derives that answer from `daily_questions`, `recent_31d`, and `inactive_days`, and the source
aggregator uses the explicit vote frequency while taking identity, tier, status, mode, and top dimension from the
persona authority. Therefore adding a derived `usage_freq` field to the asset would mutate the source authority and
retain the migration defect.

The correction replaces the package asset byte-for-byte with the 105-row authority, removes `usage_freq` from the
persona-authority schema and equality comparison, requires the raw behavior fields needed by the worker, keeps the
vote's explicit four-value frequency contract, and updates prompts/tests/payload to prove all 105 unique identities
are installed and consumed. No four-person compatibility path, inferred package copy, alternate persona source, or
runtime path fallback is retained. Per the user's current scope, this remains deterministic convergence without a
new backend E2E Task.

Final prompt recall found that the first correction was still incomplete: requirements, architect, Orchestrator,
and README prose retained the smoke graph constants of 8 total goals, 7 graph contracts, 12 dependency reasons,
four persona goals, and six expert/persona workers. Those constants would continue dispatching only four persona
workers even with a 105-object asset. The complete graph is 109 goals (research + two experts + 105 personas +
report), 108 graph contracts (canonical survey + two expert votes + 105 persona votes), and 214 dependency reasons
(research to each of 107 workers, then every worker to report). Persona vote contract identifiers are generated
deterministically from every exact authority name; no four hard-coded user IDs remain.

Validation after the correction proves source/package SHA-256 equality, 105 rows, 105 unique UIDs, and 105 unique
names. The focused installed-input and production aggregate suite passes `14 / 14` tests with `554` assertions;
OpenCorvus TypeScript checking passes; historical-document health passes `21 / 21` tests. The shared payload was
regenerated once from the current package tree and its exact repository-package freshness assertion passes. The
full payload-generation suite is not reported as passing because concurrent WuJiang/MirrorTest work still leaves two
of that package's files outside the Git index; this correction does not stage, alter, or claim ownership of them.

### 2026-07-20 authoring-source formatting verification Recall

The repository authoring package was normalized to remove trailing whitespace from twenty explicitly inventoried
Markdown files. A post-format focused run proved that payload freshness remained correct but rejected five stale
Skill closure hashes in the immutable snapshot contract. A full-repository search found those old hashes only in
`SOURCE_SKILL_SNAPSHOT_CONTRACT`; runtime code, generated payload, and other tests do not duplicate them. The contract
is therefore updated to the closure hashes computed by the real Registry from the normalized authoring source. This
is a byte-contract update only: package behavior, projection, cohort authority, and runtime discovery semantics are
unchanged. Acceptance requires the focused Mirror Watch and payload tests to pass again on the new frozen index.
