你是 opencorvus 项目的资深架构评审者。这是第 4 轮评审。请尖锐、具体，不要客套。

## 上下文

第 3 轮评审你给过 RECOMMEND_WITH_CHANGES + 3 个具体必改项。我已经更新方案到 v4，落盘在
D:\myhexin-local\opencorvus\specs\fact-check-agent-2026-05-25.md

v4 顶部有 v3→v4 changelog。

## 你 round 3 的 3 个必改项 + 我的处理

1. **integrity 只在 consensus 阶段注入**
   - v4 §1.1：integrity 范围限定为 consensus 阶段（IntegrityTeamReport）
   - v4 §5.2：表格中明确 `team-agent.ts:265` 是唯一注入点，`:197` (plan) / `:404` (reviewer) **不改**
   - v4 §6.1.2：只 `IntegrityTeamReport` 加 `fact_check_items` 必填，`submit_integrity_review_plan` / `submit_reviewer_report` schema 不动

2. **external executor 单一适配点**
   - v4 §6.1.3：在 `runWithExternalProvider` / `runWithExternalProviderImpl` 包装层，`BuildResultSchema.safeParse(result.structured)` **之前** normalize：若 `fact_check_items` 缺失则注入 `[]`
   - 显式声明：**不**改 6 个 `structured` return literal（行 1846/1865/1954/1979/1999/2017），那是 rule 8 双源违规
   - external executor 不参与 fact-check 登记，是 §1.2 已声明的不覆盖范围

3. **step 1 含 prompt/runtime stub**
   - v4 §8 step 1：注册 + 最小 `fact-check-core.txt` 占位文本 + 最小 `FactCheckAgent.run` stub（throw "not implemented"），保证每个 commit 可 build

## v4 还做的事

- §5.1：抽 `withFactCheckRegistration(core: string): string` 辅助函数，放在 `fragments/fact-check-registration.ts`，6 处调用点统一调用（rule 9 抽象设计模式）
- §6.1.1：schema 单源位置矛盾修正——统一 `fact-check/schema.ts` 是 FactCheckItemSchema 等的家，`tools.ts` 与 6 worker schema 都 import 此处
- §7.2 e2e D：明确 SessionStatus.set 同进程模拟（参考 test/engine/active-sessions.test.ts:92-99）
- §6.1.1：明确 `Session.snapshotLatestAssistant` 需要新增（基于现有 `Session.messages()` + `SessionStatus.get()` 组合）
- §11：把 round 3 的 8 个问题逐项答了
- §12：列出 round 4 待复核的 6 个新问题

## 必答清单

### A. Round 3 的 3 个必改项是否真的解决？
逐项 ✅ / ⚠️ / ❌，≤ 60 字。

### B. v4 §12 6 个复核问题逐项回答（≤ 100 字）
重点 #2（循环依赖）和 #3（external executor normalize 是不是 rule 7 fallback）。

### C. v4 是否引入新问题？
重点查：
- `withFactCheckRegistration` 放在 `prompt/fragments/fact-check-registration.ts` 是否合适？这个文件名带"fact-check"业务概念，辅助函数嵌入里面似乎合理；但 build/types.ts、requirements/output-tools.ts 等导入此模块是否产生不合理的依赖方向？
- `fact-check/schema.ts` 单源：worker schemas 在 build/、architect/ 等模块里 import `@/fact-check/schema`——会不会让 fact-check/ 成为底层依赖、与"fact-check 是一个 agent"的语义错位？
- external executor 的 normalize 步骤是否真的不算 rule 7 fallback？

### D. CLAUDE.md 违规复核（v4）
还有任何违规吗？重点：
- v4 §6.1.3 在 safeParse 之前 normalize：算 fallback 吗？rule 7？
- v4 §8 step 1 的 stub（throw "not implemented"）：算"未完成实现"吗？rule 28b "禁止把失败包装成基本完成"——但 stub 是显式占位，应该不算

### E. 实施风险与顺序
v4 §8 实施步骤现在合理吗？还有强串行链没排清的地方吗？

### F. 最终结论
明确给出一个标签：
- `RECOMMEND_PROCEED`（直接推进实施，无需再改）— 必须确认你没有任何剩余诉求
- `RECOMMEND_WITH_CHANGES`（必须改后才能推进）— 列出剩余必改项数量；若 ≤ 2 项且全是 minor，请直接给 PROCEED
- `RECOMMEND_REJECT`（建议拒绝整个方向，换替代架构）

我们已经迭代到第 4 轮。若 v4 仍有问题，请评估这些问题是"必须设计阶段修改"还是"可以在实施时按方案推进、有问题再 fix"——后者应给 PROCEED。

最后一行必须以 `VERDICT: <label>` 结尾。

## 输出约束
- markdown，分节
- 全文 ≤ 3500 字
- 对你不确定的明确说"我没查到"，不要编
- 不修改代码，不修改方案文件
