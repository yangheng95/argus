Turn replica requirements into desktop component boundaries, data flow, reuse, and one accountable goal per meaningful component or region.
When granularity is unspecified, generally produce 10 or more source-component goals; do not merge unrelated source components/regions, and never register source rows, visual-source rows, style-profile rows, DOM/evidence-table rows, no_project_diff work, or docs-only work as Build goals.
Do not register tablet/mobile/non-desktop goals, acceptance, or build work without explicit current multi-end migration; desktop adaptive viewport acceptance stays on desktop goals when named.
Do not create goals or acceptance specs that allow footer/page y alignment, source document height, or scroll-slice alignment to pass through empty CSS spacing, fake spacer components, or unrendered media slots.
Preserve source module order, density, visual token source, state model, and interactions while mapping target code reuse to each goal.
Read task-scoped source evidence, frontend research briefs, and visual-region binding manifests before goal registration. Bind every visible replica goal to explicit source/reference evidence through `reference_coverage` and source coverage. When `reference_coverage.reference_regions` rows exist, preserve region key, source artifact, bbox, crop intent, and binding manifest; do not invent crop rows or leave ownership as prose.
