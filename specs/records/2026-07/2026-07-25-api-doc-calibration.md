# API and documentation calibration after the July 24 refactor

## Recall

- User request: establish a persistent goal that calibrates the broadly stale API, generated SDK/OpenAPI surfaces, and documentation after the preceding 24 hours of refactoring, then continue auditing until a complete pass finds no new issue.
- Acceptance criteria:
  - live runtime routes, generated OpenAPI, generated JavaScript SDK routes, and generated English/Chinese API references agree;
  - hand-written product documentation lists the live mounted route modules;
  - contract tests describe the current strict request schemas instead of requiring retired fields;
  - historical/current documentation health, product-document single-source, route inventory, SDK generation, typecheck, and normal Git hooks pass;
  - a second full audit pass finds no new API or documentation drift.
- Hard constraints:
  - preserve every concurrent tracked and untracked change in the shared worktree;
  - do not restore compatibility or fallback contracts, especially the retired `routing.evaluation` path;
  - do not restart, stop, refresh, or otherwise interfere with a running OpenCorvus or Overlay process;
  - use the canonical generation owners, normal hooks, and the legacy remote;
  - stage only calibration-owned files.
- Sources read:
  - `AGENTS.md` and `packages/opencorvus/test/AGENTS.md`;
  - `specs/README.md`, `specs/records/2026-07/README.md`, and `specs/current/architecture/15-agent-facts-and-turns.md`;
  - `packages/opencorvus/src/server/routes/app.ts`, `packages/opencorvus/src/server/routes/chat.ts`, `packages/opencorvus/src/engine/model.ts`;
  - `packages/opencorvus/script/check/routes.ts`, `packages/opencorvus/script/docs/render-api-md.ts`;
  - `packages/opencorvus/test/script/document-health.test.ts`, `packages/opencorvus/test/script/routes-check-openapi.test.ts`;
  - `packages/web/src/content/docs/server.mdx` and `packages/web/src/content/docs/zh-cn/server.mdx`.
- Whole-repository search evidence:
  - searched live route modules, `AppRoutes` mounts, `CreateTaskInput`, `StageRouting`, `routing.evaluation`, generated API body listings, anonymous-project and Chat capability paths, and all public/current documentation references;
  - `bun run api:routes-check` reports 32 route modules clean across six rules;
  - `bun run docs:check` reports 287 operations in 24 groups;
  - the document-health suite found the missing `chat.ts` entry in both Server pages;
  - the OpenAPI drift suite found one stale assertion that still required the retired `routing` field;
  - historical records that mention retired routing are retained as dated evidence, while current architecture explicitly records its removal.
- Independent agent feedback: not collected because the operator did not authorize sub-agent delegation for this task; the main agent performs the required second review directly.

## Baseline and ownership

The worktree already contains broad concurrent route, SDK, generated API, Overlay, test, and spec edits. Those bytes remain owned by their existing work. The calibration task does not regenerate over them while the canonical checks already prove them internally synchronized.

| Surface or call site                                      | Evidence                                                                   | Calibration disposition                                                            |
| --------------------------------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `AppRoutes` import and `/chat` mount                      | `chat.ts` is a live mounted route module                                   | Keep implementation; add the missing module name to both Server pages              |
| `packages/web/src/content/docs/server.mdx`                | Route table omits `chat.ts`                                                | Update                                                                             |
| `packages/web/src/content/docs/zh-cn/server.mdx`          | Route table omits `chat.ts`                                                | Update                                                                             |
| `CreateTaskInput` in `engine/model.ts`                    | Strict schema removed `routing` in commit `a2c802b9e`                      | Keep current API                                                                   |
| Generated OpenAPI, SDK, and API references                | Current generated task-create body omits `routing`; route/docs checks pass | Preserve concurrent generated bytes                                                |
| `routes-check-openapi.test.ts`                            | Static expected task-create body still requires `routing`                  | Replace expectation and add a negative retired-field assertion                     |
| `EnterpriseArchitectureExplorer.astro` artifact inventory | Shows retired `acceptance_candidate`; omits three current artifact kinds   | Synchronize with `ENGINE_ARTIFACT_KINDS` and visually verify the rendered explorer |
| `web-landing-page.test.ts` architecture evidence phrase   | Requires the pre-refactor sentence that omitted observable refusal         | Update to the current architecture statement                                       |
| `toolbeam-docs-theme` font imports                        | Theme requires an undeclared `@fontsource/ibm-plex-mono` package           | Add the exact direct dependency and a regression assertion                         |
| `15-agent-facts-and-turns.md` review history              | A superseded finding says `build_attempt_outcome` still exists now         | Mark it as review-time history superseded by later revisions                       |
| `04-extensions.md` built-in Skill paths                   | Package-relative paths look like nonexistent repository-root paths         | Publish full repository paths and guard them in document health                    |
| Dated historical records mentioning `StageRouting`        | Historical evidence, with current architecture recording retirement        | Retain; do not rewrite history                                                     |
| July monthly/root indexes                                 | Concurrent untracked records are already linked                            | Add this record without deleting or reordering concurrent entries                  |

## Iteration protocol

1. Repair the two proven documentation/test drift points.
2. Run the focused failing suites and canonical route/docs checks.
3. Run the entire script-test directory, SDK tests, typecheck, and documentation build/check surfaces.
4. Search public and current architecture docs for retired route, field, operation, and module claims introduced by the last 24 hours of commits.
5. Repeat the checks after every repair. Completion requires one final clean pass that discovers no additional issue.

## Verification ledger

This section is updated as calibration proceeds.

- Baseline `api:routes-check`: passed, 32 route modules and six rules.
- Baseline `docs:check`: passed, 287 operations and 24 groups; later concurrent route work increased the clean generated inventory to 290 operations in the same 24 groups.
- Baseline document-health: failed because both Server pages omit mounted `chat.ts`; two additional failures refer to concurrent untracked records already linked from the monthly index.
- Baseline OpenAPI drift suite: failed because its static task-create body expectation still includes retired `routing`.
- Expanded script suite: found a stale Enterprise Architecture Explorer artifact inventory and a stale promotional-evidence assertion after the July 24 fact/Turn refactor.
- Expanded script suite found seven tracked `.scratch` soak-fixture files. They were removed from the tracked checkout and the five repository-residue assertions now pass. The latest complete script pass reports 323 passed and five failed: one document-index failure for six concurrent untracked records, plus four executable-not-found failures because neither Windows PowerShell nor PowerShell Core is installed on the current macOS host. These are separately attributed rather than hidden.
- Expanded script suite found the concurrent anonymous-project promotion writing `ProjectTable` and `SessionTable` outside their approved single-writer modules. The transaction now delegates each write to its owning service; all 17 database-boundary tests and the real promotion route test pass.
- A second public-doc path audit found the Chinese agent-loop guide pointing to deleted `session/tool-resolver.ts`; both languages now identify `packages/opencorvus/src/session/loop.ts` and a regression test rejects the retired path.
- The same audit found both Overlay overview pages listing deleted `TaskList.tsx`, `InteractionPanel.tsx`, and `WorkflowProgressBar.tsx` owners plus a retired Settings sidebar inventory. They now name `WorkLedger.tsx`, `InteractionCard.tsx`, the existing `TracePanel.tsx`, and the `CONFIG_SECTIONS` navigation source; a bilingual file-existence test guards every listed component.
- Every handwritten `METHOD /path` reference in the English and Chinese Mission/Task guides was checked against the generated OpenAPI operation map. All current references are live, and the new exhaustive regression test prevents a future stale route from landing.
- Canonical SDK generation followed by `docs:api` synchronized the concurrent Chat capability description; the next `api:routes-check` and `docs:check` pass reports 32 route modules, 287 operations, and 24 groups.
- The focused documentation/API second pass reports 133 tests passed across document health, historical links, product-doc single source, OpenAPI drift, SDK format, Enterprise Architecture Explorer, and website evidence suites.
- Repository typecheck passed all nine scoped tasks. Astro check reports zero errors, and the production build generated 105 pages plus the Vite Enterprise Architecture Explorer bundle.
- Real Vite visual inspection first exposed the missing IBM Plex Mono package and drove the direct dependency repair. The post-repair screenshot could not be reacquired because the in-app browser rejected the localhost URL under its URL safety policy; no alternate browser or raw protocol was used to bypass that policy.
- Commits `c60514354`, `0cde9a729`, `f40471a20`, `9cce843b9`, and `988beab0e` preserve the completed repair waves. The first push attempt ran normal hooks and passed typecheck, API route drift, and API docs checks, then stopped on incomplete locale keys from a concurrently edited Sub-agent Progress Grid; the hook was not bypassed.
- A later pre-push pass caught concurrent MCP Apps schema/generator drift. Operator review rejected an invented renderer-version increment for an unreleased contract; the source schema, real resource materializer, OpenAPI, JavaScript SDK, active plan, and regression fixture now retain the single `mcp-app@1` identity while replacing its unpublished raw-HTML shape in place.
- The same-contract MCP App repair migrated the Overlay consumer and real browser fixture from the retired flat fields to the authoritative `server`, `tool`, and `resource` objects. Focused protocol and Overlay tests pass, repository typecheck passes, and the Node-launched headed Vite browser test passes after its fixture gained the newly required `/file` response.
- A later real-browser pass against the concurrently completed Host found invalid CSP output: empty resource domains produced `'none'` beside `blob:`, `data:`, and `'unsafe-inline'`, so browsers ignored that token; `frame-ancestors` was also ineffective in a meta policy. The generator now emits no appended resource token when none is declared, retains standalone deny-by-default directives, and omits the ineffective directive. Direct CSP regressions cover default-deny and declared connect/resource domains. Visual review of `packages/overlay/.scratch/overlay-inline-mcp-app-light.png` confirms that the current Host renders its app title, loaded state, action control, inline/fullscreen/PiP controls, frame height, and artifact chrome without clipping or overlap.
- The new artifact-scoped Host regression passes both backend cases: it derives the exact server/configuration binding from the artifact, filters model-only tools, persists visible tool evidence, forwards resource/prompt requests, and replaces rather than duplicates the visible model-context part.
- The latest canonical SDK and API-document regeneration reports 290 operations in 24 groups. `api:routes-check` remains clean across 32 route files and `docs:check` is clean. The focused document suite passes 99 of 100 tests; its sole failure identifies six July records already linked from the monthly index but still untracked in the shared worktree. Those records belong to concurrent tasks and are preserved rather than deleted or silently staged by this calibration.
- The latest focused API/config pass exposed one over-broad source assertion: the global-config ownership test rejected legitimate `Provider.resetAll()` calls anywhere in `global.ts`, including unrelated Provider auth and refresh routes. The regression now isolates the exact PATCH `/global/config` route block, proves that it delegates to `updateGlobalConfigPatch`, and rejects duplicate invalidation only inside that handler. The repaired test passes all three cases; the wider focused API/config run otherwise passed 206 cases.
- Concurrent record ownership has since reduced the document-index mismatch from six records to two: the MCP Apps Host and Settings audit records remain linked but untracked. No generated API, route, historical-link, or product-doc content check is failing.
- A normal pre-push hook then caught a transient duplicate declaration of the new MCP resource/prompt list-change events while the shared Host implementation was still converging. Whole-file call-site enumeration proved that both notifications are emitted with an exact server name and consumed by the artifact event stream, so the final source retains one required-server schema for each event. Repository typecheck passes all nine tasks again; no hook was bypassed.
- The next hook pass caught the Host event-stream teardown using the non-contract initiator `"consumer"`. It now uses the transport protocol's existing `"consumer-dispose"` value, matching the stream service's intentional-close handling; the next nine-task typecheck is clean.
- The following route hook correctly rejected the stale generated snapshot after the artifact-scoped MCP event stream was mounted. Canonical SDK generation and bilingual API-doc rendering now include `GET /session/{sessionID}/interactive-artifact/{artifactID}/mcp-app/events` and the required-server event schemas. The synchronized inventory is 291 operations in 24 groups; route and docs checks are clean.
- The real configured MCP server/SessionLoop integration passes and proves that an exact tool plus `ui://` resource creates one bound `mcp-app@1` message artifact. The three artifact-scoped Host cases and six protocol/persistence cases also pass in an isolated database run. A combined multi-file database run produced `SQLITE_IOERR_VNODE`; isolating the MCP route file passed all 16 cases, proving test-process database interference rather than a product regression.
- Browser-fixture review found that the new MCP App Host event-stream request previously fell through to a 404 while the generic aborted-event-stream allowance hid the failed connection. The fixture now serves a real SSE response with `mcp-app.connected`, and the Node-launched headed test explicitly proves the exact event path is requested on initial render and again after replay. The repaired browser test and fresh visual review pass.
- Artifact ownership review then found that `findInteractiveArtifact` constrained only artifact and session IDs even though the database is global. Every caller now supplies the authoritative current project ID, the SQL joins `SessionTable` and requires that project, and the GET route regression proves the same session/artifact pair returns 404 from a foreign project directory. All ten isolated protocol, Host, and real SessionLoop cases pass, followed by a clean nine-task typecheck.
- The request route documented 404 but its missing-artifact and wrong-renderer branches still threw generic errors, producing 500. The Host now raises the repository `NotFoundError` for both cases. Route regressions prove a same-project non-MCP artifact and a cross-project MCP request both return the documented 404 with the correct reason; focused tests, typecheck, route inventory, and the 291-operation docs check remain clean.
- Current panel architecture still described the retired iframe-only subset after the production Host landed. It now records the real server/tool/resource binding, stored snapshot and configuration identities, same-server proxy, visible tool/message/context evidence, confirmation-owned links/downloads, metadata-owned permissions, display lifecycle, and artifact event stream. A document-health regression rejects the retired claim and pins the new single-source contract; historical-link checks remain clean.
- A whole-repository correction audit confirms that the unreleased renderer still has exactly one identity: `mcp-app@1`. There is no successor renderer identity, version-two label, or retired flat payload-field reference. The nested `server`, `tool`, and `resource` contract replaced the unpublished v1 shape in place; no version bump or compatibility parser was introduced.
- The production Vite build then found a real compiler failure that cached typecheck had missed: `McpAppArtifact.tsx` declared both the native Host transport and the AppBridge transport as `transport`. The owners are now named `hostTransport` and `appTransport`, and the renderer source regression prevents the duplicate binding from returning.
- Real browser interaction exposed a renderer-lifecycle failure: appending the visible MCP tool part changed the message-part renderer structure, remounted the iframe, and discarded the app result. Message parts now retain stable indexed owners and the interactive artifact renderer preserves identity across adjacent tool evidence; focused unit and headed chronology tests cover the behavior.
- Conversation ownership review found that `ui/message` delegated to the currently selected session instead of the artifact-bound session. The canonical prompt sender now accepts an explicit session and directory; the ordinary active-panel path delegates to it, while the MCP App Host always supplies the artifact owner. A regression changes the active selection to a foreign session and proves the request still targets the exact artifact session.
- The Node-launched headed production test now uses a real stdio MCP server, real SessionLoop artifact creation, exact artifact Host proxy, and official AppBridge. It proves visible tool evidence, list-change events, model context, a successful visible `ui/message` turn, resources/templates/prompts, logging, confirmation-owned link/download actions, model-only tool rejection, inline/fullscreen/PiP, light/dark presentation, refresh replay, and teardown. The current-goal screenshots were inspected after the latest run.
- A later runtime-suite audit found a test importing the retired `fetchFullDiffs` API and a task-message OpenAPI fixture omitting the now-required `metadata` field. Both fixtures now use the current contracts. Focused runtime and route/OpenAPI regressions pass.
- The final live-code sweep caught a concurrent MCP lifecycle refinement after the earlier browser pass. MCP App artifacts are now created at tool-input time and update one strict `input-streaming` / `running` / `completed` / `cancelled` / `error` lifecycle in place; the real SessionLoop and browser fixtures still expected the retired result-level `display` array. Both now read the single persisted artifact part from `MessageStore`, assert the completed lifecycle, and register the processor lifecycle surface without restoring result compatibility.
- The same headed pass proved the exact-session consequence of `ui/message`: a real user turn legitimately replaces the artifact as the active conversation tail. Acceptance now completes explicit teardown and replay before sending the app message, proves its artifact/server metadata in the bound Session, and aborts only the isolated fixture turn during cleanup. The inline catalog fixture also accepts the exact Session prompt request instead of pretending it is a Task message.
- Browser runners were rerun serially after a deliberate parallel run demonstrated cross-fixture settings contamination. The final serial evidence passes the production Host, twelve-renderer catalog, message chronology, and Skill/MCP Settings projection suites; no product patch was made for the contaminated run.
- The latest complete script suite passes 324 of 329 cases. Its only repository-health failure is the two linked untracked July records; the other four are Windows/WSL PowerShell execution cases on a macOS host with neither `powershell` nor `pwsh` installed. All platform-independent script checks pass.
- A forced typecheck after the lifecycle merge caught a closure-narrowing error in the concurrent Skill catalog lock repair: `item.dir` was proven before, but not inside, the asynchronous source-lock callback. The callback now captures the validated `itemDir` once. Forced typecheck passes all nine tasks and the 31 Skill manager/source-boundary regressions pass.
- Focused Overlay source assertions still named the retired flat `tool.input` and `tool.result` fields after lifecycle delivery landed. They now pin the five official lifecycle bridge sends and explicitly reject those flat fields. The isolated Host suite also uses the correct configured versus runtime-bound MCP client seam; all four Host cases pass when run in their database-owned process.
- After both July records were staged, the complete script suite passed 325 of 329 cases. The only four failures are the Windows/WSL synchronization cases that require a `powershell` executable absent from this macOS host; all repository, API, generated-document, historical-link, packaging, database-boundary, and platform-independent script checks pass.
- The final headed browser pass added a second, input-streaming MCP App artifact and exposed three further lifecycle defects: the test endpoint was mounted behind the production wildcard route, mutable artifact reads were cacheable, and one EventSource was opened per artifact. The harness now owns its route before delegating to `Server.App`, artifact reads return `Cache-Control: no-store`, and one session-scoped Host event stream fans exact `artifactID` and `serverID` events to subscribers.
- The same browser pass proved that the upstream bridge helper sends from `setHostContext` without returning a Promise, leaking `Not connected` errors when theme/geometry effects raced cleanup. Dynamic context updates are therefore gated by the initialized bridge lifetime and disabled before teardown begins. The real browser suite passes without console, page, response, or request failures; the cancelled, fullscreen, PiP, dark, confirmation, error, and teardown screenshots were visually reviewed.
- Final canonical generation reports 291 operations in 24 groups. Forced repository typecheck passes all nine tasks, the Vite production build transforms 4,960 modules, SDK build, API route inventory, bilingual docs check, Overlay i18n check, and both staged/unstaged whitespace checks pass. The correction scan has zero successor MCP App renderer identities, version-two labels, retired flat payload fields, or flat lifecycle consumers.
- The final isolation audit proved the earlier 12 failures from a combined Bun process were mostly global MCP-client mock and shared-database contamination: project instance cache passes 33 cases, MCP routes 23, Skill routes 27, browser stdio four, scoped connection ownership two, and the real MCP App SessionLoop case one when run under their actual ownership boundaries. The remaining isolated prompt/resource process found a real OAuth cleanup defect: failed credential removal skipped callback and pending-flow cleanup. Runtime cleanup now settles in `finally`, while the storage error remains visible; its mocked client suite passes all 21 inner cases without binding the live application's fixed callback port.
- The final headed-browser startup found two additional runtime-only defects after static checks were already green: session-scoped MCP connections read `Instance.directory` from a missing implicit context while registering notifications, and layered global configuration called an unimported `pipe`. Connection creation now resolves its directory once from the explicit `cwd` or project context and passes it to notification registration, while configuration imports the canonical Remeda function. Scoped-owner, global/config route, forced typecheck, and the real production Host browser suite all pass after repair.
- The event-stream description changed during the last convergence pass, so route inventory correctly rejected the stale tracked OpenAPI. Canonical JavaScript SDK generation and bilingual API-doc rendering were rerun; the final inventory remains 291 operations in 24 groups across 32 route files and six route rules.
- A post-push shared-worktree wave exposed three more incomplete contracts and was not silently left behind. Inherited Instance leases now temporarily release and reacquire their original read/write mode around a transactional Chat-capability preflight, allowing the preflight to run before one late initializer without deadlock or a second state source. Global disposal waits for that active outer lease to return before taking write ownership. All 36 Instance-cache cases and all 27 MCP route cases pass, including concurrent inherited preflight serialization, retrying a remote transport close after its Config definition was durably removed, and keeping an explicitly disabled configured or built-in server manually connected until explicit disconnect without mutating its durable definition.
- Global merge-patch deletion now rejects attempts to delete lower-file-owned values without mutating the writable higher-precedence file. The atomic writer reads the effective global stack with `writeSchema: false`, the route documents its 409 response, and SDK/OpenAPI/bilingual API docs were regenerated at the unchanged 291-operation inventory. All five global-config route cases pass.
- The headed Channel Settings audit found that a project switch could leave the previous project's provider form visible even though the save path correctly rejected its stale owner. `ConfigDialogHost` now passes the current project directory as an explicit reactive prop, and `ChannelsPanel` renders its nested form only while that directory matches the captured owner. The forced Vite build and Node-launched browser test pass; the inspected QQ Bot dialog screenshot has no clipping, overlap, or accessibility-label regression, and no stale-directory Config PATCH is emitted.
