# v0.0.7beta c52 macOS Repackage

## Recall

- User request: code has been updated; package the application again.
- Acceptance criteria: confirm `v0.0.7beta` matches the latest `legacy-remote` remote, rebuild the native macOS ARM64 GUI package from that exact source, and verify architecture, signature, disk image integrity, packaged backend self-hosting, isolated health, artifacts, and hashes.
- Hard constraints: preserve existing untracked native artifacts until replaced by the packaging pipeline; do not restart or alter the user's running Overlay; do not claim non-native platform rows; remove transient generated schema output; commit with the `dsw-33987` prefix and push the record to `legacy-remote/v0.0.7beta`.
- Sources read: `AGENTS.md`, the package-matrix and packaged-sidecar repair records, `script/package-gui-installer-matrix.ts` ownership established by those records, branch/remote status, commit `c52d9f0dee`, package output, application `Info.plist`, and isolated `/global/health` evidence.
- Whole-repository search: the root `package:gui-installer-matrix` command remains the single native installer build/staging entry; the compiled sidecar remains under `packages/opencorvus/dist/opencorvus-overlay-server-darwin-arm64/opencorvus`; `/global/health` remains the backend health endpoint; staged darwin-arm64 assets remain the GUI executable, DMG, and application archive.
- Independent agent feedback: none; the user did not request sub-agent work.

## Source and execution

After `git fetch legacy-remote v0.0.7beta`, local and remote `v0.0.7beta` both resolved to `c52d9f0dee4a5f50da37a5055a2e2b7767a7445b`. The worktree contained only the prior untracked darwin-arm64 artifacts. `bun run package:gui-installer-matrix` regenerated the SDK input, built the Overlay production bundle, compiled and ad-hoc signed the Tauri application, produced the DMG and application archive, staged the three release assets, and truthfully skipped non-native matrix rows.

## Verification

- `OpenCorvus.app` and the staged GUI executable pass `codesign --verify --deep --strict` / `codesign --verify --strict`; `hdiutil verify` reports a valid DMG checksum.
- The staged GUI and compiled backend sidecar are Mach-O ARM64. Application short/build versions are both `0.0.7-beta`.
- The compiled sidecar self-hosts Bun `1.3.13`. With `PATH=/usr/bin:/bin`, isolated `HOME` and `OPENCORVUS_HOME`, and port `47882`, `GET /global/health` returned `healthy: true`, version `0.0.7-beta`, and runtime paths entirely under the temporary home. SIGINT shutdown reported zero live tasks, runs, sessions, and tool parts, then released the port.
- SHA-256: sidecar `0674782f52bd8667e808d044d4c7a3f7c9b10ba6d55aa3d1b32ac17f989eff49`; staged GUI `53f92d52578ae73230fdfafb9cb170f92b27f26688c2751c4335e3af66b0029b`; DMG `8946500804d6d02e3476f405b115552f8c3d692ff225b192fe66736719482d79`; application archive `209cc4cf0f38131f31ad5c76681df29935429514016ee9d62129fcb00ae3877d`.
- Second review: artifact hashes differ from the preceding package while the sidecar hash remains stable because `c52d9f0dee` changed only browser visual verification and its merge record. The native package is tied to the latest remote commit, and no running user process was touched.
