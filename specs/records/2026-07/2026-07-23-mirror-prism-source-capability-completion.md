# Mirror Prism Source Capability Completion

## Recall

| Item                  | Requirement or evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| User requirement      | Check whether the Prism Expert Squad completely integrates every capability from the original source under `/user/document/output`, explain how to complete it, then implement the completion. The resolved source root is `/Users/yangheng/Documents/output`.                                                                                                                                                                                                                                                                         |
| Acceptance            | Preserve the explicit user-invoked `mirror-prism-cluster` launcher and the five independent Expert Squad architecture. Add exact generic and AInvest collaboration contracts, restore the missing AInvest mapper/wiki and design-skill projections, prove native replacements for web search/UI/UX capture, and replace prose-only parity assertions with a machine-readable source capability contract plus executable validation.                                                                                                    |
| Source inventory      | Active `mirror-prism-pipeline/SKILL.md` v2.21.4; 11 active `prism-team` roles; 7 source Skills (`ainvest-wiki`, `claude-design`, `design-md`, `get-ui`, `get-ux`, `popular-web-designs`, `web-cli`); 7 templates; 6 scripts; 150 reference files. `SKILL.md_sol`, `mirror-prism-prd-old.md`, and `mirror-prism-prd-fallback.md` are excluded candidate/retired inputs.                                                                                                                                                                 |
| Proven gaps           | The cluster's AInvest path selected `mirror-prism-prd-stage`, which omitted `mirror-prd-ainvest-feature-mapper`; mapper/author/reviewer lacked `ainvest-wiki`; Mirror Design lacked `claude-design`, `design-md`, and `popular-web-designs`; the only collaboration always forced financial Mirror Watch and therefore did not truthfully cover generic targets; parity tests checked hand-written phrases instead of the source inventory.                                                                                            |
| Native replacements   | Source `web-cli` maps to OpenCorvus web search plus `webpage_extract`/`webpage_compile`/`webpage_runtime_state`; `get-ui` maps to Browser MCP navigation, observation, screenshot, and diagnostics; `get-ux` maps to Browser MCP interaction, keyboard/focus, screenshot, and diagnostics. These are strict capability replacements, not copied provider fallback chains.                                                                                                                                                              |
| External limitations  | The export contains only `scripts/kanban.sh`, not the required official `mirror-kanban/scripts/dispatch-bff.mjs`; remote Kanban cannot be truthfully restored. Mission has `panel.create_task` but no platform-owned project-creation/registration primitive, so automatic fresh-project creation remains unavailable and must stay explicit rather than be simulated by shell or prompt.                                                                                                                                              |
| Hard constraints      | No monolithic Prism revival, host keyword router, optional workflow node, fallback, alias, hidden state, process restart, new worktree, or duplicate AInvest design source. Each collaboration definition chooses one exact mandatory path before the first stage Task. Preserve the unrelated unstaged formatting edit in `2026-07-22-mirror-prism-full-workflow-distillation.md`.                                                                                                                                                    |
| Read records and code | Current architecture rule 15.1; `2026-07-22-mission-squad-stage-task-orchestration.md`; `2026-07-22-mirror-prism-full-workflow-distillation.md`; all five package manifests and stage prompts; cluster Skill and references; source role/Skill/template/script/reference inventory; Registry/SDK/package/payload tests; Mission panel tool surface.                                                                                                                                                                                    |
| Whole-repository grep | `mirror-prism-cluster` is projected only by Mirror PRD; collaboration references and tests are concentrated in the cluster Skill and `mirror-squads-package.test.ts`; `mirror-prism-prd-stage` is referenced by the collaboration, PRD manifest, package tests, SDK tests, and records; package Skill refs are manifest-owned and resolved only through `ExpertSquadRegistry`/`PromptProfileResolver`; no Mission project-creation tool exists.                                                                                        |
| Independent feedback  | A later user-requested independent review found that the Mirror PRD Orchestrator prompt did not enumerate the new AInvest Mission workflow, source-capability owners were ambiguous bare strings, the SDK briefly duplicated an incomplete Registry manifest schema, and the README confused agent-level Skill projection with AInvest-only use. The follow-up correction makes workflow selection explicit, types every owner, keeps strict schema validation Registry-owned, and states the AInvest prompt-use condition accurately. |

## Implementation disposition

| Surface                         | Change                                                                                                                                                                                                                                                                                       |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cluster collaboration           | Replace the ambiguous single delivery definition with exact `mirror-prism-generic` and `mirror-prism-ainvest` definitions. Generic starts at Mirror PRD; AInvest retains Mirror Watch and selects the AInvest PRD stage. Recovery definitions remain exact and unchanged.                    |
| Mirror PRD workflow             | Add `mirror-prism-ainvest-prd-stage`; require `mirror-prd-ainvest-feature-mapper` before AInvest authoring. Keep `mirror-prism-prd-stage` generic.                                                                                                                                           |
| AInvest knowledge               | Import the exact `ainvest-wiki` Skill closure into Mirror PRD and project it to the AInvest mapper, author, and reviewer. Their prompts load and apply it only for AInvest Goals; the generic workflow excludes the mapper.                                                                  |
| Mirror Design knowledge         | Import exact `claude-design`, `design-md`, and `popular-web-designs` Skill closures; project them to the page designer and project the review-relevant design contract to visual/integrity reviewers.                                                                                        |
| AInvest design source           | Do not duplicate Mirror Watch's `ainvest-design-system` Skill. The AInvest collaboration requires Watch source evidence to hand off the visible design-system evidence and citations consumed by PRD/Design.                                                                                 |
| Native source tools             | Record and test the exact source-to-native mapping for web search/extraction, UI screenshots, and UX interaction capture against manifest tool and MCP projections.                                                                                                                          |
| Source capability contract      | Add a repository artifact enumerating each active source role, Skill, template/script/reference group, owner, disposition, and verification surface; tests reject missing/duplicate ownership and verify imported closures by SHA-256 digest against an explicit source root when available. |
| Payload and metadata            | Bump affected package versions, update package README/Skill metadata, regenerate the tracked Expert Squad payload, and run `skill-creator` quick validation for every changed Skill.                                                                                                         |
| Honest unavailable capabilities | Keep remote Kanban and fresh-project creation named as unresolved platform/upstream dependencies. Do not claim complete integration while either is absent.                                                                                                                                  |

## Verification

- Focused source-capability, Expert Squad Registry, SDK collaboration, payload-generation, and dynamic package tests.
- `skill-creator/scripts/quick_validate.py` for imported and launcher Skills.
- Historical-doc links and document-health tests.
- `bun run typecheck`, `bun run api:routes-check`, and `bun run docs:check`.
- Second diff review of every source capability owner and all collaboration/workflow dependency edges.
- Real live generic/AInvest/repair E2E remains a separate runtime acceptance requirement and may not be represented by mocked contracts.

## Verification ledger

| Verification              | Result                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source capability audit   | `mirror-prism-source-capability.test.ts` verifies all 11 active role files, seven source Skills, four imported Skill closures, three native replacements, the 7-template/6-script/150-reference pipeline asset groups, 20 ordered pipeline steps/sections, four collaboration definitions, and the two explicit unavailable integrations.                 |
| Package/Registry/Resolver | 126 focused OpenCorvus tests passed across package loading, payload generation, Registry topology, scheduler projection, General, Review & Debug, Mirror packages, and repository dynamic packages.                                                                                                                                                       |
| SDK                       | All 39 SDK tests passed, including manifest dispatch validation, generic/AInvest success contracts, two Review & Debug recovery contracts, source capability validation, and writer round trips.                                                                                                                                                          |
| Documentation             | Historical link and document health suites passed: 82 tests. English and Chinese public SDK/Agent docs plus current architecture now describe Task-only review adapter constraints and the source-capability validator.                                                                                                                                   |
| Toolchain                 | `bun run typecheck`, `bun run api:routes-check`, `bun run docs:check`, and `bun run overlay:i18n-check` passed.                                                                                                                                                                                                                                           |
| Skill validation          | `ainvest-wiki`, `mirror-prism-cluster`, `claude-design`, `design-md`, and `popular-web-designs` passed the system Skill `quick_validate.py` validator.                                                                                                                                                                                                    |
| Operational limit         | No live end-to-end website recreation was claimed. Remote Kanban remains unavailable because the exported source lacks the official dispatcher, and fresh-project provisioning remains unavailable because Mission has no project create/register primitive. Existing-project generic and AInvest package paths are statically and dynamically validated. |

## Dispatch and SDK calibration addendum

### Recall extension

| Item                            | Requirement or evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Follow-up requirement           | Classify the failure boundary between Expert Squad and platform, continue the complete Prism-chain analysis, inspect every Expert Squad for analogous dispatch hazards, verify the original `/Users/yangheng/Documents/output` capability inventory, calibrate affected packages, and update the Squad authoring SDK.                                                                                                                                                     |
| Platform/squad classification   | The Author projection and workflow topology were package defects. Integrity ignoring Goal `workScope` and Browser diagnostics blocking successful actions were platform defects. The repaired platform now exposes package contracts that only appeared to work while scope was ignored.                                                                                                                                                                                  |
| State at addendum start         | Exact AInvest Wiki and three Design Skill closures, generic/AInvest collaboration split, and the AInvest PRD stage were present as uncommitted work. The machine-readable capability contract and executable audit were then still absent and had to be completed without overwriting that work.                                                                                                                                                                          |
| Whole-repository topology audit | Audited General plus every repository-authored manifest: Frontend Innovate, Frontend Replica, Research Studio, Review & Debug, Mirror Watch, Mirror PRD, Mirror Design, Mirror Code, MirrorTest, and the portable template. Goal nodes have canonical Requirements/Architect ancestry. Two pure Task workflows still misuse the Goal/spec-bound Integrity adapter.                                                                                                          |
| Proven sibling defect 1         | All Review & Debug workflows explicitly forbid Goals and active spec snapshots, yet `review-debug-code-reviewer` and `review-debug-integrity-reviewer` use `base_role: integrity`. Dispatch therefore stops in the platform Integrity runner before either read-only review session can start. Their real contract is Task-scoped delegated read-only review.                                                                                                             |
| Proven sibling defect 2         | General `interface-delivery` reuses the Goal/spec-bound `system-integrity-reviewer` at Task scope after a Task-only interface graph. It has no Requirements snapshot or Goals, so it cannot reach review. Planned delivery still needs the real Integrity adapter and must retain it; interface delivery needs a separate Task-scoped delegated reviewer identity.                                                                                                        |
| SDK gap                         | `validateExpertSquadCollaboration` checks Goal planning ancestry but neither the SDK writer nor Registry rejects Task-scoped Integrity nodes without Goal ancestry. The same invalid package shape can therefore be authored, installed, projected, and blocked only during execution.                                                                                                                                                                                    |
| Source inventory evidence       | Active source comprises one pipeline Skill, 11 active Prism role files, seven Skills, seven templates, six scripts, and 150 references. Retired PRD variants and `SKILL.md_sol` are not active capability sources. Imported Skill bodies/resources retain exact source content while required frontmatter is normalized and both source/target digests are pinned; source `web-cli`, `get-ui`, and `get-ux` are replaced by declared native webpage/Browser capabilities. |
| Preserved dirty work            | Do not overwrite the active source-capability package edits or the unrelated `2026-07-22-mirror-prism-full-workflow-distillation.md` edits. Integrate only after reviewing their complete diff.                                                                                                                                                                                                                                                                           |

### Exhaustive call-point disposition

| Surface                                          | Disposition                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ExpertSquadRegistry.validateProjectionTopology` | Add the adapter/scope semantic invariant beside existing DAG and Goal-lineage validation: an Integrity node may be Goal-scoped, or Task-scoped only when it is downstream of canonical Goal work. Reject pure Task graphs that cannot supply a spec/Goal review scope.                                                |
| SDK `expert-squad-authoring.ts`                  | Export the same pure manifest dispatch validation, invoke it before rendering/writing packages and for every collaboration manifest, and retain no workflow execution state.                                                                                                                                          |
| Review & Debug manifest/prompts                  | Reproject both Task-only reviewers as `delegated-worker`, require their visible delegated result, preserve read-only ownership, and keep all workflows Goal-free. Do not manufacture Requirements/Architect/Goals merely to satisfy Integrity.                                                                        |
| General interface workflow                       | Add one `interface-integrity-reviewer` delegated reviewer for the Task-only interface graph. Preserve `system-integrity-reviewer` as the real Goal-scoped Integrity identity in planned delivery. Update builtin embedding and catalog tests.                                                                         |
| All other manifests                              | Preserve current scopes where the topology matches adapter semantics. Mirror Design/Code Task aggregate Integrity is valid because it is downstream of canonical page Goals; PRD, Frontend, MirrorTest, General planned-delivery, and template Integrity are Goal-scoped.                                               |
| Prism source capability contract                 | Add one repository-owned machine-readable inventory under `specs/artifacts/` covering every active role, Skill group, native tool replacement, pipeline stage group, recovery path, and explicit unavailable source integration. Validate unique ownership and exact squad/workflow/agent references through the SDK. |
| Existing Prism completion work                   | Review and retain the generic/AInvest split, exact imported Skill closures, projections, and source-to-native mappings; add package/payload/SDK tests rather than a parallel mapping.                                                                                                                                 |
| Generated payload and docs                       | Regenerate after all package changes. Document the new SDK manifest and source-capability validators in English/Chinese SDK authoring references and current architecture.                                                                                                                                            |

### Extended acceptance

1. No repository-authored workflow dispatches the Integrity adapter at Task scope without downstream canonical Goal evidence.
2. Review & Debug read-only Task reviewers start through delegated-worker sessions and cannot require a spec snapshot or Goal.
3. General planned delivery retains real Goal-scoped Integrity; General interface delivery uses a distinct Task-scoped delegated reviewer.
4. Registry, SDK rendering/writing, and collaboration validation reject the invalid pure-Task Integrity shape with the same behavioral explanation.
5. The machine-readable Prism source capability inventory accounts for every active source role, Skill group, stage group, recovery path, and unavailable upstream integration exactly once, with valid current owners and evidence.
6. Generic and AInvest collaboration definitions validate independently; AInvest contains Watch and the mandatory AInvest mapper/wiki path, while generic does not force financial research.
7. Every affected package payload, SDK test, Registry test, dynamic projection test, document check, typecheck, and route check passes.

### Independent-review correction verification

| Verification              | Result                                                                                                                                                                                                                                                                                                                                      |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source filesystem closure | The repository audit now enumerates the live `/Users/yangheng/Documents/output/skills`, non-retired `prism-team` Markdown files, and non-hidden pipeline entries and compares them with the contract. All seven source Skill roots, 11 active roles, the pipeline Skill, three asset groups, and retired/excluded inputs are accounted for. |
| Typed ownership           | SDK tests reject a role/native owner that names the right Agent under the wrong Squad and reject pipeline owners or groups that name an undeclared platform surface. Agent owners are `{ squad_id, agent_id }`; pipeline owners are exact squad, collaboration, or declared platform references.                                            |
| Dispatch topology         | The repository-wide audit includes all 11 authored manifests, including Research Studio, and proves Goal planning ancestry plus the Task-scoped Integrity downstream-evidence rule. Mirror PRD package tests assert the Orchestrator enumerates all four direct/Mission workflow IDs and maps the AInvest Mission path explicitly.          |
| SDK and package suites    | The complete SDK suite passes 43 tests. The focused Registry, Mirror, portable-template, SDK authoring, and Prism collaboration set passes 103 tests; the source-filesystem audit passes four tests; scheduler capability projection passes 17 tests.                                                                                       |
| Toolchain and Skill       | SDK build, repository typecheck, API route check, public documentation generation check, Overlay internationalization check, and the portable authoring Skill validator pass.                                                                                                                                                               |
| Documentation             | Historical links, document health, and product-doc single-source suites pass 87 tests after the separate Mission Skill planning task finished its own index update. This repair did not delete, stage, or commit that task's files.                                                                                                         |
| Independent review        | Final result: `ACCEPT`. The read-only reviewer independently verified all four Prism contracts, the live 7-Skill/11-role source closure, typed owner failures, SDK/Registry ownership, all 11 manifest topologies, payload parity, 160 focused tests, documentation health, typecheck, and staged-diff hygiene.                             |
