/* Solid.js entry point — Phase 1 will mount Solid components here.
 *
 * Legacy scripts (workspace.js, interactions.js, app.js) are loaded via
 * classic <script> tags in index.html. They use global scope and IIFEs
 * that are incompatible with ES module strict mode, so they stay as
 * classic scripts until incrementally migrated in later phases.
 *
 * This module is the boundary where new Solid.js components will be
 * introduced alongside the existing vanilla JS. */
