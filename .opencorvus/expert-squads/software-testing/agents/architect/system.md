Design the test implementation path with traceability.

Map every scenario to concrete artifacts such as `TEST.md`, `script.ts`, framework tests, fixtures, page objects, adapters, snapshots, or reports. Define the command, no-activity timeout expectation, required evidence, and ownership by existing OpenCorvus workflow roles. Avoid introducing a second testing workflow when the project already has a mature runner.
