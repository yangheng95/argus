你是 opencorvus 项目的资深架构评审者。这是第 2 轮实施评审。请尖锐、具体，不要客套。

## 上下文

Round 1 你给过 `REQUEST_CHANGES` + 5 个 critical 问题 + 1 个 hygiene gap。我已经全部修复，落在新 commit `2c4468481`：
`fix(fact-check): codex impl review round 1 — error-outcome persist, scope check, target text, integrity replay, hygiene`

## 你 round 1 的 5 + 1 必改项 + 我的处理

1. **tool_error / aborted 不持久化** → 修复：orchestrator/tools.ts 加 `synthesizeToolErrorReport` 工厂；catch 块 wraps `recordFactCheckAttempt` with outcome=aborted/tool_error；synthesized report 是 schema-valid 的 inconclusive verdict + unresolved/tool_failed entry。新增 e2e [E] cancel 测试验证。

2. **target message content 没传给 fact-check agent** → 修复：fact-check/index.ts 加 `loadTargetMessageText()` 读 Message.stream，取 text+reasoning 部分拼接（capped 8000 chars）；buildFactCheckUserPrompt 加 "# Target message content" 段。

3. **scope consistency check** → 修复：orchestrator/tools.ts 在 FactCheckAgent.run 返回后，host 侧 assert `report.scope.{session, agent, message_id, content_hash}` 等于 snapshot 输入。不一致 → outcome=tool_error + 持久化合成 artifact。新增 e2e [H] scope-mismatch 测试。

4. **integrity replay 不消费 fact_check_attempt** → 修复：integrity/replay-context.ts 加 `IntegrityPriorFactCheckAttempt` 类型 + `priorFactCheckAttempts` 字段；buildIntegrityReplayContext 用 listFactCheckAttempts 填充；renderIntegrityReplayContextPrompt 新增 "Prior fact-check attempts on this task" 段（cap 10 newest）。7 个 integrity test fixture 同步更新。

5. **`?? []` fallback in team-agent.ts** → 修复：删 fallback，直接 `result.fact_check_items`（required，broken contract should throw loudly per rule 7）。

6. **prompt hygiene test 缺失** → 修复：test/agent/core-prompt-hygiene.test.ts 加 "fact-check core anti-recursion" 测试，walking 每个 `<fact-check>` literal 和 `fact_check_items` 提及，断言在 NOT/forbid 窗口内。同时调整 fact-check-core.txt line 7 从 "registered `fact_check_items[]`" 改为 "registered factual claims for verification"（避免唯一一处 positive context）。

## 新增测试

- e2e [E] cancel：agent throw AbortError → tool returns "aborted" + persists outcome=aborted artifact
- e2e [H] scope mismatch：agent returns inconsistent scope → tool returns tool_error + persists synthetic inconclusive
- worker-prompt-composition.test.ts (新)：5 tests 验证 6 个 worker 都正确注入 fragment + integrity 仅 consensus 注入 + fact-check 自己不注入 + fragment 单源

## 测试状态
- 7/7 fact-check e2e
- 5/5 worker-prompt-composition
- 131/134 fact-check + agent + integrity 总和（2 fails 都是 pre-existing 的 consensus traceability + reviewIntegrity active path，git stash 验证过）

## 必答清单

### A. Round 1 问题是否全部解决？
逐项 ✅ 解决 / ⚠️ 部分 / ❌ 未解决，附 ≤ 80 字说明。

### B. v2 是否引入新 bug？
重点查：
- synthesizeToolErrorReport 的 unresolved entry 的 severity 是 "blocking"。verdict 决策树里 unresolved.blocking 应该 → "needs_orchestrator_action"。但我设置 overall_verdict 直接为 "inconclusive"。这是不是 schema 通过但语义矛盾？
- loadTargetMessageText 的 fallback：如果 `Message.stream` 错（DB 错误、不存在 session），catch 后返回空字符串 + log warn。这算 rule 7 fallback 吗？还是合理的"读不到就空"的数据完整性容忍？
- priorFactCheckAttempts 加进 IntegrityReplayContext type 后，所有构造点都补了 fixture，但 buildIntegrityReplayContext 之外有没有别处构造 IntegrityReplayContext 类型对象？
- e2e [H] 是否真的能触发 scope mismatch 路径？我用 mock 返回错误的 target_message_id；但是 hash 等其他字段没改。

### C. CLAUDE.md 合规复核
- v2 §4: priorFactCheckAttempts 加进 replay context 是否合规？rule 5/6（禁过度工程）—— 这个字段未来 6 个月可能没人用，是不是 premature widening？
- v2 §6: hygiene 测试加 fact-check 那条，是不是太严格？fact-check-core.txt 在 "Anti-Recursion" heading 之后用 `fact_check_items`，但 heading 在 line 15，使用在 line 17，distance 200 chars 内合规。但未来 prompt 编辑很容易破。

### D. 实际跑测试
请实际 grep + bun test 验证：
- 我说 131 pass / 2 fail，请独立验证。
- worker-prompt-composition 5 个 case 是否真的覆盖所有 worker？
- 之前 round 1 列出的 missing tests（agent.test.ts、session-kinds 专项），是否还有遗漏？

### E. 实际查代码
- 跑 `rg -n "ctx\.sessionID|ctx\.signal" packages/opencorvus/src/orchestrator/tools.ts` — fact_check tool 是否真的全部用 input.signal / input.agentSessionID？
- 跑 `rg -n "fact_check_items\s*\?\?" packages/opencorvus/src` — 是否还有 fallback `?? []` 残留？
- 跑 `rg -n "factCheck" packages/opencorvus/src/integrity` — integrity 是否真的接入 listFactCheckAttempts？

### F. 最终结论
- `APPROVE`：可合并，无遗漏
- `APPROVE_WITH_NITS`：minor 问题（≤ 3 项）
- `REQUEST_CHANGES`：仍需返工，列关键问题

最后一行 `VERDICT: <label>`

## 输出约束
- markdown 分节
- ≤ 3500 字
- 不确定就说"我没查到"
- 不修改任何代码 / 文件
- 实际 rg / read 验证每个断言
