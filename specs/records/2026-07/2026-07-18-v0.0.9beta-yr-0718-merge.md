# v0.0.9beta yr-0718 Merge

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Merge `https://github.com/yangheng95/opencorvus.git/-/commits/work-v0.0.9beta-yr-0718`. |
| Target interpretation | Merge legacy remote branch `legacy-remote/work-v0.0.9beta-yr-0718` into the current Windows-host delivery branch `v0.0.9beta`, then push `v0.0.9beta` to `legacy-remote`. |
| Acceptance criteria | Preserve every pre-existing tracked and untracked worktree change; fetch and merge the latest work-line head; resolve overlapping changes by retaining one authoritative implementation per surface; run focused validation, documentation health, normal push hooks, and a post-merge diff review; prove local and legacy remote delivery heads converge. |
| Hard constraints | No fallback, compatibility alias, state-machine gate, broad reset, extra worktree, hook bypass, or intervention in running OpenCorvus/Overlay processes. Commit subjects start with `dsw-33987`. Push only the legacy remote. Browser validation, if required by conflict resolution, must use Node-launched Playwright and isolated fixtures. |
| Sources read | Repository `AGENTS.md`; `specs/records/2026-07/2026-07-18-v0.0.9beta-branch-and-version-bump.md`; `specs/records/2026-07/2026-07-08-v0.0.2-yerui-0707-merge.md`; `specs/records/2026-07/2026-07-18-work-ledger-hover-and-right-dock-visible-multi-tabs.md`; current crypto benchmark record; both spec indexes. |
| Git baseline | Current branch `v0.0.9beta` and `legacy-remote/v0.0.9beta` began equal at `771737c5c5`. Fetched work-line head is `d6289d1c27`. Their merge base is `fe12be0f31`. Both sides contain unique commits. |
| Whole-repository grep / call-site inventory | The incoming merge-base diff was enumerated completely with `git diff --name-status`. Incoming changes span Work Ledger projection/routes, Overlay App/dialog/composer/files/settings/form primitives/styles/tests, generated SDK/OpenAPI, and July spec indexes/records. The dirty worktree was enumerated with `git status --porcelain`; direct overlap includes `App.tsx`, both locale files, `main.tsx`, `app-shell.test.ts`, titlebar browser coverage, Mailbox coverage, architecture guards, and related Right Dock terminology work. Every incoming path is therefore handled by Git's three-way merge and each reported conflict is reviewed directly rather than by path-name assumptions. |
| Preservation boundary | The worktree already contained the active crypto benchmark and Mirror Watch package work plus an Overlay Right Toolbar-to-Right Dock terminology cleanup, including tracked deletions and replacement untracked files. Those changes are not merge output and must be preserved in a dedicated pre-merge snapshot commit before the overlapping merge begins. |
| Independent agent feedback | None. The user did not request sub-agents; current collaboration rules prohibit unrequested delegation. |

## Plan

1. Add this Recall record and both canonical spec-index entries, then commit and push the entire dirty worktree as an explicit pre-merge preservation snapshot.
2. Merge `legacy-remote/work-v0.0.9beta-yr-0718` with a normal merge commit.
3. Inspect every conflict against the merge base, current side, incoming side, current architecture records, and direct callers; converge each surface to one implementation.
4. Run tests selected from the actual conflict and incoming change surfaces, plus documentation health and normal repository checks.
5. Review the resulting tree and merge diff a second time, update this record with exact evidence, commit any resolution/evidence changes, and push `v0.0.9beta` to `legacy-remote` through normal hooks.

## Progress

- [x] Read on-disk plans and Recall blocks before modification.
- [x] Fetch and inventory the latest remote work line, divergence, incoming paths, dirty paths, and overlap.
- [x] Preserve and push the pre-merge worktree snapshot.
- [x] Merge and resolve all conflicts.
- [x] Validate, review, record evidence, and push the integrated delivery branch.

## Merge Resolution

- Preservation commit `489daf1077` captured all 58 pre-existing tracked/untracked paths plus this Recall record and was pushed to `legacy-remote/v0.0.9beta`. The normal pre-push hook passed SDK imports, AI runtime checks, nine package typechecks, route inventory, generated API documentation, Overlay localization, and secret scanning.
- Git reported six content conflicts. `ExpertSquadPanel.tsx` retains the explicit equal global/project install actions and shared outline Button primitive. `main.tsx` combines the Right Dock terminology convergence with the incoming click-owned Mailbox attention lifecycle: `rightDockOpen` suppresses attention only while Mailbox is already visible, explicit Mailbox opening clears attention, and `presentMailboxNotification` no longer auto-opens the Dock. The Composer browser test uses the real `model-selector-hexin-budget` node and the new shared `.oc-select-icon`. Mailbox tests and both spec indexes retain both sides' contracts and records.
- The first isolated browser pass exposed one deeper incoming regression outside the textual conflicts: the new bounded default Button radius changed the Mission task-disclosure capsule from `999px` to `8px`, while both branches' browser contract and the incoming primitive design record require semantic capsules to remain pills. The Work Ledger disclosure now explicitly projects `--oc-radius-pill` through the shared Button custom property; no duplicate button recipe or alternate component was added.

## Verification Evidence

- Conflict-surface unit tests: 83 passed with 862 expectations across Mailbox attention/panel, Button/Search/Select/settings primitives, Work Ledger consolidation, and backend Work Ledger routes.
- Overlay TypeScript check passed with no diagnostics. Historical documentation links passed 21/21. The real browser fixture rebuilt the production Vite bundle successfully with 2,500 transformed modules; only the existing chunk-size advisory remained.
- Node-launched Playwright rerun passed 4/4 scenarios: Composer action Button chrome, Windows titlebar/left Dock compositor continuity, workspace editor action, and the integrated message-header/Right Dock/Mailbox path. The runner uses inactivity-based timeout ownership and isolated fixtures.
- Visual second review inspected `.scratch/workspace-mailbox-attention-launcher.png`, `.scratch/right-dock-mailbox-click-open.png`, `.scratch/chat-composer-button-primitives.png`, and `packages/overlay/.scratch/left-dock-platform-material/win32-light.png` at original resolution. The Mailbox launcher is aligned beside Search and visibly accented only for attention; click opens the unchanged Mailbox Dock; Composer controls remain within their shared surfaces; the Windows titlebar and left Dock have no horizontal seam.
- Full historical/document-health/product-document verification passed 88/88 with 1,432 expectations after all indexed records became tracked. Overlay localization passed after removing five locale keys whose only owners were retired by the concurrently preserved Environment/Goals convergence.
- Merge commit `8ef5941461` integrates work-line head `d6289d1c27`. Post-merge preservation commits `e67f1eedb1` and `ef8f345e15` keep concurrent Environment convergence work traceable without presenting it as completed acceptance; `bd84a1d46d` removes its retired locale residue. A final fetch proved local `v0.0.9beta` and `legacy-remote/v0.0.9beta` identical at `bd84a1d46db76f83b940142b07cbc00e136deec7`. The server-side ref race rejected a redundant push only after another concurrent push had already advanced the remote to that exact target hash.
