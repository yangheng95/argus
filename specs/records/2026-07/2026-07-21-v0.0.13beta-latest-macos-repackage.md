# v0.0.13beta Latest macOS Repackage

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Pull the current branch and package it. |
| Acceptance criteria | Fast-forward local `v0.0.13beta` to the latest git-cc remote commit without losing local files; build the canonical native GUI release row from one immutable commit; validate the executable and installer assets; report exact artifact paths, sizes, hashes, architecture, signatures, and disk-image verification. |
| Hard constraints | Use `bun run package:gui-installer-matrix` without `--skip-build`; do not hand-assemble release assets, create a worktree, reset/stash/restore files, bypass hooks, or restart/refresh a running OpenCorvus/Overlay. Only the native macOS ARM64 row may be claimed as packaged. Preserve the two pre-existing untracked `.DS_Store` files. Commit subjects start with `dsw-33987`; pushes use `myhexin`. |
| Sources read | Repository `AGENTS.md`; `docs/packaging.md`; root `package.json`; `script/package-gui-installer-matrix.ts`; current branch/status/remotes; latest remote divergence; prior macOS repackage records. |
| Whole-repository grep | Root `package:gui-installer-matrix` is the canonical public GUI packaging entry. It transactionally rebuilds the SDK, Overlay server, Vite frontend, and Tauri bundles, then stages the native executable, DMG, and application archive under `packages/overlay/dist-artifacts/darwin-arm64/` and validates them with `check-release-assets.ts --require-bundle`. `package:binary-matrix` is a separate portable CLI surface and is not a canonical release asset owner. |
| Pull evidence | A fresh fetch found local `e3b9eea65` behind `myhexin/v0.0.13beta` by 22 commits. `git pull --ff-only` advanced cleanly to `ef151843e`; only `expert-squads/.DS_Store` and `expert-squads/builtin/.DS_Store` remain untracked. |
| Independent agent feedback | None requested; current collaboration policy forbids unrequested delegation. |

## Plan

1. Index and commit this Recall, re-fetch, and push it through normal hooks so packaging uses one traceable source commit.
2. Verify release-family version consistency and focused packaging contracts.
3. Run `bun run package:gui-installer-matrix` without shortcuts and let the repository owner rebuild, stage, and validate the native row.
4. Independently inspect artifact formats, ARM64 architecture, code signatures, DMG integrity, embedded application metadata, sidecar version, sizes, and SHA-256 hashes.
5. Confirm HEAD did not move during the build and the two pre-existing untracked files remain preserved; record honest native-row and skipped-row results.

## Verification Ledger

- Packaging source `bfae8fc584deb62dcbed1d92a30b8d68b7da89ba` was pushed to `myhexin/v0.0.13beta`; version synchronization, 107 focused packaging/document tests, the full pre-push typecheck, route inventory, generated API documentation, Overlay internationalization, and secret scan all passed before the build.
- The first canonical build failed before native compilation because the strict expert-squad payload generator correctly rejected the pre-existing `expert-squads/.DS_Store` as a non-directory namespace entry. Both `.DS_Store` files were hashed, temporarily moved to one `mktemp` preservation directory, and restored by an exit trap after the retry. Their post-build SHA-256 values exactly match the originals.
- `bun run package:gui-installer-matrix` then completed without `--skip-build`. It rebuilt the SDK, production frontend, embedded backend, optimized Tauri application, ad-hoc signatures, DMG, and application archive. The repository release validator passed `darwin-arm64`; Linux x64/ARM64, Darwin x64, and Windows x64 were explicitly skipped because matching native runners were unavailable.
- Independent validation confirms both the staged GUI executable and backend sidecar are ARM64 Mach-O executables; strict code-sign verification passes for the application bundle and staged executable; `hdiutil verify` reports a valid DMG checksum; the application identifier is `ai.opencorvus.overlay`; application and sidecar versions are `0.0.13-beta`; and the application archive contains the expected `OpenCorvus.app` structure.
- Staged assets: `opencorvus-overlay` is 162,789,040 bytes with SHA-256 `dccb53f6b58104576538610472dd23e90ba7b26a04fba4595d1458ba7f6900eb`; `OpenCorvus_0.0.13-beta_aarch64.dmg` is 153,787,859 bytes with SHA-256 `2835a56435b89453d0ad72d8496b6a7776a472592d2fdbd769578852f168eec8`; `OpenCorvus_0.0.13-beta_aarch64.app.tar.gz` is 153,088,088 bytes with SHA-256 `aa9adfde761973a22a0afd9f93d0f60fca3bbb5a9d7403c7646cc43389ce7944`.
- HEAD and `myhexin/v0.0.13beta` remained equal at the packaging source commit throughout the build, and the worktree returned to only the two preserved untracked `.DS_Store` files.
