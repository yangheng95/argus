# Overlay Functionality Integrity Recovery

## Recall

| Field | Content |
| --- | --- |
| User request | After the destructive Overlay UI refactor, audit which capabilities were accidentally removed, repair frontend/backend detachment and missing required functionality, and start Vite for end-to-end GUI testing. The user additionally requires that i18n verification not be omitted. |
| Acceptance criteria | Every capability is classified as intentional replacement, proven regression, or unresolved; proven frontend/backend contract breaks are repaired through one authoritative route and UI path; removed behavior is not restored by remounting retired components; focused tests cover every behavior change; an isolated Vite instance is exercised through real GUI flows and screenshots in both `en-US` and `zh-CN`; typecheck, API route inventory, i18n, docs, and independent final review pass. |
| Hard constraints | No fallback, compatibility route, hidden dual source, fake fixture-only E2E claim, state-machine workaround, or broad Git restore/reset. Preserve unrelated dirty work. Do not restart, refresh, close, or kill the user's running OpenCorvus/Overlay. Browser automation uses Node, never Bun. Preview ownership remains task-scoped backend target/evidence. |
| Sources read | `AGENTS.md`; the user's local July route-planning draft; `2026-07-10-iteration-technical-debt-remediation.md`; `2026-07-10-overlay-codex-strict-parity-remediation.md`; `2026-07-08-mission-task-chat-toolbar-consolidation-impact.md`; `2026-07-09-browser-preview-header-project-action-visibility.md`; current and `HEAD` Overlay components/tests; current Work Ledger, task, Skill, Composer, Browser Preview, and backend route sources. |
| Whole-repository grep | `git diff --name-status -- packages/overlay/src packages/overlay/test`; `rg -n "renameCodingAssistantSession|renameMission|downloadMissionProjectArchive|loadWorkLedger|nextCursor|Scheduled|work_ledger.kind.task" packages/overlay packages/opencorvus`; `rg -n "browser-preview-(address|local-home|node-comment|node-selection|progress)|saveTaskBrowserPreviewTarget|selectTaskBrowserPreviewTarget|onCommentDraft" packages/overlay packages/opencorvus`; `rg -n "project.*(copy|rename)|copy.*project|rename.*project|Work Ledger|pagination" specs packages/overlay`; `rg -n "experimental|Scheduled tasks|schedule" packages/overlay packages/opencorvus`; route and caller searches recorded in the capability matrix below. |
| Independent agent feedback | Poincare read-only audit found three P0 breaks: global task pagination derives a created-time cursor while `/global/tasks` orders by updated time; Skill Market POST mount/unmount/import calls no longer match the sole `PATCH /skill/mount` contract and return 404; Composer Stop passes `remote:false`, so it aborts only the local stream despite promising task termination. It also confirmed Work Ledger pagination loss and the fake Scheduled search behavior. The agent ran 32 focused checks, with one existing Work Ledger CSS failure, and directly probed the three Skill mutations as 404. |
| Baseline | `packages/opencorvus bun run typecheck` passed; `packages/overlay bun run typecheck` passed; root `bun run api:routes-check` passed. These compilation checks do not prove feature reachability. |

## Capability Integrity Matrix

| Severity | Surface | Evidence | Disposition |
| --- | --- | --- | --- |
| P0 | Global task pagination | Overlay constructs and tests a created-time cursor while backend `/global/tasks` sorts and pages by `engine_task.time_updated`. | Repair the client cursor/order contract to consume the backend's authoritative response shape; add update-order multi-page coverage. |
| P0 | Skill Market mount lifecycle | Visible UI calls `POST /skill/mount`, `/skill/unmount`, and `/skill/import-and-mount`; backend exposes one `PATCH /skill/mount` identity-aware contract. Direct probes return 404. | Replace old client mutations and fixture assumptions with the canonical PATCH contract. Do not restore deleted routes. |
| P0 | Composer Stop | `main.tsx` calls `stopChatRequest({ remote: false })` while UI text promises remote task termination. | Wire the visible Stop command to the existing authoritative remote cancellation/abort lifecycle and test terminal settlement. |
| P1 | Work Ledger pagination | Service and backend return `nextCursor`; `WorkLedger.tsx` requests 80 rows once and discards the cursor. | Add cursor ownership, append semantics, deduplication, and a reachable load-more control. |
| P1 | Mission actions | July 8 design requires abort/download/rename/delete; service methods still exist, but unified Work Ledger wires only abort/delete. | Restore download and rename in the unified row action model and main wiring. |
| P1 | Chat actions | July 8 design requires stop/rename/delete; rename service still exists, but unified Work Ledger wires only stop/delete. | Restore rename in the unified row action model and main wiring. |
| P1 | Scheduled navigation | Button merely sets Work Ledger search text to localized `Task`; backend schedule routes are unrelated and unreachable. | Remove the false semantic behavior and implement a real schedule surface through the canonical schedule routes, or remove the control until that complete surface lands. No mislabeled search shortcut may remain. |
| P1 | Pinned current project | Clicking the project shortcut creates a new Chat rather than selecting/opening the project. | Route it through project selection; New Chat remains a separate explicit command. |
| P2 | Browser Preview arbitrary address / node comment | Current backend intentionally makes persisted task target artifacts the only target source; PUT selects an existing target and arbitrary URL bodies are rejected. | Treat deleted arbitrary-address editing as intentional replacement. Do not restore it or node-comment UI without a current task-scoped evidence contract. |
| P2 | Project copy/rename controls | Current tests explicitly remove old copy/rename UI and old workspace rename service; backend project rename capability still exists but is not part of the unified Work Ledger contract. | Keep out of the first repair batch; audit separately as an unreachable backend capability instead of reviving stale callbacks. |
| P2 | Retired Mission/Task/Chat panels | Unified Work Ledger and composer intent selector are deliberate single-source replacements. | Do not remount deleted lists, toolbar, or legacy CSS. Restore only missing behavior in the unified surface. |
| P1 repaired | Queue reorder | The retired `TaskList` exposed queued-task drag reorder, but the unified Work Ledger initially retained only Start Now. | Work Ledger now projects authoritative `queueOrder`, enables same-directory queued-row drag only when search and pagination cannot make the queue partial, calls the canonical PATCH route, reloads after success/failure, and surfaces errors. |
| Intentional backend contract | Existing-task budget mutation | The budget PATCH route and an old fixture exist, but neither current nor `HEAD` Overlay production code exposed a budget editor; task creation still owns initial budget input. | Keep as an API/automation capability. It is not a UI regression from the destructive Work Ledger refactor. |
| Intentional maintenance contract | Project cleanup candidates | The read-only cleanup-candidates route had no current or `HEAD` Overlay production entry point; project deletion remains available through the unified Work Ledger. | Keep cleanup candidate inspection as an operator/maintenance API rather than adding a second project-cleanup UI source. |
| New requirement | Global task project ownership | `global` currently means cross-project listing, while no global create route allocates a concrete project. | Add one backend `POST /global/tasks` entry that creates a dated UUID project under `Global.Path.data`; every task retains a real project and directory, and `global` never becomes a project ID. |
| New requirement | Delegated context density | Persisted internal prompts are real `role=user` messages, but the Overlay currently renders their full context beside the agent's normal answer because `tree-writer` discards persisted origin after bridge enrichment. | Preserve `role`, `author`, `channel`, direct-reply `source`, and `messageID` through the one transcript projection. Default-collapse user-role context delivered into internal sessions, except persisted `source=overlay_direct_reply` human replies; keep content expandable/auditable, keep main user messages and assistant answer bodies expanded, and do not keyword-match prose or create a filtered message stream. |

## Implementation Order

1. Repair P0 data/action contracts and replace tests that currently encode obsolete behavior.
2. Complete Work Ledger cursor pagination and Mission/Chat action wiring in the unified component.
3. Correct false sidebar command semantics, then complete the scheduled-task surface only against canonical backend routes.
4. Run the broader deleted-component and unreachable-route audit; update this matrix before each additional implementation slice.
5. Start an isolated Vite frontend with an isolated real OpenCorvus backend, run Node-driven GUI flows, capture task-scoped screenshots, and inspect them manually.
6. Repeat GUI scenarios in `en-US` and `zh-CN`; verify labels, dialogs, errors, empty/loading states, focus paths, clipping, and overflow.

## Verification Contract

- Focused unit/route tests for each changed contract, including negative cases proving obsolete routes/behaviors stay absent.
- `bun run typecheck` in `packages/opencorvus` and `packages/overlay`.
- `bun run api:routes-check` at repository root.
- `bun run check:i18n` in `packages/overlay` plus bilingual real-render GUI assertions.
- `node packages/overlay/test/browser-runner.mjs <focused tests>`; Playwright is never launched with Bun.
- Isolated `vite --config vite.config.ts` instance and real GUI screenshots, without touching the user's running Overlay.
- `bun test packages/opencorvus/test/script/historical-docs-links.test.ts` and relevant document-health tests.
- Independent read-only adjudicator review after all automated checks; benchmark success alone is insufficient.
- Delegated-context verification must cover live `message.updated`, part-before-message replay, transcript hydration, a mixed context/assistant segment, keyboard disclosure behavior, and an inspected browser screenshot.

## Progress

- [x] Baseline typecheck and API route inventory.
- [x] Deleted/replaced component inventory and first frontend/backend contract audit.
- [x] Independent read-only integrity review.
- [x] P0 contract repairs.
- [x] Unified Work Ledger completeness repairs.
- [x] Remaining reachability audit and required repairs.
- [x] Isolated Vite plus real-backend bilingual GUI evidence.
- [x] Broad verification and independent final adjudication.

## Runtime Verification Evidence

- `POST /global/tasks` created `projects/2026/07/11/72d7e659-c856-4876-a017-a90e26926c1e`, initialized `.git`, returned concrete task/project/directory identity, and projected one project with one task in the real Work Ledger.
- A real missing-model request returned HTTP 400. Before compensation it exposed an empty-project defect; after repair, both generated UUID leaf count and `ProjectTable` rows remain unchanged.
- Isolated backend `127.0.0.1:4197` and Vite `127.0.0.1:5187` were exercised through the in-app Node browser. English and Chinese renders had no onboarding, no page overflow, one rooted project, and no locale-change alert.
- English global copy renders `What should we build?` and explains automatic project creation. Chinese renders `你想构建什么？` with fully localized notice and suggestions.
- Focused Overlay contract suites pass when mock-heavy files are isolated by process: panel task creation 1/1, task init/global routing 9/9, locale/config ownership 18/18, and the broader non-conflicting integrity set 183/183. Combining every mock-heavy file in one Bun process is invalid because `mock.module` registrations are global across test files.
- Overlay and OpenCorvus typechecks pass; `check:i18n`, `build:vite`, `api:routes-check`, `git diff --check`, historical links, and product docs single-source checks pass.
- The initial document-health run exposed two repository-index failures: the July index referenced untracked formal records and a tracked record depended on a local planning draft path. The formal records are now indexed and the draft-path dependencies are recorded only as natural-language history; the final document-health matrix passes.
- Delegated context now follows persisted origin metadata end to end: the bridge stamps `role/author/channel/originSource` on part events, `tree-writer` records exact delegated `messageID` segments, and `CardParts` uses one shared disclosure without duplicating or filtering transcript content. Main user messages, persisted `source=overlay_direct_reply` human replies, and all assistant-role answers remain expanded.
- Focused delegated-context checks pass: 76 Overlay projection/classification tests, 16 bridge tests, strict i18n validation, Overlay/OpenCorvus typechecks, Vite production build, and the Node browser chronology test. Inspected English and Chinese screenshots prove the collapsed row stays transparent and low-density while the following Architect answer remains visible; expansion reveals the original context body.
- A fresh isolated runtime at `127.0.0.1:4198` used `.scratch/integrity-e2e-home-gpt55` as its proven database/data root. Its real global catalog reported connected `hexin` with API model ID `gpt-5.5`; `POST /global/tasks` created dated UUID project `f6989583-ce4b-4050-8ede-d7777b54d891` and task `tsk_f4dc28a4d001BtesPOSeFRak5B` with concrete project ownership.
- The first `gpt-5.5` probe exposed a terminal ordering defect: `complete_task` durably completed the task but immediately interrupted its own Session processor, persisting the successful tool call as `AbortError`. Terminal tools now return the standard `opencorvusParkAfterToolResult` metadata and no longer abort their own loop before result persistence. Focused complete/fail/coordination/processor tests and OpenCorvus typecheck pass.
- The post-fix real `gpt-5.5` task completed with `providerID=hexin`, `modelID=gpt-5.5`, no assistant message error, `finish=tool-calls`, and a `manage_task` part with `state.status=completed` plus its durable completion summary. No closed database/instance lease failure recurred.
- Final real GUI review uses the durable boundary `role=user && channel!=main && source!=overlay_direct_reply`: the main `ctx:user-request` and later direct human replies stay expanded, while the user request copied into the Orchestrator session and the prompt delegated into the Build session render as collapsed `Delegated context`. Normal Orchestrator/Build assistant bodies remain visible; expansion reveals the exact original request without creating another message stream.
- Independent adjudication initially rejected completion because direct human replies were over-collapsed and two existing hydrate/agent-rail suites had 24 stale origin fixtures. The direct-reply source boundary above fixes the P0; the fixture contracts now carry real bridge origin metadata and the final four-suite matrix passes 116/116.
- Final focused browser replay passes in English and Chinese with strict browser error collection. `check:i18n`, API route inventory, historical links (20/20), and `git diff --check` pass; the browser fixture uses a finite heartbeat for its unrelated Work Ledger event endpoint so test shutdown does not manufacture an SSE abort.
- Final delegated-context verification passes with 116/116 Overlay origin/hydrate/agent-rail tests, 7/7 bridge tests, 12/12 standalone session-mirror tests, the Task transcript origin contract, the direct-reply route contract, and the Node browser chronology scenario. The browser scenario proves an internal `overlay_direct_reply` remains visible while adjacent delegated context is collapsed and assistant text remains visible.
- `originSource` is now a required normalized string across the bridge cache, persisted-message lookup, live `message.updated`, task transcript annotation, and standalone transcript enrichment. Missing persisted source becomes `""`; `tree-writer` never falls back to `extra.source` and fails if the normalized field conflicts with that audit copy.
- The independent read-only adjudicator issued final `PASS` after six review rounds and found no remaining P0/P1/P2 blocker in this scope.
- A subsequent full-goal adjudication found the capability matrix still left queue reorder, task budget, and cleanup candidates under an unresolved `Audit` label. Repository history proved queue reorder was a real UI regression; Work Ledger now restores it. Existing-task budget mutation and cleanup-candidate inspection are classified as pre-existing API/maintenance contracts because neither current nor `HEAD` Overlay production code exposed those controls.
- Queue reorder verification passes through the Work Ledger route suite (5/5), the complete Node browser controls flow (1/1), both package typechecks, and an inspected `work-ledger-queue-reorder.png` screenshot. The browser test performs a real drag event, observes `PATCH /task-queue/reorder`, reloads the ledger, and proves the DOM order changes.
- Final document health and historical-link verification passes 72/72 after formal July records were indexed and all formal-record dependencies on the local planning draft were replaced with natural-language history.
- A real current-source server at `127.0.0.1:4199` used the same isolated data root and created task `tsk_f4e1a7a980015K5LSmZSw7JXIr` with `hexin/gpt-5.5`. Selecting the active task rendered `#btnTaskInterrupt[data-mode=stop]`; clicking it replaced Stop with Send, showed a cancellation notification, and the backend persisted `status=cancelled`, `terminalReason=cancelled`, run `status=aborted`, and no active sessions. The inspected screenshot shows the cancelled task and normal transcript/composer layout.
- Composer busy state now uses the same remote task lifecycle source as the rest of the UI (`messageStore.chatRequest || isTaskInterruptable()`). Stop aborts a live local request when present; otherwise it invokes the canonical selected-task cancel path. The full Node controls test exercises this remote-only branch and passes without runtime errors.
- Final checks after queue and Composer repairs: both package typechecks, Overlay i18n, Work Ledger routes 5/5, Node controls browser 1/1, API route/OpenAPI inventory, document health plus historical links 72/72, and `git diff --check` all pass.
- The final full-goal adjudicator initially returned `FAIL` because two prompt-profile task-creation tests exceeded their 15-second wall-clock limits in the combined run and leaked a late HTTP 202 assertion. Both tests were starting the task loop even though their declared contract is task materialization and profile persistence. They now create queued tasks, removing unrelated scheduler activity without changing production behavior; the complete task-create route file passes 14/14, including the two cases at approximately 9.4 and 10.1 seconds.
- After that correction, the same independent read-only full-goal adjudicator re-ran its review and returned final `PASS` with no remaining evidenced blocker.
