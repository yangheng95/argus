# v0.0.17beta Branch Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current delivery line to `v0.0.17beta`. |
| Acceptance | Create `v0.0.17beta` from the current pushed `v0.0.16beta` tip, synchronize the release family to canonical SemVer `0.0.17-beta` and Windows MSI (Microsoft Installer) projection `0.0.17.0`, validate the repository contracts, commit only bump-owned files, and push the new branch to `legacy-remote`. |
| Git baseline | After fetch, local and `legacy-remote/v0.0.16beta` both resolve to `6466484bf63c34d19164720e3a56c0443c034293`; neither a local nor remote `v0.0.17beta` branch existed before branch creation. |
| Parallel changes | The shared worktree contains an unstaged Mirror Prism record edit and an untracked `expert-squads/.DS_Store`. They do not overlap the version registry targets, `RELEASE.md`, or this record/index entry and must remain unstaged and unmodified by the bump. |
| Remote context | Fetch advanced `legacy-remote/work-v0.0.16beta-yr-0723` to `05b60e983`, but the user requested a version-line bump, not a work-branch merge. That independent line is not merged implicitly. |
| Hard constraints | Use `script/sync-version.ts` as the only version writer; do not reset, stash, restore, create a worktree, package, restart OpenCorvus/Overlay, or bypass hooks; use the `dsw-33987` commit prefix and push only to `legacy-remote`. |
| Sources read | Root `AGENTS.md`; current status, graph, branches, remotes, and fetch result; `script/sync-version.ts`; `RELEASE.md`; the `v0.0.16beta` bump record; root and July spec indexes. |
| Whole-repository grep | The synchronizer owns nine package manifests and their Bun workspace rows, two Cargo manifest/lock pairs, and Tauri application/MSI metadata. The only live release command outside that registry is `RELEASE.md`. Historical `0.0.16-beta` records and task-specific delivery references remain immutable history. |
| Independent-agent feedback | None. The user did not request delegation and the active execution policy forbids unsolicited sub-agent delegation. |

## Plan

1. Record and push this Recall from the newly created `v0.0.17beta` branch while preserving unrelated worktree changes.
2. Run the repository version synchronizer with `v0.0.17beta` and update the live release command example.
3. Validate version convergence, focused synchronizer tests, required documentation health, and normal push-hook checks.
4. Stage and commit only version-bump-owned files, push `v0.0.17beta` to `legacy-remote`, and verify local/remote equality while retaining parallel changes.

## Verification ledger

- Branch creation: after a fresh fetch confirmed local and `legacy-remote/v0.0.16beta` equality at `6466484bf`, created `v0.0.17beta`; neither a local nor remote branch with that name existed. Recall commit `4d1e67566` was pushed through the normal hooks before version mutation.
- Version convergence: `bun run version:bump v0.0.17beta` synchronized all declared release-family targets to `0.0.17-beta` and the Tauri MSI projection to `0.0.17.0`; `bun run version:check` passes.
- Focused verification: all 5 version-synchronizer tests pass; root typecheck passes all 9 package tasks; historical links, document health, and product docs single-source suites pass 87/87.
- Codex review feedback: the first Node-launched sidebar browser run reached the current UI but its strict browser error audit exposed a stale fixture: the newly mounted `/mission-skill/catalog` request returned 404. The fixture now returns the same empty strict catalog shape used by sibling browser tests; the rerun passes 1/1 without changing product behavior.
- Visual verification: the current 1280×720 screenshot was opened at original resolution. It shows the compact `v0.0.17beta` label, `yangheng@myhexin.com` tooltip, and Online state without clipping, overlap, or alignment regression.
- Preservation audit: the Mirror Prism diff SHA-256 remains `0e7084108447c80be2cbde02ad62510399a06794207b67e87ad60af33a8cf5e7`; `expert-squads/.DS_Store` remains `6b84fb2a9bc22329b6afbd83d9348b2a5e3aa3bd3db828ea27ae998351638393`. Both remain outside the bump index.
- Delivery: bump commit `8cc3f0742` contains exactly the synchronized release-family files, live release example, browser fixture repair, and this evidence update. The normal pre-push hook passed workspace typecheck, route inventory, generated API documentation equality, Overlay internationalization, and secret scanning. A fresh fetch confirmed local and `legacy-remote/v0.0.17beta` both resolved to `8cc3f0742` with delta `0 0` before the final evidence-only commit.
