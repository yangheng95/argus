# Documentation, README, API, and SDK calibration

## Recall

- User request: comprehensively calibrate and update every documentation, README, API, and SDK surface, then continue autonomously through verification and legacy remote delivery.
- Acceptance criteria:
  - current product and architecture documentation describes the live Task, Mission, Agent, Expert Squad, API, and SDK contracts without retired fixed-pipeline or external-executor claims;
  - every tracked README is audited, and proven stale README content is replaced rather than preserved as compatibility prose;
  - runtime route schemas, generated OpenAPI, generated JavaScript/TypeScript SDK, and bilingual generated API references are regenerated from their canonical owners and have no drift;
  - handwritten English and Chinese server/API/SDK documentation agrees with the mounted route inventory and generated operation surface;
  - documentation-health tooling handles tracked non-ASCII paths as raw repository paths and catches both missing and retired route-module entries;
  - focused documentation, route, OpenAPI, SDK, package, type, and build checks pass, followed by a second audit pass;
  - every public HTTP request schema describes only fields that the corresponding caller is authorized to use, every directly implemented error branch is represented in OpenAPI, and generated SDK examples compile;
  - every tracked monthly record is indexed exactly once by its month README, while `specs/README.md` remains a root/month/current-architecture index instead of a second copy of monthly history;
  - only task-owned files are staged, committed with the `dsw-33987` prefix, and pushed from `v0.0.21beta` to the legacy remote.
- Hard constraints:
  - preserve the existing untracked `.codex-cache/`, `.codex-screenshots/`, `opencorvus-dist/`, `packages/opencorvus/test/script/web-docs-repository.test.ts`, `packages/overlay/src/services/browser-preview-link.ts`, and `packages/overlay/test/browser/message-url-preview-browser.test.ts`;
  - do not restart, refresh, stop, or otherwise interfere with a running OpenCorvus or Overlay process;
  - do not hand-edit generated OpenAPI, generated SDK, or generated API-reference bodies; use the canonical generators;
  - do not add fallback, alias, gate, route bypass, or parallel documentation sources;
  - do not create another worktree.
- Sources read:
  - `AGENTS.md`, `specs/README.md`, `specs/current/architecture/99-principles.md`, and `specs/current/architecture/README.md`;
  - `specs/records/2026-06/2026-06-04-docs-sdk-single-source.md` and `specs/records/2026-07/2026-07-25-api-doc-calibration.md`;
  - root/package/runtime READMEs, all 102 English/Chinese public docs, `packages/opencorvus/src/server/routes/app.ts`, and the ACP/QuickNote implementations;
  - `packages/opencorvus/src/engine/model.ts`, `task-api/index.ts`, `task-api/global-task-service.ts`, `server/server.ts`, `server/error.ts`, `server/error-handler.ts`, and the Task/Mission/Gateway routes;
  - `packages/sdk/js/script/build.ts`, SDK package exports/examples/runtime entrypoints, `packages/opencorvus/script/docs/render-api-md.ts`, `packages/opencorvus/script/check/routes.ts`, and the documentation/OpenAPI/SDK regression suites.
- Whole-repository search evidence:
  - tracked inventory: 23 README files, 102 public web-doc files, 33 server route files including `app.ts`, 264 OpenAPI paths / 306 operations / 24 groups, and 39 SDK files;
  - searched all route definitions and mounts, `describeRoute`/`operationId` producers, handwritten HTTP and SDK call examples, generated API/SDK owners, README commands, retired executor names, ACP capabilities, QuickNote routes, and document-health call sites;
  - `gitLsFiles()` is called for architecture docs, source/docs/squad scans, Overlay tests, tracked-file ownership, examples, Expert Squad payloads, monthly indexes, and spec records. All call sites depend on exact repository paths, so its path-decoding fix applies uniformly rather than through per-test exceptions;
  - `AppRoutes` mounts 31 focused route modules from `packages/opencorvus/src/server/routes/` plus `QuickNoteRoutes`; the English and Chinese Server pages contain the nonexistent `control.ts`;
  - root `README.md` still publishes the retired fixed Requirements/Architect/Executor pipeline, external executor auto-discovery, Run/evaluation updates, and acceptance aggregates;
  - `packages/opencorvus/src/acp/README.md` claims that streaming, tool reporting, session modes, and history restoration are unimplemented, while `agent.ts` and the bilingual ACP docs prove live `sessionUpdate`, tool/plan/usage updates, `setSessionMode`, and history replay;
  - baseline `bun run api:routes-check` passed six rules across 33 files, and `bun run docs:check` passed 306 operations across 24 groups;
  - baseline documentation tests exposed the non-ASCII `git ls-files` parsing defect and separately found a retired view fixture only in the preserved untracked Overlay browser test.
- Independent Agent Feedback:
  - three one-layer, read-only audits were dispatched for docs/README, API/OpenAPI, and SDK surfaces with explicit instructions not to modify files and not to delegate;
  - docs/README audit found 115 July records missing from the month index, 408 root/month duplicate record links, a second July H1/index tail, a broken root install URL, stale fixed-pipeline Quickstarts, duplicated and invalid CLI docs, incomplete SDK docs, and missing internal-link/index completeness checks;
  - API audit proved `artifactImports` is exposed by both public Task-create routes although `EngineService.createTask(..., { actor: "user" })` rejects every non-empty import set, and found directly implemented but undeclared 400/403/412/503 responses plus absent Basic Auth security metadata;
  - SDK audit reproduced exact live/tracked OpenAPI parity but found the shared spec version hard-coded to `0.0.1-alpha`, 17 collision-adjusted accessors not explained by the SDK guide, stale non-compiling `example/example.ts`, incomplete public-export documentation, and no Automation guide;
  - all agents confirmed they made no filesystem changes and did not delegate. Their reports are evidence for this plan; the main agent owns every implementation and final judgment.

## Baseline and ownership

`HEAD` and `legacy-remote/v0.0.21beta` both resolved to `87a2c5dc49` after fetch (`0 0` divergence), satisfying the pre-change remote checkpoint without creating an empty commit.

| Surface or call site                                                                                 | Evidence                                                                                                                                    | Disposition                                                                                                                                                                          |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `README.md`                                                                                          | Retired fixed pipeline, external executor discovery, Run/evaluation and acceptance-aggregate language                                       | Replace with the live Task/Mission, active Expert Squad, projected Agent, Artifact, HTTP/Overlay/ACP/SDK model                                                                       |
| `packages/opencorvus/src/acp/README.md`                                                              | Contradicts implemented streaming, tool updates, modes, and history replay                                                                  | Replace with a concise implementation README aligned to `agent.ts`, `session.ts`, CLI wiring, tests, and public ACP docs                                                             |
| Other tracked READMEs                                                                                | Package prompts, artifact instructions, web/GitHub/QuickNote guides are covered by path, command, payload, or package-specific tests        | Preserve unless a focused check proves drift                                                                                                                                         |
| `CONTRIBUTING.md`, QuickNote README                                                                  | Route ownership/generation wording and root-relative test commands are stale                                                                | Calibrate commands against package scripts and real paths                                                                                                                            |
| English/Chinese Quickstart                                                                           | Publishes nonexistent global stage and retry event names                                                                                    | Replace with Task/session/interaction lifecycle events and active Expert Squad workflow semantics                                                                                    |
| English/Chinese CLI docs                                                                             | `cli.mdx` and `reference/cli.mdx` duplicate one mutable command catalog and contain invalid commands/table shape                            | Keep `reference/cli.mdx` as the canonical command catalog and turn `cli.mdx` into a concise navigation page                                                                          |
| English/Chinese Mission/Task guide                                                                   | Sends retired `mission.wake.title` and omits the two `directory` roles                                                                      | Remove `title`; document client/query project scope, body execution directory, and `init-git` precisely                                                                              |
| English/Chinese SDK guide                                                                            | Misstates namespaces/permissions/goals and omits exports, embedded options, username, and accessor collision behavior                       | Align to package exports, generator resolver, and runtime entrypoints                                                                                                                |
| English/Chinese Automation docs                                                                      | No user guide or typed SDK example exists for a shipped API/UI surface                                                                      | Add one bilingual guide generated from the live route/schema vocabulary and link it from Operations                                                                                  |
| English/Chinese `server.mdx`                                                                         | Lists deleted `control.ts`                                                                                                                  | Remove it in both languages                                                                                                                                                          |
| OpenAPI info version                                                                                 | `AppDocumentation` hard-codes `0.0.1-alpha` while the release family is `0.0.21-beta`                                                       | Read the canonical package version and extend version verification                                                                                                                   |
| Public Task-create request schemas                                                                   | Reuse internal `CreateTaskInput`, advertising Mission-only `artifactImports`; `/global/tasks` also accepts an ambiguous execution directory | Introduce one public strict schema derived from the canonical input without `artifactImports`; retain `directory` because it is a real execution-repository contract and document it |
| Task/Gateway response schemas                                                                        | Runtime has explicit validation, forbidden, VCS precondition, and planner-unavailable branches missing from OpenAPI                         | Declare the exact directly implemented 400/403/412/503 responses with existing response helpers; test runtime and generated spec together                                            |
| Server-wide authentication/origin contract                                                           | Basic Auth and hostile-Origin middleware apply before documented routes but OpenAPI has no security scheme/401/403 contract                 | Project the middleware contract once in `Server.openapi()` and test every operation receives the shared response/security metadata                                                   |
| `document-health.test.ts` route inventory assertion                                                  | Checks required modules but accepts nonexistent extras                                                                                      | Compare the documented route-table set exactly with the filesystem inventory                                                                                                         |
| `document-health.test.ts::gitLsFiles` and all callers                                                | Line parsing receives Git `core.quotePath` output for Chinese paths and attempts to open quoted octal escapes                               | Use `git ls-files -z` and split decoded output on NUL; add explicit Chinese-path regression                                                                                          |
| `packages/opencorvus/test/script/web-docs-repository.test.ts`                                        | Pre-existing untracked file                                                                                                                 | Preserve and do not stage                                                                                                                                                            |
| `packages/overlay/test/browser/message-url-preview-browser.test.ts`                                  | Pre-existing untracked retired view fixture triggers broad working-tree audit                                                               | Preserve and report separately; do not weaken the production rule                                                                                                                    |
| Route schemas and `describeRoute` metadata                                                           | Canonical API source                                                                                                                        | Preserve semantics unless generation or focused tests expose a source defect                                                                                                         |
| `packages/sdk/openapi.json`, `packages/sdk/js/src/gen/**`, generated defaults/policy, and SDK `dist` | Produced transactionally by `packages/sdk/js/script/build.ts`                                                                               | Regenerate only through that script; review the exact diff                                                                                                                           |
| English/Chinese generated API references                                                             | Produced by `render-api-md.ts` from live OpenAPI metadata                                                                                   | Regenerate through `bun run docs:api`; never hand edit generated bodies                                                                                                              |
| SDK handwritten exports and authoring helpers                                                        | Root/client/server/defaults/expert-squad-authoring export surface                                                                           | Verify package tests, import checks, typecheck, and generated-method samples                                                                                                         |
| SDK example                                                                                          | Uses pre-flat request objects, missing Session kind/directory, and is excluded from typecheck                                               | Rewrite against the current generated client and compile it in SDK verification                                                                                                      |
| Specs indexes                                                                                        | July has 722 record files but only 607 links; root duplicates monthly history                                                               | Rebuild July as the exhaustive index, reduce root to month/current-architecture/artifact entrypoints, and enforce exact ownership                                                    |

## Implementation plan

1. Repair documentation-health path parsing, exact route inventory, internal-site links, canonical CLI ownership, and exact month-index coverage; rebuild the July/root indexes without a second historical source.
2. Correct the public Task-create schema, OpenAPI version, shared authentication/origin responses, and the proven Task/Gateway error responses; add runtime/OpenAPI regressions before regeneration.
3. Replace stale README/ACP/Quickstart/CLI/Mission-Task/SDK/Automation content in English and Chinese, correct QuickNote/Contributing commands, and rewrite/compile the SDK example.
4. Run the SDK and bilingual API-doc generators from their canonical owners, inspect every generated diff, and verify path/method/body/response/security/version parity.
5. Build the docs site, launch an isolated preview, inspect desktop screenshots for the changed shell/pages, correct visual defects, and rerun document/API/SDK/type checks.
6. Dispatch a fresh read-only diff review, update this verification ledger, stage only owned files, commit with `dsw-33987`, and push `v0.0.21beta` through normal legacy remote hooks.

## Verification ledger

- Pre-change legacy remote checkpoint: `87a2c5dc49`, local/remote divergence `0 0`.
- Baseline `api:routes-check`: passed, six rules across 33 route files.
- Baseline `docs:check`: passed, 306 operations across 24 groups.
- Baseline focused documentation run:
  - proved the tracked Chinese paths are misparsed by `gitLsFiles`;
  - proved both Server pages retain nonexistent `control.ts`;
  - separately reported the preserved untracked Overlay browser fixture's retired conversation view.
- Remaining verification is recorded here after each repair wave.
