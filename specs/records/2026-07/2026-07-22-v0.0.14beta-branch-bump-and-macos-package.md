# v0.0.14beta Branch Bump And macOS Package

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump to `0.0.14beta` and package again. |
| Acceptance criteria | Create `v0.0.14beta` from the latest legacy remote `v0.0.13beta`; synchronize the release family to canonical Semantic Versioning (SemVer) `0.0.14-beta` and Windows Microsoft Installer (MSI) projection `0.0.14.0`; preserve parallel files; validate and push the new branch; run the canonical native macOS ARM64 GUI installer matrix; independently verify and report the resulting assets. |
| Hard constraints | Use `script/sync-version.ts` as the only version writer and `bun run package:gui-installer-matrix` as the only GUI packaging owner. Do not stash, reset, restore, delete, broadly stage, create another worktree, bypass hooks, or restart/stop/refresh OpenCorvus or Overlay. Commit subjects start with `dsw-33987`; pushes use `legacy-remote`. Claim only the native runner row actually built. |
| Sources read | Repository `AGENTS.md`; current branch/status/remotes/history; `RELEASE.md`; `script/sync-version.ts`; root package scripts; canonical GUI matrix script and tests; the v0.0.13beta branch-bump, macOS-repackage, and payload-repair records; July spec indexes. |
| Whole-repository grep | `releasePackageTargets` owns nine package manifests; `cargoManifestTargets` owns two Cargo manifests and two lock entries; `tauriConfigTarget` owns application and MSI projections; `bunLockTarget` owns nine workspace lock projections. `RELEASE.md` is the only current release-command example. Historical records remain immutable evidence and are not rewritten. `package:gui-installer-matrix` remains the single public GUI matrix entry and stages the native executable, disk image, and application archive under `packages/overlay/dist-artifacts/<target>/`. |
| Git evidence | Fresh legacy remote inspection found no local or remote `v0.0.14beta`. The current `v0.0.13beta` advanced to legacy remote commit `104a64f3f`; `v0.0.14beta` was created directly from that commit. The only visible untracked files are the preserved `expert-squads/.DS_Store` and `expert-squads/builtin/.DS_Store`. |
| Preservation boundary | The bump may stage only synchronizer-owned release metadata, `RELEASE.md`, this record, and the two spec indexes. Build products remain ignored artifacts. The two existing Expert Squad metadata files remain untracked and byte-preserved. After version synchronization, concurrent edits appeared in `cancellation-scope.ts` and three cancellation/session test files; they remain outside this task's staging boundary and must settle before immutable-commit packaging. |
| Independent agent feedback | None requested; current collaboration policy forbids unrequested delegation. |

## Plan

1. Commit and push this indexed Recall on the new branch before changing release metadata.
2. Run `bun run version:bump v0.0.14beta`, update the canonical release example, and inspect the exact diff against the synchronizer's declared target registry.
3. Run version synchronization, focused release/packaging contracts, documentation health, typecheck, and normal pre-push hooks; commit and push the version bump.
4. Build `bun run package:gui-installer-matrix` from the pushed immutable commit with the two Expert Squad `.DS_Store` files still present.
5. Independently verify architecture, signatures, disk-image integrity, application metadata, archive structure, sizes, and SHA-256 hashes; record evidence, push the record, and prove local/remote equality.

## Verification Ledger

- `b27bd7c69 dsw-33987 plan v0.0.14beta release package` created and pushed the indexed Recall on the new legacy remote branch after normal pre-push hooks passed.
- `bun run version:bump v0.0.14beta` synchronized all declared targets to SemVer `0.0.14-beta` and MSI `0.0.14.0`; `RELEASE.md` now demonstrates the same compact product input. `bun run version:check` passes.
- Focused version and GUI release-contract tests: 30 passed, 0 failed. Historical-link and document-health tests: 82 passed, 0 failed. Root typecheck passed all 9 participating packages.
- The version staging audit contains exactly the synchronizer-owned release targets, `RELEASE.md`, and this task record. Four concurrent cancellation/session files remain unstaged and preserved.
- `ff7776198 dsw-33987 bump release family to v0.0.14beta` passed normal pre-push hooks and was pushed to legacy remote. The concurrent cancellation task then settled its preserved files in `73d5b6033 dsw-33987 settle ownerless session cancellation`, and local/remote `v0.0.14beta` converged on that commit with an empty tracked diff.
- A first authorized current-worktree build completed validation, but its tracked-diff SHA-256 changed while the concurrent task was still writing. Those race-exposed assets were rejected as final evidence and overwritten by a complete rebuild after the task committed.
- The final `bun run package:gui-installer-matrix` rebuilt the Software Development Kit, production Overlay, embedded backend, optimized Tauri application, ad-hoc signatures, disk image, and application archive from immutable commit `73d5b6033343fe3d66922e65331c6a40304c1d6d`. The repository validator passed `darwin-arm64`; Linux x64/ARM64, Darwin x64, and Windows x64 were correctly skipped because matching native runners were unavailable.
- Independent validation confirms the staged and bundled executables are ARM64 Mach-O binaries; strict code-sign verification passes for the application bundle and staged executable; `hdiutil verify` accepts the disk image; the application identifier is `ai.opencorvus.overlay`; version is `0.0.14-beta`; and the archive contains the signed `OpenCorvus.app` structure. Notarization was skipped because Apple notarization credentials are not configured.
- Final assets:
  - `OpenCorvus_0.0.14-beta_aarch64.app.tar.gz`: 285,721,227 bytes, SHA-256 `cdc0c5c458a2b2b213b81a76da1fbec3f07f4585774f6340e1cd8438f483c1d4`.
  - `OpenCorvus_0.0.14-beta_aarch64.dmg`: 286,902,151 bytes, SHA-256 `8262e046c432279a76644a0b3e6f2c99472d64e4594b57e06352457b0877ca7b`.
  - `opencorvus-overlay`: 310,967,936 bytes, SHA-256 `5c0d0a44b2b99d59cd435107709cb2e57c6446bb877681563f12b0d2587daaff`.
- After final verification, tracked diff SHA-256 is the empty-tree value `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; both preserved Expert Squad `.DS_Store` hashes remain unchanged. This record is the final evidence-only delivery delta.
