# Argus

## 定位

Argus 是一个全平台通用的GUI操作Agent，可以无人值守的模式管理桌面和窗口。也可以直接用于coding，提供一个可以托管的编程环境，用于给定目标定前提下无人值守的原型开发。Argus像是opencode和opencclaw的结合体，既可以提供一个可以托管的编程环境，也可以提供一个全平台通用的GUI操作Agent。

## 功能点

- 基于Bot的GUI操作Agent，像openclaw一样支持多种bot，例如slack，discord，telegram等。
- 通用的全平台GUI操作能力，支持Windows，MacOS，Linux等操作系统。点击，键盘输入/快捷键，拖动等基本操作都可以实现。
- 自适应分辨率的窗口定位和操作，支持多显示器环境。支持各种情况下的GUI坐标计算，例如窗口被遮挡，分辨率变化等。
- 支持无人值守的模式，可以在后台运行，定时执行任务，监控系统状态等。
- 自带一个Terminal UI (TUI)，可以在命令行界面进行无人值守的开发。
- 复用opencode的所有功能，例如tool,agent，plugin, mcp等。
- 使用skill定义全平台的GUI操作能力，提供一个统一的接口来实现各种GUI操作。例如描述平台特定的应用打开方式，窗口定位，操作步骤，组合快捷键等。
- Argus启动后即可开始操作，第一次启动的时候可以基于GUI操作的自动化的bot配置，这也是skill的一部分。覆盖多种平台和bot。
- 提供系统托盘和一个可视化的对话界面来管理Argus的运行状态，例如发送指令，查看日志，配置bot等。
- Argus有一个独立的goal单元，一个plan agent,一个视觉分析的agent（分析图片后给出坐标和操作和运行摘要，但不把截图放入主对话流），一个GUI操作的agent（根据视觉分析的结果进行操作）。plan agent负责根据goal和视觉分析的结果制定操作计划，GUI操作的agent负责执行计划中的操作步骤。
- 为了辅助视觉LLM找到GUI元素和坐标，截图被添加了自适应分辨率的坐标系，视觉分析的agent会根据这个GUI上的坐标系找出坐标，给出操作指令，例如点击(100, 200)，而不是点击(100, 200)在某个特定分辨率下的坐标。
- 一个任务进来会在plan agent中进行计划（可以联网搜索学习操作步骤），计划完成后生成一个goal目标给goal agent，goal通过鲁棒的策略判断是否goal已经完成，如果完成不了强行命令LLM继续搜索解决方案，除非LLM承认无法完成或者产生异常。GUI操作agent会根据计划中的操作步骤进行操作，如果操作失败了会返回错误信息给plan agent，plan agent会根据错误信息重新制定计划，直到操作成功或者达到重试次数上限。
- 任务进来时会进入暂存区（可以理解为队列 agent），根据当前goal的完成情况判断是否开始发布任务到plan agent，暂存区会根据任务的优先级和当前系统状态来决定任务的执行顺序，例如如果当前系统资源紧张或者正在执行一个高优先级的任务，那么新的任务可能会被暂时搁置，直到系统资源充足或者高优先级的任务完成。
- 上下文管理：截图只用于分析，不进入主对话流，视觉分析的结果会以文本的形式进入主对话流，plan agent和goal agent根据视觉分析的结果进行决策和计划制定。新的任务开始的时候总结上一轮任务的执行情况，生成一个总结，作为新的上下文的起点。提供给plan agent和goal agent进行决策和计划制定。
- 任务执行过程中，Argus会把截图发送给手机端的bot，bot会把截图展示给用户，方便用户了解当前的执行情况和系统状态。用户可以通过bot发送指令给Argus，例如暂停，继续，取消等。
- Argus在操作GUI的时候屏幕上要有视觉反馈，例如在点击一个按钮的时候，按钮会有一个高亮的效果，或者在操作完成后有一个提示框显示操作结果等。这些视觉反馈可以帮助用户更好地理解系统的操作过程和结果。
- Argus的视觉分析agent需要具备一定的鲁棒性，能够适应不同的界面布局和元素样式，妥善处理逻辑分辨率和物理分辨率（例如按钮的位置和大小可能会因为分辨率的变化而发生变化，视觉分析agent需要能够根据这些变化进行调整，找到正确的坐标进行操作）。
- 所有的设计都是通用抽象，涉及到不同平台和bot的细节都应该通过独立模块或者插件的方式来实现，防止耦合影响维护。这样可以保证系统的灵活性和可扩展性，能够适应不同的使用场景和需求。
- 像opencode的webui一样，Argus提供一个可视化的对话界面来管理和监控系统的运行状态，例如查看当前的任务队列，监控系统资源使用情况，查看操作日志等。这些功能可以帮助用户更好地了解系统的运行情况和性能，并及时发现和解决问题。点击系统托盘图标可以打开这个对话界面，用户可以在这里查看当前的任务队列，监控系统资源使用情况，查看操作日志等。
- 长期和短期记忆：Argus需要具备一定的记忆能力，能够记住之前的操作和任务执行情况，以便在新的任务中进行参考和优化。例如，如果之前的任务执行过程中遇到了某个特定的问题，Argus可以记住这个问题，并在新的任务中避免类似的问题发生。之前学习了一个操作，则记住操作步骤和经验。
- 定时任务和事件触发：Argus需要支持定时任务和事件触发的功能，能够在特定的时间或者特定的事件发生时自动执行某些操作。例如，Argus可以在每天的特定时间自动执行一个备份任务，或者在检测到某个特定的系统状态时自动执行一个修复任务等。
- 语音能力（Voice Wake + Talk Mode）
- Always-on 语音唤醒（macOS/iOS/Android）
- 连续对话模式覆层
- ElevenLabs TTS 文字转语音
- Host服务：
- agent 驱动的可视化工作区：agent 可以 push/reset/eval/snapshot canvas 内容
- 双向 A2UI 桥：前端操作可以触发 agent 工具调用，agent 可以更新前端界面
- Live Reload：WebSocket 实时推送文件变更（75ms 防抖）
- 静态文件服务：serve agent 生成的 HTML/CSS/JS/媒体文件
