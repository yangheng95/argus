# v0.0.35beta Branch Bump

## Recall

### 用户原始要求

- 合并远端 `v0.0.34beta`，然后 bump 到 `v0.0.35beta` 分支。

### 验收指标

- fetch legacy remote 并证明本地交付线包含最新 `legacy-remote/v0.0.34beta`。
- 在 `v0.0.35beta` 分支上把统一 release family 从 `0.0.34-beta` 更新为 `0.0.35-beta`。
- 使用仓库版本同步工具更新 workspace package、Cargo manifest/lock、Tauri、Windows MSI 与 `bun.lock` 投影。
- `bun run version:check`、类型检查、API route、文档、Overlay 国际化与 Git diff 检查通过。
- commit subject 使用 `dsw-33987`，并推送到 legacy remote 的 `v0.0.35beta`。

### 硬约束

- 不使用 reset、stash、restore、`--no-verify` 或向 `origin` 推送。
- 不改写远端历史；保留 `v0.0.34beta` 已验证提交。
- 不手工维护并行版本源；`script/sync-version.ts` 是唯一版本同步工具。
- 本任务不打包、不修改 User Interface，也不运行 User Interface 自动化测试。

### 已读取资料

- 根 `AGENTS.md`。
- `specs/records/2026-08/2026-08-07-v0.0.34beta-windows-native-package.md`。
- 根 `package.json` 与 `script/sync-version.ts` 的版本命令入口。
- `legacy-remote/v0.0.34beta`、`legacy-remote/v0.0.35beta` 和本地分支引用。

### 全仓 grep 结果

- 第一次 fetch 后，本地 `v0.0.34beta`、本地 `v0.0.35beta`、`legacy-remote/v0.0.34beta` 与 `legacy-remote/v0.0.35beta` 均指向 `bac3c6d5be`；当时 `git merge --ff-only legacy-remote/v0.0.34beta` 返回 `Already up to date.`。
- 用户复核时执行第二次 fresh fetch，`legacy-remote/v0.0.34beta` 已从 `bac3c6d5be` 推进到 `5e3ecd0db8`。当前分支与新远端的共同祖先仍为 `bac3c6d5be`，远端有 34 个独有提交，本地只有本方案提交，因此必须真实合并新远端后再 bump。
- `legacy-remote/v0.0.35beta` 仍指向旧基线 `bac3c6d5be`，尚未包含新 `v0.0.34beta` 提交或版本提升。
- 当前产品 release family 仍为 `0.0.34-beta`；分支已存在，但版本投影尚未提升。
- 历史 `f64725e39b` 证明 `bun run version:bump <version>` 是当前统一同步路径，覆盖八个 workspace package、两个 Cargo family、Tauri/MSI 和 `bun.lock`。

### 独立 Agent 反馈

- 用户未要求子 Agent 或并行审计；本次不启动子 Agent。

## Plan

1. 记录远端收敛证据并提交本方案，使版本实施可从磁盘 Recall 恢复。
2. 运行 `bun run version:bump 0.0.35-beta`，检查仅预期的 release-family 文件发生变化。
3. 运行 `bun run version:check`、workspace typecheck、API route、文档、Overlay 国际化以及 diff 检查。
4. 写回精确验证证据，二次复核 staged diff，提交并推送 legacy remote `v0.0.35beta`。

## Verification Ledger

- Fresh fetch first advanced `legacy-remote/v0.0.34beta` from `bac3c6d5be` to `5e3ecd0db8`, exposing 34 remote-only commits. Merge commit `d2175f1cf7` incorporated that complete line without conflicts while preserving the local plan commit.
- During integration, the remote tracking ref advanced once more to `f730a2a972` with the verified legacy remote push-transport rule. Merge commit `31e38fa952` incorporated it; the immediately preceding fetch attempt reported a transient Schannel handshake failure, so final remote stability and SHA equality must be rechecked before push.
- `bun run version:bump 0.0.35-beta` used the canonical synchronizer and changed exactly 14 release-family projections: `bun.lock`, eight workspace package manifests, two Cargo manifest/lock families, and Tauri/MSI versions.
- `bun run version:check` accepted the complete release family at `0.0.35-beta`.
- SDK import, Artificial Intelligence runtime, API route, documentation, and Overlay internationalization checks passed.
- The first full workspace typecheck exposed stale local SDK `dist` declarations after the large remote merge: generated source already contained the new Expert Squad evolution types, while Overlay resolved the prior built output. `bun run --cwd packages/sdk/js build` rebuilt the SDK through the existing OpenAPI toolchain; the unchanged source worktree then passed all eight workspace typecheck tasks.
- Version commit `cdc8badfa7` records the synchronized `0.0.35-beta` release family after `git diff --cached --check` passed.
- Three fetch attempts through the configured redirecting remote URL failed during TLS negotiation even though TCP 6443 and the HTTPS service were healthy. Fetching the canonical `.git` URL directly succeeded without changing credentials or Git payloads.
- The resulting fresh refs prove `legacy-remote/v0.0.34beta=f730a2a972` and `legacy-remote/v0.0.35beta=bac3c6d5be`; both are ancestors of `cdc8badfa7`, and `HEAD..legacy-remote/v0.0.34beta` contains zero commits. The final push is therefore a fast-forward.
- Explicit `HEAD:refs/heads/v0.0.35beta` push through the canonical legacy remote `.git` URL passed SDK import, Artificial Intelligence runtime, all eight workspace typecheck tasks, API route, documentation, Overlay internationalization, and secret-scan hooks.
- Direct `git ls-remote` verified `refs/heads/v0.0.35beta=470e866e303f23837d01317608927a015449b84f`, exactly equal to the local evidence commit before this final ledger update.

## 2026-08-07 Branch Convergence Continuation

### Recall

#### 用户原始要求

- 修复当前 Git 工作环境，把所有有效开发分支收敛到 `v0.0.35beta`。
- 用户随后明确改写远端边界：后续不再跟踪 legacy remote；GitHub `origin` 是唯一后续 fetch、push 和远端验证来源。

#### 验收指标

- `v0.0.35beta` 跟踪 `origin/v0.0.35beta`，不再跟踪、fetch、merge、push 或验证 legacy remote。
- 先刷新 `origin`，再用提交可达性、提交时间、工作树状态和树差异区分：已包含的历史发布线、语义上已恢复的旧分叉、仍有唯一有效成果的开发 tip，以及只有未提交用户工作的 worktree。
- 把仍有唯一有效成果的分支/提交真实合入；对过时历史不得把数千个旧提交机械重放到当前架构，也不得用兼容层或 fallback 掩盖冲突。
- 保留其他 worktree 的未提交内容；不暂存、不提交、不覆盖、不 stash。
- 完成类型检查、路由、文档、国际化、相关正向非 UI 契约和二次 review；commit subject 使用 `dsw-33987` 前缀，正常执行 hooks 后显式 push `HEAD:refs/heads/v0.0.35beta` 到 `origin`，再由 `git ls-remote origin` 回读精确 SHA。

#### 硬约束

- 已完整重读根 `AGENTS.md`；不使用 reset、clean、stash、`checkout --`、`--no-verify` 或强制 push，不创建新 worktree，不干预正在运行的 OpenCorvus/Overlay。
- 不新增、修改或运行 User Interface（UI，用户界面）自动化测试；若合并改变 UI，验收只能使用真实页面、交互、截图和人工复核。
- 不把 detached benchmark/cold-install 目录中的生成物或其他任务的未提交 Zapier Model Context Protocol（MCP，模型上下文协议）实现冒充分支成果。
- `origin/v0.0.35beta` 是从 GitHub `candidate` 发布的 source-snapshot 历史，与当前 legacy remote 派生本地线没有共同的近期提交；必须先保留双方历史并证明当前树选择，禁止非快进覆盖 GitHub。

#### 已读取资料

- 根 `AGENTS.md` 全部 313 行。
- 本文件原 Recall、Plan 和 Verification Ledger。
- `2026-08-07-computer-use-e2e-root-cause-repair-plan.md` 的 Recall、Implementation progress 和真实验收记录。
- `2026-08-07-v0031-unmerged-commit-recovery.md`、`2026-08-07-github-actions-native-package-matrix-repair.md`，以及当前 `specs/README.md` / `specs/records/2026-08/README.md` 索引。
- 全部本地 branch、`origin/*`、现存 worktree、detached task tip 和它们相对 `v0.0.35beta` 的可达性。

#### 全仓 grep / Git 拓扑结果

- 主 worktree 当前干净，`v0.0.35beta=c5d64af7f8`；它原来错误地跟踪已停止使用的 `legacy-remote/v0.0.35beta`。
- Fresh `origin` fetch 成功；`origin/v0.0.35beta=581a49f84c`，其 ancestry 来自 `origin/candidate` source snapshot，和本地交付线的计数为 `6840/214`，因此不能直接 fast-forward，也不能丢弃任一侧历史。
- 旧本地 release tips `v0.0.18beta` 至 `v0.0.30beta`、`v0.0.34beta` 和 `codex/goal-dependency-retirement` 均已是当前线祖先。`v0.0.31beta` 的 15 个 Git 独有提交已有专门 semantic-recovery 记录，须复核树级结果而非再次重放旧 VM/UI 架构。
- `v0.0.33beta` 有 3 个 Git 独有提交，包含 Task recovery/archive authority 和 secure remote MCP credential；需逐提交审查与当前代码冲突后真实集成或证明已由更新实现覆盖。
- detached `d03002cf05` 是当前 Computer Use 的 host-native CUA（Computer-Using Agent，计算机操作智能体）driver 完整成果，尚未进入 `v0.0.35beta`，必须真实合入并保留其正向契约和真实截图证据。
- `C:\Users\hengu\.codex\worktrees\8535\opencorvus` 有另一任务的未提交 Zapier/MCP/Overlay/spec 修改；benchmark worktree 有大量古老未提交迁移；cold-install worktree 只有生成的 `bun.lock` 改动。这些都不是可合并 branch commit，本任务只保留，不获取所有权。
- 多个 `codex/*`、`feat/*` 和 `coding-assistant` 本地分支来自数千提交前的旧架构线。直接 merge 会重新引入已删除的 Goal/delivery gate、旧 package spec 树和 UI 自动化资产，违反当前单一来源与禁止 gate/兼容规则；它们只能在树/patch 审计确认仍有当前唯一成果时做 semantic integration，不能按名字机械合并。

#### 独立 Agent 反馈

- 用户没有要求子 Agent；按当前协作约束不启动子 Agent。

### Continuation Plan

1. 固定 origin-only authority：刷新 origin，审查 Git 配置与所有 ref/worktree 状态，在不删除 remote 的前提下把当前 branch upstream 和默认 push 目标切到 origin。
2. 先把 `origin/v0.0.35beta` 作为历史父线收敛进当前分支，并用树差异证明选择当前 `0.0.35-beta` 主树，不做非快进覆盖。
3. 逐项审查并集成 `v0.0.33beta` 的 3 个提交和 detached Computer native-driver 提交；对 `v0.0.31beta` 与旧 feature tips 用 patch/tree 证据确认已恢复或过时，不重引入旧架构。
4. 运行受影响的正向非 UI 契约、SDK/OpenAPI 同步检查、workspace typecheck、routes、docs、Overlay i18n、diff 检查；若 UI 实际改变，启动隔离真实页面并截图人工复核，绝不运行 UI 自动化测试。
5. 二次 review 完整 ancestry、最终树和其他 worktree 状态；以 `dsw-33987` 提交，正常 hooks push 到 `origin/v0.0.35beta`，回读 SHA 并更新本 Ledger。

### Continuation Verification Ledger

- Fresh `origin` fetch and direct `git ls-remote origin refs/heads/v0.0.35beta` both resolved `581a49f84c`. Its source-snapshot tree differed from the pre-convergence local tree in only the 13 subsequently committed Mission/spec files, despite the histories having diverged by thousands of commits. Commit `bf73472049` recorded the plan; merge `4160d7f9c9` retained the newer local tree while making the GitHub snapshot history reachable, so the final push is fast-forward rather than a forced replacement.
- The three Git-unique `v0.0.33beta` commits were merged as parent `c8dd5e0c06`. Conflict review proved the current tree already carried their complete Task recovery, archive header, and secure MCP credential behavior through the later `v0.0.34beta` semantic migration; resolution retained the newer host/task process-authority and scoped-owner-key fixes. The merge therefore changed ancestry without duplicating code.
- Native Computer commit `d03002cf05` was merged as parent `9d0beb94e7`. Conflict resolution deliberately selected its CUA Driver backend and viewer/runtime deletions, retained the later Expert Squad harness positive contract, combined the current lockfile with the exact `@trycua/cua-driver@0.12.2` native closure, and retained all current spec indexes. Focused verification passed `15 tests / 69 assertions`; package TypeScript passed.
- `b5702fedf0` records every remaining local branch tip as a merge parent after audit classified the May/June lines as superseded architecture and the `v0.0.31beta` line as already semantically recovered. `715e7688b9` similarly closes the three remaining old GitHub branch tips. These closure merges preserve the current tree instead of reintroducing retired Goal/delivery gates, package-local specs, old Coding Assistant surfaces, or UI automation assets.
- A complete post-merge reachability scan reports `ALL_LOCAL_AND_ORIGIN_REFS_REACHABLE`. Every registered worktree tip, including detached `9bd2c03408`, `6ec621995b`, `0e80b5c981`, `1457fb3532`, and `d03002cf05`, is also an ancestor of the final line. Three registrations whose directories no longer existed were removed with `git worktree prune`; no existing directory or worktree content was deleted.
- Dependency installation initially failed because the user-level npm registry and fake-IP direct path returned `ConnectionClosed`/HTTP 502. The repaired invocation used process-local `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY=http://127.0.0.1:7890` plus `bun install --frozen-lockfile --registry=https://registry.npmjs.org`; it installed the exact lock successfully without changing global network configuration.
- SDK regeneration completed and exposed stale generated API projections left by the cross-branch merge; the generated OpenAPI and TypeScript outputs were synchronized in a dedicated follow-up commit. The reviewed tree then passed SDK import, AI runtime, all eight workspace TypeScript tasks, API routes (`6 rules / 34 files`), docs (`322 operations / 25 groups`), Overlay i18n, release-family version `0.0.35-beta`, and `git diff --check`. The AGENTS-mandated historical-docs test path is absent in this checkout, so no nonexistent test was claimed or run.
- Existing Computer screenshots were opened and manually reviewed after integration: real Windows host input is visible, the control surface clearly distinguishes Agent ownership, human takeover, and returned Agent ownership, and the return notice requires the new run to attach and observe before input. No UI automated test was added, modified, or run.
- During final validation another process wrote six browser-MCP/GitHub-actions files in the shared main worktree. They were neither staged nor committed by this convergence task. Their owner later caused SDK regeneration to see additional current API descriptions; only the canonical generated SDK/OpenAPI outputs and this ledger are owned by the follow-up synchronization commit.
- The first normal origin push stopped in the unmodified pre-push hook because the default Bun was `1.3.13` while the repository requires `^1.3.14`. Re-running with the repository's existing `.scratch/bun-1.3.14/bun-windows-x64` directory prepended to that process's `PATH` passed the complete hook without replacing the user's global Bun.
- The ensuing full-history fast-forward upload was rejected by GitHub `GH001`: old branch ancestry contains retired build outputs between 121 MB and 155 MB under `packages/overlay/dist-artifacts/**` and `opencorvus-dist/**`, exceeding GitHub's 100 MB object limit. This proves why blindly publishing the raw consolidated legacy remote ancestry cannot be the GitHub delivery mechanism. No force push or history rewrite was attempted.
- GitHub delivery therefore uses the repository's already recorded source-snapshot contract: one new commit whose parent is the existing `origin/v0.0.35beta` and whose tree is exactly the final validated local `v0.0.35beta` tree. It is pushed by ordinary fast-forward with normal hooks. Local history retains every branch as an ancestor; GitHub receives the exact current source without the retired binary objects. The remote snapshot is explicitly a source-tree delivery, not a claim that GitHub stores the raw historical ancestry.
