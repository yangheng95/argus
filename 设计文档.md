# OpenCorvus

## 定位

- [ ] OpenCorvus 是一个全平台通用的GUI操作Agent，可以无人值守的模式管理桌面和窗口。
- [ ] OpenCorvus 也可以直接用于coding，提供一个可以托管的编程环境，用于给定目标前提下无人值守的原型开发。
- [ ] OpenCorvus 像是 opencode 和 openclaw 的结合体，既提供可托管的编程环境，也提供全平台通用的 GUI 操作能力。

## 原始能力清单（保留原文语义）

- [ ] 基于Bot的GUI操作Agent，像openclaw一样支持多种bot，例如slack，discord，telegram等。
- [ ] 通用的全平台GUI操作能力，支持Windows，MacOS，Linux等操作系统。点击，键盘输入/快捷键，拖动等基本操作都可以实现。
- [ ] 自适应分辨率的窗口定位和操作，支持多显示器环境。支持各种情况下的GUI坐标计算，例如窗口被遮挡，分辨率变化等。
- [ ] 支持无人值守的模式，可以在后台运行，定时执行任务，监控系统状态等。
- [ ] 自带一个Terminal UI (TUI)，可以在命令行界面进行无人值守的开发。
- [ ] 复用opencode的所有功能，例如tool, agent, plugin, mcp等。
- [ ] 使用skill定义全平台的GUI操作能力，提供一个统一的接口来实现各种GUI操作。例如描述平台特定的应用打开方式，窗口定位，操作步骤，组合快捷键等。
- [ ] OpenCorvus启动后即可开始操作，第一次启动的时候可以基于GUI操作的自动化的bot配置，这也是skill的一部分。覆盖多种平台和bot。
- [ ] 提供系统托盘和一个可视化的对话界面来管理OpenCorvus的运行状态，例如发送指令，查看日志，配置bot等。
- [ ] 为了辅助视觉LLM找到GUI元素和坐标，截图被添加了自适应分辨率的坐标系。
- [ ] 任务执行过程中，OpenCorvus会把截图发送给手机端的bot，bot会把截图展示给用户，方便用户了解当前的执行情况和系统状态。用户可以通过bot发送指令给OpenCorvus，例如暂停，继续，取消等。
- [ ] OpenCorvus在操作GUI的时候屏幕上要有视觉反馈，例如在点击一个按钮的时候，按钮会有一个高亮的效果，或者在操作完成后有一个提示框显示操作结果等。这些视觉反馈可以帮助用户更好地理解系统的操作过程和结果。
- [ ] OpenCorvus的视觉分析能力需要具备一定的鲁棒性，能够适应不同的界面布局和元素样式，妥善处理逻辑分辨率和物理分辨率。
- [ ] 所有的设计都是通用抽象，涉及到不同平台和bot的细节都应该通过独立模块或者插件的方式来实现，防止耦合影响维护。
- [ ] 像opencode的webui一样，OpenCorvus提供一个可视化的对话界面来管理和监控系统的运行状态，例如查看当前的任务队列，监控系统资源使用情况，查看操作日志等。
- [ ] 长期和短期记忆：OpenCorvus需要具备一定的记忆能力，能够记住之前的操作和任务执行情况，以便在新的任务中进行参考和优化。
- [ ] 定时任务和事件触发：OpenCorvus需要支持定时任务和事件触发的功能，能够在特定的时间或者特定的事件发生时自动执行某些操作。
- [ ] 语音能力（Voice Wake + Talk Mode）。
- [ ] Always-on 语音唤醒（macOS/iOS/Android）。
- [ ] 连续对话模式覆层。
- [ ] ElevenLabs TTS 文字转语音。
- [ ] Host服务。
- [ ] agent 驱动的可视化工作区：agent 可以 push/reset/eval/snapshot canvas 内容。
- [ ] 双向 A2UI 桥：前端操作可以触发 agent 工具调用，agent 可以更新前端界面。
- [ ] Live Reload：WebSocket 实时推送文件变更（75ms 防抖）。
- [ ] 静态文件服务：serve agent 生成的 HTML/CSS/JS/媒体文件。

## 架构调整决议（当前新增）

- [x] 放弃 a2a 结构。
- [x] 把 vision、plan、gui 操作全部抽象为 tool。
- [x] tool 执行过程中产生的中间上下文按 opencode 策略舍弃。
- [x] 主消息流只保留 tool 执行结果（必要结构化结果与关键错误）。
- [x] `a2a_task_queue` 仅保留为历史数据库命名，不代表当前仍使用 a2a 架构。

## 工具映射（与实现对齐）

- [x] 规划能力：`planner`（对应原文 `plan_tool`）。
- [x] 视觉摘要能力：`vision_analyze`（对应原文 `vision_tool`）。
- [x] GUI 执行能力：`screen` + `input`（共同对应原文 `gui_tool`）。
- [x] 任务队列入口：`/session/:id/prompt_async` -> `TaskQueueService`。
- [x] prompt 执行内核统一：`/session/:id/message`（同步）与 `/session/:id/prompt_async`（异步）共用 `TaskQueueService.executePrompt`。
- [x] 记忆能力：`memory`（`search/get/write/list/delete`）。

## 执行流程（tool-first）

- [ ] 异步任务先进入队列；同步任务直连执行器；两者共用同一 prompt 执行内核，并基于当前goal完成情况与系统状态推进。
- [ ] 主流程先调用 `memory(search)` 检索历史经验与约束；命中后按相关性读取 `memory(get)`。
- [ ] 主流程调用 `planner` 产出步骤并维护任务状态（add/update/list）。
- [ ] 遇到外部知识缺口时必须调用 `websearch`；拿到明确 URL 后调用 `webfetch` 补全细节。
- [ ] 视觉分析优先调用 `vision_analyze` 获取界面状态、元素和坐标摘要（截图不进入主消息流）。
- [ ] GUI 执行由 `screen` + `input` 完成并返回结构化结果。
- [ ] 如果操作失败，错误回流到 `planner` 重新制定计划。
- [ ] 直到操作成功或达到重试次数上限。
- [ ] 每个关键里程碑（完成子目标、出现新坑、最终交付）必须调用 `memory(write)` 沉淀结论。

## 上下文管理（明确化）

- [ ] `vision_analyze` 的截图只用于分析，不进入主对话流。
- [ ] `screen.screenshot` 的截图会在当前轮作为 tool 附件进入上下文；历史轮次会自动清理截图附件，仅保留文本描述。
- [ ] 视觉分析结果以文本形式进入主对话流。
- [ ] 新任务开始时总结上一轮执行情况，作为新上下文起点。
- [ ] 保留：tool最终结果、关键证据、关键错误、已写入的记忆条目ID。
- [ ] 舍弃：tool中间推理、冗长过程输出、重复无效历史。
- [ ] 记忆写入最小规范：`做了什么` / `为什么` / `结果` / `坑点` / `下次建议`。

## 迁移清单（checkbox）

- [ ] 移除 a2a_plan / a2a_vision / a2a_gui 的自动委派入口（若仅剩历史命名，补充注释说明）。
- [ ] plan/vision/gui 能力全部沉降到 tool 层（实现名统一为 `planner` / `vision_analyze` / `screen+input`）。
- [ ] 统一 tool 结果结构：`status` / `summary` / `artifacts` / `error` / `next_action`。
- [ ] 禁止历史 screenshot 原图长期滞留主消息流（允许当前轮临时附件，并要求文本描述沉淀）。
- [ ] 增加防卡机制：单tool超时、总任务超时、无进展检测、重试上限。
- [ ] 增加可恢复机制：task_id 续跑、失败分级、恢复建议。
- [ ] 补齐日志观测：耗时、重试、失败分类、上下文压缩率、记忆命中率与写入频次。

## 验收标准

- [ ] 历史轮次的 screenshot 原图附件会被自动清理，主消息流不保留长期图片负担。
- [ ] 长任务上下文长度稳定，不再持续膨胀。
- [ ] 出现失败时可重规划推进；不可推进时有明确失败原因和恢复建议。
- [ ] Slack 全链路可跑通：发起 -> 执行 -> 失败重试 -> 恢复 -> 完成。
