# v0.0.8beta Native-menu Matrix Repackage

## Recall

### User request

- Rebuild the package matrix after the macOS native application-menu and titlebar traffic-light alignment changes.

### Acceptance criteria

1. Fetch legacy remote and prove that the local `v0.0.8beta` source is aligned with `legacy-remote/v0.0.8beta`.
2. Run the established production matrix in the required order: Graphical User Interface (GUI) installer first, portable Command-Line Interface (CLI) binary second.
3. Build and validate the native macOS ARM64 row. Report Linux x64/ARM64, macOS x64, and Windows x64 as skipped because matching native runners are unavailable.
4. Verify canonical version, executable architecture, application metadata, strict code signatures, Apple Disk Image (DMG) integrity, archive contents, isolated packaged-runtime health, and artifact SHA-256 (Secure Hash Algorithm 256-bit) values.
5. Visually inspect the packaged macOS application without interfering with the user's running OpenCorvus process, then preserve generated outputs as untracked artifacts and push only the evidence record to legacy remote.

### Hard constraints

- Do not reset the worktree, create another worktree, or restart/close/refresh a user-running OpenCorvus or Overlay process.
- Preserve the four unrelated Frontend replica End-to-End (E2E) files and keep them outside all commits.
- Use `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` as the sole production owners. Existing generated matrix outputs may be replaced only by those owners because the user explicitly requested this rebuild.
- Do not claim non-native rows as packaged or verified. Do not commit generated installers, binaries, archives, or Tauri schemas.
- Commit subjects use the required `dsw-33987` prefix and push only to `legacy-remote/v0.0.8beta` through repository hooks.

### Sources read

- Repository `AGENTS.md`, including package evidence, version-control, visual acceptance, running-process, and remote baseline boundaries.
- `script/package-gui-installer-matrix.ts` and `script/package-binary-matrix.ts`.
- `docs/packaging.md`, `RELEASE.md`, root package scripts, release workflow call sites, and matrix contract-test call sites.
- `specs/records/2026-07/2026-07-17-v0.0.8beta-latest-matrix-build.md`, `specs/records/2026-07/2026-07-17-v0.0.8beta-100bb-matrix-repackage.md`, and `specs/records/2026-07/2026-07-17-v0.0.8beta-remote-merge-and-matrix-build.md`.

### Whole-repository search evidence

| Surface / call site | Disposition |
| --- | --- |
| Root `package:gui-installer-matrix` | Retain as the five-row GUI installer owner. It invokes real SDK, Overlay, Tauri, staging, and release-asset validation paths for the matching native host row. |
| Root `package:binary-matrix` | Retain as the five-row portable CLI owner. It invokes, executes, archives, and validates only the matching native host row. |
| Root `package:linux-binary` | Exclude. It is the separate Linux x64/baseline remote-service bundle flow and this macOS-native matrix request does not authorize an upload. |
| `.github/workflows/build.yml` and `build-overlays.yml` | No edit. They already invoke the canonical GUI matrix owner on native runners. |
| `docs/packaging.md` and `RELEASE.md` | No edit. Their GUI/CLI entrypoints and native-runner semantics match this execution. |
| Matrix and release-contract tests | Run after packaging to prove row selection, artifact ownership, and release workflow contracts. |
| Existing generated `dist-artifacts/darwin-arm64` | Replace only through the requested GUI matrix owner; keep the regenerated directory untracked. |

### Independent agent feedback

- None. The user did not request sub-agent work.

## Starting evidence and execution plan

- Fresh fetch proves local `v0.0.8beta` and `legacy-remote/v0.0.8beta` both resolve to `7bce3baba366472111f7ac8c9063049e80a44554` with divergence `0 0`.
- `bun run version:check` passes at the canonical repository version `0.0.8-beta`.
- The four unrelated Frontend replica E2E files are recorded by SHA-256 before packaging and remain unstaged. Their test-only paths are not runtime package inputs.
- Run GUI matrix, then CLI matrix. Inspect native package identity and integrity, launch an isolated packaged GUI for screenshot inspection, run isolated packaged CLI health/shutdown verification, execute focused matrix and document tests, record exact evidence, review the final diff, commit only this record/index updates, fetch again, and push to legacy remote.

## Verification record

### Production matrix

- `bun run package:gui-installer-matrix` completed first. It rebuilt and release-validated the native macOS ARM64 GUI executable, `OpenCorvus_0.0.8-beta_aarch64.dmg`, and `OpenCorvus_0.0.8-beta_aarch64.app.tar.gz`. Linux x64/ARM64, macOS x64, and Windows x64 were explicitly reported as skipped because matching native runners were unavailable. Tauri emitted only the two existing macOS taskbar-badge dead-code warnings.
- `bun run package:binary-matrix` completed second. It compiled and executed the native macOS ARM64 CLI and produced `opencorvus-darwin-arm64.tar.gz`; the same four non-host rows were explicitly skipped.
- The staged GUI executable, application executable, and CLI runtime are Mach-O 64-bit ARM64. CLI `--version`, `CFBundleShortVersionString`, and `CFBundleVersion` all report `0.0.8-beta`.
- Strict code-sign verification passed for the application bundle and staged GUI executable. `hdiutil verify` reported the DMG checksum valid. The application archive contains the signed `OpenCorvus.app`; the CLI archive contains the runtime, embedded UI, Browser Model Context Protocol (MCP) sidecar, Browser Node runtime, colocated modules, package metadata, and `bin` directory.

### Runtime and visual acceptance

- Finder-like CLI verification used the packaged runtime with `PATH=/usr/bin:/bin`, isolated `HOME` and `OPENCORVUS_HOME`, and port `47886`. `/global/health` returned `healthy: true`, version `0.0.8-beta`, and database/data/home paths entirely under `/tmp/opencorvus-matrix-*`. SIGINT shutdown reported zero live tasks, runs, goal runs, ownerships, sessions, tool parts, or corrupt tasks, then released the port.
- The exact packaged application was launched with `open -n` and task-specific temporary `HOME`/`OPENCORVUS_HOME` values. Its bundled sidecar returned healthy `0.0.8-beta` state from an isolated runtime path. Original-resolution screenshots were captured under `.scratch/v0.0.8beta-native-menu-matrix-*` and personally inspected: the close/minimize/zoom controls and titlebar actions share the same horizontal center line; the window contains no duplicate HTML File/Edit/View/Help menu; and the native macOS menu bar exposes `OpenCorvus`, `File`, `Edit`, `View`, `Window`, and `Help`. No clipping, overlap, or blank loaded state remained after foreground activation.
- The isolated application was closed through its native `Quit OpenCorvus` item. Its sidecar reported zero live execution state and ports `7879`/`7880` were released. The pre-existing OpenCorvus process was not closed, restarted, or refreshed.

### Artifact identity

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| GUI executable `opencorvus-overlay` | 155 MiB | `1853be662b1525145c304dd1c2415aeb10b7c0de3ee02162ef328bbeb90ced44` |
| `OpenCorvus_0.0.8-beta_aarch64.dmg` | 146 MiB | `f80b96b00c9075869ec9e247bd5a1cb29267cd2e6842a73463a6bf4b4b3e7827` |
| `OpenCorvus_0.0.8-beta_aarch64.app.tar.gz` | 145 MiB | `99c232c7d2326a58708f96b921982d1b67f068f21868b51ee75441e6b784738d` |
| CLI runtime `opencorvus` | 96 MiB | `6ad58ae56a398ca332881731b0cf2344ce5b2db18c94eba98d7298ecc3a2d019` |
| `opencorvus-darwin-arm64.tar.gz` | 111 MiB | `330feb7f92fd4c1a3e88681be833fa50c70621ad5bacd293ae85c28c5cd2d903` |

### Tests and preservation

- GUI matrix, binary matrix, native binary, and release-workflow contract suites passed 36/36 with 103 expectations.
- Historical-link, document-health, and product-document single-source suites passed 81/81 with 1,282 expectations.
- The transient generated Tauri macOS schema was removed. CLI outputs remain under ignored `packages/opencorvus/dist/`. During finalization, a concurrent OpenCorvus Mission created its own checkpoint and included its four Frontend replica E2E files together with the regenerated GUI artifact directory. This packaging task did not stage or alter those paths; their exact checkpoint tree was preserved rather than rewriting another task's content.
- The four concurrent Frontend replica E2E files retain their pre-build SHA-256 values `6fae1c9…4307`, `a3ca379…0434`, `a52df19…5f10`, and `6755693…ad5`. Their status changed from local/untracked to tracked only through that concurrent Mission checkpoint.
- A post-build fetch kept local and `legacy-remote/v0.0.8beta` aligned at plan commit `394983b33` with divergence `0 0`. The executable source remains the native-menu commit `7bce3baba`; the plan commit changes documentation only.
- The first final push was rejected because the concurrent checkpoint subject lacked the mandatory task prefix. Only its unpushed commit header and this evidence commit's parent link were reconstructed: `263a156bb` became `266acfa02` with the `dsw-33987` prefix, while both commit trees, authors, timestamps, and file bytes remained identical. No reset, rebase, checkout, or worktree-content rewrite was used.
