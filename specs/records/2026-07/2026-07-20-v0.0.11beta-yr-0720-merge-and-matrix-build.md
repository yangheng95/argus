# v0.0.11beta yr-0720 Merge And Matrix Build

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Synchronize `work-v0.0.11beta-yr-0720` into the current code and compile again. |
| Acceptance criteria | Integrate the complete legacy remote work-line tip into `v0.0.11beta`; preserve delivery-line work; resolve conflicts from current architecture and tests; push the merged branch; run GUI installer and CLI binary matrices from one stable commit; verify version, native signatures, disk image, artifact hashes, and matrix contracts. |
| Hard constraints | Do not reset, create a worktree, restart OpenCorvus/Overlay, bypass hooks, manufacture fallback behavior, or stage the pre-existing untracked `C:/` directory. Use commit prefix `dsw-33987` and push only to legacy remote. Native matrix rows must be reported honestly; this macOS ARM64 host cannot validate other target operating systems/architectures. |
| Sources read | Repository `AGENTS.md`; prior `2026-07-20-v0.0.11beta-branch-and-version-bump.md`; current status/branch; fetched delivery/work tips; divergence, commit log, and three-dot diff statistics. |
| Whole-repository merge surface | The work line changes 100 files across Mailbox routes/UI, Work Ledger session projection, project folder refresh, conversation/delegated-context display, Settings, status mapping, generated SDK/OpenAPI docs, architecture/spec records, and corresponding unit/browser tests. All affected production paths and sibling tests are present in the three-dot diff; conflict resolution must retain delivery-line work not visible in that diff. |
| Git evidence | Local and `legacy-remote/v0.0.11beta` are identical at `6794955d6`. Work tip is `017985661`; delivery and work lines have 26 and 13 unique commits respectively. |
| Preservation boundary | The tracked worktree is clean. The untracked `C:/` directory predates this task and remains outside staging. |
| Independent agent feedback | None requested; current collaboration policy forbids unrequested delegation. |

## Plan

1. Commit and push this Recall/plan before merging.
2. Re-fetch both branches, merge `legacy-remote/work-v0.0.11beta-yr-0720` with an explicit merge commit, and resolve every conflict by inspecting base/ours/theirs plus current architecture/test ownership.
3. Run focused tests for conflicted and changed surfaces, Overlay/OpenCorvus typechecks, documentation health, and any required Node browser scenarios; visually inspect task-scoped screenshots for the merged frontend surfaces.
4. Update this verification ledger, commit the merge, push through normal hooks, and prove the work tip is an ancestor of an identical local/remote delivery head.
5. Fix the build commit, run `version:check`, GUI installer matrix, then CLI binary matrix without allowing the source commit to move.
6. Validate produced artifacts, macOS signature, DMG checksum, CLI version, SHA-256 hashes, matrix contract tests, final remote equality, and worktree preservation.

## Verification Ledger

- Pre-merge plan commit `270d71914` was pushed to `legacy-remote/v0.0.11beta`; the work-line tip remained `0179856615a20401301abc5e8feb94cc3ec12798` at merge time.
- The merge produced four textual conflicts. `WorkLedger.tsx` retains the delivery line's priority/manual/updated sorting while accepting the work line's retirement of Task/Mission/Chat pin state. `messages.css` retains reasoning-display retirement and shares transcript variables with delegated context through one selector owner. Both spec indexes retain records from both lines.
- Full Overlay unit verification exposed and repaired stale cross-line assertions/config fixtures instead of restoring retired behavior: public settings fixtures now provide Work Ledger organization/sort values; opacity uses semantic tokens; left-rail/titlebar tests reflect the shadowless rail and opaque semantic titlebar; Work Ledger title assertions follow the reactive translation owner; Settings/Memory tests follow opacity tokens; the retired Settings tab count is removed; and the shared transcript selector no longer increases duplicate-selector debt. The complete Overlay unit suite then passed from the first file through the last with zero failures.
- Focused backend/contract verification passed 54 tests across Mailbox routes, Work Ledger routes, generated Software Development Kit contracts, and transport contracts. Repository TypeScript verification passed all 9 applicable Turbo tasks.
- Documentation verification passed 87 tests / 1,408 expectations across historical links, document health, and product-document single-source coverage.
- Five Node-launched real browser scenarios passed: left-sidebar Mailbox mark-all-read, folder picker project-list refresh, goal status colors, Settings resizing/entry focus, and chat-bubble disclosure. Current-task screenshots were reviewed at original resolution; the Mailbox, project list, compact goal rows, opaque Settings surface, and expanded transcript have coherent layout, alignment, state, and readable chrome without a restored rail seam or focus residue.
- The pre-existing untracked `C:/` directory remains unstaged and untouched. Matrix build evidence is reported after this merge record is committed so the artifact source commit stays immutable throughout packaging.
