# v0.0.18beta macOS ARM64 Matrix Packaging

## Recall

| Item                       | Evidence / constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| User request               | Run matrix packaging for the current `v0.0.18beta` branch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Acceptance                 | Run the native GUI installer matrix before the portable command-line interface matrix, package only the host-supported macOS ARM64 rows, verify version, architecture, signatures, disk image integrity, archive contents, and checksums, and report every unsupported row as skipped.                                                                                                                                                                                                                                                                                   |
| Git and version baseline   | Packaging started from local and `legacy-remote/v0.0.18beta` commit `92d765c38e4952d5f6097df6b1c326fb3634837f`; `bun run version:check` confirmed canonical `0.0.18-beta`; the host is macOS ARM64.                                                                                                                                                                                                                                                                                                                                                                            |
| Parallel changes           | The shared worktree contained a large active source/test refactor plus five untracked files. Before packaging, the tracked binary-diff SHA-256 was `7d002ccb2c1aca40f329799741f7aff53b716d648f36871f54932c1e026a06ec`; two stability samples matched. The five untracked file hashes remained unchanged during packaging, including `expert-squads/.DS_Store` at `6b84fb2a9bc22329b6afbd83d9348b2a5e3aa3bd3db828ea27ae998351638393`. Concurrent tracked edits continued during the build, so their aggregate diff hash legitimately advanced independently of this task. |
| Hard constraints           | Preserve and exclude all parallel edits; do not stash, reset, restore, create a worktree, restart OpenCorvus/Overlay, or push. Use the repository matrix commands and native platform tools rather than claiming cross-host verification.                                                                                                                                                                                                                                                                                                                                |
| Sources read               | Root `AGENTS.md`; prior `v0.0.18beta` Recall; `docs/packaging.md`; `script/package-gui-installer-matrix.ts`; `script/package-binary-matrix.ts`; `script/package-native-binary.ts`; the focused packaging tests; the repository beta-release workflow record.                                                                                                                                                                                                                                                                                                             |
| Whole-repository grep      | `package:gui-installer-matrix` is owned by `script/package-gui-installer-matrix.ts`; `package:binary-matrix` delegates through `script/package-binary-matrix.ts` to `script/package-native-binary.ts`. The native packager's only pre-fix runtime acceptance was file completeness plus `--version` smoke checks; no macOS code-sign step or strict signature verification existed elsewhere in the portable command-line interface path.                                                                                                                                |
| Independent-agent feedback | None. The user did not request delegation and the active execution policy forbids unsolicited sub-agent delegation.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Plan

1. Confirm a stable worktree sample and canonical release version without modifying protected files.
2. Run `bun run package:gui-installer-matrix`, then `bun run package:binary-matrix`.
3. Independently verify native artifacts and repair any packaging defect at its owner with regression coverage.
4. Rerun the affected matrix and complete artifact, test, and preservation audits without pushing.

## Verification ledger

- GUI matrix: `darwin-arm64` packaged successfully and the release-asset checker passed. Linux x64, Linux ARM64, macOS x64, and Windows x64 were explicitly skipped because they require matching native hosts.
- GUI artifacts: `opencorvus-overlay` is a thin ARM64 Mach-O with a valid strict ad-hoc signature; the complete `.app` bundle also passes deep strict signature verification. `OpenCorvus_0.0.18-beta_aarch64.dmg` passes `hdiutil verify`; both bundle version fields are `0.0.18-beta`. Tauri explicitly skipped notarization because no Apple notarization credentials were configured.
- Independent audit found the first portable command-line interface artifact had an invalid linker-generated ad-hoc signature even though its version smoke check passed. Root cause: the native packager did not re-sign or strictly verify the Bun-produced executable after packaging.
- Root repair: `script/package-native-binary.ts` now uses native `codesign --force --sign -` followed immediately by `codesign --verify --strict --verbose=2` for macOS artifacts before smoke verification and archive creation. Non-macOS matrix rows remain unchanged. Regression coverage pins the exact native commands and platform scope.
- CLI matrix rerun: `darwin-arm64` packaged successfully; Linux x64, Linux ARM64, macOS x64, and Windows x64 were explicitly skipped. The staged executable and a separately extracted archive copy both pass strict signature verification, report thin ARM64 Mach-O, and return `0.0.18-beta`.
- Packaging regressions: the six documented suites pass 49/49, including native, Linux, GUI, matrix, release-contract, and install-script coverage. A cross-platform Windows installer fixture was corrected to invoke `opencorvus.exe` explicitly when the test simulates Git Bash on a non-Windows host; real Windows keeps its extensionless invocation assertion.
- Repository checks: version convergence, route inventory, and generated API documentation checks pass. Full typecheck is not currently green because concurrent work in `packages/channel-runtime/src/core.ts` still supplies a removed `system` prompt-input field. This is outside the packaging files and remains owned by the parallel task; it was not modified or hidden here.
- Final artifact hashes and sizes:
  - GUI executable: `ce4c89e938d8fbd774c815f89a15b63d4ca041b76c6508e6bcdd9fc6f4cf9d50`, 166071872 bytes.
  - DMG: `9544e0f74d1a37cee4f2e5f0d84638813022d727d4b9a753c4d219ac7840a5ec`, 158333017 bytes.
  - App archive: `e5e1cf52892d96db0ec9a8cd7f2a885d282f75637ac9a4b3bd4587c2b0b857c7`, 157618789 bytes.
  - CLI executable: `527096fa0daf647a9449c9c12af56569905a09c8eb1be503a19f73bd3053f051`, 101249936 bytes.
  - CLI archive: `d7bb5ae4584912a14c909a0c8dd7630c03755c63744f2fd5815b7d33710f59b4`, 118636134 bytes.
- Delivery boundary: generated artifacts remain in their ignored distribution directories; only the packaging root repair, its tests, this record, and its indexes belong to the local task commit. No push is authorized or performed.

## Post-merge rebuild

After local merge commit `eca6bcf9e`, both matrices were rebuilt from the merged `v0.0.18beta` source in the required order.

- `bun run version:check`: passed at `0.0.18-beta`.
- `bun run package:gui-installer-matrix`: packaged and release-validated `darwin-arm64`; Linux x64/ARM64, Darwin x64, and Windows x64 remained explicit native-runner skips.
- `bun run package:binary-matrix`: packaged and runtime-validated `darwin-arm64`; the same four non-native rows remained explicit skips.
- Both staged executables and the executable extracted from the CLI archive are thin ARM64 Mach-O files, pass strict code-sign verification, and report `0.0.18-beta`.
- The built and separately extracted application bundles pass deep strict code-sign verification. Both application version fields are `0.0.18-beta`; the disk image passes `hdiutil verify`; the application archive contains the complete `OpenCorvus.app`.
- Post-merge artifact hashes and byte sizes:
  - GUI executable: `76e923d3120b7242ee1f1f9a813db4dbc1b41f51947c2004886d76b66f00cee3`, 166055360 bytes.
  - DMG: `0cadd72286b6c9425e519c459db2e8ca444a2d2be47920a3847d88ca615d1794`, 158324157 bytes.
  - App archive: `7037b14a0b3310c168b87ee98b84832dcf3e74465e77ede94ff54865a9a0f87a`, 157619004 bytes.
  - CLI executable: `3fee829ab8d89c1c36b1530c47dbc833a6ce019d68471f48277ffec4498a5179`, 101249936 bytes.
  - CLI archive: `e13adc0f0971ae034e440ae3f6ea2174999a023a86f24440b3348ffc5e467bad`, 118629881 bytes.
- The unrelated untracked `expert-squads/.DS_Store` remained untouched. No push was performed.
