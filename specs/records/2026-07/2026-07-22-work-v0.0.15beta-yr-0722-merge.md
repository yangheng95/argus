# work-v0.0.15beta-yr-0722 Merge

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Merge `https://git-cc.myhexin.com:6443/yangheng/opencorvus/-/commits/work-v0.0.15beta-yr-0722` into the current delivery branch. |
| Acceptance criteria | Preserve every current local/parallel change; merge the complete remote work branch into `v0.0.15beta`; resolve conflicts without dropping either side; validate the merged Overlay and repository contracts; commit and push the resulting `v0.0.15beta` to `myhexin`. |
| Git baseline | After fetch, `myhexin/work-v0.0.15beta-yr-0722` is `47f500e4e`. It contains six commits after common ancestor `62127b40a`. The pre-merge parallel Mission/squad-stage work was preserved exactly in checkpoint `a3953f902` and pushed to `myhexin/v0.0.15beta` before the merge began. |
| Parallel work | Four later unstaged parallel edits remain in `orchestrator-core.txt`, `core-prompt-hygiene.test.ts`, `2026-07-22-mirror-prism-full-workflow-distillation.md`, and `2026-07-22-review-debug-expert-squad.md`. They remain outside the merge index and must not be staged, overwritten, or claimed by this merge. |
| Hard constraints | No new worktree, reset, stash, hook bypass, fallback/dual source, process restart, or deletion of generated packages. Merge from the named `myhexin` branch and push only `v0.0.15beta` to `myhexin`. Commit subjects use `dsw-33987`. |
| Sources read | Replacement `AGENTS.md`; current status and local/remote graph; all six incoming commit summaries; incoming July records for left-rail alignment, Mailbox stability, Environment/Goals density and chat scrollbar, and Settings app-dialog context; current spec indexes. |
| Whole-repository grep / diff | The incoming branch changes 47 Overlay/UI, browser-test, architecture, and July-record/index paths. The in-progress parallel work changes expert-squad/runtime/SDK/docs paths. Exact set intersection is only `specs/README.md` and `specs/records/2026-07/README.md`; production and test code have zero overlap. Both indexes will be merged additively. |
| Independent-agent feedback | None. The active execution policy forbids unsolicited sub-agent delegation. |

## Plan

1. Preserve the already active parallel Mission/squad-stage work as an independently committed checkpoint before merging.
2. Fetch both remote branches again and converge local `v0.0.15beta` with any newer delivery-branch commit.
3. Merge `myhexin/work-v0.0.15beta-yr-0722` with a traceable merge commit; resolve the two additive spec-index conflicts while retaining every record from both sides.
4. Run focused Overlay source tests, Overlay typecheck/build/i18n, Node-launched browser verification for the affected surfaces, required documentation health, version check, and exact diff review.
5. Record verification, commit/push the record and merged delivery branch, then confirm local/remote equality while retaining the separately owned unstaged parallel edits.

## Verification ledger

- Source integration: merged all six commits through `47f500e4e` with `--no-ff`. Conflicts were limited to `task-progress-collapse.test.ts` plus the two additive spec indexes. The task-progress resolution keeps the current single-argument `locateGoalExecutionRecord(goalID)` contract while retaining the incoming assertions that duplicate counts, segmented bar, and `hotGoalID` state stay removed. Both indexes retain records from both histories.
- Parallel preservation: checkpoint `a3953f902` was pushed before merge. The four later unstaged files listed in Recall remain excluded from the merge index.
- Focused Overlay source tests: 121 passed, 0 failed across 16 affected test files.
- Browser verification: the seven affected Node-launched browser files passed 26/26. Two initial evidence failures were repaired in the browser test itself: left-rail alignment now compares action-button centers instead of an optically corrected SVG center, and the Kobalte workspace menu waits for menu autofocus before sending `End`. The complete group then passed with no failures.
- Visual review: inspected current browser screenshots for conversation bottom-pinning, visible/resting scrollbar states, expert-squad details, catalog failure recovery, uninstall confirmation, and package configuration. The bottom control stays above the composer and disappears after pinning; settings hierarchy remains compact; dialogs preserve visible context with clear focus and primary actions. No visual blocker found.
- Overlay/repository checks: Overlay focused typecheck passed; Vite build passed with 2,648 modules; i18n source check passed; `version:check` reported `0.0.15-beta`; root `bun run typecheck` passed all 9 package tasks.
- Documentation checks: historical links, document health, and product docs single-source suites passed 87/87.
- Delivery: merge commit `27ccf520b` passed the complete pre-push hook and was pushed to `myhexin/v0.0.15beta`; the hook repeated root typecheck, route inventory, generated API docs, Overlay i18n, and secret scanning successfully. Post-push fetch reports local/remote delta `0 0`, both refs at `27ccf520b`, and confirms `47f500e4e` is an ancestor of the delivery branch.
