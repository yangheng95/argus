# v0.0.21beta Work-branch Convergence

Status: in progress

## Recall

### User request

- Merge the complete remote `work-v0.0.19beta-yr-0727` history.
- Publish the converged result as `v0.0.21beta`.
- Preserve every existing committed, tracked, untracked, and parallel change.
- Resolve overlapping implementations in favor of the user's current
  `v0.0.20beta` side.
- Change Environment so hover presents it transiently, leaving hover dismisses
  it, clicking pins it, and clicking again unpins it.

### Acceptance criteria

1. `v0.0.21beta` contains both the current `v0.0.20beta` history and
   `myhexin/work-v0.0.19beta-yr-0727`.
2. The pre-existing dirty Overlay and specification changes are committed
   without deletion or broad restoration.
3. Release-family metadata is aligned at canonical `0.0.21-beta`, including
   the Windows MSI projection.
4. Version, focused affected tests, typechecks, API route checks, documentation
   checks, and normal Git hooks pass.
5. The pushed `myhexin/v0.0.21beta` and local branch compare as `0 0`.
6. Environment uses one controlled mature primitive: trigger/content hover
   keeps the transient surface visible, leaving both dismisses it, click pins
   across pointer leave, and a second click unpins and closes it.

### Hard constraints

- Do not stash, reset, restore, delete, or create a temporary worktree.
- Resolve merge conflicts from the common ancestor and preserve the intent of
  both sides; do not choose an entire side wholesale.
- Stage exact task-owned paths for release metadata, and retain the original
  change grouping through separate preservation, merge, and version commits.
- Commit subjects use the required `dsw-33987` prefix.
- Push only to the git-cc `myhexin` remote through normal hooks.
- Do not restart, refresh, close, or otherwise interfere with a running
  OpenCorvus or Overlay process.

### Sources read

- `AGENTS.md`
- `script/sync-version.ts`
- `docs/packaging.md`
- The repository beta-release workflow in Codex memory.
- Current and remote Git histories, branch references, working-tree status,
  and canonical version output.

### Whole-repository and history inventory

- `git status --short --branch` recorded the complete protected dirty-file
  inventory before branch creation.
- `git log HEAD..myhexin/work-v0.0.19beta-yr-0727` found 30 source-only
  commits.
- `git log myhexin/work-v0.0.19beta-yr-0727..HEAD` found 20 current-only
  commits.
- `git diff --name-status $(git merge-base HEAD
  myhexin/work-v0.0.19beta-yr-0727)..myhexin/work-v0.0.19beta-yr-0727`
  enumerated every source-branch path. The source overlaps the protected
  Overlay, tests, `specs/README.md`, `specs/current/architecture/07-panel.md`,
  and July record index, so a merge before preserving the dirty work would be
  unsafe.
- `git ls-remote --heads myhexin` confirmed that `v0.0.21beta` did not exist
  before this delivery.
- `bun run version:check` confirmed the starting release family was
  `0.0.20-beta`.

### Independent agent feedback

- None. The user did not request delegated or parallel agents; the primary
  agent owns the merge, verification, second review, and delivery.

## Execution plan

1. Create `v0.0.21beta` from the current verified `v0.0.20beta` HEAD while
   retaining the dirty worktree.
2. Review and validate the protected dirty changes, then commit them as a
   preservation commit on the new branch.
3. Merge `myhexin/work-v0.0.19beta-yr-0727`; resolve each conflict against the
   common ancestor and run tests covering the merged surfaces.
4. Run the canonical version synchronizer for `v0.0.21beta`, verify exact
   release-owned paths, and commit the version family.
5. Run repository validation and a second diff/history review.
6. Fetch once more, integrate any new remote source update if needed, push
   `v0.0.21beta` through normal hooks, and verify local/remote equality.

## Validation

Pending.
