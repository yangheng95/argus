# v0.0.8beta Remote Merge and Matrix Build

## Recall

### User request

- Merge the remote branch from `https://git-cc.myhexin.com:6443/yangheng/opencorvus` and run the package matrix.

### Acceptance criteria

1. Fetch git-cc and integrate the latest active `v0.0.8beta` work branch into the current `v0.0.8beta` delivery branch.
2. Preserve all pre-existing uncommitted Frontend replica End-to-End (E2E) work and generated package artifacts.
3. Run the established production package matrix in the required order: Graphical User Interface (GUI) installer first, portable Command-Line Interface (CLI) binary second.
4. Verify the native macOS ARM64 artifacts, runtime/application versions, executable architectures, signatures, Apple Disk Image (DMG), archives, release contracts, and hashes. Report non-host matrix rows as skipped.
5. Run focused tests plus repository documentation and push-hook checks, commit only intended source/spec changes, and push the final `v0.0.8beta` branch to the `myhexin` git-cc remote.

### Hard constraints

- Do not reset the worktree, create another worktree, or restart/interrupt a running OpenCorvus or Overlay process.
- Do not overwrite, stage, or commit the four pre-existing Frontend replica E2E files or generated package artifacts.
- Use `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` as the single production packaging entrypoints, in that order.
- Generated installers, binaries, archives, and Tauri schemas remain untracked delivery artifacts.
- Commit subjects use the required `dsw-33987` prefix; push only to `myhexin/v0.0.8beta` without bypassing hooks.

### Sources read

- Repository `AGENTS.md` instructions supplied in the task context.
- `script/package-gui-installer-matrix.ts` and `script/package-binary-matrix.ts`.
- `specs/records/2026-07/2026-07-17-v0.0.8beta-latest-matrix-build.md`.
- `specs/records/2026-07/2026-07-17-v0.0.8beta-100bb-matrix-repackage.md`.
- Current branch graph, remote branch graph, worktree status, and remote branch path diff after `git fetch --prune myhexin`.

### Whole-repository search evidence

| Surface / call site | Disposition |
| --- | --- |
| `myhexin/work-v0.0.8beta-yr-0717` | Merge into `v0.0.8beta`; it is the only newly advanced work line for the current release family and already contains the latest `work-v0.0.7beta-yr-0716` history. |
| Root `package:gui-installer-matrix` | Retain as the five-platform GUI matrix owner; it builds and validates only the matching native host row. |
| `packageGuiInstallerMatrix` / `GUI_INSTALLER_MATRIX` | Retain as the SDK, Overlay, Tauri, staging, and release-contract owner for GUI artifacts. |
| Root `package:binary-matrix` | Retain as the explicit five-platform portable CLI matrix owner. |
| `packageBinaryMatrix` / native binary owner | Retain as the native CLI compilation, runtime probe, staging, and archive owner. |
| `.github/workflows/build.yml` | No edit; it invokes the GUI matrix per platform and intentionally excludes the portable CLI from release assets. |
| `docs/packaging.md` and `RELEASE.md` | No edit; their documented entrypoints match the production commands used here. |
| Focused matrix tests | Run GUI matrix, binary matrix, and release-overlay contract tests after integration. |

### Independent agent feedback

- No sub-agent was requested, so none was started.

## Starting evidence

- Current branch and `myhexin/v0.0.8beta` both resolve to `1e4aa733f` after fetch.
- `myhexin/work-v0.0.8beta-yr-0717` resolves to `7869ad87d`; divergence from the delivery branch is 19 delivery-only commits and 7 work-only commits.
- The work branch changes 25 Overlay/test/spec paths and does not change the four pre-existing uncommitted Frontend replica E2E paths.
- Existing `packages/overlay/dist-artifacts/darwin-arm64/` output is untracked and will be regenerated only by the production matrix owner.

## Execution plan

1. Commit and push this pre-integration record without staging user-owned E2E files or generated artifacts.
2. Merge `myhexin/work-v0.0.8beta-yr-0717` into `v0.0.8beta`, resolve conflicts from source evidence, and run the merged branch's focused UI/test suites.
3. Run version validation, then the GUI matrix and CLI matrix in order.
4. Verify artifact identity, architecture, version, signatures, DMG integrity, archive contents, isolated packaged-runtime health, sizes, and SHA-256 hashes.
5. Record exact evidence, run spec/document checks and push-hook checks, conduct a second diff review, commit, fetch/recheck divergence, and push `v0.0.8beta` to git-cc.

## Verification ledger

### Remote integration

- `git fetch --prune myhexin` resolved `myhexin/work-v0.0.8beta-yr-0717` at `7869ad87d`. Its seven work-only commits already contained the latest `work-v0.0.7beta-yr-0716` integration, so no second merge source was needed.
- The pre-integration plan was committed and pushed as `569615aa6`. The work branch then merged into `v0.0.8beta` as `10f1814ce` and was pushed through the full git-cc hooks.
- The only textual merge conflict was the July record index. Both sides' entries were retained. The product diff merged without a conflict.
- The merge exposed two stale test assumptions rather than product regressions: the architecture assertion still required project-only Agent Models copy after the delivery branch introduced global/project copy, and the new close-dialog browser fixture clicked Windows/Linux-only `#btnClose` on a native macOS build. The first now asserts the canonical scope-dependent copy expression. The second triggers the shared `onCloseRequested` lifecycle, proving the native close event is prevented and exercising the same `confirmOverlayWindowClose` owner on every platform.
- A final fetch after packaging found one later remote commit, `e628b4a4b`, which changes only this release line's sparse-Chat acceptance record. It was merged as `ef0cde11f`. Because it changes no product source, test source, manifest, generated payload, version, or packaging input, the already completed binary matrix remains the exact package for the latest executable tree; regenerating identical binaries was not required.
- Focused merged Overlay source tests passed 213/213 with 9,023 expectations. Native-window ownership tests passed 8/8 with 62 expectations. Overlay TypeScript and panel localization revision `8086d1b8b04d6219` passed.
- Node-launched real browser verification passed all seven selected paths across the close confirmation, sparse populated Chat, long Conversation/Agent Rail, and Expert Squad fixtures. Current screenshots were opened at original resolution. The confirmation contains only title, message, Cancel, and Quit; both Chat fixtures keep the one composer at the panel bottom with final-content clearance; the Expert Squad catalog/team glyphs remain distinct and unclipped.
- Historical links, document health, and product-document single-source verification passed 81/81 with 1,282 expectations. Both pushes passed SDK import, AI runtime, full repository typecheck, route, docs, Overlay i18n, and secret-scan hooks.

### Package matrix

- `bun run version:check` passed with the canonical release-family version `0.0.8-beta`.
- `bun run package:gui-installer-matrix` completed first. It built and validated the real macOS ARM64 GUI executable, `OpenCorvus_0.0.8-beta_aarch64.dmg`, and `OpenCorvus_0.0.8-beta_aarch64.app.tar.gz`; Linux x64/ARM64, macOS x64, and Windows x64 were explicitly skipped. Tauri emitted only the two existing macOS-only taskbar badge dead-code warnings.
- `bun run package:binary-matrix` completed second. It built and executed the real macOS ARM64 portable CLI and emitted `opencorvus-darwin-arm64.tar.gz`; all non-host rows were explicitly skipped.
- The GUI executable, application executable, and CLI runtime are Mach-O 64-bit ARM64. CLI `--version`, `CFBundleShortVersionString`, and `CFBundleVersion` all report `0.0.8-beta`.
- Strict code-sign verification passed for the application bundle and staged GUI executable. `hdiutil verify` reported the DMG checksum valid. The application archive contains the signed `OpenCorvus.app`; the CLI archive contains the runtime, UI, Browser Model Context Protocol (MCP) sidecar, Browser Node runtime, colocated modules, package metadata, and `bin` directory.
- Finder-like packaged-runtime verification used the compiled CLI with `PATH=/usr/bin:/bin`, isolated `HOME` and `OPENCORVUS_HOME`, and port `47885`. `/global/health` returned `healthy: true`, version `0.0.8-beta`, and paths entirely under `/tmp/opencorvus-matrix-health.JDMz0Q`. SIGINT shutdown reported zero live tasks, runs, goal runs, ownerships, sessions, tool parts, or corrupt tasks, then released the port.
- Matrix and release-contract tests passed 36/36 with 103 expectations across the GUI matrix, binary matrix, native binary owner, and release workflow contract.

### Artifact identity

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| GUI executable `opencorvus-overlay` | 155 MiB | `35d48c4774430bd17a8cf980403bd8a6be64c2dfcd48a6f62498f43415f8794c` |
| `OpenCorvus_0.0.8-beta_aarch64.dmg` | 146 MiB | `94f362e1afee4d2c0e8604bb22159eb2d07d67861ed8ad725e344a5382a877ea` |
| `OpenCorvus_0.0.8-beta_aarch64.app.tar.gz` | 161 MiB | `b2346d640034baf00b6e18786acb80ba0caaf6e5b5b8275741b4a4d00a6cd114` |
| CLI runtime `opencorvus` | 96 MiB | `a2a5adcd0030920aa394f41d181d6fe02a536bd84bda95a16baac7a3d0ec7551` |
| `opencorvus-darwin-arm64.tar.gz` | 112 MiB | `804a71dabedfa996d1f1a517331701c4a80df7ad55dd390a3b2695e4185de97b` |

### Preservation and final state

- The pre-existing Frontend replica E2E files retained the same SHA-256 values before and after merge/package: `6fae1c9…4307`, `a3ca379…0434`, `a52df19…5f10`, and `6755693…ad5`. They remain unstaged and outside this delivery.
- The production matrix replaced only its owned generated artifact directories. The transient untracked Tauri macOS schema was removed after packaging; native GUI artifacts remain under `packages/overlay/dist-artifacts/darwin-arm64/`, while CLI artifacts remain under `packages/opencorvus/dist/`.
- The post-build fetch first showed `v0.0.8beta` and `myhexin/v0.0.8beta` at divergence `0 0`. After the documentation-only work branch advance, merge `ef0cde11f` was pushed through the full hooks; the final evidence commit performs one more fetch/ancestry review before delivery.

## Follow-up integration at `ea34c5835`

- A later fetch resolved `myhexin/work-v0.0.8beta-yr-0717` at `ac6770da4`, adding 11 work-line commits after the previous integration. The delivery branch and work line had diverged by 30 and 11 commits respectively before merge.
- Merge `ea34c5835` retains the delivery branch's global/project expert-squad install and uninstall ownership while adopting the work line's canonical `ui/Icon` primitive. The newer work-line Tools/Reasoning continuous text-flow implementation and its matching browser/source tests replace the earlier plain-activity styling. Both sides' July record-index entries remain present.
- Overlay TypeScript passed. Conflict-focused source tests passed 48/48 with 613 expectations. Six Node-launched browser files exercised 11 real rendered scenarios covering Tools/Reasoning chronology, Work Ledger, environment details, task-directory actions, and Review Dock. Screenshots for the expanded transcript, Work Ledger, and Review Dock were opened at original resolution and showed no overlap, clipping, or density regression.
- `bun run version:check` confirmed `0.0.8-beta`. The GUI installer matrix completed first and the portable CLI matrix completed second. Both packaged the native macOS ARM64 row and explicitly skipped Linux x64/ARM64, macOS x64, and Windows x64 because matching native runners were unavailable.
- The staged GUI executable, application executable, and CLI runtime are Mach-O 64-bit ARM64. CLI `--version`, `CFBundleShortVersionString`, and `CFBundleVersion` report `0.0.8-beta`. Strict code-sign verification passed for the application and staged GUI executable, and `hdiutil verify` reported a valid DMG checksum.
- Matrix and release-contract tests passed 36/36 with 103 expectations. Historical-link and document-health tests passed 77/77 with 1,257 expectations; product-document single-source tests passed 4/4 with 25 expectations. The first documentation command named the retired `product-document-single-source.test.ts` path and therefore ran only the two existing suites; the real `product-docs-single-source.test.ts` suite was then run explicitly and passed.
- A post-package fetch found no new work-line commits: `HEAD...myhexin/work-v0.0.8beta-yr-0717` was `31 0`. The delivery branch was 12 commits ahead of `myhexin/v0.0.8beta` before the final evidence commit and push.

### Follow-up artifact identity

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| GUI executable `opencorvus-overlay` | 155 MiB | `e58a225c1269a8382dbb8aa21c967921e2605c5c46a5cca8c2927c5c0daa7663` |
| `OpenCorvus_0.0.8-beta_aarch64.dmg` | 146 MiB | `0a4021890705e271c4c51a72d8430b253e5ac2c4a8e97b61c6493ef0b0eff11c` |
| `OpenCorvus_0.0.8-beta_aarch64.app.tar.gz` | 145 MiB | `404197ed5a80297e3e258e11320cd8759bcb1352047b6cf9f11c1a4c88fa0144` |
| CLI runtime `opencorvus` | 96 MiB | `a2a5adcd0030920aa394f41d181d6fe02a536bd84bda95a16baac7a3d0ec7551` |
| `opencorvus-darwin-arm64.tar.gz` | 111 MiB | `bdef1c18df2cd07829aaac9585b6ccae76bf1cd25b2208efc78b767f059b0e1c` |
