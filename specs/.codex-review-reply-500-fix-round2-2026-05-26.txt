独立审查任务 — round 2。

# 背景

第一轮 codex review（specs/.codex-review-reply-500-fix-2026-05-26.txt）发现 4 个
问题（2 major + 2 minor）。我已经在 commit f2c52dd19 修复全部 4 条：

  major #1: 恢复 target.prompt.agent === "build" 拒绝（packages/opencorvus/src/task-api/index.ts ~279）
  major #2: 给 MissingModelConfigError 加 onError 映射到 400（packages/opencorvus/src/server/server.ts ~98）
  minor #3: 给 reply route 的 400 用 NamedError union schema（packages/opencorvus/src/server/error.ts + routes/orchestrator.ts）
  minor #4: build session test 重命名 + 新增 hybrid envelope.agent==="build" 的真实 BuildSessionDirectReplyError 测试（packages/opencorvus/test/server/reply-error-taxonomy.test.ts）

# 你的任务

只审查 f2c52dd19 这一个 commit 的修复。重点：

1. **major #1 的恢复**
   - 我同时改了 message 内容（区分 session.kind === "build" 和 hybrid 两种）。
     文案是否准确传达了原因？错误 data 字段是否足够让 overlay 渲染对应的 UX？
   - 我没改 overlay 端的错误展示（AgentSessionReplyBox 已经按 name 走分支，
     BuildSessionDirectReplyError 走 kind_not_allowed 文案）。在 hybrid 场景下
     "kind_not_allowed" 这个文案是否合适？用户可能困惑因为 session.kind 实际
     是 architect 而不是 build。

2. **major #2 的映射**
   - MissingModelConfigError → 400 是合理 status 吗？还是 412
     "Precondition Failed" 更准确？看 onError 现有的 WorktreeNotGitError
     是 412 的先例。
   - reply route 之外其他路径也会抛 MissingModelConfigError，我加在全局
     onError 会改变其他路径的 status（之前 500，现在 400）。grep 全仓
     `MissingModelConfigError` 看哪些路径受影响，是否有意外。

3. **minor #3 的 OpenAPI schema**
   - packages/opencorvus/src/server/error.ts 的 `replyRouteErrors` helper
     是不是过度抽象？只有 reply route 一个调用方。直接 inline 是不是更
     符合 rule 5/6？
   - namedErrorUnionSchema 用了 z.union(...names.map(...)) 的 zod union
     实际生成的 OpenAPI schema 是 oneOf/anyOf 吗？还是 union 散开？这会
     影响 SDK 生成的 TypeScript 类型。

4. **minor #4 的测试**
   - reply-error-taxonomy.test.ts 新增的 "non-build session with envelope.agent
     === 'build' → 400 BuildSessionDirectReplyError" 测试覆盖足够吗？
     - 它建了一个 architect session 然后 envelope.agent: "build"。
     - resolveAgentModelRef 之前 contract validator 之前抛 error，所以不
       需要 model overlay。但这是不是依赖了我的执行顺序而非显式契约？
   - "worker kind with contract but no model config → 400 MissingModelConfigError"
     测试 — 这个测试依赖 contract validator 顺序在 model resolve 之前。如果
     有人将来调换顺序，这个测试就被秘密短路。要不要在测试里加注释说明
     依赖了 task-api/index.ts 的特定顺序？

5. **总体回归**
   - 我没动 orchestrator/tools.ts 的 steer_subagent — 它也调
     EngineService.replyAgentSession。我的所有错误改成 NamedError 后，
     steer_subagent 接收 throw 会怎样传给 LLM？看下 steer_subagent 的
     try/catch（packages/opencorvus/src/orchestrator/tools.ts 大约 4488）。
   - 我没加 hybrid envelope.agent === "build" 的 cancel 测试。cancel
     路径用 assertDirectAgentSession 走的是另一个 kind 集合
     (DIRECT_AGENT_SESSION_CONTROL_KINDS)，所以理论上不受影响 — 但你
     看一下确认。

# 输出

按 critical → minor 列出新发现的问题。如果第一轮 4 个问题确认已修，
也明说"round 1 issues confirmed fixed"。报告 ≤700 字。
