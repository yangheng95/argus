# Agent: goal (attempt 1)
- Time: 2026-04-02T02:17:25.661Z
- Sequence: 2
- mode: initial
- model: kimi-k2.5
- toolCalls: 8
- finishReason: stop

## System Prompt

````
You are a senior software architect acting as the goal decomposition brain for OpenCorvus, an autonomous coding orchestrator. You receive a structured specification with requirements and produce executable implementation goals for downstream executor agents.

CRITICAL: You MUST explore the codebase BEFORE producing goals. Goals without real file paths from actual exploration will be rejected.

## Your Process

### Step 0: RECALL + EXPLORE

1. Search project memory for prior decompositions, patterns, lessons learned.
2. List active project conventions and constraints. Preferences are BINDING.
3. **Greenfield detection**: If the spec states the project is greenfield or nearly empty (only root config files exist), minimize exploration — the spec already explored the project structure. Focus on mapping requirements to goals using the spec's findings. Use tools only to verify 1-2 key files (e.g., package.json) still exist.
4. Explore the codebase thoroughly (skip or minimize for greenfield projects):
   - List project root and key subdirectories — understand module layout
   - Read package.json / build config — tech stack, scripts
   - Search for code related to each requirement cluster
   - Read key interfaces, types, APIs that goals will touch
   - Identify shared/boundary files (config, types, entry points)
   - Map dependency relationships between modules
   - Identify test patterns and locations
5. Do NOT use web_search. All information must come from codebase exploration.

### Step 1: MAP REQUIREMENTS → EXPLICIT GOALS

Go through EVERY requirement in the spec. For each one:
- Assign it to a goal (group related requirements that touch the same module)
- Set owned_paths from REAL file paths discovered in Step 0
- Define done_definition with concrete, verifiable criteria
- Set dependencies based on actual code relationships

**Every requirement MUST appear in at least one goal. No exceptions.**

### Step 2: FILL IN IMPLICIT GOALS

Now look at the explicit goals you just created. For each one, trace what it needs to work:
- Does it import types that don't exist yet? → Add a shared types goal
- Does the project need scaffolding first? → Add a bootstrap goal
- Does it need database tables? → Add a migration goal
- Do tests need infrastructure? → Add a test setup goal
- Does the module need to be wired into the app? → Add an integration goal

Implicit goals have NO requirement_ids (leave empty, set `source: implicit`).
They MUST appear earlier in the dependency chain than the explicit goals that need them.

## Output Format

Output using section tags:

<summary>One-line summary</summary>
<goals>
- id: goal_id
  title: Short title
  objective: What this goal accomplishes
  requirement_ids: REQ-1, REQ-2
  source: explicit
  depends_on_goal_ids:
  owned_paths: src/foo.ts, src/bar.ts
  done_definition: Concrete pass/fail criteria
  qa_rule_selectors: build, test
  priority: blocking
  kind: feature

- id: goal_infra
  title: Infrastructure goal
  objective: What it provides
  requirement_ids:
  source: implicit
  depends_on_goal_ids:
  owned_paths: src/types/shared.ts
  done_definition: Types compile and are importable
  qa_rule_selectors: build
  priority: blocking
  kind: bootstrap
</goals>

**Goal kinds:** bootstrap, feature, verification, integration, system
**Priority:** blocking or advisory
**qa_rule_selectors:** build, test, lint, verify_cmd, startup, ui_review, code_quality, code_review, dead_code_review, spec_check

## Parallel Execution & Isolation

Goals are executed **in parallel, each in an isolated git worktree**. Maximize parallelism:

- **Parallel-first design**: The default state is that goals have NO dependencies and run simultaneously. Only add `depends_on_goal_ids` when there is a genuine data dependency (one goal MUST read output produced by another). Do NOT add dependencies just because goals touch the same domain — if their interfaces are well-defined, they can run in parallel.
- **File ownership must be disjoint**: Two concurrent goals MUST NOT write to the same file. If two goals both need to modify `src/index.ts`, one must depend on the other OR a final integration goal should wire them together.
- **Shared code goes into a bootstrap goal**: Types, interfaces, config schemas, database migrations, and any code needed by multiple goals MUST be in an early bootstrap/infrastructure goal that others depend on. This goal runs first and its output is available to all downstream goals. Keep bootstrap goals minimal — only truly shared definitions.
- **Dependencies encode data flow, not domain proximity**: If Goal B reads files produced by Goal A, Goal B MUST list Goal A in `depends_on_goal_ids`. But if Goal A and Goal B work on independent modules that will be integrated later, they SHOULD NOT depend on each other — use an integration goal instead.
- **Minimize cross-goal coupling**: Design goals so each one produces a self-contained module. Avoid designs where many goals modify a single shared entry point file — instead, use a final integration goal that wires modules together.
- **Owned paths = exclusive write access**: During execution, each goal can ONLY write to its `owned_paths`. If you assign `src/db.ts` to Goal A, no other concurrent goal may modify it.
- **Anti-pattern: serial chain**: If your goals form a strict linear chain (A→B→C→D→E), your decomposition has failed. A well-decomposed plan should have most feature goals at the same dependency depth, branching from a shared bootstrap.

## Rules

- ALWAYS explore codebase before producing goals.
- Every owned_path MUST come from actual tool exploration — never guess paths.
- Every spec requirement MUST be covered by an explicit goal.
- Implicit goals MUST exist for infrastructure that explicit goals depend on.
- Owned paths of concurrent goals (no dependency between them) MUST NOT overlap.
- **Maximum 10 goals total** (explicit + implicit). If you have more, merge related goals. Fewer, larger goals with clear boundaries are better than many small, interdependent ones.
- Write in the same language as the request.

Output text directly. Do NOT output JSON. Do NOT wrap in code blocks.

## Quality Self-Check (MANDATORY)

Before outputting, verify each of these. If ANY answer is NO, use more tools:

1. Does every goal have owned_paths discovered via tools (not guessed)?
2. Is every spec requirement covered by at least one goal?
3. Are dependencies correct (types/shared code before consumers)?
4. Does each goal have a concrete, verifiable done_definition?
5. Are goals independently executable (respecting declared dependencies)?
6. Could a planner create implementation steps from these goals WITHOUT further exploration?
7. **Parallelism check**: Can at least 50% of feature goals run in parallel (depth ≤ 2 from root)? If most goals form a linear chain, redesign the decomposition.

````

## Messages (1)

### Message 1 [user]

# Task

Title: prd-stock-trading-sim

Request:
模拟股票交易 Web 应用 PRD — 交付一个商业级可运行的 MVP 全栈应用。

## 技术栈（固定）

### 后端
- **运行时**: Bun
- **语言**: TypeScript (strict mode)
- **框架**: Hono
- **数据库**: SQLite via `bun:sqlite`，数据文件 `data/trading.db`
- **实时推送**: Server-Sent Events (SSE) — 行情推送
- **密码哈希**: `Bun.password.hash()` / `Bun.password.verify()`
- **入口**: `index.ts`，同时服务 API 和前端静态资源

### 前端
- **框架**: React 19 + TypeScript
- **脚手架**: Vite (react-ts 模板结构)
- **路由**: React Router v7
- **样式**: Tailwind CSS v4
- **图表**: Recharts（K线简化版用柱状图 + 折线图）
- **HTTP**: 原生 fetch + SSE EventSource
- **前端目录**: `web/`，构建产物 `web/dist/`

### 项目结构
```
index.ts                 # Hono 入口，挂载 API + SSE + 静态资源
src/
  db.ts                  # SQLite DDL + 初始化 + 种子数据
  auth.ts                # 认证中间件 + 路由
  market.ts              # 行情路由 + SSE 推送
  trading.ts             # 交易路由（买入/卖出/撤单）
  portfolio.ts           # 持仓 + 资产路由
  history.ts             # 交易历史 + 收益统计
  simulator.ts           # 行情模拟引擎（随机波动 + 趋势）
web/
  index.html
  src/
    main.tsx
    App.tsx
    api.ts               # API client + SSE 订阅
    components/
    pages/
    hooks/
    styles/
  tailwind.config.ts
  vite.config.ts
  tsconfig.json
data/                    # SQLite（gitignore）
tests/
  api.test.ts            # 后端 API 测试
```

### 启动方式
```bash
cd web && bun install && bun run build && cd ..
bun run index.ts
# 浏览器打开 http://localhost:3000
```

## 交付范围（MVP P0 功能）

### 后端 API

1. **认证**
   - `POST /api/auth/register` — 用户名+密码注册，初始资金 1,000,000
   - `POST /api/auth/login` — 登录返回 Bearer token

2. **行情**
   - `GET /api/market/stocks` — 股票列表（代码、名称、最新价、涨跌幅、成交量）
   - `GET /api/market/stocks/:code` — 单只股票详情 + 最近 60 个价格点
   - `GET /api/market/sse` — SSE 实时行情推送（每 2 秒一次全量快照）

3. **交易**
   - `POST /api/trading/buy` — 限价买入（股票代码、数量、价格）
   - `POST /api/trading/sell` — 限价卖出
   - `POST /api/trading/cancel/:id` — 撤销挂单
   - `GET /api/trading/orders` — 当前委托单列表（pending/filled/cancelled）

4. **持仓**
   - `GET /api/portfolio` — 当前持仓（股票、数量、成本价、市值、盈亏）
   - `GET /api/portfolio/summary` — 总资产、可用资金、总市值、总盈亏、收益率

5. **历史**
   - `GET /api/history/trades` — 成交记录（支持 ?stock=&limit=&offset=）
   - `GET /api/history/daily` — 每日资产曲线（最近 30 天）

### 行情模拟引擎

- 预置 10 只模拟股票（代码 SH600001-SH600010，中文名称）
- 每 2 秒生成一次新价格：
  - 基于上一个价格 ± 随机波动（正态分布，σ=0.5%）
  - 叠加趋势因子（每只股票有隐含趋势 -0.1% ~ +0.1%）
  - 涨跌幅限制 ±10%（基于当日开盘价）
- 每次价格变动自动撮合挂单：
  - 买单：市价 ≤ 买入价 → 成交
  - 卖单：市价 ≥ 卖出价 → 成交
- 服务启动时生成当日历史价格（60 个点，模拟开盘至今）

### 前端页面

React SPA，5 个主要视图：

1. **登录/注册页** (`/login`, `/register`)
   - 用户名+密码表单
   - 注册成功显示初始资金
   - 登录成功跳转首页

2. **行情大盘** (`/`) — 首页
   - 10 只股票实时报价表格（SSE 驱动，实时刷新）
   - 每行：代码、名称、最新价、涨跌额、涨跌幅%、成交量
   - 涨红跌绿配色（中国股市惯例）
   - 点击某只股票进入详情

3. **股票详情 + 交易** (`/stock/:code`)
   - 价格走势图（Recharts AreaChart，60 个点，实时追加）
   - 当前价格、涨跌幅、最高/最低/开盘价
   - 买入/卖出面板：
     - 输入数量（100 的整数倍）和价格
     - 可用资金/可卖数量提示
     - 买入/卖出按钮
   - 该股票的当前挂单列表（可撤单）

4. **我的持仓** (`/portfolio`)
   - 资产概览：总资产、可用资金、总市值、总盈亏（金额+百分比）
   - 持仓列表：股票、数量、成本价、现价、市值、盈亏额、盈亏率
   - 盈亏颜色：盈利红色、亏损绿色
   - 每日资产曲线图（Recharts LineChart，30 天）

5. **交易记录** (`/history`)
   - 成交记录表格：时间、股票、方向（买/卖）、数量、价格、金额
   - 筛选：按股票代码
   - 分页加载

### 全局 UI

- **顶部导航**: 行情 | 持仓 | 记录 | 退出
- **实时连接状态**: SSE 连接指示器（绿点=连接中）
- **主题**: 深色主题（交易软件标配）
- **响应式**: 桌面优先，平板可用
- **数字格式**: 价格保留 2 位小数，金额千分位分隔，百分比 1 位小数

## 数据表

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  balance REAL NOT NULL DEFAULT 1000000,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE sessions (
  token TEXT PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE stocks (
  code TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  open_price REAL NOT NULL,
  current_price REAL NOT NULL,
  high_price REAL NOT NULL,
  low_price REAL NOT NULL,
  volume INTEGER NOT NULL DEFAULT 0,
  trend REAL NOT NULL DEFAULT 0,
  updated_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE price_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  stock_code TEXT NOT NULL REFERENCES stocks(code),
  price REAL NOT NULL,
  volume INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE orders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  stock_code TEXT NOT NULL REFERENCES stocks(code),
  direction TEXT NOT NULL CHECK(direction IN ('buy', 'sell')),
  quantity INTEGER NOT NULL,
  price REAL NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'filled', 'cancelled')),
  filled_at INTEGER,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE positions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  stock_code TEXT NOT NULL REFERENCES stocks(code),
  quantity INTEGER NOT NULL DEFAULT 0,
  cost_basis REAL NOT NULL DEFAULT 0,
  UNIQUE(user_id, stock_code)
);

CREATE TABLE daily_snapshots (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  total_assets REAL NOT NULL,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);
```

### 种子数据（10 只股票）

| 代码 | 名称 | 开盘价 | 趋势 |
|------|------|--------|------|
| SH600001 | 浦发银行 | 8.50 | +0.02% |
| SH600002 | 齐鲁石化 | 12.30 | -0.01% |
| SH600003 | 东北高速 | 15.80 | +0.05% |
| SH600004 | 白云机场 | 22.40 | +0.03% |
| SH600005 | 武钢股份 | 5.60 | -0.03% |
| SH600006 | 东风汽车 | 9.20 | +0.01% |
| SH600007 | 中国国贸 | 18.90 | +0.04% |
| SH600008 | 首创环保 | 3.80 | -0.02% |
| SH600009 | 上海机场 | 45.60 | +0.06% |
| SH600010 | 包钢股份 | 2.10 | -0.04% |

## 安全规则

- 密码用 Argon2 哈希存储
- 未认证请求返回 401
- 用户只能操作自己的订单和持仓
- 买入校验：可用资金 ≥ 数量 × 价格
- 卖出校验：持仓数量 ≥ 卖出数量
- 交易数量必须是 100 的整数倍（1 手 = 100 股）
- 挂单价格限制：当前价 ±10%

## 测试要求

`tests/api.test.ts` 使用 `bun:test`，至少覆盖：

1. 注册返回用户信息和初始资金
2. 登录返回 token
3. 获取股票列表返回 10 只股票
4. 买入成功扣减资金
5. 买入数量不是 100 倍数被拒绝
6. 资金不足买入被拒绝
7. 卖出成功增加资金
8. 持仓不足卖出被拒绝
9. 撤单成功恢复资金/持仓
10. 用户隔离（A 不能撤 B 的单）

## 验收标准

- `cd web && bun install && bun run build` 前端构建成功
- `bunx tsc --noEmit` 后端类型检查通过
- `bun test` API 测试通过
- `bun run index.ts` 启动后，浏览器打开 http://localhost:3000 看到完整交易应用
- 行情表格每 2 秒实时刷新（SSE 驱动，非轮询）
- 可完成完整流程：注册 → 登录 → 查看行情 → 买入 → 查看持仓 → 卖出 → 查看记录
- 深色主题，涨红跌绿
- 没有 README、文档等无关产物

# Spec Requirements (15 total)

Each requirement MUST be covered by at least one goal.

- **req_1**: Backend database layer (src/db.ts) [blocking] — Create src/db.ts with SQLite DDL for all 7 tables (users, sessions, stocks, price_history, orders, positions, daily_snapshots). Implement database initialization, connection management using bun:sqlite, and seed function to insert 10 stocks with initial data. Database file path: data/trading.db
- **req_2**: Authentication module (src/auth.ts) [blocking] — Create src/auth.ts with Hono routes for POST /api/auth/register and POST /api/auth/login. Use Bun.password.hash() and Bun.password.verify() for Argon2 hashing. Implement Bearer token generation and validation middleware. Initial user balance: 1,000,000.
- **req_3**: Market data module (src/market.ts) [blocking] — Create src/market.ts with Hono routes GET /api/market/stocks (list all stocks with calculated change%), GET /api/market/stocks/:code (detail + last 60 price points), and GET /api/market/sse (SSE endpoint streaming full market snapshot every 2 seconds).
- **req_4**: Trading module (src/trading.ts) [blocking] — Create src/trading.ts with Hono routes POST /api/trading/buy, POST /api/trading/sell, POST /api/trading/cancel/:id, and GET /api/trading/orders. Implement validation: quantity multiple of 100, sufficient funds/holdings, price within ±10% of current. Orders stored as 'pending' until filled.
- **req_5**: Portfolio module (src/portfolio.ts) [blocking] — Create src/portfolio.ts with Hono routes GET /api/portfolio (current holdings with P&L calculations) and GET /api/portfolio/summary (total assets, available cash, market value, total P&L, return percentage).
- **req_6**: History module (src/history.ts) [blocking] — Create src/history.ts with Hono routes GET /api/history/trades (filled orders with pagination, optional stock filter) and GET /api/history/daily (last 30 days of daily_snapshots for asset curve).
- **req_7**: Market simulator (src/simulator.ts) [blocking] — Create src/simulator.ts with price simulation engine. Generate prices every 2 seconds using normal distribution (σ=0.5%) + trend factor. Enforce ±10% limit from open. On startup generate 60 historical points per stock. Implement order matching logic that fills orders when market price crosses order price.
- **req_8**: Main server entry (index.ts) [blocking] — Create index.ts as Hono entry point. Mount all API routes under /api/, serve SSE endpoint, serve static files from web/dist/. Start simulator on server start. Listen on port 3000.
- **req_9**: Frontend Vite configuration (web/) [blocking] — Set up web/ directory with Vite react-ts template. Create vite.config.ts, tsconfig.json, tailwind.config.ts. Install dependencies: react@19, react-dom@19, react-router@7, recharts, tailwindcss@4, vite, @vitejs/plugin-react.
- **req_10**: Frontend API client (web/src/api.ts) [blocking] — Create web/src/api.ts with fetch wrappers for all backend endpoints and SSE EventSource subscription handler for real-time market data. Include auth token management.
- **req_11**: Frontend pages and routing (web/src/pages/, web/src/App.tsx) [blocking] — Create all page components: LoginPage, RegisterPage, MarketPage, StockDetailPage, PortfolioPage, HistoryPage. Set up React Router v7 in App.tsx with routes: /login, /register, /, /stock/:code, /portfolio, /history.
- **req_12**: Frontend UI components (web/src/components/) [blocking] — Create reusable components: StockTable (real-time quotes, red/green), PriceChart (Recharts AreaChart), OrderForm (buy/sell with validation), OrderList (pending with cancel), PortfolioSummary (asset cards), AssetChart (30-day line chart), TradeHistoryTable (paginated), Navigation (top nav with SSE status).
- **req_13**: Frontend styling (web/src/styles/, Tailwind) [blocking] — Implement dark theme with Tailwind CSS v4. Chinese market color scheme: red for up/profit, green for down/loss. Number formatting utilities. Responsive layout.
- **req_14**: Backend API tests (tests/api.test.ts) [blocking] — Create tests/api.test.ts using bun:test covering all 10 test cases: register/login, stock list, buy/sell with validations, cancel order, user isolation.
- **req_15**: Root package.json with dependencies [blocking] — Create root package.json with hono dependency and scripts for build, test, start.

# Spec Summary

Stock Trading Simulator Web Application - Full-stack MVP with Hono backend, React 19 frontend, SQLite database, and SSE real-time market data

Now recall memory, check preferences, explore the codebase thoroughly, then output your goal decomposition using section tags as described in your instructions. Ensure EVERY spec requirement is covered.

## Output

This is a greenfield project with minimal existing structure. The spec already provides the complete project structure. I will now decompose the requirements into executable goals.

Based on the spec, this is a full-stack stock trading simulator with:
- Backend: Bun + Hono + SQLite (bun:sqlite)
- Frontend: React 19 + Vite + Tailwind CSS v4 + React Router v7 + Recharts
- Real-time: SSE for market data
- 15 requirements covering all components

Let me create the goal decomposition:

<summary>Decompose stock trading simulator into 8 parallel executable goals: 1) Bootstrap (shared types/config), 2) Backend core (DB + auth + market + trading + portfolio + history), 3) Market simulator engine, 4) Main server entry, 5) Frontend scaffolding, 6) Frontend API client, 7) Frontend pages & components, 8) API tests & root package.json</summary>
<goals>
- id: goal_bootstrap
  title: Project Bootstrap - Shared Types and Configuration
  objective: Create shared TypeScript types, database schemas, and project configuration files that all other goals depend on. This includes tsconfig.json updates, shared types file, and database schema definitions.
  requirement_ids: req_1, req_9, req_15
  source: implicit
  depends_on_goal_ids:
  owned_paths: src/types.ts, tsconfig.json, package.json, web/tsconfig.json, web/package.json, web/tailwind.config.ts, web/vite.config.ts, web/index.html
  done_definition: |
    1. All TypeScript configuration files are valid JSON with proper compiler options
    2. Shared types.ts defines all database entity interfaces (User, Stock, Order, Position, etc.)
    3. Root package.json has hono dependency and required scripts
    4. web/package.json has all frontend dependencies (react@19, react-router@7, recharts, tailwindcss@4, etc.)
    5. web/vite.config.ts configures React plugin and build output to dist/
    6. web/tailwind.config.ts configures dark theme and Chinese market colors (red=up, green=down)
    7. web/index.html exists with proper root div for React mounting
  qa_rule_selectors: build
  priority: blocking
  kind: bootstrap

- id: goal_backend_core
  title: Backend Core Modules - DB, Auth, Market, Trading, Portfolio, History
  objective: Implement all backend API modules including database layer, authentication, market data, trading operations, portfolio management, and history tracking. This is the core business logic of the trading simulator.
  requirement_ids: req_1, req_2, req_3, req_4, req_5, req_6
  source: explicit
  depends_on_goal_ids: goal_bootstrap
  owned_paths: src/db.ts, src/auth.ts, src/market.ts, src/trading.ts, src/portfolio.ts, src/history.ts
  done_definition: |
    1. src/db.ts: Creates all 7 tables (users, sessions, stocks, price_history, orders, positions, daily_snapshots), implements seed function for 10 stocks, exports db instance
    2. src/auth.ts: Implements POST /api/auth/register (Argon2 hash, initial balance 1,000,000), POST /api/auth/login (Bearer token), auth middleware
    3. src/market.ts: Implements GET /api/market/stocks (list with change%), GET /api/market/stocks/:code (detail + 60 price points)
    4. src/trading.ts: Implements POST /api/trading/buy, POST /api/trading/sell, POST /api/trading/cancel/:id, GET /api/trading/orders with validations (100 multiple, funds check, ±10% price limit)
    5. src/portfolio.ts: Implements GET /api/portfolio (holdings with P&L), GET /api/portfolio/summary (total assets, cash, return %)
    6. src/history.ts: Implements GET /api/history/trades (filled orders with filter/pagination), GET /api/history/daily (30-day asset curve)
    7. All modules use proper TypeScript types from src/types.ts
    8. All routes properly validate authentication (401 for unauthenticated)
    9. User isolation enforced (can only access own orders/positions)
  qa_rule_selectors: build, test
  priority: blocking
  kind: feature

- id: goal_simulator
  title: Market Simulator Engine
  objective: Implement the price simulation engine that generates realistic stock price movements and automatically matches orders. This runs as a background process updating prices every 2 seconds.
  requirement_ids: req_7
  source: explicit
  depends_on_goal_ids: goal_bootstrap
  owned_paths: src/simulator.ts
  done_definition: |
    1. Generates 60 historical price points per stock on startup (simulating market open to now)
    2. Updates prices every 2 seconds using normal distribution (σ=0.5%) + trend factor (-0.1% to +0.1%)
    3. Enforces ±10% price limit from daily open price
    4. Updates stocks table current_price, high_price, low_price, volume
    5. Inserts new records to price_history table
    6. Implements order matching: buy orders fill when market price ≤ order price, sell orders fill when market price ≥ order price
    7. On fill: updates order status to 'filled', updates user balance, creates/updates position, records trade
    8. Exports startSimulator() and stopSimulator() functions
  qa_rule_selectors: build, test
  priority: blocking
  kind: feature

- id: goal_server_entry
  title: Main Server Entry Point
  objective: Create the main Hono application entry point that wires all modules together, serves static files, and starts the simulator.
  requirement_ids: req_3, req_8
  source: explicit
  depends_on_goal_ids: goal_backend_core, goal_simulator
  owned_paths: index.ts
  done_definition: |
    1. Creates Hono app instance
    2. Mounts all API routes: /api/auth/*, /api/market/*, /api/trading/*, /api/portfolio/*, /api/history/*
    3. Implements GET /api/market/sse endpoint for Server-Sent Events (streams full market snapshot every 2 seconds)
    4. Serves static files from web/dist/ for all non-API routes
    5. Calls startSimulator() on server startup
    6. Listens on port 3000
    7. Proper error handling and CORS if needed
  qa_rule_selectors: build, startup
  priority: blocking
  kind: integration

- id: goal_frontend_scaffold
  title: Frontend Scaffolding and Styling
  objective: Set up the frontend Vite project structure, Tailwind configuration with dark theme, and base styling utilities for the trading application.
  requirement_ids: req_9, req_13
  source: explicit
  depends_on_goal_ids: goal_bootstrap
  owned_paths: web/src/main.tsx, web/src/App.tsx, web/src/styles/index.css, web/src/hooks/useAuth.ts, web/src/hooks/useMarket.ts
  done_definition: |
    1. web/src/main.tsx: React 19 root rendering with StrictMode
    2. web/src/App.tsx: Basic app shell with Router setup (routes defined but components can be stubs)
    3. web/src/styles/index.css: Tailwind directives, dark theme base styles, Chinese market color variables (--color-up: red, --color-down: green)
    4. web/src/hooks/useAuth.ts: Hook for auth state and token management
    5. web/src/hooks/useMarket.ts: Hook for SSE connection and market data
    6. Dark theme applied to entire app
    7. Number formatting utilities (price: 2 decimals, amount: thousands separator, percent: 1 decimal)
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_api
  title: Frontend API Client
  objective: Create the frontend API client with fetch wrappers for all backend endpoints and SSE subscription handler for real-time market data.
  requirement_ids: req_10
  source: explicit
  depends_on_goal_ids: goal_bootstrap
  owned_paths: web/src/api.ts
  done_definition: |
    1. Implements auth API: register(username, password), login(username, password)
    2. Implements market API: getStocks(), getStockDetail(code), subscribeMarket(callback) for SSE
    3. Implements trading API: buy(stockCode, quantity, price), sell(stockCode, quantity, price), cancelOrder(orderId), getOrders()
    4. Implements portfolio API: getPortfolio(), getPortfolioSummary()
    5. Implements history API: getTrades(stock?, limit?, offset?), getDailySnapshots()
    6. Handles Bearer token injection in request headers
    7. SSE EventSource connects to /api/market/sse, parses JSON, calls callback with market data
    8. Proper error handling and TypeScript types for all responses
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_ui
  title: Frontend Pages and Components
  objective: Implement all React pages and reusable UI components for the trading application including market view, stock details, portfolio, and history.
  requirement_ids: req_11, req_12
  source: explicit
  depends_on_goal_ids: goal_frontend_scaffold, goal_frontend_api
  owned_paths: web/src/pages/LoginPage.tsx, web/src/pages/RegisterPage.tsx, web/src/pages/MarketPage.tsx, web/src/pages/StockDetailPage.tsx, web/src/pages/PortfolioPage.tsx, web/src/pages/HistoryPage.tsx, web/src/components/StockTable.tsx, web/src/components/PriceChart.tsx, web/src/components/OrderForm.tsx, web/src/components/OrderList.tsx, web/src/components/PortfolioSummary.tsx, web/src/components/AssetChart.tsx, web/src/components/TradeHistoryTable.tsx, web/src/components/Navigation.tsx
  done_definition: |
    1. LoginPage: Username/password form, redirects on success
    2. RegisterPage: Registration form, shows initial balance on success
    3. MarketPage: StockTable with real-time quotes (SSE), red/green colors, links to detail
    4. StockDetailPage: PriceChart (Recharts AreaChart, 60 points), current stats, OrderForm, OrderList
    5. PortfolioPage: PortfolioSummary cards, position list with P&L, AssetChart (30-day line)
    6. HistoryPage: TradeHistoryTable with pagination, stock filter
    7. Navigation: Top nav with links, SSE connection status indicator
    8. OrderForm: Quantity (100 multiple), price inputs, validation, buy/sell buttons
    9. OrderList: Pending orders with cancel button
    10. All components use Tailwind dark theme, proper number formatting
    11. React Router routes: /login, /register, /, /stock/:code, /portfolio, /history
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_tests
  title: Backend API Tests and Final Integration
  objective: Create comprehensive API tests covering all business logic and verify the complete application works end-to-end.
  requirement_ids: req_14, req_15
  source: explicit
  depends_on_goal_ids: goal_server_entry
  owned_paths: tests/api.test.ts
  done_definition: |
    1. Test: Register returns user info and initial balance 1,000,000
    2. Test: Login returns valid Bearer token
    3. Test: Get stock list returns 10 stocks with proper data
    4. Test: Buy success deducts funds and creates position
    5. Test: Buy rejected when quantity not multiple of 100
    6. Test: Buy rejected when insufficient funds
    7. Test: Sell success increases funds and reduces position
    8. Test: Sell rejected when insufficient holdings
    9. Test: Cancel order restores funds/position
    10. Test: User isolation - user A cannot cancel user B's order
    11. All tests use bun:test, clean up database state between tests
    12. Root package.json has test script configured
  qa_rule_selectors: test
  priority: blocking
  kind: verification
</goals>
