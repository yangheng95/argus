# 2026-07-10 Iteration Technical-Debt Remediation

Status: implementation approved by the user; expert-squad remediation explicitly excluded; remaining remediation in progress.

## Recall

### User Request

- Deep-audit OpenCorvus technical debt for twenty rounds without changing files.
- After reviewing the evidence-backed module-level findings, handle and resolve those problems.
- Follow-up scope correction: do not handle the expert-squad/dynamic-agent-identity portion; resolve the other recorded problems only.
- Preserve unrelated dirty worktree changes and continue autonomously until the measurable acceptance matrix passes.

### Acceptance Criteria

1. Expert-squad packages, registry, resolver, dynamic identity, payload, manifests, routes, SDK projection, and their tests are not edited by this task.
2. Persisted conversation authorship is explicit at message creation. The protocol bridge does not infer a speaker from session kind, and model adaptation creates no synthetic user-authored message.
3. Provider model metadata has one required runtime source. Missing or invalid data fails explicitly; no cache/environment/snapshot/empty-record resolution chain remains.
4. Fact Check and Integrity failures remain explicit errors or terminal evidence and are never converted into schema-valid synthetic reports.
5. Production dependency analysis reports no unexplained non-expert-squad circular dependency, unused production dependency, duplicate export alias, or undeclared runtime binary.
6. Orchestrator tool implementations are separated by domain behind one composition surface; cross-domain report synthesis and lifecycle logic do not remain in the composition module.
7. Same-session Build retry preserves the canonical request, task overlays, and context packets from one prompt builder.
8. Queue execution directory has one authority and queue submission failures keep distinct typed failure reasons rather than collapsing into `ignored`.
9. Build evidence uses one canonical attachment reference field; `legacy_attachment_url` is absent from production, tests, SDK, docs, and architecture.
10. Ordinary build and validation commands do not rewrite tracked non-expert-squad source files. Model metadata and embedded Overlay bytes are explicit checked artifacts or build-directory outputs, never transient source mutations.
11. Overlay has one Solid application root and one lifecycle owner for DOM bindings; static mount islands and imperative root creation are removed without visual or interaction regression.
12. Test processes use stdout/stderr inactivity timeout. Package scripts and test-local options do not measure timeout mechanically from process start.
13. Proven stale surfaces are removed: deprecated duplicate Engine Store aliases, unused `promptDrafts`, retired theme compatibility writes, and the legacy single-CSS test shim.
14. Acceptance remains the product verification/evidence domain, not an Agent identity. Current architecture and code contain no retired independent Acceptance-agent routing or vocabulary.
15. Focused non-expert-squad regressions, Overlay typecheck, docs health, dependency analysis, build-artifact cleanliness, browser tests, and `git diff --check` pass. Full OpenCorvus typecheck/API checks are rerun and any remaining failures must be confined to the explicitly excluded expert-squad worktree changes.
16. Overlay changes are rendered in an isolated real page, inspected through screenshots, and corrected until the current desktop visual contract is preserved.
17. A final manual source review finds no fallback, compatibility branch, synthetic message/report, second source, or unexplained generated residue in the touched non-expert-squad surfaces.

### Hard Constraints

- No fallback, compatibility alias, dual source, hidden/synthetic message, workflow gate, host routing state machine, keyword classifier, or broad Git rollback.
- Do not create a worktree and do not restart, refresh, close, or kill the user's running OpenCorvus/Overlay processes.
- Windows host is the only edit source. No WSL edit or copied-file workflow is allowed.
- Preserve unrelated dirty files. Use focused patches and focused diffs.
- Do not edit `.opencorvus/expert-squads/**`, `packages/opencorvus/src/expert-squad/**`, expert-squad package manifests/payload/template, dynamic-agent identity projection, or their dedicated tests and SDK changes.
- Every behavioral change requires a regression test. Proven legacy code listed in the prior audit is authorized for deletion by the user's follow-up request to handle all recorded problems.
- Tests and subprocess checks use real stdout/stderr inactivity timeout; elapsed timeout is not acceptance evidence.
- Frontend refactoring requires an isolated real render, screenshots bound to this task, and manual visual review.
- No commit, branch, or push is performed unless the user explicitly requests it in this Codex task; repository instructions that require delivery commits cannot override the host coding-agent prohibition on unsolicited commits.

### Sources Read

- `AGENTS.md`
- `packages/opencorvus/test/AGENTS.md`
- `C:/Users/chuan/.codex/skills/benchmark-debug-template/SKILL.md`
- the user's local July route-planning draft (working context only)
- `specs/current/architecture/99-principles.md`
- `specs/current/architecture/01-agents.md`
- `specs/current/architecture/07-panel.md`
- `specs/current/architecture/09-verification-evidence.md`
- `specs/records/2026-07/2026-07-10-dynamic-expert-squad-agent-identity.md`
- `specs/records/2026-07/2026-07-10-overlay-codex-strict-parity-remediation.md`
- Provider model, message/session protocol, queue, Build, evidence, generated-artifact, expert-squad, Orchestrator, Overlay root/theme/store, acceptance, and test-runner sources and tests.

### Whole-Repository Search Evidence

The audit and remediation baseline enumerated all known consumers with these searches:

```powershell
rg -n "manifest\.team|manifest\.workflow|workflowsFromManifest|defaultWorkflow|virtual_agents|virtualAgents|dynamic_attributes|dispatch_targets|target_id|agentProjectionForBaseRole" packages .opencorvus specs
rg -n "resolvedRole|overlay_direct_reply|role: \"user\"|role: \"assistant\"|createUserMessage|toModelMessages|synthetic|Attached image\(s\)" packages/opencorvus packages/overlay
rg -n "OPENCORVUS_MODELS_PATH|models-snapshot|ModelsDev\.Data|ModelsDev\.refresh|provider/refresh" packages/opencorvus packages/overlay
rg -n "synthesizeToolErrorReport|synthetic-no-goals|FactCheckReport|IntegrityReport" packages/opencorvus
rg -n "buildRetryFeedbackPrompt|Task-Specific Build Overlays|legacy_attachment_url|taskCwd|COALESCE\(|return \"ignored\"" packages/opencorvus packages/sdk packages/web specs
rg -n "generateBuildArtifacts|generateExpertSquadPayloadModule|modelsSnapshotPath|overlay-ui-embedded.generated" packages/opencorvus
rg -n "createRoot\(|render\(|getElementById|querySelector|addEventListener|promptDrafts|readLegacyStylesCss|--ui-scale|document\.body" packages/overlay
rg -n "timeout: [0-9]|--timeout [0-9]|setTimeout\(|sleep\(" packages/*/test packages/*/package.json
rg -n "Acceptance Agent|acceptance agent|agent.*acceptance|acceptance.*agent" packages specs/current
bunx knip --config knip.config.js --no-progress --production --include files,dependencies,unlisted,binaries
bunx knip --config knip.config.js --no-progress --include exports,duplicates,dependencies
```

Call-point disposition:

| Debt surface | Current consumers | Required disposition |
| --- | --- | --- |
| Expert-squad identity/workflow | registry, resolver, workflow registry, runner, mounts, routes, SDK, package manifests | Explicitly out of scope after the user's correction; observe only and preserve all concurrent edits. |
| Message speaker | session message schema, prompt creation, runner, mirror/bridge, Overlay tree writer | Persist explicit author identity once and derive provider-model roles only in model adaptation. |
| Model catalog | provider resolution, refresh route, CLI, tests, build snapshot generator | Select one required catalog file contract; explicit refresh atomically replaces that source. |
| Synthetic reports | Orchestrator Fact Check tool and Integrity no-goal branch | Return typed terminal/error evidence; never manufacture an agent report. |
| Dependency cycles | agent/session barrel, skill mounts, global tools, task API/executor/MCP | Replace barrel imports with owner modules and invert shared contracts into leaf modules until cycle analysis is clean. |
| Orchestrator monolith | tool schemas, implementations, evidence composition, lifecycle helpers | Extract domain-owned modules and leave `createOrchestratorTools` as composition only. |
| Build retry | initial/retry prompt builders and prompt-context tests | One canonical prompt renderer parameterized by attempt evidence. |
| Queue cwd/result | queue SQL, task lookup, submission callers, tests | Require the session directory as the execution authority and expose typed submission outcomes. |
| Evidence attachment | Build evidence manifest, garbage collection, routes/tests/docs | Replace legacy URL with one canonical attachment reference. |
| Generated source | build script, payload generator, model snapshot, embedded Overlay module | Make generation explicit or write only under build output; verify ordinary build leaves tracked sources unchanged. |
| Overlay ownership | `index.html`, `main.tsx`, component roots, service singletons | One App root and scoped lifecycle resources; preserve current desktop behavior through browser evidence. |
| Test timeout | package scripts, isolated runner, fixed per-test options | Route process suites through inactivity supervision and remove elapsed test options. |
| Manifest/dead state | package manifests, duplicate exports, app store/theme, CSS guards | Remove proven unused entries and stale compatibility surfaces; configure analysis only for proven dynamic entry points. |
| Acceptance naming | verification evidence, acceptance checks, API/Overlay vocabulary | Retain verification-domain terminology and remove only retired Agent-role implications. |

### Independent Feedback

- The preceding twenty-round audit independently rechecked findings against source history, current architecture, active task records, typecheck, API generation, Knip dependency/export/cycle analysis, and focused source review.
- Findings that lacked proof were rejected: no production non-streaming LLM path, no live synthetic goal-run ID, no runtime God CSS file, and no evidence that the acceptance verification domain itself is unreachable.
- The active dynamic-identity record already contains two independent agent reviews. One proposal to retain package workflow data was explicitly rejected because it creates the second workflow/dispatch source prohibited by repository rule 15.1.

## Baseline

- `packages/opencorvus` typecheck fails with five errors in the explicitly excluded workflow binding, resolver, and Orchestrator dynamic-ID worktree changes.
- `packages/overlay` typecheck passes.
- `api:routes-check` fails because generated OpenAPI reflects the half-migrated package workflow contract while tracked SDK/OpenAPI reflects the previous contract.
- Production Knip reports 24 unexplained dependencies and two undeclared binaries (`sqlite3`, `pgrep`).
- Dependency-cycle analysis reports 104 cycles, rooted mainly in agent/session/tool barrels.
- Package test scripts use fixed 60-second and 120-second elapsed timeouts despite the repository already having an inactivity-based isolated runner.

## Benchmark Definition

### Input and Output

- Input: the current dirty `v0.0.2beta` worktree and the seventeen evidence-backed module debt findings.
- Output: one source per runtime concept, explicit failure evidence, no generated-source mutation, a single-root Overlay, and all acceptance commands passing without touching unrelated work.

### Environment

- Windows host repository: `C:/Users/chuan/myhexin-local/opecorvus`.
- Test isolation: `OPENCORVUS_HOME`, project directories, databases, and generated output use test-owned temporary directories.
- No live OpenCorvus/Overlay process intervention.

### Timeout

- Bun test-local elapsed timeout is disabled.
- Long-running test commands execute through the shared isolated runner or an equivalent process wrapper that resets only on stdout/stderr activity.
- Focused default inactivity interval is 120 seconds; build/browser checks may use a documented larger inactivity interval.

### Acceptance Commands

```powershell
bun run --cwd packages/opencorvus typecheck
bun run --cwd packages/overlay typecheck
bun run api:routes-check
bun run docs:check
bun run check:dead-code
bun test packages/opencorvus/test/script/historical-docs-links.test.ts
bun test packages/opencorvus/test/script/document-health.test.ts
bun test packages/opencorvus/test/script/product-docs-single-source.test.ts
node packages/overlay/test/browser-runner.mjs
git diff --check
```

Focused suites are added for every implementation slice and run through inactivity supervision before the broad matrix.

## Implementation Slices

1. Replace inferred/synthetic message and report authorship with explicit persisted participants and typed terminal errors.
2. Collapse provider model metadata, queue cwd, Build retry context, and evidence attachment fields to one authority each.
3. Remove non-expert-squad build-time tracked-source mutation and prove artifact cleanliness.
4. Break non-expert-squad agent/session/tool dependency cycles and split Orchestrator domain implementations from composition.
5. Replace fixed test timeouts, remove stale dependency/API/state/CSS compatibility residue, and make Knip clean outside the excluded surface.
6. Refactor Overlay to one Solid root, run real browser screenshots, and preserve current visual/interaction behavior.
7. Clarify Acceptance as a verification domain, run the non-expert-squad benchmark, classify excluded global failures, and perform manual second review.

## Progress Log

- Recall, whole-repository search, baseline typecheck, API check, Overlay typecheck, and production dependency scan completed.
- No remediation code changed before this record was written.
- The user then explicitly removed expert-squad/dynamic-agent-identity remediation from this task; all such files are preserved and excluded from acceptance ownership.
- Persisted message origin is explicit across the non-expert browser fixtures and runtime bridge. A TypeScript abstract-syntax-tree audit found zero fixture `info` objects that declare `role` and `resolvedRole` without also declaring `author`.
- Provider model metadata, Build retry context, queue working-directory ownership, evidence attachment references, explicit Fact Check/Integrity failure evidence, generated-artifact ownership, inactivity supervision, and retired compatibility surfaces were reduced to their recorded single-source contracts with focused regressions.
- Orchestrator route database ownership now goes through `task-session-lineage.ts`; `bun run api:routes-check` passes all six route-boundary rules across thirty route files.
- Overlay now mounts through one Solid `render` call in `main.tsx`. Screenshot Browser virtualization uses one complete row source plus a native estimated item size, and collapsed Build work details no longer synchronously mount an entire 120-part card before the browser can paint.
- The isolated Screenshot Browser desktop and narrow-panel renders were inspected at `packages/overlay/.scratch/screenshot-browser-panel-browser.png` and `packages/overlay/.scratch/screenshot-browser-panel-browser-narrow-panel.png`; both preserve the intended dense desktop contract.
- The final non-expert browser matrix covered ninety-two files and 136 tests: 136 passed, zero failed, and no test file changed during the run.
- Root typecheck, Overlay typecheck, `api:routes-check`, `docs:check`, the canonical workspace-scoped `check:dead-code`, historical-doc links, product-doc single-source, and `git diff --check` pass. Lockfiles contain zero `repositories.myhexin.com` URLs.
- At the time of this remediation, `document-health.test.ts` exposed two repository-index failures involving concurrent July records and a local planning draft. The later integrity recovery formally indexed the records and removed the draft-path dependency.
- A transformed `dpdm` audit originally reduced the graph to eighty-seven OpenCorvus runtime paths in two strongly connected components and one Overlay component. Explicit recovery-scheduler injection removed `sse.ts -> events.ts -> selected-task-recovery.ts -> sse.ts`; the Overlay entry point now reports zero runtime cycles.
- The smaller OpenCorvus component was removed completely. A leaf LSP server-ID catalog stopped Config from importing the full LSP/Instance runtime; Instance lifecycle capabilities now flow through an explicit context into FileWatcher and Scheduler, removing the FileWatcher/Instance and Scheduler/Instance reverse imports without a default implementation or registry.
- OpenCorvus runtime cycle paths fell from eighty-seven to eighty-two. The remaining paths are one 129-module component centered on `engine/queue -> orchestrator/loop`, `orchestrator/tools -> engine/persist`, `task-api -> engine/*`, and the session prompt/tool/scheduler chain. Acceptance criterion 5 and the cycle portion of implementation slice 4 remain incomplete; this record does not label the overall remediation complete.
- Focused cycle-remediation regressions pass: 122 Overlay event/recovery/SSE tests, eighteen FileWatcher lifecycle tests under inactivity supervision, the LSP catalog consistency test, and fifteen Scheduler ownership tests under inactivity supervision. OpenCorvus and Overlay typechecks pass after the splits.
- Engine terminal state no longer dynamically imports the full task-api facade. `TaskCompleted`, `TaskFailed`, and `TaskCancelled` protocol events are consumed by one instance-scoped EngineService subscription whose disposer is owned by `createInstanceState`.
- Child-task terminal results no longer travel through the operator-message path as `author: "user"`. The parent task receives a persisted root-session message authored by `orchestrator`, then a normal task-loop note wake; mission lineage continues through the explicit `SessionWake` reason contract. The protocol bridge now preserves an explicit non-user author on provider-role `user` root messages instead of overwriting the participant identity.
- Lineage and bridge regressions pass eleven tests covering parent wake, mission wake, root orchestrator authorship, sub-agent routing, and root assistant rejection. Removing the state-to-task-api back edge reduced OpenCorvus runtime cycle paths from eighty-two to seventy-eight; the remaining single large component is still unresolved.
- `OpencorvusExecutor.submit` and `resume` no longer label evaluator/system-dispatched worker prompts as `author: "user"`; queued prompts now retain `author: "orchestrator"` and a focused queue assertion pins that identity.
- The Executor event API was an `async function*` whose body did not subscribe until generator advancement, despite comments claiming call-time subscription. It is now a synchronous factory that registers GlobalBus immediately and returns an inner async generator. Event fixtures now persist their source message before publishing `message.part.delta`, matching the strict bridge contract instead of relying on unreachable-message deltas.
- All six Executor tests pass under inactivity supervision, including session-scoped events, permission filtering, goal child-session activity, status/abort, resume authorship, and acceptance summaries. The previous full-file 120-second inactivity hang is removed.
- Skill frontmatter validation no longer imports `AgentToolPool` or the global tool implementation loader. `tool-id-catalog.ts` owns canonical global and retired duplicate IDs, while `tool-pool-data.ts` is the single pure-data source for role assignments and reserved core IDs. Runtime tool-pool behavior and schema validation derive from those leaves; no copied allowlist was introduced. The five retired-ID failures, the non-canonical-ID failure, the dependency boundary test, and OpenCorvus typecheck pass under inactivity supervision.
- The transformed non-expert dependency graph is generated from `src/index.ts` with `dpdm -T`, then filtered only for the explicitly excluded expert-squad and dynamic-identity modules before Tarjan strongly connected component analysis. Moving tool ID data to leaves reduced the sole component from 129 to 123 modules.
- `server/in-process-client.ts` no longer dynamically imports the Server composition root. It requires one explicitly installed app capability; `Server.App` and CLI bootstrap own installation, a second different provider is rejected, and a missing provider throws instead of falling back. The in-process transport and boundary regressions pass. This split reduced the 123-module component into a 64-module component plus a two-module `agent/model <-> config/effective` component.
- Session-overlay and root-session resolution now belong to `EffectiveConfig.overlay`; `agent/model.ts` consumes that configuration service instead of owning a function that `EffectiveConfig` dynamically imported back. `MissingModelConfigError` moved to a configuration leaf. No old `resolveSessionOverlay` re-export remains. Four isolated overlay regressions cover session ID, task ID, null task root, and contradictory roots; the dependency boundary and base model tests also pass. The two-module component is gone, leaving one 64-module non-expert component.
- An intermediate OpenCorvus typecheck exposed four concurrent projected-worker errors in `session/runtime-contract.ts`; after those excluded edits settled, a later full typecheck passed. The full `model-overlay.test.ts` process still has an existing 120-second no-output hang when run as one file; its four directly affected cases pass independently. Two unrelated task-config-isolation cases fail because `test/tool/fixtures/models-api.json` lacks the now-required local `hexin` provider; neither failure is treated as acceptance success.
- The `createGlobalTask` composition responsibility moved out of the core `EngineService` namespace into `GlobalTaskService`: it owns implicit-project creation, full instance bootstrap, task API invocation, and failure cleanup. `task-api/index.ts` no longer imports the project bootstrap root. All three `POST /global/tasks` regressions pass, including pre-directory body validation and implicit-project cleanup after task creation failure.
- Queue loop launch no longer passes `InstanceBootstrap` into a second `Instance.provide`. Queue dispatch has already proven task ownership against the active `Instance.project`; the nested lease reuses that active project cache entry without requesting another initialization source. The same three real global-task route regressions and full OpenCorvus typecheck pass after this change.
- `dpdm -T --skip-dynamic-imports circular` now reports zero static production circular dependencies. The full transformed graph still reports one 63-module strongly connected component because runtime dynamic imports include `engine/queue -> orchestrator/loop` and other deferred composition edges. That dynamic component remains explicit unfinished debt; static zero-cycle evidence does not satisfy the full cycle acceptance item by itself.
- Queue no longer dynamically imports the orchestrator loop. `InstanceBootstrap` injects the one task-loop runner into instance-scoped queue runtime; a second different runner or a missing runner is an explicit error. The real global-task creation chain and typecheck pass after the injection, and the full dynamic component fell from 63 to 37 modules.
- Orchestrator tool-ownership completion no longer dynamically imports queue. The former completion API was async so every production caller waited for drain completion instead of racing an unobservable fire-and-forget operation. An intermediate implementation incorrectly registered this internal coordination as a public `BusEvent`, which changed generated OpenAPI; the final design used one internal completion port installed by Queue module composition. The API inventory was unchanged, and the focused durable ownership wake transitioned from pending to drained. This split reduced the component from 37 to 24 modules.
- `EngineRuntime` no longer dynamically imports queue for terminal-goal refill. `RuntimeTaskLoopDispatch` is an explicit capability threaded through task-api and interaction runtime composition. The convergence regression proves `beforeAcceptedWake` records both terminal goal facts before the injected dispatch returns; the component fell from 24 to 22 modules.
- MCP stdio shutdown no longer imports the task-api facade to delete its transport-owned assistant session. After server/transport close it removes that session through the Session owner in the current project. MCP command/tool-surface tests pass until an unrelated pre-existing strict SkillTool fixture failure (`skillSurface` missing), after which the full file reaches its existing inactivity timeout. The dependency boundary passes and the component fell from 22 to 20 modules.
- Session shell completion no longer dynamically imports SessionLoop. SessionLoop installs one explicit resume function after its definition; shell execution requires that function only when prompt callbacks remain. The single-runner behavior test, dependency boundaries, and typecheck pass; the full non-expert dynamic component is now 19 modules. Remaining nodes are the global tool registry's deferred panel/schedule/wait/request-decision loaders and scheduler/queue/writer/task-api/executor wake-cleanup edges, so cycle acceptance remains incomplete.
- Queue dead-owner convergence and engine interruption finalization now use one required instance-scoped termination runtime installed by `InstanceBootstrap`. Queue no longer dynamically imports the full writer, and writer no longer dynamically imports the scheduler task-queue service; a missing or second different runtime fails explicitly. Sixteen focused contract/boundary tests and OpenCorvus typecheck pass. The full dynamic SCC remains 19 modules because parallel `SessionLoop -> ToolRegistry -> global-tools` and panel/scheduler/task-api paths still reconnect every node; static analysis remains zero-cycle, but dynamic cycle acceptance is still incomplete.
- `global-tools.ts` no longer dynamically loads Panel, Schedule, Wait, or Request Orchestrator Decision implementations. One process-wide control-plane loader composition is installed by production bootstrap and test preload; the registry requires it and rejects a second different source. Removing these four deferred reverse edges split the 19-module component down to the two-module Queue/Cron loop without changing projected-worker identity or expert-squad code.
- Queue and Cron now coordinate accepted waits and due task wakes through one explicit task-wake runtime composition instead of dynamically importing each other. Twenty port/boundary tests, two Queue wait-consumption tests, two Cron task-wake tests, OpenCorvus typecheck, and `git diff --check` pass. The transformed `dpdm -T` graph filtered only for the excluded expert/dynamic-identity modules now reports `NON_EXPERT_DYNAMIC_SCC=[]`; `dpdm -T --skip-dynamic-imports circular` also reports zero. Root typecheck, API route inventory, docs render check, canonical workspace dead-code analysis, and seventy-six document-health/single-source tests pass.
- The final package-context dependency audit analyzed 733 modules and 689 non-expert nodes; dynamic SCC remains empty after replacing the interim public ownership Bus event with the internal completion port. `api:routes-check` again passes all six rules across thirty files, proving that internal queue coordination does not alter OpenAPI or SDK contracts.
- Canonical `check:dead-code` leaves the current `bun.lock` byte-identical (`62A67E37DD48ADC8AAB65A825D41EF01A37B724E09C1C926534C816BE6562012` before and after). Build/SDK/OpenAPI transaction tests pass 69 cases; one remaining assertion expects the excluded old agent/session/skill mount request instead of the current qualified mount override and is not treated as non-expert acceptance evidence.
- Overlay typecheck passes. The single-root, architecture, theme, and unhandled-rejection suites pass 136 tests. The Screenshot Browser fixture now carries explicit `originSource`, its Node browser test passes, and both regenerated desktop/narrow-panel screenshots were manually inspected: each renders the real 120-item grouped thumbnail surface without an error overlay or empty-state substitution.
- The full `engine/queue.test.ts` fixture debt is repaired. Tasks now carry parentless root sessions, IDs use current prefixes, persisted messages carry explicit authors, runtime capabilities are installed explicitly, and async runner assertions occur after the submitting Instance lease exits instead of self-deadlocking inside it. The file passes 34/34 under stdout/stderr inactivity supervision and contains no test-local elapsed timeout.
- Browser fixture authorship is now complete across the non-expert Browser set: every persisted message fixture that declares `role` also declares `author` and `originSource`. The repaired browser batches pass 31/31, 28/28, and 16/16; the remaining non-expert files pass in focused batch/rerun evidence. `overlay-global-live-pressure-browser.test.ts` and `skill-mount-matrix-browser.test.ts` retain only dynamic skill/expert projection assertions and remain excluded by the user's scope correction.
- Screenshot Browser keeps one complete row source and native Virtua item sizing. Reducing row overscan from one to zero lowered the initial mount from the diagnosed five virtual rows/twelve cards and restored the explicit RAF/long-task threshold without reintroducing a partial-item source. Its real 120-item Browser test passes after removing a fixed conversation-resource wait timeout, and regenerated desktop/narrow screenshots were manually reviewed again.
- Titlebar visual review exposed two real contract drifts: a 48px titlebar owned a 36px top row, and the Brand Guide popover overflowed the 390px viewport. The top row now consumes the canonical titlebar-height token; the Brand Guide uses Kobalte `fitViewport` and `--kb-popper-content-available-width`. Desktop and narrow browser tests pass and screenshots show no empty titlebar band or popover clipping.
- Canonical Knip found `overlay-layout-frame.ts` reachable only from tests. That implementation and its self-preserving unit test are deleted; `overlay-window-size-contract.test.ts` now asserts the parallel TypeScript size source stays absent. Runtime sizing remains owned by the native/CSS size contracts. `check:dead-code` passes and leaves `bun.lock` byte-identical at `62A67E37DD48ADC8AAB65A825D41EF01A37B724E09C1C926534C816BE6562012`.
- A final TypeScript abstract-syntax-tree audit found 136 remaining non-expert Bun test calls with non-zero per-test elapsed timeouts. They now disable local elapsed timing, while Channel Runtime, SDK JS, OpenCorvus, and VS Code package scripts all execute `bun test --timeout=0` behind the shared stdout/stderr inactivity supervisor. The supervisor contract passes 13/13; Channel Runtime, SDK JS, and VS Code package scripts pass end to end. Removing the masking SDK timeout exposed a Windows suite-order hang: its cwd/env/process-tree scenarios now run before the pure source assertion, and the full SDK file passes 5/5. The VS Code browser-preview rejection fixture also carries the protocol-required `scopeKey`, and its package suite passes 139/139.
- Manual criterion-by-criterion review rejected the earlier assumption that Orchestrator tool-domain separation was complete: `createOrchestratorTools` still owned roughly 9,500 implementation lines inside a 709 KB module. Domain extraction now owns runtime repair (`bash`, `wait`, `browser_preview`), interaction (`inject_operator_message`, `question`), task lifecycle (`complete_task`, `fail_task`, `cancel_task`, `retry_task`), goal lifecycle (`add_goal`, `modify_goal`, `complete_goal`, `delete_goal`), live goal-mutation ownership guards, Build feedback/evidence synthesis, Build requirement/collaboration/fidelity context scoping, evidence-anchored task proposal, failed-goal diagnostics, bounded audit-context reading, structured Refine analysis, explicit sub-agent cancellation, cancellation target resolution, shared live-build cancellation runtime, and dispatch-agent option parsing outside `tools.ts`. Terminal workspace cleanup and reusable A2A task failure moved with lifecycle rather than remaining callbacks in composition. Structural guards prohibit these implementations and helpers from returning; focused runtime-repair/wait tests pass 15/15, task lifecycle/A2A tests pass 6/6, goal lifecycle tests pass 12/12, goal contract diff tests pass 16/16, Build context tests pass 4/4, proposal tests pass 6/6, failed-goal diagnostics pass 1/1, read-context budget/pointer tests pass 2/2, Build feedback tests pass 3/3, Refine prompt ownership passes 1/1, cancellation behavior passes 9/9, cancellation description passes 1/1, OpenCorvus typecheck passes, static `dpdm` reports zero cycles across 604 modules, and `git diff --check` passes. `tools.ts` is reduced to 13,417 lines / 595,092 bytes. Criterion 6 remains explicitly incomplete until the remaining large dispatch implementations leave the composition module.
- Continued composition review moved goal contract audit, persisted architect fidelity, visual-spec parsing, stage-input hashing, stage-continuation recovery, acceptance prompt rendering, real tool-execution ownership checks, complete Requirements/Frontend Research/Deep Research dispatchers, Workload Analysis, Intent Analysis, the read-only Explore tool, and Build retry-session selection into domain modules. Invalid persisted fidelity, visual specs, acceptance specs, and persisted workload goal columns now throw through canonical schemas instead of degrading to empty/stringified prompt data. Missing Build terminal evidence remains a typed `BuildAgentContractError`; the Orchestrator no longer manufactures schema-valid failed `BuildResult` reports for contract or infrastructure throws. The focused strictness and Build contract batches pass 39 cases when the excluded projected-worker continuation fixture is omitted; Build retry classification passes 2/2, domain ownership passes 94 structural assertions, OpenCorvus typecheck, static `dpdm`, and `git diff --check` pass. Two real Requirements tests are currently blocked before dispatch by the skipped concurrent projected-worker tool-schema change and therefore are not counted as passing. `tools.ts` is reduced to 11,379 lines / 507,433 bytes. Criterion 6 remains incomplete because Architect, Visual QA, coordination response, frontend design, and Build implementations still reside in composition.
- Visual QA dispatch, continuation recovery, context assembly, report persistence, and rendered visual-feedback verification now belong to `visual-qa-stage.ts`; `tools.ts` only injects task/session tracking and continuation digest helpers. The ownership guard now executes 95 assertions, OpenCorvus typecheck passes, and `dpdm` reports zero static cycles across 380 entry-graph modules. Concurrent edits changed surrounding composition size while this extraction ran; the current measured `tools.ts` size is 11,445 lines / 507,159 bytes. Criterion 6 remains incomplete because Architect, coordination response, frontend design, and Build implementations still reside in composition.
- Architect dispatch, continuation recovery, transactional spec/goal/contract persistence, fidelity remapping, and protocol publication now belong to `architect-stage.ts`. Existing persisted Goal acceptance specs and string-array columns are parsed through canonical strict schemas; malformed JSON or shape errors abort explicitly instead of degrading to empty arrays. The focused Architect strictness and ownership suites pass 4/4 with 101 assertions, OpenCorvus typecheck passes, and `git diff --check` reports only unrelated line-ending warnings. `tools.ts` is now 11,093 lines / 492,255 bytes. Criterion 6 remains incomplete because frontend design and Build still reside in composition; coordination response remains pending classification against the excluded dynamic-worker identity boundary before any move.
- Frontend Design execution, Figma MCP materialization, local material validation, live webpage evidence preparation, resource-manifest persistence, public-report materialization, and decision-log publication now belong to `frontend-design-tool.ts`; `tools.ts` retains only the shared input schema and dependency wiring. The first generated module was rejected immediately because the shell transport truncated a large source block; it was rebuilt from the Git baseline, then reconciled with current explicit agent/session identity contracts before validation. Domain ownership now passes 104 assertions, the focused source-acquisition contract passes, OpenCorvus typecheck passes, static `dpdm` remains zero-cycle across 380 modules, and `git diff --check` reports only unrelated line-ending warnings. Two runtime material-path tests still stop before this tool at the explicitly skipped concurrent projected-worker input-schema failure (`target expected never`), so they are recorded as blocked rather than passing. `tools.ts` is now 10,129 lines / 446,139 bytes. Criterion 6 remains incomplete only for Build among non-excluded tool implementations; `respond_agent_coordination` is excluded because its 707-line body is dominated by frozen projected-worker identity and redispatch binding semantics.
- Build execution, Goal reference resolution, coordinator/direct-run creation, durable retry selection, ownership, worktree handling, prompt/evidence composition, contract audit, background terminal completion, goal-run finalization, and host diff attribution now belong to `build-tool.ts`. Persisted Goal acceptance/dependency columns are parsed through canonical strict schemas instead of empty-array degradation. `tools.ts` retains the shared Build input schema and composition callbacks only; `awaitOrchestratorBackgroundGoalBuilds` is re-exported from the Build owner. TypeScript organize-imports removed the extraction residue from the composition module. Domain ownership passes 114 assertions, strict parsing suites pass 8/8, OpenCorvus typecheck, canonical production dead-code analysis, static zero-cycle analysis across 380 modules, and `git diff --check` pass. The broader Build goal-reference file reports eight failures solely because the skipped concurrent projected-worker projection removes the public `tools.build` entry before execution; eleven unaffected Build context/retry/audit tests pass and none of those eight failures entered `build-tool.ts`. `tools.ts` is now 8,376 lines / 357,689 bytes. Criterion 6 is satisfied for the non-excluded scope: remaining direct tools are thin stage wrappers or the explicitly excluded expert-squad selector/skill and projected-worker coordination response.
- Manual second review rejected the first criterion-6 conclusion because the full Integrity review runner still lived in composition behind a generic singleflight wrapper. `stage-evidence-snapshot.ts` now owns the canonical evidence count/digest schema, while `integrity-review-stage.ts` owns strict Goal projection, continuation validation, review context/evidence assembly, ownership, consensus execution, integrity-attempt persistence, decision history, and artifact-missing protocol status publication. The composition module now injects only task/session/scheduler inputs into that owner. Integrity history and ownership suites pass 6/6 with 137 assertions, OpenCorvus typecheck passes, canonical production dead-code analysis passes, and static `dpdm` remains zero-cycle across 381 modules. `tools.ts` is now 7,791 lines / 334,889 bytes. Criterion 6 is now satisfied for the non-excluded scope after the required second review correction.
- Final root and OpenCorvus typechecks, Overlay typecheck, API route inventory, docs render check, 136 Overlay single-root/architecture tests, the Queue 34-test file, focused runtime composition tests, static zero-cycle analysis, non-expert dynamic SCC analysis, and `git diff --check` pass. The document-health batch currently reports 75/76 solely because concurrent untracked `specs/records/2026-07/2026-07-11-settings-detail-gui-remediation.md` is already linked by the concurrently modified July README but is not present in `git ls-files`; this task does not stage or rewrite that unrelated record and therefore does not claim final completion yet.
- Broad unscoped Knip output is not acceptance evidence because it includes excluded expert-squad files and unrelated root tooling. The canonical workspace-scoped production command is the authoritative dead-code/dependency check and passes.
- Final strictness review removed the last silent degradation in the extracted Build path: malformed persisted visual-feedback verification now throws, acceptance feedback uses the canonical `AcceptanceReviewVerdict`, required architect fidelity `owned_paths` and Frontend Design structured arrays no longer degrade to empty arrays, and Integrity Goal columns use canonical persisted-column parsers. The focused fidelity, Build feedback, domain ownership, and Integrity history batch passes 21/21 with 179 assertions.
- The 2026-07-11 wrap-up matrix was rerun after the user explicitly authorized skipping the concurrent dynamic-projection fault. Overlay typecheck, `docs:check` (249 operations across 24 groups), canonical production `check:dead-code`, static `dpdm` analysis across 371 entry-graph modules, and `git diff --check` pass. OpenCorvus typecheck has one remaining error in concurrently modified excluded `engine/stage-continuation.ts` (`AgentRoleContract` is absent), while `api:routes-check` reports concurrently generated OpenAPI `additionalProperties: false` drift. Neither failure enters the extracted non-expert tool implementations.
- Document-health wrap-up reports 73/76. The three failures are concurrent repository-state failures outside this task's preserved scope: `bun.lock` currently contains internal `repositories.myhexin.com` package URLs; the concurrently modified July README links three untracked July records; and the tracked expert-squad template file `specs/artifacts/portable-expert-squad-template/package/virtual-agents/architect/system.md` is deleted in the working tree. Historical-link and product-document single-source tests pass. The task does not rewrite the lockfile, stage unrelated records, or restore the excluded expert template.
- Final manual review finds no remaining non-expert fallback, compatibility branch, synthetic report/message path, second source, unexplained generated residue, or Orchestrator domain implementation retained in composition. The non-expert remediation itself is complete, but the global acceptance matrix is deliberately not represented as green while the explicitly skipped concurrent failures remain present.
- A resumed criterion-by-criterion audit found one non-expert omission hidden by a test alias: `executor/live-official.test.ts` registered `liveCodex` and `liveClaude` with Bun's 180-second elapsed test timeout. Both registrations now use `timeout: 0`, leaving stdout/stderr inactivity supervision as the process timeout owner. `inactivity-timeout-process.test.ts` includes a source-level alias regression; the full inactivity runner suite passes 14/14 with 43 assertions. A TypeScript abstract-syntax-tree scan across OpenCorvus and Overlay test trees finds no remaining non-expert test registration with a non-zero elapsed timeout; its only real remaining registration is the excluded expert-squad selector browser test.
- The resumed single-source verification batch passes 47/47 with 206 assertions across protocol message persistence, session mirroring, Queue directory/typed-failure ownership, and canonical model catalog discovery/refresh. The retained `taskCwd()` helper resolves only `task.session_id -> session.directory`; the remaining `dispatchTaskLoop()` `"ignored"` result is a successful suppression of a passive stream-error wake, not a collapsed submission failure. `OPENCORVUS_MODELS_PATH` selects the one canonical catalog file documented by `06-provider.md`; startup reads no second model source and missing/invalid catalogs fail explicitly.
- The resumed real-browser batch passes 9/9 across Screenshot Browser grouping, titlebar menus, responsive locale widths, Alt access, global-task startup, project clearing, resizers, right toolbar, and Brand Guide popover. It ran through the Node browser runner with stdout/stderr inactivity supervision and did not restart or reuse the user's live Overlay process.
- The latest global rerun still fails only in concurrently changing excluded surfaces: OpenCorvus typecheck now reports seven `session/loop.ts` mismatches caused by dynamic Agent projection omitting required `name`/`mode`; OpenAPI and docs regeneration now expose concurrent `expert_squads` and `runtime_templates` session-config fields absent from tracked generated outputs. Overlay typecheck, canonical production dead-code analysis, and `git diff --check` pass. These moving failures supersede the earlier `stage-continuation.ts` snapshot and further demonstrate that the concurrent expert/dynamic-identity branch is still unsettled rather than a failure in the completed non-expert remediation.
- A wider workspace audit disproved the earlier claim that all test-local elapsed timeouts were removed. The first audit covered object options but missed Bun's legacy numeric third argument and `setDefaultTimeout()`: 374 non-expert test registrations across 79 files still carried numeric elapsed limits, and `gateway/e2e.test.ts` installed a 30-second default. All are now `0`. `inactivity-timeout-process.test.ts` scans 2,162 workspace test files with the TypeScript abstract-syntax tree for numeric arguments, object `{ timeout }` options, known aliases, and imported non-zero `setDefaultTimeout()` calls while preserving the explicit expert-squad exclusion. The full runner/guard suite passes 16/16 with 45 assertions.
- Removing Gateway's elapsed timeout exposed real fixture debt rather than a slow test. The file directly invoked `EngineService` after Queue gained required task-loop and termination capabilities, but it only spied the retired module-call path and then waited for loop ownership while still holding the owning `Instance` lease. The fixture now injects an explicit mock task-loop runner plus the same termination functions used by production bootstrap, exits the submitting lease before awaiting ownership idle, and uses a deliberately releasable runner for the queued-sibling scenario. The focused three race paths pass 3/3 and the complete Gateway end-to-end file passes 18/18 in 78 seconds under stdout/stderr inactivity supervision, with no 60-second cancellation waits or swallowed fixture cleanup errors.
- After the complete timeout and Gateway remediation, Overlay typecheck, canonical production dead-code analysis, and `git diff --check` pass. The task record retains the concurrent expert/dynamic-identity typecheck, OpenAPI, docs, lockfile, and document-index failures as excluded global blockers rather than mislabeling them as resolved.
- A third resumed manual review found that the Gateway fixture still contradicted its own no-swallowed-cleanup claim: two queued-task deletes and one retry cleanup used `.catch(() => undefined)`. Those catches are removed and the full Gateway file still passes 18/18, so cleanup failures now remain observable.
- Process-supervisor termination/disposal contained a semantic duplicate: after aggregating every rejected settled result and throwing, each function repeated `if (exit.status === "rejected") throw exit.reason` solely to narrow TypeScript. The result checks now branch directly on `cleanup.status` or `exit.status`, construct the aggregate only inside the failure branch, and return the narrowed fulfilled value. `promise-boundaries.test.ts` rejects restoration of the duplicate branch and now reads runtime-repair process ownership from `orchestrator/runtime-repair-tools.ts` rather than the retired monolithic `tools.ts` location. It also reflects the current MCP stdio Session-owner cleanup. The focused promise-boundary suite passes 2/2 with 66 assertions, and OpenCorvus typecheck is currently green.
- The latest full-matrix retry therefore improves from the previous snapshot: OpenCorvus and Overlay typechecks, production dead-code analysis, real browser coverage, focused runtime/Gateway/timeout suites, and `git diff --check` pass. Remaining global failures are unchanged excluded/concurrent state: generated OpenAPI and API docs include `expert_squads`/`runtime_templates` while tracked artifacts do not; `bun.lock` contains the internal registry; July README links three untracked concurrent records; and the tracked expert template architect prompt remains deleted. This is the third consecutive resumed turn with the same external acceptance blockers.
- Auxiliary evidence publication is no longer silent in the reviewed non-expert paths. Agent observer disposal, failed-run trace collector reads, and workflow-step protocol publication now emit structured error logs without replacing the primary terminal result. The promise-boundary suite passes 2/2 with 72 assertions, OpenCorvus typecheck passes, and `git diff --check` remains clean apart from unrelated line-ending warnings.
- Session diff storage had a dead compatibility-shaped abstraction: `sessionDiffPathReadCandidates()` returned exactly one path, while `Session.diff` and `SessionSummary.diff` swallowed every read or JSON parse failure and projected corrupt storage as an empty diff. The candidate helper is removed; both readers use the canonical `sessionDiffPath`, return an empty array only for `ENOENT`, and propagate malformed content. The focused Session and runtime-path suites pass 19/19.
- Managed sidecar shutdown no longer converts cleanup failure into success. Watchdog stop and lock-acquisition cleanup failures are logged, lock release failures reject shutdown, server stop failure still prevents premature lock release, and the completion chain exits `0` only on success or `1` on failure instead of scheduling `process.exit(0)` from `.finally()`. The shutdown lifecycle suite passes 4/4 with 21 assertions and OpenCorvus typecheck passes.
- The latest wrap-up matrix passes the inactivity contract 16/16, Gateway 18/18, both OpenCorvus and Overlay typechecks, API route inventory, and `git diff --check`. The user-authorized concurrent exclusion remains the only global blocker: docs regeneration includes unsettled expert/runtime-template fields; document health reports the internal-registry lockfile, six untracked July records already linked by the monthly README, and the deleted tracked expert template prompt. These files remain untouched by this remediation.
- Browser-preview route diagnostics no longer manufacture usable-looking evidence when response-body, DOM, page-size, title, or screenshot collection fails. The real route suite passes 4/4 with 35 assertions, including a source contract that keeps those evidence operations observable.
- Sidecar lock handling now treats only `ENOENT` as absence. Malformed/stale lock deletion failures, malformed owned-lock release, and retry exhaustion propagate; contention diagnostics retain a structured warning when the competing lock cannot be read. Sidecar lock and shutdown suites pass 17/17 with 59 assertions.
- Eleven dead nonzero timeout constants left after registration-timeout removal are deleted. The TypeScript abstract-syntax-tree inactivity guard remains green at 16/16 with 45 assertions.
- Overlay deterministic browser regressions were repaired without changing the excluded dynamic Agent identity surface: composer runtime controls can shrink and cannot intercept the Stop action, the conversation rail retains a 24px centered outer gutter, Provider credential tests open the intended collapsible editor, and stale fixture/theme assumptions now match required task timestamps and the single-source flat palette. The focused batch passes 11/11 after the rail rerun.
- Real screenshots were manually reviewed at `.scratch/conversation-agent-rail-scroll-browser/chat-section-after-locate.png`, `.scratch/provider-settings-primitive-owner.png`, and `.scratch/light-theme-codex-reference.png`; composer actions do not overlap, the rail belongs to the centered conversation group, and Provider/light-theme hierarchy remains readable.
- The Node-owned non-expert browser run, excluding the user-deferred global pressure test and explicitly excluded expert-squad files, passes 135/138. All three failures are downstream of the concurrent dynamic Agent change: two Settings tests still expect the removed `task` permission row, and the Chat Bubble test waits for removed sub-Agent fixture content. The independently repaired deterministic files all pass. No excluded test or production identity projection was edited.
- Final root typecheck passes 9/9 packages, `api:routes-check` passes 6 rules across 30 files, `docs:check` passes 249 operations across 24 groups, canonical production `check:dead-code` passes, and `git diff --check` reports only pre-existing line-ending warnings. Manual focused search finds no silent catch, fallback, legacy attachment, synthetic report, retired prompt draft, or CSS compatibility source in the touched non-expert surfaces.
- The deferred global pressure test was rerun alone at wrap-up and passes 1/1 in 9.5 seconds. Its worst reported frame gap is 119.2ms during native Browser Preview completion, with no retired live-route/prompt requests and no horizontal overflow. The earlier full-run failure is therefore classified as concurrent resource interference rather than a product regression.
- Historical document scanning exposed a test-tool scaling defect after real browser evidence grew `.scratch` to 264MB: the test launched one `rg` process per retired-path pattern, and Bun killed a child during repeated scans. The scanner now performs one native recursive content pass, skips known generated dependency/build directories and NUL-bearing binary files, and classifies every text match against the same canonical pattern table. Unknown text extensions remain covered and the historical-link suite passes 20/20 with 69 assertions.
- Final document-health status is 73/76. The three failures remain preserved concurrent repository state outside this task: internal registry URLs in `bun.lock`, six untracked July records already linked by the monthly README, and the deleted tracked expert-template architect prompt. Product-document single-source passes 4/4; historical links pass after the scanner repair.
