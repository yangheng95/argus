# v0.0.8beta Composer Discovery Matrix Repackage

## Recall

### User request

- Run the package matrix after the Composer discovery-action, model Provider entry, and single-line expert-squad Tooltip delivery.

### Acceptance criteria

1. Prove the `v0.0.8beta` source and git-cc remote are aligned before packaging.
2. Run the canonical production matrix in order: Graphical User Interface (GUI) installer first, portable Command-Line Interface (CLI) binary second.
3. Build and validate the native macOS ARM64 row; explicitly report every unavailable non-native row as skipped.
4. Verify release version, architectures, signatures, Disk Image (DMG) integrity, archive contents, checksums, and isolated packaged-runtime health.
5. Launch only an isolated packaged application, capture and inspect the delivered Composer/model/Tooltip surfaces, and do not interfere with the user's running OpenCorvus instance.
6. Record the generated artifacts as evidence, restore the repository-tracked artifact snapshot after verification, commit only the record/index changes with the required prefix, and push to `myhexin/v0.0.8beta`.

### Hard constraints

- Preserve all concurrent user edits, including `packages/overlay/src/components/App.tsx` and the browser tests that appeared during packaging. Do not reset, restore, stage, or commit them.
- Do not create another worktree, fake cross-platform verification, invoke the separate Linux remote-service upload flow, or commit generated binaries/installers.
- Use only `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` as production owners.
- Do not stop, restart, refresh, close, or reuse the user's running Overlay. Any packaged runtime verification uses isolated temporary homes and ports.
- Commit subjects use `dsw-33987`; push only through repository hooks to git-cc remote `myhexin`.

### Sources read

- Repository `AGENTS.md` packaging, visual acceptance, version-control, and running-process boundaries.
- `script/package-gui-installer-matrix.ts`, `script/package-binary-matrix.ts`, root `package.json`, `docs/packaging.md`, and `RELEASE.md`.
- Prior native production evidence in `2026-07-17-v0.0.8beta-native-menu-matrix-repackage.md`, `2026-07-17-v0.0.8beta-remote-merge-and-matrix-build.md`, and `2026-07-17-v0.0.8beta-latest-matrix-build.md`.

### Whole-repository search evidence

| Surface | Disposition |
| --- | --- |
| `package:gui-installer-matrix` | Canonical five-row GUI owner; builds SDK, Overlay, Tauri bundle, stages the matching host row, and validates release assets. Run first. |
| `package:binary-matrix` | Canonical five-row portable CLI owner; compiles, executes, archives, and validates only the matching native host row. Run second. |
| `package:linux-binary` | Excluded because it owns the separate Linux baseline/remote-service bundle and no upload was requested. |
| Release workflows and packaging docs | No code change required; they already call the two canonical owners with native-runner semantics. |
| Matrix/release contract tests | Run after packaging to verify row declarations, staging ownership, archive contracts, and release workflow paths. |
| Existing generated outputs | May be replaced only by the requested production matrix owners for verification. The macOS GUI artifacts are repository-tracked, so restore their checked-in snapshot after recording the fresh output evidence rather than mixing binaries into the documentation commit. |
| Concurrent user edits | Leave all such paths unstaged and untouched. `App.tsx` remains a one-line indentation-only diff; three browser-test edits appeared after matrix execution and are likewise outside this delivery. |

### Independent agent feedback

- None. The user did not request delegation.

## Starting evidence and execution plan

- `bun run version:check` reports the canonical `0.0.8-beta` release family.
- A fresh `git fetch myhexin v0.0.8beta` proves local and remote both resolve to `6497446f1bcf71bb74823bf9ab64834c10bddcc4` with divergence `0 0`.
- Native host identity is `Darwin arm64`; therefore only the `darwin-arm64` row can be honestly built and runtime-verified here.
- Run GUI matrix, then CLI matrix. Verify artifacts and isolated runtime/visual behavior, run focused package/document tests, update this record with exact evidence, review/stage only documentation, commit, fetch, and push.

## Verification record

### Production matrix

- `bun run package:gui-installer-matrix` passed first. It built and release-validated the native `darwin-arm64` GUI executable, DMG (Apple Disk Image), and application archive. `linux-x64`, `linux-arm64`, `darwin-x64`, and `windows-x64` were explicitly reported as skipped because this host cannot truthfully build or execute those native rows. The only compiler output was the two established macOS taskbar-badge dead-code warnings.
- `bun run package:binary-matrix` then passed. It compiled, executed, archived, and validated the native `darwin-arm64` portable CLI runtime; the same four non-host rows were explicitly skipped.
- The GUI executable, bundled application executable, and CLI executable were each identified as Mach-O 64-bit ARM64. The CLI `--version`, application `CFBundleShortVersionString`, and application `CFBundleVersion` all reported `0.0.8-beta`.
- `codesign --verify --deep --strict` accepted `OpenCorvus.app`; strict verification also accepted the staged GUI executable. `hdiutil verify` reported the DMG checksum valid. Archive inspection proved that the application archive contains the signed `.app`, while the CLI archive contains its executable, `bin`, `node_modules`, browser MCP (Model Context Protocol) Node runtime, package manifest, and bundled UI.

| Fresh artifact | Size | SHA-256 |
| --- | ---: | --- |
| Retained build output `packages/overlay/src-tauri/target/release/opencorvus-overlay` | 155 MiB | `e238f9fe2479596d05a59a784c1cd2688a42695176543d278916aa614f0cdcce` |
| Retained build output `packages/overlay/src-tauri/target/release/bundle/dmg/OpenCorvus_0.0.8-beta_aarch64.dmg` | 146 MiB | `de2f7f6ac69b0bcdafba4a5852dfa5d8cf706698b7e17c23ae7b8bbdeb41d27a` |
| Verified transient release-stage output `OpenCorvus_0.0.8-beta_aarch64.app.tar.gz` | 145 MiB | `cee23a6082d51e45eb409d335e3df1f0105b4ff957c7e08d70eb080bc9fe2692` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64/opencorvus` | 96 MiB | `eed488c9aeaada995e3508f05c5260742497aa7b44c7ea4c07cbe2dd9a8dd234` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64.tar.gz` | 111 MiB | `76ef5a4689f0aa45f91f609c42a86802e12b3ac7d7366e4172fe00740765d134` |

### Isolated runtime and visual acceptance

- The packaged CLI ran with an isolated `HOME`, isolated `OPENCORVUS_HOME`, reduced `PATH`, and port `47887`. `/global/health` returned `healthy: true`, version `0.0.8-beta`, and only temporary-root database/data/home paths. Graceful interrupt settled zero tasks, runs, goal runs, ownerships, sessions, tool parts, or corrupt tasks; the port was released. The only capability notices were the expected absence of Codex and Claude Code from the deliberately reduced test `PATH`.
- A separate packaged `OpenCorvus.app` instance ran with isolated storage. Its sidecar returned healthy version `0.0.8-beta` on the isolated port. The exact test application process was closed with Command-Q after inspection; its sidecar exited and released the port.
- `.scratch/v0.0.8beta-composer-discovery-matrix/packaged-app.png` was personally inspected: the native window loaded the empty workspace, Composer, and `0.0.8beta` version without clipping or blank regions.
- `packaged-model-menu.png` was personally inspected: the model selector exposes the no-provider state and the terminal `Configure Provider…` discovery action with its stable explanation.
- `packaged-mention-menu.png` was personally inspected: typing `@` exposes the Skill and Expert Squad categories, with the `@skill` and `@squad` badges visibly emphasized and no menu clipping.
- `packaged-squad-menu.png` was personally inspected: the isolated catalog intentionally contains only Chat, followed by `Install more expert squads…`. Because no expert squad was installed in this clean runtime, this native screenshot cannot exercise an installed squad's description Tooltip; the same final source had already passed the immediately preceding real-browser visual check for the single-line squad row and hover-only explanation. This limitation is reported rather than represented as a native Tooltip proof.

### Regression and workspace integrity

- Focused packaging/release contracts passed: `36` tests, `103` expectations, zero failures across the GUI matrix, CLI matrix, native binary, and release Overlay contract suites.
- Historical links, document health, and product-document single-source suites passed after the new record entered the Git index: `81` tests, `1282` expectations, zero failures.
- The release-stage macOS GUI binaries/installers are tracked snapshots. After recording and validating the fresh staged files, their four packaging diffs were restored precisely from `HEAD`; the transient generated `macOS-schema.json` was removed. The freshly built GUI executable and DMG remain available under Tauri's ignored `target/release` output tree, and the portable CLI output remains under its ignored `dist` tree. The matrix-generated application archive was a transient staged validator input and is identified by its recorded digest rather than left as a tracked binary diff.
- Concurrent user edits remain unstaged and outside the delivery. In particular, `App.tsx` still has only its one-line indentation diff and SHA-256 `dece961d7bcfa51396f1abeea55c9c1ef808fcf3a975e4a2bb89662ecae16ecd`; three browser-test edits that appeared during matrix verification were not touched.
