# v0.0.4beta workspace version bump

## Recall

### User requirement

- Merge the committed changes from the current `v0.0.3beta` line into a new release line.
- Switch the entire primary workspace to `v0.0.4beta`.
- Upgrade the repository-owned OpenCorvus release surfaces together, including the version shown by Overlay.
- Establish one convenient version-bump command so future releases cannot drift between surfaces.
- Preserve all concurrent Agent working-tree changes.

### Acceptance criteria

- The primary worktree is on branch `v0.0.4beta`, based on the current committed `v0.0.3beta` HEAD.
- Existing unstaged and untracked Mirror Watch/session work remains byte-for-byte outside this task's staged diff.
- OpenCorvus release-family package manifests, native Cargo manifests, Tauri configuration, and generated lockfile entries carry canonical SemVer `0.0.4-beta`.
- Overlay derives the requested visible label `v0.0.4beta` from that canonical version rather than storing a second display version.
- Root commands provide one bump entrypoint and one drift check; CI continues to use the same checker.
- Unit tests cover compact beta input normalization, target completeness, drift detection, and Overlay label formatting.
- Focused tests, typechecks, version drift check, documentation health checks, diff review, commit, and legacy remote push succeed without bypassing hooks.

### Hard constraints

- No fallback, compatibility alias, second version source, gate, keyword-based repository rewrite, or state machine.
- Use the current primary worktree; do not create another worktree, reset, rebase, overwrite tracked files, or touch running OpenCorvus/Overlay processes.
- Do not stage, amend, or otherwise claim concurrent Mirror Watch/session files.
- Package and Cargo metadata use legal Semantic Versioning (SemVer) `0.0.4-beta`; the compact product label is a pure formatting projection.
- `packages/web` remains on its independently versioned `0.1.0`; dependency versions and fixture/example values are not product release metadata.
- Every new commit subject starts with `dsw-33987` and delivery targets the legacy remote.

### Sources read

- Repository-provided `AGENTS.md` instructions.
- `specs/records/2026-07/2026-07-14-v0.0.3beta-branch-sync.md`.
- `RELEASE.md`, root `package.json`, `script/sync-version.ts`, and `script/release`.
- All tracked workspace `package.json`, product Cargo manifests and lockfiles, Overlay Tauri configuration, Vite version injection, version utility, Sidebar label, build scripts, workflows, and version-related tests.
- Current branch graph, remote divergence, status, and concurrent working-tree diff inventory.

### Whole-repository search evidence

- `git ls-files` enumerated every tracked `package.json`, `Cargo.toml`, `Cargo.lock`, Tauri configuration, and `bun.lock` rather than scanning generated build directories.
- `git grep sync-version` found all callers in `.github/workflows/build-overlays.yml`, `.github/workflows/build-vscode-extension.yml`, `.github/workflows/build.yml`, `.github/workflows/test.yml`, `RELEASE.md`, and `script/release`.
- `git grep` found the runtime version chain through `packages/opencorvus` build defines, native packaging scripts, Tauri's `CARGO_PKG_VERSION`, Overlay Vite injection, `src/utils/version.ts`, and `SidebarVersionLabel.tsx`.
- Release-family version targets are: AInvest workspace app, channel config/runtime, OpenCorvus, Overlay, plugin, JavaScript software development kit (SDK), transport protocol, utility package, Visual Studio Code extension, native process supervisor, Overlay Tauri package/configuration, and their Bun/Cargo lock entries.
- `packages/web` is already independently versioned at `0.1.0`; `packages/script` has no package version. Neither is silently coerced into the OpenCorvus release family.

### Independent agent feedback

- No sub-Agent was started: the user required protection of other Agents' concurrent modifications, and this task does not require delegated or parallel Agent work.

## Implementation plan

1. Keep the primary workspace on `v0.0.4beta` and snapshot hashes for all concurrent dirty files.
2. Refactor the existing `script/sync-version.ts` into the sole explicit release-family target registry, accepting compact beta input but storing canonical SemVer.
3. Add root bump/check scripts, update every declared version target and generated lock entry to `0.0.4-beta`, and update release documentation.
4. Format Overlay's visible prerelease label from the canonical injected version and add focused unit/browser assertions.
5. Run version, lock, package, documentation, type, and focused Overlay validation; visually inspect the real browser screenshot for the version label.
6. Recheck concurrent-file hashes and staged paths, commit only this task's files, fetch/merge the current legacy remote release branch if needed, then push `v0.0.4beta` through hooks.

## Codex review feedback

- The mandatory document-health run exposed a stale workflow assertion introduced before commit `a714da6991` moved CLI software development kit (SDK) generation into `script/package-native-binary.ts` and removed the duplicate workflow step. The production workflow intentionally has one explicit Overlay SDK step; CLI packaging owns its SDK-first command order inside the canonical native packager.
- The plan is revised to update that regression test to verify the current two owners directly. Restoring the deleted workflow step would duplicate work and violate the single-source packaging path.
- Final staged-diff review found that the initial repository-alignment and browser assertions embedded `0.0.4-beta` as a second maintenance point. They were revised to read the canonical manifest and derive the visible label, so the next version bump does not require editing tests.

## Result

- The primary worktree was cut from committed `v0.0.3beta` HEAD `37ab547f99` onto `v0.0.4beta`; every concurrent Mirror Watch/session path remained unstaged and outside this delivery.
- `packages/opencorvus/package.json` remains the single release source. `bun run version:bump 0.0.4beta` normalizes input to `0.0.4-beta` and synchronizes the explicit release-family package, Tauri, Cargo, Bun-lock, and Cargo-lock targets. `bun run version:check` rejects any target drift.
- Overlay formats the injected canonical SemVer into visible `v0.0.4beta`; it does not persist a second display-version value.
- PASS: 5 focused version/label tests, including compact input normalization, explicit target inventory, current-tree alignment, no-write drift rejection, and display formatting.
- PASS: 17 native/Linux package tests; Cargo metadata parsed both native manifests/locks at `0.0.4-beta`.
- PASS: full repository typecheck after repairing the local AInvest dependency links (10/10 Turbo workspaces).
- PASS: Overlay typecheck, production Vite build, and Node-launched Sidebar browser test. Both the in-app real page and the 1280×720 Playwright screenshot showed the left-bottom `v0.0.4beta` label without clipping or layout regression; the hover tooltip remained visible and correctly styled.
- PASS: document-health, product documentation single-source, and ordinary historical link assertions after updating the stale SDK workflow assertion to the current native-packager owner.
- NOT PASSED: the full historical-docs scratch audit found 31 pre-existing files inside other Agents' `.scratch/o4`, `.scratch/opentest-message-*`, and Vite cache snapshots. Those artifacts were preserved per the user's explicit protection requirement; neither deletion nor an ignore rule was introduced.
- NOT PASSED: `bun install --frozen-lockfile` reports repository lock drift beyond the release version fields. Regeneration against the machine registry tried to add internal mirror URLs; regeneration against the public registry also changed unrelated existing Solid, Vite, fsevents, and workspace resolution entries. Both generated worktree rewrites were discarded after diff review, and the staged portable lock remains limited to the ten declared workspace versions.
- PASS: final staged-diff review contained only the 27 declared version/tool/test/spec files; all concurrent Agent paths remained unstaged.
- PASS: implementation commit `a94f78c6ad` was accepted by `legacy-remote/v0.0.4beta`. The non-bypassed pre-push hook passed full typecheck (10/10 workspaces), API route inventory, generated API documentation check (254 operations / 24 groups), Overlay internationalization (i18n), and tracked-source secret scan (0 hits). A direct `ls-remote` check confirmed the legacy remote ref at the same commit after the server emitted a non-fatal new-branch revision-range warning.
