# Dispatch Algorithm / Agent Boundary Audit

Date: 2026-06-21

## Objective

Run a careful adversarial audit of OpenCorvus scheduling, orchestrator, and
agent contracts. Only high-confidence findings may be fixed directly; low
confidence concerns are recorded for human review. Each fix must preserve a
clear execution-flow explanation, focused regression coverage, and a clean
commit/push trail.

## Recalled Constraints

- `AGENTS.md` requires no fallback logic, no gate-style bypass, no blind patching,
  and no broad git reset.
- Existing design records must be recalled before edits.
- Scheduling bugs must be explained as an execution chain, not as a final
  surface status such as `cancelled`, `aborted`, or `timeout`.
- Frontend/UI work requires visual verification. This audit is currently
  scheduler/agent logic only; no UI surface has been modified in this round.
- High-confidence dead code may be removed only when call-site evidence proves
  it is not used. Low-confidence cleanup candidates stay in this file.

## Benchmark Definition

Focused checks for this audit round:

- Orchestrator / scheduler / goal execution: targeted tests under
  `packages/opencorvus/test/orchestrator`, `test/engine`, and `test/scheduler`.
- Agent tool/schema contract: targeted tests under `test/agent`,
  `test/frontend-design`, `test/frontend-research`, `test/research`, and
  `test/orchestrator/orchestrator-tool-descriptions.test.ts`.
- Context pressure: `test/session/compaction-dispatch-anchor.test.ts` and
  `test/session/compaction.test.ts`.
- Whole-package safety: `bun run --cwd packages/opencorvus typecheck`.
- Pre-push safety: repository pre-push hook (`check:sdk-imports`,
  `check:ai-runtime`, `turbo run typecheck`, `api:routes-check`, `docs:check`,
  overlay i18n, secret scan).

Timeout strategy: use commands that emit normal test progress; treat lack of
process activity as the timeout signal instead of a fixed wall-clock cutoff.

## Current Baseline

- Previous high-confidence frontend-design/frontend-research/compaction fixes
  were committed and pushed as `a911e71a2b fix: harden frontend handoff agents`.
- `packages/opencorvus/src/provider/models-snapshot.ts` remains dirty from
  `bun run build:overlay` generation and is not part of this audit baseline.

## Independent Audit Lanes

| Lane | Scope                                                         | Status  |
| ---- | ------------------------------------------------------------- | ------- |
| A    | scheduler / goal runner / orchestrator execution flow         | running |
| B    | agent definitions / tool schema / terminal finalizer recovery | running |
| C    | agent responsibility boundaries / handoff purpose / dead code | running |

## Findings

| ID               | Confidence | Status        | Finding                                                                                                                                                                                                                                                                                                                                                                                                                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------- | ---------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SCHED-001        | high       | fixed, tested | `TaskQueueService.recover()` marked stale `running` rows failed in SQLite but could not release the corresponding in-memory hung Promise because `inFlight` stored a bare `Set<Promise<void>>`. Any later `runNow()` then waited forever on a task that the durable queue had already declared terminal.                                                                                                               | `packages/opencorvus/src/scheduler/task-queue-service.ts`; regression test `recovery releases stale in-flight prompt promises that stop producing activity` simulates a prompt Promise that never resolves and emits no activity. `bun test packages/opencorvus/test/scheduler/task-queue-service.test.ts` passed 19/19.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Changed `inFlight` to a task-id keyed map with a progress-listener cleanup hook; stale recovery now removes and cleans that in-memory record when it writes the terminal DB status.                                                                                                                                                                                                                                                                                        |
| SCHED-002        | high       | fixed, tested | Stale queue recovery failed the durable `running` row and released the queue's local `inFlight` listener, but did not cancel the corresponding live `SessionPrompt` loop. After recovery removed the durable `running` blocker, the next same-session queued task could be claimed while the old prompt loop still owned `SessionPromptState` for that session directory.                                              | Independent lane A traced `recover()` in `packages/opencorvus/src/scheduler/task-queue-service.ts`, `pending()` same-session exclusion, `SessionPrompt.loop()` directory-scoped state in `packages/opencorvus/src/session/loop.ts`, and `SessionPromptState.cancel()` in `packages/opencorvus/src/session/prompt/state.ts`. Regression `recovery releases stale in-flight prompt promises that stop producing activity` now asserts recovery calls `SessionPrompt.cancel(session.id, session.directory)`. Focused test, full `task-queue-service.test.ts` 19/19, and `packages/opencorvus` typecheck passed.                                                                                                                                                                                | `recover()` now fetches the owning session, uses a CAS-style `returning()` update so cancellation only follows a successful stale-row terminal write, calls `SessionPrompt.cancel` with the session directory, then releases the in-flight cleanup and publishes the visible terminal error.                                                                                                                                                                               |
| SCHED-003        | high       | fixed, tested | Terminal goal refill sync dispatched one orchestrator wake per terminal goal run in the same parent run. A task with two completed goals could therefore self-wake twice in one runtime observation even though one orchestrator turn can read all refill facts, directly wasting tokens and producing duplicate scheduler churn.                                                                                      | Independent lane A traced `syncTerminalGoalRefills()` in `packages/opencorvus/src/engine/runtime.ts` and the regression test that expected two `runTaskLoop` calls for two terminal goal runs in `runtime-goal-run-convergence.test.ts`. `runtime-goal-run-convergence.test.ts` 14/14 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Runtime now collects all unreported terminal goal refill facts first, starts or queues one task-loop wake, then records one durable fact per terminal goal run with the shared dispatch result.                                                                                                                                                                                                                                                                            |
| SCHED-004        | high       | fixed, tested | The passive restart/zombie test and task-api liveness comments still described the retired no-live-goal batch wake contract. That stale contract could mislead a future scheduler repair into restoring the old batch wake path instead of the selected terminal-goal refill facts.                                                                                                                                    | Independent lane A/Avicenna traced `zombie-task-revive.test.ts`, `task-api/index.ts`, `engine/runtime.ts`, and the selected `2026-06-19-goal-fifo-refill-scheduling-impact.md` contract. `zombie-task-revive.test.ts` 2/2, `runtime-goal-run-convergence.test.ts` 14/14, and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Updated the restart-resume test to assert terminal refill facts and negative assertions for `noLiveGoalWakeFingerprint` / `syncNoLiveGoalRuns`; updated task-api comments to describe the narrow runtime observer instead of no-live batch settlement.                                                                                                                                                                                                                     |
| SCHED-005        | high       | fixed, tested | Live `orchestrator_tool_ownership` used a process-local single-slot `queuedTaskEvents` map, so two operator messages sent while a child tool owned the task overwrote each other before the ownership completion wake. Persisting the root task messages did not rescue the lost wake because the orchestrator turn runs in its child session and `inject_operator_message` only sees the current event payload.       | Independent lane A traced `appendAndWakeTaskOperatorMessage()` → `dispatchTaskLoop()` → `queuedTaskEvents.set()` overwrite → `drainQueuedTaskEvent()` single-event launch → `inject_operator_message` single current-event read. Regression `operator message wakes queued behind live ownership drain in order` verifies two message IDs drain as two ordered orchestrator wakes after ownership completion.                                                                                                                                                                                                                                                                                                                                                                      | Operator-message queued wakes now persist as `engine_artifact` kind `queued_operator_wake` with `pending`/`drained` labels; the process-local queue remains only for non-operator volatile events. Drain peeks before consuming, respects live ownership, and launches one ordered wake per queued operator message.                                                                                                                                                       |
| AGENT-001        | high       | fixed, tested | `deep_research`, `workload_analysis`, and `visual_qa` wrapped terminal-tool agents but did not expose `continuation_artifact_id`, did not consistently capture the child session id, or did not pass `continuation` to `runAgentSession`. A terminal finalizer miss therefore either threw away same-session recovery context or would have emitted an invalid continuation tool name.                                 | Independent lanes B/Hubble and Confucius audited `orchestrator/tools.ts`, `research/agent.ts`, `goal-workload-analyst/agent.ts`, and `visual-qa/agent.ts`. Regressions `deep_research terminal finalizer miss returns same-session continuation`, `workload_analysis terminal finalizer miss returns same-session continuation`, and `visual_qa terminal finalizer miss returns same-session continuation` passed, along with the existing `frontend_research` continuation test. `orchestrator-tool-descriptions.test.ts` and package typecheck passed.                                                                                                                                                                                                                                    | Wired `continuation_artifact_id`, `continuationFromArtifact`, `continuationResultForTerminalFinalizerMiss`, `runnerSessionID`, and agent `continuation` through those three wrappers. Corrected continuation tool names for `deep-research`, `goal-workload-analyst`, and `intent-analysis`.                                                                                                                                                                               |
| AGENT-002        | high       | fixed, tested | `frontend_design` accepts `continuation_artifact_id` together with fresh visual scope fields (`urls`, `figma_url`, `materials`) and then silently drops the fresh scope in continuation mode.                                                                                                                                                                                                                          | Independent lane B traced `FrontendDesignInputSchema` and continuation execution in `packages/opencorvus/src/orchestrator/tools.ts`; `frontend_research` already has an exactly-one guard for the comparable URL/continuation case. `bun test packages/opencorvus/test/orchestrator/orchestrator-tool-descriptions.test.ts` passed 8/8.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Added schema rejection for continuation plus fresh visual scope fields and regression assertions in `orchestrator-tool-descriptions.test.ts`.                                                                                                                                                                                                                                                                                                                              |
| AGENT-003        | high       | fixed, tested | `frontend_research.source_urls` advertised multiple URLs, but the evidence/output/prompt contract is single-page and only consumed the first URL.                                                                                                                                                                                                                                                                      | Independent lane C traced `source_urls` through `orchestrator/tools.ts`, `research/agent.ts`, `webpage-prd-evidence.ts`, `webpage-evidence.ts`, `research/output-tools.ts`, `research/schema.ts`, and `research/prompt-section.ts`. `bun test packages/opencorvus/test/orchestrator/orchestrator-tool-descriptions.test.ts` passed 8/8.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Constrained `source_urls` to exactly one URL and updated the tool description to call `frontend_research` separately for additional pages.                                                                                                                                                                                                                                                                                                                                 |
| AGENT-004        | high       | fixed, tested | Deep research and frontend research evidence refs merged as bare IDs, so duplicate `ev_1` IDs across briefs became ambiguous but still validated.                                                                                                                                                                                                                                                                      | Independent lane C traced `research/prompt-section.ts`, `requirements/agent.ts`, `requirements/output-tools.ts`, and `research/schema.ts`. Focused requirements, research persistence, architect output-tools tests, and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Downstream research evidence refs are now source-qualified as `deep_research:<id>` or `frontend_research:<id>`. Requirements and Architect validation reject bare ambiguous refs.                                                                                                                                                                                                                                                                                          |
| ORCH-001         | high       | fixed, tested | `restart_from_stage("executor")` exposes deleted scheduler/tool action names such as `submit_execution` / `create_run` as continuation facts.                                                                                                                                                                                                                                                                          | Independent lane C traced `orchestrator/scheduler.ts`, `orchestrator/tools.ts`, `agent/agent.ts`, and `test/orchestrator/scheduler.test.ts`. `bun test packages/opencorvus/test/orchestrator/scheduler.test.ts` passed 7/7.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Changed executor restart continuation facts to current tool semantics (`build` with active plan, `architect` without active plan) and removed fake `nextAction(runID)` formatting.                                                                                                                                                                                                                                                                                         |
| GOAL-001         | high       | fixed, tested | Goal build background finalization errors can be swallowed, leaving a permanent live `goal_run` that blocks retry and terminal refill.                                                                                                                                                                                                                                                                                 | Independent lane A traced goal build async finalization in `orchestrator/tools.ts`, persistence in `engine/persist.ts`, runtime refill in `engine/runtime.ts`, and live-run dedupe. Targeted tests `goal build returns started after binding goal_run and finalizes in the background` and `goal build background finalize failure does not leave a live goal_run` both passed.                                                                                                                                                                                                                                                                                                                                                                                                             | If `finalizeBuildAttempt` throws while the goal_run is still live, the build tool now marks that goal_run failed and records the finalization error instead of leaving it running.                                                                                                                                                                                                                                                                                         |
| GOAL-002         | high       | fixed, tested | `modify_goal` refused live build tool ownership but did not refuse a durable live `goal_run` when no ownership artifact remained. An async build can return `started` after opening a live `goal_run`; mutating the goal contract before that attempt reaches terminal status invalidates the child session's contract mid-run.                                                                                        | Independent lane A traced async build ownership closure in `packages/opencorvus/src/orchestrator/tools.ts`, live `goal_run` persistence in `packages/opencorvus/src/engine/persist.ts`, and the missing durable live-run check in `modify_goal`. Regression added in `tools.test.ts` seeds a running `goal_run` without live ownership and requires `modify_goal` to leave the goal unchanged. `tools.test.ts -t "contract mutation"` 2/2 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                                                                                       | `modify_goal` now checks the same durable live `goal_run` source used by build duplicate-dispatch protection before applying contract updates.                                                                                                                                                                                                                                                                                                                             |
| REVIEW-003       | low        | human review  | Queued task path may report `started` even when `advanceQueue(cwd)` cannot start due to an active owner.                                                                                                                                                                                                                                                                                                               | Independent lane A traced `engine/queue.ts`; production impact depends on terminal refill path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Review after current high-confidence fixes.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| REVIEW-004       | low        | human review  | `findActiveRunForTask` name implies active but implementation returns newest run.                                                                                                                                                                                                                                                                                                                                      | Independent lane A traced `engine/store.ts`; key runtime callers have ID guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Rename/contract cleanup only after call-site audit.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| REVIEW-005       | low        | human review  | Terminal goal refill wake fact insertion is not obviously atomic across public concurrent `syncRun` / `syncTask` calls.                                                                                                                                                                                                                                                                                                | Independent lane A traced `engine/runtime.ts`; production concurrency not confirmed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Needs concurrency model confirmation.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| REVIEW-006       | low        | human review  | Integrity direct evidence tools do not include a dedicated `frontend_research` section.                                                                                                                                                                                                                                                                                                                                | Independent lane C traced integrity team/acceptance tools; visual QA already consumes frontend research.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Product/reviewer responsibility clarification needed.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| REVIEW-007       | low        | human review  | `frontend-design` static/session tool lists differ.                                                                                                                                                                                                                                                                                                                                                                    | Independent lane C traced runtime injection and static list; difference may be intentional.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No code change without contract decision.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| REVIEW-008       | low        | human review  | `continuationToolName` contains names not matching all current orchestrator tool names.                                                                                                                                                                                                                                                                                                                                | Independent lane C noted current active call sites only cover existing fixed agents.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Revisit during broad finalizer recovery repair.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| TEST-001         | high       | fixed, tested | Several older `tools.test.ts` goal-build tests seeded tasks under synthetic project ids while running inside `Instance.provide()`, so filtered runs such as `bun test --timeout 15000 packages/opencorvus/test/orchestrator/tools.test.ts -t "goal build"` failed with `Task not found in current project` before reaching the behavior under test.                                                                    | During GOAL-001 verification, the wider name-filtered subset exposed stale fixtures that passed `projectID` to `insertWorkflowTaskWithGoal()` instead of the current `Instance.project.id`; retry attachment assertions also expected the old synthetic attachment URL prefix after the attachment was correctly written under the current project. The focused filtered run now passes 18/18.                                                                                                                                                                                                                                                                                                                                                                                              | Updated only affected test fixtures to bind their task/attachments to `Instance.project.id` and avoid duplicate project insertion. Production `taskPrimaryProjectRoot` remains strict.                                                                                                                                                                                                                                                                                     |
| AGENT-005A       | high       | fixed, tested | `fact_check` is a terminal-tool agent but did not expose same-session recovery when it ended without `report_fact_check_result`. A naive retry would start a fresh verifier session, while a naive continuation could retarget the latest assistant message instead of the failed attempt's original snapshot.                                                                                                         | Sartre independently audited the `fact_check` wrapper and confirmed `StageContinuationStage` already includes `fact-check`, `continuationToolName()` maps it to `fact_check`, and `FactCheckAgent.run()` already has terminal tool `report_fact_check_result`. Regression `[I] terminal finalizer miss persists tool_error before same-session continuation` verifies first-call `tool_error` persistence, stage-continuation artifact scope, second-call continuation, and final completed-cache behavior. `orchestrator-tool-descriptions.test.ts`, `fact-check/orchestrator-tool.test.ts`, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                               | Added exact fresh-vs-continuation schema, persisted the original snapshot in `normalized_stage_input`, made continuation-only calls use that snapshot as the single source of target data, passed `AgentSessionContinuation` into `FactCheckAgent.run`, and preserved the existing synthetic `tool_error` artifact before returning the continuation pointer.                                                                                                              |
| AGENT-005B       | high       | fixed, tested | `integrity` is a terminal-tool agent but did not expose same-session recovery when it ended without `submit_integrity_consensus`. A schema-only fix would have been wrong because integrity also owns task-level orchestrator tool ownership while the child session runs.                                                                                                                                             | Wegener independently audited `integrity` and confirmed `StageContinuationStage` includes `integrity`, but the `integrity` tool only exposed `reason`, `reviewIntegrity` did not accept `continuation`, and ownership/session lifecycle must be reopened for continuation because `runAgentSession` does not call `onSessionCreated` when resuming an existing session. Regression `integrity terminal finalizer miss returns same-session continuation with ownership closed` verifies continuation artifact scope, no missing-finalizer `integrity_attempt`, one final artifact after continuation, and no live ownership leaks. `team-agent.test.ts`, orchestrator schema tests, the targeted orchestrator wrapper test, `packages/opencorvus` typecheck, and `git diff --check` passed. | Added `continuation_artifact_id`, passed `AgentSessionContinuation` through `reviewIntegrity`, reopened ownership for resumed sessions, closed ownership on both finalizer-miss and completed continuation paths, and persisted `active_spec_snapshot_id` / phase / goal ids in `normalized_stage_input` with a hard scope check before resuming.                                                                                                                          |
| AGENT-008        | high       | fixed, tested | `fact_check` emitted `workflow.step.updated` running for workflows containing a `tool: "fact_check"` step, but none of its return paths emitted completed or failed. Custom workflow board replay could therefore leave the fact-check step visually running after cached, rejected, continuation, tool-error, or successful exits because `taskStepStatusByTool()` has no persistent `fact_check` projection.         | Independent lane B traced `trackStepStart("fact_check")` in `packages/opencorvus/src/orchestrator/tools.ts`, the missing `trackStepComplete("fact_check")`, and `findStepByTool()` custom workflow binding. `fact-check/orchestrator-tool.test.ts` now verifies live workflow events for normal completed, cached completed, target-streaming rejection failed, and terminal-finalizer-miss continuation failed. Full fact-check orchestrator tool test 10/10 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                                                                   | Wrapped the fact-check tool body in a scoped `try/finally` that emits `trackStepComplete("fact_check", failed)` on every exit. The step defaults to failed; cached and completed agent outcomes set it to completed before returning.                                                                                                                                                                                                                                      |
| AGENT-009        | high       | fixed, tested | `renderFrontendDesignHandoffReference()` returned a non-empty "Frontend Design Public Report" path block before checking whether any canonical `frontend_design` decision-log handoff entry existed. Downstream agents treated that non-empty string as real task-specific frontend_design context, producing phantom handoffs and misleading build overlays from path shells.                                         | Independent lane C traced `packages/opencorvus/src/frontend-design/handoff.ts`, orchestrator/build consumers that use non-empty handoff text as a context signal, and `packages/opencorvus/src/build/prompt-context.ts` overlay activation. Regressions verify no decision-log handoff entries produce an empty string, while real `public_report` entries still produce relative/absolute paths. `frontend-design/handoff.test.ts`, `pipeline/decision-log-bundle.test.ts`, `build-agent/prompt-context.test.ts`, `orchestrator/build-goal-reference.test.ts`, and `packages/opencorvus` typecheck passed.                                                                                                                                                                                 | Moved decision-log handoff-key lookup ahead of path rendering. The renderer now returns `""` until the canonical `frontend_design` phase contains at least one handoff key; path rendering and absolute-path validation only happen after real handoff evidence exists.                                                                                                                                                                                                    |
| AGENT-010        | high       | fixed, tested | `submit_workload_analysis` finalized a workload report when only one goal had a brief, even though the Goal Workload Analyst prompt and tool purpose require one `register_workload_brief` per architect goal. The PASS response merely noted uncovered goals, so downstream build prompts could receive partial sizing guidance without a terminal error.                                                             | Independent lane C traced the prompt requirement in `packages/opencorvus/src/goal-workload-analyst/prompt.ts`, the partial validator in `packages/opencorvus/src/goal-workload-analyst/output-tools.ts`, and build-time per-goal injection in `packages/opencorvus/src/orchestrator/tools.ts`. Regression added in `goal-workload-analyst/output-tools.test.ts` blocks a two-goal submission with only `gol_a` and requires `gol_b` before PASS. `goal-workload-analyst/output-tools.test.ts` 6/6 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                               | `validate()` now compares collector briefs against the full known-goal set, `isReadyToFinalize()` shares the same predicate, and PASS no longer contains a "without a brief" note because missing briefs are terminal blockers.                                                                                                                                                                                                                                            |
| AGENT-011        | high       | fixed, tested | `deep_research` accepted fresh `source_urls` together with `continuation_artifact_id`, persisted both in the normalized stage input, then resumed the existing child session with `sourceUrls: undefined`. Fresh URLs were therefore silently dropped in continuation mode.                                                                                                                                            | Independent lane C traced the input schema and execution in `packages/opencorvus/src/orchestrator/tools.ts`: schema exposed both fields without mutual exclusion, while the runner explicitly ignores `source_urls` when `continuation` is present. Regression added in `orchestrator-tool-descriptions.test.ts` rejects `continuation_artifact_id` plus non-empty `source_urls`. `orchestrator-tool-descriptions.test.ts` 8/8 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                                                                                                                                                  | Added a single `DeepResearchInputSchema` with a schema-level data-integrity rejection for continuation plus fresh source URLs; the tool now uses that schema as its only input contract.                                                                                                                                                                                                                                                                                   |
| AGENT-012        | high       | fixed, tested | The `AGENT-009` phantom frontend-design handoff fix still treated `reference_artifacts` alone as enough to render a "Frontend Design Public Report" path block. That path block activates build prompt context as if a public report exists, even though `reference_artifacts` is a Visual QA/reference-parity input and may exist without a finalized public report.                                                  | Independent lane C traced `FRONTEND_DESIGN_HANDOFF_KEYS` including `reference_artifacts`, `renderFrontendDesignHandoffReference()` rendering public-report paths for any handoff key, and build prompt context treating non-empty handoff text as `frontend-design-handoff` / `visual-reference` context. Regression added in `frontend-design/handoff.test.ts` seeds only `reference_artifacts` and requires an empty renderer result. The frontend-design handoff, decision-log bundle, build prompt context, build goal reference suites 40/40 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                               | `renderFrontendDesignHandoffReference()` now requires a canonical `public_report` entry before rendering public-report paths. `reference_artifacts` remains available through Visual QA's dedicated frontend-design context.                                                                                                                                                                                                                                               |
| AGENT-013        | high       | fixed, tested | `visual_qa` and `deep_research` accepted fresh prompt-only scope fields together with `continuation_artifact_id`, but continuation mode resumes an existing child session and skips the normal fresh prompt path. Those fresh fields were therefore silently discarded while the tool call looked accepted.                                                                                                            | Independent lane B traced `VisualQaInputSchema`, `DeepResearchInputSchema`, the visual QA and deep research wrappers in `packages/opencorvus/src/orchestrator/tools.ts`, and `runAgentSession` continuation behavior in `packages/opencorvus/src/agent/runner.ts`. Regression assertions were added to `orchestrator-tool-descriptions.test.ts` for `visual_qa` continuation plus `focus` / `app_url` / `preview_command`, and `deep_research` continuation plus `target_deliverable` / `source_urls` / `focus`. `orchestrator-tool-descriptions.test.ts` 8/8 and `packages/opencorvus` typecheck passed.                                                                                                                                                                                   | Schema validation now treats continuation as an exclusive same-session recovery mode for those tools and rejects mixed fresh scope fields.                                                                                                                                                                                                                                                                                                                                 |
| AGENT-014        | high       | fixed, tested | The `frontend_research` tool contract now requires one source URL per call, but downstream prompt consumers only read `findLatestFrontendResearchBriefArtifact()`. Multiple single-page calls therefore hid all but the newest page. A naive concatenation would also reintroduce ambiguous `frontend_research:ev_1` refs across page-local evidence ids.                                                              | Independent lane C traced `tools.ts` source URL contract, `engine/store.ts` latest-only frontend brief lookup, `research/prompt-section.ts` prompt injection, read_context projection, and Visual QA frontend research context. Regressions cover two active frontend research briefs with the same local `ev_1` id and assert Requirements, Architect, Build, Visual QA, and read_context include both pages. `research/persist-describe.test.ts`, `visual-qa/context.test.ts`, Requirements/Architect evidence-ref tests 64/64 combined, `tools.test.ts -t "read_context surfaces multiple frontend research briefs"` 1/1, and `packages/opencorvus` typecheck passed.                                                                                                                    | Added a list API for validated frontend research brief artifacts, changed downstream prompt renderers to aggregate non-stale briefs with a deterministic cap, changed read_context to summarize multiple frontend briefs, fixed its stale-check import path, and scoped frontend research evidence refs as `frontend_research:<artifact_id>:<local_evidence_id>`.                                                                                                          |
| AGENT-015        | high       | fixed, tested | Generic same-session stage continuation stored `normalized_stage_input` / `input_digest` but most tool wrappers did not compare the stored digest with the current durable task/spec/goal scope before resuming. A stale continuation artifact could therefore append to an old child session after the active spec or goal graph changed.                                                                             | Independent lane B traced `stage-continuation.ts` storage, `continuationFromArtifact()` in `orchestrator/tools.ts`, and architect / workload / visual QA wrappers that previously passed only stage + finalizer. Regressions mutate the active requirements spec or workload goal graph after a terminal finalizer miss and require the continuation call to fail before the mocked agent is invoked. `tools.test.ts -t "continuation"` passed 8/8, `orchestrator-tool-descriptions.test.ts` 8/8, `fact-check/orchestrator-tool.test.ts -t "terminal finalizer miss"` 1/1, and `packages/opencorvus` typecheck passed.                                                                                                                                                                      | Added stable task/spec/goal scope fingerprints, made `continuationFromArtifact()` optionally compare the expected digest, removed `continuation_artifact_id` from generic normalized stage scope, and kept pointer human-readable reasons separate from scope digests.                                                                                                                                                                                                     |
| AGENT-016        | high       | fixed, tested | `fact_check` accepted any `target_session_id` and persisted the resulting attempt under the current task, so an orphan session or another task's worker session could poison current-task read_context / integrity replay.                                                                                                                                                                                             | Independent lane B traced `FactCheckInputSchema`, `Session.snapshotLatestAssistant()`, `taskIDForSession()`, and fact-check persistence. Regressions now create a current task and a separate task, pass the other task's target session, and assert the tool rejects before dispatching `FactCheckAgent` or writing a fact-check attempt. Full `fact-check/orchestrator-tool.test.ts` 12/12, orchestrator tool schema tests 8/8, and package typecheck passed.                                                                                                                                                                                                                                                                                                                             | Fresh fact-check calls now require `taskIDForSession(target_session_id) === current taskID`; fixture target sessions are real child sessions of their owning task root.                                                                                                                                                                                                                                                                                                    |
| AGENT-017        | high       | fixed, tested | `fact_check.target_agent` was a caller-controlled second source for the target agent while the target session already has a durable `session.kind`; idempotency also ignored `target_agent`, so a wrong first call could cache a mislabeled scope.                                                                                                                                                                     | Independent lane B traced `target_agent` schema, `resolvedArgs`, prompt scope echo, and fact-check idempotency lookup. Regression creates a `requirements` target session and passes `target_agent: "build"`, requiring a rejection before agent dispatch. The happy path omits `target_agent` and still records `build` derived from `session.kind`.                                                                                                                                                                                                                                                                                                                                                                                                                                       | `target_agent` is now optional consistency input only. The host derives persisted `target_agent` from `sessionRole(target_session_id)` and rejects caller assertions that disagree with the session kind.                                                                                                                                                                                                                                                                  |
| AGENT-018        | high       | fixed, tested | `describeTask()` projected `frontend_template_path` and `source_manifest_path` for any `frontend_design` decision-log entry, including abort-only evidence with no materialized public report or manifest. That gave downstream orchestrator/build context a plausible path shell for design artifacts that did not exist.                                                                                             | Independent lane C traced `describeFrontendDesignHandoff()` and `renderFrontendDesignHandoffDesc()` in `packages/opencorvus/src/engine/describe.ts`. Regression seeds only `abort_no_visual_input` and requires no path fields, while the partial public-report test still shows only the public-report path. `describe-frontend-design-handoff.test.ts` 3/3, related frontend-design/build handoff suites 40/40, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                           | Frontend-design handoff paths are now conditional on the corresponding durable keys: `public_report` gates `frontend_template_path`; `evidence_source_manifest` gates `source_manifest_path`. Abort-only and partial handoffs still expose present/missing keys without inventing files.                                                                                                                                                                                   |
| AGENT-019        | high       | fixed, tested | After `frontend_research.source_urls` became exactly one URL per fresh call, role/tool/core prompts still described frontend_research as a task-scope single-shot that should be dispatched once and not used after any brief exists. That contradicted the multi-page contract and could make the orchestrator skip additional source pages.                                                                          | Second-round lane C prompt audit plus grep found stale wording in `role-contract.ts`, `orchestrator/tools.ts`, `frontend-research-core.txt`, and `orchestrator-core.txt`. Regression checks now require source-page-scoped wording, separate calls for additional pages, and no old `dispatch once for the relevant webpage investigation scope` wording in `src`. Focused core-prompt, role-contract, and orchestrator tool description/schema tests passed.                                                                                                                                                                                                                                                                                                                               | Frontend research is now described as one fresh session per source page URL. The no-repeat rule is scoped to the same page brief; additional source pages still require separate frontend_research calls.                                                                                                                                                                                                                                                                  |
| AGENT-020        | high       | fixed, tested | `deep_research` persisted multiple active `research_brief` artifacts but downstream consumers read only the latest one. Aggregating naively would collide local evidence ids such as `ev_1` because deep refs were `deep_research:ev_1` without an artifact id.                                                                                                                                                        | Popper's independent audit traced latest-only calls in `engine/store.ts`, `research/prompt-section.ts`, Requirements/Architect prompt injection, `read_context`, and `describeTask`. Regressions now seed two active deep briefs sharing local evidence id `ev_1` and assert Requirements/Architect allowed refs, read_context, task description, and prompt JSON include both artifact-qualified refs. `research/persist-describe.test.ts`, `tools.test.ts -t "read_context surfaces multiple..."`, Requirements evidence tests, Architect output tests, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                                                                   | Added `listResearchBriefArtifacts`, made deep research prompt/evidence helpers consume a bounded active list, changed deep refs to `deep_research:<artifact_id>:<local_evidence_id>`, projected multiple deep briefs in read_context and task descriptions, and removed dead singular non-stale helper exports.                                                                                                                                                            |
| AGENT-021        | high       | fixed, tested | `visual_qa` and `integrity` same-session continuations only compared task/spec/goal identity, not the review evidence actually fed to those agents. A stale child session could therefore resume after newer frontend_design, visual_qa, build, requirement-status, replay, visual-evidence, or integrity-attempt material appeared.                                                                                   | Euclid's second-round audit traced `continuationFromArtifact()`, `integrityContinuationFromArtifact()`, `visual_qa` context construction, and `integrity` replay/review context construction in `packages/opencorvus/src/orchestrator/tools.ts`. Regressions mutate frontend_design or visual_qa decision-log evidence after a terminal finalizer miss and assert the stale continuation is rejected before `VisualQaAgent.analyze` / `reviewIntegrity` is called. `tools.test.ts -t "stale.*evidence"`, `tools.test.ts -t "(terminal finalizer miss returns same-session continuation                                                                                                                                                                                                      | stale.\*evidence)"`, `tools.test.ts -t "continuation"`, `packages/opencorvus`typecheck, and`git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                      | Added a bounded evidence snapshot digest map to continuation normalized input. `visual_qa` includes frontend_design/frontend_research/build/prior visual_qa/latest visual evidence/latest integrity attempt; `integrity` includes acceptance, requirement/status, replay, goal_run, frontend_design, visual_qa, latest visual evidence, and latest integrity attempt. Integrity still ignores continuation `reason` for identity because the recovery call naturally uses a different reason string. |
| AGENT-022        | high       | fixed, tested | `visual_qa` consumed multiple frontend research briefs but rendered page-local evidence ids such as `ev_ref` without the owning artifact id. Two page briefs with the same local id were indistinguishable in the QA prompt even though downstream frontend research refs had already become artifact-qualified.                                                                                                       | Peirce's independent research/design lane traced `listFrontendResearchBriefArtifacts()` through `visual_qa` context creation and found `renderVisualQaFrontendResearchContext()` accepted only bare `ResearchBrief` payloads. Regressions now assert reference image ids and every webpage contract evidence list render as `frontend_research:<artifact_id>:<local_evidence_id>` for multiple page briefs. `visual-qa/context.test.ts`, `visual-qa/strict-reference-fidelity.test.ts`, `research/persist-describe.test.ts`, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                                                                                                | Visual QA frontend research context now receives `{ artifactID, brief }` entries, prints the artifact id per page, and scopes all frontend research evidence refs before handing them to the QA agent.                                                                                                                                                                                                                                                                     |
| AGENT-023        | high       | fixed, tested | `describeTask()` projected only the latest `frontend_research` brief even after frontend research became one fresh session per source page. The orchestrator task description could therefore hide earlier required page evidence and make the next turn believe the multi-page investigation was incomplete or unavailable.                                                                                           | Peirce's independent research/design lane traced `describeTaskFromRow()` still calling `findLatestFrontendResearchBriefArtifact()` while Requirements, Architect, Build, and Visual QA had already moved to list-based frontend research consumption. Regression now seeds two frontend research briefs and asserts the task description includes both artifact ids, both source URLs, and both summaries in newest-first order. `research/persist-describe.test.ts`, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                       | Task descriptions now use `listFrontendResearchBriefArtifacts()` with the same compact cap as deep research, render numbered frontend research sections for multiple briefs, and include `source_urls` for traceability.                                                                                                                                                                                                                                                   |
| AGENT-024        | high       | fixed, tested | Several orchestrator tool schemas accepted unknown fields and let Zod strip them. The highest-risk case was `build({ goal_id, request, directBuildIntent: "modify_files" })`: the misspelled goal id was removed, leaving a valid task-level direct build input that could run outside the intended per-goal scope.                                                                                                    | Anscombe's schema/continuation audit traced `build.inputSchema` and continuation-capable stage schemas (`requirements`, `architect`, `workload_analysis`, `deep_research`, `visual_qa`, `integrity`) that were not strict. Regression asserts `goal_id` is rejected instead of stripped, and every affected stage schema rejects an otherwise valid input with an unknown `accidental_scope` field. `orchestrator-tool-descriptions.test.ts`, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                               | Added strict Zod schemas for the affected stage/review/build tool inputs. This is a data-shape integrity constraint, not a host-side routing gate; execution behavior is unchanged for valid inputs.                                                                                                                                                                                                                                                                       |
| AGENT-025        | high       | fixed, tested | `frontend_design.urls` allowed multiple non-Figma live page URLs, but the host-prepared webpage clone evidence path is a single task-scoped primary package and `ensureLiveWebpageEvidence()` selects one source URL. The schema therefore implied all live URLs received clone evidence while downstream handoff artifacts could only name one primary source package.                                                | Peirce's research/design lane traced `frontend_design.urls` through URL screenshot materialization, `ensureLiveWebpageEvidence({ urls })`, the single `evidence.url` decision-log entry, and task-scoped `fd/webpage-evidence` / `fd/web-clone-source` paths. Regression asserts one live URL remains valid and two non-Figma live URLs are rejected. `orchestrator-tool-descriptions.test.ts`, `packages/opencorvus` typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                             | `frontend_design` now accepts at most one non-Figma live/page URL per fresh call while preserving Figma/material materialization. This keeps the primary clone-evidence package single-source instead of pretending multiple live pages share one complete evidence bundle.                                                                                                                                                                                                |
| AGENT-026        | high       | fixed, tested | Reusing an already consumed, already claimed, claim-failed, missing, or wrong-contract `continuation_artifact_id` could surface as a raw orchestrator tool error or wrapper failure instead of a visible recovery result. In the consumed and claim-failed cases this also made the caller ambiguous about whether a fresh child session had started.                                                                  | Anscombe's continuation audit traced `claimStageContinuationRequest()`, `continuationFromArtifact()`, the generic continuation wrappers, and the custom `integrity` / `fact_check` continuation paths. Regression `frontend_research unavailable continuation artifacts return visible recovery results` seeds consumed and claim-failed continuation artifacts, asserts clear state/details in the tool result, and asserts the child frontend research agent is not invoked. `tools.test.ts -t "frontend_research .*continuation"`, `fact-check/orchestrator-tool.test.ts -t "continuation                                                                                                                                                                                                | terminal finalizer miss"`, `packages/opencorvus`typecheck, and`git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                                                   | Added a shared unavailable-continuation result path before claiming artifacts. Valid pending artifacts still use the existing claim path; unavailable artifacts now return an orchestrator-visible result with state, detail, and next action, and wrappers close their workflow step as completed/handled instead of starting a new worker.                                                                                                                                                         |
| SCHED-006        | high       | fixed, tested | Durable `queued_operator_wake` rows could survive a process restart with no drain trigger. The original drain path only ran when a live orchestrator tool ownership completed in the same process, so pending operator wakes that already had clear ownership after restart could remain inert until another unrelated event.                                                                                          | Kepler's scheduler lane traced durable queued wake rows in `engine/queue.ts` and the liveness observer in `task-api/index.ts`. Regression `startup liveness drains pending durable operator wakes when ownership is already clear` seeds a pending durable wake, starts liveness, and asserts exactly one queued task-loop launch. `queued-wake-ownership-drain.test.ts`, `server/replan-routes.test.ts`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                 | Added `drainPendingQueuedOperatorWakes()` and invoked it from the existing liveness scheduler after runtime monitoring. The repair reuses the durable queue source; it does not introduce a second scheduler path.                                                                                                                                                                                                                                                         |
| SCHED-007        | high       | fixed, tested | Retry and replan operator intents queued behind live orchestrator tool ownership were still process-local because they do not carry `operatorMessage.messageID`. A restart or ownership wait could therefore lose the user's explicit retry/replan wake even after operator messages had been made durable.                                                                                                            | Kepler traced `appendAndWakeTaskOperatorMessage()`, retry/replan route events, `enqueueTaskEvent()`, and the durable operator wake artifact shape. Regression `retry and replan operator intents queued behind live ownership are durable and ordered` verifies both intents persist and drain in order after ownership clears. `queued-wake-ownership-drain.test.ts`, `server/replan-routes.test.ts`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                    | `enqueueTaskEvent()` now persists events with `operatorIntent` as `queued_operator_wake` artifacts as well as operator messages. Message IDs remain the de-dupe key for real messages; intent-only wakes get unique wake IDs.                                                                                                                                                                                                                                              |
| SCHED-008        | high       | fixed, tested | The durable operator-wake liveness drain added for SCHED-006 could bypass cwd queue serialization when the target task was still `queued`. It checked only task-local tool ownership, then launched `runTaskLoop()` directly; `runTaskLoopInner()` marks queued tasks active, so a queued task with a pending wake could start while another same-cwd task was still active.                                           | Descartes traced `dispatchTaskLoop()` queued-event persistence, `claimNextForCwd()` active-sibling guard, `drainPendingQueuedOperatorWakes()`, and `runTaskLoopInner()` queued-to-active marking. Regression `durable operator wakes do not bypass queued same-cwd task serialization` seeds active task A plus queued task B with a durable wake, asserts liveness drain leaves B queued and wake pending while A is active, then completes A and asserts B starts once with the original wake. `queued-wake-ownership-drain.test.ts`, full `queue.test.ts`, package typecheck, and `git diff --check` passed.                                                                                                                                                                             | `drainQueuedTaskEvent()` is now async and, when the target task is still queued, delegates to `advanceQueue(cwd)` instead of consuming the wake directly. The single queue-claim SQL remains the only path that can start queued work.                                                                                                                                                                                                                            |
| AGENT-027        | high       | fixed, tested | Live webpage evidence detection was case-sensitive in frontend design, frontend research, and direct `webpage_extract` validation. URLs such as `HTTPS://...` could be treated as non-webpage materials in one path and invalid in another, so the evidence runner might never capture the source page even though the URL is semantically valid.                                                                      | Zeno's frontend evidence lane traced `ensureLiveWebpageEvidence()`, `prepareWebpagePrdEvidence()`, and `webpage_extract`. Regressions cover uppercase HTTP(S) in shared URL helpers, frontend-design evidence, frontend-research evidence, and direct extraction validation. `web-url.test.ts`, `webpage-evidence.test.ts`, `webpage-prd-evidence.test.ts`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                                               | Added one shared `isHttpWebpageUrl()` helper and replaced the three case-sensitive checks. This removes a duplicated URL predicate without adding fallback behavior.                                                                                                                                                                                                                                                                                                       |
| AGENT-028        | high       | fixed, tested | `create_frontend_skeleton_project` promised a bounded task-scoped evidence skeleton but accepted arbitrary `outputDir`, allowing frontend-design to generate and then consume a source clone outside the canonical `fd/frontend-design-skeleton` surface. That made the tool output non-traceable and undermined the visual-skeleton-first workflow.                                                                   | Zeno traced `skeleton-project-tool.ts` against `ProjectRuntimePaths.frontendDesignPaths()` and the tool's own bounded-output instructions. Regression `task-scoped skeleton project tool rejects output outside frontend-design-skeleton evidence` verifies task-scoped calls reject non-canonical output. `frontend-design/prompt.test.ts -t "skeleton project tool"`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                                   | Output now resolves to the canonical task-scoped `frontend-design-skeleton` directory when `taskID` is present, or the project-root `frontend-design-skeleton` directory for non-task usage. Arbitrary output paths are rejected instead of silently creating a second evidence source.                                                                                                                                                                                    |
| AGENT-029        | high       | fixed, tested | Second-round continuation/schema audit found remaining cases where the caller could still observe a raw or misleading contract: stale pending continuation scope mismatches still threw in generic wrappers, unavailable continuation exits marked workflow steps failed rather than handled, `fact_check` continuation accepted fresh `target_agent`, and `analyze_intent` stripped unknown continuation-like fields. | Socrates traced generic continuation wrappers, custom `fact_check` / `integrity` paths, workflow step closure, and `analyze_intent.inputSchema`. Regressions assert `scope_mismatch` is a visible tool result for workload, visual QA, integrity, and architect stale continuations; fact-check continuation rejects `target_agent`; analyze_intent rejects unknown fields; unavailable continuation workflow events close as completed/handled. `tools.test.ts -t "continuation"`, `orchestrator-tool-descriptions.test.ts`, `fact-check/orchestrator-tool.test.ts -t "terminal finalizer miss                                                                                                                                                                                             | continuation"`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                                                                          | `stageContinuationUnavailableResult()` now handles stale scope as a visible recovery result before child agents start, affected workflow steps close as completed/handled for unavailable recovery artifacts, `fact_check` continuation mode rejects all fresh target fields, and `analyze_intent` is strict.                                                                                                                                                                                        |
| AGENT-030        | high       | fixed, tested | Failed `analyze_intent` live events showed failed, but reload projection marked the step completed because `projectTaskSteps()` treated any `intent_analysis` decision-log entry as success, including `abort_intent_analysis_failed`.                                                                                                                                                                                 | Pauli traced `trackStepComplete("analyze_intent", failed=true)`, the abort decision-log entry, and `workflow.ts` projection. Regression `projects failed analyze_intent abort decisions as failed after reload` seeds only `phase=intent_analysis,key=abort_intent_analysis_failed` and asserts `projectTaskSteps().analyze_intent.status === "failed"`. Full `workflow-integrity-step.test.ts`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                          | `taskStepStatusByTool("analyze_intent")` now checks abort keys first and projects them as failed; normal intent-analysis entries still project completed.                                                                                                                                                                                                                                                                                                                  |
| AGENT-031        | high       | fixed, tested | `integrity` is a pipeline task step but emitted no live `workflow.step.updated` events. A terminal-finalizer miss could therefore return a continuation and close ownership while the overlay never saw `integrity` running or terminal; reload also stayed pending because no `integrity_attempt` exists.                                                                                                             | Pauli traced the pipeline integrity step, `integrity.execute`, terminal-finalizer miss handling, and persistent post-build `integrity_attempt` projection. Regression in `integrity terminal finalizer miss returns same-session continuation with ownership closed` now passes a pipeline workflow and asserts integrity events `running, failed, running, failed` for pre-build miss plus pre-build continuation pass. Continuation subset, full workflow projection tests, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                             | `integrity.execute` now wraps `runIntegrityReview()` with `trackStepStart/Complete("integrity")`. Only post-build pass marks the pipeline gate completed; blocked, continuation, non-pass, and pre-build advisory exits close the live step as failed instead of leaving it invisible/running.                                                                                                                                                                             |
| AGENT-032        | high       | fixed, tested | A `stage_continuation_request` could be durably stuck in `claimed` forever if the process died after `claimStageContinuationRequest()` wrote `claimed_at` but before `runAgentSession` appended and consumed the continuation user message. Later calls returned `state=claimed` / "wait" with no worker able to make progress.                                                                                        | Pauli traced `claimStageContinuationRequest()` writing before append, normal consumed/claim-failed transitions in `agent/runner.ts`, and `stageContinuationUnavailableResult()` returning wait for any claimed artifact. Regression extends `frontend_research unavailable continuation artifacts return visible recovery results` with a previous-process claimed artifact and asserts the tool returns `claim_failed`, mentions the previous owner, emits handled workflow events, and does not call the child agent. Continuation-focused orchestrator tests, runner continuation tests, package typecheck, and `git diff --check` passed.                                                                                                                                               | Continuation claims are now stamped with `claim_owner = processOwner()`. When a later process observes an unconsumed claim from a different or unstamped owner, it deterministically marks the artifact `claim-failed` and returns the existing visible recovery result. Same-process claimed artifacts still return `claimed` so concurrent in-process calls do not steal a live append.                                                                                  |
| AGENT-033        | high       | fixed, tested | `frontend_design.visual_validation_evidence` accepted a path ending in `.png` whose bytes were plain text as long as the reported SHA matched. That made a text-only "screenshot" satisfy visual baseline acceptance.                                                                                                                                                                                                  | Heisenberg traced `isUsableRenderedScreenshotEvidence()` checking path roots and hashes but not image structure; the existing test fixture itself wrote text into `.png` files. Regression `submit_frontend_template rejects text files masquerading as visual baseline screenshots` replaces the screenshot file with text and matching hash, then asserts submission is rejected. Full `frontend-design/output-tools.test.ts`, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                          | Visual baseline screenshot and source-reference artifacts must now parse as PNG/JPEG/WebP files by basic header structure before SHA equality can make the evidence usable.                                                                                                                                                                                                                                                                                                |
| AGENT-034        | high       | fixed, tested | Visual QA treated any `frontend_design.reference_artifacts` decision-log entry as mandatory reference parity, even when no authoritative region list existed. Accepted reports then failed because the validator correctly requires task-scoped region evidence rather than self-reported regions.                                                                                                                     | Heisenberg traced `deriveVisualQaReferenceParityContext()` and `visual-qa/output-tools.ts`; regression `reference artifacts alone do not require authoritative region parity` proves `reference_artifacts` keeps advisory context but only VisualEvidenceBundle/acceptance scorers can require region parity. Strict reference fidelity tests, orchestrator schema tests, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                                 | Removed the `reference_artifacts` parity trigger. Reference artifacts still render as context; required parity now comes from explicit acceptance/visual evidence sources with region keys.                                                                                                                                                                                                                                                                                |
| AGENT-035        | high       | fixed, tested | `frontend_research` accepted non-HTTP `source_urls` and the research output tools accepted any `webpage_contract.source_url`, so a worker could publish a source-backed webpage contract for a different page than the host-prepared evidence.                                                                                                                                                                         | Heisenberg traced `FrontendResearchInputSchema`, `runResearchSession()`, `createResearchOutputTools()`, and `set_webpage_contract_source`. Regressions reject `file:///tmp/source.html`, reject non-HTTP webpage contract source URLs, and reject source URL mismatch against the prepared frontend-research URL. Full `research/output-tools.test.ts`, orchestrator schema tests, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                        | Fresh `frontend_research.source_urls` must be HTTP(S). Frontend-research output tools receive the prepared source URL and refuse a mismatched `webpage_contract.source_url`.                                                                                                                                                                                                                                                                                               |
| AGENT-036        | high       | fixed, tested | Stage continuation digests for `requirements`, `frontend_design`, and `architect` omitted prompt-changing durable inputs. A stale child session could resume after newer frontend-design evidence, visual artifacts, requirements rows, decision-log context, workload briefs, or existing goal graph changes.                                                                                                         | Pauli identified digest omissions. Regressions cover requirements after frontend-design public_report appears, frontend_design after task visual system_artifacts change, architect after existing goal graph mutation, plus the existing active-spec stale test. Continuation subset, package typecheck, and `git diff --check` passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Those stages now include compact evidence snapshots of their durable prompt inputs: attachments/design specs/system artifacts, frontend-design handoff, research sections, clarifications/operator notes, requirements/decisions, workload briefs, decision-log prompt, and existing goals as appropriate.                                                                                                                                                                 |
| AGENT-037        | high       | fixed, tested | `frontend_research` continuation accepted fresh `focus` and lost the original source URL binding on resume, so the output tools no longer knew which source page `webpage_contract.source_url` had to match.                                                                                                                                                                                                           | Locke and Halley traced `FrontendResearchInputSchema`, `normalizedStageInput`, continuation restoration, `FrontendResearchAgent.run()`, and `createResearchOutputTools()`. Regression coverage rejects `continuation_artifact_id + focus` at schema level and asserts a same-session continuation restores the stored source URL while not sending fresh focus into the resumed worker.                                                                                                                                                                                                                                                                                                                                                                                                     | `frontend_research` continuation now stores/restores the original source URL and focus in `normalizedStageInput`, passes the stored source URL back into the runner/output tools during continuation, and rejects fresh `focus` with continuation.                                                                                                                                                                                                                         |
| AGENT-038        | high       | fixed, tested | Continuation scope digests did not match the actual rendered prompts: architect omitted retry labels, frontend_design digested unrelated task metadata but omitted host-prepared source evidence, and architect digested unrendered workload fields. This created both false-fresh and false-stale continuation decisions.                                                                                             | Faraday traced `goalsContinuationScope()`, architect prompt goal labels, frontend_design metadata rendering, host-prepared evidence loading, and workload brief prompt projection. Regressions cover retry-count stale rejection, unrelated metadata ignored, host-prepared source manifest stale rejection, and unflagged workload brief changes ignored.                                                                                                                                                                                                                                                                                                                                                                                                                                  | Continuation scope now uses prompt-projected snapshots: goal retry counts are included, frontend_design metadata is reduced to `figma_url`, host-prepared compact evidence is digested when present, and architect workload scope only includes decomposition concerns and rendered inventory counts.                                                                                                                                                                      |
| AGENT-039        | high       | fixed, tested | Screenshot validation still accepted crafted PNG headers that could not decode as images, so a non-image file could pass visual-baseline evidence checks if it matched the old header predicate.                                                                                                                                                                                                                       | Halley traced the remaining header-only screenshot path in `frontend-design/output-tools.ts`. Regression `submit_frontend_template rejects forged image headers as visual baseline screenshots` writes a file with PNG magic bytes but invalid raster contents and verifies final submission is rejected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Final frontend_design visual-baseline submission now validates screenshot/source-reference files by decoding PNG/JPEG/WebP metadata through `sharp`, not by extension or magic header alone. Later AGENT-040 tightened this further by binding screenshot evidence to a fresh Node-sidecar render of the submitted entrypoint.                                                                                                                                             |
| AGENT-040        | high       | fixed, tested | `frontend_design` visual-baseline finalization still trusted any valid raster file with a matching hash. A task could submit a real PNG that was never rendered from `visual-html-skeleton/index.html`, so the screenshot evidence remained self-attested instead of artifact-derived.                                                                                                                                 | Carver reproduced the gap with a valid PNG path and matching SHA. Regression `submit_frontend_template rejects valid screenshots not rendered from the current entrypoint` creates valid screenshot evidence, mutates the entrypoint, and verifies final submit rejects it. Full `frontend-design/output-tools.test.ts` 36/36 and package typecheck passed.                                                                                                                                                                                                                                                                                                                                                                                                                                 | `submit_frontend_template` now re-renders the submitted `rendered_entrypoint` through the existing Node browser sidecar, derives the viewport from the evidence label or raster dimensions, and requires the fresh render SHA to match the submitted screenshot SHA. The synchronous report path no longer treats unverified artifact headers as submit-grade evidence.                                                                                                    |
| AGENT-041        | high       | fixed, tested | Several evidence schemas silently stripped unknown fields, letting caller/model drift hide in terminal outputs: frontend-design submit evidence, frontend-research webpage contract source, and VisualEvidenceBundle.                                                                                                                                                                                                  | Carver traced `.parse()` paths using non-strict nested objects in `frontend-design/schema.ts`, `research/output-tools.ts`, and `acceptance/visual-evidence.ts`. Regressions cover unknown top-level, nested `frontend_project`, `visual_validation_evidence`, `webpage_contract_source`, rendered bundle, and region fields.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Added `.strict()` to the affected evidence contracts and nested objects. Verified `frontend-design/schema.test.ts`, `research/output-tools.test.ts`, `visual-qa/strict-reference-fidelity.test.ts`, full frontend-design output tools, orchestrator schema/continuation subsets, package typecheck, and `git diff --check`.                                                                                                                                                |
| AGENT-042        | high       | fixed, tested | The AGENT-041 strictness repair was not recursive enough: `VisualRegionEvidence.bounds` and multiple frontend-design evidence-bearing item schemas still accepted and silently stripped unknown nested fields. `output-tools.ts` also normalized legacy `replacement_guard` into `parity_guard`, undermining the unknown-field rejection contract.                                                                     | Hume traced non-strict nested schemas in `acceptance/visual-evidence.ts` and `frontend-design/schema.ts`, and the old provider-alias normalization in `frontend-design/output-tools.ts`. Regressions now cover unknown fields inside `bounds`, final/tool `frontend_project`, final/tool `visual_validation_evidence`, final/tool component reuse, baseline replacement, compact template, and material item schemas.                                                                                                                                                                                                                                                                                                                                                                       | Added `.strict()` to nested bounds and frontend-design item schemas, removed `replacement_guard` alias normalization instead of keeping compatibility, and updated the old test to use canonical `parity_guard`. Verified full frontend-design output/schema tests, research output tools, strict Visual QA reference fidelity, continuation subset, `packages/opencorvus` typecheck, and `git diff --check`.                                                              |
| AGENT-043        | high       | fixed, tested | Architect's model-facing goal tools exposed the full acceptance scorer union, including `heuristic.spec.kind="script_ref"`. The worker could pick that high-branch schema, retry on missing script validation, and burn context even though final visual parity should use `llm_judge:visual_evidence` or `contract_audit`.                                                                                            | Boole's schema audit traced repeated `nonexistent script_ref` goal attempts despite the agent reasoning about visual evidence and contract audit. Regression `architect goal tool schemas expose scriptless acceptance_specs shape to the model` asserts Architect-visible `register_goal` / `modify_goal` schemas do not contain `script_ref` while execution-time script_ref validation remains tested.                                                                                                                                                                                                                                                                                                                                                                                   | Added Architect-only scriptless scorer schemas for `register_goal` and `modify_goal`, kept canonical execute-time `AcceptanceSpec` validation, and added `register_visual_evidence_acceptance` so final visual verification goals get the exact `llm_judge` scorer without hand-writing nested schema. Verified Architect schema, output-tools, grep-only prompt tests, package typecheck, and `git diff --check`.                                                         |
| AGENT-044        | high       | fixed, tested | `frontend_design` output was not useless as evidence, but its maintainable handoff lacked a structured implementation-phase contract and could claim unavailable mature components from prose. Downstream build agents therefore received screenshots/source IR without a reliable implementation target, entrypoint list, or package/source proof.                                                                    | Locke compared existing frontend_design artifacts with the TradingView clone failures: reference screenshots and source IR existed, but there was no phase outcome contract and reuse declarations could name packages absent from `package.json`. Regressions reject maintainable mode without `implementation_phase_outcomes`, reject missing implementation entrypoints, reject uninstalled mature libraries, and accept installed packages with real entrypoints.                                                                                                                                                                                                                                                                                                                       | Added five required implementation phases for maintainable replacements, rendered them into the public handoff, returned them through `FrontendDesignAgent.Result`, persisted them in orchestrator decision logs, and validated implementation target roots/entrypoints plus concrete package/source evidence for mature component reuse. Verified frontend-design output, prompt, schema, orchestrator tool-description tests, package typecheck, and `git diff --check`. |
| SCHED-009        | high       | fixed, tested | After an active run was blocked by `orchestrator_stream_error` such as a provider certificate failure, passive scheduler wakes could still call `dispatchTaskLoop()` and start another orchestrator turn. This made a blocked task self-wake without new operator input and wasted tokens.                                                                                                                             | Maxwell traced the gap: `runtime.ts` had a narrow terminal-refill guard, but the shared `engine/queue.ts` dispatch and queued-wake drain had no equivalent run-status check. Regressions cover direct passive dispatch ignored for stream-error blocked runs, explicit replan intent still starting, and passive queued wakes discarded after ownership clears if the run became stream-error blocked while queued.                                                                                                                                                                                                                                                                                                                                                                         | `dispatchTaskLoop` and `drainQueuedTaskEvent` now suppress only passive wakes when the newest run is `blocked` with `blocking_reason="orchestrator_stream_error"`. Structured operator events (`operatorIntent` / `operatorMessage`) still pass through, and existing operator/replan/ownership queue tests remain green. Verified full `engine/queue.test.ts`, package typecheck, and `git diff --check`.                                                        |
| REVIEW-009       | low        | human review  | The suspicion that `frontend_design` output from one screenshot is broadly "not useful" is directionally plausible but not proven as a static code bug by this round. The high-confidence defect was the unbounded skeleton output path, not the entire frontend-design agent purpose.                                                                                                                                 | Zeno's responsibility audit and the user's production traces show poor task outcomes under the TradingView clone prompt, but a single failure can mix prompt quality, reference evidence preparation, skeleton workflow, and downstream build behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Keep as product/process review: the likely correct direction is screenshot-first reference/evidence capture followed by a bounded visual skeleton, then region implementation from source-backed evidence. Do not replace frontend_design wholesale without an end-to-end benchmark proving the new workflow wins.                                                                                                                                                         |
| REVIEW-010       | medium     | human review  | `deep_research` continuation identity still has an unresolved design question: the old worker session holds fresh-only source parameters such as `source_urls`, but continuation schema intentionally forbids resending those fields.                                                                                                                                                                                  | Pauli's digest audit and the `frontend_research` repair show the right pattern is a narrow stored fresh-call scope plus current durable-evidence drift comparison. `deep_research` has not yet been proven to need the same specialized stored-source contract in this batch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Do not patch by allowing fresh parameters with continuation or by dropping continuation checks. Design a narrow continuation payload schema for deep research only if an end-to-end stale/fresh evidence case proves it.                                                                                                                                                                                                                                                   |
| REVIEW-011       | low        | human review  | `frontend_design.design_specs` still enters continuation identity as the raw task array instead of a separately documented prompt-projection helper.                                                                                                                                                                                                                                                                   | Faraday flagged this as a possible false-stale source, but current prompt usage and schema shape were not fully enumerated in this batch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Leave unchanged until a call-site audit proves which `design_specs` fields are prompt-affecting.                                                                                                                                                                                                                                                                                                                                                                           |
| REVIEW-012       | low        | human review  | `isHttpWebpageUrl()` remains a shared HTTP(S) prefix classifier rather than a full syntactic URL parser.                                                                                                                                                                                                                                                                                                               | Halley flagged parser strictness as review-only. Existing high-confidence bug was source binding and non-HTTP rejection, which the shared classifier already covers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Consider URL parser hardening only with route/tool compatibility evidence; do not introduce a separate parallel URL validator.                                                                                                                                                                                                                                                                                                                                             |
| AGENT-006        | medium     | human review  | `analyze_intent` has `StructuredOutputError` recovery represented in `StageContinuationFailureName`, but no orchestrator continuation path.                                                                                                                                                                                                                                                                            | Hubble traced `StageContinuationFailureName = "TerminalToolMissingError"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | "StructuredOutputError"`and`analyze_intent` structured-output usage. This is not a terminal finalizer miss and should not be repaired by the terminal-tool helper without a separate structured-output continuation design.                                                                                                                                                                                                                                                | Human/next-round review. Avoid adding fallback-style broad catch behavior.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| AGENT-007        | medium     | human review  | `build` does not participate in generic stage continuation and instead converts missing `report_build_result` into build-specific failure/retry evidence.                                                                                                                                                                                                                                                              | Confucius noted `StageContinuationStage` excludes `build`; `BuildAgent` already maps missing terminal report into `BuildAgentContractError(missing_terminal_report)`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Requires a product decision: same-session build continuation might conflict with current goal-run/worktree ownership semantics. Do not change in this batch.                                                                                                                                                                                                                                                                                                               |
| REVIEW-001       | low        | human review  | Stage continuation claim reads artifact before update; concurrent multi-process claim behavior is unclear.                                                                                                                                                                                                                                                                                                             | Independent lane B noted `engine/stage-continuation.ts` lacks obvious compare-and-swap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Deployment concurrency model needs confirmation before code change.                                                                                                                                                                                                                                                                                                                                                                                                        |
| REVIEW-002       | low        | superseded    | `frontend_research` allows `continuation_artifact_id` together with fresh `focus`; unclear whether `focus` is diagnostic or part of scope.                                                                                                                                                                                                                                                                             | A later independent audit reproduced this as a concrete source-binding defect rather than a product ambiguity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Superseded by fixed `AGENT-037`.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| HOUSEKEEPING-001 | high       | handled       | `bun run build:overlay` left generated files dirty. One generated overlay embed file referenced local `dist-vite` artifacts that were not present for TypeScript, causing typecheck failure unrelated to the scheduler/agent fix.                                                                                                                                                                                      | `bun run --cwd packages/opencorvus typecheck` failed on missing `overlay/dist-vite` imports from `overlay-ui-embedded.generated.ts`. `models-snapshot.ts` remains a separate dirty generated snapshot and is intentionally excluded from this audit commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Restored `overlay-ui-embedded.generated.ts` to the repository source form with an empty embedded table. Typecheck then passed. The unrelated `models-snapshot.ts` generated diff remains uncommitted and is called out for user review.                                                                                                                                                                                                                                    |
| TBD              | -          | collecting    | Waiting for final independent no-new-high-confidence audit results.                                                                                                                                                                                                                                                                                                                                                    | -                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | -                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Progress Log

- Created this audit ledger after committing the prior verified frontend-agent
  hardening batch.
- Spawned three independent read-only audit agents for scheduling, tool/schema,
  and responsibility/dead-code surfaces.
- Main-thread audit found `SCHED-001` before independent agents returned; applied
  the structural queue-state fix and added a regression test.
- Independent lane B returned two high-confidence agent/tool contract issues and
  two low-confidence review items. High-confidence items are recorded as
  `AGENT-001` / `AGENT-002`; low-confidence items are recorded for human review.
- Independent lanes A and C returned additional high-confidence goal/orchestrator
  and research-contract findings. Registered them as `GOAL-001`, `ORCH-001`,
  and `AGENT-003` / `AGENT-004`; low-confidence items are recorded for review.
- Applied schema-level fixes for `AGENT-002` and `AGENT-003`; focused
  orchestrator tool schema tests and `packages/opencorvus` typecheck passed.
- Applied `ORCH-001` restart handoff cleanup; focused scheduler tests and
  `packages/opencorvus` typecheck passed.
- Applied `GOAL-001` background finalization failure repair; targeted normal and
  failure-path goal build tests plus `packages/opencorvus` typecheck passed.
- Applied `AGENT-004` scoped research evidence-ref contract repair. Deep and
  frontend research brief internals still use local evidence ids, but prompt
  projections and downstream Requirements/Architect registration now use
  source-qualified refs. Verified with `requirements/output-tools-evidence`,
  `research/persist-describe`, full `architect/output-tools`, and
  `packages/opencorvus` typecheck.
- Applied the first `AGENT-001` finalizer-recovery repair batch for
  `deep_research`, `workload_analysis`, and `visual_qa`. Verified same-session
  continuation artifact creation and second-call continuation for each wrapper,
  plus schema exposure and package typecheck. Full `tools.test.ts` still fails
  on the previously recorded `REVIEW-009` project-id fixture mismatch, so that
  fixture cleanup remains a separate follow-up.
- Applied `AGENT-005A` for `fact_check`. The wrapper now supports exact fresh
  or continuation modes, stores the failed attempt's original target
  message/hash in the continuation artifact, persists the existing synthetic
  `tool_error` attempt before returning a continuation pointer, and resumes the
  same fact-check session without re-snapshotting the worker output. Verified
  with `fact-check/orchestrator-tool.test.ts`, orchestrator tool schema tests,
  `packages/opencorvus` typecheck, and `git diff --check`. `AGENT-005B`
  (`integrity`) remains the next high-confidence fix because ownership semantics
  need a separate targeted patch.
- Applied `AGENT-005B` for `integrity`. The integrity tool now exposes
  `continuation_artifact_id`, records active spec / phase / goal ids in the
  continuation artifact, refuses continuation if that scope has drifted, and
  reopens/closes task-level tool ownership around resumed integrity sessions.
  Verified with full `integrity/team-agent.test.ts`, targeted orchestrator
  wrapper coverage, orchestrator tool schema tests, `packages/opencorvus`
  typecheck, and `git diff --check`.
- Applied `SCHED-002` for stale prompt-loop cancellation. Durable stale-row
  recovery now fetches the owning session, performs a CAS-style terminal write,
  calls `SessionPrompt.cancel(session.id, session.directory)` on successful
  recovery, then releases queue-local in-flight cleanup. Verified with the
  focused stale in-flight regression, full `task-queue-service.test.ts` 19/19,
  and `packages/opencorvus` typecheck.
- Applied `AGENT-008` for `fact_check` workflow step closure. The tool now
  always emits a terminal workflow-step event after starting a matching
  custom workflow step: failed by default, completed for cached or completed
  agent outcomes. Verified normal, cached, early reject, and terminal-finalizer
  continuation paths in `fact-check/orchestrator-tool.test.ts` 10/10 and
  `packages/opencorvus` typecheck.
- Applied `AGENT-009` for phantom frontend_design handoff context. The handoff
  renderer now reads canonical `frontend_design` decision-log keys before
  rendering path blocks and returns an empty string when no handoff exists.
  Verified empty-handoff, relative path, absolute path, build prompt context,
  and goal build context tests plus `packages/opencorvus` typecheck.
- Applied `AGENT-010` for partial workload-analysis finalization. The terminal
  validator now requires a brief for every known architect goal before PASS.
  Verified with full `goal-workload-analyst/output-tools.test.ts` and
  `packages/opencorvus` typecheck.
- Applied `AGENT-011` for deep-research continuation input ambiguity.
  `deep_research` now rejects `continuation_artifact_id` plus fresh
  `source_urls`, matching the runner behavior that resumes the existing child
  session rather than fetching fresh URLs. Verified with orchestrator tool
  schema tests and `packages/opencorvus` typecheck.
- Applied `AGENT-012` after second-round lane C audit found that
  `reference_artifacts` alone still triggered frontend-design public-report
  path projection. The renderer now requires `public_report` before emitting
  those paths. Verified with frontend-design handoff, decision-log bundle,
  build prompt context, build goal reference suites, and package typecheck.
- Applied `AGENT-013` for mixed continuation/fresh-scope inputs on
  `visual_qa` and `deep_research`. Both tools now reject fresh prompt-only
  scope fields when `continuation_artifact_id` is present, matching the
  runner behavior that resumes the existing child session instead of building
  a new prompt.
- Applied `SCHED-003` for terminal goal refill wake coalescing. Runtime sync
  now records all newly observed terminal goal refill facts after one
  successful `dispatchTaskLoop` result, instead of starting one orchestrator
  pass per terminal goal run in the same observation.
- Applied `SCHED-004` for stale no-live-goal liveness documentation/tests.
  The passive restart regression now asserts the selected terminal-goal refill
  observer contract and explicitly rejects the retired `noLiveGoalWakeFingerprint`
  / `syncNoLiveGoalRuns` path. `task-api` comments now describe the runtime
  observer as interaction-blocker + terminal-refill projection, not no-live
  batch settlement.
- Applied `GOAL-002` for live `goal_run` contract protection. `modify_goal`
  now rejects durable live goal attempts even when the build tool ownership
  artifact has already closed, preventing async child sessions from running
  against a contract that changed mid-attempt.
- Applied `AGENT-014` for multi-page frontend_research consumption. Downstream
  Requirements, Architect, Build, and Visual QA contexts now consume all
  non-stale frontend research briefs up to a compact cap, and frontend
  evidence refs include the artifact id so page-local evidence ids cannot
  collide.
- Applied `AGENT-015` for stale same-session stage continuation scope.
  Continuation artifacts now compare stored scope digests against current
  durable task/spec/goal fingerprints before resuming generic stage agents.
  Architect and workload-analysis regressions mutate the active spec or goal
  graph after a finalizer miss and assert the stale continuation is rejected
  before the child agent mock is invoked. Scope fingerprints intentionally
  exclude `continuation_artifact_id`; pointer reasons remain separate so
  recovery instructions stay readable without becoming part of identity.
- Applied `TEST-001` for stale goal-build fixtures that no longer matched the
  current project-scoped task lookup contract. The filtered `goal build`
  benchmark now passes 18/18 without relaxing production project isolation.
- Applied `SCHED-005` for live-ownership operator-message loss. Operator
  messages queued behind live child tool ownership are now durable artifacts
  and drain as ordered individual wakes after ownership clears, instead of
  overwriting in a process-local single slot.
- Applied `AGENT-016` / `AGENT-017` for fact-check target scope. Fresh
  fact-check now rejects target sessions outside the current task and derives
  target_agent from the target session kind instead of trusting caller input.
- Applied `AGENT-018` for frontend-design task description projection.
  `describeTask()` no longer synthesizes template or source-manifest paths
  from abort-only or partial frontend_design evidence; path fields now require
  the specific durable keys they claim to describe.
- Applied `AGENT-019` for stale frontend_research prompt/tool wording.
  `frontend_research` is now consistently documented as source-page-scoped:
  one fresh call per source page URL, no repeat crawler/repair loop for the same
  page, and separate calls for additional pages.
- Applied `AGENT-020` for multi-brief deep_research consumption. Downstream
  Requirements, Architect, read_context, and describe projections now consume
  bounded active deep research briefs instead of only the latest one, and deep
  evidence refs include the artifact id so local evidence ids cannot collide.
- Applied `AGENT-021` for stale review/evidence continuation drift. `visual_qa`
  and `integrity` continuation identity now includes compact digests of the
  concrete evidence each agent consumes, so an old child session cannot resume
  after newer design, review, build, requirement, replay, visual evidence, or
  integrity-attempt material appears. Verified stale rejection before child
  agent invocation, existing continuation happy paths, package typecheck, and
  `git diff --check`.
- Applied `AGENT-022` / `AGENT-023` from Peirce's second-round
  research/design audit. Visual QA now preserves frontend research artifact ids
  in every webpage evidence ref, and `describeTask()` renders all compact
  frontend research briefs instead of only the latest one. Verified Visual QA
  context, strict reference fidelity, research describe projection, package
  typecheck, and `git diff --check`.
- Applied `AGENT-024` from Anscombe's schema audit. Build and the affected
  continuation-capable stage/review tools now reject unknown fields instead of
  silently stripping them. Verified the `goal_id` typo no longer turns into a
  task-level direct build input, plus package typecheck and `git diff --check`.
- Applied `AGENT-025` from Peirce's frontend-design handoff audit.
  `frontend_design` now rejects multiple non-Figma live/page URLs in one fresh
  call because the host clone-evidence package is task-scoped and single-source.
  Verified the schema regression, package typecheck, and `git diff --check`.
- Applied `AGENT-026` from Anscombe's continuation audit. Stage continuation
  artifacts that are consumed, claimed, claim-failed, missing, or scoped to the
  wrong finalizer now return a visible recovery result before any child worker
  starts. Verified consumed and claim-failed frontend research artifacts do not
  invoke the child agent, plus fact-check continuation coverage, package
  typecheck, and `git diff --check`.
- Applied `SCHED-006` / `SCHED-007` from Kepler's queued-wake audit. Pending
  durable operator wakes are now drained by the existing liveness observer after
  restart, and retry/replan operator intents persist behind live tool ownership
  instead of remaining process-local. Verified `queued-wake-ownership-drain`
  and `server/replan-routes`.
- Applied `AGENT-027` / `AGENT-028` from Zeno's frontend evidence audit.
  HTTP(S) URL detection is now shared and case-insensitive across design,
  research, and direct extract paths; `create_frontend_skeleton_project`
  rejects non-canonical output directories instead of creating a second evidence
  source. Verified shared URL evidence tests, skeleton-project prompt tests,
  package typecheck, and `git diff --check`.
- Applied `AGENT-029` from Socrates' schema/continuation audit. Stale pending
  continuation scopes now return visible `scope_mismatch` results, unavailable
  continuation artifacts close matching workflow steps as handled, fact-check
  continuation rejects fresh target fields, and `analyze_intent` rejects unknown
  continuation-like inputs. Verified continuation-focused orchestrator tests,
  orchestrator schema tests, fact-check recovery coverage, package typecheck,
  and `git diff --check`.
- Recorded `REVIEW-009` for the broader frontend-design output usefulness
  concern. The current evidence supports bounded screenshot/skeleton workflow
  pressure, but does not yet prove a safe wholesale replacement of
  `frontend_design`; keep that as a product/process benchmark item.
- Applied `SCHED-008` from Descartes' queue serialization audit. Durable
  operator wakes no longer let the liveness drain launch a task that is still
  queued behind an active same-cwd sibling; queued targets now re-enter through
  `advanceQueue(cwd)` and the existing queue-claim SQL. Verified the new blocked
  queued-wake regression, full `engine/queue.test.ts`, package typecheck, and
  `git diff --check`.
- Applied `AGENT-030` / `AGENT-031` from Pauli's workflow/continuation audit.
  Failed intent-analysis abort decisions now project as failed after reload,
  and integrity emits live workflow-step running/terminal events instead of
  returning continuations or advisory reports with no board event. Verified
  full `workflow-integrity-step.test.ts`, continuation-focused orchestrator
  tests, package typecheck, and `git diff --check`.
- Applied `AGENT-032` from Pauli's claimed-continuation audit. Continuation
  claims now carry a process-owner stamp; a later process converts unconsumed
  foreign/unstamped claimed artifacts into claim-failed visible recovery
  results instead of telling the orchestrator to wait forever. Verified
  continuation-focused orchestrator tests, runner continuation tests, package
  typecheck, and `git diff --check`.
- Applied `AGENT-033` / `AGENT-034` / `AGENT-035` from Heisenberg's
  frontend-evidence audit. Frontend-design visual validation now rejects
  text files masquerading as screenshots, Visual QA no longer treats plain
  `reference_artifacts` as authoritative required-region parity, and
  frontend-research source URLs plus webpage_contract source URLs are bound to
  real HTTP(S) prepared source pages. Verified full frontend-design output
  tools, full research output tools, strict Visual QA reference fidelity,
  orchestrator tool schemas, package typecheck, and `git diff --check`.
- Applied `AGENT-036` from Pauli's continuation digest audit for the
  high-confidence durable prompt inputs. Requirements, frontend_design, and
  architect continuation identity now includes compact snapshots of the
  durable evidence each stage actually reads. Verified stale frontend-design
  evidence, stale visual artifacts, stale active spec, and stale goal graph
  regressions plus the wider continuation subset. Recorded `REVIEW-010` for
  the remaining research-stage fresh-call-scope design question rather than
  patching it into a broken continuation contract.
- Applied `AGENT-037` / `AGENT-038` / `AGENT-039` from Locke, Faraday, and
  Halley's continuation/evidence audit. Frontend research continuation now
  restores the original source URL binding and rejects fresh focus, prompt
  scope digests now match the rendered frontend_design and architect inputs,
  and frontend_design visual-baseline screenshots must decode as real raster
  images rather than just matching a forged file header. Verified schema,
  forged-image, frontend_research continuation, frontend_design stale visual
  artifact, frontend_design stale host-prepared evidence, frontend_design
  unrelated metadata, architect retry-count, and architect workload projection
  regressions. Recorded `REVIEW-011` / `REVIEW-012` for lower-confidence
  design-spec projection and URL parser strictness questions.
- Applied `AGENT-040` from Carver's evidence audit. Visual-baseline screenshot
  finalization is no longer self-attested by a valid raster file plus SHA; the
  submit path now re-renders the declared visual HTML entrypoint through the
  existing Node browser sidecar and requires the fresh render hash to match the
  screenshot artifact. Verified stale-entrypoint, forged-header, text-file, and
  runtime-prefixed evidence regressions plus full
  `frontend-design/output-tools.test.ts` 36/36.
- Applied `AGENT-041` from Carver's schema audit. Frontend-design submit
  schemas, frontend-research webpage contract source, and VisualEvidenceBundle
  now reject unknown top-level and nested fields instead of stripping evidence
  drift. Verified focused schema regressions, full research output tools,
  strict Visual QA reference fidelity, orchestrator tool descriptions, the
  continuation subset, `packages/opencorvus` typecheck, and `git diff --check`.
- Applied `AGENT-042` from Hume's final schema audit. The strictness repair now
  reaches nested `VisualRegionEvidence.bounds` and frontend-design
  evidence-bearing item schemas, and the old `replacement_guard` compatibility
  normalization was removed so unknown fields are rejected rather than silently
  mapped. Verified full frontend-design output/schema tests, research output
  tools, strict Visual QA reference fidelity, continuation subset, package
  typecheck, and `git diff --check`.
- Handled `HOUSEKEEPING-001`: `overlay-ui-embedded.generated.ts` was restored
  from an invalid `build:overlay` generated state that referenced missing
  `dist-vite` files and broke typecheck. The separate generated
  `models-snapshot.ts` diff remains deliberately unstaged and outside this
  audit baseline.
- Applied `AGENT-043` after the Architect schema audit. Architect-visible
  goal tool schemas no longer expose `script_ref`; the execute path still
  validates legacy/canonical `script_ref` specs, and final visual parity has a
  dedicated `register_visual_evidence_acceptance` helper that emits the
  canonical `llm_judge:visual_evidence` scorer.
- Applied `AGENT-044` after the frontend-design usefulness audit. The evidence
  artifact remains useful when bounded, but maintainable replacement handoff
  now requires implementation-phase outcomes, real implementation target
  entrypoints, and package/source proof for mature component reuse.
- Applied `SCHED-009` after the stream-error wake audit. Passive internal wakes
  for `orchestrator_stream_error` blocked runs are ignored both at direct
  dispatch and queued-wake drain, while structured operator retry/replan/message
  wakes remain allowed.
- Focused verification for this round passed:
  - `bun test packages/opencorvus/test/engine/queue.test.ts --timeout 120000`
  - `bun test packages/opencorvus/test/architect/goal-tool-schema-visible.test.ts packages/opencorvus/test/architect/output-tools.test.ts packages/opencorvus/test/architect/grep-only-as-rejection.test.ts --timeout 120000`
  - `bun test packages/opencorvus/test/frontend-design/output-tools.test.ts packages/opencorvus/test/frontend-design/prompt.test.ts packages/opencorvus/test/frontend-design/schema.test.ts --timeout 120000`
  - `bun test packages/opencorvus/test/orchestrator/orchestrator-tool-descriptions.test.ts packages/opencorvus/test/frontend-design/smoke.test.ts --timeout 120000`
  - `bun run --cwd packages/opencorvus typecheck`
  - `git diff --check -- . ':!packages/opencorvus/src/provider/models-snapshot.ts'`
