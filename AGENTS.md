# AGENTS.md

## Skill Trigger

- For any benchmark or eval work, benchmark scaffolding, full benchmark debugging, timeout design, acceptance metric design, or repeated run-debug-fix cycles, always use the `benchmark-debug-template` skill from `C:\Users\chuan\.codex\skills\benchmark-debug-template`.
- If the user mentions `benchmark`, `eval`, `full benchmark`, `debug loop`, `acceptance criteria`, or requests unattended iterative debugging, treat that as a required trigger for `benchmark-debug-template`.
