# v0.0.22beta Latest macOS ARM64 Repackage

Status: completed

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Repackage the current `v0.0.22beta` delivery. |
| Acceptance | Rebuild from the latest committed and pushed `v0.0.22beta` source; run the native GUI installer matrix before the portable CLI (Command-Line Interface) matrix; independently verify canonical version, ARM64 architecture, signatures, DMG (Disk Image) integrity, archive contents, runtime payloads, hashes, and sizes; explicitly report unsupported native rows as skipped; commit and push the verification record through normal hooks. |
| Baseline | Local and `myhexin/v0.0.22beta` both resolve to `8460b32ea4d3af700808ee7267f7241972b0c45e`, with `HEAD...myhexin/v0.0.22beta` at `0 0`, a clean worktree, and release-family metadata aligned at `0.0.22-beta`. |
| Hard constraints | Use the repository matrix owners; GUI must run before CLI because both use shared distribution output. Do not stash, reset, restore, delete repository work, create a worktree, bypass hooks, claim cross-host validation, or interfere with a running OpenCorvus/Overlay process. Commit subjects use the required `dsw-33987` prefix and push only to `myhexin`. |
| Sources read | Root `AGENTS.md`; current Git status, branch, local/remote commit graph, and version output; `docs/packaging.md`; the completed `v0.0.22beta` bump/package record; root and July specification indexes. |
| Whole-repository grep | Root `package:gui-installer-matrix` is owned by `script/package-gui-installer-matrix.ts`; `package:binary-matrix` delegates through `script/package-binary-matrix.ts` to `script/package-native-binary.ts`; their focused contract tests and ignored output locations are enumerated in `docs/packaging.md` and the script test tree. No second packaging path is authorized. |
| Independent-agent feedback | None. The user did not request delegated agents, so the primary agent owns execution and second review. |

## Plan

1. Commit and push this clean-baseline Recall before regenerating ignored artifacts.
2. Run `bun run package:gui-installer-matrix`, then `bun run package:binary-matrix`.
3. Independently extract and validate the application and CLI archives, strict signatures, ARM64 binaries, application and executable versions, DMG integrity, and complete runtime payload.
4. Run the six documented packaging regression suites and canonical version/check surfaces.
5. Record final hashes, sizes, native-row skips, and preservation evidence; commit and push the completed ledger; verify local/remote equality.

## Verification ledger

- Repackage source: the product source baseline was
  `8460b32ea4d3af700808ee7267f7241972b0c45e`; the pre-build Recall commit was
  `4836f789aa`. `bun run version:check` confirmed canonical `0.0.22-beta`
  before and after both matrix builds.
- GUI matrix: the real Vite production build, embedded backend build, native
  Tauri build, installer bundling, staging, and release-asset validation all
  passed for `darwin-arm64`. Linux x64, Linux ARM64, macOS x64, and Windows x64
  were explicitly skipped because they require matching native hosts.
- GUI acceptance: the staged executable is a thin ARM64 Mach-O and passes
  strict signature verification. The independently extracted
  `OpenCorvus.app` passes deep strict signature verification; both application
  version fields are `0.0.22-beta`; the archive contains the complete bundle;
  and `hdiutil verify` reports the DMG checksum valid. Tauri explicitly skipped
  Apple notarization because no notarization credentials were configured.
- CLI matrix: `darwin-arm64` packaged successfully and the same four
  non-native rows were explicitly skipped. The staged executable and
  independently extracted archive executable are thin ARM64 Mach-O files,
  pass strict signature verification, and return `0.0.22-beta`. The extracted
  archive also contains executable `bin/rg`, the Browser MCP (Model Context
  Protocol) Node runtime, and the embedded Overlay UI.
- Regression and repository checks: all 49 documented native, Linux, GUI,
  matrix, release-contract, and install-script tests pass. Version convergence,
  API route inventory, and generated bilingual API documentation checks pass.
  The worktree remained clean because distribution artifacts use the declared
  ignored output locations.
- Rebuilt artifact SHA-256 and byte sizes:
  - GUI executable:
    `0449133c8b1d42db705a4386fb529449c41f5791fffc8a03cb793c9163907d4d`,
    174542112 bytes.
  - DMG:
    `37dc163f647835d2272591b0d8982c9481390838d8af32edd8204eac834d3915`,
    166745623 bytes.
  - Application archive:
    `e3a108ea941fbd02d4a5cb1511d6c23764418129648aa19de7153efcd39c7060`,
    166039438 bytes.
  - CLI executable:
    `d77ec45486a79c1fec12887839b9f82d1078aff87f8667f8cb0a9ddae4361b35`,
    102300560 bytes.
  - CLI archive:
    `e0afecd0fefa7633fccb06d4bad62f5de319f35cd4912ecee6ff8149e6ee9fa9`,
    122496074 bytes.
