---
name: debug
description: Build or repair an end-to-end benchmark/eval loop for a large in-development project, then use it to iteratively debug until explicit acceptance criteria pass. Use when the task involves benchmark scaffolding, full-chain evaluation, timeout design, acceptance metrics, or repeated debug-eval-fix cycles. Do not use for one-off edits with no measurable success criteria.
---

# 调试模板

## 目的

为大型开发中的项目提供早期全链路评估与 debug 能力。先建立可重复、可验收、可定位问题的 benchmark，再基于 benchmark 反复迭代，直到满足验收标准。

## 何时使用

在以下场景使用本 skill：

- 用户要求为项目建立 benchmark、eval、回归测试或端到端验收链路。
- 用户要求调试一个仍在快速开发中的系统，并且需要用可量化标准判断是否修复。
- 用户希望无人值守地反复执行「运行 → 定位 → 修复 → 复测」的闭环。
- 用户给出的任务需要明确输入输出契约、环境加载方式、超时策略和验收标准。

## 不要在这些场景使用

- 只是一次性回答概念问题，没有实际执行或验收需求。
- 只是做微小改动，且没有可量化的成功标准。
- 用户明确只要讨论方案，不需要构建或运行 benchmark。

## 必须保留的关键信息

无论是否发生上下文压缩，都必须保留并优先维护以下信息：

- 任务定义
- 输入 -> 输出说明
- 环境变量或运行环境加载地址
- 超时策略
- 验收标准
- 当前失败现象与已验证结论
- 已落盘方案、设计决策与历史约束

## 如果还没有测试链路

如果还没有测试链路，先构建 benchmark，再开始 debug。

benchmark 至少必须包含：

1. 任务定义：要完成什么任务。
2. 输入 -> 输出说明：输入是什么，合格输出是什么。
3. 环境加载地址：环境变量、密钥、依赖、服务入口从哪里加载。
4. 超时设置：设置“无活动后的真实超时”，不要从进程启动时开始计算总时长。
5. 验收标准：必须是可执行、可比较、可判定通过或失败的标准。

## benchmark 构建示例

例如，构建一个端到端评估套件测试 URL 复刻：

- 输入：一个 URL。
- 输出：一个与目标完全一致的网页。
- 环境变量：在本机查找并加载。
- 超时：3 分钟无活动则判定失败。
- 验收标准：网页视觉相似度 >= 98%。

## 执行流程

1. 先明确任务边界与成功标准。
   - 把任务定义、输入输出、环境、超时、验收标准写清楚。
   - 如果用户给出的目标模糊，优先把模糊目标压成可测指标。

2. 在必要时 recall 已有方案与约束。
   - 当改动涉及已有设计决策或历史约束时，先查看硬盘上的相关方案。
   - 对于简单、局部的改动，无需强制 recall。

3. 建立或校验 benchmark。
   - 如果 benchmark 不存在，先搭建。
   - 如果 benchmark 已存在，先验证它是否真的能反映目标，而不是跑形式。

4. 运行 benchmark，记录失败。
   - 收集失败样本、日志、trace、产物 diff。
   - 先定位真实失败点，不要在根因未明时直接打补丁。

5. 做根因分析。
   - 区分症状、触发条件、根因。
   - 优先修复本质问题，而不是用 fallback、兜底分支或临时补丁掩盖问题。

6. 实施修复并复测。
   - 每次修改都要回到 benchmark 复测。
   - 没达到目标或验收指标，就继续迭代。
   - 保持无人值守可重复执行，不依赖人工临场判断。

7. benchmark 通过后再做人工 review。
   - benchmark 通过不代表直接交付。
   - 必须自己 review 交付物，进行二次校验，确认结果真正合格。

8. 你不可以直接修复交付物。
   - 你只能修复 benchmark 中暴露的问题。
   - 只有 benchmark 通过了，自动给出交付物才算合格。

## 非协商约束

1. 不允许任何 fallback 逻辑。
   - fallback 会累计 bug、制造级联问题，并使问题不可定位、不可修复。

2. 任务要求与验收指标必须长期保留。
   - 即使上下文压缩，也不能丢失任务要求、验收标准和关键约束。

3. 测试超时必须是“无活动后的真实超时”。
   - 不要把超时从进程启动时刻开始机械计时。

4. 采用无人值守、自我迭代的方式推进。
   - 不达到目标或验收指标，就继续重复执行、修复、复测。

5. 禁止无脑打补丁。
   - 先思考本质问题。任何掩盖问题的补丁都不应视为合格修复。

6. 禁止无脑使用 git 回退修改。
   - 不要通过粗暴回退破坏未提交代码或掩盖真实问题。

7. 如果调试工具本身异常，先修工具。
   - 例如 git、rg、测试命令、运行器不可用时，应主动修复工具链，再继续任务。
   - 已知陷阱：drizzle `$inferSelect` 类型在 schema 列变化后可能因 turbo / bun /
     tsgo 缓存 stale 而仍报"Property X does not exist"。复现/修复时跑
     `bun run typecheck:fresh` 一键清缓存重跑。

8. benchmark 通过后仍必须复核交付物。
   - 二次 review 不通过，不算完成。

9. 不要长时间闷头执行后才一次性暴露大量低级问题。
   - 应进行频繁自检与阶段性校验，尽早暴露问题、尽早纠偏。

10. 如果用户的问题定义浮于表面，或当前路径无法根本解决问题，必须主动警示并重定向。

    - 不要迎合表面要求，更不要掩盖真正问题。

11. 在必要时查看硬盘上的方案并进行 recall。

    - 当改动涉及已有设计决策、架构约束或历史方案时，应先查看相关记录再修改。
    - 对于简单、局部、上下文已充分的改动，无需强制 recall。

12. 不要一味堆砌代码。

    - 如果发现死代码、无意义代码、过时代码或废弃逻辑，应先向用户说明并询问是否删除。

13. 禁止任何关键字匹配规则。

    - 任何形式的关键词过滤或敏感词屏蔽都是不可接受的，视为严重违规。
  
14. 禁止任何“最简单的修复”
    - 不要为了快速修复而牺牲代码质量、系统健壮性或长期可维护性。
    - 任何“最简单的修复”如果没有经过充分思考和验证，都不应视为合格修复。

## 产出要求

最终产出至少应包含：

- benchmark 定义
- 当前结果（通过 / 失败）
- 失败原因或根因分析
- 修改内容与理由
- 验收结果
- 人工二次 review 结论
- 本次参考过的已落盘方案或设计记录
- 发现的死代码 / 过时代码 / 废弃逻辑，以及是否已获准删除

## 完成标准

只有同时满足以下条件才算完成：

- benchmark 覆盖了真实目标。
- benchmark 已通过，且满足验收标准。
- 没有依赖 fallback、临时兜底或表面补丁。
- 交付物已经完成人工二次 review。
- 涉及已有设计决策的改动前已检查相关方案。
- 发现的明显死代码、无意义代码、过时代码或废弃逻辑，已经明确记录并与用户确认处理方式。
- 没有发现新的明显问题或未解释的异常。
