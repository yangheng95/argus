# v0.0.10beta Branch And Version Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the latest changes to the `0.0.10beta` branch. |
| Acceptance criteria | Create the local and legacy remote `v0.0.10beta` delivery branch from the synchronized `v0.0.9beta` head; include every newer commit from `legacy-remote/work-v0.0.9beta-yr-0718`; synchronize the release family to canonical Semantic Versioning (SemVer) `0.0.10-beta` and Windows Microsoft Installer (MSI) projection `0.0.10.0`; pass focused merge, version, documentation, and normal push-hook checks. |
| Hard constraints | Use `script/sync-version.ts` as the single version writer. Do not reset, create a worktree, touch running OpenCorvus/Overlay processes, hand-edit generated version targets, add fallback behavior, or stage the pre-existing untracked `C:/` directory. Commit subjects start with `dsw-33987`; push only to the legacy remote. |
| Sources read | Repository `AGENTS.md`; `script/sync-version.ts`; `specs/records/2026-07/2026-07-18-v0.0.9beta-branch-and-version-bump.md`; current branch, remote, status, divergence, log, and version grep evidence. |
| Whole-repository grep | Current release values occur in nine release-family package manifests, Bun workspace lock entries, two Cargo manifests and their package lock entries, the Tauri application/MSI configuration, `RELEASE.md`, and historical/current specs. `script/sync-version.ts` is the only writer and its focused test enumerates the declared targets. Historical specs remain immutable; only the current release documentation and new record/index entries may change. |
| Latest-change evidence | `v0.0.9beta` and `legacy-remote/v0.0.9beta` were identical at `3a6b634324`. The work line has three commits not present in the delivery branch: `eedbf9cc0`, `ff5d4701e`, and `31fd7fb1b`, covering the Work Ledger single-selection behavior and Codex-style Settings control hierarchy. The delivery branch has no tracked worktree changes. |
| Independent agent feedback | None requested; this branch/version operation is serial and bounded. |

## Plan

1. Commit and push this Recall/plan on the newly created `v0.0.10beta` branch so the implementation reads from a durable source.
2. Merge `legacy-remote/work-v0.0.9beta-yr-0718` into the target branch, resolve conflicts from repository evidence, and verify the exact three work-line commits are ancestors.
3. Run `bun run version:bump 0.0.10beta` so the existing synchronizer writes every declared target; update only the current release example where required.
4. Run version synchronizer tests, version checks, documentation health, and focused tests for the merged Work Ledger/Settings changes; inspect the complete diff and preserved untracked boundary.
5. Commit with the required prefix, fetch to detect remote movement, push through normal hooks, fetch again, and prove local/remote equality.

## Verification Ledger

- The target branch was created from `3a6b634324`, and plan commit `696685dd0` was pushed before implementation. Merge commit `7c01bbd3e` integrates work-line commits `eedbf9cc0`, `ff5d4701e`, and `31fd7fb1b`; ancestry checks cover the exact remote tip.
- Six conflicts were resolved without choosing an entire side: compact Project-row density was retained while Project selected state was retired; child selected state remained authoritative; the canonical mailbox fixture replaced duplicated response construction; Settings adopted the shared `Badge` primitive without losing global/project install-scope copy; both spec indexes retain all records.
- Focused merged-change source tests, Overlay typecheck, and historical documentation health passed. Six Node-launched browser scenarios passed after a real 2,499-module Vite build. Visual review of `work-ledger-child-only-selected.png`, `button-format-light.png`, and `settings-segmented-aria-label.png` confirmed one child selection, neutral Project headers, and coherent Settings control hierarchy.
- `bun run version:bump 0.0.10beta` synchronized the release family and MSI projection. The first `version:check` exposed a pre-existing double-digit patch defect: compact-version parsing backtracked canonical `0.0.10-beta` into `0.0.1-0-beta`. The single synchronizer now permits compact prerelease suffixes only when they begin with a letter, preserving canonical SemVer without ambiguity; regression tests cover both `0.0.10beta` and `v0.0.10-beta`.
- Final version, synchronizer, historical-link, document-health, and product-document checks passed with 92 tests and zero failures. The Node-launched sidebar version scenario passed after a fresh Vite build; visual review of `sidebar-version-tooltip-hover.png` confirms the rendered `v0.0.10beta` label and author Tooltip.
