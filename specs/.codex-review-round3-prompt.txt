你是 opencorvus 项目的资深架构评审者。这是第 3 轮评审。请尖锐、具体，不要客套。

## 上下文

第 2 轮评审你已经给过 RECOMMEND_WITH_CHANGES，列出了 3 个必改项。我已经更新方案到 v3，落盘在
D:\myhexin-local\opencorvus\specs\fact-check-agent-2026-05-25.md

v3 顶部有 v2→v3 changelog，覆盖你 round 2 的所有反馈。

## 你 round 2 的 3 个必改项 + 我的处理

1. **重写 §5.2，按实际静态 core / collector 架构列出每个 worker 真实注入点**
   - v3 §5.2：6 个 worker 的 agent.ts `core:` 行实际位置全部列出（含行号）：
     - build/agent.ts:747（composeBuildCore）
     - requirements/agent.ts:99
     - architect/agent.ts:121
     - design-analyst/agent.ts:98（既有 `[...].join("\n\n")` 末尾追加）
     - intent-analysis/agent.ts:74
     - integrity/team-agent.ts:197/265/404（3 处）

2. **修正 §3.1 覆盖范围和 schema 契约**
   - v3 §1.1：覆盖范围重定义为"有 terminal report schema 的 worker"——6 个，coding/general 因定义不适用而不在范围（不是例外）。
   - v3 §3.1：删 `default([])`，FactCheckItemListSchema 是 `z.array(FactCheckItemSchema)` 必填字段。
   - v3 §3.1：删 host-side regex 禁词检查（rule 20），改为 prompt 文本 + fact-check agent 复核 (ambiguous classification)。

3. **修正 §6.1 触点清单——删"推测/待复核"，改实际文件**
   - v3 §6.1.2 全部用 grep 验证的实际路径：build/types.ts、5 个 output-tools.ts、integrity/team-schema.ts、config/config.ts、decision-log/index.ts。

## v3 还新增了

- §3.3 Snapshot 协议：target session 必须 finished=true，否则工具入口 reject。
- §3.3 verdict 决策树显式 items_total=0 → clean。
- §6.1.3 契约破坏面：external executor / fixture / NATIVE_DEFAULTS / collector / overlay parser / contract test。
- §8 实施步骤重排，强串行链先：SESSION_KINDS → schema+artifact → worker → prompt → agent → orchestrator → decision-log；UI/test 后置并行。
- §11 列了 8 个待 round 3 复核问题。

## 必答清单

### A. 你 round 2 的 3 个必改项是否全部解决？
逐项 ✅ / ⚠️ / ❌，≤ 60 字说明。

### B. v3 §11 8 个复核问题逐项回答（≤ 100 字）
重点关注 #1（Session.snapshotLatestAssistant 实际存在吗？）和 #7（v3 §4.3 工具入口逻辑是否违反 rule 6.1）。

### C. v3 是否引入新问题？
重点查：
- §3.3 snapshot 拒绝路径在测试 e2e D 中如何模拟？现有 session 测试基建支持 "finished=false" 状态吗？
- §6.1.3 external executor 适配："显式构造空数组让 schema 通过"——你 grep 看具体代码位置，这种构造放在哪个函数？是否破坏 external executor 的 single-source 哲学？
- §5.2 integrity 3 处 core 拼接：reviewer 阶段（line 265）也注入 fragment 是否会让 reviewer 误以为自己也要登记 fact_check_items？integrity 三阶段中 reviewer 是产出 finding 不是产出 fact_check_items 的角色。

### D. CLAUDE.md 违规复核
v3 文档现在还有任何 CLAUDE.md 违规吗？引用具体的方案文字和规则编号。重点关注：
- Rule 6.1 prompt-over-host-invariant：v3 §4.3 工具入口的 snapshot+reject+cached 逻辑算 host 拦截吗？v3 §10 第 8 条已经辩护过，请二次审视。
- Rule 8 单源：fact-check 的 FCI schema 在 fact-check/tools.ts，6 个 worker schema 都引用——这是否真的单源？还是每个 worker schema 都本地 import？
- Rule 11 设计模式纪律：6 个 worker 的 agent.ts 都做一行 `[CORE, fragment].join("\n\n")`——这是不是 9 个调用点（含 integrity 3 处）的重复，违反 rule 9（主动抽象设计模式）？

### E. 实施风险与顺序
v3 §8 step 1 类型骨架闭合：注册了 fact-check Info 但实现在 step 5——能 build 通过吗？还是必须 stub 实现一起？

### F. 最终结论
明确给出一个标签：
- `RECOMMEND_PROCEED`（直接推进实施，无需再改）
- `RECOMMEND_WITH_CHANGES`（必须改后才能推进，列出最关键前 3 项）
- `RECOMMEND_REJECT`（建议拒绝整个方向，换替代架构）

如果给 `RECOMMEND_PROCEED`，必须确认你没有任何剩余诉求。
如果给 `RECOMMEND_WITH_CHANGES`，给出剩余必改项数量（避免无限迭代——若 ≤ 3 项且全是 minor，建议直接 PROCEED）。

最后一行必须以 `VERDICT: <label>` 结尾。

## 输出约束
- markdown，分节
- 全文 ≤ 4000 字
- 对你不确定的明确说"我没查到"，不要编
- 不修改代码，不修改方案文件
