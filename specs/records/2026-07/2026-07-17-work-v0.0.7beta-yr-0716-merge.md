# work-v0.0.7beta-yr-0716 merge

## Recall

### User requirement

- Fetch the latest commits from legacy remote branch `work-v0.0.7beta-yr-0716` and merge them into the current local branch.
- Preserve every existing parallel-agent change.
- The user explicitly cancelled the originally requested `0.0.8beta` version bump; keep the current release version and branch.

### Acceptance criteria

- Merge the freshly fetched `legacy-remote/work-v0.0.7beta-yr-0716` tip into local `v0.0.7beta` without losing committed history, the dirty `packages/opencorvus/src/expert-squad/payload.ts` change, or untracked macOS artifacts.
- Resolve the two predicted spec-index conflicts by retaining valid entries from both histories.
- Keep the release family at canonical SemVer `0.0.7-beta` and MSI (Microsoft Installer) projection `0.0.7.0`.
- Run merge-sensitive tests, version consistency, spec health, typechecks, API (Application Programming Interface) route/docs checks, and a final graph/status review.
- Commit with the required `dsw-33987` prefix and push `v0.0.7beta` to legacy remote without bypassing hooks.

### Hard constraints

- Do not reset, stash, rebase, rewrite history, create a worktree, or overwrite tracked/untracked parallel-agent files.
- Do not stage or claim the existing `payload.ts` change or macOS build artifacts.
- Do not bump versions or create a `v0.0.8beta` branch.
- Do not restart, refresh, close, or otherwise interfere with a running OpenCorvus or Overlay process.
- Resolve index conflicts by combining both sides' valid entries, not by whole-file checkout.

### Sources read

- Root `AGENTS.md` instructions supplied in the task context.
- `specs/records/2026-07/2026-07-16-work-v0.0.6beta-yr-0716-merge-and-v0.0.7beta-bump.md`.
- `script/sync-version.ts` and `packages/opencorvus/test/script/sync-version.test.ts`.
- Current status, remotes, branch tips, merge base, commit graph, source diff, version-call inventory, and merge-tree simulation.

### Whole-repository search evidence

- Fresh fetch resolved `legacy-remote/work-v0.0.7beta-yr-0716` to `f4e7709394665b2e2994bfd6e4efd8cf555c4290`; current `HEAD` and `legacy-remote/v0.0.7beta` both resolved to `d5ff1a43dbca939d9149aa8fd29d7f478e9b56a5`.
- The merge base is `01944f687bd20119508ed82da11d47e35bacdbed`; current history has 25 unique commits and the source has 6 unique commits.
- The source changes backend mailbox/tool/catalog/runtime code, generated SDK/docs, Overlay mailbox/right-dock/UI code and tests, and spec records/indexes. It does not change the dirty `packages/opencorvus/src/expert-squad/payload.ts` or the two existing untracked macOS artifact paths.
- `git merge-tree --write-tree HEAD legacy-remote/work-v0.0.7beta-yr-0716` predicts only `specs/README.md` and `specs/records/2026-07/README.md` conflicts; all product-code overlaps auto-merge.
- Release-family search found ten package manifests, two Cargo manifests/locks, Tauri version plus MSI projection, and Bun workspace rows owned by `script/sync-version.ts`. Per the user's correction, none will be modified; check mode will prove they remain aligned at `0.0.7-beta`.

### Independent agent feedback

- No new subagent was launched because the active multi-agent boundary forbids spawning unless explicitly requested. Preservation, topology, conflicts, version scope, and final integration are reviewed directly in this worktree.

## Implementation plan

1. Commit and push this traceable merge plan alone while excluding every pre-existing parallel-agent path.
2. Re-fetch both refs, merge the exact source tip into `v0.0.7beta`, and combine the two spec indexes so all valid records from both histories remain discoverable.
3. Run focused tests for the incoming mailbox/runtime/Overlay surfaces plus version, spec, docs, type and generated-contract checks.
4. Perform a second graph, source-ancestry, version and status review; update this Result, commit only merge-owned files, and push `v0.0.7beta` through repository hooks.

## Result

- Fresh fetch retained source tip `f4e7709394665b2e2994bfd6e4efd8cf555c4290`; merge commit `873de5913c` contains that tip and both pre-existing `v0.0.7beta` histories. The two spec-index conflicts were resolved by retaining every valid entry from both sides.
- A parallel agent completed its previously dirty `payload.ts` and global-New-Chat record as independent commit `456e5525fa`; that commit is on top of the merge and was pushed without being absorbed into the merge resolution. The untracked Darwin package artifact directory remains uncommitted and unchanged.
- Per the user's correction, no version bump or `v0.0.8beta` branch was created. Version synchronization check and five version regressions pass at canonical `0.0.7-beta` with MSI projection `0.0.7.0`.
- Focused Mailbox/runtime/Overlay coverage passed 228 tests with one platform skip. Six unrelated Overlay architecture-guard failures were also observed; three-way path and assertion comparison proves their triggering production files and assertions predate and are unchanged by this source merge.
- The Node-started desktop browser scenario built 2,462 modules, rendered the real fixture, and passed both complete scenarios after repairing one test synchronization defect: overflow stability now begins only after the asynchronous `New tab` title and expected hidden Review tab projection have settled, while still requiring 24 frames with zero overflow mutations. Mutation detail is retained for future causal diagnostics.
- Visual review accepted the current 360-pixel light and 280-pixel dark Mailbox Dock screenshots: three mixed message rows, two unread markers, attention emphasis, badges, timestamps, search, view controls, and truncation remain coherent without overlap or clipping. Archive/restore and overflow interactions passed.
- PASS: repository typecheck, 10 tasks across 12 packages; version, historical-links, document-health and product-doc single-source suites, 86 tests and 1,296 assertions; API route inventory, 6 rules across 31 files; generated API docs, 266 operations in 24 groups; Overlay panel i18n revision `1399ade75015b6f8`; and final diff whitespace checks.
