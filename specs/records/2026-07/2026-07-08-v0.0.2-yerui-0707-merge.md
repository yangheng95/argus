# v0.0.2-yerui-0707 Merge Record

## Recall

- User request: merge `https://github.com/yangheng95/opencorvus/tree/v0.0.2-yerui-0707`.
- Target interpretation: merge remote branch `legacy-remote/v0.0.2-yerui-0707` into the current branch `v0.0.2beta` in the existing Windows host worktree.
- Acceptance criteria:
  - Fetch the target branch from legacy remote.
  - Preserve the already-dirty local worktree before merging because it overlaps incoming paths.
  - Merge the remote branch without fallback logic, branch aliases, extra worktrees, or broad git reset.
  - Resolve real conflicts if they occur, keep a single source for browser preview / transport / generated API surfaces, and verify with focused tests plus docs health where specs are touched.
  - Commit and push the resulting `v0.0.2beta` state to `legacy-remote`.
- Hard constraints:
  - No fallback / compatibility path, no gate workaround, no synthetic hidden message path.
  - No `git reset` for worktree/index cleanup; do not revert unrelated local edits.
  - Do not create a new worktree without explicit user authorization.
  - Do not restart or refresh running OpenCorvus / overlay processes.
  - Browser / overlay validation that needs Playwright on Windows must use `node`, not `bun`, for the browser runner.
- On-disk sources read before implementation:
  - `AGENTS.md`
  - `specs/README.md`
  - `specs/artifacts/长程编排测试.md`
  - `specs/records/2026-07/README.md`
- Git evidence read before implementation:
  - `git status --short --branch`
  - `git remote -v`
  - `git branch --show-current`
  - `git fetch legacy-remote v0.0.2-yerui-0707`
  - `git fetch legacy-remote v0.0.2beta`
  - `git log --oneline --decorate --graph --left-right --cherry-pick HEAD...legacy-remote/v0.0.2-yerui-0707 -n 80`
  - `git diff --name-status HEAD..legacy-remote/v0.0.2-yerui-0707`
  - `git diff --name-only`
  - `git diff --stat`
  - `git diff --stat HEAD..legacy-remote/v0.0.2-yerui-0707`
- Whole-repository search evidence:
  - `rg -n "browser-preview|v0\\.0\\.2-yerui|yerui|BrowserPreview|transport-protocol|prompt_profile|expert-squad|right-toolbar|conversation-agent|work-ledger|message pane|toolbar" specs packages/opencorvus packages/overlay packages/transport-protocol -g "!*node_modules*"`
  - The search confirms the merge area is browser preview, native webview transport, transport-protocol route scope, OpenAPI/SDK docs, overlay toolbar/layout surfaces, and July records.
- Independent agent feedback:
  - No subagent was launched because the current tool instructions only authorize subagents when the user explicitly asks for delegation. The merge plan therefore uses direct repository evidence and post-merge self-review.

## Incoming Branch Summary

- Remote branch: `legacy-remote/v0.0.2-yerui-0707`
- Divergence before preserving local work:
  - Incoming commits only relative to current `HEAD`: `0c95db8429`, `bc24e43657`, `492702bd87`, `245b3245f7`, `026c9dc6b3`, `2c173feae4`.
  - Current `HEAD` had no unique committed changes relative to the incoming branch, but the worktree had many uncommitted local edits.
- Incoming changed areas:
  - `packages/opencorvus/src/server/routes/browser-preview.ts`
  - `packages/opencorvus/src/agent/tool-pool-contract.ts`
  - `packages/overlay/src/components/BrowserPreviewPanel.tsx`
  - `packages/overlay/src/services/browser-preview*.ts`
  - `packages/overlay/src/services/{host-transport,tauri-transport}.ts`
  - `packages/overlay/src-tauri/src/main.rs`
  - `packages/transport-protocol/src/index.ts`
  - generated SDK / OpenAPI / API docs
  - `specs/records/2026-07/2026-07-08-browser-preview-dom-selection-sync.md`

## Plan

1. Add this Recall record and the July README index entry.
2. Commit the current dirty worktree as a pre-merge preservation commit on `v0.0.2beta`.
3. Merge `legacy-remote/v0.0.2-yerui-0707` into `v0.0.2beta`.
4. Resolve conflicts by reading both sides and retaining one authoritative implementation for each touched surface.
5. Run focused browser-preview / transport / overlay / docs validation and a post-merge review.
6. Commit the merge result and push `v0.0.2beta` to `legacy-remote`.
