# v0.0.15beta Post-merge Darwin ARM64 Matrix Repackage

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Package the current `v0.0.15beta` after merging `work-v0.0.15beta-yr-0722`. |
| Acceptance criteria | Run the canonical GUI installer matrix first and portable Command-Line Interface (CLI) binary matrix second; build and validate the native macOS ARM64 rows; report non-host rows as skipped; inspect architectures, versions, signatures, disk image integrity, sizes, and SHA-256 checksums. |
| Source provenance | Local and `legacy-remote/v0.0.15beta` both resolve to `e87863bb02e7c47a35990f9e83d21b06d3755974`; `47f500e4e` is an ancestor. Release-family version verification reports `0.0.15-beta`. |
| Parallel work | Four unstaged parallel edits remain in `orchestrator-core.txt`, `core-prompt-hygiene.test.ts`, `2026-07-22-mirror-prism-full-workflow-distillation.md`, and `2026-07-22-review-debug-expert-squad.md`. The core prompt is a package input, so packaging directly from the main worktree would contaminate the immutable delivery source. |
| Hard constraints | Use `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` without `--skip-build`; do not create a Git worktree, reset/stash/restore files, bypass hooks, restart/refresh OpenCorvus, or claim non-native rows. Export the committed tree as a temporary non-Git source snapshot, reuse only dependency/build caches, validate there, then publish generated artifacts to the canonical ignored output directories. |
| Sources read | Replacement `AGENTS.md`; current Git status/graph/version; root package scripts; GUI and binary matrix implementations; prior `v0.0.15beta` matrix record; current spec indexes. |
| Whole-repository grep | `script/package-gui-installer-matrix.ts` is the sole GUI matrix owner and stages under `packages/overlay/dist-artifacts/<platform>/`; `script/package-binary-matrix.ts` through `script/package-native-binary.ts` owns the portable CLI matrix under `packages/opencorvus/dist/`. Both matrices expose five native rows and package only the matching host row. |
| Independent-agent feedback | None. The active execution policy does not permit unsolicited sub-agent delegation. |

## Plan

1. Export immutable commit `e87863bb0` to a temporary non-Git source snapshot, link the existing dependency tree and Rust target cache, and verify the snapshot contains none of the four parallel diffs.
2. Run focused package-contract tests in the snapshot.
3. Run `bun run package:gui-installer-matrix` and then `bun run package:binary-matrix` without shortcuts.
4. Independently verify release assets, architectures, embedded versions, signatures, disk image integrity, bundle completeness, sizes, and SHA-256 checksums.
5. Copy only validated generated artifacts to the canonical ignored output directories, remove the temporary snapshot, verify the four parallel diffs are unchanged, and record/push the packaging evidence.

## Verification ledger

- Immutable-source isolation: the exported snapshot core-prompt SHA-256 (`1edfd959...`) matched `HEAD` and differed from the parallel worktree copy (`8e21c3be...`). The four parallel diffs had aggregate SHA-256 `9c4b8e1f...` before packaging and the same hash after artifact publication.
- Snapshot toolchain repair: linking only root `node_modules` initially left package-level Bun workspace dependencies unresolved; linking the existing package dependency trees fixed that temporary environment and the four package-contract suites passed 37/37. The first GUI run then reached the real Vite checker but correctly rejected a source snapshot without a Git index during expert-squad payload generation. Reusing the main repository's read-only index through explicit `GIT_DIR`/`GIT_WORK_TREE` inputs gave the generator its tracked-file authority without creating another Git worktree; the snapshot reported no tracked drift.
- GUI matrix: `bun run package:gui-installer-matrix` rebuilt the Software Development Kit (SDK), two 2,648-module production Vite passes, generated payloads, embedded backend, optimized Rust/Tauri application, ad-hoc signatures, disk image, and application archive. The official release checker accepted `darwin-arm64`; Linux x64/ARM64, Darwin x64, and Windows x64 were explicitly skipped.
- CLI matrix: `bun run package:binary-matrix` rebuilt and smoke-tested the native `darwin-arm64` portable runtime and archive; the same four non-host rows were explicitly skipped.
- Independent inspection: staged GUI, application, CLI, Ripgrep, and Browser Model Context Protocol (MCP) Node executables are Mach-O 64-bit ARM64. CLI reports `0.0.15-beta`, Ripgrep `15.2.0`, Node `v22.23.1`, and both application plist versions are `0.0.15-beta`. Strict deep code-sign verification passed for `ai.opencorvus.overlay`; the signature is ad-hoc and not notarized because Apple release credentials were not configured. `hdiutil verify` accepted the DMG.
- Bundle completeness: `package.json`, `ui/index.html`, `bin/rg`, `browser-mcp-node/browser.mjs`, Node, and Playwright metadata are present. Post-publication release validation passed again. A checksum comparison caught that the first directory sync had left the old canonical CLI executable in place; the canonical directory was replaced from the newly validated archive, then architecture, versions, completeness, and checksums were rerun successfully.
- Repository verification: release-family versions remain aligned at `0.0.15-beta`; historical links, document health, and product docs single-source tests passed 87/87 after the record entered the Git index.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `packages/overlay/dist-artifacts/darwin-arm64/opencorvus-overlay` | 162,888,112 | `7c3cad2df4a5bc225d304de13144a6632ace2a5c4519827acbc2d91a430c78e0` |
| `packages/overlay/dist-artifacts/darwin-arm64/OpenCorvus_0.0.15-beta_aarch64.dmg` | 153,809,722 | `fadbb9be4be84f03af5deedc15718b35cff94c4faead09a06c98ebb3792c06d2` |
| `packages/overlay/dist-artifacts/darwin-arm64/OpenCorvus_0.0.15-beta_aarch64.app.tar.gz` | 153,111,058 | `5c2a5aa2a463d28e4cde1f21350f56898b4b2e942191b7c89e87bfbe28524737` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64/opencorvus` | 100,684,450 | `75e6bb5e0922e1518ae0dcb26c2190e6572a3c597732544b37524892490626e1` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64.tar.gz` | 116,889,607 | `c6280aaa3a46f0a3e0723087e22e5c35e66f3b852003cfc0bb8b43e2e4855f8f` |
