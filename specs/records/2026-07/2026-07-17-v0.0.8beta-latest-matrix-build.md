# v0.0.8beta Latest Matrix Build

## Recall

### User request

- Pull the latest source from legacy remote and compile the package matrix.

### Acceptance criteria

1. Confirm the current `v0.0.8beta` worktree is fast-forward aligned with `legacy-remote/v0.0.8beta` without losing local files.
2. Run the production Graphical User Interface (GUI) installer matrix and portable Command-Line Interface (CLI) binary matrix in the established order: GUI first, CLI second.
3. Verify the native macOS ARM64 row through the real Tauri and OpenCorvus build paths; report all non-host rows as skipped rather than packaged.
4. Verify canonical package version, executable architectures, runtime version output, archive contents/hashes, application metadata, and Apple Disk Image (DMG) integrity.
5. Preserve build outputs as untracked artifacts, commit only this build evidence, and push the record to the legacy remote `v0.0.8beta` branch.

### Hard constraints

- Do not reset the worktree, create another worktree, or interfere with a running OpenCorvus/Overlay process.
- Do not infer package version from the branch name. The repository manifests and `version:check` remain authoritative.
- Use `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` as the single production entrypoints.
- Existing generated artifact directories may be replaced only through those production matrix owners as explicitly requested by this rebuild.
- Do not commit generated installers, binaries, archives, or Tauri schemas.

### Sources read

- `AGENTS.md` from the current task context.
- `script/package-gui-installer-matrix.ts`.
- `script/package-binary-matrix.ts`.
- `specs/records/2026-07/2026-07-16-v0.0.7beta-package-matrix-convergence.md`.
- `specs/records/2026-07/2026-07-16-v0.0.7beta-latest-repackage.md`.

### Whole-repository search evidence

| Surface / call site | Disposition |
| --- | --- |
| Root `package:gui-installer-matrix` | Retain as the five-row GUI matrix owner. It invokes the real SDK and Tauri builds and stages only the native host row. |
| Root `package:binary-matrix` | Retain as the five-row portable CLI matrix owner. It invokes and runtime-verifies only the native host row. |
| `guiInstallerBuildEnvironment` | Retain its canonical manifest-version projection through `OPENCORVUS_VERSION`. |
| `packageNativeBinary` | Retain as the CLI build, execution probe, archive, and staging owner. |
| Existing matrix records | Retain the proven GUI-first, CLI-second order because the GUI sidecar build clears the OpenCorvus distribution directory. |
| `package:linux-binary` | Exclude from this host matrix request; it is the separate Linux/Windows Subsystem for Linux remote-service bundle path. |

### Independent agent feedback

No independent-agent work was requested, so no sub-agent was started.

## Starting evidence

- Current branch: `v0.0.8beta`.
- Local and `legacy-remote/v0.0.8beta` both resolve to `50dbbc78f7507a426eab07157ca40ea3c6e7934b` after fetch.
- Canonical repository version is `0.0.7-beta`; `bun run version:check` passes. The branch name is not a package-version source.
- Initial worktree contains only untracked generated output under `packages/overlay/dist-artifacts/darwin-arm64/` and `packages/overlay/src-tauri/gen/schemas/macOS-schema.json`.

## Execution plan

1. Run the GUI installer matrix and retain the native macOS ARM64 executable, DMG, and application archive.
2. Run the CLI binary matrix so its native runtime and archive remain present after the GUI sidecar build.
3. Inspect executable architectures and versions, application/sidecar metadata, DMG integrity, artifact sizes, and SHA-256 hashes.
4. Run focused matrix tests and repository health checks, record exact evidence, commit only this record/index update, and push to legacy remote.

## Verification ledger

| Command / evidence | Result |
| --- | --- |
| `git fetch legacy-remote v0.0.8beta` before and after compilation | Local HEAD and remote both remained `50dbbc78f7507a426eab07157ca40ea3c6e7934b`; divergence `0 0`. |
| `bun run version:check` | Passed with the canonical release-family version `0.0.7-beta`. |
| `bun run package:gui-installer-matrix` | Packaged the real `darwin-arm64` executable, DMG, and application archive. Linux x64/ARM64, Darwin x64, and Windows x64 were explicitly skipped. Tauri emitted only two existing macOS dead-code warnings. |
| `bun run package:binary-matrix` | Packaged and executed the real `darwin-arm64` CLI bundle, producing a 96 MiB runtime and 111 MiB archive. All non-host rows were explicitly skipped. |
| Native architecture | CLI runtime, staged GUI executable, and application executable are all Mach-O 64-bit ARM64. |
| Runtime/application version | CLI `--version` reports `0.0.7-beta`; `CFBundleShortVersionString` and `CFBundleVersion` both report `0.0.7-beta`. |
| Application integrity | `codesign --verify --deep --strict` passed for `OpenCorvus.app`; `hdiutil verify` reported the staged DMG checksum valid. |
| CLI SHA-256 | Runtime `00c343c014a69dbce2b22ddd74487cf6b9b43bbecf5e11da749b8d670a0279d1`; archive `d2aaf99eeb17fcb5e495611107f9cba9cea5ce7dadf0102f4ef976de2ea324a2`. |
| GUI SHA-256 | Executable `2088c02d0a7fe561a63a56ca143b1739c567985e2eb43f6b60419cfa230e2d70`; DMG `b45faeb8424801739a2788af4a924fe8fbc81637e26930d0fbb340888da676a7`; application archive `ec85e5c9349addd50ff2c8be18135f1efff6be055712e3cd618fdc08ffb38f08`. |
| Matrix contract tests | 25 passed, 0 failed across GUI matrix, binary matrix, and native binary owners. |
| Spec links | Historical-document suite passed 21 tests. The first document-health run failed only because this indexed record was not tracked yet; after staging, the document-health suite passed all 56 tests. |
