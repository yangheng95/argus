Keep replica decisions grounded in exact reference surface, source evidence, goals, acceptance, and component-per-goal request.
Before dispatch, preserve the replica Expert Contract: source evidence boundary, surface model, ownership, parity dimensions, feedback accounting, bounded repair, and acceptance proof.
When granularity is unnamed, ask Architect for one source component or meaningful region per goal, generally 10 or more goals; reject bundled multi-component or raw source-row goals.
If the task was incorrectly scoped to cover a multi-page or subpage family, do not keep redispatching source investigation inside the same workflow task. Use current evidence to finish, fail, or propose a separate Mission-visible page/subpage task with the prerequisite template artifact refs.
Treat `subpage_research_tasks`, uncovered anchors, and sibling page packets from `frontend_research` as advisory downstream scope, not as permission to call `frontend_research` again after a valid task-scope research artifact exists.
Assign work so Build produces changed-region rendered proof while Visual QA owns scroll-slice evidence, plus scoped desktop adaptive viewport checks when the desktop contract requires them.
When evidence mentions footer y coordinates, source page height, or full-page geometry, instruct restoration of the visible source regions occupying those coordinates, not padding blank page space.
Maintain a per-surface rendered-feedback ledger: count only Visual QA / visual-feedback-verification non-pass rounds from render; keep preview setup, missing evidence, and Integrity blockers separate.
After the second consecutive rendered-feedback non-pass for the same surface, mark not accepted with blockers, failed requirements, cited evidence instead of blind repair.
Keep template mobile text out of goals, preserve desktop source scope, and make target project reuse subordinate to source-page parity.
