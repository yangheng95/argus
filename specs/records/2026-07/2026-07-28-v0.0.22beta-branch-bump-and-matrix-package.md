# v0.0.22beta Branch Bump and macOS ARM64 Matrix Package

Status: completed

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current delivery line to `v0.0.22beta` and package it. |
| Acceptance | Create `v0.0.22beta` from the verified `v0.0.21beta` baseline; synchronize the release family to canonical `0.0.22-beta` and MSI (Microsoft Installer) projection `0.0.22.0`; run the native GUI installer matrix before the portable CLI (Command-Line Interface) matrix; verify version, architecture, signatures, DMG (Disk Image) integrity, archive contents, and checksums; commit only release-owned files; push through normal hooks to `myhexin`; and verify local/remote equality. |
| Git baseline | Local `v0.0.21beta` and `myhexin/v0.0.21beta` both started at `7a026df64b777dc8b433ce91367bf2dcc8de678c`; no local or remote `v0.0.22beta` branch existed. The local branch was created at that commit. Its initial push was correctly rejected by the normal pre-push hook because concurrent uncommitted `gateway.ts` and `panel.ts` work does not currently typecheck. |
| Parallel changes | The shared worktree contains extensive uncommitted Architect, Engine, Orchestrator, Panel, Task lifecycle, Overlay, generated SDK/OpenAPI, bilingual documentation, tests, and specification changes. They are protected and must remain outside the release commit. |
| Hard constraints | Do not stash, reset, restore, delete, broadly stage, create a worktree, bypass hooks, or interfere with a running OpenCorvus/Overlay process. Commit subjects use the required `dsw-33987` prefix. Push only to the git-cc `myhexin` remote. |
| Sources read | Root `AGENTS.md`; current Git status/branches/remotes and remote lookup; `script/sync-version.ts`; `RELEASE.md`; `docs/packaging.md`; the repository beta-release playbook; prior `v0.0.19beta` bump and `v0.0.18beta` matrix-package records; root and July specification indexes. |
| Whole-repository grep | Live synchronizer entrypoints are root `version:bump` / `version:check`, `script/release`, and CI (Continuous Integration) checks/workflows. `script/sync-version.ts` owns eight package manifests and their Bun workspace rows, two Cargo manifest/lock pairs, Tauri application/MSI metadata, and the canonical OpenCorvus package version. GUI packaging is owned by `script/package-gui-installer-matrix.ts`; portable CLI packaging delegates from `script/package-binary-matrix.ts` to `script/package-native-binary.ts`. Historical release records are evidence and remain unchanged. |
| Independent-agent feedback | None. The user did not request delegated agents, so the primary agent owns execution and second review. |

## Plan

1. Preserve the complete initial dirty-file inventory and synchronize only the declared release-family targets plus the current release example.
2. Verify exact diffs, canonical `0.0.22-beta`, MSI `0.0.22.0`, focused synchronizer tests, document health, route/API consistency, and the normal hook surface.
3. Stage and commit only release-owned files and exact additive index hunks.
4. Run `bun run package:gui-installer-matrix`, then `bun run package:binary-matrix`.
5. Independently validate native ARM64 artifacts, signatures, DMG/archive contents, versions, hashes, and unsupported-row skips.
6. Re-audit protected changes, push through normal hooks after the concurrent type errors are resolved, and verify `HEAD...myhexin/v0.0.22beta` is `0 0`.

## Verification ledger

- Branch and version: `v0.0.22beta` was created from the exact
  `v0.0.21beta`/`myhexin/v0.0.21beta` commit
  `7a026df64b777dc8b433ce91367bf2dcc8de678c`. The canonical synchronizer
  aligned every declared release target at `0.0.22-beta` and projected Tauri
  MSI metadata to `0.0.22.0`. `bun run version:check` and all 5 synchronizer
  tests pass.
- Release commit: `7d8e77baaa66823217e4e8d1dd288024e168e99f`
  contains only the 14 version-family targets, current release example, this
  record, and the two exact additive specification-index hunks. The complete
  parallel source/test/generated-document worktree remained outside the
  commit.
- GUI matrix: `darwin-arm64` packaged successfully after a real Vite production
  build, embedded backend build, native Tauri build, installer bundle, staging,
  and release-asset validation. Linux x64, Linux ARM64, macOS x64, and Windows
  x64 were explicitly skipped because they require matching native hosts.
- GUI artifact acceptance: the staged executable is a thin ARM64 Mach-O and
  passes strict signature verification. The independently extracted
  `OpenCorvus.app` passes deep strict signature verification; both application
  version fields are `0.0.22-beta`; the application archive contains the
  complete bundle; and `hdiutil verify` reports the DMG checksum valid. Tauri
  explicitly skipped Apple notarization because no notarization credentials
  were configured.
- CLI matrix: `darwin-arm64` packaged successfully; the same four non-native
  rows were explicitly skipped. The staged executable and independently
  extracted archive executable are thin ARM64 Mach-O files, pass strict
  signature verification, and return `0.0.22-beta`. The extracted runtime also
  contains executable `bin/rg`, the Browser MCP (Model Context Protocol) Node
  runtime, and the embedded Overlay UI.
- Packaging regressions: all 49 documented native, Linux, GUI, matrix,
  release-contract, and install-script tests pass.
- Documentation regressions: all 98 synchronizer, historical-link,
  document-health, and product-document single-source tests pass. The
  document-health run used an isolated temporary Git index containing the
  concurrent task's already-linked untracked record so the test could evaluate
  the complete live document graph without staging or committing that
  parallel record.
- Repository validation: full workspace typecheck, SDK (Software Development
  Kit) import and AI runtime checks, API route inventory, generated bilingual
  API documentation, Overlay internationalization, and tracked-source secret
  scan pass through the normal pre-push hook. An initial hook attempt exposed
  concurrent `gateway.ts`/`panel.ts` type errors; after that work converged,
  the hook passed. The canonical SDK build plus `docs:api` synchronized the
  concurrent gateway schema change without including those generated files in
  the release commit.
- Artifact SHA-256 and byte sizes:
  - GUI executable:
    `57731ac151fc15963251485a2094cbbaa3852753adf5062a3cd3c82946d72e0c`,
    174542112 bytes.
  - DMG:
    `b09a0830e03661b332864fed2f2ebf827235df5ea88fb96a8587fa36585ff7d8`,
    166744779 bytes.
  - Application archive:
    `94b220bbcace8ef0feeb2cbf7573f94120145f7a238f5ac5a2a2c8e110901fc8`,
    166035748 bytes.
  - CLI executable:
    `781a01bf351f700898ddfa3db5c7be908857f8fbf0b57234b01ceb7572893b35`,
    102300560 bytes.
  - CLI archive:
    `33aa1a8c1e5dc74f680b124a6784b7d5f32fd70552d47ae56439d1d5b5a9bc02`,
    122502062 bytes.

## Repackage run

Status: completed

### Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Repackage the current `v0.0.22beta` delivery. |
| Acceptance | Rebuild the native GUI matrix before the CLI matrix from the current branch state; verify canonical version, native ARM64 architecture, strict signatures, application metadata, DMG integrity, archive contents, runtime version, checksums, and unsupported-row skips; preserve all concurrent staged, unstaged, and untracked work; record and push the evidence through normal hooks. |
| Git/version baseline | A concurrent delivery advanced local and `myhexin/v0.0.22beta` together to `b1bf929e5bdcfee373a639fdfdcf99c87f63afb6`; fetch confirms `0 0` divergence. `bun run version:check` reports `0.0.22-beta`; the native host is macOS ARM64. |
| Parallel changes | The shared worktree contains staged and unstaged Engine, Task API, storage, server, Overlay test, generated SDK/OpenAPI, bilingual API documentation, and specification work plus an untracked fixture-cleanup record. The complete pre-existing index is protected and must remain unchanged by this repackage. |
| Hard constraints | Run GUI before CLI because GUI owns shared distribution cleanup. Do not stash, reset, restore, delete, broadly stage, create a worktree, bypass hooks, or interfere with a running OpenCorvus/Overlay process. |
| Sources reread | Current Git branch/status/remote equality, canonical version, native host identity, existing package record, current artifact hashes, `docs/packaging.md`, and the repository beta-release playbook. |
| Independent-agent feedback | None. The user did not request delegated agents. |

### Plan

1. Confirm no concurrent packager owns the artifact directories and preserve the exact pre-run Git index.
2. Run `bun run package:gui-installer-matrix`, then `bun run package:binary-matrix`.
3. Independently extract and verify the GUI and CLI archives, architecture, signatures, application versions, DMG integrity, runtime payload, executable version, hashes, and sizes.
4. Run the focused packaging regressions and canonical version check.
5. Append final evidence, commit only this record through an isolated index based on the latest HEAD, push normally, and verify local/remote equality.

### Verification

- Source stability: local and `myhexin/v0.0.22beta` remained at
  `b1bf929e5bdcfee373a639fdfdcf99c87f63afb6` with `0 0` divergence through
  both matrix builds. Canonical release metadata remained aligned at
  `0.0.22-beta`.
- GUI matrix: a fresh SDK generation, real Vite production build, embedded
  backend build, native Tauri release build, application/DMG bundle, staging,
  and release validation completed successfully for `darwin-arm64`. Linux x64,
  Linux ARM64, macOS x64, and Windows x64 were explicitly skipped because they
  require matching native hosts. Tauri skipped notarization because no Apple
  notarization credentials were configured.
- GUI acceptance: the staged executable is a thin ARM64 Mach-O and passes
  strict signature verification. The independently extracted
  `OpenCorvus.app` passes deep strict signature verification; both application
  version fields are `0.0.22-beta`; the archive contains the complete
  application bundle; and `hdiutil verify` reports the DMG checksum valid.
- CLI matrix: a fresh Vite/SDK/native build completed successfully for
  `darwin-arm64`; the same four non-native rows were explicitly skipped. The
  staged and independently extracted executables are thin ARM64 Mach-O files,
  pass strict signature verification, and return `0.0.22-beta`. The extracted
  runtime contains executable `bin/rg`, the Browser MCP Node runtime, and the
  embedded Overlay UI.
- Packaging regressions: all 49 native, Linux, GUI, matrix, release-contract,
  and install-script tests pass.
- Repackage SHA-256 and byte sizes:
  - GUI executable:
    `f8f505ac7043c06f1961be3ef56d3f8748f173336e9d358bdf982c3c4e5d4c59`,
    174542112 bytes.
  - DMG:
    `052c11e222c538f48490e760a7015f0566af88d468b36fe43c089dcfc0d92de7`,
    166755386 bytes.
  - Application archive:
    `5ead8a6ff164b7e106ec97ad3b18a04763b0592f76c9915789f2f3dd22a0ad33`,
    166048401 bytes.
  - CLI executable:
    `690804ff90384fac12fdc33c305ffd70d443332e85b0e916a44c640f9ddbe219`,
    102300560 bytes.
  - CLI archive:
    `ceaa65f36bbabb51c57a01d34ad083bf103097a3291111792cba1d2f4908429f`,
    122508833 bytes.

## Second repackage run

Status: completed

### Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Repackage `v0.0.22beta` again from the current delivery head. |
| Acceptance | Rebuild the native GUI matrix before the CLI matrix from the current clean branch; verify canonical version, native ARM64 architecture, strict signatures, application metadata, DMG integrity, archive contents, runtime version, checksums, focused packaging regressions, and unsupported-row skips; record and push the new evidence through normal hooks. |
| Git/version baseline | Fresh fetch confirms local and `myhexin/v0.0.22beta` are both `ce7cfffefc80d8fe030eec233009b18efe3a0ae8` with `0 0` divergence. The worktree and index are clean, `bun run version:check` reports `0.0.22-beta`, and the native host is macOS ARM64. |
| Prior artifact boundary | Existing ignored artifacts still match the first repackage hashes rooted at the older `b1bf929e5b` baseline. They are stale for the current `ce7cfffefc` source and must be replaced by both matrix owners. |
| Hard constraints | Run GUI before CLI because GUI owns shared distribution cleanup. Do not stash, reset, restore, delete, create a worktree, bypass hooks, or interfere with a running OpenCorvus/Overlay process. |
| Sources reread | The complete existing release/repackage Recall, current branch/status/remote equality, canonical version, host architecture, live artifact hashes, packaging documentation, and repository beta-release playbook. |
| Whole-repository inventory | GUI packaging remains owned by `script/package-gui-installer-matrix.ts`; portable CLI packaging remains delegated from `script/package-binary-matrix.ts` to `script/package-native-binary.ts`. No second packaging implementation or artifact source is introduced. |
| Independent-agent feedback | None. The user did not request delegated agents. |

### Plan

1. Run `bun run package:gui-installer-matrix` from exact source commit `ce7cfffefc`.
2. Run `bun run package:binary-matrix` only after GUI completion.
3. Independently extract and verify both archives, native architecture, strict signatures, application versions, DMG integrity, runtime payload, executable version, hashes, and sizes.
4. Run canonical version, focused packaging regression, route, documentation, and typecheck validation.
5. Replace this pending ledger with final evidence, commit only this record, push normally, and verify local/remote equality.

### Verification

- Source stability: both matrix builds and independent verification ran from
  exact source commit `ce7cfffefc80d8fe030eec233009b18efe3a0ae8`.
  Canonical release metadata remained aligned at `0.0.22-beta`.
- GUI matrix: a fresh SDK generation, real Vite production build, embedded
  backend build, native Tauri release build, application/DMG bundle, staging,
  and release validation completed successfully for `darwin-arm64`. Linux x64,
  Linux ARM64, macOS x64, and Windows x64 were explicitly skipped because they
  require matching native hosts. Tauri skipped notarization because no Apple
  notarization credentials were configured.
- GUI acceptance: the staged and independently extracted executables are thin
  ARM64 Mach-O files. The staged executable passes strict signature
  verification, the extracted complete `OpenCorvus.app` passes deep strict
  signature verification, both application version fields are
  `0.0.22-beta`, the application archive contains the complete bundle, and
  `hdiutil verify` reports the DMG checksum valid.
- CLI matrix: a fresh Vite/SDK/native build completed successfully for
  `darwin-arm64`; the same four non-native rows were explicitly skipped. The
  staged and independently extracted executables are thin ARM64 Mach-O files,
  pass strict signature verification, and return `0.0.22-beta`. The extracted
  runtime contains executable `bin/rg`, the Browser MCP Node runtime, and the
  embedded Overlay UI.
- Validation: all 49 native, Linux, GUI, matrix, release-contract, and
  install-script tests pass. Full workspace typecheck, API route inventory,
  generated bilingual API documentation, and canonical version checks pass.
- Second-repackage SHA-256 and byte sizes:
  - GUI executable:
    `6b13b7c697cf3625abd39308d961b348dfe2ccf7c176742e08aacf9671a1ad0d`,
    174542112 bytes.
  - DMG:
    `ff47ef80c93070555d367a9b47b6361a8712503649e3a3f3efdb5a8c7ee06d34`,
    166742206 bytes.
  - Application archive:
    `91d3be8677e29f6e7e81d38c57ffd9ea62afdde51f2232337d37a1e0da3a79de`,
    166034532 bytes.
  - CLI executable:
    `690804ff90384fac12fdc33c305ffd70d443332e85b0e916a44c640f9ddbe219`,
    102300560 bytes.
  - CLI archive:
    `917129f889c2228844eefe6a51fffa8b2883667687146e51a5b8aee306191f4d`,
    122503778 bytes.
