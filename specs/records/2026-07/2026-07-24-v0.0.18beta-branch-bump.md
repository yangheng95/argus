# v0.0.18beta Branch Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current branch to `v0.0.18beta` while preserving parallel changes. |
| Acceptance | Create `v0.0.18beta` from the current pushed `v0.0.17beta` tip, synchronize the release family to canonical SemVer `0.0.18-beta` and Windows MSI (Microsoft Installer) projection `0.0.18.0`, validate repository contracts, and commit only bump-owned files. The user later explicitly waived the final push. |
| Git baseline | After fetch, local `v0.0.17beta` and `legacy-remote/v0.0.17beta` both resolved to `6be776f4c720d99631df248a1d465ed77d5cf266` with delta `0 0`; neither a local nor remote `v0.0.18beta` branch existed before branch creation. |
| Parallel changes | The shared worktree initially contained the untracked `expert-squads/.DS_Store`, whose SHA-256 is `6b84fb2a9bc22329b6afbd83d9348b2a5e3aa3bd3db828ea27ae998351638393`. During verification, a separate 72-line addition appeared in `specs/current/architecture/15-agent-facts-and-turns.md`, with binary-diff SHA-256 `beaf21fab8a17fe349af5e5f56556fc71555faedd417e40e7c750b4d9f765539`. Neither overlaps the release registry, release documentation, or this record; both must remain unstaged and unchanged by the bump. |
| Remote context | Fetch advanced the independent `legacy-remote/work-v0.0.17beta-yr-0723` branch to `5661b8cfe`; the user requested a version-line bump, not a work-branch merge, so that line is not merged implicitly. |
| Hard constraints | Use `script/sync-version.ts` as the only version writer; do not reset, stash, restore, create a worktree, package, restart OpenCorvus/Overlay, or bypass hooks; use the `dsw-33987` commit prefix. Stop push attempts after the user's explicit override. |
| Sources read | Root `AGENTS.md`; current status, branch, graph, remotes, fetch result, and release-branch existence; `docs/packaging.md`; `script/sync-version.ts`; `RELEASE.md`; the `v0.0.17beta` bump record; root and July spec indexes; the repository beta-release workflow record. |
| Whole-repository grep | `package.json` owns the `version:bump` and `version:check` commands. `script/sync-version.ts` is the sole implementation and owns nine package manifests plus Bun workspace rows, two Cargo manifest/lock pairs, Tauri application metadata, and the MSI projection. `packages/opencorvus/test/script/sync-version.test.ts` is the focused contract suite. `RELEASE.md` is the only live command example still naming `0.0.17beta`; architecture and historical record references remain immutable evidence. |
| Independent-agent feedback | None. The user did not request delegation and the active execution policy forbids unsolicited sub-agent delegation. |

## Plan

1. Commit and push this Recall from the newly created `v0.0.18beta` branch while preserving the unrelated worktree file.
2. Run the repository version synchronizer with `v0.0.18beta` and update the live release command example.
3. Validate version convergence, focused synchronizer tests, required documentation health, and the normal push-hook checks.
4. Stage and commit only version-bump-owned files and verify the protected-file checksum. The original push step was cancelled by the user's later instruction.

## Verification ledger

- Branch baseline: `v0.0.18beta` was created from the fetched and synchronized `v0.0.17beta` tip `6be776f4c`; the Recall commit `82d3a22d5` passed the normal pre-push hook and was published before version mutation.
- Version convergence: `bun run version:bump v0.0.18beta` synchronized all declared release-family targets to `0.0.18-beta` and the Tauri MSI projection to `0.0.18.0`; `bun run version:check` passes.
- Focused verification: all 5 version-synchronizer tests pass; historical links, document health, and product docs single-source suites pass with the combined run at 92/92.
- Repository verification: root typecheck passed all 9 package tasks on the isolated bump state; `bun run api:routes-check` reports 6 clean rules across 32 files; `bun run docs:check` reports 284 operations across 24 groups.
- Scope audit: the bump-owned implementation contains only the 15 synchronizer-owned release-family files, the live `RELEASE.md` example, and this verification record. The separately modified architecture file is excluded; its existing references to `v0.0.17beta` and prior release records remain historical evidence rather than bump targets.
- Preservation audit: `expert-squads/.DS_Store` remains untracked and outside the index with its original SHA-256 `6b84fb2a9bc22329b6afbd83d9348b2a5e3aa3bd3db828ea27ae998351638393`; the parallel architecture diff remains outside the index with SHA-256 `beaf21fab8a17fe349af5e5f56556fc71555faedd417e40e7c750b4d9f765539`.
- Delivery: local bump commit `9fa6db0cb` contains exactly the 17 bump-owned files. A subsequent normal push attempt was blocked because concurrent edits temporarily made new control-message fields required before updating all ingress constructors; those edits remained outside the bump commit. The user then explicitly instructed that no push was needed, so pushing stopped. Local `v0.0.18beta` is intentionally one commit ahead of `legacy-remote/v0.0.18beta`, whose tip remains the already-published Recall commit `82d3a22d5`.
