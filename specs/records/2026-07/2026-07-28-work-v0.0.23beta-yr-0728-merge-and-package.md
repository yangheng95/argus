# work-v0.0.23beta-yr-0728 Merge and Native Package

Status: complete

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Merge `https://git-cc.myhexin.com:6443/yangheng/opencorvus/-/commits/work-v0.0.23beta-yr-0728` and package the converged code. |
| Acceptance | Converge the complete source history into `v0.0.23beta`; retain canonical release version `0.0.23-beta`; verify the merged Browser Preview, Settings titlebar menu, website demo, generated API, and documentation surfaces; run the native GUI matrix before the CLI matrix; independently verify ARM64 architecture, strict signatures, application versions, DMG integrity, archive contents, runtime version, hashes, and unsupported-row skips; commit and push through normal hooks; and prove local/remote equality. |
| Git baseline | Fresh fetch found local and `myhexin/v0.0.23beta` equal at `5f3d23302f52c3ca878ffe4c9d1947fef8dc470b`, with a clean worktree and index. `myhexin/work-v0.0.23beta-yr-0728` is `6b957c201db739a751360d514d17435f2da8ba47`; the current delivery commit is the first parent of that merge commit, so the source is 19 commits ahead and the delivery branch has zero unique commits. |
| Merge topology | `git merge-base HEAD myhexin/work-v0.0.23beta-yr-0728` equals the current `v0.0.23beta` HEAD and `git rev-list --left-right --count` reports `0 19`. The correct convergence is an exact fast-forward to the existing source merge commit; no synthetic conflict resolution, history rewrite, side selection, or second implementation is required. |
| Change inventory | The 54-path source delta contains 3485 insertions and 923 deletions across Browser Preview manual URL routing/UI/tests, Settings titlebar menu interaction and AppKit cleanup, generated SDK/OpenAPI and bilingual API docs, the website landing/demo assets and QA scripts, and consolidated specification records/indexes. It does not modify the canonical version synchronizer, version-family manifests/locks/Tauri metadata, or package-matrix owners. |
| Version evidence | `bun run version:check` passes at `0.0.23-beta`, and the source branch's canonical `packages/opencorvus/package.json` is also `0.0.23-beta`. |
| Hard constraints | Do not stash, reset, restore, delete, create another worktree, bypass hooks, replace an entire side, add fallback/gate/state-machine logic, or interfere with a running OpenCorvus/Overlay process. Playwright, if needed, must run through Node. Commit subjects use the `dsw-33987` prefix and pushes go only to `myhexin`. |
| Sources read | Root `AGENTS.md`; current Git status/branches/remotes and fresh refs; complete 19-commit source log; merge parents and common ancestor; full source name/status/stat inventory; current/source version metadata; `docs/packaging.md`; the existing `v0.0.23beta` package record; and the repository beta-release playbook. |
| Whole-repository inventory | Browser Preview, Settings, website, generated API, specification, and package-matrix source paths were enumerated before merge. GUI packaging remains solely owned by `script/package-gui-installer-matrix.ts`; CLI packaging remains delegated from `script/package-binary-matrix.ts` to `script/package-native-binary.ts`; version remains solely owned by `script/sync-version.ts`. |
| Independent-agent feedback | None. The user did not request delegated agents. |

## Plan

1. Fast-forward `v0.0.23beta` to exact source commit `6b957c201d` and verify the source is now an ancestor with no residual diff.
2. Add this record to the consolidated root/month indexes, then run canonical version, focused merged-surface tests, full typecheck, route, generated documentation, and document-health validation.
3. Run `bun run package:gui-installer-matrix`, then `bun run package:binary-matrix`.
4. Independently extract and verify both native archives, ARM64 architecture, strict signatures, application versions, DMG integrity, runtime payload, executable version, hashes, and sizes.
5. Replace the pending ledger with final evidence; commit the integration-contract repairs, this record, and exact additive index changes; push normally; and verify local/remote `0 0`.

## Verification

- Git convergence: `v0.0.23beta` fast-forwarded from `5f3d23302f52c3ca878ffe4c9d1947fef8dc470b` to exact source commit `6b957c201db739a751360d514d17435f2da8ba47`; the merged source ref and branch had no residual diff.
- Integration repair: merged Task Artifact publication made `persistBrowserPreviewEvidence` asynchronous, required a Task root Session, and replaced mutable runtime-path reads with immutable resource snapshots. The affected Browser Preview tests now await publication, seed the canonical root Session, assert resource refs, reject unsafe inputs before publication, and prove later source replacement cannot change readable evidence. The Dialog adoption inventory no longer names the retired `ScheduledAutomationsHost`.
- Focused regressions: Browser Preview routes passed `38/38`; layout geometry, Browser Preview tool, and Dialog adoption passed `38/38`. Packaging contracts passed `49/49`.
- Static validation: `bun run version:check` reported `0.0.23-beta`; all eight typecheck tasks passed; `api:routes-check` passed across 33 route files; `docs:check` passed with 307 operations in 24 groups; historical-doc links passed; the staged monthly-record tracking assertion passed.
- Visual validation: the real Astro/Vite site rendered at `/docs/`; the English landing hero and `#demo` region were inspected from screenshots, the demo anchor moved to the intended section, the video reached ready state `4`, the page had no horizontal overflow, and browser console warning/error output was empty.
- GUI matrix ran before CLI. Native `darwin-arm64` produced `opencorvus-overlay`, `OpenCorvus_0.0.23-beta_aarch64.dmg`, and `OpenCorvus_0.0.23-beta_aarch64.app.tar.gz`. Linux x64/ARM64, macOS x64, and Windows x64 were explicitly skipped because this host cannot natively verify them.
- CLI matrix produced `opencorvus-darwin-arm64/opencorvus` and `opencorvus-darwin-arm64.tar.gz`; Linux x64/ARM64, macOS x64, and Windows x64 were explicitly skipped for the same native-host reason.
- Independent verification: every extracted executable (`opencorvus-overlay`, `opencorvus`, Browser MCP Node, and `rg`) is ARM64 Mach-O; strict deep code-signature verification passed for the staged executable, extracted app, and CLI; both app plist versions, CLI runtime output, and archive package metadata report `0.0.23-beta`; packaged Node reports `v22.23.1`; the DMG checksum is valid; and both archives contain their expected complete app/runtime trees.
- SHA-256 and byte sizes:
  - GUI executable: `cf4f0162d23eadce92bbdbb4371fca946d7b9ef3a54147ec93c3063c56f9a4da`, `174525600`.
  - DMG: `94ee0ef70fd9c5de37beee068ecb4cb1744ebc1ff2c76649b51d459b4ab20937`, `166739647`.
  - App archive: `903fb4c47b38d58f53295f01261ed03fc578b8e316f63d03ed249e5bb6b56775`, `166036711`.
  - CLI executable: `a9ee825a06feb40bdd7b1b573711c552e8a8a539e749b3bab4c3ab8091c99fcf`, `102316976`.
  - CLI archive: `1ffb98a8b1798dec2aad3406833c8ab05c8f5fa7f86b18306cc142ead141d04f`, `122506865`.
- Delivery commit `9276c11327caf115d5b72a8148542aeb18e03f76` pushed through the normal `myhexin` hooks. A fresh fetch confirmed local and `myhexin/v0.0.23beta` are identical at that commit with `0 0`.
