你是 opencorvus 项目的资深架构评审者。这是第 3 轮实施评审。请尖锐、具体，不要客套。

## 上下文

Round 2 你给过 REQUEST_CHANGES，列出 2 个必修 bug：
1. synthesizeToolErrorReport: severity=blocking 但 verdict=inconclusive，语义矛盾
2. loadTargetMessageText: catch 吞 DB/session-not-found 错误返回 ""

以及测试覆盖差：缺非空 integrity replay 测试。

## 我的处理（commit 0ae7441f3）

1. **synthesizeToolErrorReport** 修复：
   - severity 改 "minor"（tool 失败 ≠ "找到坏东西"）
   - verdict 改用 `deriveFactCheckVerdict()` 计算（unify with agent reports）
   - 结果：unresolved.minor + items_inspected=0 < items_total/2 → "inconclusive"，正确

2. **loadTargetMessageText** 修复：
   - 删 catch 块，让 Message.stream 异常向上传播（DB 错误 / session 不存在）
   - 只有"message 存在但无 text/reasoning parts"返回 ""
   - "stream 完成但没看到 target message id" → throw "snapshot stale" 显式错
   - 任何 throw 会被 orchestrator catch 持久化 outcome=tool_error

3. **测试**：新建 test/fact-check/integrity-replay.test.ts (2 case)
   - non-empty 路径：recordFactCheckAttempt → buildIntegrityReplayContext → renderIntegrityReplayContextPrompt 含 "Prior fact-check attempts" + verdict + correctedCount
   - empty 路径：渲染不含该段

## 必答清单

### A. Round 2 问题是否全部解决？
逐项 ✅ / ⚠️ / ❌，附 ≤ 80 字。

### B. 是否引入新 bug？
重点查：
- synthesizeToolErrorReport 现在 severity=minor 用于 tool_failed。但 deriveFactCheckVerdict 对 unresolved=[minor] 的判定路径：items_total=N, items_inspected=0, corrected=[], unresolved=[minor] → "inconclusive" (correct because items_inspected < total/2)。如果 items_total=0 呢？我用 deriveFactCheckVerdict 的 boundary `items_total === 0 → "clean"`。但 tool 失败 + items_total=0 应该是什么？现在会返回 "clean"，这不对——tool 失败就是失败。
- loadTargetMessageText throw "snapshot stale" 时，orchestrator catch 会持久化 outcome=tool_error，artifact 里 unresolved.claim 是 error 消息（reason），含 messageID 等明文。会泄漏内部 id 吗？是否敏感？

### C. 实际跑测试 + grep 验证
- 实际 grep `severity:\s*"blocking"` in src/orchestrator/tools.ts —— 应该零
- 实际 grep `loadTargetMessageText.*catch\b` —— 应该零
- 跑 `bun test packages/opencorvus/test/fact-check/` 看是否 25+ 个 case 全过

### D. 测试还缺什么必要的吗？
- agent.test.ts 是否真的需要 fact-check 专项？现在 role-contract.test.ts 已覆盖角色注册。
- session-kinds 专项测试是否真的需要独立文件？SESSION_KINDS 数组验证在哪儿？

### E. CLAUDE.md 复核
- synthesizeToolErrorReport 用 deriveFactCheckVerdict — rule 8 单源 OK 吗？还是说"工厂内嵌算法"已经是 rule 5 过度工程？
- loadTargetMessageText 现在的 throw + 上层 catch — rule 6.1 是否允许？工具入口数据完整性 (a) + 上层负责持久化，看起来 OK。

### F. 最终结论
- `APPROVE` — 实施可合并，无遗漏
- `APPROVE_WITH_NITS` — 有 minor 问题，列出
- `REQUEST_CHANGES` — 仍需返工

最后一行 `VERDICT: <label>`

## 输出约束
- markdown 分节，≤ 3500 字
- 实际 grep / read 验证每个断言
- 不修改代码或文件
