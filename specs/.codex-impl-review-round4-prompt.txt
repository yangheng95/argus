你是 opencorvus 项目的资深架构评审者。这是第 4 轮实施评审。请尖锐、具体，不要客套。

## 上下文

Round 3 你给过 REQUEST_CHANGES，唯一 blocker：
- synthesizeToolErrorReport 在 items_total=0 时通过 deriveFactCheckVerdict 路径返回 verdict=clean，污染 tool-error 持久化。

加上 minor 建议：
- role-contract 测试 `if (!agent) continue` 静默通过，应改强断言

## 我的处理（commit 6af7c8199）

1. **Blocker 修复**：synthesizeToolErrorReport 现在 hardcodes `overall_verdict = "inconclusive"`，删除 deriveFactCheckVerdict 调用。doc comment 解释：tool failure / cancel / scope-mismatch 是**定义上**就是 inconclusive，与 verdict tree 正交（verdict tree 假设 agent 成功运行）。

2. **role-contract 强断言**：把 `if (!agent) continue` 改成 `expect(agent).toBeDefined()` with 错误消息指明 contract id。漏注册（如 fact-check 漏 agent.ts state()）会响亮 fail。

3. **回归测试**：
   - empty items + agent throw → outcome=tool_error AND verdict=inconclusive (pin blocker fix)
   - stale targetMessageID throws + persists tool_error (close loadTargetMessageText regression hole)

## 必答清单

### A. Round 3 blocker 是否解决？
- synthesizeToolErrorReport 现在所有路径（items=0 / items>0 / cancel / scope mismatch）verdict 是否都正确？
- regression 测试是否 lock 住所有边界？

### B. 是否引入新 bug？
重点查：
- synthesizeToolErrorReport hardcodes "inconclusive"，未来如果 verdict 枚举变化或者需要新的 "tool_failed" 专属 verdict，会不会被忘记？
- role-contract 强断言会不会让其他遗漏（如 compaction / title / summary 这些已 hidden 的 internal agents）也炸？run rg 确认 AgentRoleContract.all 的所有 key 都在 Agent.state()。
- 9 个 e2e 测试有没有任何 case 现在期望的 verdict 跟新实现冲突？

### C. 实际跑测试
- bun test packages/opencorvus/test/fact-check/  — 应全过
- bun test packages/opencorvus/test/agent/role-contract.test.ts  — 应全过
- bun test packages/opencorvus/test/prompt/worker-prompt-composition.test.ts  — 应全过
- 跑这 3 个 + 报告 pass/fail 数

### D. 实际 grep
- `rg -n 'overall_verdict' packages/opencorvus/src` — 看所有写入 verdict 的位置
- `rg -n 'synthesizeToolErrorReport' packages/opencorvus/src` — 确认调用点合理
- `rg -n 'if \(!agent\)' packages/opencorvus/test/agent/role-contract.test.ts` — 应零

### E. 还有遗漏吗？
- 实施代码是否还有 silent fallback、未持久化的失败、未被测试覆盖的边界？
- 整个 fact-check 实施总共 commit 多少？每个 commit 是否独立 build clean？

### F. 最终结论
- `APPROVE` — 可合并，无遗漏
- `APPROVE_WITH_NITS` — minor 问题（列出 ≤ 3 项）
- `REQUEST_CHANGES` — 仍需返工

注意：我们已经迭代到第 4 轮实施评审。如果剩余问题都是 minor 文档/重构性问题（非语义/合规 bug），请直接 APPROVE 或 APPROVE_WITH_NITS。

最后一行 `VERDICT: <label>`

## 输出约束
- markdown 分节，≤ 3000 字
- 实际 grep / read 验证每条断言
- 不修改代码或文件
