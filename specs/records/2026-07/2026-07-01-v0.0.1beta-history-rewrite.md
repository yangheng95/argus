# v0.0.1beta History Rewrite

## Recall

### User Request

- Create a branch named `v0.0.1beta`.
- Include all current worktree content.
- Starting after the last `dev` commit, rewrite every commit message so it uses `dsw-33987`.
- Submit each commit one by one in history, do not squash.
- Push the result to the git-cc remote, which this repository names `myhexin`.

### Acceptance Criteria

- `v0.0.1beta` exists locally and is the current delivery branch.
- The branch contains the previous current content plus the dirty worktree content from `packages/opencorvus/src/provider/models-snapshot.ts`.
- Every commit in `dev..v0.0.1beta` has a subject beginning with `dsw-33987`.
- The commit count in `dev..v0.0.1beta` remains stable across the message rewrite except for the explicit current-content commit made before rewriting.
- Verification records the exact `dev` commit used as the boundary.
- `v0.0.1beta` is pushed to the `myhexin` remote.

### Hard Constraints

- Do not use broad `git reset` or worktree rollback.
- Do not create an extra git worktree.
- Do not alter user-owned unrelated changes except by the explicit "include all current content" request.
- Do not use `origin` / GitHub push as a substitute for the git-cc `myhexin` push.
- Preserve the dev-relative commit sequence; this is a message rewrite, not a squash.
- If a tool fails, inspect and fix or route around the toolchain issue with evidence rather than hiding it.

### Sources Read

- `AGENTS.md` instructions included in the user prompt.
- `specs/README.md`.
- `specs/records/2026-07/README.md`.
- Current Git branch and status via `git status --short --branch`.
- Branch and remote topology via `git branch --all --verbose --no-abbrev`, `git remote -v`, and `git remote show myhexin`.
- Commit range and boundary checks via `git rev-parse HEAD dev origin/dev myhexin/coding-assistant`, `git merge-base --is-ancestor dev HEAD`, `git rev-list --count dev..HEAD`, and `git log --reverse --format='%h%x09%s' dev..HEAD`.

### Whole-Repository Search Evidence

- `rg -n "dsw-33987|v0\.0\.1beta|commit message|历史改写|dev" specs AGENTS.md` found no existing task plan for this branch rewrite.
- `rg -n "v0\.0\.1beta|dsw-33987|dsw-0000|commit message|filter-branch|git-filter-repo|dev\.\.HEAD|dev\.\.v0" . -g "!*node_modules*" -g "!*.lock"` found existing commit-message guidance and historical `dsw-0000` references, but no existing `v0.0.1beta` or `dsw-33987` release branch plan.

### Independent Agent Feedback

- No sub-agent was launched. The available multi-agent tool contract says not to spawn sub-agents unless the user explicitly asks for sub-agents, delegation, or parallel agent work. This task is deterministic Git history manipulation, so the main agent retains responsibility for inspection, rewrite, verification, and push.

## Execution Plan

1. Confirm `v0.0.1beta` is checked out and `myhexin/v0.0.1beta` does not already exist.
2. Commit all current worktree content with a `dsw-33987` message.
3. Rewrite commit messages in `dev..v0.0.1beta` so subjects begin with `dsw-33987`, replacing an existing leading `dsw-0000` or `dsw-33987` token and otherwise prefixing the original subject.
4. Verify commit count, verify every rewritten subject in `dev..v0.0.1beta`, and inspect the final branch status.
5. Push `v0.0.1beta` to `myhexin`.
