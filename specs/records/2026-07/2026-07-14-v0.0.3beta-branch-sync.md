# v0.0.3beta branch sync

## Recall

### User requirement

- Synchronize the latest `v0.0.3beta` code into the current branch.
- Push the synchronized result to the remote branch `work-v0.0.3beta-yr-0714`.

### Acceptance criteria

- Preserve the current branch's committed work and merge the latest git-cc `v0.0.3beta` history.
- Resolve any conflicts from repository evidence rather than discarding either side wholesale.
- Run merge-focused source, type, documentation, and real-browser visual verification.
- Push the verified current branch to git-cc `work-v0.0.3beta-yr-0714` without bypassing hooks.

### Hard constraints

- Use the current main worktree; do not create a worktree, reset, rebase, rewrite history, or bypass hooks.
- Do not use the stale local `v0.0.3beta` ref; `origin/v0.0.3beta` is the fetched git-cc source.
- Do not restart, close, or otherwise interfere with a running OpenCorvus or Overlay process.
- Preserve the pre-existing dirty work as an independent `dsw-33987` baseline commit before merging.

### Sources read

- `AGENTS.md`
- `specs/records/2026-07/2026-07-14-overlay-startup-and-chrome-parity.md`
- `specs/records/2026-07/2026-07-14-mission-delete-settled-queue-reference-integrity.md`
- `specs/records/2026-07/2026-07-14-right-dock-panel-ownership-and-browser-draft.md`
- Current Git status, branch refs, merge base, commit graph, and overlapping path inventory.

### Whole-repository search evidence

- `git rev-list`, `git merge-base`, `git log --all`, and `git merge-tree` covered the current branch, the target remote branch, and the fetched source branch.
- The current branch and `origin/v0.0.3beta` diverged from `05adf00bf`: the current branch had one original unique commit and the source had four unique commits at initial inspection.
- The stale local `v0.0.3beta` ref remained at the merge base and is not a valid synchronization source.
- Source changes overlapped the preserved work on six paths: Overlay translations, `tree-writer.ts`, conversation styles, and both spec indexes.

### Independent agent feedback

- Git topology audit confirmed a merge is required and recommended committing the dirty baseline before merging the fetched remote-tracking source.
- Dirty-worktree audit identified cross-cutting Overlay, native protocol, deletion-integrity, generated API, and documentation changes, so the work was preserved as its own commit rather than hidden in the merge.

## Implementation plan

1. Fetch git-cc again and re-check both source and target remote refs immediately before integration.
2. Merge `origin/v0.0.3beta` into the current branch with a `dsw-33987` merge subject.
3. Resolve overlapping paths against their current owners and run focused protocol, Overlay, backend, type, route, documentation, and Node-launched browser checks.
4. Review the final commit graph and diff, then push `HEAD` to `origin/work-v0.0.3beta-yr-0714` through the repository hooks.

## Result

- Merged the four fetched `origin/v0.0.3beta` commits after preserving the current branch work and its synchronization plan as separate `dsw-33987` commits.
- A final pre-push fetch found three later source commits; they were merged in a second pass so the delivered branch includes `origin/v0.0.3beta` through `18ee8cfb1`.
- Resolved both index conflicts by retaining records from both branches; removed five index links whose target files do not exist in the repository.
- Restored the repository-provided Drizzle ORM one-time-statement patch through `bun install`. The generated registry-only `bun.lock` rewrite was not retained because the lockfile was clean before installation and package versions did not change.
- PASS: focused Overlay, protocol, timeline-order, Mission deletion, task deletion, typecheck, route inventory, and documentation health tests.
- PASS: the pre-push documentation check exposed and the canonical generator repaired the English and Chinese `/global/projects/discover` error-list drift.
- PASS: after the final source refresh, the canonical artifact generator reconciled the tracked OpenAPI, JavaScript SDK, and bilingual API references with the two newly registered queue and browser-preview evidence routes.
- Repaired three incomplete source-branch contracts exposed by their own new tests: static Bus notification annotations no longer reparse projected payloads; task protocol projections require and expose canonical event source; caller-owned database lifecycle activity, plugin hook snapshots, and browser request evidence identity now have their referenced implementations.
- Completed the source branch's stream-lifecycle contract by keeping close-initiator types in the transport protocol and requiring each Overlay stream owner to report its real disposal, abort, error, supersession, or watchdog cause.
- PASS: five Node-launched real-browser scenarios covering Work Ledger startup, Right Dock/chrome, message chronology, Agent Rail hover context, and settings dialog resizing.
- PASS: manual review of the newly generated scoped screenshots found no merge-induced clipping, ordering, menu, tab, loading-state, or dialog-layout regression.
