# Agent: goal (attempt 1)
- Time: 2026-04-02T06:19:34.553Z
- Sequence: 2
- mode: initial
- model: kimi-k2.5
- toolCalls: 7
- finishReason: stop

## System Prompt

````
You are a senior software architect acting as the goal decomposition brain for OpenCorvus, an autonomous coding orchestrator. You receive a structured specification with requirements and produce executable implementation goals for downstream executor agents.

CRITICAL: You MUST explore the codebase BEFORE producing goals. Goals without real file paths from actual exploration will be rejected.

## Your Process

### Step 0: RECALL + EXPLORE

1. Search project memory for prior decompositions, patterns, lessons learned.
2. List active project conventions and constraints. Preferences are BINDING.
3. **Greenfield detection**: If the spec states the project is greenfield or nearly empty (only root config files exist), minimize exploration — the spec already explored the project structure. Focus on mapping requirements to goals using the spec's findings. Use tools only to verify 1-2 key files (e.g., package.json) still exist.
4. Explore the codebase thoroughly (skip or minimize for greenfield projects):
   - List project root and key subdirectories — understand module layout
   - Read package.json / build config — tech stack, scripts
   - Search for code related to each requirement cluster
   - Read key interfaces, types, APIs that goals will touch
   - Identify shared/boundary files (config, types, entry points)
   - Map dependency relationships between modules
   - Identify test patterns and locations
5. Do NOT use web_search. All information must come from codebase exploration.

### Step 1: MAP REQUIREMENTS → EXPLICIT GOALS

Go through EVERY requirement in the spec. For each one:
- Assign it to a goal (group related requirements that touch the same module)
- Set owned_paths from REAL file paths discovered in Step 0
- Define done_definition with concrete, verifiable criteria
- Set dependencies based on actual code relationships

**Every requirement MUST appear in at least one goal. No exceptions.**

### Step 2: FILL IN IMPLICIT GOALS

Now look at the explicit goals you just created. For each one, trace what it needs to work:
- Does it import types that don't exist yet? → Add a shared types goal
- Does the project need scaffolding first? → Add a bootstrap goal
- Does it need database tables? → Add a migration goal
- Do tests need infrastructure? → Add a test setup goal
- Does the module need to be wired into the app? → Add an integration goal

Implicit goals have NO requirement_ids (leave empty, set `source: implicit`).
They MUST appear earlier in the dependency chain than the explicit goals that need them.

## Output Format

Output using section tags:

<summary>One-line summary</summary>
<goals>
- id: goal_id
  title: Short title
  objective: What this goal accomplishes
  requirement_ids: REQ-1, REQ-2
  source: explicit
  depends_on_goal_ids:
  owned_paths: src/foo.ts, src/bar.ts
  done_definition: Concrete pass/fail criteria
  qa_rule_selectors: build, test
  priority: blocking
  kind: feature

- id: goal_infra
  title: Infrastructure goal
  objective: What it provides
  requirement_ids:
  source: implicit
  depends_on_goal_ids:
  owned_paths: src/types/shared.ts
  done_definition: Types compile and are importable
  qa_rule_selectors: build
  priority: blocking
  kind: bootstrap
</goals>

**Goal kinds:** bootstrap, feature, verification, integration, system
**Priority:** blocking or advisory
**qa_rule_selectors:** build, test, lint, verify_cmd, startup, ui_review, code_quality, code_review, dead_code_review, spec_check

## Parallel Execution & Isolation

Goals are executed **in parallel, each in an isolated git worktree**. Maximize parallelism:

- **Parallel-first design**: The default state is that goals have NO dependencies and run simultaneously. Only add `depends_on_goal_ids` when there is a genuine data dependency (one goal MUST read output produced by another). Do NOT add dependencies just because goals touch the same domain — if their interfaces are well-defined, they can run in parallel.
- **File ownership must be disjoint**: Two concurrent goals MUST NOT write to the same file. If two goals both need to modify `src/index.ts`, one must depend on the other OR a final integration goal should wire them together.
- **Shared code goes into a bootstrap goal**: Types, interfaces, config schemas, database migrations, and any code needed by multiple goals MUST be in an early bootstrap/infrastructure goal that others depend on. This goal runs first and its output is available to all downstream goals. Keep bootstrap goals minimal — only truly shared definitions.
- **Dependencies encode data flow, not domain proximity**: If Goal B reads files produced by Goal A, Goal B MUST list Goal A in `depends_on_goal_ids`. But if Goal A and Goal B work on independent modules that will be integrated later, they SHOULD NOT depend on each other — use an integration goal instead.
- **Minimize cross-goal coupling**: Design goals so each one produces a self-contained module. Avoid designs where many goals modify a single shared entry point file — instead, use a final integration goal that wires modules together.
- **Owned paths = exclusive write access**: During execution, each goal can ONLY write to its `owned_paths`. If you assign `src/db.ts` to Goal A, no other concurrent goal may modify it.
- **Anti-pattern: serial chain**: If your goals form a strict linear chain (A→B→C→D→E), your decomposition has failed. A well-decomposed plan should have most feature goals at the same dependency depth, branching from a shared bootstrap.

## Rules

- ALWAYS explore codebase before producing goals.
- Every owned_path MUST come from actual tool exploration — never guess paths.
- Every spec requirement MUST be covered by an explicit goal.
- Implicit goals MUST exist for infrastructure that explicit goals depend on.
- Owned paths of concurrent goals (no dependency between them) MUST NOT overlap.
- **Maximum 10 goals total** (explicit + implicit). If you have more, merge related goals. Fewer, larger goals with clear boundaries are better than many small, interdependent ones.
- Write in the same language as the request.

Output text directly. Do NOT output JSON. Do NOT wrap in code blocks.

## Quality Self-Check (MANDATORY)

Before outputting, verify each of these. If ANY answer is NO, use more tools:

1. Does every goal have owned_paths discovered via tools (not guessed)?
2. Is every spec requirement covered by at least one goal?
3. Are dependencies correct (types/shared code before consumers)?
4. Does each goal have a concrete, verifiable done_definition?
5. Are goals independently executable (respecting declared dependencies)?
6. Could a planner create implementation steps from these goals WITHOUT further exploration?
7. **Parallelism check**: Can at least 50% of feature goals run in parallel (depth ≤ 2 from root)? If most goals form a linear chain, redesign the decomposition.

````

## Messages (1)

### Message 1 [user]

# Task

Title: prd-stock-trading-sim

Request:
模拟股票交易 Web 应用 PRD — 交付一个商业级可运行的 MVP 全栈应用。

## 技术栈（固定）

### 后端
- **运行时**: Bun
- **语言**: TypeScript (strict mode)
- **框架**: Hono
- **数据库**: SQLite via `bun:sqlite`，数据文件 `data/trading.db`
- **实时推送**: Server-Sent Events (SSE) — 行情推送
- **密码哈希**: `Bun.password.hash()` / `Bun.password.verify()`
- **入口**: `index.ts`，同时服务 API 和前端静态资源

### 前端
- **框架**: React 19 + TypeScript
- **脚手架**: Vite (react-ts 模板结构)
- **路由**: React Router v7
- **样式**: Tailwind CSS v4
- **图表**: Recharts（K线简化版用柱状图 + 折线图）
- **HTTP**: 原生 fetch + SSE EventSource
- **前端目录**: `web/`，构建产物 `web/dist/`

### 项目结构
```
index.ts                 # Hono 入口，挂载 API + SSE + 静态资源
src/
  db.ts                  # SQLite DDL + 初始化 + 种子数据
  auth.ts                # 认证中间件 + 路由
  market.ts              # 行情路由 + SSE 推送
  trading.ts             # 交易路由（买入/卖出/撤单）
  portfolio.ts           # 持仓 + 资产路由
  history.ts             # 交易历史 + 收益统计
  simulator.ts           # 行情模拟引擎（随机波动 + 趋势）
web/
  index.html
  src/
    main.tsx
    App.tsx
    api.ts               # API client + SSE 订阅
    components/
    pages/
    hooks/
    styles/
  tailwind.config.ts
  vite.config.ts
  tsconfig.json
data/                    # SQLite（gitignore）
tests/
  api.test.ts            # 后端 API 测试
```

### 启动方式
```bash
cd web && bun install && bun run build && cd ..
bun run index.ts
# 浏览器打开 http://localhost:3000
```

## 交付范围（MVP P0 功能）

### 后端 API

1. **认证**
   - `POST /api/auth/register` — 用户名+密码注册，初始资金 1,000,000
   - `POST /api/auth/login` — 登录返回 Bearer token

2. **行情**
   - `GET /api/market/stocks` — 股票列表（代码、名称、最新价、涨跌幅、成交量）
   - `GET /api/market/stocks/:code` — 单只股票详情 + 最近 60 个价格点
   - `GET /api/market/sse` — SSE 实时行情推送（每 2 秒一次全量快照）

3. **交易**
   - `POST /api/trading/buy` — 限价买入（股票代码、数量、价格）
   - `POST /api/trading/sell` — 限价卖出
   - `POST /api/trading/cancel/:id` — 撤销挂单
   - `GET /api/trading/orders` — 当前委托单列表（pending/filled/cancelled）

4. **持仓**
   - `GET /api/portfolio` — 当前持仓（股票、数量、成本价、市值、盈亏）
   - `GET /api/portfolio/summary` — 总资产、可用资金、总市值、总盈亏、收益率

5. **历史**
   - `GET /api/history/trades` — 成交记录（支持 ?stock=&limit=&offset=）
   - `GET /api/history/daily` — 每日资产曲线（最近 30 天）

### 行情模拟引擎

- 预置 10 只模拟股票（代码 SH600001-SH600010，中文名称）
- 每 2 秒生成一次新价格：
  - 基于上一个价格 ± 随机波动（正态分布，σ=0.5%）
  - 叠加趋势因子（每只股票有隐含趋势 -0.1% ~ +0.1%）
  - 涨跌幅限制 ±10%（基于当日开盘价）
- 每次价格变动自动撮合挂单：
  - 买单：市价 ≤ 买入价 → 成交
  - 卖单：市价 ≥ 卖出价 → 成交
- 服务启动时生成当日历史价格（60 个点，模拟开盘至今）

### 前端页面

React SPA，5 个主要视图：

1. **登录/注册页** (`/login`, `/register`)
   - 用户名+密码表单
   - 注册成功显示初始资金
   - 登录成功跳转首页

2. **行情大盘** (`/`) — 首页
   - 10 只股票实时报价表格（SSE 驱动，实时刷新）
   - 每行：代码、名称、最新价、涨跌额、涨跌幅%、成交量
   - 涨红跌绿配色（中国股市惯例）
   - 点击某只股票进入详情

3. **股票详情 + 交易** (`/stock/:code`)
   - 价格走势图（Recharts AreaChart，60 个点，实时追加）
   - 当前价格、涨跌幅、最高/最低/开盘价
   - 买入/卖出面板：
     - 输入数量（100 的整数倍）和价格
     - 可用资金/可卖数量提示
     - 买入/卖出按钮
   - 该股票的当前挂单列表（可撤单）

4. **我的持仓** (`/portfolio`)
   - 资产概览：总资产、可用资金、总市值、总盈亏（金额+百分比）
   - 持仓列表：股票、数量、成本价、现价、市值、盈亏额、盈亏率
   - 盈亏颜色：盈利红色、亏损绿色
   - 每日资产曲线图（Recharts LineChart，30 天）

5. **交易记录** (`/history`)
   - 成交记录表格：时间、股票、方向（买/卖）、数量、价格、金额
   - 筛选：按股票代码
   - 分页加载

### 全局 UI

- **顶部导航**: 行情 | 持仓 | 记录 | 退出
- **实时连接状态**: SSE 连接指示器（绿点=连接中）
- **主题**: 深色主题（交易软件标配）
- **响应式**: 桌面优先，平板可用
- **数字格式**: 价格保留 2 位小数，金额千分位分隔，百分比 1 位小数

## 数据表

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  balance REAL NOT NULL DEFAULT 1000000,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE sessions (
  token TEXT PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE stocks (
  code TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  open_price REAL NOT NULL,
  current_price REAL NOT NULL,
  high_price REAL NOT NULL,
  low_price REAL NOT NULL,
  volume INTEGER NOT NULL DEFAULT 0,
  trend REAL NOT NULL DEFAULT 0,
  updated_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE price_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  stock_code TEXT NOT NULL REFERENCES stocks(code),
  price REAL NOT NULL,
  volume INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE orders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  stock_code TEXT NOT NULL REFERENCES stocks(code),
  direction TEXT NOT NULL CHECK(direction IN ('buy', 'sell')),
  quantity INTEGER NOT NULL,
  price REAL NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'filled', 'cancelled')),
  filled_at INTEGER,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE TABLE positions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  stock_code TEXT NOT NULL REFERENCES stocks(code),
  quantity INTEGER NOT NULL DEFAULT 0,
  cost_basis REAL NOT NULL DEFAULT 0,
  UNIQUE(user_id, stock_code)
);

CREATE TABLE daily_snapshots (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  total_assets REAL NOT NULL,
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);
```

### 种子数据（10 只股票）

| 代码 | 名称 | 开盘价 | 趋势 |
|------|------|--------|------|
| SH600001 | 浦发银行 | 8.50 | +0.02% |
| SH600002 | 齐鲁石化 | 12.30 | -0.01% |
| SH600003 | 东北高速 | 15.80 | +0.05% |
| SH600004 | 白云机场 | 22.40 | +0.03% |
| SH600005 | 武钢股份 | 5.60 | -0.03% |
| SH600006 | 东风汽车 | 9.20 | +0.01% |
| SH600007 | 中国国贸 | 18.90 | +0.04% |
| SH600008 | 首创环保 | 3.80 | -0.02% |
| SH600009 | 上海机场 | 45.60 | +0.06% |
| SH600010 | 包钢股份 | 2.10 | -0.04% |

## 安全规则

- 密码用 Argon2 哈希存储
- 未认证请求返回 401
- 用户只能操作自己的订单和持仓
- 买入校验：可用资金 ≥ 数量 × 价格
- 卖出校验：持仓数量 ≥ 卖出数量
- 交易数量必须是 100 的整数倍（1 手 = 100 股）
- 挂单价格限制：当前价 ±10%

## 测试要求

`tests/api.test.ts` 使用 `bun:test`，至少覆盖：

1. 注册返回用户信息和初始资金
2. 登录返回 token
3. 获取股票列表返回 10 只股票
4. 买入成功扣减资金
5. 买入数量不是 100 倍数被拒绝
6. 资金不足买入被拒绝
7. 卖出成功增加资金
8. 持仓不足卖出被拒绝
9. 撤单成功恢复资金/持仓
10. 用户隔离（A 不能撤 B 的单）

## 验收标准

- `cd web && bun install && bun run build` 前端构建成功
- `bunx tsc --noEmit` 后端类型检查通过
- `bun test` API 测试通过
- `bun run index.ts` 启动后，浏览器打开 http://localhost:3000 看到完整交易应用
- 行情表格每 2 秒实时刷新（SSE 驱动，非轮询）
- 可完成完整流程：注册 → 登录 → 查看行情 → 买入 → 查看持仓 → 卖出 → 查看记录
- 深色主题，涨红跌绿
- 没有 README、文档等无关产物

# Spec Requirements (18 total)

Each requirement MUST be covered by at least one goal.

- **req_1**: 后端数据库模块实现 [blocking] — 创建 src/db.ts，实现 SQLite 数据库初始化、DDL 执行、种子数据插入（10 只股票）、当日 60 个价格历史生成。使用 bun:sqlite，数据文件路径 data/trading.db。
- **req_2**: 后端认证模块实现 [blocking] — 创建 src/auth.ts，实现 POST /api/auth/register、POST /api/auth/login 路由，Bearer token 认证中间件，使用 Bun.password.hash/verify() 进行密码处理。
- **req_3**: 后端行情模块实现 [blocking] — 创建 src/market.ts，实现 GET /api/market/stocks、GET /api/market/stocks/:code 路由，以及 GET /api/market/sse SSE 端点，每 2 秒推送全量股票快照。
- **req_4**: 后端交易模块实现 [blocking] — 创建 src/trading.ts，实现 POST /api/trading/buy、POST /api/trading/sell、POST /api/trading/cancel/:id、GET /api/trading/orders 路由，包含数量 100 倍数校验、资金/持仓校验、价格范围校验。
- **req_5**: 后端持仓模块实现 [blocking] — 创建 src/portfolio.ts，实现 GET /api/portfolio、GET /api/portfolio/summary 路由，计算持仓市值、盈亏、总资产。
- **req_6**: 后端历史模块实现 [blocking] — 创建 src/history.ts，实现 GET /api/history/trades（支持筛选分页）、GET /api/history/daily 路由。
- **req_7**: 行情模拟引擎实现 [blocking] — 创建 src/simulator.ts，实现每 2 秒价格生成（正态分布随机波动 + 趋势因子 + 涨跌停限制），以及自动撮合 pending 订单逻辑。
- **req_8**: 后端入口文件实现 [blocking] — 创建 index.ts，初始化 Hono 应用，挂载所有 API 路由，配置 SSE 端点，服务 web/dist 静态资源，监听 3000 端口。
- **req_9**: 前端项目初始化 [blocking] — 创建 web/ 目录，初始化 Vite + React 19 + TypeScript 项目，配置 Tailwind CSS v4，安装 react-router v7 和 recharts。
- **req_10**: 前端 API 客户端实现 [blocking] — 创建 web/src/api.ts，封装 fetch API 调用，实现 SSE EventSource 订阅管理，自动处理 Bearer token 认证。
- **req_11**: 前端页面实现 - 登录注册 [blocking] — 创建 web/src/pages/Login.tsx 和 Register.tsx，实现表单验证，登录成功后存储 token 并跳转。
- **req_12**: 前端页面实现 - 行情大盘 [blocking] — 创建 web/src/pages/Market.tsx 和 StockTable.tsx，实现 SSE 驱动的实时股票表格，涨红跌绿配色，点击跳转详情。
- **req_13**: 前端页面实现 - 股票详情 [blocking] — 创建 web/src/pages/StockDetail.tsx、StockChart.tsx、TradePanel.tsx、OrderList.tsx，实现价格走势图（Recharts）、买卖面板、挂单列表。
- **req_14**: 前端页面实现 - 持仓 [blocking] — 创建 web/src/pages/Portfolio.tsx 和 PortfolioTable.tsx，实现资产概览、持仓列表、每日资产曲线图。
- **req_15**: 前端页面实现 - 交易记录 [blocking] — 创建 web/src/pages/History.tsx 和 TradeHistory.tsx，实现成交记录表格、股票筛选、分页加载。
- **req_16**: 前端导航和布局组件 [blocking] — 创建 web/src/components/Navigation.tsx 和 SSEIndicator.tsx，实现顶部导航栏和 SSE 连接状态指示器。
- **req_17**: API 测试文件 [blocking] — 创建 tests/api.test.ts，使用 bun:test 实现 10 个测试用例覆盖注册、登录、股票列表、买入、卖出、撤单、用户隔离。
- **req_18**: 根级 package.json 配置 [blocking] — 配置根级 package.json，添加 hono 依赖，配置测试脚本、启动脚本。

# Spec Summary

股票交易模拟器全栈应用 - 基于 Hono + Bun + SQLite 后端和 React 19 + Vite + Tailwind v4 前端的 MVP 实现

Now recall memory, check preferences, explore the codebase thoroughly, then output your goal decomposition using section tags as described in your instructions. Ensure EVERY spec requirement is covered.

## Output

This is a greenfield project with no existing source files. The spec has already detailed the complete project structure. I'll now create the goal decomposition based on the spec requirements.

Given this is a full-stack application with clear module boundaries, I'll organize goals to maximize parallel execution:

1. **Bootstrap goals** (run first): Root package.json, Database schema
2. **Backend modules** (can run in parallel after bootstrap): Auth, Market, Trading, Portfolio, History, Simulator
3. **Backend entry** (after all backend modules): index.ts
4. **Frontend bootstrap**: Vite + React setup
5. **Frontend modules** (can run in parallel after frontend bootstrap): API client, Pages, Components
6. **Frontend entry** (after frontend modules): App.tsx, main.tsx
7. **Integration/Testing**: API tests

<summary>股票交易模拟器全栈应用 - 基于 Hono + Bun + SQLite 后端和 React 19 + Vite + Tailwind v4 前端的 MVP 实现，包含认证、行情、交易、持仓、历史等完整功能模块</summary>
<goals>
- id: goal_root_config
  title: 根级项目配置
  objective: 配置根级 package.json，添加 Hono、Bun 类型等依赖，配置启动和测试脚本
  requirement_ids: req_18
  source: explicit
  depends_on_goal_ids:
  owned_paths: package.json
  done_definition: package.json 包含 hono、bun-types 依赖，配置 start/test 脚本，bun install 成功
  qa_rule_selectors: build
  priority: blocking
  kind: bootstrap

- id: goal_db
  title: 数据库模块
  objective: 创建 SQLite 数据库模块，实现 DDL、初始化、种子数据插入（10只股票）、当日价格历史生成
  requirement_ids: req_1
  source: explicit
  depends_on_goal_ids: goal_root_config
  owned_paths: src/db.ts
  done_definition: src/db.ts 实现所有表 DDL、种子数据插入函数、价格历史生成函数，使用 bun:sqlite，数据文件路径 data/trading.db
  qa_rule_selectors: build
  priority: blocking
  kind: bootstrap

- id: goal_auth
  title: 认证模块
  objective: 实现用户注册、登录、Bearer token 认证中间件
  requirement_ids: req_2
  source: explicit
  depends_on_goal_ids: goal_db
  owned_paths: src/auth.ts
  done_definition: src/auth.ts 实现 POST /api/auth/register、POST /api/auth/login 路由，Bearer token 中间件，使用 Bun.password.hash/verify
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_market
  title: 行情模块
  objective: 实现股票列表、单只股票详情、SSE 实时行情推送
  requirement_ids: req_3
  source: explicit
  depends_on_goal_ids: goal_db
  owned_paths: src/market.ts
  done_definition: src/market.ts 实现 GET /api/market/stocks、GET /api/market/stocks/:code、GET /api/market/sse 端点，SSE 每 2 秒推送全量快照
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_trading
  title: 交易模块
  objective: 实现买入、卖出、撤单、委托单查询，包含数量和资金校验
  requirement_ids: req_4
  source: explicit
  depends_on_goal_ids: goal_db, goal_auth
  owned_paths: src/trading.ts
  done_definition: src/trading.ts 实现 POST /api/trading/buy、sell、cancel/:id、GET /api/trading/orders 路由，包含 100 倍数、资金、持仓、价格范围校验
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_portfolio
  title: 持仓模块
  objective: 实现持仓查询和资产概览，计算市值、盈亏、总资产
  requirement_ids: req_5
  source: explicit
  depends_on_goal_ids: goal_db, goal_auth
  owned_paths: src/portfolio.ts
  done_definition: src/portfolio.ts 实现 GET /api/portfolio、GET /api/portfolio/summary 路由，正确计算持仓市值、盈亏、总资产
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_history
  title: 历史模块
  objective: 实现成交记录查询和每日资产曲线
  requirement_ids: req_6
  source: explicit
  depends_on_goal_ids: goal_db, goal_auth
  owned_paths: src/history.ts
  done_definition: src/history.ts 实现 GET /api/history/trades（支持筛选分页）、GET /api/history/daily 路由
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_simulator
  title: 行情模拟引擎
  objective: 实现价格生成算法和自动撮合逻辑
  requirement_ids: req_7
  source: explicit
  depends_on_goal_ids: goal_db
  owned_paths: src/simulator.ts
  done_definition: src/simulator.ts 实现每 2 秒价格生成（正态分布波动+趋势因子+涨跌停限制），自动撮合 pending 订单逻辑
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_backend_entry
  title: 后端入口文件
  objective: 创建 Hono 应用入口，挂载所有路由和中间件，配置静态资源服务
  requirement_ids: req_8
  source: explicit
  depends_on_goal_ids: goal_auth, goal_market, goal_trading, goal_portfolio, goal_history, goal_simulator
  owned_paths: index.ts
  done_definition: index.ts 初始化 Hono，挂载所有 API 路由，配置 SSE 端点，服务 web/dist 静态资源，监听 3000 端口
  qa_rule_selectors: build, startup
  priority: blocking
  kind: integration

- id: goal_frontend_bootstrap
  title: 前端项目初始化
  objective: 初始化 Vite + React 19 + TypeScript 项目，配置 Tailwind CSS v4，安装依赖
  requirement_ids: req_9
  source: explicit
  depends_on_goal_ids: goal_root_config
  owned_paths: web/package.json, web/vite.config.ts, web/tsconfig.json, web/tailwind.config.ts, web/index.html
  done_definition: web/ 目录包含完整 Vite React 项目结构，package.json 包含 react-router v7、recharts、tailwindcss v4 依赖，vite build 成功
  qa_rule_selectors: build
  priority: blocking
  kind: bootstrap

- id: goal_frontend_api
  title: 前端 API 客户端
  objective: 实现 API 封装和 SSE 订阅管理
  requirement_ids: req_10
  source: explicit
  depends_on_goal_ids: goal_frontend_bootstrap
  owned_paths: web/src/api.ts
  done_definition: web/src/api.ts 封装 fetch API 调用，实现 SSE EventSource 订阅管理，自动处理 Bearer token
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_auth_pages
  title: 前端登录注册页面
  objective: 实现登录和注册页面，表单验证和跳转逻辑
  requirement_ids: req_11
  source: explicit
  depends_on_goal_ids: goal_frontend_api
  owned_paths: web/src/pages/Login.tsx, web/src/pages/Register.tsx
  done_definition: Login.tsx 和 Register.tsx 实现用户名密码表单，表单验证，登录成功存储 token 并跳转首页
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_market
  title: 前端行情大盘页面
  objective: 实现行情大盘页面，SSE 驱动的实时股票表格
  requirement_ids: req_12
  source: explicit
  depends_on_goal_ids: goal_frontend_api
  owned_paths: web/src/pages/Market.tsx, web/src/components/StockTable.tsx
  done_definition: Market.tsx 和 StockTable.tsx 实现 SSE 驱动的实时股票表格，涨红跌绿配色，点击跳转详情
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_stock_detail
  title: 前端股票详情页面
  objective: 实现股票详情页，包含价格走势图、买卖面板、挂单列表
  requirement_ids: req_13
  source: explicit
  depends_on_goal_ids: goal_frontend_api
  owned_paths: web/src/pages/StockDetail.tsx, web/src/components/StockChart.tsx, web/src/components/TradePanel.tsx, web/src/components/OrderList.tsx
  done_definition: StockDetail.tsx 实现价格走势图（Recharts AreaChart），TradePanel.tsx 实现买卖面板，OrderList.tsx 实现挂单列表和撤单功能
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_portfolio
  title: 前端持仓页面
  objective: 实现持仓页面，包含资产概览、持仓列表、资产曲线图
  requirement_ids: req_14
  source: explicit
  depends_on_goal_ids: goal_frontend_api
  owned_paths: web/src/pages/Portfolio.tsx, web/src/components/PortfolioTable.tsx
  done_definition: Portfolio.tsx 实现资产概览和每日资产曲线图（Recharts LineChart），PortfolioTable.tsx 实现持仓列表，盈亏红绿配色
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_history
  title: 前端交易记录页面
  objective: 实现交易记录页面，包含成交记录表格、筛选、分页
  requirement_ids: req_15
  source: explicit
  depends_on_goal_ids: goal_frontend_api
  owned_paths: web/src/pages/History.tsx, web/src/components/TradeHistory.tsx
  done_definition: History.tsx 和 TradeHistory.tsx 实现成交记录表格，支持股票代码筛选和分页加载
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_components
  title: 前端导航和布局组件
  objective: 实现顶部导航栏和 SSE 连接状态指示器
  requirement_ids: req_16
  source: explicit
  depends_on_goal_ids: goal_frontend_api
  owned_paths: web/src/components/Navigation.tsx, web/src/components/SSEIndicator.tsx
  done_definition: Navigation.tsx 实现顶部导航（行情|持仓|记录|退出），SSEIndicator.tsx 实现 SSE 连接状态指示器（绿点=连接中）
  qa_rule_selectors: build
  priority: blocking
  kind: feature

- id: goal_frontend_entry
  title: 前端入口和路由
  objective: 实现 App.tsx 路由配置和 main.tsx 入口，整合所有页面
  requirement_ids: req_9
  source: explicit
  depends_on_goal_ids: goal_frontend_auth_pages, goal_frontend_market, goal_frontend_stock_detail, goal_frontend_portfolio, goal_frontend_history, goal_frontend_components
  owned_paths: web/src/App.tsx, web/src/main.tsx
  done_definition: App.tsx 配置 React Router v7 路由（/login, /register, /, /stock/:code, /portfolio, /history），main.tsx 渲染应用，深色主题
  qa_rule_selectors: build
  priority: blocking
  kind: integration

- id: goal_tests
  title: API 测试
  objective: 实现后端 API 测试，覆盖注册、登录、交易、撤单、用户隔离等场景
  requirement_ids: req_17
  source: explicit
  depends_on_goal_ids: goal_backend_entry
  owned_paths: tests/api.test.ts
  done_definition: tests/api.test.ts 使用 bun:test 实现 10 个测试用例，覆盖注册、登录、股票列表、买入、卖出、撤单、用户隔离，bun test 通过
  qa_rule_selecters: test
  priority: blocking
  kind: verification
</goals>
