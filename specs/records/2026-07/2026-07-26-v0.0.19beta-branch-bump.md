# v0.0.19beta Branch Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current delivery line to `v0.0.19beta` while preserving every repository change. |
| Acceptance | Create and publish `v0.0.19beta` from the current `v0.0.18beta` HEAD, synchronize the release family to canonical SemVer `0.0.19-beta` and MSI (Microsoft Installer) projection `0.0.19.0`, validate repository contracts, commit only bump-owned files, push to `legacy-remote`, and leave all parallel tracked/untracked changes intact. |
| Git baseline | Local `v0.0.18beta` started at `188d0f6e6b`, two commits ahead of `legacy-remote/v0.0.18beta`; no local or remote `v0.0.19beta` branch existed. `v0.0.19beta` was created at that HEAD and its baseline was pushed through normal hooks. |
| Parallel changes | The shared worktree contains 43 tracked source/test/document edits plus untracked runtime, fixture, screenshot, and test files. The pre-push route check also proved generated OpenAPI, SDK (Software Development Kit), and bilingual API documentation drift from the parallel config-route work; the canonical generators refreshed those files, and they remain protected parallel work outside the bump commit. |
| Hard constraints | Use `script/sync-version.ts` as the sole version writer; do not stash, reset, restore, delete, broadly stage, create another worktree, package, or restart OpenCorvus/Overlay; use the `dsw-33987` commit prefix and push only to `legacy-remote`. |
| Sources read | Root `AGENTS.md`; beta-release skill; current status, graph, branches, remotes, and remote target lookup; `docs/packaging.md`; `script/sync-version.ts`; `RELEASE.md`; prior `v0.0.17beta` record; root and July spec indexes. |
| Whole-repository grep | Live callers of the single synchronizer are root `version:bump` / `version:check`, three CI (Continuous Integration) workflows, and `script/release`. The synchronizer owns eight package manifests and Bun workspace rows, two Cargo manifest/lock pairs, Tauri application/MSI metadata, and the canonical OpenCorvus package version. Historical release records remain immutable. |
| Independent-agent feedback | None. The user did not request sub-agents, and active execution policy forbids unsolicited delegation. |

## Plan

1. Publish the current HEAD as the `v0.0.19beta` branch baseline without touching protected worktree changes.
2. Land this Recall and indexes, then synchronize the declared release family and update the live release example.
3. Validate version convergence, focused synchronizer tests, documentation health, and normal push-hook checks.
4. Stage and commit only bump-owned files, push `v0.0.19beta` to `legacy-remote`, verify local/remote equality, and re-audit preservation of parallel changes.

## Verification ledger

- Branch baseline: `v0.0.19beta` was created at `188d0f6e6b` and pushed through normal hooks after canonical SDK/OpenAPI/API-document generation resolved drift caused by protected parallel route work.
- Version convergence: `bun run version:bump v0.0.19beta` synchronized every declared target to `0.0.19-beta` and the Tauri MSI projection to `0.0.19.0`; `bun run version:check` passes.
- Focused verification: all 5 synchronizer tests and all 92 historical-link, document-health, and product-doc single-source tests pass. Route inventory and generated API documentation checks also pass.
- Preservation boundary: generated SDK/OpenAPI/API-document changes and every pre-existing tracked/untracked path remain outside the release index. Only the synchronizer targets, `RELEASE.md`, and this verification record are owned by the bump commit.
