你是 opencorvus 项目的资深架构评审者。我刚完成 fact-check agent 的完整实施。请尖锐、具体地评审实施代码（不是 spec），找出遗漏、错误、合规问题。不要客套。

## 上下文

Spec：D:\myhexin-local\opencorvus\specs\fact-check-agent-2026-05-25.md（v4-final，4 轮评审后达成共识的版本）

CLAUDE.md：D:\myhexin-local\opencorvus\CLAUDE.md（项目硬规则）

实施跨 9 个 commit，分别完成 spec §8 的 step 1–9。最近的 commits：
- d3de1d0c0  docs(specs): add fact-check agent design v2
- 1620fd141  feat(agent): register fact-check role + session kind (step 1 skeleton)
- 1bd47a431  feat(fact-check): add schemas + artifact persistence + idempotency
- d4a0fce50  feat(workers): require fact_check_items in 6 worker terminal schemas
- 1db6b7143  feat(fact-check): implement agent runtime
- 1c3a20bec  feat(orchestrator): expose fact_check dispatch tool
- be8634968  feat(orchestrator): decision_log + read_context for fact-check
- fe429ac94  feat(overlay): register fact-check role surface
- 3bee7b894  test(fact-check): e2e coverage A/C/D/F/G via orchestrator tool

主要新增文件：
- packages/opencorvus/src/fact-check/schema.ts
- packages/opencorvus/src/fact-check/persist.ts
- packages/opencorvus/src/fact-check/tools.ts
- packages/opencorvus/src/fact-check/index.ts
- packages/opencorvus/src/prompt/core/fact-check-core.txt
- packages/opencorvus/src/prompt/fragments/fact-check-registration.ts
- packages/opencorvus/test/fact-check/ (4 个测试文件，25 个测试)

主要修改文件：
- packages/opencorvus/src/agent/{role-contract,agent}.ts — 注册 fact-check role + orchestrator 白名单加 fact_check tool
- packages/opencorvus/src/session/{session.sql,index}.ts — SESSION_KINDS + Session.snapshotLatestAssistant
- packages/opencorvus/src/engine/engine.sql.ts — EngineArtifactKind 加 fact_check_attempt
- packages/opencorvus/src/build/{types,agent}.ts — BuildResultBase 加 fact_check_items；makeExternalPassed/FailedBuildResult 工厂
- packages/opencorvus/src/{requirements,architect,design-analyst,intent-analysis}/{agent,output-tools}.ts — terminal schema 加 fact_check_items + core 注入 fragment
- packages/opencorvus/src/integrity/{team-agent,team-schema}.ts — 只 consensus 阶段加 fact_check_items 字段 + 注入 fragment
- packages/opencorvus/src/orchestrator/tools.ts — fact_check tool + read_context 新 section + decision_log 写入
- packages/opencorvus/src/prompt/core/orchestrator-core.txt — §Fact-Check Dispatch 段
- packages/overlay/src/{utils/message.ts,components/Avatar.tsx,utils/card-color.ts,i18n/{en-US,zh-CN}.json} — 角色注册
- packages/opencorvus/test/{integrity,build-agent,requirements}/ — 11 个 fixture 文件更新（fact_check_items: []）

## 必答清单

### A. 与 spec 对齐
逐项核对 spec §8 的 step 1–9 是否完整实施。重点：
- §1.1 覆盖范围：6 个 worker 是否全部覆盖（build/requirements/architect/design-analyst/intent-analysis/integrity-consensus）？
- §3.1 schema 必填、无 default：6 个 worker schema 是否都做到？
- §3.2 terminal tool 无 fact_check_items 字段：fact-check/tools.ts 的 report_fact_check_result 是否真的没有这个字段？
- §3.3 snapshot 协议：Session.snapshotLatestAssistant 是否真的在 streaming/retry 时拒绝？
- §3.3 幂等键完整：(orchestrator_session, target_session, target_message_id, target_message_content_hash)
- §5.2 6 个 core 注入点：实际行号是否匹配 spec 表格（build:747, requirements:99, architect:121, design-analyst:98, intent-analysis:74, integrity team-agent:265）？
- §6.1.3 external executor 6 个 return literal 是否都走 factory？

### B. CLAUDE.md 合规
- rule 7 单源/禁 fallback：fact_check_items 是否真的无 default？工厂函数是否真的在构造点写死 [] 而不是在 runtime "if missing inject"？
- rule 8 单源：FactCheckItemListSchema 是否真的只在 fact-check/schema.ts 定义？所有 worker schema 都 import？
- rule 9 抽象设计模式：6 处 core 拼接是否都走 withFactCheckRegistration 而不是裸 [].join()？
- rule 13 禁状态机：orchestrator fact_check tool 的 snapshot+cached+dispatch 是不是 host 拦截 vs 数据完整性？
- rule 15 禁合成消息：源 worker session 的 assistant message 是否真的不被改写？
- rule 16/17/18 禁补丁/兼容：旧 DB 是否真的兼容（schema 改动是否 breaking）？
- rule 20 禁关键字规则：claim 禁词是否真的删了 host-side regex（应该只在 prompt 文本里描述）？
- rule 22/33/36 测试 + commit：每个 step 是否 commit + 配测试？

### C. 实际查代码（必须 grep）
- search `ctx.sessionID` in orchestrator/tools.ts — 我用了 `input.agentSessionID`，但 ctx 在 AI SDK tool 的 execute 第二个参数里也有 sessionID。是否有冲突？
- 实际验证 spec 行号：6 个 worker agent.ts 中 `core: withFactCheckRegistration(...)` 调用的具体行号。
- fact-check core prompt 是否真的不含 inline `<fact-check>` 标签字面或 `fact_check_items` 使用（只能在 NOT 上下文）？
- engine_artifact 表新 kind 是否在 DB schema 引起约束（应该是 plain text，无约束）？
- TypeScript 类型方向：fact-check/schema.ts 是否真的零 runtime import（不 import Session/orchestrator/build/persist/tools）？

### D. 潜在 bug / 边界情况
- fact_check 工具 cached 分支：cached.payload.report.scope 是否包含正确的 target_message_id（snapshot 时刻的 vs cached 当时的）？
- recordFactCheckAttempt 的 outcome 字段：tool_error 路径是否真的被写入（看 fact-check/index.ts run() 的 catch 块 + orchestrator/tools.ts 的 try/catch）？
- runWithExternalProvider 中 6 个 makeExternalXxxBuildResult 调用是否对每个 return literal 都改了？还是漏了某个？
- 编辑 ChatBubble.tsx 或其他 overlay 文件后，AgentRole union 加 "fact-check" 是否打破了任何 switch/case？(typescript 已 typecheck 通过，但代码逻辑呢)
- factory makeExternalFailedBuildResult 接受 `files_changed?:` optional，但 BuildPassedResultSchema 是 z.array() 非 optional。如果有人调用 makeExternalFailedBuildResult 没传 files_changed 但 status=failed schema 又要 files_changed？check 一下默认。

### E. 测试覆盖
- 25 个 fact-check 测试是否覆盖了 spec §7 列出的所有必测场景？
- 缺哪些 case 没测？e2e B (LLM 决策不调 fact_check) 和 E (cancel) 缺失，是否合理？
- contract tests 是否都同步更新了 fixture？run `bun test packages/opencorvus/test/build-agent/types.test.ts` 看是否过。

### F. 现存测试是否被破坏
对照 git stash 前后的 pass/fail 数：
- 实施前 pre-existing fail 数（不算我引入的）
- 实施后 fail 数
- 增量 fail 数

请 grep 实际跑测试并报告。

### G. 最终结论
- `APPROVE`（实施可合并，无遗漏）
- `APPROVE_WITH_NITS`（实施基本完成但有 minor 问题，列出 ≤ 5 项）
- `REQUEST_CHANGES`（必须返工，列出关键问题）

最后一行必须以 `VERDICT: <label>` 结尾。

## 输出约束
- markdown，分节
- 全文 ≤ 4500 字
- 对你不确定的明确说"我没查到"
- 不要修改任何代码或文件
- 实际跑 rg / grep / read 验证你说的每一条事实
