# work-v0.0.6beta-yr-0716 merge and v0.0.7beta bump

## Recall

### User requirement

- Fetch and merge `https://github.com/yangheng95/opencorvus/tree/work-v0.0.6beta-yr-0716`.
- Bump the integrated delivery to the `v0.0.7beta` branch.

### Acceptance criteria

- Preserve the current `v0.0.6beta` committed history and the recorded in-progress candidate without losing unrelated user files.
- Merge all commits from `legacy-remote/work-v0.0.6beta-yr-0716` through the freshly fetched remote tip.
- Create the new delivery branch `v0.0.7beta`; the remote branch did not exist at initial inspection.
- Synchronize the complete release family to canonical SemVer `0.0.7-beta` through `script/sync-version.ts` and retain the numeric MSI (Microsoft Installer) projection `0.0.7.0`.
- Pass focused merge/version tests, repository type/document checks, hook validation, and a final diff/graph review.
- Push `v0.0.7beta` to the legacy remote without bypassing hooks.

### Hard constraints

- Use the current Windows host worktree; do not create another worktree, reset, rebase, rewrite history, stash, or bypass hooks.
- Do not restart, refresh, close, or otherwise interfere with a running OpenCorvus or Overlay process.
- Keep `dashboard/data/personas.json` deletion and `packages/overlay/dist-artifacts/**` outside commits: the existing platform-runtime Recall explicitly identifies both as unrelated to its candidate.
- Preserve both branches' semantics when resolving conflicts; do not use whole-file checkout as a conflict shortcut.
- Commit subjects use the required `dsw-33987` prefix.

### Sources read

- Root `AGENTS.md` instructions supplied in the task context.
- `specs/records/2026-07/2026-07-11-platform-runtime-external-goal-team.md`, including its latest candidate/refreeze Recall.
- `specs/records/2026-07/2026-07-14-v0.0.3beta-branch-sync.md`.
- `specs/records/2026-07/2026-07-08-v0.0.2-yerui-0707-merge.md`.
- `script/sync-version.ts` and `packages/opencorvus/test/script/sync-version.test.ts`.
- Current status, remotes, branch tips, merge base, commit graph, diffs, untracked paths, and merge-tree simulation.

### Whole-repository search evidence

- Fresh fetch resolved `legacy-remote/work-v0.0.6beta-yr-0716` to `2aa7aeee249941c4557ac95de4c72c39788e2eca`; `legacy-remote/v0.0.7beta` did not exist.
- The merge base is `aaa616377726cb8475af2b70f9c3fbb10a511c7c`; current `HEAD` has 5 unique commits and the source has 19 unique commits.
- Source history changes 108 paths and current unique committed history changes 105 paths. Their six overlaps are both Overlay translation files, the task-dirbar browser test, generated OpenAPI, and both spec indexes. `git merge-tree` produced no conflict markers.
- The dirty candidate has 233 status entries. Only `packages/overlay/test/browser/task-dirbar-keyboard.test.ts`, `packages/sdk/openapi.json`, and `specs/README.md` overlap incoming paths.
- Release-family searches found the canonical package version plus nine sibling package manifests, two Cargo manifests and locks, Tauri config/MSI projection, and ten Bun lock workspace rows. They are all owned by `script/sync-version.ts`; workflow and release callers invoke that one script.
- The source branch does not modify version-family targets. Current committed history modifies `package.json`, `script/sync-version.ts`, and Tauri config to establish the present single-source version mechanism.

### Independent agent feedback

- No subagent was launched because the active instructions authorize subagents only when the user explicitly requests delegation. The topology, overlap, merge simulation, and version-call inventory were therefore checked directly and will receive a second local review after integration.

## Implementation plan

1. Preserve the recorded dirty platform/runtime candidate as its own `dsw-33987` commit, excluding the unrelated persona deletion and Overlay installer artifacts.
2. Re-fetch the source tip and merge it into `v0.0.6beta` with a conforming merge subject; resolve any newly appearing conflict from the base/current/source evidence.
3. Create `v0.0.7beta` from the integrated commit and run the canonical version synchronizer with `0.0.7beta`.
4. Run version, merge-sensitive focused tests, typechecks, API/docs checks, and spec health checks. For any visual surface changed by conflict resolution, use the repository Node-launched browser workflow and inspect the scoped screenshot.
5. Perform a second graph/diff/version review, commit the bump and verification record, re-fetch both source and target refs, and push `v0.0.7beta` to `legacy-remote` through hooks.

## Result

- Preserved the recorded platform/runtime candidate as `3f25b95299` while leaving the unrelated `dashboard/data/personas.json` deletion and `packages/overlay/dist-artifacts/**` uncommitted.
- Merged freshly fetched `legacy-remote/work-v0.0.6beta-yr-0716@2aa7aeee24` with merge commit `73f22a6c1d`; the only conflicts were the root and July spec indexes, resolved by retaining all valid records from both sides.
- Created `v0.0.7beta` from the integrated history and synchronized the complete release family to `0.0.7-beta`, including MSI projection `0.0.7.0`.
- Repaired one stale source-branch browser assertion: the final card-header check now observes the complete `.card__head`, where the source branch deliberately moved elapsed time to a trailing sibling. Product rendering already exposed the correct `2s`; the corrected test retains the exact visible title, summary, and trailing-duration contract.
- The canonical generator reconciled one merge-induced OpenAPI drift; generated JavaScript SDK and bilingual API documents were already current.
- PASS: release version, historical links, document health, and product-doc single-source suites — 83 tests, 1,218 assertions.
- PASS: OpenCorvus, Overlay, and JavaScript SDK typechecks; API route inventory — 6 rules across 30 route files; API docs check — 261 operations in 24 groups.
- PASS: 26 focused Overlay component tests and three Node-launched browser scenarios covering card metadata, message chronology/tool timing, and Review Changes/empty Dock.
- PASS: manual review of four current-goal screenshots found no clipping, overlap, duplicate header/tab, chronology, alignment, or density regression.
- A concurrent strict-contract correction wrote three production schema paths during the bump and was inadvertently included in the bump commit. History was not reset or rewritten. Its remaining Architect source and Acceptance/Architect/Requirements/Goal-lifecycle regressions were completed as a separate follow-up; 31 focused tests with 254 assertions pass, including rejection at every fixed nested boundary while preserving the intentionally open prebuilt `config` record.
