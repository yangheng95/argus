# v0.0.23beta Branch Bump and macOS ARM64 Matrix Package

Status: completed

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current delivery line to `v0.0.23beta` and package it. |
| Acceptance | Create `v0.0.23beta` from the verified `v0.0.22beta` baseline; synchronize the release family to canonical `0.0.23-beta` and MSI (Microsoft Installer) projection `0.0.23.0`; run the native GUI installer matrix before the portable CLI (Command-Line Interface) matrix; verify version, ARM64 architecture, signatures, DMG (Disk Image) integrity, archive contents, checksums, and matrix skips; commit only release-owned files; push through normal hooks to `legacy-remote`; and verify local/remote equality. |
| Git baseline | The clean local `v0.0.22beta` and `legacy-remote/v0.0.22beta` both resolve to `6e4d3130a719e2d88f9f2bf3d7dcd0ec23ecfc2d`. No local or remote `v0.0.23beta` branch exists. |
| Parallel changes | None were present at the initial audit. Any changes appearing later remain protected and outside the release commit unless they are declared release outputs. |
| Hard constraints | Do not stash, reset, restore, delete, broadly stage, create a worktree, bypass hooks, or interfere with a running OpenCorvus/Overlay process. Commit subjects use the required `dsw-33987` prefix. Push only to the legacy remote. |
| Native host | macOS ARM64 (`Darwin`, `arm64`). Only the matching `darwin-arm64` GUI and CLI matrix rows may be reported as packaged; all other native rows must be reported as skipped. |
| Sources read | Root `AGENTS.md`; the OpenCorvus beta-release skill; current Git status, branches, remotes, target-branch lookup, canonical version output, `docs/packaging.md`, prior `v0.0.22beta` package records, and the root/month specification indexes. |
| Whole-repository grep | Live synchronizer entrypoints are root `version:bump` / `version:check`, `script/release`, and CI (Continuous Integration) workflows. `script/sync-version.ts` is the single release-family writer. GUI packaging is owned by `script/package-gui-installer-matrix.ts`; portable CLI packaging delegates from `script/package-binary-matrix.ts` to `script/package-native-binary.ts`. Historical release records remain immutable evidence. |
| Independent-agent feedback | None. The user did not request delegated agents, so the primary agent owns execution and second review. |

## Plan

1. Create `v0.0.23beta` from the exact verified `v0.0.22beta` delivery head.
2. Run the canonical version synchronizer, inspect every owned target, and verify canonical SemVer and MSI projections.
3. Run focused version/document checks, stage only release-owned files, and commit through normal hooks.
4. Build the native GUI installer matrix before the portable CLI matrix.
5. Independently verify native artifacts, signatures, DMG/archive integrity, runtime versions, hashes, sizes, and unsupported-row skips.
6. Complete this verification ledger, commit the final record, push to `legacy-remote`, and prove local/remote equality with a clean worktree.

## Verification ledger

- Branch/version: `v0.0.23beta` was created from the exact clean
  `v0.0.22beta` / `legacy-remote/v0.0.22beta` commit
  `6e4d3130a719e2d88f9f2bf3d7dcd0ec23ecfc2d`. The canonical synchronizer
  aligned every declared release target at `0.0.23-beta` and projected Tauri
  MSI metadata to `0.0.23.0`. All five focused synchronizer tests and
  `bun run version:check` pass.
- Release commit: `e7300c54eae9ad7d9cfae41a399c0f73fe1cd25d` contains only the
  declared version-family targets, current release example, this record, and
  its two exact index additions. Concurrent Scheduled/Settings work remained
  outside that commit.
- Source convergence: the concurrent Scheduled/Settings delivery committed and
  pushed two follow-ups while packaging was active. Builds whose source
  fingerprint changed were rejected rather than reported. The accepted matrix
  run starts and ends on clean local and remote commit
  `21631ce43f808397e76d632966c988bc41437f03`, with full checkout content
  fingerprint
  `7c8fd6b736e85bdb238c7175335b8344eeab5a15b5be45af35a965072a96e098`.
- GUI matrix: the accepted run performed a real Vite production build,
  embedded `0.0.23-beta` backend build, native Tauri release build, ad-hoc
  signing, installer bundle, staging, and release-asset validation.
  `darwin-arm64` was packaged. Linux x64, Linux ARM64, macOS x64, and Windows
  x64 were explicitly skipped because they require matching native hosts.
  Apple notarization was explicitly skipped because notarization credentials
  were not configured.
- GUI acceptance: the staged executable and independently extracted
  `OpenCorvus.app` executable are thin ARM64 Mach-O files. Strict executable
  signature verification and deep strict application verification pass. Both
  application version fields are `0.0.23-beta`; the application archive has
  nine entries; and `hdiutil verify` reports the DMG checksum valid.
- CLI matrix: the accepted CLI run followed the accepted GUI run.
  `darwin-arm64` was packaged and the same four non-native rows were explicitly
  skipped. The staged and independently extracted executables are thin ARM64
  Mach-O files, pass strict signature verification, and report
  `0.0.23-beta`. The 15,988-entry archive contains executable Ripgrep `15.2.0`,
  Node `v22.23.1`, Browser MCP (Model Context Protocol), and the embedded
  Overlay UI.
- Regression evidence: all 54 packaging, matrix, release-contract,
  install-script, and version tests pass. All 85 historical-link and document
  health tests pass. Full workspace typecheck, API route inventory, generated
  API documentation, Overlay internationalization, and tracked-source secret
  scan pass through the normal pre-push hook. The first push attempt correctly
  stopped on two temporarily unused localization keys in concurrent work; the
  original hook passed after that work fixed its own source.
- Accepted artifact SHA-256 and byte sizes:
  - GUI executable:
    `cf1a9fdf28a04fd0b31a8e7b81592d97662f1c9b0df146f98983ccecdb69e5c5`,
    174558624 bytes.
  - DMG:
    `0c06a3cf3c9036a857103db451c1fe831b82f3e9a3818f1826bb4a93f26dfcab`,
    166763779 bytes.
  - Application archive:
    `e46e4fbb7edcf6933dafc519dd087d1bdb3b6df71b2c358e830cf908451981f9`,
    166056346 bytes.
  - CLI executable:
    `4611ff82ccaf168e37d13a1f6bdfe2388eab2b0a8a181b064777d4729a5fc240`,
    102316976 bytes.
  - CLI archive:
    `32074b83eb8854bdc4ccab6496f1eede300ec804fac2e91efffe7b9215fc064f`,
    122515866 bytes.
