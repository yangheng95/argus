# v0.0.15beta Darwin ARM64 Matrix Package

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Run the package matrix for `v0.0.15beta`. |
| Acceptance criteria | Run the canonical GUI installer matrix and portable CLI binary matrix; build and independently validate the native macOS ARM64 rows; truthfully report every non-host row as skipped; record artifact paths, architectures, versions, sizes, checksums, and final Git provenance. |
| Host evidence | `uname -s` reports `Darwin`; `uname -m` reports `arm64`. The canonical matrices therefore own only `darwin-arm64` on this host. |
| Git evidence | The worktree was clean at `880ee41b5`; `myhexin/v0.0.15beta` was two commits behind because the local branch already contained the preserved research-studio parallel change and its merge commit. Version verification reported `0.0.15-beta`. |
| Hard constraints | Use `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` as the only artifact owners. Do not use `--skip-build`, claim cross-platform output, create a worktree, reset/stash, bypass hooks, or restart/refresh the running OpenCorvus/Overlay. Package outputs are generated artifacts, not source commits. |
| Sources read | Root `AGENTS.md`, `RELEASE.md`, `docs/packaging.md`, root package scripts, `script/package-gui-installer-matrix.ts`, the previous Windows matrix record, current branch/status/remotes/version history, and package output contracts. |
| Whole-repository grep | GUI matrix ownership is `script/package-gui-installer-matrix.ts` and stages under `packages/overlay/dist-artifacts/<platform>/`; portable CLI ownership is `script/package-binary-matrix.ts` through `script/package-native-binary.ts` and stages under `packages/opencorvus/dist/opencorvus-<platform>/` plus archives. Release validation is `script/check-release-assets.ts`. |
| Independent-agent feedback | None. The active execution policy does not permit unsolicited sub-agent delegation. |

## Plan

1. Commit and push this Recall together with the already preserved local branch commits so the package source is an immutable remote commit.
2. Run focused package-contract tests, then the GUI installer matrix without `--skip-build`.
3. Run the portable CLI binary matrix without shortcuts.
4. Independently inspect artifact names, executable architectures, embedded versions, signatures/installers where applicable, sizes, and SHA-256 checksums.
5. Record results, commit/push the evidence record, and verify local/remote equality while leaving generated packages uncommitted.

## Verification ledger

- Immutable package source: local and `myhexin/v0.0.15beta` both resolved to `40077b540b8d6b2fe4cdd6a6812d3f09be8b6bbf` before either matrix started. A concurrent later commit changed only a Research Studio test and record, not package inputs or generated artifacts.
- Focused package contract tests passed: 37 tests across GUI matrix, binary matrix, native bundle, and release-overlay ownership.
- `bun run package:gui-installer-matrix` completed the native `darwin-arm64` row and the release asset checker accepted the executable, DMG, and application archive. Linux x64/ARM64, macOS x64, and Windows x64 were explicitly skipped as non-host rows.
- `bun run package:binary-matrix` completed the native `darwin-arm64` row and its built-in smoke checks. Linux x64/ARM64, macOS x64, and Windows x64 were explicitly skipped as non-host rows.
- Independent architecture inspection reports Mach-O 64-bit ARM64 for the staged GUI executable, application executable, CLI executable, packaged Ripgrep, and packaged Browser MCP Node runtime.
- Runtime/version evidence: CLI `0.0.15-beta`; Ripgrep `15.2.0`; Node `v22.23.1`; application `CFBundleShortVersionString` and `CFBundleVersion` both `0.0.15-beta`.
- Application integrity: strict deep code-sign verification passed for identifier `ai.opencorvus.overlay`; the local build uses the repository's ad-hoc signature and was not notarized because Apple release credentials were not configured. `hdiutil verify` accepted the DMG checksum.
- Portable bundle completeness was rechecked for `package.json`, `ui/index.html`, `bin/rg`, `browser-mcp-node/browser.mjs`, Node, and Playwright metadata.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `packages/overlay/dist-artifacts/darwin-arm64/opencorvus-overlay` | 162,838,576 | `dad7e4335a714584cd4344cd12bc1dd84b4763a842626eec9ef499e21e408924` |
| `packages/overlay/dist-artifacts/darwin-arm64/OpenCorvus_0.0.15-beta_aarch64.dmg` | 153,763,760 | `1ac04cd696c85ac7405ef63645f358e261d4605279076372d7e7c0502e5c0ca0` |
| `packages/overlay/dist-artifacts/darwin-arm64/OpenCorvus_0.0.15-beta_aarch64.app.tar.gz` | 153,063,604 | `0effbc04412b8c4011166bfc537f17622e2e7f200a3a92dc17a5c2ff3ddb4385` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64/opencorvus` | 100,486,306 | `686eefcfa32ef441cc3045055d204dfbc9ff0b5bf016ffd64ad13c507fb5f156` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64.tar.gz` | 116,847,448 | `e4901dae482b4381cc1c3fbe9b6d032dce0bba4f47418bc02932a91bc6b896ad` |
