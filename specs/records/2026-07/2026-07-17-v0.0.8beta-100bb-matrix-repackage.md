# v0.0.8beta 100bb Matrix Repackage

## Recall

- User request: rebuild the package matrix once more.
- Acceptance criteria: fetch and confirm the latest `myhexin/v0.0.8beta`; run the established Graphical User Interface (GUI) installer matrix first and Command-Line Interface (CLI) binary matrix second; verify the native macOS ARM64 outputs, version, signatures, disk image, runtime execution, archives, and hashes.
- Hard constraints: preserve concurrent frontend-replica End-to-End (E2E) files; do not reset the worktree, create another worktree, interfere with the running Overlay, commit generated packages, or claim non-native platform rows as verified. Use only the existing production matrix owners. Commit records with the `dsw-33987` prefix and push to `myhexin/v0.0.8beta` through hooks.
- Sources read: `specs/records/2026-07/2026-07-17-v0.0.8beta-latest-matrix-build.md`, `specs/records/2026-07/2026-07-17-v0.0.8beta-version-correction-and-macos-repackage.md`, root package scripts, matrix package tests, current Git status, version checker, and native artifact directory.
- Whole-repository search: `package:gui-installer-matrix` remains the sole GUI matrix owner and builds the embedded backend; `package:binary-matrix` remains the sole portable CLI matrix owner. GUI must precede CLI because the GUI sidecar build owns and refreshes the OpenCorvus distribution directory. Non-host rows are reported as skipped by both tools.
- Independent agent feedback: none; the user did not request sub-agent work.

## Starting evidence and plan

- Fresh fetch confirms local and remote `v0.0.8beta` at `100bbccda`; version check reports `0.0.8-beta`.
- The four concurrent frontend-replica E2E files have recorded SHA-256 values and remain outside this task.
- Run GUI matrix, then CLI matrix. Verify release contracts, native architectures, version projections, application signature, Apple Disk Image (DMG), isolated packaged-backend health, archive contents, and SHA-256 values. Record results, stage only this record/indexes, rerun document checks, fetch, commit, and push.

## Verification record

- Fresh fetch kept local and remote `v0.0.8beta` aligned at `100bbccda`; canonical version check passed at `0.0.8-beta`.
- `bun run package:gui-installer-matrix` completed first and staged the macOS ARM64 GUI executable, DMG, and application archive. `bun run package:binary-matrix` completed second and produced the macOS ARM64 CLI runtime plus portable archive. Both tools explicitly skipped Linux x64/ARM64, macOS x64, and Windows x64 because this host cannot execute those native rows.
- GUI, application, and CLI executables are Mach-O ARM64. The CLI reports `0.0.8-beta`; application short/build versions both report `0.0.8-beta`. The application and staged GUI pass strict code-sign verification, and `hdiutil verify` reports the DMG checksum valid.
- The CLI archive contains the runtime, UI, Browser Model Context Protocol (MCP) sidecar, browser Node runtime, colocated modules, package metadata, and bin directory. The CLI runtime is 96 MiB; its archive is 111 MiB. GUI executable is 155 MiB, DMG 146 MiB, and application archive 145 MiB.
- Finder-like isolated backend verification used the packaged CLI with `PATH=/usr/bin:/bin`, isolated `HOME` and `OPENCORVUS_HOME`, and port `47884`. `/global/health` returned `healthy: true` and version `0.0.8-beta`; SIGINT shutdown reported zero live tasks, runs, sessions, or tool parts and released the port.
- SHA-256: GUI executable `8dad69d8c233b2dcd2a5917872357037ffaccd8f2dcd3f1092eeefb64fa1cb31`; DMG `baa2f979902738f8731b982b05040913ab88210e1ad894e8f41a7211f4741bc0`; application archive `8d64c7355ef3e704813e50c05144fc02cdc54c17d2e4a8f7e0b637644e0129c9`; CLI runtime `a2a5adcd0030920aa394f41d181d6fe02a536bd84bda95a16baac7a3d0ec7551`; CLI archive `c15179494acdc0d8b80681a6750c52816bf1aff07fa591d73667aaa6d19ecae7`.
- Matrix contract tests pass 25/25. The transient generated Tauri schema is removed, native artifacts remain untracked, and all four concurrent frontend-replica E2E file hashes remain unchanged.
- Final remote review: while the matrices were running, the shared main worktree received commit `0684fd79c`, whose only changed path is the verification record `2026-07-17-mac-first-launch-settings-global-ownership.md`. It contains no product, manifest, generated payload, packaging, or runtime change, so the `100bbccda` package bytes remain the package for the latest executable source; no redundant rebuild is required. The matrix record commit is based on that documentation-only commit and pushed as `14339d5b0`.
