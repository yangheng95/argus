# v0.0.15beta Latest-HEAD Darwin ARM64 Matrix Repackage

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Package the current `v0.0.15beta`. |
| Acceptance criteria | Build through the canonical GUI installer matrix first and portable Command-Line Interface (CLI) matrix second; validate the native macOS ARM64 artifacts; report every non-host row as skipped; publish only verified generated outputs. |
| Source provenance | Local and `myhexin/v0.0.15beta` both resolve to `675e87bae`; release-family version verification reports `0.0.15-beta`. This HEAD contains five product-fix commits after the previous post-merge matrix package. |
| Parallel work | Four unstaged parallel edits remain in `orchestrator-core.txt`, `core-prompt-hygiene.test.ts`, `2026-07-22-mirror-prism-full-workflow-distillation.md`, and `2026-07-22-review-debug-expert-squad.md`. Their aggregate binary-diff SHA-256 is `9c4b8e1f400e548eea896bcbb3ce01e2206fe36e646584f275b2a60fdd2c1fbd`. The core prompt is a package input, so the main worktree is not an immutable package source. |
| Host evidence | `uname -s` is `Darwin`; `uname -m` is `arm64`. The matrices may therefore claim only the native `darwin-arm64` row. |
| Hard constraints | Use `bun run package:gui-installer-matrix` and `bun run package:binary-matrix` without shortcuts. Do not create a Git worktree, reset/stash/restore files, bypass hooks, restart/refresh OpenCorvus, or claim non-native output. Export committed HEAD to a temporary non-Git snapshot and preserve all parallel diffs. |
| Sources read | Replacement `AGENTS.md`; current Git graph/status/version; root package scripts; GUI/binary matrix implementations; previous `v0.0.15beta` matrix records; spec indexes. |
| Whole-repository grep | `script/package-gui-installer-matrix.ts` exclusively owns GUI build, staging, and release validation under `packages/overlay/dist-artifacts/<platform>/`. `script/package-binary-matrix.ts` delegates to `script/package-native-binary.ts` and owns the portable runtime directory/archive under `packages/opencorvus/dist/`. Both expose five native rows and package only the matching host. |
| Independent-agent feedback | None. The user did not request delegation and current execution policy forbids unsolicited sub-agents. |

## Plan

1. Commit and push this Recall without staging any parallel edit, then export that immutable HEAD into a temporary non-Git source snapshot.
2. Link only existing dependency/build caches, prove the snapshot differs from the dirty worktree where expected, and run focused package-contract tests.
3. Run the GUI matrix and then CLI matrix without `--skip-build` or `--skip-ui`.
4. Independently validate artifact contracts, architectures, versions, signatures, disk image integrity, bundle completeness, sizes, and SHA-256 checksums.
5. Publish only validated generated artifacts to their canonical ignored directories, remove the temporary snapshot, prove parallel diffs are unchanged, record results, commit/push the evidence, and confirm local/remote equality.

## Verification ledger

- Immutable source: Recall commit `9ed66cb1b` was pushed before packaging. A temporary non-Git export of that exact commit used the repository's read-only Git index for tracked expert-squad payload authority and linked only existing dependency/Rust caches. The exported prompt SHA-256 was `1edfd959...`, while the parallel worktree prompt remained `8e21c3be...`.
- Package contracts: GUI matrix, binary matrix, native bundle, and release ownership suites passed 37/37 before production builds.
- GUI matrix: `bun run package:gui-installer-matrix` rebuilt the SDK, two 2,648-module production Vite passes, generated payloads, embedded backend, optimized Rust/Tauri application, ad-hoc signatures, DMG, and application archive. The official release checker accepted `darwin-arm64`; Linux x64/ARM64, Darwin x64, and Windows x64 were explicitly skipped.
- CLI matrix: `bun run package:binary-matrix` rebuilt, executed, archived, and validated the native `darwin-arm64` portable runtime. The same four non-host rows were explicitly skipped.
- Independent inspection: GUI, application, CLI, Ripgrep, and Browser Model Context Protocol (MCP) Node executables are Mach-O 64-bit ARM64. CLI reports `0.0.15-beta`, Ripgrep `15.2.0`, Node `v22.23.1`, and both application plist versions are `0.0.15-beta`. Strict deep code-sign verification passed for identifier `ai.opencorvus.overlay`; the local signature is ad-hoc and not notarized because Apple release credentials were not configured. `hdiutil verify` accepted the DMG.
- Publication audit: the GUI assets copied cleanly. The first large-tree CLI `rsync` process was killed with exit 137 before replacement, and the canonical checksum audit correctly exposed the prior CLI bytes. The newly validated CLI archive was then extracted to a complete staging directory, checked for all required bundle paths, moved into the canonical generated destination, and revalidated. No failure was hidden or claimed as success.
- Final canonical validation: Overlay release assets pass `check-release-assets.ts --require-bundle`; CLI bundle contains `package.json`, `ui/index.html`, Ripgrep, browser script, Node, and Playwright metadata. The four parallel diffs retain their exact pre-build aggregate SHA-256 `9c4b8e1f400e548eea896bcbb3ce01e2206fe36e646584f275b2a60fdd2c1fbd`.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `packages/overlay/dist-artifacts/darwin-arm64/opencorvus-overlay` | 162,888,112 | `9cef5ffbad4324941fb984e04844030b14fa6d4331d4ed5cd3ac98df8113b602` |
| `packages/overlay/dist-artifacts/darwin-arm64/OpenCorvus_0.0.15-beta_aarch64.dmg` | 153,811,738 | `1aba21a52e7a73da850804ce483876c0fd45c67837b9ce57fc8da69b58d0bf98` |
| `packages/overlay/dist-artifacts/darwin-arm64/OpenCorvus_0.0.15-beta_aarch64.app.tar.gz` | 153,112,156 | `df347542bc5f321237da1839acc8cf149f1c5038b0e5bd71c042e7738bd8df8e` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64/opencorvus` | 100,700,962 | `f848270ab04d815ac77faf43775ae39066f0cb4f4af05acc74eaa0666063e227` |
| `packages/opencorvus/dist/opencorvus-darwin-arm64.tar.gz` | 116,896,065 | `bfb2df8c89e30b419a16245d4d6645f1ede8923440934a3ee9cb38a4cbc5e70f` |
