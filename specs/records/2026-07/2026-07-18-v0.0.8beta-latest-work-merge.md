# v0.0.8beta Latest Work Merge

## Recall

### User request

- Merge the latest code.

### Acceptance criteria

1. Fetch the git-cc remote named `myhexin` and identify the latest active code line for the current `v0.0.8beta` delivery branch.
2. Integrate every new commit from `myhexin/work-v0.0.8beta-yr-0717` into local `v0.0.8beta` without losing either branch's newer behavior.
3. Resolve conflicts from source and test evidence, run focused Overlay tests and TypeScript checks, and visually inspect the merged frontend through real Node-launched Playwright screenshots.
4. Run documentation health checks, perform a second diff review, and push the verified delivery branch to `myhexin/v0.0.8beta` without bypassing hooks.

### Hard constraints

- Do not reset the worktree, create another worktree, or restart/interrupt OpenCorvus or Overlay processes.
- Do not use one-sided conflict checkout as a substitute for semantic reconciliation.
- Do not add fallback, compatibility, gate, state-machine, or duplicate-source behavior.
- Preserve the current delivery branch's two local commits and all new work-line commits.
- Commit subjects use the required `dsw-33987` prefix; push only to the git-cc remote named `myhexin`.

### Sources read

- Repository `AGENTS.md` instructions supplied in the task context.
- `specs/records/2026-07/2026-07-17-v0.0.8beta-remote-merge-and-matrix-build.md`.
- Current branch graph, clean worktree status, remote topology, fetched remote heads, three-way path diff, and `git merge-tree` output.

### Whole-repository search and branch evidence

- `git fetch --prune myhexin` left `myhexin/v0.0.8beta` at `ebeb4c78b0` and advanced the active work line from `0b0c686c8b` to `5e81f755ce`.
- `HEAD...myhexin/work-v0.0.8beta-yr-0717` is `114 8`: the delivery line has 114 unique commits and the work line has 8 new commits to integrate.
- The work-only commits implement compact Work Ledger hover/context summaries, right-Dock capsule tabs, titlebar Brand Guide retirement, and their records/tests.
- The full `git diff --name-status HEAD...myhexin/work-v0.0.8beta-yr-0717` enumerates 38 paths: Overlay App/Work Ledger/titlebar components; locale JSON; card/conversation/titlebar/work-ledger/workspace CSS; 19 Overlay source/browser tests; four July records; and both spec indexes.
- `git merge-tree` identifies overlapping evolution in App, Work Ledger, locales, shared CSS, browser/source tests, and spec indexes. Deleted Brand Guide files are unchanged on the delivery side and should remain deleted unless merged call sites prove otherwise.
- No sub-agent was requested, so none was started.

## Execution plan

1. Commit and push this pre-integration record so the task contract survives compaction.
2. Merge `myhexin/work-v0.0.8beta-yr-0717` into `v0.0.8beta`; resolve each conflict against both histories and the new work-line tests.
3. Run conflict-focused Overlay source tests and TypeScript checks, then run the affected Node-launched Playwright browser scenarios and inspect screenshots at original resolution.
4. Run spec/document health checks, review the final diff and branch graph, fetch once more for remote movement, then push `v0.0.8beta` to `myhexin` through normal hooks.

## Verification ledger

- Fetched work head `5e81f755ce` and semantically integrated all eight work-line commits into `v0.0.8beta` while retaining delivery-line task-pill sizing, scrollbar compensation, active-project context, and runtime rows.
- Resolved 16 merge conflicts. The second review restored the complete symmetric five-column Agent Rail parent grid instead of retaining only its child placement rules.
- Browser-fixture failures were traced to missing required task `orderKey` data; shared Work Ledger and titlebar fixtures now provide the real contract. Project selection assertions follow the current single-source context rule: the parent project remains active when its child task is selected.
- `bun run --cwd packages/overlay typecheck` passed.
- Focused Overlay and documentation tests passed: 274 tests, 0 failures, and 10,504 assertions across 15 files.
- Serial Node-launched Playwright verification passed all 10 scenarios. The long-transcript scenario reported a 48.9 ms maximum animation-frame gap, zero long tasks, six mounted messages, and preserved ordering across 241 messages.
- Visually reviewed the 1440x900 conversation top-edge fade, dark compact goal rows, Work Ledger project/task selection, and right-Dock capsule screenshots. The merged surfaces are visible, unclipped, and free of incoherent overlap.
- Existing unrelated `packages/opencorvus` and July 16 record changes remain unstaged and outside this merge.
- The final pre-commit fetch found five additional remote-only commits through `9ed7eccafa`. Their second semantic merge accepted the latest 36/26/34/26 px navigation rhythm, per-edge scrollbar measurement, tooltip line height, and retired Mission label removal while retaining the actual 280-392 px rail token contract.
- The second merge rejected two regressions against current recorded contracts: it retained the complete Agent Rail parent grid and kept `activeProjectDirectory` responsible for parent-project highlighting while a child Task is selected. Duplicate spec-index entries were removed during the same reconciliation.
- Post-second-merge Overlay TypeScript checking passed. The focused source replay passed 149 tests with 0 failures and 8,694 assertions; the earlier documentation replay remains 82/82.
- The final Node-launched Playwright replay passed all eight selected browser scenarios. Fresh screenshots were inspected for the 1120 px minimum legal frame, Work Ledger parent/child selection, compact project tooltip, long-transcript fade, task focus, and titlebar states; no clipping, overlap, or selection-context regression remains.
- A last remote movement check found and merged documentation-only head `605984846d`, which records the next Work Ledger hover and visible multi-tab task. Its spec indexes pass the same 82/82 documentation replay.
- Final delivery merge `c1c9e7e457` was pushed to `myhexin/v0.0.8beta`; fetched local and remote delivery heads are identical. The work line has zero remote-only commits (`HEAD...work = 3/0`). Normal pre-push hooks passed package TypeScript checks, API route inventory, docs rendering, Overlay/panel internationalization checks, and secret scanning.
