# v0.0.15beta Branch Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Create the `v0.0.15beta` branch. Packaging was not requested. |
| Acceptance | Branch from the current pushed `v0.0.14beta` baseline, project canonical version becomes `0.0.15-beta`, Windows MSI projection becomes `0.0.15.0`, validation passes, and the branch is committed and pushed to `myhexin`. |
| Git baseline | Before the branch operation, `v0.0.14beta` was at `d51c4f26f0ac996d89535759f75fcef71063de63`; neither a local nor remote `v0.0.15beta` branch existed. |
| Parallel changes | Existing `.DS_Store` files and concurrent chat-scroll spec/index edits are outside this task and must remain untouched and uncommitted by this task. |
| Hard constraints | Use `script/sync-version.ts` as the version single source; do not package; do not reset, stash, create a worktree, restart OpenCorvus, or bypass hooks; commit subjects start with `dsw-33987`; push only to `myhexin`. |
| Sources read | Root `AGENTS.md`, repository status/branches/remotes, `script/sync-version.ts`, `RELEASE.md`, prior `v0.0.14beta` bump record, and the two spec indexes. |
| Whole-repository grep | The synchronizer owns nine package manifests, the Bun lock workspace versions, both Cargo manifest/lock pairs, and Tauri configuration. Historical records are immutable; the live release example is the only non-generated release text to update. |
| Independent-agent feedback | None. The active execution policy does not permit unsolicited sub-agent delegation. |

## Plan

1. Create `v0.0.15beta` from the inspected `v0.0.14beta` baseline without disturbing parallel work.
2. Run the repository version synchronizer with `v0.0.15beta` and update the live release command example.
3. Validate version consistency, focused tests, type checking, and documentation health.
4. Commit only task-owned files and push `v0.0.15beta` to `myhexin` through normal hooks.

## Verification ledger

- `bun run version:check`: passed; the release family is aligned at `0.0.15-beta`.
- `bun test packages/opencorvus/test/script/sync-version.test.ts --timeout=0`: passed (5 tests).
- `bun run typecheck`: passed (9 tasks).
- Historical-doc links passed. The combined documentation-health run's only failure was the concurrent chat-scroll record remaining intentionally untracked; the bump record itself is tracked and indexed.
- The normal pre-push hook passed type checking, API route inventory, generated API documentation, overlay internationalization, and secret scanning.
- No packaging command was run.
