# CodeClaw

## 定位

CodeClaw 是一个全平台通用的 GUI 操作 Agent，能以无人值守的模式管理桌面和窗口。也可以直接用于 coding，提供一个可以托管的编程环境，用于在给定目标前提下无人值守的原型开发。CodeClaw 像是 OpenCode 和 OpenClaw 的结合体，既可以提供一个可以托管的编程环境，也可以提供一个全平台通用的 GUI 操作 Agent。

## 功能点

- 基于 Bot 的 GUI 操作 Agent，像 OpenClaw 一样支持多种 Bot，例如 Slack、Discord、Telegram 等。
- 通用的全平台 GUI 操作能力，支持 Windows、macOS、Linux 等操作系统。点击、键盘输入/快捷键、拖动等基本操作都可以实现。
- 自适应分辨率的窗口定位和操作，支持多显示器环境。支持各种情况下的 GUI 坐标计算，例如窗口被遮挡、分辨率变化等。
- 支持无人值守的模式，可以在后台运行，定时执行任务，监控系统状态等。
- 自带一个 Terminal UI（TUI），可以在命令行界面进行无人值守的开发。
- 复用 OpenCode 的所有功能，例如 tool、agent、plugin、MCP 等。
- 使用 Skill 定义全平台的 GUI 操作能力，提供一个统一的接口来实现各种 GUI 操作。例如描述平台特定的应用打开方式、窗口定位、操作步骤、组合快捷键等。
- CodeClaw 启动后即可开始操作，第一次启动时可以基于 GUI 操作自动化完成 Bot 配置，这也是 Skill 的一部分，覆盖多种平台和 Bot。
- 提供系统托盘和一个可视化的对话界面来管理 CodeClaw 的运行状态，例如发送指令、查看日志、配置 Bot 等。
- CodeClaw 有一个独立的 Goal 单元、一个 Plan Agent、一个视觉分析 Agent（分析图片后给出坐标、操作指令和运行摘要，但不把截图放入主对话流），以及一个 GUI 操作 Agent（根据视觉分析的结果执行操作）。Plan Agent 负责根据 Goal 和视觉分析结果制定操作计划，GUI 操作 Agent 负责执行计划中的操作步骤。
- 为了辅助视觉 LLM 找到 GUI 元素和坐标，截图被添加了自适应分辨率的坐标系，视觉分析 Agent 会根据 GUI 上的坐标系找出坐标，给出操作指令——例如"点击坐标系上的 (100, 200)"，而不是"在某个特定分辨率下点击绝对像素位置"。
- 一个任务进来会在 Plan Agent 中进行计划（可以联网搜索学习操作步骤），计划完成后生成一个 Goal 目标给 Goal Agent。Goal Agent 通过鲁棒的策略判断 Goal 是否已完成，如果完成不了则强制要求 LLM 继续搜索解决方案，除非 LLM 承认无法完成或产生异常。GUI 操作 Agent 会根据计划中的操作步骤进行操作，如果操作失败则返回错误信息给 Plan Agent，Plan Agent 根据错误信息重新制定计划，直到操作成功或达到重试次数上限。
- 任务进来时会进入暂存区（可以理解为队列 Agent），根据当前 Goal 的完成情况判断是否向 Plan Agent 发布任务。暂存区会根据任务的优先级和当前系统状态决定任务的执行顺序，例如如果当前系统资源紧张或正在执行高优先级任务，新任务可能会被暂时搁置，直到系统资源充足或高优先级任务完成。
- 上下文管理：截图只用于分析，不进入主对话流；视觉分析的结果会以文本形式进入主对话流，Plan Agent 和 Goal Agent 根据视觉分析结果进行决策和计划制定。新任务开始时总结上一轮任务的执行情况，生成摘要作为新上下文的起点，供 Plan Agent 和 Goal Agent 参考。
- 任务执行过程中，CodeClaw 会把截图发送给手机端 App，App 将截图展示给用户，方便用户了解当前的执行情况和系统状态。用户可以通过 App 发送指令给 CodeClaw，例如暂停、继续、取消等。
- CodeClaw 在操作 GUI 时屏幕上要有视觉反馈，例如点击按钮时按钮有高亮效果，或操作完成后有提示框显示操作结果等。这些视觉反馈可以帮助用户更好地理解系统的操作过程和结果。
- CodeClaw 的视觉分析 Agent 需要具备一定的鲁棒性，能够适应不同的界面布局和元素样式，妥善处理逻辑分辨率和物理分辨率的差异（例如按钮的位置和大小可能因分辨率变化而改变，视觉分析 Agent 需要能够根据这些变化进行调整，找到正确的坐标进行操作）。
- 所有设计都是通用抽象，涉及不同平台和 Bot 的细节都应通过独立模块或插件来实现，防止耦合影响维护，保证系统的灵活性和可扩展性，以适应不同的使用场景和需求。
- 像 OpenCode 的 Web UI 一样，CodeClaw 提供一个可视化的对话界面来管理和监控系统的运行状态，例如查看当前的任务队列、监控系统资源使用情况、查看操作日志等。点击系统托盘图标即可打开该界面。
