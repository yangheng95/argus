# v0.0.13beta Branch And Version Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current delivery line to the `v0.0.13beta` branch while preserving every parallel worktree change. |
| Acceptance criteria | Create `v0.0.13beta` from the exact current `v0.0.12beta` commit; keep all tracked and untracked parallel edits byte-preserved in the shared worktree; synchronize the release family to canonical Semantic Versioning (SemVer) `0.0.13-beta` and Windows Microsoft Installer (MSI) projection `0.0.13.0`; validate and push the branch to git-cc remote `myhexin`. |
| Hard constraints | Do not stash, reset, restore, delete, or broadly stage parallel changes. Use `script/sync-version.ts` as the only version writer. Do not bypass hooks or create another worktree. Commit subjects start with `dsw-33987`. Do not restart or refresh OpenCorvus or Overlay. |
| Sources read | Repository `AGENTS.md`; current branch, status, remotes, and recent history; `RELEASE.md`; `script/sync-version.ts`; `packages/sdk/js/script/build.ts`; July spec indexes; `2026-07-20-work-v0.0.11beta-yr-0720-merge-and-v0.0.12beta-bump.md`. |
| Whole-repository grep | `releasePackageTargets` owns nine package manifests; `cargoManifestTargets` owns both native manifests and locks; `tauriConfigTarget` and `bunLockTarget` own the remaining synchronized projections. `RELEASE.md` contains the current compact bump example. The generated OpenAPI and JavaScript SDK are owned transactionally by `packages/sdk/js/script/build.ts`. |
| Git evidence | Fresh fetch found no local or remote `v0.0.13beta`. The branch was created directly from `v0.0.12beta` commit `1218de5da`. The shared worktree contained 16 tracked modifications and 3 untracked files before the branch cut; an additional parallel browser-test edit appeared afterward and is also outside this bump's staging boundary. |
| Preservation boundary | The release bump may stage only the explicit version registry targets, `RELEASE.md`, and this record plus the two spec indexes. Existing attachment, composer, protocol, architecture, and browser-test work remains unstaged. Generated SDK/OpenAPI repair needed by the pre-push checker also remains part of the parallel feature surface and must not be folded into the release commit. |
| Independent agent feedback | None requested; current collaboration policy forbids unrequested delegation. |

## Plan

1. Record and index this preservation boundary before changing release metadata.
2. Run `bun run version:bump v0.0.13beta`, update the canonical release example, and inspect the exact diff against the synchronizer's declared target registry.
3. Run focused version synchronizer tests, `version:check`, required document-health checks, and repository hook checks.
4. Stage only the version bump and indexed release record, commit with the required prefix, then push `v0.0.13beta` to `myhexin` without bypassing hooks.
5. Prove local/remote commit equality and re-audit that every pre-existing parallel path remains present and outside the release commit.

## Verification Ledger

- Created `v0.0.13beta` directly at preserved baseline `1218de5da`; no local or remote branch with that name existed after a fresh `myhexin` fetch.
- `bun run version:bump v0.0.13beta` synchronized the declared release family to `0.0.13-beta` and the Tauri MSI projection to `0.0.13.0`; `bun run version:check` passes.
- The transactional JavaScript SDK build synchronized the parallel attachment route into OpenAPI and generated client artifacts without adding those feature files to the release commit; `bun run api:routes-check` passes all six inventory rules across 31 route files.
- Focused version, historical-link, and document-health verification passes with 87 tests and 1,372 expectations.
- Release commit `059b25956` contains only the 19 declared version, release-documentation, and indexed record files. The normal pre-push hook passed workspace type checking, all six route-inventory rules, generated API documentation equality for 283 operations in 23 groups, Overlay internationalization checks, and the tracked-source secret scan.
- `myhexin/v0.0.13beta` was created from that commit without bypassing hooks. The post-push audit confirms the attachment, Composer, protocol, architecture, browser-test, generated SDK/OpenAPI, and generated bilingual API-documentation changes remain outside the release commit in the shared worktree.
