# Agent Rules Entry Point

`AGENTS.md` is the single source of truth for agent execution rules in this repository.

Do not copy or fork those rules into this file. Claude Code agents must read and follow `AGENTS.md` before changing repository files.
