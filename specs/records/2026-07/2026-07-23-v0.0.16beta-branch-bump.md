# v0.0.16beta Branch Bump

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Bump the current delivery line to `v0.0.16beta`. |
| Acceptance | Create `v0.0.16beta` from the current pushed `v0.0.15beta` tip, synchronize the release family to canonical SemVer `0.0.16-beta` and Windows MSI (Microsoft Installer) projection `0.0.16.0`, validate the repository contracts, commit only bump-owned files, and push the new branch to `legacy-remote`. |
| Git baseline | After fetch, local and `legacy-remote/v0.0.15beta` both resolve to `7594394577f8a09d4c608105b4555eedff1600bf`; neither a local nor remote `v0.0.16beta` branch exists. |
| Parallel changes | In-progress Mirror Prism package, reference, architecture, and historical-record edits are present in the shared worktree. They have zero overlap with the version registry targets, `RELEASE.md`, or this record/index entry and must remain unstaged and unmodified by the bump. |
| Hard constraints | Use `script/sync-version.ts` as the only version writer; do not reset, stash, create a worktree, restart OpenCorvus/Overlay, package, or bypass hooks; use the `dsw-33987` commit prefix and push only to `legacy-remote`. |
| Sources read | Root `AGENTS.md`; current status, graph, branches, remotes, and divergence; `script/sync-version.ts`; `RELEASE.md`; prior `v0.0.15beta` bump record; root and July spec indexes. |
| Whole-repository grep | The synchronizer owns nine package manifests and their Bun workspace rows, two Cargo manifest/lock pairs, and Tauri application/MSI metadata. Historical `0.0.15-beta` records and test fixtures remain historical or fixture-owned; only the live release example is updated outside the synchronizer registry. |
| Independent-agent feedback | None. The active execution policy does not permit unsolicited sub-agent delegation. |

## Plan

1. Create `v0.0.16beta` from the verified pushed `v0.0.15beta` tip while carrying the unrelated parallel worktree edits unchanged.
2. Run the repository version synchronizer with `v0.0.16beta` and update the live release command example.
3. Validate version convergence, focused synchronizer tests, required documentation health, and the normal push-hook checks.
4. Stage and commit only version-bump-owned files, push `v0.0.16beta` to `legacy-remote`, and verify local/remote equality while retaining parallel edits.

## Verification ledger

- Branch creation: after a fresh fetch confirmed local/remote `v0.0.15beta` equality at `759439457`, created `v0.0.16beta`; no local or remote branch with that name previously existed.
- Version convergence: `bun run version:bump v0.0.16beta` synchronized all declared release-family targets to `0.0.16-beta` and the Tauri MSI projection to `0.0.16.0`; `bun run version:check` passes.
- Focused verification: all 5 version-synchronizer tests pass; root typecheck passes all 9 package tasks; historical links, document health, and product docs single-source suites pass 87/87 after the new record entered the Git index.
- Visible version verification: the Node-launched sidebar browser test passes 1/1. Its current screenshot was inspected and shows the compact `v0.0.16beta` label, author email tooltip, and Online status without clipping or alignment regressions.
- Preservation audit: the bump index contains only the 19 declared version/release/record files. Mirror Prism package, generated payload, tests, architecture, references, and historical record work remains outside the index.
- Delivery: commit `6f2f666b5` contains exactly the 19 bump-owned files. The normal pre-push hook passed workspace typecheck, route inventory, generated API documentation equality, Overlay internationalization, and secret scanning. The legacy remote server printed `Invalid revision range 0000000000000000000000000000000000000000..6f2f666b5` from a remote hook while creating the new branch, but accepted the push; a fresh fetch confirms local and `legacy-remote/v0.0.16beta` both resolve to `6f2f666b5` with delta `0 0`.
