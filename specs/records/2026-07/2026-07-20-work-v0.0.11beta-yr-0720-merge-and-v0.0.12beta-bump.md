# work-v0.0.11beta yr-0720 Merge, v0.0.12beta Bump, And Matrix Build

## Recall

| Item | Evidence / constraint |
| --- | --- |
| User request | Merge the branch again, bump the result to `0.0.12beta`, and package it. The branch in scope is the previously requested `myhexin/work-v0.0.11beta-yr-0720`. |
| Acceptance criteria | Synchronize the latest remote `v0.0.11beta`; merge the complete latest work-line tip; push the converged delivery line; create and push `v0.0.12beta`; synchronize the release family to canonical Semantic Versioning (SemVer) `0.0.12-beta` and Windows Microsoft Installer (MSI) projection `0.0.12.0`; run GUI installer and command-line interface (CLI) binary matrices from one immutable commit; verify native artifacts, signatures, disk image, version, hashes, contracts, and remote equality. |
| Hard constraints | Use `script/sync-version.ts` as the only version writer. Do not reset, create a worktree, restart or refresh OpenCorvus/Overlay, bypass hooks, invent fallback behavior, stage the pre-existing untracked `C:/` directory, or claim non-native matrix rows were built. Commit subjects start with `dsw-33987`; pushes go to git-cc remote `myhexin`. |
| Sources read | Repository `AGENTS.md`; current branch/status/remotes; fetched delivery/work refs; full divergence, work-only commit log, three-dot file statistics; `2026-07-20-v0.0.11beta-branch-and-version-bump.md`; release documentation; version synchronizer and its focused tests; spec indexes. |
| Whole-repository merge surface | After synchronizing the delivery line, the work line has five unique commits affecting 24 files: project/Memory hover boundaries, Settings persistence and task-selection error handling, Button/Tooltip primitives, Work Ledger and project group components, focused unit/browser tests, architecture notes, and July records. Merge resolution must preserve the delivery line's independently advanced Expert Squad, Browser Preview, message-projection, and runtime work. |
| Git evidence | Local `v0.0.11beta` initially pointed at `5d9704b14`; a fresh fetch found remote delivery tip `231cc0a57`, so the local branch was fast-forwarded by 16 commits before planning. At that synchronized baseline the delivery/work divergence is 46/5, and work tip is `8b6232ee2`. No remote `v0.0.12beta` exists. |
| Version evidence | `bun run version:check` passes at `0.0.11-beta`. `package.json` exposes `version:bump` through `script/sync-version.ts`; current release documentation still names `0.0.11beta` and must be updated as the current example when the new line is cut. |
| Preservation boundary | Tracked files are clean before this plan. The untracked `C:/` directory predates the task and remains outside staging. |
| Independent agent feedback | None requested; the active collaboration policy forbids unrequested delegation. |

## Plan

1. Commit and push this Recall/plan on synchronized `v0.0.11beta` before merging.
2. Re-fetch both v0.0.11 lines, merge `myhexin/work-v0.0.11beta-yr-0720` with an explicit merge commit, resolve conflicts from current production/test ownership, and run focused plus repository-level verification including Node browser scenarios and screenshot review for changed frontend surfaces.
3. Commit and push the converged `v0.0.11beta`, then prove the work tip is an ancestor of an identical local/remote delivery head.
4. Create `v0.0.12beta` from that exact head, commit/push the branch baseline, run `bun run version:bump 0.0.12beta`, update the current release example, and verify every synchronized target and focused version contract.
5. Commit and push the immutable `v0.0.12beta` build source through normal hooks, then run version check, GUI installer matrix, and CLI binary matrix without moving the source commit.
6. Validate native artifact formats, macOS signatures, disk-image checksum, CLI version, sizes, SHA-256 hashes, packaging contracts, final remote equality, and preservation of `C:/`.

## Verification Ledger

- Synchronized `v0.0.11beta` and merged `myhexin/work-v0.0.11beta-yr-0720` at work tip `8b6232ee2`; six textual conflicts were resolved by retaining the delivery line's current Work Ledger organization while incorporating the work line's pane-width sanitization, hover ownership, and error-boundary behavior.
- The merged Tooltip change exposed Kobalte's unconditional inline `pointer-events: auto` write. The repository's existing `@kobalte/core` patch now clears that inline value when no pointer-blocking layer exists, covering both distributed module formats; the primitive CSS remains a normal declaration and focused patch/primitive tests pass.
- A forced frozen install exposed an undeclared `jsonc-parser` test import in Overlay. `@opencorvus-ai/overlay` now declares the exact direct development dependency and both normal lock generation and `bun install --frozen-lockfile` pass.
- Focused merged tests pass, including Settings persistence, task selection, Memory, Button, Tooltip, left-rail density, Work Ledger consolidation, pane configuration, Kobalte patch declarations, and the remaining Overlay unit suite from `tree-writer-hierarchy.test.ts` onward (including 59 hierarchy tests and all subsequent files).
- `bunx turbo run typecheck` passes across all 9 typechecked packages. Historical links, document health, and documentation single-source coverage pass with 82 tests.
- Node-sidecar browser verification passes for project grouping/hover ownership and fractional pane-width task selection. Reviewed screenshots show project-only hover chrome, task-only action chrome, and the hydrated conversation without a false load error; no browser runtime errors or failed fixture responses remain.
- A concurrent git-cc delivery update arrived during the first push attempt. Its effective delta was the Multica import repair plan and two spec indexes; it was merged without conflicts, the 82 documentation tests passed again, and synchronized `v0.0.11beta` commit `f0e7428c8` was pushed with work tip `8b6232ee2` proven as an ancestor.
- Created and pushed `v0.0.12beta` from exact delivery commit `f0e7428c8`. `bun run version:bump 0.0.12beta` synchronized all registered release-family targets to `0.0.12-beta` and the Tauri Windows projection to `0.0.12.0`; the release command example now names the current line.
- `bun run version:check`, all 5 focused version synchronizer tests, and the combined 87 version/document tests pass. The Node sidebar-version browser test also passes, and screenshot review confirms the visible compact label `v0.0.12beta` and author tooltip.
- The refreshed `v0.0.11beta` delivery merge exposed a real cross-line fixture defect: `v0.0.12beta` project startup now posts the canonical Git initialization endpoint before the Multica and Chat browser scenarios, while generic visual fixtures had no writable repository or route response. Generic browser settings now explicitly opt out of that mutation; the Multica merge scenario explicitly opts in, serves the idempotent endpoint, and asserts both full-page startups call it before completing the repair-or-cancel interaction.
- Immutable source commit, matrices, artifact validation, and final remote equality remain pending.
