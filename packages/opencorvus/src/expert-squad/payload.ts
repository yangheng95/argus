// Auto-generated by packages/opencorvus/script/generate-expert-squad-payload.ts. Do not edit.
import payload_algorithm_agents_architect_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/architect/system.md" with { type: "text" }
import payload_algorithm_agents_build_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/build/system.md" with { type: "text" }
import payload_algorithm_agents_coding_assistant_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/coding-assistant/system.md" with { type: "text" }
import payload_algorithm_agents_coding_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/coding/system.md" with { type: "text" }
import payload_algorithm_agents_deep_research_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/deep-research/system.md" with { type: "text" }
import payload_algorithm_agents_explore_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/explore/system.md" with { type: "text" }
import payload_algorithm_agents_fact_check_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/fact-check/system.md" with { type: "text" }
import payload_algorithm_agents_general_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/general/system.md" with { type: "text" }
import payload_algorithm_agents_goal_workload_analyst_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/goal-workload-analyst/system.md" with { type: "text" }
import payload_algorithm_agents_integrity_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/integrity/system.md" with { type: "text" }
import payload_algorithm_agents_intent_analysis_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/intent-analysis/system.md" with { type: "text" }
import payload_algorithm_agents_mission_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/mission/system.md" with { type: "text" }
import payload_algorithm_agents_orchestrator_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/orchestrator/system.md" with { type: "text" }
import payload_algorithm_agents_requirements_system_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/agents/requirements/system.md" with { type: "text" }
import payload_algorithm_expert_squad_jsonc from "../../../../.opencorvus/expert-squads/builtin/algorithm/expert-squad.jsonc" with { type: "text" }
import payload_algorithm_README_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/README.md" with { type: "text" }
import payload_algorithm_selector_md from "../../../../.opencorvus/expert-squads/builtin/algorithm/selector.md" with { type: "text" }
import payload_backend_agents_architect_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/architect/system.md" with { type: "text" }
import payload_backend_agents_build_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/build/system.md" with { type: "text" }
import payload_backend_agents_coding_assistant_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/coding-assistant/system.md" with { type: "text" }
import payload_backend_agents_coding_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/coding/system.md" with { type: "text" }
import payload_backend_agents_deep_research_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/deep-research/system.md" with { type: "text" }
import payload_backend_agents_explore_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/explore/system.md" with { type: "text" }
import payload_backend_agents_fact_check_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/fact-check/system.md" with { type: "text" }
import payload_backend_agents_general_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/general/system.md" with { type: "text" }
import payload_backend_agents_goal_workload_analyst_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/goal-workload-analyst/system.md" with { type: "text" }
import payload_backend_agents_integrity_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/integrity/system.md" with { type: "text" }
import payload_backend_agents_intent_analysis_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/intent-analysis/system.md" with { type: "text" }
import payload_backend_agents_mission_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/mission/system.md" with { type: "text" }
import payload_backend_agents_orchestrator_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/orchestrator/system.md" with { type: "text" }
import payload_backend_agents_requirements_system_md from "../../../../.opencorvus/expert-squads/builtin/backend/agents/requirements/system.md" with { type: "text" }
import payload_backend_expert_squad_jsonc from "../../../../.opencorvus/expert-squads/builtin/backend/expert-squad.jsonc" with { type: "text" }
import payload_backend_README_md from "../../../../.opencorvus/expert-squads/builtin/backend/README.md" with { type: "text" }
import payload_backend_selector_md from "../../../../.opencorvus/expert-squads/builtin/backend/selector.md" with { type: "text" }
import payload_frontend_automation_debug_agents_architect_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/architect/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_build_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/build/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_coding_assistant_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/coding-assistant/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_coding_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/coding/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_deep_research_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/deep-research/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_explore_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/explore/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_fact_check_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/fact-check/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_frontend_design_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/frontend-design/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_frontend_research_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/frontend-research/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_general_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/general/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_goal_workload_analyst_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/goal-workload-analyst/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_integrity_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/integrity/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_intent_analysis_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/intent-analysis/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_mission_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/mission/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_orchestrator_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/orchestrator/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_requirements_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/requirements/system.md" with { type: "text" }
import payload_frontend_automation_debug_agents_visual_qa_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/agents/visual-qa/system.md" with { type: "text" }
import payload_frontend_automation_debug_expert_squad_jsonc from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/expert-squad.jsonc" with { type: "text" }
import payload_frontend_automation_debug_README_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/README.md" with { type: "text" }
import payload_frontend_automation_debug_selector_md from "../../../../.opencorvus/expert-squads/builtin/frontend-automation-debug/selector.md" with { type: "text" }
import payload_frontend_innovate_agents_architect_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/architect/system.md" with { type: "text" }
import payload_frontend_innovate_agents_build_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/build/system.md" with { type: "text" }
import payload_frontend_innovate_agents_coding_assistant_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/coding-assistant/system.md" with { type: "text" }
import payload_frontend_innovate_agents_coding_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/coding/system.md" with { type: "text" }
import payload_frontend_innovate_agents_deep_research_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/deep-research/system.md" with { type: "text" }
import payload_frontend_innovate_agents_explore_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/explore/system.md" with { type: "text" }
import payload_frontend_innovate_agents_fact_check_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/fact-check/system.md" with { type: "text" }
import payload_frontend_innovate_agents_frontend_design_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/frontend-design/system.md" with { type: "text" }
import payload_frontend_innovate_agents_frontend_research_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/frontend-research/system.md" with { type: "text" }
import payload_frontend_innovate_agents_general_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/general/system.md" with { type: "text" }
import payload_frontend_innovate_agents_goal_workload_analyst_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/goal-workload-analyst/system.md" with { type: "text" }
import payload_frontend_innovate_agents_integrity_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/integrity/system.md" with { type: "text" }
import payload_frontend_innovate_agents_intent_analysis_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/intent-analysis/system.md" with { type: "text" }
import payload_frontend_innovate_agents_mission_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/mission/system.md" with { type: "text" }
import payload_frontend_innovate_agents_orchestrator_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/orchestrator/system.md" with { type: "text" }
import payload_frontend_innovate_agents_requirements_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/requirements/system.md" with { type: "text" }
import payload_frontend_innovate_agents_visual_qa_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/agents/visual-qa/system.md" with { type: "text" }
import payload_frontend_innovate_expert_squad_jsonc from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/expert-squad.jsonc" with { type: "text" }
import payload_frontend_innovate_README_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/README.md" with { type: "text" }
import payload_frontend_innovate_selector_md from "../../../../.opencorvus/expert-squads/builtin/frontend-innovate/selector.md" with { type: "text" }
import payload_frontend_replica_agents_architect_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/architect/system.md" with { type: "text" }
import payload_frontend_replica_agents_build_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/build/system.md" with { type: "text" }
import payload_frontend_replica_agents_coding_assistant_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/coding-assistant/system.md" with { type: "text" }
import payload_frontend_replica_agents_coding_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/coding/system.md" with { type: "text" }
import payload_frontend_replica_agents_explore_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/explore/system.md" with { type: "text" }
import payload_frontend_replica_agents_frontend_design_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/frontend-design/system.md" with { type: "text" }
import payload_frontend_replica_agents_frontend_research_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/frontend-research/system.md" with { type: "text" }
import payload_frontend_replica_agents_general_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/general/system.md" with { type: "text" }
import payload_frontend_replica_agents_integrity_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/integrity/system.md" with { type: "text" }
import payload_frontend_replica_agents_intent_analysis_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/intent-analysis/system.md" with { type: "text" }
import payload_frontend_replica_agents_mission_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/mission/system.md" with { type: "text" }
import payload_frontend_replica_agents_orchestrator_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/orchestrator/system.md" with { type: "text" }
import payload_frontend_replica_agents_requirements_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/requirements/system.md" with { type: "text" }
import payload_frontend_replica_agents_visual_qa_system_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/agents/visual-qa/system.md" with { type: "text" }
import payload_frontend_replica_expert_squad_jsonc from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/expert-squad.jsonc" with { type: "text" }
import payload_frontend_replica_README_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/README.md" with { type: "text" }
import payload_frontend_replica_selector_md from "../../../../.opencorvus/expert-squads/builtin/frontend-replica/selector.md" with { type: "text" }
import payload_opentest_agents_orchestrator_skills_workflow_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/agents/orchestrator/skills/workflow/SKILL.md" with { type: "text" }
import payload_opentest_agents_orchestrator_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/agents/orchestrator/system.md" with { type: "text" }
import payload_opentest_expert_squad_jsonc from "../../../../.opencorvus/expert-squads/wujiang/opentest/expert-squad.jsonc" with { type: "text" }
import payload_opentest_protocol_engine_opentest_contract_json from "../../../../.opencorvus/expert-squads/wujiang/opentest/protocol-engine/opentest-contract.json" with { type: "text" }
import payload_opentest_README_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/README.md" with { type: "text" }
import payload_opentest_selector_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/selector.md" with { type: "text" }
import payload_opentest_virtual_agents_architect_skills_test_architecture_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/architect/skills/test-architecture/SKILL.md" with { type: "text" }
import payload_opentest_virtual_agents_architect_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/architect/system.md" with { type: "text" }
import payload_opentest_virtual_agents_build_skills_test_implementation_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/build/skills/test-implementation/SKILL.md" with { type: "text" }
import payload_opentest_virtual_agents_build_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/build/system.md" with { type: "text" }
import payload_opentest_virtual_agents_integrity_skills_test_review_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/integrity/skills/test-review/SKILL.md" with { type: "text" }
import payload_opentest_virtual_agents_integrity_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/integrity/system.md" with { type: "text" }
import payload_opentest_virtual_agents_intent_analysis_skills_test_intent_analysis_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/intent-analysis/skills/test-intent-analysis/SKILL.md" with { type: "text" }
import payload_opentest_virtual_agents_intent_analysis_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/intent-analysis/system.md" with { type: "text" }
import payload_opentest_virtual_agents_requirements_skills_test_requirements_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/requirements/skills/test-requirements/SKILL.md" with { type: "text" }
import payload_opentest_virtual_agents_requirements_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/requirements/system.md" with { type: "text" }
import payload_opentest_virtual_agents_visual_qa_skills_visual_test_review_SKILL_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/visual-qa/skills/visual-test-review/SKILL.md" with { type: "text" }
import payload_opentest_virtual_agents_visual_qa_system_md from "../../../../.opencorvus/expert-squads/wujiang/opentest/virtual-agents/visual-qa/system.md" with { type: "text" }

import type { ExpertSquadRegistry } from "./registry"

const textPayload = (value: unknown): string => value as string

export const payloadPackageSources: readonly ExpertSquadRegistry.EmbeddedPackageSource[] = [
  {
    namespace: "builtin",
    id: "algorithm",
    manifestText: textPayload(payload_algorithm_expert_squad_jsonc),
    files: {
      "agents/architect/system.md": textPayload(payload_algorithm_agents_architect_system_md),
      "agents/build/system.md": textPayload(payload_algorithm_agents_build_system_md),
      "agents/coding-assistant/system.md": textPayload(payload_algorithm_agents_coding_assistant_system_md),
      "agents/coding/system.md": textPayload(payload_algorithm_agents_coding_system_md),
      "agents/deep-research/system.md": textPayload(payload_algorithm_agents_deep_research_system_md),
      "agents/explore/system.md": textPayload(payload_algorithm_agents_explore_system_md),
      "agents/fact-check/system.md": textPayload(payload_algorithm_agents_fact_check_system_md),
      "agents/general/system.md": textPayload(payload_algorithm_agents_general_system_md),
      "agents/goal-workload-analyst/system.md": textPayload(payload_algorithm_agents_goal_workload_analyst_system_md),
      "agents/integrity/system.md": textPayload(payload_algorithm_agents_integrity_system_md),
      "agents/intent-analysis/system.md": textPayload(payload_algorithm_agents_intent_analysis_system_md),
      "agents/mission/system.md": textPayload(payload_algorithm_agents_mission_system_md),
      "agents/orchestrator/system.md": textPayload(payload_algorithm_agents_orchestrator_system_md),
      "agents/requirements/system.md": textPayload(payload_algorithm_agents_requirements_system_md),
      "expert-squad.jsonc": textPayload(payload_algorithm_expert_squad_jsonc),
      "README.md": textPayload(payload_algorithm_README_md),
      "selector.md": textPayload(payload_algorithm_selector_md),
    },
  },
  {
    namespace: "builtin",
    id: "backend",
    manifestText: textPayload(payload_backend_expert_squad_jsonc),
    files: {
      "agents/architect/system.md": textPayload(payload_backend_agents_architect_system_md),
      "agents/build/system.md": textPayload(payload_backend_agents_build_system_md),
      "agents/coding-assistant/system.md": textPayload(payload_backend_agents_coding_assistant_system_md),
      "agents/coding/system.md": textPayload(payload_backend_agents_coding_system_md),
      "agents/deep-research/system.md": textPayload(payload_backend_agents_deep_research_system_md),
      "agents/explore/system.md": textPayload(payload_backend_agents_explore_system_md),
      "agents/fact-check/system.md": textPayload(payload_backend_agents_fact_check_system_md),
      "agents/general/system.md": textPayload(payload_backend_agents_general_system_md),
      "agents/goal-workload-analyst/system.md": textPayload(payload_backend_agents_goal_workload_analyst_system_md),
      "agents/integrity/system.md": textPayload(payload_backend_agents_integrity_system_md),
      "agents/intent-analysis/system.md": textPayload(payload_backend_agents_intent_analysis_system_md),
      "agents/mission/system.md": textPayload(payload_backend_agents_mission_system_md),
      "agents/orchestrator/system.md": textPayload(payload_backend_agents_orchestrator_system_md),
      "agents/requirements/system.md": textPayload(payload_backend_agents_requirements_system_md),
      "expert-squad.jsonc": textPayload(payload_backend_expert_squad_jsonc),
      "README.md": textPayload(payload_backend_README_md),
      "selector.md": textPayload(payload_backend_selector_md),
    },
  },
  {
    namespace: "builtin",
    id: "frontend-automation-debug",
    manifestText: textPayload(payload_frontend_automation_debug_expert_squad_jsonc),
    files: {
      "agents/architect/system.md": textPayload(payload_frontend_automation_debug_agents_architect_system_md),
      "agents/build/system.md": textPayload(payload_frontend_automation_debug_agents_build_system_md),
      "agents/coding-assistant/system.md": textPayload(payload_frontend_automation_debug_agents_coding_assistant_system_md),
      "agents/coding/system.md": textPayload(payload_frontend_automation_debug_agents_coding_system_md),
      "agents/deep-research/system.md": textPayload(payload_frontend_automation_debug_agents_deep_research_system_md),
      "agents/explore/system.md": textPayload(payload_frontend_automation_debug_agents_explore_system_md),
      "agents/fact-check/system.md": textPayload(payload_frontend_automation_debug_agents_fact_check_system_md),
      "agents/frontend-design/system.md": textPayload(payload_frontend_automation_debug_agents_frontend_design_system_md),
      "agents/frontend-research/system.md": textPayload(payload_frontend_automation_debug_agents_frontend_research_system_md),
      "agents/general/system.md": textPayload(payload_frontend_automation_debug_agents_general_system_md),
      "agents/goal-workload-analyst/system.md": textPayload(payload_frontend_automation_debug_agents_goal_workload_analyst_system_md),
      "agents/integrity/system.md": textPayload(payload_frontend_automation_debug_agents_integrity_system_md),
      "agents/intent-analysis/system.md": textPayload(payload_frontend_automation_debug_agents_intent_analysis_system_md),
      "agents/mission/system.md": textPayload(payload_frontend_automation_debug_agents_mission_system_md),
      "agents/orchestrator/system.md": textPayload(payload_frontend_automation_debug_agents_orchestrator_system_md),
      "agents/requirements/system.md": textPayload(payload_frontend_automation_debug_agents_requirements_system_md),
      "agents/visual-qa/system.md": textPayload(payload_frontend_automation_debug_agents_visual_qa_system_md),
      "expert-squad.jsonc": textPayload(payload_frontend_automation_debug_expert_squad_jsonc),
      "README.md": textPayload(payload_frontend_automation_debug_README_md),
      "selector.md": textPayload(payload_frontend_automation_debug_selector_md),
    },
  },
  {
    namespace: "builtin",
    id: "frontend-innovate",
    manifestText: textPayload(payload_frontend_innovate_expert_squad_jsonc),
    files: {
      "agents/architect/system.md": textPayload(payload_frontend_innovate_agents_architect_system_md),
      "agents/build/system.md": textPayload(payload_frontend_innovate_agents_build_system_md),
      "agents/coding-assistant/system.md": textPayload(payload_frontend_innovate_agents_coding_assistant_system_md),
      "agents/coding/system.md": textPayload(payload_frontend_innovate_agents_coding_system_md),
      "agents/deep-research/system.md": textPayload(payload_frontend_innovate_agents_deep_research_system_md),
      "agents/explore/system.md": textPayload(payload_frontend_innovate_agents_explore_system_md),
      "agents/fact-check/system.md": textPayload(payload_frontend_innovate_agents_fact_check_system_md),
      "agents/frontend-design/system.md": textPayload(payload_frontend_innovate_agents_frontend_design_system_md),
      "agents/frontend-research/system.md": textPayload(payload_frontend_innovate_agents_frontend_research_system_md),
      "agents/general/system.md": textPayload(payload_frontend_innovate_agents_general_system_md),
      "agents/goal-workload-analyst/system.md": textPayload(payload_frontend_innovate_agents_goal_workload_analyst_system_md),
      "agents/integrity/system.md": textPayload(payload_frontend_innovate_agents_integrity_system_md),
      "agents/intent-analysis/system.md": textPayload(payload_frontend_innovate_agents_intent_analysis_system_md),
      "agents/mission/system.md": textPayload(payload_frontend_innovate_agents_mission_system_md),
      "agents/orchestrator/system.md": textPayload(payload_frontend_innovate_agents_orchestrator_system_md),
      "agents/requirements/system.md": textPayload(payload_frontend_innovate_agents_requirements_system_md),
      "agents/visual-qa/system.md": textPayload(payload_frontend_innovate_agents_visual_qa_system_md),
      "expert-squad.jsonc": textPayload(payload_frontend_innovate_expert_squad_jsonc),
      "README.md": textPayload(payload_frontend_innovate_README_md),
      "selector.md": textPayload(payload_frontend_innovate_selector_md),
    },
  },
  {
    namespace: "builtin",
    id: "frontend-replica",
    manifestText: textPayload(payload_frontend_replica_expert_squad_jsonc),
    files: {
      "agents/architect/system.md": textPayload(payload_frontend_replica_agents_architect_system_md),
      "agents/build/system.md": textPayload(payload_frontend_replica_agents_build_system_md),
      "agents/coding-assistant/system.md": textPayload(payload_frontend_replica_agents_coding_assistant_system_md),
      "agents/coding/system.md": textPayload(payload_frontend_replica_agents_coding_system_md),
      "agents/explore/system.md": textPayload(payload_frontend_replica_agents_explore_system_md),
      "agents/frontend-design/system.md": textPayload(payload_frontend_replica_agents_frontend_design_system_md),
      "agents/frontend-research/system.md": textPayload(payload_frontend_replica_agents_frontend_research_system_md),
      "agents/general/system.md": textPayload(payload_frontend_replica_agents_general_system_md),
      "agents/integrity/system.md": textPayload(payload_frontend_replica_agents_integrity_system_md),
      "agents/intent-analysis/system.md": textPayload(payload_frontend_replica_agents_intent_analysis_system_md),
      "agents/mission/system.md": textPayload(payload_frontend_replica_agents_mission_system_md),
      "agents/orchestrator/system.md": textPayload(payload_frontend_replica_agents_orchestrator_system_md),
      "agents/requirements/system.md": textPayload(payload_frontend_replica_agents_requirements_system_md),
      "agents/visual-qa/system.md": textPayload(payload_frontend_replica_agents_visual_qa_system_md),
      "expert-squad.jsonc": textPayload(payload_frontend_replica_expert_squad_jsonc),
      "README.md": textPayload(payload_frontend_replica_README_md),
      "selector.md": textPayload(payload_frontend_replica_selector_md),
    },
  },
  {
    namespace: "wujiang",
    id: "opentest",
    manifestText: textPayload(payload_opentest_expert_squad_jsonc),
    files: {
      "agents/orchestrator/skills/workflow/SKILL.md": textPayload(payload_opentest_agents_orchestrator_skills_workflow_SKILL_md),
      "agents/orchestrator/system.md": textPayload(payload_opentest_agents_orchestrator_system_md),
      "expert-squad.jsonc": textPayload(payload_opentest_expert_squad_jsonc),
      "protocol-engine/opentest-contract.json": textPayload(payload_opentest_protocol_engine_opentest_contract_json),
      "protocol-engine/opentest-protocol-engine.ts": "import { readdir, readFile, stat } from \"node:fs/promises\"\nimport path from \"node:path\"\nimport * as ts from \"typescript\"\n\nexport type ProtocolContract = {\n  schema_version: 1\n  protocol: string\n  artifacts: {\n    test_markdown: string\n    script: string\n    project_runtime_dir: string\n    context_contract: string\n    acceptance: string\n    run_database: string\n    run_result_pattern: string\n    case_run_result_pattern: string\n    batch_run_result_pattern: string\n    case_run_screenshot_pattern: string\n  }\n  frontmatter: {\n    required_fields: string[]\n    status_field: string\n    default_status: string\n    lifecycle: string[]\n    test_points_field: string\n    test_point_name_field: string\n  }\n  script: {\n    required_export: string\n    mark_point_callee: string\n    mark_point_name_sources: string[]\n    mark_point_argument_count: number\n    mark_point_required_properties: string[]\n    mark_point_optional_properties: string[]\n    mark_point_allow_unknown_properties: boolean\n    context_contract_mark_point_pattern: string\n  }\n  lifecycle_rules: {\n    script_newer_than_test_markdown_downgrades: {\n      from: string\n      to: string\n    }\n    draft_run_must_not_update_acceptance: boolean\n  }\n  acceptance: {\n    attention_statuses: string[]\n    project_relative: boolean\n    model_draft_run_updates_acceptance: boolean\n  }\n}\n\nexport type OpenTestArtifactInventory = {\n  schema_version: 1\n  protocol: string\n  root: string\n  scanned_file_count: number\n  scan_limit_reached: boolean\n  test_cases: Array<{\n    directory: string\n    test_md: string\n    script_ts: string | null\n    context_contract: string | null\n    acceptance: string | null\n    run_database: string | null\n    run_results: string[]\n  }>\n  context_files: string[]\n  run_results: string[]\n  case_run_results: string[]\n  batch_run_results: string[]\n  run_databases: string[]\n  screenshots: string[]\n  acceptance_files: string[]\n  test_configs: string[]\n  package_scripts: PackageScript[]\n}\n\nexport type OpenTestValidationResult = {\n  schema_version: 1\n  protocol: string\n  directory: string\n  valid: boolean\n  errors: string[]\n  artifacts: {\n    test_md: string\n    script_ts: string | null\n    context_contract: string | null\n    acceptance: string | null\n    run_database: string | null\n    run_results: string[]\n  }\n  lifecycle: {\n    declared_status: string | null\n    effective_status: string | null\n    script_modified_after_test_md: boolean\n    active_downgraded_to_draft: boolean\n    draft_run_updates_acceptance: boolean\n  }\n  test_points: string[]\n  mark_points: string[]\n}\n\ntype PackageScript = {\n  package_json: string\n  script: string\n  command: string\n}\n\ntype FileEntry = {\n  absolute: string\n  relative: string\n}\n\nconst skippedDirectoryNames = new Set([\".git\", \"node_modules\", \"dist\", \"build\", \".next\", \".turbo\", \"coverage\"])\nconst testConfigPattern = /^(playwright|vitest|jest|karma|cypress|mocha|ava)\\.config\\.[cm]?[jt]s$|^pytest\\.ini$|^tox\\.ini$|^noxfile\\.py$/\nconst testScriptPattern = /\\b(test|vitest|playwright|jest|mocha|pytest|cypress|ava|node --test|bun test)\\b/i\n\nexport function parseProtocolContract(text: string): ProtocolContract {\n  let parsed: unknown\n  try {\n    parsed = JSON.parse(text)\n  } catch (error) {\n    const message = error instanceof Error ? error.message : String(error)\n    throw new Error(`OpenTest protocol contract invalid: JSON parse failed: ${message}`)\n  }\n  assertProtocolContract(parsed)\n  return parsed\n}\n\nexport function assertProtocolContract(contract: unknown): asserts contract is ProtocolContract {\n  const fail = (message: string): never => {\n    throw new Error(`OpenTest protocol contract invalid: ${message}`)\n  }\n  const object = (value: unknown, pathName: string): Record<string, unknown> => {\n    if (!value || typeof value !== \"object\" || Array.isArray(value)) fail(`${pathName} must be an object`)\n    return value as Record<string, unknown>\n  }\n  const stringField = (source: Record<string, unknown>, key: string, pathName: string) => {\n    const value = source[key]\n    if (typeof value !== \"string\" || !value.trim()) fail(`${pathName}.${key} must be a nonempty string`)\n    return value\n  }\n  const booleanField = (source: Record<string, unknown>, key: string, pathName: string) => {\n    const value = source[key]\n    if (typeof value !== \"boolean\") fail(`${pathName}.${key} must be a boolean`)\n    return value\n  }\n  const positiveIntegerField = (source: Record<string, unknown>, key: string, pathName: string) => {\n    const value = source[key]\n    if (!Number.isInteger(value) || (value as number) < 1) fail(`${pathName}.${key} must be a positive integer`)\n    return value as number\n  }\n  const stringArrayField = (source: Record<string, unknown>, key: string, pathName: string) => {\n    const value = source[key]\n    if (!Array.isArray(value) || value.length === 0) fail(`${pathName}.${key} must be a nonempty string array`)\n    const items = value as unknown[]\n    for (const [index, item] of items.entries()) {\n      if (typeof item !== \"string\" || !item.trim()) fail(`${pathName}.${key}[${index}] must be a nonempty string`)\n    }\n    return items as string[]\n  }\n  const root = object(contract, \"contract\")\n  const artifacts = object(root.artifacts, \"artifacts\")\n  const frontmatter = object(root.frontmatter, \"frontmatter\")\n  const script = object(root.script, \"script\")\n  const lifecycleRules = object(root.lifecycle_rules, \"lifecycle_rules\")\n  const acceptance = object(root.acceptance, \"acceptance\")\n  const downgrade = object(\n    lifecycleRules.script_newer_than_test_markdown_downgrades,\n    \"lifecycle_rules.script_newer_than_test_markdown_downgrades\",\n  )\n  if (root.schema_version !== 1) fail(\"schema_version must be 1\")\n  stringField(root, \"protocol\", \"contract\")\n  for (const key of [\n    \"test_markdown\",\n    \"script\",\n    \"project_runtime_dir\",\n    \"context_contract\",\n    \"acceptance\",\n    \"run_database\",\n    \"run_result_pattern\",\n    \"case_run_result_pattern\",\n    \"batch_run_result_pattern\",\n    \"case_run_screenshot_pattern\",\n  ]) {\n    stringField(artifacts, key, \"artifacts\")\n  }\n  for (const key of [\"run_result_pattern\", \"case_run_result_pattern\", \"batch_run_result_pattern\", \"case_run_screenshot_pattern\"]) {\n    try {\n      new RegExp(artifacts[key] as string)\n    } catch (error) {\n      const message = error instanceof Error ? error.message : String(error)\n      fail(`artifacts.${key} is not a valid regular expression: ${message}`)\n    }\n  }\n  stringArrayField(frontmatter, \"required_fields\", \"frontmatter\")\n  const lifecycle = stringArrayField(frontmatter, \"lifecycle\", \"frontmatter\")\n  for (const key of [\"status_field\", \"default_status\", \"test_points_field\", \"test_point_name_field\"]) {\n    stringField(frontmatter, key, \"frontmatter\")\n  }\n  if (!lifecycle.includes(frontmatter.default_status as string)) {\n    fail(\"frontmatter.default_status must be listed in frontmatter.lifecycle\")\n  }\n  stringField(script, \"required_export\", \"script\")\n  stringField(script, \"mark_point_callee\", \"script\")\n  const nameSources = stringArrayField(script, \"mark_point_name_sources\", \"script\")\n  positiveIntegerField(script, \"mark_point_argument_count\", \"script\")\n  const supportedNameSources = new Set([\"object_name_property_first_argument\"])\n  for (const source of nameSources) {\n    if (!supportedNameSources.has(source)) fail(`script.mark_point_name_sources contains unsupported source ${source}`)\n  }\n  stringArrayField(script, \"mark_point_required_properties\", \"script\")\n  stringArrayField(script, \"mark_point_optional_properties\", \"script\")\n  booleanField(script, \"mark_point_allow_unknown_properties\", \"script\")\n  stringField(script, \"context_contract_mark_point_pattern\", \"script\")\n  try {\n    new RegExp(script.context_contract_mark_point_pattern as string)\n  } catch (error) {\n    const message = error instanceof Error ? error.message : String(error)\n    fail(`script.context_contract_mark_point_pattern is not a valid regular expression: ${message}`)\n  }\n  stringField(downgrade, \"from\", \"lifecycle_rules.script_newer_than_test_markdown_downgrades\")\n  stringField(downgrade, \"to\", \"lifecycle_rules.script_newer_than_test_markdown_downgrades\")\n  booleanField(lifecycleRules, \"draft_run_must_not_update_acceptance\", \"lifecycle_rules\")\n  stringArrayField(acceptance, \"attention_statuses\", \"acceptance\")\n  booleanField(acceptance, \"project_relative\", \"acceptance\")\n  booleanField(acceptance, \"model_draft_run_updates_acceptance\", \"acceptance\")\n  if (!lifecycle.includes(downgrade.from as string)) {\n    fail(\"script downgrade source status must be listed in frontmatter.lifecycle\")\n  }\n  if (!lifecycle.includes(downgrade.to as string)) {\n    fail(\"script downgrade target status must be listed in frontmatter.lifecycle\")\n  }\n}\n\nfunction normalizeRelative(value: string) {\n  return value.split(path.sep).join(\"/\")\n}\n\nfunction assertContained(base: string, target: string, label: string) {\n  const relative = path.relative(base, target)\n  if (relative === \"\" || (!relative.startsWith(\"..\") && !path.isAbsolute(relative))) return\n  throw new Error(`${label} must stay inside the active project directory`)\n}\n\nfunction shouldSkipDirectory(base: string, target: string) {\n  const relative = normalizeRelative(path.relative(base, target))\n  const segments = relative.split(\"/\").filter(Boolean)\n  if (segments.some((segment) => skippedDirectoryNames.has(segment))) return true\n  return segments[0] === \".opencorvus\" && [\"r\", \"runtime\", \"worktrees\"].includes(segments[1] ?? \"\")\n}\n\nfunction isRunResult(contract: ProtocolContract, relativePath: string) {\n  return new RegExp(contract.artifacts.run_result_pattern).test(relativePath)\n}\n\nfunction isCaseRunResult(contract: ProtocolContract, relativePath: string) {\n  return new RegExp(contract.artifacts.case_run_result_pattern).test(relativePath)\n}\n\nfunction isBatchRunResult(contract: ProtocolContract, relativePath: string) {\n  return new RegExp(contract.artifacts.batch_run_result_pattern).test(relativePath)\n}\n\nfunction isCaseRunScreenshot(contract: ProtocolContract, relativePath: string) {\n  return new RegExp(contract.artifacts.case_run_screenshot_pattern).test(relativePath)\n}\n\nfunction uniqueSorted(values: Iterable<string>) {\n  return [...new Set(values)].sort((a, b) => a.localeCompare(b))\n}\n\nasync function collectFiles(base: string, root: string, maxFiles: number): Promise<FileEntry[]> {\n  const files: FileEntry[] = []\n  async function walk(current: string): Promise<void> {\n    if (files.length >= maxFiles) return\n    const entries = await readdir(current, { withFileTypes: true })\n    entries.sort((left, right) => left.name.localeCompare(right.name))\n    for (const entry of entries) {\n      if (files.length >= maxFiles) return\n      const target = path.join(current, entry.name)\n      if (entry.isDirectory()) {\n        if (!shouldSkipDirectory(base, target)) await walk(target)\n        continue\n      }\n      if (entry.isFile()) {\n        files.push({\n          absolute: target,\n          relative: normalizeRelative(path.relative(base, target)),\n        })\n      }\n    }\n  }\n  await walk(root)\n  return files\n}\n\nasync function collectPackageScripts(file: string, relativePath: string): Promise<PackageScript[]> {\n  const parsed = JSON.parse(await readFile(file, \"utf8\")) as { scripts?: Record<string, unknown> }\n  return Object.entries(parsed.scripts ?? {})\n    .filter(([, command]) => typeof command === \"string\" && testScriptPattern.test(command))\n    .map(([script, command]) => ({\n      package_json: relativePath,\n      script,\n      command: command as string,\n    }))\n}\n\nasync function readJsonFile(file: string): Promise<unknown> {\n  return JSON.parse(await readFile(file, \"utf8\"))\n}\n\nfunction normalizedAbsolute(file: string) {\n  return normalizeRelative(path.resolve(file))\n}\n\nfunction resultMatchesTestDirectory(projectDirectory: string, testDirectory: string, result: unknown) {\n  if (!result || typeof result !== \"object\") return false\n  const record = result as Record<string, unknown>\n  const expectedAbsolute = normalizedAbsolute(path.resolve(projectDirectory, testDirectory))\n  const expectedRelative = normalizeRelative(testDirectory)\n  const testPath = typeof record.testPath === \"string\" ? record.testPath : undefined\n  if (testPath) {\n    const normalizedTestPath = path.isAbsolute(testPath)\n      ? normalizedAbsolute(testPath)\n      : normalizeRelative(testPath)\n    if (normalizedTestPath === expectedAbsolute || normalizedTestPath === expectedRelative) return true\n  }\n  return false\n}\n\nasync function collectCaseRunResults(input: {\n  projectDirectory: string\n  testDirectory: string\n  files: FileEntry[]\n  contract: ProtocolContract\n}) {\n  const results: string[] = []\n  for (const file of input.files) {\n    if (!isCaseRunResult(input.contract, file.relative)) continue\n    const parsed = await readJsonFile(file.absolute).catch(() => undefined)\n    if (resultMatchesTestDirectory(input.projectDirectory, input.testDirectory, parsed)) results.push(file.relative)\n  }\n  return results.sort((left, right) => left.localeCompare(right))\n}\n\nfunction contractPath(directory: string, contractRelativePath: string) {\n  return normalizeRelative(path.posix.join(directory === \".\" ? \"\" : directory, contractRelativePath))\n}\n\nexport async function inspectOpenTestArtifacts(input: {\n  contract: ProtocolContract\n  projectDirectory: string\n  root: string\n  maxFiles: number\n}): Promise<OpenTestArtifactInventory> {\n  assertProtocolContract(input.contract)\n  const projectDirectory = path.resolve(input.projectDirectory)\n  const root = path.resolve(projectDirectory, input.root)\n  assertContained(projectDirectory, root, \"root\")\n  const rootInfo = await stat(root)\n  if (!rootInfo.isDirectory()) throw new Error(\"root must point to a directory\")\n\n  const files = await collectFiles(projectDirectory, root, input.maxFiles)\n  const relativeFiles = files.map((file) => file.relative)\n  const fileSet = new Set(relativeFiles)\n  const testMarkdownName = path.posix.basename(input.contract.artifacts.test_markdown)\n  const testDirectories = uniqueSorted(\n    relativeFiles.filter((file) => path.posix.basename(file) === testMarkdownName).map((file) => path.posix.dirname(file)),\n  )\n  const runResults = relativeFiles.filter((file) => isRunResult(input.contract, file)).sort((a, b) => a.localeCompare(b))\n  const caseRunResults = relativeFiles.filter((file) => isCaseRunResult(input.contract, file)).sort((a, b) => a.localeCompare(b))\n  const batchRunResults = relativeFiles.filter((file) => isBatchRunResult(input.contract, file)).sort((a, b) => a.localeCompare(b))\n  const screenshots = relativeFiles.filter((file) => isCaseRunScreenshot(input.contract, file)).sort((a, b) => a.localeCompare(b))\n  const runDatabases = relativeFiles\n    .filter((file) => file === input.contract.artifacts.run_database)\n    .sort((a, b) => a.localeCompare(b))\n  const acceptanceFiles = relativeFiles\n    .filter((file) => file === input.contract.artifacts.acceptance)\n    .sort((a, b) => a.localeCompare(b))\n  const contextFiles = relativeFiles\n    .filter((file) => file === input.contract.artifacts.context_contract)\n    .sort((a, b) => a.localeCompare(b))\n  const testConfigs = relativeFiles\n    .filter((file) => testConfigPattern.test(path.posix.basename(file)))\n    .sort((a, b) => a.localeCompare(b))\n\n  const packageScripts: PackageScript[] = []\n  for (const file of files) {\n    if (path.posix.basename(file.relative) !== \"package.json\") continue\n    packageScripts.push(...(await collectPackageScripts(file.absolute, file.relative)))\n  }\n\n  const testCases = await Promise.all(\n    testDirectories.map(async (directory) => {\n      const testMd = contractPath(directory, input.contract.artifacts.test_markdown)\n      const script = contractPath(directory, input.contract.artifacts.script)\n      return {\n        directory,\n        test_md: testMd,\n        script_ts: fileSet.has(script) ? script : null,\n        context_contract: fileSet.has(input.contract.artifacts.context_contract) ? input.contract.artifacts.context_contract : null,\n        acceptance: fileSet.has(input.contract.artifacts.acceptance) ? input.contract.artifacts.acceptance : null,\n        run_database: fileSet.has(input.contract.artifacts.run_database) ? input.contract.artifacts.run_database : null,\n        run_results: await collectCaseRunResults({\n          projectDirectory,\n          testDirectory: directory,\n          files,\n          contract: input.contract,\n        }),\n      }\n    }),\n  )\n\n  return {\n    schema_version: 1,\n    protocol: input.contract.protocol,\n    root: normalizeRelative(path.relative(projectDirectory, root)) || \".\",\n    scanned_file_count: relativeFiles.length,\n    scan_limit_reached: relativeFiles.length >= input.maxFiles,\n    test_cases: testCases,\n    context_files: contextFiles,\n    run_results: runResults,\n    case_run_results: caseRunResults,\n    batch_run_results: batchRunResults,\n    run_databases: runDatabases,\n    screenshots,\n    acceptance_files: acceptanceFiles,\n    test_configs: testConfigs,\n    package_scripts: packageScripts.sort((left, right) =>\n      `${left.package_json}:${left.script}`.localeCompare(`${right.package_json}:${right.script}`),\n    ),\n  }\n}\n\nfunction stringValue(value: unknown) {\n  return typeof value === \"string\" && value.trim().length > 0 ? value.trim() : undefined\n}\n\nfunction fieldValue(data: Record<string, unknown>, field: string) {\n  return data[field]\n}\n\nfunction parseYamlScalar(raw: string): unknown {\n  const value = raw.trim()\n  if (!value) return \"\"\n  const quote = value[0]\n  if ((quote === \"\\\"\" || quote === \"'\") && value.endsWith(quote)) {\n    return value.slice(1, -1)\n  }\n  return value\n}\n\nfunction parseYamlArray(lines: string[], start: number): { value: unknown[]; next: number } {\n  const value: unknown[] = []\n  let index = start\n  while (index < lines.length) {\n    const line = lines[index]!\n    if (!/^\\s+-\\s*/.test(line)) break\n    const item: Record<string, unknown> = {}\n    const rest = line.replace(/^\\s+-\\s*/, \"\")\n    const inline = rest.match(/^([A-Za-z0-9_-]+):\\s*(.*)$/)\n    if (inline) item[inline[1]!] = parseYamlScalar(inline[2] ?? \"\")\n    else if (rest.trim()) value.push(parseYamlScalar(rest))\n    index += 1\n\n    while (index < lines.length) {\n      const child = lines[index]!\n      const property = child.match(/^\\s{4,}([A-Za-z0-9_-]+):\\s*(.*)$/)\n      if (!property) break\n      item[property[1]!] = parseYamlScalar(property[2] ?? \"\")\n      index += 1\n    }\n\n    if (inline) value.push(item)\n  }\n  return { value, next: index }\n}\n\nfunction parseYamlFrontmatter(text: string, errors: string[], contract: ProtocolContract): Record<string, unknown> {\n  const lines = text.replace(/\\r\\n/g, \"\\n\").split(\"\\n\")\n  if (lines[0]?.trim() !== \"---\") {\n    errors.push(`${contract.artifacts.test_markdown} missing YAML frontmatter`)\n    return {}\n  }\n  const end = lines.findIndex((line, index) => index > 0 && line.trim() === \"---\")\n  if (end < 0) {\n    errors.push(`${contract.artifacts.test_markdown} missing closing YAML frontmatter delimiter`)\n    return {}\n  }\n  const data: Record<string, unknown> = {}\n  const frontmatterLines = lines.slice(1, end)\n  let index = 0\n  while (index < frontmatterLines.length) {\n    const line = frontmatterLines[index]!\n    if (!line.trim() || line.trimStart().startsWith(\"#\")) {\n      index += 1\n      continue\n    }\n    const field = line.match(/^([A-Za-z0-9_-]+):\\s*(.*)$/)\n    if (!field) {\n      errors.push(`${contract.artifacts.test_markdown} frontmatter line is not supported: ${line.trim()}`)\n      index += 1\n      continue\n    }\n    const key = field[1]!\n    const raw = field[2] ?? \"\"\n    if (raw.trim()) {\n      data[key] = parseYamlScalar(raw)\n      index += 1\n      continue\n    }\n    const array = parseYamlArray(frontmatterLines, index + 1)\n    data[key] = array.value\n    index = array.next\n  }\n  return data\n}\n\nfunction parseTestMarkdown(contract: ProtocolContract, text: string, errors: string[]) {\n  const data = parseYamlFrontmatter(text, errors, contract)\n  for (const field of contract.frontmatter.required_fields) {\n    const value = fieldValue(data, field)\n    if (value === undefined) {\n      errors.push(`${contract.artifacts.test_markdown} frontmatter missing ${field}`)\n      continue\n    }\n    if (field !== contract.frontmatter.test_points_field && typeof value === \"string\" && !value.trim()) {\n      errors.push(`${contract.artifacts.test_markdown} frontmatter ${field} must be nonempty`)\n    }\n  }\n  const status = stringValue(fieldValue(data, contract.frontmatter.status_field))\n  const declaredStatus =\n    status === undefined\n      ? contract.frontmatter.default_status\n      : contract.frontmatter.lifecycle.includes(status)\n        ? status\n        : null\n  if (!declaredStatus) {\n    errors.push(\n      `${contract.artifacts.test_markdown} frontmatter ${contract.frontmatter.status_field} must be ${contract.frontmatter.lifecycle.join(\", \")}`,\n    )\n  }\n  const rawTestPoints = fieldValue(data, contract.frontmatter.test_points_field)\n  if (!Array.isArray(rawTestPoints) || rawTestPoints.length === 0) {\n    errors.push(`${contract.artifacts.test_markdown} frontmatter missing nonempty ${contract.frontmatter.test_points_field}`)\n    return { status: declaredStatus, testPoints: [] }\n  }\n  const testPoints: string[] = []\n  for (const [index, point] of rawTestPoints.entries()) {\n    if (!point || typeof point !== \"object\") {\n      errors.push(`${contract.frontmatter.test_points_field}[${index}] must be an object`)\n      continue\n    }\n    const name = stringValue((point as Record<string, unknown>)[contract.frontmatter.test_point_name_field])\n    if (!name) {\n      errors.push(`${contract.frontmatter.test_points_field}[${index}] missing ${contract.frontmatter.test_point_name_field}`)\n      continue\n    }\n    testPoints.push(name)\n  }\n  for (const name of duplicateValues(testPoints)) {\n    errors.push(`${contract.frontmatter.test_points_field} duplicate ${contract.frontmatter.test_point_name_field} \"${name}\"`)\n  }\n  return { status: declaredStatus, testPoints }\n}\n\nfunction nodeModifiers(node: ts.Node) {\n  const modifiers = (node as ts.Node & { modifiers?: readonly ts.ModifierLike[] }).modifiers\n  return modifiers ? Array.from(modifiers) : []\n}\n\nfunction hasExportModifier(node: ts.Node) {\n  return nodeModifiers(node).some((modifier) => modifier.kind === ts.SyntaxKind.ExportKeyword)\n}\n\nfunction exportedAliasNames(source: ts.SourceFile, exportName: string) {\n  const aliases = new Set<string>()\n  for (const statement of source.statements) {\n    if (!ts.isExportDeclaration(statement) || !statement.exportClause) continue\n    if (!ts.isNamedExports(statement.exportClause)) continue\n    for (const element of statement.exportClause.elements) {\n      const exported = element.name.text\n      if (exported !== exportName) continue\n      aliases.add(element.propertyName?.text ?? element.name.text)\n    }\n  }\n  return aliases\n}\n\nfunction exportedStepRoots(source: ts.SourceFile, exportName: string): ts.Node[] {\n  const exportedLocals = exportedAliasNames(source, exportName)\n  const names = new Set([exportName, ...exportedLocals])\n  const roots: ts.Node[] = []\n  for (const statement of source.statements) {\n    if (ts.isFunctionDeclaration(statement) && statement.name && names.has(statement.name.text)) {\n      if (hasExportModifier(statement) || exportedLocals.has(statement.name.text)) {\n        roots.push(statement.body ?? statement)\n      }\n      continue\n    }\n    if (!ts.isVariableStatement(statement)) continue\n    const exported = hasExportModifier(statement)\n    for (const declaration of statement.declarationList.declarations) {\n      if (!ts.isIdentifier(declaration.name) || !names.has(declaration.name.text)) continue\n      if (exported || exportedLocals.has(declaration.name.text)) roots.push(declaration.initializer ?? declaration)\n    }\n  }\n  return roots\n}\n\nfunction dottedExpressionName(node: ts.Expression): string | undefined {\n  if (ts.isIdentifier(node)) return node.text\n  if (ts.isPropertyAccessExpression(node)) {\n    const left = dottedExpressionName(node.expression)\n    return left ? `${left}.${node.name.text}` : undefined\n  }\n  return undefined\n}\n\nfunction literalStringFromExpression(node: ts.Expression): string | undefined {\n  if (ts.isStringLiteral(node) || ts.isNoSubstitutionTemplateLiteral(node)) return node.text\n  return undefined\n}\n\nfunction literalNameFromObjectExpression(node: ts.Expression, propertyName: string): string | undefined {\n  if (!ts.isObjectLiteralExpression(node)) return undefined\n  for (const property of node.properties) {\n    if (!ts.isPropertyAssignment(property)) continue\n    const name =\n      ts.isIdentifier(property.name) || ts.isStringLiteral(property.name)\n        ? property.name.text\n        : undefined\n    if (name !== propertyName) continue\n    return literalStringFromExpression(property.initializer)\n  }\n  return undefined\n}\n\nfunction objectLiteralPropertyNames(node: ts.Expression) {\n  if (!ts.isObjectLiteralExpression(node)) return undefined\n  return new Set(\n    node.properties\n      .map((property) => {\n        if (!ts.isPropertyAssignment(property)) return undefined\n        if (ts.isIdentifier(property.name) || ts.isStringLiteral(property.name)) return property.name.text\n        return undefined\n      })\n      .filter((name): name is string => Boolean(name)),\n  )\n}\n\nfunction markPointNameFromCall(contract: ProtocolContract, node: ts.CallExpression): string | undefined {\n  const first = node.arguments[0]\n  if (!first) return undefined\n  if (contract.script.mark_point_name_sources.includes(\"object_name_property_first_argument\")) {\n    const name = literalNameFromObjectExpression(first, contract.frontmatter.test_point_name_field)\n    if (name) return name\n  }\n  return undefined\n}\n\nfunction validateMarkPointCallShape(contract: ProtocolContract, node: ts.CallExpression, errors: string[]) {\n  const first = node.arguments[0]\n  if (node.arguments.length !== contract.script.mark_point_argument_count) {\n    errors.push(\n      `${contract.artifacts.script} ${contract.script.mark_point_callee} calls must use exactly ${contract.script.mark_point_argument_count} argument`,\n    )\n  }\n  const properties = first ? objectLiteralPropertyNames(first) : undefined\n  if (!properties) {\n    errors.push(`${contract.artifacts.script} ${contract.script.mark_point_callee} calls must use an object argument`)\n    return\n  }\n  for (const property of contract.script.mark_point_required_properties) {\n    if (!properties.has(property)) {\n      errors.push(`${contract.artifacts.script} ${contract.script.mark_point_callee} object argument missing ${property}`)\n    }\n  }\n  if (!contract.script.mark_point_allow_unknown_properties) {\n    const allowed = new Set([...contract.script.mark_point_required_properties, ...contract.script.mark_point_optional_properties])\n    for (const property of [...properties].sort((left, right) => left.localeCompare(right))) {\n      if (!allowed.has(property)) {\n        errors.push(`${contract.artifacts.script} ${contract.script.mark_point_callee} object argument has unknown property ${property}`)\n      }\n    }\n  }\n}\n\nfunction parseScript(contract: ProtocolContract, text: string, errors: string[]) {\n  const source = ts.createSourceFile(contract.artifacts.script, text, ts.ScriptTarget.Latest, true, ts.ScriptKind.TS)\n  const markPoints: string[] = []\n  const roots = exportedStepRoots(source, contract.script.required_export)\n  if (roots.length === 0) {\n    errors.push(`${contract.artifacts.script} missing exported ${contract.script.required_export}`)\n    return markPoints\n  }\n  const visit = (node: ts.Node) => {\n    if (ts.isCallExpression(node) && dottedExpressionName(node.expression) === contract.script.mark_point_callee) {\n      validateMarkPointCallShape(contract, node, errors)\n      const name = markPointNameFromCall(contract, node)\n      if (name) markPoints.push(name)\n      else {\n        errors.push(`${contract.artifacts.script} ${contract.script.mark_point_callee} calls must use a literal test point name`)\n      }\n    }\n    ts.forEachChild(node, visit)\n  }\n  for (const root of roots) visit(root)\n  return markPoints\n}\n\nfunction validateContextContract(contract: ProtocolContract, text: string, errors: string[]) {\n  const pattern = new RegExp(contract.script.context_contract_mark_point_pattern, \"m\")\n  if (!pattern.test(text)) {\n    errors.push(`${contract.artifacts.context_contract} missing ${contract.script.mark_point_callee} object signature`)\n  }\n}\n\nfunction duplicateValues(values: string[]) {\n  const seen = new Set<string>()\n  const duplicates = new Set<string>()\n  for (const value of values) {\n    if (seen.has(value)) duplicates.add(value)\n    seen.add(value)\n  }\n  return [...duplicates].sort((a, b) => a.localeCompare(b))\n}\n\nasync function fileExists(file: string) {\n  const info = await stat(file).catch((error: NodeJS.ErrnoException) => {\n    if (error.code === \"ENOENT\") return undefined\n    throw error\n  })\n  return info?.isFile() ? info : undefined\n}\n\nexport async function validateOpenTestCase(input: {\n  contract: ProtocolContract\n  projectDirectory: string\n  testDirectory: string\n  maxFiles?: number\n}): Promise<OpenTestValidationResult> {\n  assertProtocolContract(input.contract)\n  const projectDirectory = path.resolve(input.projectDirectory)\n  const testDirectory = path.resolve(projectDirectory, input.testDirectory)\n  assertContained(projectDirectory, testDirectory, \"testDirectory\")\n  const relativeDirectory = normalizeRelative(path.relative(projectDirectory, testDirectory)) || \".\"\n  const errors: string[] = []\n\n  const testMd = path.join(testDirectory, input.contract.artifacts.test_markdown)\n  const script = path.join(testDirectory, input.contract.artifacts.script)\n  const contextContract = path.join(projectDirectory, input.contract.artifacts.context_contract)\n  const acceptance = path.join(projectDirectory, input.contract.artifacts.acceptance)\n  const runDatabase = path.join(projectDirectory, input.contract.artifacts.run_database)\n\n  const [testMdInfo, scriptInfo, contextInfo, acceptanceInfo, runDatabaseInfo] = await Promise.all([\n    fileExists(testMd),\n    fileExists(script),\n    fileExists(contextContract),\n    fileExists(acceptance),\n    fileExists(runDatabase),\n  ])\n  if (!testMdInfo) errors.push(`missing ${input.contract.artifacts.test_markdown}`)\n  if (!scriptInfo) errors.push(`missing ${input.contract.artifacts.script}`)\n  if (!contextInfo) errors.push(`missing ${input.contract.artifacts.context_contract}`)\n  if (!acceptanceInfo) errors.push(`missing ${input.contract.artifacts.acceptance}`)\n  if (!runDatabaseInfo) errors.push(`missing ${input.contract.artifacts.run_database}`)\n\n  const testMdRelative = normalizeRelative(path.relative(projectDirectory, testMd))\n  const scriptRelative = scriptInfo ? normalizeRelative(path.relative(projectDirectory, script)) : null\n  const contextRelative = contextInfo ? normalizeRelative(path.relative(projectDirectory, contextContract)) : null\n  const acceptanceRelative = acceptanceInfo ? normalizeRelative(path.relative(projectDirectory, acceptance)) : null\n  const runDatabaseRelative = runDatabaseInfo ? normalizeRelative(path.relative(projectDirectory, runDatabase)) : null\n\n  const testContract = testMdInfo\n    ? parseTestMarkdown(input.contract, await readFile(testMd, \"utf8\"), errors)\n    : { status: null as string | null, testPoints: [] }\n  const markPoints = scriptInfo ? parseScript(input.contract, await readFile(script, \"utf8\"), errors) : []\n  if (contextInfo) validateContextContract(input.contract, await readFile(contextContract, \"utf8\"), errors)\n\n  for (const name of duplicateValues(markPoints)) errors.push(`${input.contract.artifacts.script} duplicate ${input.contract.script.mark_point_callee} \"${name}\"`)\n  const declared = new Set(testContract.testPoints)\n  const actual = new Set(markPoints)\n  for (const name of [...declared].sort((a, b) => a.localeCompare(b))) {\n    if (!actual.has(name)) errors.push(`${input.contract.frontmatter.test_points_field} \"${name}\" has no matching ${input.contract.script.mark_point_callee}`)\n  }\n  for (const name of [...actual].sort((a, b) => a.localeCompare(b))) {\n    if (!declared.has(name)) errors.push(`${input.contract.script.mark_point_callee} \"${name}\" is not declared in ${input.contract.frontmatter.test_points_field}`)\n  }\n\n  const inventory = await inspectOpenTestArtifacts({\n    contract: input.contract,\n    projectDirectory,\n    root: \".\",\n    maxFiles: input.maxFiles ?? 1000,\n  })\n  const matchingCase = inventory.test_cases.find((testCase) => testCase.directory === relativeDirectory)\n  const runResults = matchingCase?.run_results ?? []\n  if (runResults.length === 0) errors.push(`missing ${input.contract.artifacts.run_result_pattern} run result artifact`)\n  const runResultInfos = await Promise.all(runResults.map((file) => stat(path.join(projectDirectory, file))))\n  const latestRunResultMtime = runResultInfos.reduce((latest, info) => Math.max(latest, info.mtimeMs), 0)\n  const scriptModifiedAfterTestMarkdown = Boolean(testMdInfo && scriptInfo && scriptInfo.mtimeMs > testMdInfo.mtimeMs)\n  const downgradeRule = input.contract.lifecycle_rules.script_newer_than_test_markdown_downgrades\n  const activeDowngradedToDraft = testContract.status === downgradeRule.from && scriptModifiedAfterTestMarkdown\n  const effectiveStatus = activeDowngradedToDraft ? downgradeRule.to : testContract.status\n  const draftRunUpdatesAcceptance = Boolean(\n    input.contract.lifecycle_rules.draft_run_must_not_update_acceptance &&\n      effectiveStatus === downgradeRule.to &&\n      acceptanceInfo &&\n      latestRunResultMtime > 0 &&\n      acceptanceInfo.mtimeMs >= latestRunResultMtime,\n  )\n  if (draftRunUpdatesAcceptance) {\n    errors.push(`model ${downgradeRule.to} run must not update ${input.contract.artifacts.acceptance}`)\n  }\n\n  return {\n    schema_version: 1,\n    protocol: input.contract.protocol,\n    directory: relativeDirectory,\n    valid: errors.length === 0,\n    errors,\n    artifacts: {\n      test_md: testMdRelative,\n      script_ts: scriptRelative,\n      context_contract: contextRelative,\n      acceptance: acceptanceRelative,\n      run_database: runDatabaseRelative,\n      run_results: runResults,\n    },\n    lifecycle: {\n      declared_status: testContract.status,\n      effective_status: effectiveStatus,\n      script_modified_after_test_md: scriptModifiedAfterTestMarkdown,\n      active_downgraded_to_draft: activeDowngradedToDraft,\n      draft_run_updates_acceptance: draftRunUpdatesAcceptance,\n    },\n    test_points: testContract.testPoints,\n    mark_points: markPoints,\n  }\n}\n\n// Payload embeds this source as text; the default export is not a protocol entrypoint.\nexport default null\n",
      "README.md": textPayload(payload_opentest_README_md),
      "selector.md": textPayload(payload_opentest_selector_md),
      "tools/opentest-protocol-engine.ts": "import { tool } from \"@opencorvus-ai/plugin\"\nimport contractSource from \"../protocol-engine/opentest-contract.json\" with { type: \"text\" }\nimport {\n  inspectOpenTestArtifacts,\n  parseProtocolContract,\n  validateOpenTestCase,\n} from \"../protocol-engine/opentest-protocol-engine\"\n\nconst contractText = contractSource as unknown as string\n\nexport default tool({\n  description:\n    \"Validate OpenTest artifacts by dynamically parsing the external opentest protocol contract.\",\n  args: {\n    mode: tool.schema.enum([\"contract\", \"inventory\", \"validate\"]),\n    root: tool.schema.string().min(1).optional().describe(\"Project-relative root for inventory mode.\"),\n    test_directory: tool.schema\n      .string()\n      .min(1)\n      .optional()\n      .describe(\"Project-relative OpenTest case directory for validate mode.\"),\n    max_files: tool.schema.number().int().min(1).max(2000).optional().describe(\"Maximum number of files to inspect.\"),\n  },\n  async execute(args, context) {\n    const contract = parseProtocolContract(contractText)\n    if (args.mode === \"contract\") {\n      return JSON.stringify({ schema_version: 1, contract }, null, 2)\n    }\n    if (args.mode === \"inventory\") {\n      if (!args.root) throw new Error(\"root is required for inventory mode\")\n      return JSON.stringify(\n        await inspectOpenTestArtifacts({\n          contract,\n          projectDirectory: context.directory,\n          root: args.root,\n          maxFiles: args.max_files ?? 1000,\n        }),\n        null,\n        2,\n      )\n    }\n    if (!args.test_directory) throw new Error(\"test_directory is required for validate mode\")\n    return JSON.stringify(\n      await validateOpenTestCase({\n        contract,\n        projectDirectory: context.directory,\n        testDirectory: args.test_directory,\n        maxFiles: args.max_files ?? 1000,\n      }),\n      null,\n      2,\n    )\n  },\n})\n",
      "tools/opentest-runner.ts": "import { spawn, type ChildProcess } from \"node:child_process\"\nimport { mkdir, stat, writeFile } from \"node:fs/promises\"\nimport path from \"node:path\"\nimport { tool } from \"@opencorvus-ai/plugin\"\nimport contractSource from \"../protocol-engine/opentest-contract.json\" with { type: \"text\" }\nimport { parseProtocolContract, validateOpenTestCase } from \"../protocol-engine/opentest-protocol-engine\"\n\nconst contractText = contractSource as unknown as string\nconst decoder = new TextDecoder()\nconst TERMINATE_GRACE_MS = 2_000\nconst WINDOWS_POWERSHELL_CLEANUP_TIMEOUT_MS = 5_000\n\ntype CommandRun = {\n  startedAt: string\n  finishedAt: string\n  durationMs: number\n  exitCode: number | null\n  signal: NodeJS.Signals | null\n  timedOut: boolean\n  aborted: boolean\n  error: string | null\n  stdout: string\n  stderr: string\n}\n\nfunction normalizeRelative(value: string) {\n  return value.split(path.sep).join(\"/\")\n}\n\nfunction assertContained(base: string, target: string, label: string) {\n  const relative = path.relative(base, target)\n  if (relative === \"\" || (!relative.startsWith(\"..\") && !path.isAbsolute(relative))) return\n  throw new Error(`${label} must stay inside the active project directory`)\n}\n\nfunction resolveProjectPath(projectDirectory: string, projectRelative: string, label: string) {\n  const value = projectRelative.trim()\n  if (!value) throw new Error(`${label} must be a nonempty project-relative path`)\n  if (path.isAbsolute(value)) throw new Error(`${label} must be project-relative`)\n  const target = path.resolve(projectDirectory, value)\n  assertContained(projectDirectory, target, label)\n  return target\n}\n\nfunction projectRelativePath(projectDirectory: string, target: string) {\n  return normalizeRelative(path.relative(projectDirectory, target)) || \".\"\n}\n\nfunction assertRunID(runID: string) {\n  if (!/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/.test(runID)) {\n    throw new Error(\n      \"run_id must start with an ASCII letter or digit and contain only ASCII letters, digits, '.', '_', or '-'\",\n    )\n  }\n}\n\nfunction caseIDForTestDirectory(testDirectory: string) {\n  const normalized = normalizeRelative(path.normalize(testDirectory)).replace(/^\\.\\//, \"\")\n  const caseID = normalized.replace(/[^A-Za-z0-9._-]+/g, \"_\").replace(/^_+|_+$/g, \"\")\n  return caseID || \"root\"\n}\n\nfunction statusFromRun(run: CommandRun) {\n  if (run.timedOut) return \"timed_out\"\n  if (run.aborted) return \"aborted\"\n  if (run.error) return \"failed_to_start\"\n  return run.exitCode === 0 ? \"passed\" : \"failed\"\n}\n\nfunction processGroupIsRunning(pid: number) {\n  try {\n    process.kill(-pid, 0)\n    return true\n  } catch (error) {\n    const code = (error as NodeJS.ErrnoException).code\n    if (code === \"ESRCH\") return false\n    return code === \"EPERM\"\n  }\n}\n\nfunction signalProcessGroup(pid: number, signal: NodeJS.Signals) {\n  try {\n    process.kill(-pid, signal)\n  } catch (error) {\n    if ((error as NodeJS.ErrnoException).code === \"ESRCH\") return\n    throw error\n  }\n}\n\nasync function waitForProcessGroupExit(pid: number, timeoutMs: number) {\n  const deadline = Date.now() + timeoutMs\n  while (Date.now() < deadline) {\n    if (!processGroupIsRunning(pid)) return true\n    await new Promise((resolve) => setTimeout(resolve, 25))\n  }\n  return !processGroupIsRunning(pid)\n}\n\nasync function killWindowsProcessTree(pid: number, includeRoot: boolean) {\n  const script = `\n$ErrorActionPreference = \"Stop\"\n$root = ${pid}\n$includeRoot = ${includeRoot ? \"$true\" : \"$false\"}\n$processes = @(Get-CimInstance Win32_Process | Select-Object ProcessId, ParentProcessId)\n$children = @{}\nforeach ($process in $processes) {\n  $parent = [int]$process.ParentProcessId\n  if (-not $children.ContainsKey($parent)) {\n    $children[$parent] = New-Object System.Collections.Generic.List[int]\n  }\n  $children[$parent].Add([int]$process.ProcessId)\n}\n$queue = New-Object System.Collections.Generic.Queue[int]\n$seen = New-Object System.Collections.Generic.HashSet[int]\n$targets = New-Object System.Collections.Generic.List[int]\n$queue.Enqueue([int]$root)\nwhile ($queue.Count -gt 0) {\n  $current = $queue.Dequeue()\n  if (-not $seen.Add($current)) { continue }\n  if (-not $children.ContainsKey($current)) { continue }\n  foreach ($child in $children[$current]) {\n    $targets.Add([int]$child)\n    $queue.Enqueue([int]$child)\n  }\n}\nif ($includeRoot) {\n  foreach ($process in $processes) {\n    if ([int]$process.ProcessId -eq [int]$root) {\n      $targets.Add([int]$root)\n      break\n    }\n  }\n}\n$unique = @($targets | Sort-Object -Unique)\nif ($unique.Count -gt 0) {\n  foreach ($target in $unique) {\n    Stop-Process -Id $target -Force -ErrorAction SilentlyContinue\n  }\n  $unique | ConvertTo-Json -Compress\n} else {\n  Write-Output \"[]\"\n}\n`\n  const stopped = await runWindowsPowerShellForProcessIDs(script, `OpenTest command process tree ${pid}`)\n  await waitForWindowsProcessIDsExit(stopped, TERMINATE_GRACE_MS, `OpenTest command process tree ${pid}`)\n}\n\nasync function runWindowsPowerShellForProcessIDs(script: string, label: string): Promise<number[]> {\n  const output = await new Promise<string>((resolve, reject) => {\n    const runner = spawn(\"powershell.exe\", [\"-NoProfile\", \"-ExecutionPolicy\", \"Bypass\", \"-Command\", script], {\n      stdio: [\"ignore\", \"pipe\", \"pipe\"],\n      windowsHide: true,\n    })\n    let settled = false\n    let stdout = \"\"\n    let stderr = \"\"\n    const settle = (fn: () => void) => {\n      if (settled) return\n      settled = true\n      clearTimeout(timer)\n      fn()\n    }\n    const timer = setTimeout(() => {\n      runner.kill()\n      settle(() =>\n        reject(new Error(`${label} PowerShell cleanup timed out after ${WINDOWS_POWERSHELL_CLEANUP_TIMEOUT_MS}ms`)),\n      )\n    }, WINDOWS_POWERSHELL_CLEANUP_TIMEOUT_MS)\n    if (typeof timer.unref === \"function\") timer.unref()\n    runner.stdout.setEncoding(\"utf8\")\n    runner.stderr.setEncoding(\"utf8\")\n    runner.stdout.on(\"data\", (chunk) => {\n      stdout += chunk\n    })\n    runner.stderr.on(\"data\", (chunk) => {\n      stderr += chunk\n    })\n    runner.once(\"exit\", (code, signal) => {\n      settle(() => {\n        if (code === 0) resolve(stdout)\n        else reject(new Error(`${label} PowerShell cleanup exited with ${signal ?? code}: ${stderr.trim()}`))\n      })\n    })\n    runner.once(\"error\", (error) => settle(() => reject(error)))\n  })\n  const trimmed = output.trim()\n  if (!trimmed) return []\n  const parsed = JSON.parse(trimmed) as number | number[]\n  return (Array.isArray(parsed) ? parsed : [parsed]).filter((value) => Number.isInteger(value) && value > 0)\n}\n\nasync function waitForWindowsProcessIDsExit(pids: number[], timeoutMs: number, label: string): Promise<void> {\n  const deadline = Date.now() + timeoutMs\n  while (Date.now() < deadline) {\n    if (pids.every((pid) => !processIDIsRunning(pid))) return\n    await new Promise((resolve) => setTimeout(resolve, 25))\n  }\n  const live = pids.filter(processIDIsRunning)\n  if (live.length > 0) throw new Error(`${label} did not exit after cleanup: ${live.join(\", \")}`)\n}\n\nfunction processIDIsRunning(pid: number) {\n  try {\n    process.kill(pid, 0)\n    return true\n  } catch (error) {\n    const code = (error as NodeJS.ErrnoException).code\n    if (code === \"ESRCH\") return false\n    return code === \"EPERM\"\n  }\n}\n\nasync function terminateProcess(processHandle: ChildProcess) {\n  const pid = processHandle.pid\n  if (!pid) return\n  if (process.platform === \"win32\") {\n    await killWindowsProcessTree(pid, processHandle.exitCode === null && processHandle.signalCode === null)\n    return\n  }\n  if (!processGroupIsRunning(pid)) return\n  signalProcessGroup(pid, \"SIGTERM\")\n  if (await waitForProcessGroupExit(pid, TERMINATE_GRACE_MS)) return\n  signalProcessGroup(pid, \"SIGKILL\")\n  if (await waitForProcessGroupExit(pid, TERMINATE_GRACE_MS)) return\n  throw new Error(`OpenTest command process group ${pid} did not exit after SIGKILL`)\n}\n\nasync function runCommand(input: {\n  executable: string\n  args: string[]\n  cwd: string\n  inactiveTimeoutMs: number\n  abort: AbortSignal\n}): Promise<CommandRun> {\n  if (input.abort.aborted) throw new Error(\"OpenTest runner command was aborted before spawn.\")\n  const startedAt = new Date()\n  const startedMs = Date.now()\n  let stdout = \"\"\n  let stderr = \"\"\n  let timedOut = false\n  let aborted = false\n  let inactivityTimer: ReturnType<typeof setTimeout> | undefined\n  let termination: Promise<void> | undefined\n  let terminationError: unknown\n  let rejectTerminationFailure: (error: unknown) => void = () => {}\n  const terminationFailure = new Promise<never>((_resolve, reject) => {\n    rejectTerminationFailure = reject\n  })\n\n  const child = spawn(input.executable, input.args, {\n    cwd: input.cwd,\n    env: process.env,\n    shell: false,\n    stdio: [\"ignore\", \"pipe\", \"pipe\"],\n    windowsHide: true,\n    detached: process.platform !== \"win32\",\n  })\n\n  const clearInactivityTimer = () => {\n    if (inactivityTimer) clearTimeout(inactivityTimer)\n    inactivityTimer = undefined\n  }\n\n  const resetInactivityTimer = () => {\n    clearInactivityTimer()\n    inactivityTimer = setTimeout(() => {\n      timedOut = true\n      requestTermination()\n    }, input.inactiveTimeoutMs)\n  }\n\n  const onAbort = () => {\n    aborted = true\n    requestTermination()\n  }\n\n  const requestTermination = () => {\n    if (termination) return\n    termination = terminateProcess(child).catch((error) => {\n      terminationError = error\n      rejectTerminationFailure(error)\n    })\n  }\n\n  let completion: {\n    exitCode: number | null\n    signal: NodeJS.Signals | null\n    error: string | null\n  }\n  try {\n    input.abort.addEventListener(\"abort\", onAbort, { once: true })\n    if (input.abort.aborted) onAbort()\n    resetInactivityTimer()\n\n    const completionPromise = new Promise<{\n      exitCode: number | null\n      signal: NodeJS.Signals | null\n      error: string | null\n    }>((resolve) => {\n      let settled = false\n      const settle = (result: { exitCode: number | null; signal: NodeJS.Signals | null; error: string | null }) => {\n        if (settled) return\n        settled = true\n        resolve(result)\n      }\n\n      child.stdout?.on(\"data\", (chunk: Buffer) => {\n        stdout += decoder.decode(chunk)\n        resetInactivityTimer()\n      })\n      child.stderr?.on(\"data\", (chunk: Buffer) => {\n        stderr += decoder.decode(chunk)\n        resetInactivityTimer()\n      })\n      child.once(\"error\", (error) => {\n        settle({ exitCode: null, signal: null, error: error instanceof Error ? error.message : String(error) })\n      })\n      child.once(\"close\", (exitCode, signal) => {\n        settle({ exitCode, signal, error: null })\n      })\n    })\n    completion = await Promise.race([completionPromise, terminationFailure])\n  } finally {\n    clearInactivityTimer()\n    input.abort.removeEventListener(\"abort\", onAbort)\n  }\n\n  if (!termination) termination = terminateProcess(child)\n  if (termination) await termination\n  if (terminationError) throw terminationError\n\n  return {\n    startedAt: startedAt.toISOString(),\n    finishedAt: new Date().toISOString(),\n    durationMs: Date.now() - startedMs,\n    exitCode: completion.exitCode,\n    signal: completion.signal,\n    timedOut,\n    aborted,\n    error: completion.error,\n    stdout,\n    stderr,\n  }\n}\n\nasync function assertDirectory(directory: string, label: string) {\n  const info = await stat(directory).catch((error: NodeJS.ErrnoException) => {\n    if (error.code === \"ENOENT\") return undefined\n    throw error\n  })\n  if (!info?.isDirectory()) throw new Error(`${label} must point to an existing directory`)\n}\n\nexport default tool({\n  description:\n    \"Run an explicitly supplied OpenTest command, write protocol run artifacts, and validate the case through the external OpenTest contract.\",\n  args: {\n    run_id: tool.schema.string().min(1).describe(\"Explicit OpenTest run identifier.\"),\n    working_directory: tool.schema.string().min(1).describe(\"Project-relative working directory for the command.\"),\n    executable: tool.schema.string().min(1).describe(\"Executable to spawn directly without shell inference.\"),\n    args: tool.schema.array(tool.schema.string()).describe(\"Argument vector passed to the executable.\"),\n    inactive_timeout_ms: tool.schema\n      .number()\n      .int()\n      .min(1000)\n      .max(1_800_000)\n      .describe(\"Timeout measured from the last stdout or stderr activity.\"),\n    test_directory: tool.schema\n      .string()\n      .min(1)\n      .optional()\n      .describe(\"Project-relative OpenTest case directory for case result and validation.\"),\n    max_files: tool.schema\n      .number()\n      .int()\n      .min(1)\n      .max(2000)\n      .optional()\n      .describe(\"Maximum files for post-run validation.\"),\n  },\n  async execute(args, context) {\n    assertRunID(args.run_id)\n    const contract = parseProtocolContract(contractText)\n    const projectDirectory = path.resolve(context.directory)\n    const workingDirectory = resolveProjectPath(projectDirectory, args.working_directory, \"working_directory\")\n    await assertDirectory(workingDirectory, \"working_directory\")\n\n    const testDirectory = args.test_directory\n      ? resolveProjectPath(projectDirectory, args.test_directory, \"test_directory\")\n      : undefined\n    if (testDirectory) await assertDirectory(testDirectory, \"test_directory\")\n\n    const run = await runCommand({\n      executable: args.executable,\n      args: args.args,\n      cwd: workingDirectory,\n      inactiveTimeoutMs: args.inactive_timeout_ms,\n      abort: context.abort,\n    })\n    const status = statusFromRun(run)\n    const command = {\n      executable: args.executable,\n      args: args.args,\n      working_directory: projectRelativePath(projectDirectory, workingDirectory),\n    }\n    const runRoot = path.join(projectDirectory, contract.artifacts.project_runtime_dir, \"runs\", args.run_id)\n    const batchResultPath = path.join(runRoot, \"result.json\")\n    const batchResult = {\n      schema_version: 1,\n      protocol: contract.protocol,\n      run_id: args.run_id,\n      status,\n      command,\n      exit_code: run.exitCode,\n      signal: run.signal,\n      timed_out: run.timedOut,\n      aborted: run.aborted,\n      error: run.error,\n      started_at: run.startedAt,\n      finished_at: run.finishedAt,\n      duration_ms: run.durationMs,\n      stdout: run.stdout,\n      stderr: run.stderr,\n    }\n    await mkdir(path.dirname(batchResultPath), { recursive: true })\n    await writeFile(batchResultPath, `${JSON.stringify(batchResult, null, 2)}\\n`)\n\n    let caseResultPath: string | null = null\n    let validation: Awaited<ReturnType<typeof validateOpenTestCase>> | null = null\n    if (testDirectory && args.test_directory) {\n      const testPath = projectRelativePath(projectDirectory, testDirectory)\n      caseResultPath = path.join(runRoot, caseIDForTestDirectory(testPath), \"result.json\")\n      const caseResult = {\n        ...batchResult,\n        testPath,\n      }\n      await mkdir(path.dirname(caseResultPath), { recursive: true })\n      await writeFile(caseResultPath, `${JSON.stringify(caseResult, null, 2)}\\n`)\n      validation = await validateOpenTestCase({\n        contract,\n        projectDirectory,\n        testDirectory: testPath,\n        maxFiles: args.max_files ?? 1000,\n      })\n    }\n\n    return JSON.stringify(\n      {\n        schema_version: 1,\n        protocol: contract.protocol,\n        run_id: args.run_id,\n        status,\n        command,\n        artifacts: {\n          batch_result: projectRelativePath(projectDirectory, batchResultPath),\n          case_result: caseResultPath ? projectRelativePath(projectDirectory, caseResultPath) : null,\n        },\n        validation,\n      },\n      null,\n      2,\n    )\n  },\n})\n",
      "virtual-agents/architect/skills/test-architecture/SKILL.md": textPayload(payload_opentest_virtual_agents_architect_skills_test_architecture_SKILL_md),
      "virtual-agents/architect/system.md": textPayload(payload_opentest_virtual_agents_architect_system_md),
      "virtual-agents/build/skills/test-implementation/SKILL.md": textPayload(payload_opentest_virtual_agents_build_skills_test_implementation_SKILL_md),
      "virtual-agents/build/system.md": textPayload(payload_opentest_virtual_agents_build_system_md),
      "virtual-agents/integrity/skills/test-review/SKILL.md": textPayload(payload_opentest_virtual_agents_integrity_skills_test_review_SKILL_md),
      "virtual-agents/integrity/system.md": textPayload(payload_opentest_virtual_agents_integrity_system_md),
      "virtual-agents/intent-analysis/skills/test-intent-analysis/SKILL.md": textPayload(payload_opentest_virtual_agents_intent_analysis_skills_test_intent_analysis_SKILL_md),
      "virtual-agents/intent-analysis/system.md": textPayload(payload_opentest_virtual_agents_intent_analysis_system_md),
      "virtual-agents/requirements/skills/test-requirements/SKILL.md": textPayload(payload_opentest_virtual_agents_requirements_skills_test_requirements_SKILL_md),
      "virtual-agents/requirements/system.md": textPayload(payload_opentest_virtual_agents_requirements_system_md),
      "virtual-agents/visual-qa/skills/visual-test-review/SKILL.md": textPayload(payload_opentest_virtual_agents_visual_qa_skills_visual_test_review_SKILL_md),
      "virtual-agents/visual-qa/system.md": textPayload(payload_opentest_virtual_agents_visual_qa_system_md),
    },
  },
]
