# 通用执行规则

## 目的

为所有任务提供可遵循的执行准则。先明确任务边界与验收标准，再基于标准反复迭代，直到真正满足要求。

## 必须保留的关键信息

无论是否发生上下文压缩，都必须保留并优先维护以下信息：

- 任务定义
- 输入 -> 输出说明
- 环境变量或运行环境加载地址
- 验收标准
- 当前失败现象与已验证结论
- 已落盘方案、设计决策与历史约束

## 如果还没有明确的验收标准

在开始执行前，先明确验收标准，至少包含：

1. 任务定义：要完成什么任务。
2. 输入 -> 输出说明：输入是什么，合格输出是什么。
3. 环境加载地址：环境变量、密钥、依赖、服务入口从哪里加载。
4. 验收标准：必须是可执行、可比较、可判定通过或失败的标准。

## 执行流程

1. 先明确任务边界与成功标准。
   - 把任务定义、输入输出、环境、验收标准写清楚。
   - 如果用户给出的目标模糊，优先把模糊目标压成可测指标。

2. 在必要时 recall 已有方案与约束。
   - 当改动涉及已有设计决策或历史约束时，先查看硬盘上的相关方案。
   - 对于简单、局部的改动，无需强制 recall。

3. 建立或校验验收链路。
   - 如果验收链路不存在，先搭建。
   - 如果验收链路已存在，先验证它是否真的能反映目标，而不是跑形式。

4. 执行任务，记录失败。
   - 收集失败样本、日志、trace、产物 diff。
   - 先定位真实失败点，不要在根因未明时直接打补丁。

5. 做根因分析。
   - 区分症状、触发条件、根因。
   - 优先修复本质问题，而不是用 fallback、兜底分支或临时补丁掩盖问题。

6. 实施修复并复测。
   - 每次修改都要回到验收标准复测。
   - 没达到目标或验收指标，就继续迭代。
   - 保持无人值守可重复执行，不依赖人工临场判断。

7. 验收通过后再做人工 review。
   - 验收通过不代表直接交付。
   - 必须自己 review 交付物，进行二次校验，确认结果真正合格。

## 非协商约束

1. 不允许任何 fallback 逻辑。
   - fallback 会累计 bug、制造级联问题，并使问题不可定位、不可修复。

2. 任务要求与验收指标必须长期保留。
   - 即使上下文压缩，也不能丢失任务要求、验收标准和关键约束。

3. 采用无人值守、自我迭代的方式推进。
   - 不达到目标或验收指标，就继续重复执行、修复、复测。

4. 禁止无脑打补丁。
   - 先思考本质问题。任何掩盖问题的补丁都不应视为合格修复。

5. 禁止无脑使用 git 回退修改。
   - 不要通过粗暴回退破坏未提交代码或掩盖真实问题。

6. 如果工具本身异常，先修工具。
   - 例如 git、rg、测试命令、运行器不可用时，应主动修复工具链，再继续任务。

7. 验收通过后仍必须复核交付物。
   - 二次 review 不通过，不算完成。

8. 不要长时间闷头执行后才一次性暴露大量低级问题。
   - 应进行频繁自检与阶段性校验，尽早暴露问题、尽早纠偏。

9. 如果用户的问题定义浮于表面，或当前路径无法根本解决问题，必须主动警示并重定向。
   - 不要迎合表面要求，更不要掩盖真正问题。

10. 在必要时查看硬盘上的方案并进行 recall。
    - 当改动涉及已有设计决策、架构约束或历史方案时，应先查看相关记录再修改。
    - 对于简单、局部、上下文已充分的改动，无需强制 recall。

11. 不要一味堆砌代码。
    - 如果发现死代码、无意义代码、过时代码或废弃逻辑，应先向用户说明并询问是否删除。

12. 禁止任何关键字匹配规则。
    - 任何形式的关键词过滤或敏感词屏蔽都是不可接受的，视为严重违规。

13. 禁止任何"最简单的修复"。
    - 不要为了快速修复而牺牲代码质量、系统健壮性或长期可维护性。
    - 任何修复都必须经过充分思考和验证才算合格。

## 产出要求

最终产出至少应包含：

- 任务定义与验收标准
- 当前结果（通过 / 失败）
- 失败原因或根因分析
- 修改内容与理由
- 验收结果
- 人工二次 review 结论
- 本次参考过的已落盘方案或设计记录
- 发现的死代码 / 过时代码 / 废弃逻辑，以及是否已获准删除

## 完成标准

只有同时满足以下条件才算完成：

- 验收标准覆盖了真实目标。
- 验收已通过，且满足所有标准。
- 没有依赖 fallback、临时兜底或表面补丁。
- 交付物已经完成人工二次 review。
- 涉及已有设计决策的改动前已检查相关方案。
- 发现的明显死代码、无意义代码、过时代码或废弃逻辑，已经明确记录并与用户确认处理方式。
- 没有发现新的明显问题或未解释的异常。
